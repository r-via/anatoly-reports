# Tests — Shard 1

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-core/src/replay.rs` | CRITICAL | 1 | 92% | [details](../reviews/rustguard-core-src-replay.rev.md) |
| `rustguard-core/src/handshake.rs` | NEEDS_REFACTOR | 16 | 82% | [details](../reviews/rustguard-core-src-handshake.rev.md) |
| `rustguard-core/src/messages.rs` | NEEDS_REFACTOR | 4 | 92% | [details](../reviews/rustguard-core-src-messages.rev.md) |
| `rustguard-cli/src/main.rs` | NEEDS_REFACTOR | 10 | 88% | [details](../reviews/rustguard-cli-src-main.rev.md) |
| `rustguard-core/src/timers.rs` | NEEDS_REFACTOR | 3 | 95% | [details](../reviews/rustguard-core-src-timers.rev.md) |
| `rustguard-core/src/cookie.rs` | NEEDS_REFACTOR | 6 | 92% | [details](../reviews/rustguard-core-src-cookie.rev.md) |
| `rustguard-core/src/session.rs` | NEEDS_REFACTOR | 1 | 90% | [details](../reviews/rustguard-core-src-session.rev.md) |

## Symbol Details

### `rustguard-core/src/replay.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `ReplayWindow` | L13–L19 | WEAK | 92% | [USED] Exported pub struct with 1 runtime importer (rustguard-core/src/sessio... |

### `rustguard-core/src/handshake.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `initial_chain_key` | L23–L25 | WEAK | 70% | [USED] Non-exported function called locally at lines 139 (create_initiation_w... |
| `initial_hash` | L27–L29 | WEAK | 70% | [USED] Non-exported function called locally at lines 140 (create_initiation_w... |
| `mix_hash` | L32–L34 | WEAK | 70% | [USED] Non-exported function called at 8+ locations in create_initiation_with... |
| `mix_key` | L38–L41 | WEAK | 70% | [USED] Non-exported function called at 6+ locations in create_initiation_with... |
| `encrypt_and_hash` | L44–L48 | WEAK | 70% | [USED] Non-exported function called at lines 153, 156 (create_initiation_with... |
| `decrypt_and_hash` | L51–L55 | WEAK | 72% | [USED] Non-exported function called at lines 316, 327 (process_initiation_wit... |
| `compute_mac1` | L59–L65 | WEAK | 80% | [USED] Exported function with 4 runtime importers per pre-computed analysis (... |
| `InitiatorHandshake` | L72–L81 | WEAK | 75% | [USED] Exported struct with 4 runtime importers per pre-computed analysis. Co... |
| `create_initiation` | L85–L91 | WEAK | 82% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `create_initiation_psk` | L95–L104 | WEAK | 80% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `create_initiation_with` | L108–L170 | WEAK | 80% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `process_response` | L173–L221 | WEAK | 82% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `process_initiation` | L227–L233 | WEAK | 82% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `process_initiation_psk` | L237–L246 | WEAK | 80% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `process_initiation_with` | L249–L357 | WEAK | 82% | [USED] Exported function with 4 runtime importers per pre-computed analysis. ... |
| `constant_time_eq` | L360–L363 | WEAK | 72% | [USED] Non-exported function called at line 308 in process_initiation_with fo... |

### `rustguard-core/src/messages.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Initiation` | L22–L29 | WEAK | 88% | [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie... |
| `Response` | L45–L52 | WEAK | 88% | [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie... |
| `Transport` | L64–L68 | WEAK | 88% | [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie... |
| `CookieReply` | L79–L83 | NONE | 92% | [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie... |

### `rustguard-cli/src/main.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `main` | L6–L28 | NONE | 85% | [USED] main() is the Rust program entry point, invoked by the runtime. It dis... |
| `usage` | L30–L42 | NONE | 85% | [USED] usage() called on lines 21 and 25 in main's match arms for unknown/mis... |
| `cmd_up` | L44–L66 | NONE | 85% | [USED] cmd_up() invoked on line 13 when 'up' command is matched in main's com... |
| `cmd_serve` | L68–L152 | NONE | 88% | [USED] cmd_serve() invoked on line 14 when 'serve' command is matched in main... |
| `cmd_join` | L154–L199 | NONE | 88% | [USED] cmd_join() invoked on line 15 when 'join' command is matched in main's... |
| `cmd_open` | L201–L214 | NONE | 85% | [USED] cmd_open() invoked on line 16 when 'open' command is matched in main's... |
| `cmd_close` | L216–L224 | NONE | 85% | [USED] cmd_close() invoked on line 17 when 'close' command is matched in main... |
| `cmd_status` | L226–L234 | NONE | 85% | [USED] cmd_status() invoked on line 18 when 'status' command is matched in ma... |
| `cmd_genkey` | L236–L240 | NONE | 85% | [USED] cmd_genkey() invoked on line 19 when 'genkey' command is matched in ma... |
| `cmd_pubkey` | L242–L255 | NONE | 85% | [USED] cmd_pubkey() invoked on line 20 when 'pubkey' command is matched in ma... |

### `rustguard-core/src/timers.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `elapsed_since` | L21–L23 | NONE | 85% | [USED] no_std feature version, used in same call sites as std version; kernel... |
| `elapsed_since` | L26–L30 | NONE | 85% | [USED] no_std feature version, used in same call sites as std version; kernel... |
| `SessionTimers` | L57–L68 | WEAK | 92% | [USED] Exported pub struct with 1 runtime importer (rustguard-daemon/src/peer... |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `CookieChecker` | L48–L57 | WEAK | 85% | [USED] Primary server-side public API in library crate; exports cookie genera... |
| `CookieState` | L60–L65 | WEAK | 88% | [USED] Primary client-side public API in library crate; exports cookie decryp... |
| `encode_addr` | L227–L243 | WEAK | 85% | [USED] Called at lines 138 and 153 to encode socket addresses for cookie MAC ... |
| `random_bytes` | L245–L249 | WEAK | 88% | [USED] Called at line 93 in maybe_rotate_secret to generate cryptographically... |
| `random_nonce` | L251–L255 | WEAK | 85% | [USED] Called at line 121 in create_reply_from_bytes to generate random nonce... |
| `constant_time_eq_16` | L273–L279 | WEAK | 88% | [USED] Called at lines 131 and 147 for constant-time comparison of MAC1 and M... |

### `rustguard-core/src/session.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `TransportSession` | L13–L30 | WEAK | 90% | [USED] Exported pub struct with 3 runtime importers across multiple workspace... |

## Test Improvements

- [ ] `rustguard-core/src/replay.rs` — 1 weak
  Improve `rustguard-core/src/replay.test.rs` covering: ReplayWindow
  - ReplayWindow: [USED] Exported pub struct with 1 runtime importer (rustguard-core/src/session.rs) per pre-computed analysis | [UNIQUE] No RAG data available; struct implements WireGuard anti-replay sliding window algorithm (RFC 6479) | [ERROR] shift_window (lines 109–116) applies the sub-word bit-shift in the WRONG direction with the WRONG iteration order, silently dropping bits for recently-seen counters and breaking replay protection. The bitmap stores age-0 at bit 0 of bitmap[0] and older counters at progressively higher bit/word indices. When top advances by 'shift', every existing bit must move to a HIGHER index — a LEFT-SHIFT of the 2048-bit array (bitmap[0] is the least-significant 64 bits). The correct loop iterates forward (low→high index), left-shifts each word by bit_shift, and propagates overflow with `new_carry = *word >> (64 - bit_shift)`. The current code does the opposite: it iterates in reverse (.rev()), RIGHT-shifts each word (`*word >> bit_shift`), and discards the carry that overflows out of bitmap[0]. Concrete impact: after check_and_update(0) then check_and_update(5), counter 0's bit is erased; a subsequent check(0) returns true, accepting the replayed packet. All existing tests pass despite this bug because none of them attempt to replay a previously-accepted counter after the window has been advanced forward. | [LEAN] Minimal struct with exactly two fields needed by the RFC 6479 / WireGuard sliding-window algorithm: the high watermark and the seen-counter bitmap. No generics, no trait objects, no builder pattern. The struct is the direct mechanical translation of the protocol data structure. | [WEAK] Per prior deliberation (Known False Positives), WEAK is the established classification. Nine inline tests cover the main behavioral contract (sequential acceptance, duplicate rejection, out-of-order within window, boundary conditions, large-jump reset, stress paths) exclusively through check_and_update(). The public check() and update() methods are never tested in isolation. More critically, the shift_window sub-word bit-shift direction is inverted (right-shift moves bits toward lower ages instead of higher), causing replayed counters to be silently accepted after a same-word shift; none of the tests re-check a previously-marked counter immediately after a non-word-boundary shift has occurred, so this correctness bug goes undetected. The struct is imported by session.rs in critical packet-handling paths, raising the stakes of these gaps. | [PARTIAL] Public struct. Both fields carry `///` doc comments (L15 `top`, L17 `bitmap`), and all three public methods (`check`, `update`, `check_and_update`) have `///` doc comments. However, the struct-level type definition itself has no `///` doc comment, the `new()` constructor has no doc comment, and no `# Examples` section appears on any method. This is clearly partial, not absent. Previously reclassified by deliberation from UNDOCUMENTED to PARTIAL. (deliberated: confirmed — ERROR confirmed with high confidence by manual trace. The sub-word bit-shift in shift_window (L112-116) right-shifts each word and propagates carry from high→low via .rev(), but the bitmap encodes age as j*64+i so aging requires left-shift with low→high carry. Concrete trace: after check_and_update(0) then check_and_update(2), counter 0's bit (bitmap[0] bit 0) is erased by the right-shift (1>>2=0), and a replay of counter 0 would incorrectly succeed. WEAK tests confirmed: all nine tests only assert forward acceptance of new counters; none re-check a previously-accepted counter after a non-word-aligned shift, so the bit-erasure goes undetected. PARTIAL documentation confirmed: fields and public methods have /// doc comments, but the struct-level type definition and new() constructor lack them.)

- [ ] `rustguard-core/src/handshake.rs` — 16 weak
  Improve `rustguard-core/src/handshake.test.rs` covering: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, InitiatorHandshake, create_initiation, create_initiation_psk, create_initiation_with, process_response, process_initiation, process_initiation_psk, process_initiation_with, constant_time_eq
  - initial_chain_key: [USED] Non-exported function called locally at lines 139 (create_initiation_with) and 303 (process_initiation_with). Core utility for protocol initialization. | [UNIQUE] Trivial one-liner wrapper around crypto::hash with a constant. No similar functions found. | [OK] *(axis crashed — see transcript)* | [LEAN] Single-expression named primitive for the Noise protocol's initial chain key. Naming this rather than inlining it makes the handshake flow read like the spec. Minimal and purposeful. | [WEAK] Private helper exercised only indirectly through the full handshake flow. No direct unit test validates its output against a known-good constant derived from the Noise protocol spec. The five inline tests all go through it but never assert on the intermediate ck value. | [PARTIAL] Has two-line /// block ('Precomputed initial handshake state from the protocol constants. These are the same for every handshake — compute once.'). Private function; no param docs needed (no params), return type not described, no # Examples. Tolerated under private-item leniency but doc is present.
  - initial_hash: [USED] Non-exported function called locally at lines 140 (create_initiation_with) and 304 (process_initiation_with). Essential for hash initialization. | [UNIQUE] Trivial function hashing chaining key and identifier. No duplicate functions in RAG results. | [OK] *(axis crashed — see transcript)* | [LEAN] Same pattern as initial_chain_key — names one Noise protocol step. No abstraction overhead; the name directly maps to the WireGuard RFC step. | [WEAK] Private helper tested only indirectly via full handshake tests. No test verifies its output against a known-good reference vector, which would be important for interoperability with the WireGuard spec. | [UNDOCUMENTED] No /// doc comment present. The preceding doc block belongs to initial_chain_key. Private function, so tolerated under leniency rules, but the absence is total.
  - mix_hash: [USED] Non-exported function called at 8+ locations in create_initiation_with (lines 143, 146, 154, 157) and process_initiation_with (lines 306, 310, 320, 325, 337, 342). Core Noise pattern operation. | [UNIQUE] Trivial hash combining wrapper. No duplicate functions in RAG results. | [OK] *(axis crashed — see transcript)* | [LEAN] Called ~10 times across both initiator and responder paths. Naming this Noise operation keeps the top-level functions readable and mirrors protocol documentation vocabulary. | [WEAK] Private helper exercised implicitly through every handshake test but never directly. No test checks output determinism, commutativity concerns, or reference spec values. | [PARTIAL] Has single-line /// ('Mix a public key into the hash. Used to bind the responder's identity.'). Description is slightly misleading — the function mixes arbitrary &[u8], not just public keys. No param descriptions, no return description, no # Examples. Private-item leniency applies.
  - mix_key: [USED] Non-exported function called at 6+ locations in create_initiation_with (lines 150, 155) and process_initiation_with (lines 314, 318, 329, 333). Essential for DH result mixing. | [UNIQUE] Structurally similar to kmod version (score 0.963) but fundamentally different: rustguard-core uses SharedSecret wrapper (userspace, x25519-dalek) while kmod uses raw [u8;32] (kernel, C FFI). Different compilation targets with different type systems. Per known false positives, cross-target deduplication is architecturally infeasible. | [OK] *(axis crashed — see transcript)* | [LEAN] Wraps crypto::hkdf with a domain-specific return type (new_ck, key), discarding the unused third output. Called repeatedly; the named abstraction matches the Noise spec step name exactly. | [WEAK] Private helper indirectly covered by all five inline tests but never tested in isolation. The returned (ck, key) pair is critical for key derivation correctness; no test asserts intermediate key material values. | [PARTIAL] Has two-line /// block describing purpose and the return tuple semantics ('Returns (new_ck, key) where key is for AEAD encryption.'). No param-level docs, no # Examples. Private function; leniency applies but return description is genuinely helpful.
  - encrypt_and_hash: [USED] Non-exported function called at lines 153, 156 (create_initiation_with) and 341 (process_initiation_with). Core AEAD encryption with hash threading. | [UNIQUE] Similar to kmod version (score 0.891) but different semantic contracts: core returns heap-allocated Vec<u8>, kmod uses buffer parameter and returns size. Different memory models (alloc vs stack) and Option vs direct return indicate separate design decisions, not accidental duplication. | [OK] *(axis crashed — see transcript)* | [LEAN] Canonical Noise protocol operation: AEAD encrypt then mix ciphertext into hash. Used in both initiator and responder paths. Complexity is protocol-mandated, not invented. | [WEAK] Private helper only exercised indirectly through create_initiation_with and process_initiation_with. The tampered_initiation_rejected test exercises a failure path but via MAC1 failing first, not AEAD. No direct test of encrypt-then-mix semantics. | [PARTIAL] Has single-line /// ('Encrypt-and-hash: AEAD encrypt, then mix ciphertext into hash.'). Describes the two-step operation but omits parameter semantics, return tuple layout, and # Examples. Private function; leniency applies.
  - decrypt_and_hash: [USED] Non-exported function called at lines 316, 327 (process_initiation_with) and 211 (process_response). Core AEAD decryption with hash threading. | [UNIQUE] Similar to kmod version (score 0.900) but different return contracts: core returns Vec<u8>, kmod uses mutable buffer and returns size. Different memory management strategies reflect architectural separation between userspace and kernel module. | [OK] *(axis crashed — see transcript)* | [LEAN] Mirror of encrypt_and_hash for the decryption direction. Returns Option for authentication failures. Both operations are fundamental Noise primitives — extracting them is appropriate. | [WEAK] Private helper with some implicit coverage: tampered_initiation_rejected exercises a rejection path (though MAC1 fails first), and tampered_response_rejected exercises the None path via AEAD failure. Happy path covered indirectly. No standalone unit test or reference-vector test. | [PARTIAL] Has single-line /// ('Decrypt-and-hash: AEAD decrypt, then mix ciphertext into hash.'). Does not describe the Option return (authentication failure vs success), parameter roles, or include # Examples. Private function; leniency applies.
  - compute_mac1: [USED] Exported function with 4 runtime importers per pre-computed analysis (integration.rs, tunnel.rs, client.rs, server.rs). WireGuard MAC1 computation. | [UNIQUE] Very similar to kmod version (score 0.960) but different parameter type: core takes PublicKey wrapper (userspace abstraction), kmod takes raw [u8;32]. Per known false positives, these are intentional parallel implementations for different compilation targets with incompatible type systems. | [OK] *(axis crashed — see transcript)* | [LEAN] WireGuard-specific MAC1 computation per the RFC. The implementation is direct: hash the key material, truncate to 16 bytes. No unnecessary generalization; used by both sides. | [WEAK] Widely imported by daemon, enroll client/server, and integration tests. Exercised indirectly: wrong_responder_key_rejects and tampered_initiation_rejected both cause MAC1 verification to fail, validating the check path. However, no direct unit test asserts that compute_mac1 produces a specific known-correct 16-byte output for a given input, which is critical for WireGuard interoperability. | [PARTIAL] Public function with two-line /// block giving purpose and the exact MAC1 formula ('BLAKE2s-MAC(HASH(LABEL_MAC1 || responder_public), msg_bytes)'). Cryptographic algorithm is well explained. Missing param-level docs, no # Examples section, and no note about the 16-byte truncation from a full BLAKE2s output.
  - InitiatorHandshake: [USED] Exported struct with 4 runtime importers per pre-computed analysis. Core state type for initiator handshake flow. | [UNIQUE] Struct definition with Zeroize derives. No struct duplication candidates in RAG results. This is a type-level construct specific to userspace Noise handshake state management. | [OK] *(axis crashed — see transcript)* | [LEAN] Necessary state container bridging the two handshake messages. ZeroizeOnDrop is a security requirement (not overengineering) for key material. The #[zeroize(skip)] annotations on non-sensitive fields show deliberate, minimal application of the derive macro. | [WEAK] Struct with ZeroizeOnDrop runtime behavior. Constructed and consumed in all five inline tests. However, the zeroize-on-drop behavior is never tested (no test verifies key material is wiped after drop), and the PSK field is always set to all-zeros in existing tests — the non-zero PSK case is untested. | [PARTIAL] Public struct with two-line /// block covering purpose and zeroize-on-drop security property. All fields are private so field-level docs are not required. However, there is no # Examples section and no guidance on how consumers obtain or use this type (it is opaque output from create_initiation_with). Reasonable coverage for a non-constructable public type.
  - create_initiation: [USED] Exported function with 4 runtime importers per pre-computed analysis. std convenience wrapper for handshake initiation creation. | [UNIQUE] Std-feature-gated convenience wrapper that delegates to create_initiation_psk with zero PSK. This is intentional API layering, not duplication. Different contract (no PSK param) from the function it calls. | [OK] *(axis crashed — see transcript)* | [LEAN] Thin std convenience wrapper for the common non-PSK case. In WireGuard, PSK is optional (zero-key default is spec-defined), so offering a named no-PSK entry point is ergonomic and avoids caller confusion about what to pass. One line of delegation. | [WEAK] Std convenience wrapper calling create_initiation_psk with zero PSK. Invoked in all five inline tests covering happy path and error paths. Missing: no test of the non-zero PSK path (covered by the PSK variant), and no cross-implementation interoperability vector. | [PARTIAL] Public std-gated function with single-line /// ('Create a handshake initiation message (std convenience — uses OsRng and system clock).'). Notes the randomness source but omits parameter descriptions, return tuple semantics, and # Examples. Delegates to create_initiation_psk but that relationship is undocumented.
  - create_initiation_psk: [USED] Exported function with 4 runtime importers per pre-computed analysis. std convenience wrapper for PSK-enabled initiations. | [UNIQUE] Std-feature-gated convenience wrapper that delegates to create_initiation_with after generating ephemeral and timestamp. Intentional API layering, not duplication. Differs semantically from create_initiation_with which requires explicit ephemeral/timestamp. | [OK] *(axis crashed — see transcript)* | [ACCEPTABLE] Three-layer API (create_initiation → create_initiation_psk → create_initiation_with) adds a thin middle layer. The layering is justified: the _with variant must be no_std-compatible (no RNG, no clock), so std wrappers that inject randomness and timestamp are necessary. However, create_initiation could have been merged into this function with a default PSK parameter, slightly reducing the layer count. Complexity is mild and justified. | [WEAK] Called indirectly in every test via create_initiation, which delegates to it with a zero PSK. The non-zero PSK code path through this function is never tested. No test verifies that a matching non-zero PSK on both sides produces a working session, or that a mismatched PSK causes failure. | [PARTIAL] Public std-gated function with single-line /// ('Create a handshake initiation with PSK (std convenience).'). Does not document the psk parameter semantics (all-zeros = no PSK), the ephemeral source, or the return tuple. No # Examples. PARTIAL.
  - create_initiation_with: [USED] Exported function with 4 runtime importers per pre-computed analysis. Core initiator handshake implementation, no_std compatible. | [UNIQUE] Core implementation (score 0.891 vs kmod) but operates on different types (StaticSecret/PublicKey wrappers vs raw arrays). Rustguard-core (userspace, x25519-dalek) vs rustguard-kmod (kernel, C FFI) are fundamentally different compilation targets. Type differences reflect architectural separation. | [OK] *(axis crashed — see transcript)* | [LEAN] Core Noise_IKpsk2 initiator implementation. Every line maps to a named step in the WireGuard RFC. The explicit ephemeral and timestamp parameters enable deterministic testing (see test suite). Length is justified by protocol complexity, not accidental complexity. | [WEAK] Core initiation builder tested indirectly through the std convenience functions in all five inline tests. The explicit ephemeral/timestamp parameters (the no_std path) are never exercised directly. No test uses create_initiation_with directly to validate deterministic output for a fixed ephemeral and timestamp. | [PARTIAL] Public core function with two-line /// block ('Create a handshake initiation with explicit ephemeral key and timestamp. This is the core function — usable from both std and no_std.'). The no_std compatibility note is valuable. However, no param-level docs (psk semantics, sender_index role, ephemeral ownership), no return description, and no # Examples on the most critical public API in this module.
  - process_response: [USED] Exported function with 4 runtime importers per pre-computed analysis. Processes handshake response and derives transport keys. | [UNIQUE] Similar to kmod version (score 0.837) but operates on different types: core uses InitiatorHandshake/PublicKey/Response (userspace abstractions), kmod uses raw byte arrays. Different compilation targets with incompatible type systems. | [OK] *(axis crashed — see transcript)* | [LEAN] Implements the initiator's response-processing path verbatim from the Noise spec: verify index, two DH rounds, PSK phase, decrypt-and-hash, derive transport keys. No unnecessary abstractions; protocol fidelity requires this structure. | [WEAK] Covered by three tests: full_handshake_and_transport (happy path with bidirectional transport), tampered_response_rejected (AEAD failure), and mismatched_receiver_index_rejected (index check). Missing: PSK mismatch scenario (only zero PSK used), and no test exercises the DH failure path where initiator ephemeral is wrong. | [PARTIAL] Public function with single-line /// ('Process a handshake response and derive transport keys.'). Does not document the Option failure modes (MAC failure, index mismatch, AEAD failure), parameter roles (state is consumed), or include # Examples. The consumed-state ownership semantic is particularly important for callers.
  - process_initiation: [USED] Exported function with 4 runtime importers per pre-computed analysis. std convenience for responder-side initiation processing. | [UNIQUE] Std-feature-gated convenience wrapper that delegates to process_initiation_psk with zero PSK and None timestamp. Intentional API layering. Different contract (fewer params) from the function it calls. | [OK] *(axis crashed — see transcript)* | [LEAN] Convenience wrapper mirroring create_initiation. One line delegating to process_initiation_psk with zero PSK. Provides the simple, no-PSK entry point symmetrically to the initiator side. | [WEAK] Std convenience wrapper, invoked in all five inline tests. Delegates to process_initiation_psk with zero PSK and no last_timestamp. The timestamp replay protection (last_timestamp Some variant) is never tested. Non-zero PSK path is untested. | [PARTIAL] Public std-gated function with single-line /// ('Process a handshake initiation on the responder side (std convenience).'). Return tuple of four elements (initiator public key, timestamp, response, session) is completely undocumented, as are failure modes (MAC1 check, AEAD decryption). No # Examples.
  - process_initiation_psk: [USED] Exported function with 4 runtime importers per pre-computed analysis. std convenience for PSK-enabled responder processing. | [UNIQUE] Std-feature-gated convenience wrapper that delegates to process_initiation_with after generating responder ephemeral. Intentional API layering. Differs semantically from process_initiation_with which requires explicit ephemeral. | [OK] *(axis crashed — see transcript)* | [ACCEPTABLE] Same three-layer concern as create_initiation_psk. The middle layer generates the responder ephemeral and delegates to process_initiation_with. Justified by the no_std/std split and testability requirements, but the boundary between this layer and the top layer is thin. Mild redundancy. | [WEAK] Called indirectly via process_initiation in all tests, always with zero PSK and None for last_timestamp. The non-zero PSK path and the timestamp replay prevention logic (last_timestamp Some with stale vs fresh timestamps) are entirely untested. | [PARTIAL] Public std-gated function with single-line /// ('Process a handshake initiation with PSK (std convenience).'). The last_timestamp anti-replay parameter is security-critical but entirely undocumented. No description of PSK semantics (all-zeros = no PSK), return tuple, or failure conditions. No # Examples.
  - process_initiation_with: [USED] Exported function with 4 runtime importers per pre-computed analysis. Core responder handshake implementation, no_std compatible. | [UNIQUE] Core implementation (score 0.864 vs kmod) but operates on different types (Initiation/PublicKey/Response vs raw arrays). Rustguard-core uses userspace type abstractions while kmod uses kernel-compatible C FFI types. Fundamentally different compilation targets. | [OK] *(axis crashed — see transcript)* | [LEAN] Longest function in the file but appropriately so: it validates MAC1, runs the full Noise handshake in responder role, validates the timestamp against replay, builds and MACs the response, and derives transport keys. Every section is a mandatory RFC step. No spurious abstraction layers. | [WEAK] Core responder function with solid coverage across the inline tests: MAC1 failure (wrong_responder_key_rejects, tampered_initiation_rejected), AEAD decryption failure (tampered_initiation_rejected triggers MAC1 first), and happy path. Key gaps: timestamp replay protection (last_timestamp Some branch) is never tested, non-zero PSK matching/mismatching is untested, and MAC2/cookie path is explicitly marked TODO with no coverage. | [PARTIAL] Public no_std-compatible function with single-line /// ('Process a handshake initiation with explicit ephemeral (no_std compatible).'). This is the most complex function in the module (109 lines) but has the least documentation relative to its complexity. The last_timestamp anti-replay requirement, MAC1 pre-check behavior, and four-element return tuple are all undocumented. No # Examples.
  - constant_time_eq: [USED] Non-exported function called at line 308 in process_initiation_with for constant-time MAC1 comparison. Security-critical utility. | [UNIQUE] Similar to kmod versions (scores 0.853, 0.852) but different parameter types and implementations: core takes fixed [u8;16] arrays and uses safe subtle crate API, kmod takes generic slices and uses unsafe C FFI (wg_crypto_memneq). Different compilation contexts (userspace vs kernel) with incompatible underlying crypto implementations. | [OK] *(axis crashed — see transcript)* | [LEAN] Minimal private helper wrapping subtle::ConstantTimeEq. The named function makes the security intent explicit at the call site in process_initiation_with and localises the subtle import. Not NIH — subtle is the correct, installed crate for this. Three lines total, no unnecessary generalization. | [WEAK] Private helper exercised indirectly: wrong_responder_key_rejects and tampered_initiation_rejected both trigger the MAC1 verification failure path through constant_time_eq. No direct unit test confirms constant-time semantics or validates the function against known equal/unequal inputs. | [PARTIAL] Private function with single-line /// ('Constant-time comparison for MACs using the `subtle` crate.'). Mentions the crate and use-case. No param descriptions or # Examples, but this is a private 3-line helper — private-item leniency applies. The security-critical nature (constant-time) is clearly documented.

- [ ] `rustguard-core/src/messages.rs` — 1 untested, 3 weak
  Improve `rustguard-core/src/messages.test.rs` covering: Initiation, Response, Transport, CookieReply
  - Initiation: [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie.rs, rustguard-enroll/src/client.rs, rustguard-enroll/src/server.rs). Core handshake message type. | [UNIQUE] No RAG similarity data available; marking as unique per protocol. | [OK] Field types and sizes are consistent with the spec: ephemeral(32), encrypted_static(48=32+16tag), encrypted_timestamp(28=12+16tag), mac1(16), mac2(16). to_bytes and from_bytes byte ranges are exact inverses: [0..4] type, [4..8] sender, [8..40] ephemeral, [40..88] static_enc, [88..116] timestamp, [116..132] mac1, [132..148] mac2, totalling 148 bytes. | [LEAN] Plain data struct whose fields map 1:1 to the WireGuard handshake initiation wire layout. No generics, no inheritance, no factory. Fixed-size arrays enforce protocol field widths at the type level, which is idiomatic and appropriate. | [WEAK] initiation_roundtrip tests both to_bytes() and from_bytes() and verifies the type byte, sender_index, and ephemeral fields. However, encrypted_static, encrypted_timestamp, mac1, and mac2 are never asserted after the roundtrip. Zero edge cases tested (e.g., boundary sender_index values). Critical crypto fields go unverified. | [PARTIAL] Struct has a thorough /// doc block (lines 10–20) with a text-diagram wire layout covering all six fields by name, byte size, and purpose. However, individual public fields (sender_index, ephemeral, encrypted_static, encrypted_timestamp, mac1, mac2) carry no per-field /// comments, and no # Examples section is provided for this public API type. (deliberated: confirmed — Tests WEAK is fair: initiation_roundtrip only asserts sender_index and ephemeral, leaving 4 of 6 fields unverified after roundtrip. Although the copy_from_slice pattern is mechanical and an off-by-one would likely corrupt adjacent fields too, explicit assertions on all fields is best practice for protocol wire formats. Documentation PARTIAL is correct: the struct has an excellent wire-layout diagram but individual public fields lack per-field /// comments. Given the diagram already describes each field, this is low-severity.)
  - Response: [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie.rs, rustguard-enroll/src/client.rs, rustguard-enroll/src/server.rs). Core handshake message type. | [UNIQUE] No RAG similarity data available; marking as unique per protocol. | [OK] Field types match spec: ephemeral(32), encrypted_empty(16=0+16tag), mac1(16), mac2(16). to_bytes byte ranges: [0..4] type, [4..8] sender, [8..12] receiver, [12..44] ephemeral, [44..60] encrypted_empty, [60..76] mac1, [76..92] mac2. from_bytes is an exact inverse. Total 92 bytes. | [LEAN] Plain data struct matching the WireGuard handshake response wire layout exactly. Same reasoning as Initiation — minimal and spec-faithful. | [WEAK] response_roundtrip tests both to_bytes() and from_bytes() and verifies the type byte, sender_index, and receiver_index. However, ephemeral, encrypted_empty, mac1, and mac2 are never asserted after the roundtrip, leaving 4 of 6 fields unverified. | [PARTIAL] Struct has a thorough /// doc block (lines 33–43) with a wire layout diagram. Individual public fields (sender_index, receiver_index, ephemeral, encrypted_empty, mac1, mac2) have no per-field /// comments, and no # Examples section is provided for this public type. (deliberated: confirmed — Tests WEAK: same pattern as Initiation — response_roundtrip verifies sender_index and receiver_index but not ephemeral, encrypted_empty, mac1, or mac2 (4 of 6 fields unverified). Documentation PARTIAL: thorough struct-level wire diagram exists, but no per-field /// comments. Both findings are correct and consistent with Initiation's assessment.)
  - Transport: [USED] Exported struct imported by 3 runtime files (rustguard-core/src/cookie.rs, rustguard-enroll/src/client.rs, rustguard-enroll/src/server.rs). Core transport data message type. | [UNIQUE] No RAG similarity data available; marking as unique per protocol. | [OK] to_bytes correctly allocates TRANSPORT_HEADER_SIZE+payload capacity and writes type(4)+receiver(4)+counter(8)+payload. from_bytes guards buf.len() < 16 before indexing, correctly parses receiver from [4..8] and counter from [8..16], and slices payload from [16..]. All offset arithmetic is correct. | [LEAN] Variable-length payload correctly uses Vec<u8> since transport frames have no fixed payload bound. Struct fields match the WireGuard data message layout. No unnecessary indirection. | [WEAK] transport_roundtrip covers the happy-path roundtrip, asserting receiver_index, counter, and payload.len(). Gaps: payload byte content is not checked; the short-buffer path (buf.len() < TRANSPORT_HEADER_SIZE returning None) is never exercised; empty payload and maximum-size payloads are untested. | [PARTIAL] Struct has a /// doc block (lines 56–63) with a wire layout diagram covering receiver_index, counter, and payload by name and purpose. Individual public fields carry no per-field /// comments and no # Examples section is present. Also notably lacks #[derive(Clone)] unlike the other message structs — not a documentation issue but may affect usability. (deliberated: confirmed — Tests WEAK is well-justified: transport_roundtrip doesn't verify payload byte content (only length), and critically the short-buffer guard path (buf.len() < TRANSPORT_HEADER_SIZE returning None) has zero coverage. This is actual branching logic unlike the fixed-size structs. Documentation PARTIAL: wire diagram exists at struct level but no per-field docs and notably missing #[derive(Clone)] unlike peers — not a doc issue but noted in the finding.)

- [ ] `rustguard-cli/src/main.rs` — 10 untested
  Create `rustguard-cli/src/main.test.rs` covering: main, usage, cmd_up, cmd_serve, cmd_join, cmd_open, cmd_close, cmd_status, cmd_genkey, cmd_pubkey

- [ ] `rustguard-core/src/timers.rs` — 2 untested, 1 weak
  Improve `rustguard-core/src/timers.test.rs` covering: elapsed_since, elapsed_since, SessionTimers
  - SessionTimers: [USED] Exported pub struct with 1 runtime importer (rustguard-daemon/src/peer.rs); core API for managing per-peer session lifecycle state in timer module. | [UNIQUE] Unique struct managing peer session lifecycle timers with five distinct timestamp and state fields | [NEEDS_FIX] needs_keepalive (L154–L168) contains a self-contradictory guard when last_sent is None. The fallback `sent.unwrap_or(received)` sets last_send_time = received, making the combined condition `elapsed_since(received) >= interval && elapsed_since(received) < interval` identically false for every value of elapsed time. Consequently, if a peer sends data to us but we have never transmitted in this session (last_sent = None), needs_keepalive can never return true regardless of the keepalive interval, silently suppressing the keepalive that should acknowledge the peer. | [LEAN] Five fields that map directly to the distinct time-tracking concerns of a WireGuard peer (session establishment, handshake sent, last RX, last TX, rekey flag). The dual std/no_std method pairs (e.g. session_started / session_started_at) are justified by the live kernel module crate in rustguard-kmod/. All logic methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake) implement exactly the checks described in the whitepaper with no speculative generalization. | [WEAK] Six inline tests cover new(), session_started(), is_expired() for no-session and message-count cases, needs_rekey() for message-count trigger, needs_keepalive() returning false only (None and zero-interval), and should_retry_handshake() when never sent. Untested: handshake_timed_out() entirely, packet_sent() and packet_received() directly, needs_keepalive() returning true, should_retry_handshake() after a handshake was sent, rekey_requested=true short-circuit in needs_rekey(), all time-based expiry/dead/rekey paths, and all no_std variants (session_started_at, packet_sent_at, packet_received_at). | [DOCUMENTED] Struct-level /// doc comment present ('Timer state for a peer's session lifecycle.') and all five public fields carry individual /// doc comments. Public methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) all have /// comments. Per deliberation ruling, DOCUMENTED classification is confirmed.

- [ ] `rustguard-core/src/cookie.rs` — 6 weak
  Improve `rustguard-core/src/cookie.test.rs` covering: CookieChecker, CookieState, encode_addr, random_bytes, random_nonce, constant_time_eq_16
  - CookieChecker: [USED] Primary server-side public API in library crate; exports cookie generation and MAC2 verification methods | [UNIQUE] Struct for server-side cookie generation and MAC2 validation; no similar structs found | [OK] Struct fields are well-typed. The impl methods (new, new_with, maybe_rotate_secret, make_cookie_from_bytes, create_reply_from_bytes, verify_mac1, verify_mac2_from_bytes) are logically consistent with the WireGuard cookie spec. new_with provides a proper no_std initialization path with an externally-supplied timestamp. | [LEAN] Exactly the fields the WireGuard cookie responder requires: public key, rotating secret, generation timestamp, and load flag. No unnecessary generics or patterns. | [WEAK] Per known false-positive precedent, reclassified WEAK. Four tests exercise create_reply and verify_mac2 (happy path, wrong source, wrong mac1). However, verify_mac1 is never called in any test, new_with constructor is untested, and secret rotation via maybe_rotate_secret is not exercised. | [PARTIAL] Struct carries a /// summary ('Server-side cookie state: generates cookies and validates MAC2.') and all four fields have individual /// doc comments. Public impl methods (create_reply_from_bytes, verify_mac1, verify_mac2_from_bytes, etc.) lack # Examples sections. Consistent with prior deliberation reclassification to PARTIAL. (deliberated: confirmed — Tests WEAK confirmed: verify_mac1 has zero test coverage, new_with is untested, and maybe_rotate_secret (secret rotation) is never exercised. These are meaningful gaps in a security-critical struct. Documentation PARTIAL confirmed: struct and fields have doc comments, but public methods lack # Examples sections, which is standard for Rust library crates with public API.)
  - CookieState: [USED] Primary client-side public API in library crate; exports cookie decryption and MAC2 computation methods | [UNIQUE] Struct for client-side cookie storage and freshness tracking; no similar structs found | [NEEDS_FIX] In no_std builds, process_reply (L195-L196) sets self.received = Some(now()) only under #[cfg(feature = "std")]. There is no #[cfg(not(feature = "std"))] block to set self.received to any sentinel value. Consequently self.received stays None after a successful cookie decryption, and is_fresh() (L218-L221) returns false for None, so compute_mac2 always returns [0u8; 16] regardless of a valid stored cookie. The entire client-side cookie mechanism is silently non-functional in no_std builds. Unlike CookieChecker, which provides new_with() for no_std callers, CookieState has no equivalent escape hatch. | [LEAN] Minimal client-side state: an optional cookie bytes and an optional receive timestamp. Matches the protocol requirement precisely. | [WEAK] Per known false-positive precedent, reclassified WEAK. process_reply success and failure, compute_mac2 with/without cookie are all covered. However, the stale-cookie path (is_fresh returning false for an old timestamp) is not tested, and the no_std silent-zero behaviour (received is never set) has no test. | [PARTIAL] Struct has '/// Client-side cookie state: stores a received cookie for MAC2.' and both private fields carry /// comments. Public methods process_reply and compute_mac2 have doc comments but no # Examples. Consistent with prior deliberation reclassification to PARTIAL. (deliberated: confirmed — Correction NEEDS_FIX confirmed at higher confidence. In no_std builds, process_reply successfully decrypts and stores the cookie (self.cookie = Some(cookie)) but never sets self.received because the `#[cfg(feature = "std")]` gate on line 196 has no no_std counterpart. Since is_fresh() returns false for None, compute_mac2 always returns zeros even with a valid stored cookie. This is a real silent-failure bug: elapsed_since already returns Duration::ZERO in no_std (which would pass the freshness check), so setting received to any Timestamp value would fix it. Tests WEAK confirmed: stale-cookie path and the no_std zero-mac2 behavior are both untested. Documentation PARTIAL confirmed: struct/field docs present but methods lack examples.)
  - encode_addr: [USED] Called at lines 138 and 153 to encode socket addresses for cookie MAC key derivation | [UNIQUE] Encodes IPv4/IPv6 socket addresses to byte sequences for MAC computation; no similar functions found | [OK] IPv4 and IPv6 branches both encode IP octets followed by port in big-endian order, matching the documented encoding format. Previously deliberated and reclassified to OK. | [LEAN] Straightforward V4/V6 branch encoding IP+port into bytes. Using Vec<u8> is idiomatic under std. A fixed-size array approach would be a micro-optimization with no clarity benefit here. | [WEAK] Private helper, tested only indirectly via create_reply and verify_mac2. All five tests use an IPv4 SocketAddr (10.0.0.1:12345), so the IPv6 branch (SocketAddr::V6) is completely untested. | [PARTIAL] Private cfg(std) function with a single-line /// comment 'Encode a socket address to bytes for cookie MAC.' Doc is present but omits the encoding format (IPv4: 4+2=6 bytes; IPv6: 16+2=18 bytes), which is only visible in a comment on the caller. No param or return docs, but private-item leniency applies. (deliberated: confirmed — Tests WEAK confirmed: all five tests use IPv4 (10.0.0.1:12345), so the IPv6 branch is completely untested. For a protocol implementation, this is a meaningful coverage gap. Documentation PARTIAL confirmed: private function has a one-line doc comment but doesn't describe the encoding format (IP octets + big-endian port).)
  - random_bytes: [USED] Called at line 93 in maybe_rotate_secret to generate cryptographically random cookie secret | [UNIQUE] Generates 32-byte random buffer via fill_random helper; no similar functions found | [OK] Allocates a zero-initialised 32-byte buffer, fills it via fill_random, and returns it. No correctness issues. | [LEAN] Minimal typed helper that allocates a [u8;32] and delegates to fill_random. The explicit size in the name and return type is clearer than a const-generic alternative for two distinct uses. | [WEAK] Private helper exercised indirectly through CookieChecker::new() inside every test's setup(). No dedicated test; cryptographic randomness cannot be meaningfully unit-tested, but even a sanity check (non-zero output, different on two calls) is absent. | [UNDOCUMENTED] Private function with no /// doc comment. Name is self-descriptive; body delegates entirely to fill_random into a 32-byte array. Tolerated under private-item leniency. (deliberated: reclassified: documentation: UNDOCUMENTED → DOCUMENTED — Tests WEAK kept: no dedicated test, though crypto randomness is inherently hard to test. A non-zero sanity check would be reasonable. Documentation reclassified UNDOCUMENTED → DOCUMENTED: private 3-line function, name is perfectly self-descriptive (returns random bytes), body is trivially a fill_random call. Private-item leniency applies.)
  - random_nonce: [USED] Called at line 121 in create_reply_from_bytes to generate random nonce for cookie encryption | [UNIQUE] Generates 24-byte random nonce via fill_random helper; no similar functions found | [OK] 24-byte nonce for XChaCha20 is the correct size; allocation and fill pattern are correct. | [LEAN] Same pattern as random_bytes but for [u8;24]. Having two small explicitly-typed functions is clearer than a const-generic abstraction for two call sites in security-critical code. | [WEAK] Private helper exercised indirectly through create_reply in four tests. No dedicated test; a nonce-uniqueness check or non-zero sanity assertion is absent, important for security. | [DOCUMENTED] Private 4-line function whose name precisely describes its behaviour (produce a random nonce). No /// doc comment, but body is a trivial fill_random call into a 24-byte buffer. Per prior deliberation reclassification, private thin wrappers with self-descriptive names meet the DOCUMENTED bar under leniency rules. (deliberated: confirmed — Tests WEAK confirmed with higher confidence: exercised only indirectly through create_reply. No nonce-uniqueness or non-zero assertion. For a security-critical nonce generator, this is a fair concern even though dedicated testing of randomness is limited in value.)
  - constant_time_eq_16: [USED] Called at lines 131 and 147 for constant-time comparison of MAC1 and MAC2 to prevent timing attacks | [UNIQUE] Constant-time equality comparison for 16-byte arrays using subtle::ConstantTimeEq; no similar functions found | [OK] The early-exit on b.len() < 16 is defensive; all callers pass &expected[..16] which is always exactly 16 bytes so the branch is never taken in practice. The actual comparison uses subtle::ConstantTimeEq correctly. | [LEAN] Security-critical helper that wraps the subtle crate's ConstantTimeEq with a bounds check. The named wrapper communicates intent at call sites (MAC comparison must be constant-time). Appropriate use of an established crypto utility. | [WEAK] Exercised indirectly through verify_mac2 (true and false outcomes) and wrong_mac1_fails_decrypt. However, the b.len() < 16 short-circuit guard — a security-relevant early-return — is never tested, and verify_mac1 (the only other caller) has no dedicated tests. | [UNDOCUMENTED] Private function with no /// doc comment. Name encodes the semantics (constant-time equality on 16-byte values using subtle crate). Security-critical path, but tolerated under private-item leniency given the unambiguous name. (deliberated: reclassified: documentation: UNDOCUMENTED → DOCUMENTED — Tests WEAK confirmed: the b.len() < 16 early-return guard is security-relevant but never triggered in tests (all callers pass exactly 16 bytes). verify_mac1 (a caller) has no tests at all. Documentation reclassified UNDOCUMENTED → DOCUMENTED: private function with an extremely descriptive name encoding semantics (constant-time, equality, 16 bytes). Body is 4 lines using the well-known subtle::ConstantTimeEq. Private-item leniency applies.)

- [ ] `rustguard-core/src/session.rs` — 1 weak
  Improve `rustguard-core/src/session.test.rs` covering: TransportSession
  - TransportSession: [USED] Exported pub struct with 3 runtime importers across multiple workspace crates: rustguard-core/src/handshake.rs, rustguard-daemon/src/peer.rs, rustguard-enroll/src/client.rs. Per rule 3, exported symbols with 1+ runtime importers are USED. | [UNIQUE] No RAG similarity data available; struct represents a cryptographic transport session with unique semantics for encrypted packet handling and replay protection. | [OK] Struct definition is correct. `key_send` and `key_recv` are included in zeroize (no skip), which is right for sensitive key material. Non-sensitive fields `our_index`, `their_index`, `send_counter`, and `recv_window` are correctly annotated with `#[zeroize(skip)]`. Field types are appropriate: u32 for WireGuard-style session indices, [u8; 32] for 256-bit AEAD keys, u64 for a nonce counter that gives ~18 quintillion packets before exhaustion. The `impl` methods (L32–L88) are also correct: `encrypt` saves the pre-increment counter before bumping with `checked_add(1)?`, so the right nonce is passed to `seal`; `decrypt` checks the replay window before the expensive AEAD and only calls `recv_window.update` on AEAD success, preventing window-poisoning attacks; `encrypt_to` and `decrypt_in_place` mirror the same patterns. The `make_session_pair` helper cross-wires keys correctly (initiator key_send == responder key_recv and vice versa). No logic errors, type mismatches, or unsafe operations detected. | [LEAN] The struct holds exactly the fields required for a WireGuard-style transport session: two directional keys, two session indices, a nonce counter, and an anti-replay window. `Zeroize`/`ZeroizeOnDrop` is a security necessity for cryptographic key material, not a design indulgence. The selective `#[zeroize(skip)]` annotations on non-sensitive fields (indices, counter, window) are intentional and correct. No generics, no factory, no inheritance — minimal and fit for purpose. | [WEAK] Six inline #[cfg(test)] functions cover encrypt/decrypt roundtrip, counter monotonicity, wrong-counter rejection, replay rejection, out-of-order delivery, and bidirectional use. However, the zero-alloc variants encrypt_to and decrypt_in_place (separate code paths through seal_to/open_to) have zero test coverage, and nonce exhaustion (send_counter u64 overflow via checked_add returning None) is never exercised. This matches the previously deliberated WEAK classification for this symbol. | [DOCUMENTED] Struct carries a thorough five-line /// block (preceding lines 7–11) explaining purpose, derivation from handshake, and the initiator/responder key-role convention. Both public fields (our_index, their_index) have /// field-level comments, and all four private fields (key_send, key_recv, send_counter, recv_window) also carry /// doc comments. Two public items on the impl block (new, send_counter) lack doc comments, but the struct-level and field-level coverage is comprehensive. Previously flagged as UNDOCUMENTED and overturned by deliberation to DOCUMENTED. (deliberated: confirmed — Tests WEAK is correct and well-justified. The six existing tests thoroughly cover the allocating encrypt/decrypt path, counter monotonicity, replay rejection, out-of-order delivery, and bidirectional use. However, the zero-alloc variants encrypt_to and decrypt_in_place follow separate code paths (seal_to/open_to) and have zero test coverage. Nonce exhaustion (checked_add returning None at u64::MAX) is also unexercised. These are real coverage gaps in a security-critical module. All other axes are clean: correction OK (crypto logic is sound with pre-increment counter, replay-window-poisoning prevention, and correct key cross-wiring in tests), utility USED (3+ cross-crate importers confirmed), duplication UNIQUE (cryptographic session with unique semantics), overengineering LEAN (minimal fields, no unnecessary abstractions), documentation DOCUMENTED (comprehensive struct and field docs, minor gaps on new/send_counter are noted as best-practice WARNs but do not rise to UNDOCUMENTED).)
