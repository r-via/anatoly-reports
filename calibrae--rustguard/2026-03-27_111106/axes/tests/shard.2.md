[← Back to Tests](./index.md) · [← Back to report](../../public_report.md)

# Tests — Shard 2

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-enroll/src/control.rs` | NEEDS_REFACTOR | 10 | 95% | [details](#rustguard-enrollsrccontrolrs) |
| `rustguard-cli/src/main.rs` | NEEDS_REFACTOR | 10 | 90% | [details](#rustguard-clisrcmainrs) |
| `rustguard-daemon/src/config.rs` | NEEDS_REFACTOR | 7 | 95% | [details](#rustguard-daemonsrcconfigrs) |
| `rustguard-enroll/src/protocol.rs` | NEEDS_REFACTOR | 4 | 92% | [details](#rustguard-enrollsrcprotocolrs) |
| `rustguard-tun/src/linux.rs` | NEEDS_REFACTOR | 8 | 90% | [details](#rustguard-tunsrclinuxrs) |
| `rustguard-tun/src/bpf_loader.rs` | NEEDS_REFACTOR | 9 | 88% | [details](#rustguard-tunsrcbpfloaderrs) |
| `rustguard-crypto/src/aead.rs` | NEEDS_REFACTOR | 5 | 92% | [details](#rustguard-cryptosrcaeadrs) |
| `rustguard-tun/src/macos.rs` | NEEDS_REFACTOR | 7 | 90% | [details](#rustguard-tunsrcmacosrs) |
| `rustguard-kmod/src/cookie.rs` | NEEDS_REFACTOR | 6 | 92% | [details](#rustguard-kmodsrccookiers) |
| `rustguard-tun/src/linux_mq.rs` | NEEDS_REFACTOR | 6 | 90% | [details](#rustguard-tunsrclinuxmqrs) |

## Symbol Details

### `rustguard-enroll/src/control.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `socket_path` | L18–L20 | NONE | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by start_listener (L89) and send_command (L154). | [UNIQUE] Trivial utility returning hardcoded socket path. No similar functions found. | [OK] Simple function returning a hardcoded PathBuf. No correctness issues. | [LEAN] Single source of truth for the socket path. Trivially simple, no abstraction overhead. | [NONE] No test file exists for control.rs. socket_path has no tests whatsoever. | [PARTIAL] Has `/// Default socket path.` but does not mention the actual hardcoded path `/tmp/rustguard.sock`, omits return-value description, and has no `# Examples` section for a public API function. (deliberated: confirmed — Tests NONE confirmed — no test file exists. Documentation PARTIAL is accurate: the `/// Default socket path.` comment exists but omits the actual hardcoded value `/tmp/rustguard.sock` and has no `# Examples`. For a trivial one-liner this is minor but technically correct.) |
| `new_window` | L26–L28 | NONE | 95% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for creating enrollment window state. | [UNIQUE] Trivial constructor wrapping AtomicI64 initialization. No similar functions found. | [OK] Correctly initialises Arc<AtomicI64> to 0 (closed state). No issues. | [LEAN] Minimal constructor for the EnrollmentWindow type alias. The one-liner factory is justified as it pairs cleanly with the opaque type alias and avoids callers spelling out Arc<AtomicI64::new(0)> everywhere. | [NONE] No test file exists for control.rs. new_window has no tests whatsoever. | [UNDOCUMENTED] No `///` doc comment on the function itself. The `EnrollmentWindow` type alias above has docs, but the constructor function has none — no description of purpose, initial state (deadline=0), or `# Examples`. (deliberated: confirmed — Tests NONE confirmed — no tests exist. Documentation UNDOCUMENTED is correct: the function is `pub` and has zero `///` doc comments. The type alias above it is documented, but the constructor itself is not. This is a legitimate gap for a public API item.) |
| `is_open` | L31–L41 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for checking enrollment window status. | [UNIQUE] Checks if enrollment open by comparing deadline to current time. Distinct semantic contract from remaining(). | [OK] Logic is correct: 0 => closed, compare now < deadline. The `.unwrap()` on `duration_since(UNIX_EPOCH)` would panic if the system clock were set before 1970, but this is practically impossible on a running system in 2026. The i64 cast of as_secs() is safe for all timestamps until year 2262. | [LEAN] Single-responsibility predicate with minimal branching. Straightforward deadline comparison against UNIX timestamp. | [NONE] No test file exists for control.rs. is_open has no tests; edge cases like deadline==0, now==deadline, now<deadline all go untested. | [PARTIAL] Has `/// Check if enrollment is currently open.` but omits description of the `window` parameter, does not document the boolean return semantics (expired deadline treated as closed), and has no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed — no test coverage for edge cases (deadline==0, now==deadline, now<deadline). Documentation PARTIAL is fair: `/// Check if enrollment is currently open.` is present but omits parameter description and return value semantics (e.g., expired deadline → false).) |
| `open_window` | L44–L51 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L116. | [UNIQUE] Opens enrollment by calculating deadline from duration. No similar functions found. | [OK] Correctly computes deadline as now + duration and stores it atomically. The `duration_secs as i64` cast is safe for any plausible enrollment duration. Same theoretical `unwrap()` concern as is_open; inconsequential in practice. | [LEAN] Minimal setter that computes deadline and stores it atomically. No unnecessary indirection. | [NONE] No test file exists for control.rs. open_window has no tests; deadline arithmetic and atomic store are untested. | [PARTIAL] Has `/// Open enrollment for \`duration_secs\` seconds.` which references one parameter inline, but does not describe the `window` parameter, does not document overflow/wrap behavior for large durations, and has no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL accurate: the inline reference to `duration_secs` is helpful but `window` parameter and overflow behavior are undocumented. Both findings stand.) |
| `close_window` | L54–L56 | NONE | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L122. | [UNIQUE] Trivial function storing zero to close window. No similar functions found. | [OK] Atomically stores 0 to signal closed state. Correct and complete. | [LEAN] One-liner that stores the sentinel value 0. As minimal as it gets. | [NONE] No test file exists for control.rs. close_window has no tests. | [PARTIAL] Has `/// Close enrollment immediately.` — minimal but present. Missing `window` parameter description and no `# Examples` section for a public API function. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL is technically correct — `/// Close enrollment immediately.` is present and clear for such a trivial function, but missing `window` param docs means it doesn't meet full Rust doc standards. Both findings stand.) |
| `remaining` | L59–L73 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L128. | [UNIQUE] Returns seconds remaining in window. Different semantic contract from is_open(). | [OK] Guards against the past-deadline case before casting to u64, preventing underflow. Logic is correct. Same theoretical unwrap concern on duration_since as the other time-reading functions. | [LEAN] Simple arithmetic to compute remaining seconds. The UNIX timestamp snippet is repeated from is_open/open_window but that is a minor DRY gap, not overengineering. Logic is flat and readable. | [NONE] No test file exists for control.rs. remaining has no tests; branches for deadline==0, now>=deadline, and now<deadline are all untested. | [PARTIAL] Has `/// Seconds remaining in the enrollment window. 0 if closed.` which covers the return value semantics. Missing `window` parameter description, no mention of behavior when deadline is in the past vs exactly now, and no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed — multiple branches (deadline==0, now>=deadline, now<deadline) all untested. Documentation PARTIAL is fair: return semantics are documented (`0 if closed`) but parameter description is missing. Both findings hold.) |
| `start_listener` | L77–L104 | NONE | 78% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Substantial function that sets up socket listener and spawns background thread. | [UNIQUE] Initializes Unix socket listener with permissions and spawns handler thread. No similar functions found. | [NEEDS_FIX] The single background thread handles connections sequentially via `handle_client`, which loops over `reader.lines()` with no server-side read timeout set on the accepted stream. A client that connects but never sends data (or never closes the connection) will block the thread inside `handle_client` indefinitely, making the control socket unresponsive to all subsequent `OPEN`/`STATUS` commands for the lifetime of the server. `send_command` sets a client-side read timeout but there is no corresponding server-side write or read timeout, so a stalled or malicious client can permanently wedge the listener. | [LEAN] Appropriately complex for spawning a UNIX socket listener: stale-socket cleanup, bind, chmod, and a single background thread. Each step is necessary. No layers of abstraction around what is inherently a multi-step OS setup. | [NONE] No test file exists for control.rs. start_listener has no tests; socket binding, stale-socket removal, permission setting, and background thread spawning are all untested. | [PARTIAL] Has two `///` lines describing purpose and return value. Missing descriptions for both `window` and `peer_count` parameters, no documentation of error conditions (`io::Error` cases), no mention of the background thread lifecycle or stale-socket cleanup, and no `# Examples` section. (deliberated: confirmed — Correction NEEDS_FIX is valid: the single background thread processes connections sequentially via `handle_client`, which blocks on `reader.lines()` with no server-side read timeout. A stalled or malicious client on the Unix socket permanently wedges the listener. The fix (set_read_timeout on the accepted stream or spawn per-connection) is small-effort. However, this is a local Unix domain socket for a system daemon, not internet-facing, which somewhat limits the practical attack surface — keeping confidence moderate at 78 rather than high. Tests NONE and documentation PARTIAL both confirmed.) |
| `handle_client` | L106–L144 | NONE | 60% | [USED] Private function called by start_listener at L102 in spawned thread. Handles incoming socket commands (OPEN, CLOSE, STATUS). | [UNIQUE] Processes OPEN/CLOSE/STATUS commands from connected client. No similar functions found. | [OK] Both `BufReader<&UnixStream>` and `&UnixStream` (for writes) hold shared references; `impl Read for &UnixStream` and `impl Write for &UnixStream` are both valid in std, so there is no aliasing unsafety. Command parsing via `to_ascii_uppercase().as_deref()` is correct. Default of 60 s for a missing OPEN argument is a reasonable fallback, not a bug. The `break` on line-read error correctly terminates on EOF/disconnect. | [LEAN] Flat line-based command dispatcher over three commands (OPEN, CLOSE, STATUS). No trait objects, no dynamic dispatch, no registration table—a plain match is the right tool for three arms. | [NONE] No test file exists for control.rs. handle_client has no tests; OPEN, CLOSE, STATUS command parsing and unknown-command error path are all untested. | [UNDOCUMENTED] Private function (`fn`, not `pub fn`) with no `///` doc comment. Tolerable given private visibility, but this function implements the entire control protocol dispatch (OPEN/CLOSE/STATUS/ERR) — inline documentation would be valuable. Confidence lowered per private-item leniency rule. (deliberated: confirmed — Tests NONE confirmed — the entire control protocol dispatch (OPEN/CLOSE/STATUS/ERR) is untested. Documentation UNDOCUMENTED is technically correct, though the function is private so the severity is low. The original evaluator already lowered confidence to 60 per private-item leniency, which is appropriate. This function implements the core protocol logic and would benefit from at least an inline comment block, but it's not a public API obligation.) |
| `send_command` | L147–L166 | NONE | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for sending control commands to the socket. | [UNIQUE] Connects to socket, sends command, reads response. Inverse role of handle_client(). No similar functions found. | [OK] Correctly connects, sets a 5-second read timeout, writes cmd + newline, flushes, and reads exactly one response line. The response will contain a trailing newline, which callers may need to trim, but that is a caller concern, not a correctness defect. No write timeout is set, but for a Unix socket with a local daemon and small payloads this is not a practical problem. | [LEAN] Minimal client-side utility: connect, write, read one line, return. Correctly wraps the connect error with a user-facing message. No unnecessary abstraction for what is a single-shot RPC. | [NONE] No test file exists for control.rs. send_command has no tests; connection failure, timeout, write, and response-read paths are all untested. | [PARTIAL] Has `/// Send a command to the running server via the control socket.` but omits: description of the `cmd` format (expected protocol strings), what the returned `String` contains (server's response line), documented error conditions (connection refused, timeout), and no `# Examples` section demonstrating usage. (deliberated: confirmed — Tests NONE confirmed — connection failure, timeout, write, and response-read paths are all untested. Documentation PARTIAL is accurate: the doc comment describes the purpose but omits the expected `cmd` format, return value contents, and error conditions. Both findings hold.) |
| `cleanup` | L169–L171 | NONE | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Utility function for socket file cleanup. | [UNIQUE] Trivial function removing socket file. No similar functions found. | [OK] Silently removes the socket file. Ignoring the result is intentional and correct for cleanup paths. | [LEAN] Trivial one-liner wrapper around remove_file. Exists solely to give callers a semantic name at the call site, which is appropriate. | [NONE] No test file exists for control.rs. cleanup has no tests. | [PARTIAL] Has `/// Cleanup: remove the socket file.` which is minimal but conveys purpose. Missing description of the `path` parameter, no note that errors are silently ignored (`let _ = ...`), and no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL accurate: `/// Cleanup: remove the socket file.` is present but doesn't note that errors are silently ignored (`let _ = ...`), which is a deliberate design choice callers should be aware of. Both findings stand.) |

### `rustguard-cli/src/main.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `main` | L6–L28 | NONE | 75% | [USED] Entry point function required by Rust. Called automatically by the runtime to start the CLI. | [UNIQUE] Entry point dispatching to command handlers based on arguments. No similar functions found. | [OK] Dispatch is correct. args[2..] is always valid (non-panicking empty slice) when args.len() >= 2, which is guaranteed by the Some(_) match branch being reached only when args.get(1) is Some. | [LEAN] Minimal hand-rolled dispatch over argv[1]. No unnecessary abstractions; the match directly delegates to named command functions. Appropriate for a CLI entry point. | [NONE] No test file found for this source file. The main dispatch logic (command routing) has zero test coverage. | [UNDOCUMENTED] No /// doc comments present. Private entry point; name is self-explanatory, but a brief crate-level or function-level doc noting the CLI dispatch pattern would aid maintainers. (deliberated: confirmed — Tests NONE is correct: no test file exists. Documentation UNDOCUMENTED is technically correct but this is a private entry point in a binary crate where `main` is universally understood. Low impact. Raising confidence from 60 since both findings are directly verifiable from source.) |
| `usage` | L30–L42 | WEAK | 72% | [USED] Called twice in main() at lines 21 and 27. Prints help text for command usage. | [UNIQUE] Prints usage information for CLI. No similar functions found. | [OK] Pure output function; no logic to evaluate. | [LEAN] Simple static help text printed to stderr. One function, one responsibility, no abstraction needed. | [NONE] No test file found. The usage/help output function has no tests verifying correct command listing or formatting. | [UNDOCUMENTED] No /// doc comments. Private helper with a clear name; the body itself serves as implicit documentation of supported commands, so leniency applies. (deliberated: confirmed — Tests WEAK is reasonable — no test file exists but this is a pure eprintln output function with minimal logic. Testing it provides low value. The detail text says NONE but WEAK is a fair merged assessment given the function's trivial nature.) |
| `cmd_up` | L44–L66 | NONE | 75% | [USED] Called in main() at line 10 via match arm for 'up' command. Handles tunnel startup. | [UNIQUE] Loads config from file and runs tunnel. No similar functions found. | [OK] Config is loaded from file and passed to tunnel::run; all error paths exit cleanly. No correctness issues. | [LEAN] Reads a single positional config path, loads Config, runs the tunnel. Minimal and appropriately scoped. | [NONE] No test file found. Missing path argument handling, config parse error path, and tunnel run error path are all untested. | [UNDOCUMENTED] No /// doc comments. Private function; naming convention (cmd_*) signals its role as a subcommand handler, but parameter semantics (args slice layout) and exit-code behaviour are undocumented. (deliberated: confirmed — Tests NONE confirmed: no test file. Documentation UNDOCUMENTED is correct — no doc comments — though the cmd_ naming convention and clear error handling make it fairly self-documenting. Both findings are directly observable from the source.) |
| `cmd_serve` | L68–L161 | NONE | 90% | [USED] Called in main() at line 11 via match arm for 'serve' command. Handles enrollment server startup. | [UNIQUE] Parses multiple options (pool, token, xdp, queues, uring, port) for serve command. No similar functions found. | [NEEDS_FIX] Two bugs: (1) The .filter(|v| !v.is_empty()) guard for --pool (L82), --token (L89), and --xdp (L99) only rejects empty strings. If a flag is immediately followed by another flag — e.g., `--token --open` — the next flag name is silently consumed as the option value (token becomes "--open"), and that flag is never processed, so open_immediately stays false. This produces a wrong token passed to the server at runtime with no parsing-level error. (2) --queues (L106) and --port (L113-L116) use .unwrap_or(default) on parse failure, silently ignoring missing or malformed values and falling back to defaults — inconsistent with --pool/--token which exit with an error on missing values. | [ACCEPTABLE] 93 lines of manual while-loop index-tracking arg parsing for 7 flags. Verbose but not artificially abstract. No CLI library (e.g. `clap`, >50M weekly downloads on crates.io) appears to be installed, so this is hand-rolled by necessity. The `or_else(|| process::exit(1))` pattern for required-value enforcement is unusual but functional. Consider adopting `clap` to reduce boilerplate and duplication with `cmd_join`. | [NONE] No test file found. Complex argument parsing logic with many flags (--pool, --token, --port, --xdp, --queues, --uring, --open) and multiple error paths are entirely untested. | [UNDOCUMENTED] No /// doc comments. This is the most complex handler (93 lines, 8 flags, XDP/uring options); the lack of any inline doc comment is the most impactful omission in the file despite the function being private. (deliberated: confirmed — Correction NEEDS_FIX confirmed with high confidence: the flag-as-value bug is directly verifiable in the source. At L89, `--token --open` would set token to "--open" because .filter(|v| !v.is_empty()) only rejects empty strings. The --open flag is consumed and open_immediately stays false. This is a real runtime bug. The --port/--queues silent default fallback (action 3) is also a legitimate inconsistency. Tests NONE confirmed — no test file. Documentation UNDOCUMENTED is correct and most impactful here given 93 lines of complex arg parsing with 8 flags. Significantly raising confidence since the bug is directly provable from the code.) |
| `cmd_join` | L163–L211 | NONE | 90% | [USED] Called in main() at line 12 via match arm for 'join' command. Handles client join operation. | [UNIQUE] Parses endpoint and token arguments for join command. No similar functions found. | [NEEDS_FIX] --token parsing at L172 uses the same .filter(|v| !v.is_empty()) guard. If --token is immediately followed by another flag (e.g., `--token --endpoint foo`), that flag's name becomes the token value and is never processed as a flag. The join will fail at runtime with an authentication error rather than a clear argument-parsing error. | [ACCEPTABLE] ~48 lines of manual arg parsing for 2 flags plus one positional. Same while-loop index-tracking pattern as `cmd_serve`, duplicating the convention. Acceptable without a CLI library but reinforces the suggestion to adopt `clap` to eliminate repeated parsing boilerplate. | [NONE] No test file found. Argument parsing for endpoint and --token, missing-argument error paths, and SocketAddr parse error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private function; flag layout and zero-config enrollment semantics are not described anywhere in the symbol. (deliberated: confirmed — Correction NEEDS_FIX confirmed: identical flag-as-value pattern to cmd_serve. At L172, `--token --endpoint foo` would set token to "--endpoint" and the actual endpoint flag is consumed silently. Directly verifiable from source. Tests NONE confirmed — no test file. Documentation UNDOCUMENTED correct — no doc comments on a moderately complex function with positional + flag args.) |
| `cmd_open` | L213–L226 | WEAK | 72% | [USED] Called in main() at line 13 via match arm for 'open' command. Handles enrollment window open. | [UNIQUE] Sends OPEN command with duration to control endpoint. No similar functions found. | [OK] Silently defaults to 60 on unparseable input; this matches the documented behavior (default 60s). The OPEN command is formatted and sent correctly. | [LEAN] Parses an optional integer argument with a sensible default and sends a single control command. Minimal and clear. | [NONE] No test file found. Default seconds fallback (60), custom seconds parsing, and control command error path are untested. | [UNDOCUMENTED] No /// doc comments. Private helper; the default-60-second fallback and the IPC mechanism used (control socket) are not described. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists but the function is a thin wrapper around control::send_command with a simple default-seconds fallback. Testing value is moderate.) |
| `cmd_close` | L228–L236 | WEAK | 72% | [USED] Called in main() at line 14 via match arm for 'close' command. Handles enrollment window close. | [UNIQUE] Sends CLOSE command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Single call to send_command with error handling. Could not be simpler for its purpose. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. 9-line function that directly delegates to control::send_command. No test file exists but function is trivially simple.) |
| `cmd_status` | L238–L246 | WEAK | 72% | [USED] Called in main() at line 15 via match arm for 'status' command. Handles status query. | [UNIQUE] Sends STATUS command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Identical structure to cmd_close; single control command dispatch. Appropriately minimal. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. Structurally identical to cmd_close — pure delegation to control::send_command. No test file exists.) |
| `cmd_genkey` | L248–L252 | WEAK | 72% | [USED] Called in main() at line 16 via match arm for 'genkey' command. Generates cryptographic key. | [UNIQUE] Generates random private key and encodes as base64. No similar functions found. | [OK] Generates a random StaticSecret, encodes bytes as base64, and prints. No correctness issues. | [LEAN] Generates a random static secret and base64-encodes it in four lines. Exactly as complex as the task demands. | [NONE] No test file found. Key generation and base64 encoding output are untested. | [UNDOCUMENTED] No /// doc comments. Private; the output format (base64-encoded 32-byte Curve25519 scalar) is implied but not stated. (deliberated: confirmed — Tests WEAK is reasonable. 5-line function generating a key and encoding it. No test file exists. The crypto correctness is in the library, not here.) |
| `cmd_pubkey` | L254–L267 | WEAK | 72% | [USED] Called in main() at line 17 via match arm for 'pubkey' command. Derives public key from private key. | [UNIQUE] Reads base64-encoded private key from stdin and derives public key. No similar functions found. | [OK] Reads a line, trims whitespace, decodes base64, coerces Vec<u8> to [u8; 32] via TryInto (panics with a clear message on wrong length), derives the public key, and prints base64. All error paths use expect() which is appropriate for a CLI tool. No logic bugs. | [LEAN] Reads a base64 private key from stdin, derives the public key, and prints it. Sequential and direct with no unnecessary abstraction. | [NONE] No test file found. stdin reading, base64 decode, 32-byte length validation, and public key derivation are all untested. | [UNDOCUMENTED] No /// doc comments. Private; the expected stdin format (base64 line), key length constraint (32 bytes), and output format are not documented, which could mislead callers piping data. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists. The function does have more logic (stdin read, base64 decode, length check, key derivation) than the simpler commands, so WEAK correctly captures that there's meaningful untested logic but it's a CLI tool where integration tests are the norm.) |

### `rustguard-daemon/src/config.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Config` | L13–L16 | WEAK | 72% | [USED] Main exported struct holding parsed WireGuard configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Public struct with unique field composition (interface, peers); no duplicate definitions found | [NEEDS_FIX] Config::parse (L120-L122) sets current_section to 'interface' but never flushes current_peer_kvs when a [Interface] header arrives while parsing a [Peer] section. If any [Peer] block precedes [Interface], its accumulated KVs are not cleared; the next [Peer] header's flush guard (current_section == Some('peer')) is then false, so those stale KVs merge with the next peer's KVs. The end-of-file flush emits a single peer that silently drops the first peer's data (last-writer-wins for duplicated keys like PublicKey). | [LEAN] Minimal two-field struct wrapping exactly the two top-level sections of a wg0.conf file. No unnecessary indirection. | [WEAK] Note: tests ARE present — embedded in the same file under #[cfg(test)], not a separate test file. `parse` is exercised by `parse_full_config` and `parse_v6_allowed_ips` covering the happy path with two peers. However, `from_file` is never tested, and error paths (malformed lines, keys outside sections, missing [Interface]) are entirely absent. | [PARTIAL] Struct has `/// Parsed WireGuard configuration.` at line 12, but fields `interface` and `peers` carry no individual `///` docs. The `from_file` and `parse` methods in the impl block (lines ~94–165) also have zero doc comments and no `# Examples` sections, which are expected on public API methods. (deliberated: confirmed — Correction NEEDS_FIX is valid: inspecting Config::parse L120-122, when [Interface] is encountered after a [Peer], current_peer_kvs is not flushed — the flush guard at L125 only triggers inside the [Peer] branch when current_section is already 'peer'. This means [Peer]-before-[Interface] ordering silently merges stale KVs. While WireGuard configs conventionally place [Interface] first, the parser doesn't enforce this and fails silently. Additionally, L163-165 wraps interface in Some() then immediately unwraps via ok_or_else — a dead branch noted in best_practices. Tests WEAK is accurate: parse_full_config and parse_v6_allowed_ips cover happy paths only; from_file is untested; error paths (malformed lines, keys outside sections, missing [Interface]) are absent. Documentation PARTIAL is accurate: struct has a one-line doc comment but fields and methods lack individual docs.) |
| `CidrAddr` | L38–L41 | WEAK | 85% | [USED] Exported struct with three utility methods (contains_v4, contains_v6, contains). Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] CIDR address representation struct; no similar type definitions found | [OK] Struct definition is correct. The contains_v4 and contains_v6 methods use proper bitmask arithmetic; the prefix_len==0 early-return, the >=32 / >=128 caps, and the shift expressions are all correct for their respective address families. | [ACCEPTABLE] Struct itself is minimal. Exposing both typed helpers (contains_v4, contains_v6) and the polymorphic contains() is mildly redundant but useful for callers with already-typed addresses. The ipnetwork crate (popular on crates.io) would eliminate the hand-rolled bit-mask arithmetic in the impl block, but the custom implementation is short and correct so the redundancy is not a real problem. | [WEAK] contains_v4 is well-covered: /24, /32 (host), /0 (default route), and cross-family rejection are all tested. contains_v6 only covers /64; the boundary cases prefix_len=0 (default route) and prefix_len=128 (host) are not tested for v6, unlike v4. The `contains()` dispatch method is never directly exercised. The Display impl is untested. | [PARTIAL] Struct has `/// An IP address with prefix length (CIDR notation). Supports v4 and v6.` at line 37, and all three methods (`contains_v4`, `contains_v6`, `contains`) have one-line `///` comments. However, the two public fields (`addr`, `prefix_len`) lack `///` docs, and none of the methods include a `# Examples` section despite being public API. (deliberated: confirmed — Overengineering ACCEPTABLE is fair: exposing both typed helpers and the polymorphic contains() is mildly redundant but practical for callers. Tests WEAK confirmed: v4 has excellent boundary coverage (/0, /24, /32, cross-family rejection) but v6 only covers /64 — missing /0 and /128 boundary cases. The contains() dispatcher and Display impl are also untested. Documentation PARTIAL confirmed: struct-level doc exists at L37 and methods have one-line docs, but fields addr and prefix_len lack individual docs and no #[doc] examples exist. Raised confidence slightly since all findings are directly verifiable in source.) |
| `decode_key` | L167–L174 | WEAK | 70% | [USED] Non-exported function called locally in parse_interface (line 211) and parse_peer (lines 251, 253). Decodes base64-encoded WireGuard keys to 32-byte arrays. | [UNIQUE] Base64 decodes string to 32-byte key. RAG found base64_key functions (scores 0.70–0.71) but they perform opposite operation (encoding, not decoding) with different signatures and semantic contracts—not interchangeable | [OK] Correctly decodes standard base64 (WireGuard uses RFC 4648 standard alphabet with padding) and validates the decoded length is exactly 32 bytes, matching the WireGuard key size. | [LEAN] Two-step base64 decode then exact-length check. Minimal and correct. | [WEAK] Exercised only implicitly via parse_full_config happy path (valid base64 keys decode without error). No tests for invalid base64 input, wrong decoded length (< or > 32 bytes), or empty string — both error branches in the function are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The name is reasonably descriptive, and per convention private items are tolerated as undocumented. Confidence lowered accordingly. (deliberated: confirmed — Tests WEAK is accurate: decode_key is only exercised via parse_full_config's happy path with valid base64 keys. Both error branches (bad base64, wrong decoded length) are untested. However, as a private helper, this is less critical. Raised confidence slightly since the finding is directly verifiable by checking the test module — no test constructs invalid base64 or wrong-length key inputs.) |
| `parse_interface` | L176–L226 | WEAK | 88% | [USED] Non-exported function called in Config::parse (line 163). Parses [Interface] section of config file with real parsing logic for keys, port, addresses. | [UNIQUE] Parses WireGuard interface config from key-value pairs; no similar parsing functions found | [NEEDS_FIX] L195: when no '/' is present in an Address part, the fallback prefix is hardcoded as the string '24'. For an IPv6 address without an explicit prefix (e.g. 'Address = fd00::1'), this silently assigns prefix_len=24 instead of a valid IPv6 prefix, corrupting the address_v6 tuple stored in InterfaceConfig. parse_cidr (L277) solves the identical problem by branching on addr.is_ipv4(); parse_interface should do the same here. | [LEAN] Length is driven by the number of distinct keys and the dual-stack address parsing. Each branch is a direct field assignment with error mapping; no unnecessary layers of abstraction. | [WEAK] Happy path covered by parse_full_config (port, v4 address, netmask). parse_v6_allowed_ips does NOT exercise the v6 address field on the interface (only peer AllowedIPs). Error paths — missing PrivateKey, missing Address, bad port, bad prefix, bad address string — are all untested. Default listen_port=51820 fallback is never verified. | [UNDOCUMENTED] Private function with no `///` doc comment. Contains non-trivial logic such as comma-separated address parsing and IPv6 handling; a brief doc noting accepted input format would be helpful, but leniency applies to private items. (deliberated: confirmed — Correction NEEDS_FIX is strongly confirmed: at L195, `.unwrap_or((part, "24"))` hardcodes prefix 24 as the fallback when no '/' is present. For an IPv6 address like 'fd00::1' (without explicit prefix), this assigns prefix_len=24 which is invalid for IPv6. The correct approach is visible in parse_cidr at L277-279 which branches on addr.is_ipv4() to select 32 or 128 as default. This is a clear, directly visible bug — raising confidence to 88. Tests WEAK is accurate: v6 interface address parsing is never exercised (parse_v6_allowed_ips only tests peer AllowedIPs), and error paths are absent. Documentation UNDOCUMENTED with private-item leniency is appropriate.) |
| `parse_peer` | L228–L269 | WEAK | 75% | [USED] Non-exported function called in Config::parse (lines 139, 153). Parses [Peer] section of config file with real parsing logic for keys, endpoints, allowed IPs. | [UNIQUE] Parses WireGuard peer config from key-value pairs; no similar parsing functions found | [OK] Correctly parses all mandatory and optional peer fields. AllowedIPs entries are accumulated across repeated keys. Duplicate scalar keys use last-writer-wins semantics, which is acceptable for this format. | [LEAN] Mirrors parse_interface in structure. Five peer fields require five match arms. No avoidable complexity. | [WEAK] Happy path exercised by parse_full_config (endpoint, multiple allowed_ips, persistent_keepalive, absent endpoint). PresharedKey parsing is never exercised. Error paths (missing PublicKey, bad SocketAddr endpoint, bad keepalive value) are all absent. Only 2 of the 5 optional fields are validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is self-explanatory; private-item leniency applies. (deliberated: confirmed — Tests WEAK confirmed: parse_full_config exercises endpoint, allowed_ips, and persistent_keepalive, but preshared_key parsing is never exercised and all error paths (missing PublicKey, bad endpoint, bad keepalive) are absent. Only 3 of 5 fields are validated in tests. Documentation UNDOCUMENTED with private-item leniency applies. Raised confidence moderately since findings are directly verifiable against the test module.) |
| `parse_cidr` | L271–L282 | WEAK | 75% | [USED] Non-exported function called in parse_peer (line 259). Parses CIDR notation strings (e.g. '10.0.0.0/24') into CidrAddr instances. | [UNIQUE] Parses CIDR notation with smart defaults (32 for IPv4, 128 for IPv6); no similar functions found | [NEEDS_FIX] L278: prefix_len is parsed as u8 (0-255) with no upper-bound validation against the address family. A CIDR like '10.0.0.1/64' is silently accepted with prefix_len=64. contains_v4 then saturates this to /32 behaviour via the 'prefix_len >= 32' guard (L51), producing semantically incorrect match results without any error. Should reject prefix_len > 32 for IPv4 and > 128 for IPv6. | [LEAN] Splits on '/', selects sensible per-family defaults, parses each part once. Compact and purposeful. | [WEAK] Indirectly exercised through parse_full_config and parse_v6_allowed_ips for well-formed CIDR strings. The no-prefix default behaviour (addr without '/', defaulting to /32 or /128) is never tested. Error paths (invalid IP string, non-numeric prefix) are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The defaulting logic (32 for v4, 128 for v6 when no prefix is given) is non-obvious behavior that would benefit from a comment, but private-item leniency applies. (deliberated: confirmed — Correction NEEDS_FIX is valid: at L278, prefix_len is parsed as u8 with no upper-bound check against address family. '10.0.0.1/64' is silently accepted; contains_v4 then saturates to /32 via the >= 32 guard at L51, producing semantically incorrect results (treats /64 as /32 without error). Adding validation is a small defensive improvement. Tests WEAK confirmed: only well-formed CIDRs with explicit prefixes are tested; no-prefix defaults and error paths are unexercised. Documentation: the original field says DOCUMENTED while the detail text says UNDOCUMENTED — there is an inline comment '// Default prefix: 32 for v4, 128 for v6.' at L272 but no /// doc comment. Per the rule against adding new findings, I preserve the original DOCUMENTED classification, though the inconsistency is noted. Confidence raised from 60 to 75 as the missing validation is directly visible.) |
| `prefix_to_netmask` | L284–L292 | WEAK | 88% | [USED] Exported function converting prefix length to IPv4 netmask. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Converts IPv4 CIDR prefix length to netmask; no similar utility functions found | [OK] Correctly handles all three regions: prefix==0 returns 0.0.0.0, prefix>=32 returns 255.255.255.255, and the general case uses u32::MAX << (32-prefix) which is safe for prefix in 1..31. Ipv4Addr::from(u32) converts via to_be_bytes(), so byte ordering is correct on all platforms. | [LEAN] Three-branch bit-shift helper converting a prefix length to an Ipv4Addr mask. Handles the two edge cases (0 and >=32) explicitly to avoid undefined-behavior in shift arithmetic. Appropriately minimal. | [WEAK] Only implicitly tested via parse_full_config which asserts netmask==255.255.255.0 for a /24 address. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised. No direct unit tests exist for this exported function despite being imported by three callers. | [UNDOCUMENTED] Public function (`pub fn`) with no `///` doc comment, no parameter description, no description of boundary behaviour for `prefix == 0` or `prefix >= 32`, and no `# Examples` section. Expected on public API. (deliberated: confirmed — Tests WEAK confirmed: only indirectly tested via parse_full_config asserting netmask==255.255.255.0 for /24. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised despite being explicit code paths. No direct unit tests exist for this exported public function. Documentation UNDOCUMENTED confirmed: pub fn at L284 has no /// doc comment, no parameter description, and no explanation of boundary behavior. This is a public API imported by three workspace crates and warrants documentation. Slightly raised confidence.) |

### `rustguard-enroll/src/protocol.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `parse_request` | L40–L51 | WEAK | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment request messages. | [UNIQUE] Structurally similar to parse_response (similarity: 0.759) but semantically distinct: returns Option<[u8;32]> not Option<EnrollmentOffer>, uses ENROLL_REQUEST_MAGIC not ENROLL_RESPONSE_MAGIC, indices [28..76] not [28..81], and performs simple try_into conversion instead of complex struct construction. Different return types and contracts make this unique. | [OK] Length and magic checks are performed before any indexing. Nonce extracted from buf[4..28], ciphertext from buf[28..76] (48 bytes matching the expected seal output). xopen result is converted to [u8; 32] via try_into, which fails safely if the plaintext length differs. Logic is correct. | [LEAN] Straightforward length-and-magic validation followed by decryption. Returns Option which is idiomatic Rust for fallible parsing; no excess abstraction. | [WEAK] Happy path, wrong-token AEAD failure, and ciphertext tampering are all tested. However, the short-buffer early return (buf.len() < ENROLL_REQUEST_SIZE) and the wrong-magic early return (buf[0..4] != ENROLL_REQUEST_MAGIC, e.g. feeding a response packet to the request parser) are not exercised. Two None-returning branches are untested. | [PARTIAL] Two `///` lines describe purpose and return value briefly. Missing: parameter descriptions for `token_key` and `buf`, failure conditions for returning `None` (bad magic, AEAD failure, length mismatch), and `# Examples` section. (deliberated: confirmed — Tests: Three tests cover happy path, wrong-token AEAD failure, and ciphertext tampering — good for the crypto paths. However, the short-buffer guard (L42-44) and wrong-magic guard (L45-47) are untested, leaving two early-return branches uncovered. WEAK is fair but borderline — the crypto-critical paths are well tested while only simple guard clauses are missed. Documentation: Two `///` lines present but missing parameter descriptions and None-returning conditions. PARTIAL confirmed.) |
| `build_response` | L61–L75 | WEAK | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Constructs encrypted enrollment response messages. | [UNIQUE] Structurally similar to build_request (similarity: 0.738) but semantically distinct: accepts EnrollmentOffer not raw pubkey, constructs intermediate plaintext payload from struct fields, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, and uses indices [28..81] not [28..76]. Different parameters and payload logic make this unique despite structural overlap. | [OK] Plaintext is correctly assembled: server_pubkey at [0..32], ip octets at [32..36], prefix_len at [36]. xseal of 37-byte plaintext produces 53 bytes (37+16 tag), placed at buf[28..81]. All offsets match ENROLL_RESPONSE_SIZE=81. | [LEAN] Serialises EnrollmentOffer into the 81-byte wire frame. Fixed-size arithmetic matches the documented layout; no abstraction overhead. | [WEAK] Only response_roundtrip covers build_response; there is no analogue of request_wrong_token_fails (verifying that a response built with key A is unreadable under key B) nor a tampered_response_rejected test. A single happy-path roundtrip is insufficient for a security-critical wire-format builder. | [PARTIAL] Single-line `///` comment 'Build an enrollment response.' No description of the `token_key` or `offer` parameters, no explanation of what the returned byte array contains, and no `# Examples` section on this public function. (deliberated: confirmed — Tests: Only the response_roundtrip happy path covers build_response. No analogue of request_wrong_token_fails or tampered_request_rejected exists for the response path. This asymmetry is a genuine coverage gap for security-critical code. WEAK is well-justified. Documentation: Single-line `///` comment with no parameter or return descriptions. PARTIAL confirmed.) |
| `parse_response` | L78–L102 | WEAK | 86% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment response messages. | [UNIQUE] Structurally similar to parse_request (similarity: 0.759) but semantically distinct: returns Option<EnrollmentOffer> not Option<[u8;32]>, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, indices [28..81] not [28..76], includes plaintext length validation, and constructs complex EnrollmentOffer from decomposed bytes. Different return contracts and invariants make this unique. | [OK] Length and magic guards are in place before indexing. Nonce from buf[4..28], ciphertext from buf[28..81] (53 bytes). The explicit plaintext.len() != 37 check is redundant given a correctly functioning AEAD but is not harmful. Fields are extracted at correct offsets. All None-returning paths use ? or explicit return None. | [LEAN] Mirrors parse_request in structure; the extra plaintext-length check (L90) is a defensive sanity check appropriate for a security-sensitive protocol, not gold-plating. | [WEAK] Only the happy-path roundtrip is tested. Missing: wrong-token AEAD failure, ciphertext tampering, short-buffer early return, wrong-magic early return (e.g. feeding a request packet), and the explicit plaintext.len() != 37 guard. Four out of five failure branches are entirely untested, contrasting with the more thorough coverage of parse_request. | [PARTIAL] Single-line `///` comment 'Parse and decrypt an enrollment response.' Missing parameter descriptions, no enumeration of `None`-returning conditions (bad magic, AEAD failure, wrong plaintext length), and no `# Examples` section for a public function. (deliberated: confirmed — Tests: Only the happy-path roundtrip is tested. Four of five failure branches (wrong-token, tampering, short-buffer, wrong-magic, plaintext length check at L90) are entirely untested. This is the weakest test coverage in the file and contrasts sharply with parse_request's coverage. WEAK is clearly correct. Documentation: Single-line `///` with no parameter descriptions or enumeration of None-returning conditions. PARTIAL confirmed.) |
| `random_nonce` | L104–L108 | WEAK | 70% | [USED] Internal helper function called by build_request (L29) and build_response (L69). Generates cryptographic nonces. | [UNIQUE] Shows high similarity (0.943) to random_nonce in rustguard-core/src/cookie.rs but resides in separate crate. Per established precedent: cross-crate deduplication of trivial 3-line private helpers introduces unnecessary inter-crate coupling. Independent usage in different crates reflects intentional architecture, not harmful duplication. | [OK] Uses getrandom::getrandom for OS-provided entropy into a 24-byte buffer. Panicking on failure is appropriate for a crypto primitive — a system that cannot supply entropy cannot operate securely. No correctness issues. | [LEAN] Tiny typed wrapper around getrandom that gives the operation a name and a concrete return type. Avoids scattered unsafe-ish getrandom calls; well-justified as a local helper. | [WEAK] Private helper tested only indirectly: roundtrip tests verify the nonces it produces are valid (AEAD succeeds), but uniqueness across calls, panic behavior if getrandom fails, and the 24-byte length are never directly asserted. Indirect coverage is plausible but fragile for a CSPRNG wrapper. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name is self-descriptive and the function is not part of the public API, so omission is tolerable by convention. The `expect` message documents the panic condition inline. (deliberated: confirmed — Tests: Private 3-line helper tested only indirectly via roundtrip tests. Indirect coverage does verify the nonce works (AEAD succeeds), but nonce uniqueness and the panic path are untested. For a private helper this small, WEAK is borderline — indirect testing is the norm for trivial private helpers. However, since it's a CSPRNG wrapper and no direct assertions exist, WEAK is defensible. Bumping confidence slightly since indirect testing through multiple roundtrip tests does provide meaningful coverage.) |

### `rustguard-tun/src/linux.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `last_os_error` | L51–L53 | WEAK | 85% | [USED] Helper function used for OS error conversion in create(), configure_interface(), read(), and write() | [UNIQUE] Trivial 2-line wrapper with no duplicates found via RAG | [OK] Straightforward delegation to io::Error::last_os_error(); no correctness issues. | [OVER] Single-line alias for io::Error::last_os_error() that saves only the 'io::Error::' prefix. Adds a call-stack frame with no semantic benefit; callers could use io::Error::last_os_error() directly. Borderline, hence moderate confidence. | [NONE] No test file found for rustguard-tun/src/linux.rs. last_os_error has zero test coverage. | [UNDOCUMENTED] Private helper function, no `///` doc comment. Name is self-explanatory. Tolerated for private items. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: this 2-line wrapper is used at 4 distinct call sites (create, configure_interface, read, write), providing a consistent shorter alias alongside the companion close_and_error pattern. In Rust FFI code with many libc error checks, such aliases are a common and accepted convention. Tests WEAK retained — no test file exists for this module.) |
| `close_and_error` | L55–L59 | WEAK | 85% | [USED] Helper function to close fd and return error; used throughout create() and configure_interface() | [DUPLICATE] Identical 5-line helper found in linux_mq.rs with score 0.987 | [OK] Correctly captures errno via last_os_error() before calling close(), preventing the close() syscall from overwriting errno. Error is returned after fd is closed. | [LEAN] Captures errno before closing the fd to prevent clobbering — a legitimate and important pattern in unsafe libc code. Used consistently at every error exit point. | [NONE] No test file found for rustguard-tun/src/linux.rs. close_and_error has zero test coverage. Error path (fd cleanup on failure) is entirely untested. | [UNDOCUMENTED] Private helper function that closes an fd and captures the last OS error. No `///` doc comment. Tolerated for private items with clear names. (deliberated: confirmed — Duplication is genuine — identical 5-line function exists in linux_mq.rs. Both capture errno before close(fd) to prevent clobbering. Could be extracted to a shared internal utility module (e.g., linux_common.rs). Tests WEAK retained — no test file for this module.) |
| `make_sockaddr_in` | L61–L70 | WEAK | 85% | [USED] Helper function called 3 times in configure_interface() to construct sockaddr_in structs | [DUPLICATE] Identical 10-line function in linux_mq.rs with score 0.973 | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. On a little-endian host, from_ne_bytes([a,b,c,d]) produces the integer 0xdcba…, which when stored in memory LE lays down bytes [a,b,c,d] — preserving network (big-endian) byte order in memory exactly as the kernel expects. | [LEAN] Encapsulates boilerplate sockaddr_in initialisation used three times in configure_interface; avoids repetition and ensures sin_family/sin_zero are always correctly set. | [NONE] No test file found for rustguard-tun/src/linux.rs. make_sockaddr_in has zero test coverage. The byte-order conversion (u32::from_ne_bytes) and struct initialization are not validated by any test. | [UNDOCUMENTED] Private helper constructing a `sockaddr_in` from an `Ipv4Addr`. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 10-line function in linux_mq.rs constructing libc::sockaddr_in from Ipv4Addr with identical field initialization. Same semantic contract in both files, prime candidate for shared module extraction. Tests WEAK retained.) |
| `set_name` | L72–L76 | WEAK | 85% | [USED] Helper function used in create() and configure_interface() to copy interface name into buffers | [DUPLICATE] Identical 5-line function in linux_mq.rs with score 0.968 | [OK] Copies up to IFNAMSIZ-1 bytes, leaving the final byte as the zero from the caller's zero-initialized buffer. Null termination is always present. | [LEAN] Tiny but used at five distinct call sites; centralises the null-termination and length-clamping logic that would otherwise be repeated inline each time. | [NONE] No test file found for rustguard-tun/src/linux.rs. set_name has zero test coverage. Truncation at IFNAMSIZ-1 for long names and NUL-termination behavior are not tested. | [UNDOCUMENTED] Private helper writing a name into a fixed-size `[u8; IFNAMSIZ]` buffer. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 5-line function in linux_mq.rs copying null-terminated name bytes into fixed-size buffer. Same contract, same truncation logic. Tests WEAK retained.) |
| `create` | L78–L122 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for platform module but likely re-exported by parent, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.856 with macos.rs but different semantic contract — Linux opens /dev/net/tun with TUNSETIFF; macOS opens AF_SYSTEM socket with CTLIOCGINFO and connect | [OK] Logic is sound: opens /dev/net/tun, issues TUNSETIFF with the correctly-sized IfreqFlags (40 bytes), extracts the kernel-assigned name via UTF-8 validated scan for the first NUL, and delegates address configuration. All early-exit paths close the fd before returning. | [LEAN] Three-step sequential logic (open device, TUNSETIFF, delegate to configure_interface) with clean resource cleanup on every failure path; no unnecessary abstraction layers. | [NONE] No test file found for rustguard-tun/src/linux.rs. create has zero test coverage. The primary public entry point for TUN device creation is entirely untested, including fd open failure, TUNSETIFF failure, UTF-8 name extraction, and configure_interface error paths. | [UNDOCUMENTED] Public function (`pub fn create`) with no `///` doc comment whatsoever. As the primary public entry point for creating a Linux TUN device it requires at minimum a purpose description, parameter docs for `config`, return value docs, error conditions, and an `# Examples` section. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD → USED: this is the primary public entry point of a platform-specific library module (linux.rs). It's exported for consumption by the parent crate module (likely conditionally compiled via cfg(target_os)). Classic false positive for library crate public APIs with no in-crate callers. Tests NONE and documentation UNDOCUMENTED are both valid — no test file exists and the public function has zero /// doc comments despite being the main API entry point.) |
| `configure_interface` | L124–L189 | NONE | 85% | [USED] Internal helper function called once from create() at line 113 for interface network configuration | [DUPLICATE] RAG score 0.954 with linux_mq.rs variant; both apply identical ioctl sequence (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK, SIOCSIFMTU, SIOCGIFFLAGS, SIOCSIFFLAGS) | [NEEDS_FIX] All three address-setting ioctls (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK) use IfreqAddr, which is 8 bytes short of the kernel's 40-byte ifreq on 64-bit Linux. The kernel reads 40 bytes from the pointer on each call, touching 8 bytes of adjacent stack. Functional in practice for these SIOCS operations but is UB and fails under ASAN. Fix: add _pad: [u8; 8] to IfreqAddr. | [LEAN] Performs six distinct but sequentially dependent ioctl operations on a temporary socket; extracted from create for clarity and testability. Linear complexity matching the task requirements. | [NONE] No test file found for rustguard-tun/src/linux.rs. configure_interface has zero test coverage. All ioctl branches (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK, SIOCSIFMTU, SIOCGIFFLAGS, SIOCSIFFLAGS) and their error paths are untested. | [UNDOCUMENTED] Private function handling ioctl calls for address/MTU/flags setup. No `///` doc comment. Tolerated for private items, though a note on the temporary SOCK_DGRAM socket pattern would help maintainers. (deliberated: confirmed — Correction NEEDS_FIX confirmed: all three address-setting ioctls pass IfreqAddr (32 bytes) but kernel reads 40 bytes via copy_from_user, causing UB. Duplication confirmed at ~95% with linux_mq.rs — same 6-ioctl sequence. Tests NONE and documentation UNDOCUMENTED both valid. Private function so UNDOCUMENTED is tolerable but the function's complexity (65 lines, 6 ioctls) warrants at least a brief note.) |
| `read` | L193–L199 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for TUN read but likely re-exported by parent module, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.777 below threshold (0.82); macos.rs read() has different logic (strips 4-byte AF header), this version reads directly | [OK] libc::read returns ssize_t; negative check is correct; n as usize is safe after the guard. | [LEAN] Minimal safe wrapper around libc::read translating return value to io::Result<usize>; appropriate boundary between safe and unsafe code. | [NONE] No test file found for rustguard-tun/src/linux.rs. read has zero test coverage. The error path (n < 0) and successful read path are both untested. | [PARTIAL] Public function has two `///` lines describing purpose and the IFF_NO_PI behaviour. Missing: `fd` and `buf` parameter descriptions, explicit return-value docs (number of bytes read), error conditions, and a `# Examples` section expected on public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: public library API, same rationale as create. Documentation reclassified UNDOCUMENTED → PARTIAL: source code at lines 193-194 has two /// doc comment lines describing purpose and IFF_NO_PI behavior. Missing parameter docs and return value docs, hence PARTIAL not DOCUMENTED. Tests NONE retained — no test file exists.) |
| `write` | L203–L212 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for TUN write but likely re-exported by parent module, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.830 meets threshold but source code has different semantic contract — linux.rs writes packet directly; macos.rs prepends AF family header and adjusts return value | [OK] Empty-packet guard is correct. libc::write return value is checked for < 0 before casting to usize. No correctness issues. | [LEAN] Safe wrapper around libc::write with a cheap empty-packet guard; the guard prevents a kernel write(2) call that would return an error anyway, making the error message clearer. | [NONE] No test file found for rustguard-tun/src/linux.rs. write has zero test coverage. The empty-packet guard (InvalidInput error), the libc::write failure path, and the happy path are all untested. | [PARTIAL] Public function has two `///` lines covering purpose and the no-header contract. Missing: `fd` and `packet` parameter descriptions, return-value docs (bytes written), error conditions (documents the empty-packet check implicitly but not explicitly), and a `# Examples` section. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: public library API, same rationale as create and read. Documentation reclassified UNDOCUMENTED → PARTIAL: source code at lines 203-204 has two /// doc comment lines covering purpose and no-header contract. Missing parameter docs and error condition docs, hence PARTIAL. Tests NONE retained.) |

### `rustguard-tun/src/bpf_loader.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `XdpProgram` | L37–L41 | NONE | 88% | [DEAD] Exported public struct with 0 runtime importers and 0 type-only importers; no other files import or use it. | [UNIQUE] Public struct holding prog_fd, xsks_map_fd, and ifindex. Single definition with unique semantic contract. | [NEEDS_FIX] load_and_attach leaks file descriptors on partial failure. xsks_map_fd is created at L51 but if parse_and_patch_elf (L54), bpf_prog_load (L58), or attach_xdp (L63) subsequently return an error, the function exits via ? before the XdpProgram value is constructed, so its Drop impl never runs and the open kernel fds are never closed. Similarly, if bpf_prog_load succeeds but attach_xdp fails, both xsks_map_fd and prog_fd are leaked. | [LEAN] Clean RAII struct with 3 fields, a constructor, one accessor method, and a Drop impl that properly detaches and closes fds. No unnecessary abstraction. | [NONE] No test file found. The struct and its methods (load_and_attach, register_xsk, Drop) have zero test coverage. Critical paths like XDP attachment, XSKMAP registration, and fd cleanup on drop are entirely untested. | [PARTIAL] Has a struct-level `/// A loaded XDP program with its XSKMAP.` doc comment. However, public fields `prog_fd` and `xsks_map_fd` have no `///` field-level documentation explaining their semantics (e.g., that they are Linux file descriptors, what map type xsks_map_fd represents). No `# Examples` section exists. Qualifies as PARTIAL. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX confirmed: the fd leak in load_and_attach is real — xsks_map_fd (and later prog_fd) can leak if subsequent steps fail via ?. Utility reclassified DEAD→USED: XdpProgram is a pub struct and the primary public API of this BPF loader module. Being exported from a library sub-module means it's intended for consumption by the parent crate or external users. The 0-importer scan likely didn't cover the full crate workspace or the struct is newly integrated. Documentation reclassified UNDOCUMENTED→PARTIAL: the struct has a `/// A loaded XDP program with its XSKMAP.` doc comment at line 37, so it's not fully undocumented; however field-level docs are missing, making PARTIAL accurate. Tests NONE confirmed.) |
| `bpf_create_xskmap` | L93–L122 | NONE | 60% | [USED] Function called from XdpProgram::load_and_attach() on line 55. | [UNIQUE] Creates BPF XSKMAP via syscall. No similar functions found; unique syscall wrapper. | [OK] BpfAttrMapCreate struct layout (four u32 = 16 bytes, #[repr(C)]) matches kernel BPF_MAP_CREATE attr. key_size=4 and value_size=4 are correct for XSKMAP. Syscall return cast to i32 and negative check are correct. | [LEAN] Minimal inline struct + single syscall. Local struct definition is justified because no libbpf is available; no excess indirection. | [NONE] No test file found. This BPF syscall wrapper has zero test coverage including no tests for the error path (fd < 0) or verifying the resulting fd is valid. | [UNDOCUMENTED] Private function with no `///` doc comment. No description of the `max_entries` parameter or the returned fd semantics. Tolerated for private items. (deliberated: confirmed — Tests NONE confirmed — raw BPF syscall wrapper requires root and kernel BPF subsystem, making unit testing difficult but the lack is still notable. Documentation UNDOCUMENTED is technically accurate though tolerated for a private function. No reclassification needed.) |
| `bpf_prog_load` | L124–L175 | NONE | 60% | [USED] Function called from XdpProgram::load_and_attach() on line 61. | [UNIQUE] Loads BPF program with verification logging via syscall. No similar functions found; unique syscall wrapper. | [OK] BpfAttrProgLoad struct: content fields occupy 44 bytes; with _pad=[u8;208] the declared size is 252, but #[repr(C)] rounds up to the struct alignment (8, from u64 fields), giving sizeof = 256 — consistent with the 'Pad to 256 bytes total' comment. All three borrowed pointers (insns, license, log_buf) remain valid across the syscall. insn_cnt = len/8 is correct for 8-byte BPF instructions. | [LEAN] The explicit 208-byte padding and documented field layout are required by the kernel ABI. Verifier log capture on failure is appropriate operational tooling, not gold-plating. | [NONE] No test file found. Complex function handling verifier log extraction and error propagation — happy path and verifier-rejection error path are entirely untested. | [UNDOCUMENTED] Private function with no `///` doc comment. Inline comments within the body explain implementation choices but no external-facing doc exists. Tolerated for private items. (deliberated: confirmed — Tests NONE confirmed — complex syscall wrapper with verifier log handling has zero coverage. Documentation UNDOCUMENTED accurate for a private function; tolerated but the function's complexity (padding, log buffer, attr struct) would benefit from a doc comment. No reclassification.) |
| `bpf_map_update` | L177–L206 | NONE | 60% | [USED] Function called from XdpProgram::register_xsk() on line 82. | [UNIQUE] Updates BPF map element via syscall. No similar functions found; unique syscall wrapper. | [OK] BpfAttrMapElem with #[repr(C)]: map_fd(u32) at 0, 4-byte compiler padding, key(u64) at 8, value(u64) at 16, flags(u64) at 24 — matches kernel bpf_attr layout for BPF_MAP_UPDATE_ELEM. Key and value pointers are valid during syscall. Return checked correctly. | [LEAN] Minimal syscall wrapper with a correctly sized attr struct; straightforward. | [NONE] No test file found. BPF map update syscall wrapper with zero test coverage for both success and error paths. | [UNDOCUMENTED] Private function with no `///` doc comment. Parameters and BPF_ANY flag semantics are unexplained at the symbol level. Tolerated for private items. (deliberated: confirmed — Tests NONE confirmed — BPF map update syscall requires kernel subsystem. Documentation UNDOCUMENTED accurate, tolerated for private item. No reclassification.) |
| `attach_xdp` | L210–L233 | NONE | 75% | [USED] Function called from XdpProgram::load_and_attach() on line 68. | [UNIQUE] Tries ip command first, falls back to attach_xdp_netlink. Different semantic contract from attach_xdp_netlink: try-fallback pattern vs direct implementation. Not interchangeable despite 0.841 similarity—different code paths and responsibilities. | [OK] The `ip link ... xdpgeneric pinned /proc/self/fd/N` path may silently fail on older iproute2 (pinned expects a BPF-fs path, not a /proc fd), but the fallback to attach_xdp_netlink handles this correctly. The overall logic is sound. | [OVER] The primary path shells out to `ip link set dev <name> xdpgeneric pinned /proc/self/fd/<fd>`, which is not valid iproute2 XDP syntax (pinned expects a bpffs path, not a /proc/self/fd reference). In practice this path always fails, making it dead code that adds process-spawning overhead and fragility in a low-level system library. The raw netlink fallback (`attach_xdp_netlink`) is the only path that actually works, so the outer wrapper provides no real benefit while adding complexity. | [NONE] No test file found. The ip-command-then-netlink fallback logic is completely untested; neither the success branch nor the fallback path is exercised. | [UNDOCUMENTED] Private function with no `///` doc comment. Has extensive inline `//` comments explaining its strategy and fallback, but no external doc comment. Tolerated for private items. (deliberated: confirmed — Overengineering OVER confirmed: the `ip link set ... xdpgeneric pinned /proc/self/fd/N` path uses invalid iproute2 syntax (pinned expects a bpffs path, not a procfs fd). This primary path effectively always fails, making the wrapper dead code that adds process-spawning overhead before falling back to the netlink path. The function should just call attach_xdp_netlink directly. Tests NONE and documentation UNDOCUMENTED confirmed. Confidence raised slightly since the OVER finding is well-supported.) |
| `attach_xdp_netlink` | L235–L342 | NONE | 80% | [USED] Called from attach_xdp() on line 231 and from detach_xdp() on line 346. | [UNIQUE] Direct netlink RTM_SETLINK implementation for XDP attachment. Different from attach_xdp despite 0.841 similarity: full 108-line implementation vs wrapper that delegates to this. Called from both attach_xdp and detach_xdp; serves as the core implementation. | [NEEDS_FIX] Two correctness issues: (1) The NLMSG_ERROR payload guard checks `n >= 16` but then reads resp[16..20] (the 4-byte error code) — NLMSG_ERROR requires at least 20 bytes. When 16 <= n < 20 (malformed kernel message) the code reads zero-initialized stack bytes and incorrectly returns Ok(()), silently swallowing the error. The guard must be `n >= 20`. (2) `libc::close(sock)` is called unconditionally before `io::Error::last_os_error()` on the recv-failure path; if close() itself fails (e.g., EINTR), it overwrites errno and the returned error describes the close failure rather than the recv failure. | [LEAN] Hand-crafted RTM_SETLINK/IFLA_XDP netlink message construction is inherently verbose. Given the explicit no-libbpf constraint, this is the necessary domain complexity. A dead import (`use std::os::unix::io::FromRawFd`) is present but that is a code quality issue, not overengineering. | [NONE] No test file found. The most complex function in the file — hand-crafted netlink message construction, ACK parsing, and NLMSG_ERROR handling — has zero test coverage. | [UNDOCUMENTED] Private function with no `///` doc comment. Inline comments document the netlink message layout in detail but there is no doc comment explaining the function's contract or the meaning of passing fd=-1 for detach. Tolerated for private items. (deliberated: confirmed — Correction NEEDS_FIX confirmed with two valid issues: (1) NLMSG_ERROR guard checks `n >= 16` but reads resp[16..20], so when 16 ≤ n < 20 it reads zero-initialized bytes and returns Ok(()) silently swallowing errors — guard must be `n >= 20`. (2) On recv failure, `libc::close(sock)` runs before `io::Error::last_os_error()`, allowing close to clobber errno. Both are genuine bugs in error handling paths. Tests NONE confirmed — the most complex function in the file has zero coverage. Documentation UNDOCUMENTED confirmed. Confidence raised from 60 to 80 as both issues are verifiable from the source.) |
| `detach_xdp` | L344–L346 | NONE | 85% | [USED] Function called from Drop implementation on line 89. | [UNIQUE] Trivial 1-line wrapper that calls attach_xdp_netlink(ifindex, -1). Intentional API wrapper for clarity, not a duplicate. | [OK] Passes fd=-1 to attach_xdp_netlink, which is the correct Linux convention for detaching an XDP program via RTM_SETLINK/IFLA_XDP_FD. | [LEAN] One-line wrapper reusing attach_xdp_netlink with fd=-1 to detach; perfectly minimal. | [NONE] No test file found. Thin wrapper around attach_xdp_netlink with fd=-1; no tests exist. | [UNDOCUMENTED] Private one-line wrapper with no `///` doc comment. The inline comment `// fd=-1 detaches` inside the body is not a doc comment. Tolerated for private items. (deliberated: reclassified: tests: WEAK → NONE — Tests reclassified WEAK→NONE: the detail text explicitly states 'No test file found' with zero test coverage. The merged field value of WEAK contradicts its own detail. NONE is the correct classification — this one-line wrapper has no tests at all.) |
| `ifindex_to_name` | L348–L357 | NONE | 85% | [USED] Function called from attach_xdp() on line 224. | [UNIQUE] Converts interface index (u32) to interface name (String). Semantically inverse operation from if_nametoindex (which converts name to index). Different contracts, not duplicates, despite 0.757 similarity. | [OK] Correctly calls if_indextoname, checks the null return, locates the null terminator, and decodes UTF-8. unwrap_or(16) for the missing-null fallback is safe since IFNAMSIZ=16 is the buffer size. | [LEAN] Thin safe wrapper around libc::if_indextoname with proper null terminator handling; appropriately minimal. | [NONE] No test file found. The null-return error path and the invalid UTF-8 error path are entirely untested. | [UNDOCUMENTED] Private utility function with no `///` doc comment. Function name is self-descriptive but no doc comment explains error conditions or buffer size constraints. Tolerated for private items. (deliberated: reclassified: tests: WEAK → NONE — Tests reclassified WEAK→NONE: the detail text explicitly states 'No test file found. The null-return error path and the invalid UTF-8 error path are entirely untested.' The merged WEAK field contradicts the NONE assessment in the detail. NONE is correct — no test file exists for this function.) |
| `parse_and_patch_elf` | L361–L470 | NONE | 75% | [USED] Function called from XdpProgram::load_and_attach() on line 57. | [UNIQUE] Comprehensive ELF parser that extracts program bytecode, processes relocations, and patches map fd references. No similar functions found; unique complex utility. | [NEEDS_FIX] Inside the section-parsing loop, a first binding `let sh_info = u32::from_le_bytes(sh[28..32]...)` reads bytes 28–31, which are the upper half of the sh_offset field (bytes 24–31) in Elf64_Shdr — the wrong offset. This dead binding is immediately shadowed by the correct `let sh_info = u32::from_le_bytes(sh[44..48]...)`. Because only the second binding is used, runtime behavior for a well-formed ELF is unaffected, but the erroneous first declaration (acknowledged by the self-correction comment) is live dead code that will produce a spurious 'unused variable' compiler warning and could mislead a maintainer who removes the second binding. | [ACCEPTABLE] ~110 lines of custom ELF64 parsing reimplements what the `goblin` crate (widely adopted Rust ELF parser, >5M weekly downloads) provides natively. However, the file header explicitly justifies this: the object is 1.4KB, static, and never changes, making a full parser dependency disproportionate. The implementation also contains a dead `sh_info` binding (first computed at offset 28..32 then immediately shadowed by the correct offset 44..48), which is a minor quality issue. NIH is present but the stated constraint makes it ACCEPTABLE rather than OVER. | [NONE] No test file found. This is the most unit-testable function in the file (pure byte-slice in, Vec<u8> out, no syscalls) yet has zero coverage. Critical paths — invalid magic, wrong e_type/e_machine, missing 'xdp' section, relocation patching of LD_IMM64 instructions — are all untested. | [UNDOCUMENTED] Private function with no `///` doc comment. This is the most complex function in the file (110 lines of ELF parsing and BPF relocation patching) yet has no doc comment explaining its preconditions, the ELF subset it accepts, the relocation types it handles, or the patching semantics. Tolerated for private items but notable omission. (deliberated: confirmed — Correction NEEDS_FIX confirmed: the dead `let sh_info = u32::from_le_bytes(sh[28..32]...)` at line ~401 reads wrong ELF header bytes and is immediately shadowed by the correct binding at sh[44..48]. Not a runtime bug since the shadow takes effect, but it's live dead code that produces a compiler warning and could mislead maintainers. Overengineering ACCEPTABLE confirmed: 110-line custom ELF parser is justified by the file header's rationale (1.4KB static object, no dependency needed). Tests NONE confirmed — this is the most unit-testable function in the file (pure byte-slice in, Vec out, no syscalls) yet has zero coverage; this is a significant omission. Documentation UNDOCUMENTED confirmed for this complex 110-line function. Confidence raised from 60 to 75.) |

### `rustguard-crypto/src/aead.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `xseal` | L48–L54 | NONE | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Encrypts with XChaCha20-Poly1305 (24-byte nonce) for cookie encryption, intentionally different from seal which uses ChaCha20-Poly1305 with 12-byte counter-derived nonce. Different cipher algorithm and nonce handling. | [OK] nonce is &[u8; 24] and XNonce is 24 bytes; from_slice will not panic. XChaCha20-Poly1305 API usage is correct. | [LEAN] Separate function for XChaCha20 cookie encryption with a 24-byte nonce is justified — it's a distinct cipher variant used for a specific protocol purpose (cookie encryption), not a redundant abstraction. | [NONE] No test in the inline test module or any referenced test file exercises xseal. Neither happy-path encryption nor authentication-failure paths are covered. | [PARTIAL] Single-line doc comment mentions 24-byte nonce and cookie-encryption use case. No parameter descriptions, no return-value documentation, and no `# Examples` section. Very terse for a public API function. (deliberated: confirmed — Tests NONE is confirmed — the test module contains no test that calls xseal, neither directly nor via a round-trip with xopen. This is a real gap for a public crypto function. Documentation PARTIAL is correct — the single-line comment is terse but at least mentions the 24-byte nonce and cookie-encryption use case. Confidence raised slightly as the test finding is unambiguous from the provided test code.) |
| `xopen` | L57–L63 | NONE | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Decrypts with XChaCha20-Poly1305 (24-byte nonce) for cookie decryption, intentionally different from open which uses ChaCha20-Poly1305 with counter-derived nonce. Different cipher algorithm and nonce handling. | [OK] Symmetric counterpart to xseal; same nonce sizing guarantee applies. Authentication failure returned as None. | [LEAN] Matching decrypt for xseal. Protocol-driven distinction from open is appropriate and keeps the two cipher families clearly separated. | [NONE] No test exercises xopen. No round-trip with xseal, no wrong-key/wrong-nonce/tampered checks are present. | [PARTIAL] Single-line doc comment mirrors `xseal` but for decryption. No parameter descriptions, no documentation of `None` failure semantics beyond what is implied by the name, no `# Examples` section. (deliberated: confirmed — Tests NONE is confirmed — no test exercises xopen anywhere in the provided test module. Documentation PARTIAL mirrors xseal assessment. Both findings are well-supported by the source code.) |
| `seal_to` | L68–L84 | NONE | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Encrypts ChaCha20-Poly1305 in-place (zero allocations), returns usize. Distinct semantic contract from seal: writes to pre-allocated buffer instead of allocating Vec. Different return type, different caller expectations, different invariants. | [OK] Buffer length is asserted before use. Plaintext is copied into buf, encrypted in-place, and the 16-byte tag is appended at buf[len..len+16]. Returned length is correct. Empty AAD matches the companion open_to, so the pair is internally consistent. | [LEAN] Zero-allocation in-place variant is justified for hot-path packet processing in a VPN tunnel. The additional complexity over seal is warranted by the explicit performance requirement stated in the doc comment. | [NONE] No test exercises the zero-allocation in-place encryption path. The assert!(buf.len() >= ...) panic path, correct output length, and tag placement are all untested. | [PARTIAL] Three-line doc comment explains return value (total length), buffer-size precondition, and zero-allocation property. Missing formal parameter descriptions and a `# Examples` section. The panic condition from the `assert!` is not documented in the doc comment. (deliberated: confirmed — Tests NONE is confirmed — no test in the test module exercises seal_to. The assert! panic path, output length correctness, and tag placement are all untested. Documentation PARTIAL is fair — the three-line doc comment covers return value, buffer precondition, and zero-allocation property, but the panic condition from the assert! is undocumented and param descriptions are missing.) |
| `open_to` | L89–L102 | NONE | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Decrypts ChaCha20-Poly1305 in-place (zero allocations), returns Option<usize>. Distinct semantic contract from open: writes to pre-allocated buffer instead of allocating Vec. Different return type, different caller expectations, different invariants. | [OK] Guards ct_len >= AEAD_TAG_LEN and ct_len <= buf.len() before any indexing. split_at_mut(pt_len) yields exactly AEAD_TAG_LEN bytes for the tag slice, so Tag::from_slice cannot panic. Plaintext length is returned correctly on success. | [LEAN] Zero-allocation in-place decryption mirrors seal_to. The split_at_mut pattern to isolate tag bytes is idiomatic Rust and necessary for the AeadInPlace API. Appropriate complexity. | [NONE] No test exercises open_to. Neither the happy path (successful in-place decryption) nor the early-return guards (ct_len < AEAD_TAG_LEN, ct_len > buf.len()) are covered. | [PARTIAL] Three-line doc comment explains buffer layout (ciphertext+tag in, plaintext out), return value, and zero-allocation property. Missing formal parameter descriptions, no `# Examples` section, and the `ct_len < AEAD_TAG_LEN` early-return condition is not explicitly documented. (deliberated: confirmed — Tests NONE is confirmed — no test exercises open_to. Neither the happy path nor the early-return guard paths are covered. Documentation PARTIAL is fair — the doc comment explains buffer layout and zero-allocation property but omits the early-return condition for ct_len < AEAD_TAG_LEN and param descriptions.) |
| `build_nonce` | L105–L109 | WEAK | 78% | [USED] Non-exported internal function called 4 times within file: in seal, open, seal_to, open_to | [UNIQUE] Private helper that builds WireGuard nonce from counter (4 zero bytes + 8-byte LE counter). No similar functions found in RAG search results. | [OK] WireGuard specifies nonce = 4 zero bytes || 8-byte little-endian counter. nonce[4..] is exactly 8 bytes and to_le_bytes() produces 8 bytes; construction is correct. | [LEAN] Minimal private helper encoding the WireGuard nonce format (4 zero bytes || 8-byte LE counter). Four lines, single responsibility, well-commented at call sites. | [WEAK] Private helper exercised indirectly through seal/open tests; wrong_counter_fails implicitly validates that distinct counter values produce distinct nonces. However the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted, and seal_to/open_to (which also call build_nonce) have no tests at all. | [DOCUMENTED] Private function with a clear `///` comment describing the WireGuard nonce layout (4 zero bytes || 8-byte LE counter). For a private helper, this is sufficient; no examples are expected per private-item leniency. (deliberated: confirmed — Tests WEAK is reasonable. The function is exercised indirectly through seal/open tests, and wrong_counter_fails implicitly validates distinct counters produce distinct nonces. However, the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted. For a private helper, this indirect coverage provides some assurance but not full confidence in the byte layout. Confidence raised slightly as the indirect coverage is real, but the layout-specific gap remains valid.) |

### `rustguard-tun/src/macos.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `close_and_error` | L79–L83 | WEAK | 75% | [USED] Called 4 times to safely preserve OS error before closing file descriptor (L111, L127, L142, L165) | [DUPLICATE] Identical implementation found in linux.rs and linux_mq.rs with scores 0.977 and 0.979. Captures errno, closes fd, returns error—same logic and semantic contract across all three platform modules. | [OK] Correctly captures errno before calling close() to prevent the close() syscall from clobbering errno. | [LEAN] Solves a real, documented problem: libc::close() can clobber errno, so the error must be captured first. The comment explains the invariant clearly. Not abstraction for its own sake. | [NONE] No test file exists for this module. This private fd-cleanup helper has zero test coverage. The errno-before-close ordering is untested. | [PARTIAL] Private function with three `///` lines (lines 76–78) explaining purpose ('Capture the OS error, close the fd'), motivation ('avoids close() clobbering errno'), and a Safety precondition. Adequate for a private unsafe helper. No formal `# Parameters` or `# Examples`, but those are not expected for private functions. (deliberated: confirmed — Duplication DUPLICATE is technically accurate — identical 4-line implementations exist in linux.rs and linux_mq.rs. However, these are platform-gated modules (only one compiles per target), so deduplication into a shared utility is a low-value refactor. Keeping DUPLICATE but lowering confidence to reflect that cross-platform-module duplication is a weaker signal. Documentation PARTIAL is accurate: the three /// lines adequately document purpose and safety for a private unsafe helper.) |
| `make_sockaddr_in` | L85–L93 | WEAK | 65% | [USED] Called 3 times in configure_address() to construct SockaddrIn for address configuration (L205-207) | [UNIQUE] High RAG similarity (0.968) but returns custom SockaddrIn struct; linux variants return libc::sockaddr_in. Different struct layouts and initialization logic—macos uses sin_len field, linux uses u32::from_ne_bytes conversion. Different semantic contracts due to struct type differences. | [OK] Correctly constructs a Darwin sockaddr_in with sin_len=16, AF_INET family, zero port, and octets from Ipv4Addr. | [LEAN] Used three times in configure_address() for addr, broadaddr, and mask. DRY helper with no generics or unnecessary indirection; appropriate factoring. | [NONE] No test file exists for this module. make_sockaddr_in is a pure function (Ipv4Addr -> SockaddrIn) that is trivially unit-testable without kernel access, yet has zero test coverage. Field values including sin_len, sin_family, and sin_addr byte ordering are never asserted. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Name and single `Ipv4Addr` parameter are self-describing. Tolerated under private-item leniency. (deliberated: confirmed — Tests WEAK kept. This is a pure function (Ipv4Addr → SockaddrIn) with actual struct construction logic including sin_len=16, sin_family assignment, and octet mapping. It is trivially unit-testable without kernel access, yet has zero coverage. WEAK is appropriate rather than NONE since the function's simplicity reduces the risk.) |
| `create` | L95–L192 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. May be part of library crate public API consumed externally or by binary crates not captured in workspace analysis. Known false positives for library-entry-point functions suggest caution. | [UNIQUE] High structural similarity to linux.rs create (0.856), but platform-specific implementations differ fundamentally: macOS uses kernel control socket + connect(), Linux uses /dev/net/tun + TUNSETIFF ioctl. Different control flow, syscalls, and helper functions (configure_address vs configure_interface). Different semantic contracts for device creation methods. | [OK] Logic is correct: socket → CTLIOCGINFO → connect with sc_unit=N+1 for utunN or 0 for auto → getsockopt for name → configure address and MTU. The name_len < 2 guard correctly prevents underflow in the name_len-1 slice. The name_buf slice upper bound is safe because getsockopt caps the returned length at the supplied socklen (16). fcntl return value is unchecked but this is a non-fatal omission. | [LEAN] The six-step sequence (socket → CTLIOCGINFO → connect → getsockopt → configure_address → set_mtu) directly mirrors the Darwin kernel utun creation protocol. Each step is mandatory and the code faithfully follows the kernel contract without unnecessary layering. | [NONE] No test file exists for this module. The exported create() function requires a live macOS kernel and utun capability; integration tests would be needed. All code paths — socket creation, CTLIOCGINFO ioctl, connect, getsockopt name retrieval, configure_address delegation, set_mtu delegation, and all error branches — have zero test coverage. | [UNDOCUMENTED] Public function (`pub fn create`) with no `///` doc comment whatsoever. This is the primary public entry point for creating a utun device — the most critical symbol in the module. Missing: purpose statement, parameter semantics (`TunConfig` fields consumed), return value, error conditions (socket failure, ioctl failure, bad name format), platform requirements, and `# Examples`. Significant documentation gap. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD → USED. This is the primary public entry point of the macos.rs platform module, which is conditionally compiled via #[cfg(target_os = "macos")] and re-exported through lib.rs. Workspace analysis cannot trace cfg-gated platform module consumers — this is a textbook false positive for platform-specific library crate entry points. Tests NONE kept: requires live macOS kernel. Documentation UNDOCUMENTED confirmed: pub fn create has no /// doc comment, which is a legitimate gap for the module's main public API.) |
| `configure_address` | L194–L218 | NONE | 88% | [USED] Called from create() to configure interface IP address via ioctl (L180) | [UNIQUE] Similar to linux configure_interface (0.852 score) but uses different ioctl approaches: macOS uses single SIOCAIFADDR ioctl with IfAliasReq struct to set addr+broadcast+netmask. Linux uses three separate ioctls (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK) with IfreqAddr. Different struct types and ioctl semantics. | [NEEDS_FIX] close() is called before checking whether the ioctl returned an error. If close() modifies errno (e.g. EINTR on macOS), the subsequent last_os_error() call returns the wrong error. The code already contains close_and_error() to solve exactly this problem, but it was not used here. The fix is to capture the ioctl errno before calling close(), mirroring the pattern in close_and_error. | [LEAN] Single-responsibility: sets the interface IP address via SIOCAIFADDR. Correct separation from set_mtu since the two use different ioctl structs and codes. No unnecessary abstraction. | [NONE] No test file exists for this module. configure_address is a private function requiring a live AF_INET socket and SIOCAIFADDR ioctl capability. Both the success and failure paths have zero test coverage. | [UNDOCUMENTED] Private function with no `///` doc comment. Role is inferable from name and call site. Tolerated under private-item leniency. (deliberated: confirmed — Correction NEEDS_FIX confirmed with higher confidence. Lines 212-214 clearly show libc::close(sock) called before the `if ret < 0` check. If close() clobbers errno (e.g., EINTR on macOS), last_os_error() returns the wrong error. The codebase already has close_and_error() solving this exact problem, making the fix trivial. Tests NONE accurate. Documentation UNDOCUMENTED tolerated for private function.) |
| `set_mtu` | L220–L242 | NONE | 88% | [USED] Called from create() to set interface MTU via ioctl (L186) | [UNIQUE] No similar functions found in RAG results. Unique macOS implementation for MTU configuration via SIOCSIFMTU ioctl. | [NEEDS_FIX] Same errno-clobbering defect as configure_address: libc::close(sock) is called before the `if ret < 0` check, risking that close() overwrites the ioctl errno. The correct pattern is to save the error before closing (as done in close_and_error). | [LEAN] Single-responsibility: sets MTU via SIOCSIFMTU. Follows the same open/ioctl/close pattern as configure_address but with a distinct struct; merging them would require generics or enums that add more complexity than they remove. | [NONE] No test file exists for this module. set_mtu is a private function requiring SIOCSIFMTU ioctl. Success and ioctl-failure paths have zero test coverage. | [UNDOCUMENTED] Private function with no `///` doc comment. Name and parameters are self-descriptive. Tolerated under private-item leniency. (deliberated: confirmed — Correction NEEDS_FIX confirmed with higher confidence. Identical errno-clobbering pattern as configure_address at lines 237-239: close(sock) before the ret < 0 check. Same fix applies. Tests NONE accurate. Documentation UNDOCUMENTED tolerated for private function.) |
| `read` | L245–L263 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-specific module may be re-exported or conditionally imported; workspace analysis may miss binary crate consumers. | [UNIQUE] RAG similarity to linux read is 0.777 (below 0.82 threshold). Different semantic contract: macOS version strips utun's 4-byte AF header with buffer expansion; Linux reads directly (IFF_NO_PI means no packet info header). Validation logic differs—macOS checks minimum 4-byte header presence. | [OK] Temporary buffer is sized buf.len()+4 so payload_len = n-4 ≤ buf.len(); the copy_from_slice cannot panic. The n < 4 guard prevents underflow. Correctly strips the 4-byte utun AF header. | [LEAN] Correct and minimal implementation for stripping the utun 4-byte AF header. The temporary Vec allocation per call is a performance trade-off, not overengineering. Logic is straightforward with appropriate error handling. | [NONE] No test file exists for this module. The exported read() function contains testable logic: the 4-byte AF header stripping, the n<4 too-short guard, and the payload copy. None of these branches are covered by any test. | [PARTIAL] Public function with a single-line `///` comment ('Read an IP packet from the utun fd, stripping the 4-byte AF header'). Explains the AF-header stripping behavior, which is non-obvious. Missing: `fd` and `buf` parameter descriptions, return value semantics (the usize is the IP payload byte count), error conditions (`InvalidData` on short packet), and `# Examples` section expected for public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: same rationale as create — pub fn in a cfg-gated platform module, consumed via lib.rs re-export. Documentation reclassified UNDOCUMENTED → PARTIAL: the source clearly shows `/// Read an IP packet from the utun fd, stripping the 4-byte AF header.` at line 245. This is a meaningful single-line doc comment explaining the non-obvious AF header stripping behavior. Missing parameter/return docs keep it at PARTIAL rather than DOCUMENTED. Tests NONE accurate.) |
| `write` | L266–L296 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-specific module may be re-exported or conditionally imported; workspace analysis may miss binary crate consumers. | [UNIQUE] RAG similarity to linux write is 0.830, but logic differs significantly: macOS version inspects IP version nibble, determines AF_INET/AF_INET6, and prepends 4-byte AF header. Linux writes directly without header. macOS adjusts return value by subtracting 4 bytes. Different semantic contracts for packet transmission. | [OK] AF header [0,0,0,af] is correct for macOS utun (big-endian AF value). IP version dispatch via packet[0]>>4 is standard. saturating_sub(4) handles partial-write edge cases without panic. Empty-packet guard is correct. | [LEAN] Prepends the AF header by inspecting the IP version nibble, then writes atomically. IP version detection and AF header construction are both inherent to the utun write protocol. No unnecessary abstractions. | [NONE] No test file exists for this module. The exported write() function contains several testable branches: empty-packet rejection, IPv4 header detection (0x40 nibble), IPv6 header detection (0x60 nibble), unknown-version error, and the 4-byte AF header prepend logic. All branches have zero test coverage. | [PARTIAL] Public function with a single-line `///` comment ('Write an IP packet to the utun fd, prepending the 4-byte AF header'). Explains the AF-header prepend, which is non-obvious. Missing: `fd` and `packet` parameter descriptions, return value semantics (saturating_sub(4) meaning), error conditions (`InvalidInput` on empty packet or unknown IP version), and `# Examples` section required for public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: same platform-module reasoning. Documentation reclassified UNDOCUMENTED → PARTIAL: source shows `/// Write an IP packet to the utun fd, prepending the 4-byte AF header.` at line 266. Meaningful doc comment explaining the AF header prepend. Missing parameter/return/error docs keep it PARTIAL. Tests NONE accurate.) |

### `rustguard-kmod/src/cookie.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `hash` | L41–L51 | WEAK | 92% | [USED] Internal helper function called in verify_mac1 (line 101), create_reply (line 126), process_reply (line 154) for BLAKE2s hashing. | [UNIQUE] RAG score 0.937 with noise.rs hash, but implementations differ: this version uses array size 4 and passes uncapped chunks.len() to C function; noise.rs uses array size 8 and passes capped num. Different capacity invariants and capping behavior suggest intentionally different constraints despite semantic similarity. | [NEEDS_FIX] Line 49 passes `chunks.len() as u32` as `num_chunks` to the C function, but the loop at line 44 uses `.take(4)` and populates at most 4 entries in `ptrs` and `lens`. If `chunks.len() > 4`, the C function is told there are more chunks than have been written, causing it to read beyond the bounds of the 4-element Rust arrays — a stack over-read and undefined behavior. The argument must be clamped: `chunks.len().min(4) as u32`. | [LEAN] Thin FFI adapter that avoids repeating unsafe boilerplate at every call site. The hard cap of 4 chunks mirrors kernel-module constraints (no heap). Silent truncation beyond 4 is a mild concern, but the design is minimal and fit-for-purpose. | [WEAK] No direct tests found for this private function. However, prior deliberation established that tests such as cookie_roundtrip() and wrong_mac1_fails_decrypt() exist and exercise CookieChecker::verify_mac1(), which calls hash() internally. Indirect coverage only — hash is never tested with edge-case inputs (empty slice, >4 chunks clipped by take(4), zero-length chunks), and no assertions target hash output directly. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_hash` with a slice-of-slices interface; the chunked hashing behavior and 4-chunk maximum are not explained. (deliberated: confirmed — Correction NEEDS_FIX is confirmed with high confidence: line 44 uses .take(4) populating at most 4 entries in ptrs/lens arrays, but line 49 passes chunks.len() as u32 — the raw unclamped caller count — to the C function. If chunks.len() > 4, the C function reads past the 4-element stack arrays causing out-of-bounds access. All current callers pass exactly 2 chunks so the bug is latent, but the API provides zero enforcement. This is a genuine memory safety defect. Tests WEAK is correct: hash is only exercised indirectly through verify_mac1/create_reply/process_reply; no direct tests for edge cases (>4 chunks, empty input, zero-length chunks).) |
| `mac` | L53–L57 | WEAK | 75% | [USED] Internal helper for BLAKE2s MAC computation, called in verify_mac1 (103), make_cookie (117), verify_mac2 (143), compute_mac2 (189). | [DUPLICATE] RAG score 0.974 with noise.rs mac — identical signature and implementation. Both compute BLAKE2s-256 MAC by calling wg_blake2s_256_mac with same parameter passing and return value extraction. | [OK] Straightforward FFI wrapper for BLAKE2s-MAC. Key, data, lengths, and output pointer are all forwarded correctly with no type mismatches. | [LEAN] One-liner FFI wrapper that eliminates repeated unsafe blocks. No unnecessary generics or abstraction layers. | [WEAK] No direct tests found. Indirectly exercised through CookieChecker::verify_mac1() and make_cookie() in the test suite established by prior deliberation (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie). Edge cases such as empty key, empty data, or mismatched key lengths are never directly validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_256_mac` but the keyed-MAC semantics and return size (32 bytes) are not described. (deliberated: confirmed — Duplication: reported as 100% identical to noise.rs mac function — both are thin wrappers calling wg_blake2s_256_mac with the same parameter pattern. This is a true functional duplicate that belongs in a shared crypto utility module. Tests WEAK: only indirect coverage through CookieChecker methods; no direct tests for empty key, empty data, or edge-case inputs. Both findings maintained.) |
| `random_bytes` | L59–L63 | WEAK | 85% | [USED] Generic random bytes generator called for secret initialization (85), secret rotation (112), and nonce generation (131). | [UNIQUE] RAG score 0.841 with rustguard-core/src/cookie.rs, but different semantic contract: this version is generic <const N: usize>() returning [u8; N]; core version is fixed fn random_bytes() -> [u8; 32]. Also calls different RNG function (wg_get_random_bytes vs fill_random). | [OK] Const-generic buffer is correctly sized and its length is passed as u32. All call sites use small N (24 and 32 bytes), well within u32 range. | [LEAN] Const-generic N produces a zero-cost, type-safe wrapper over the FFI random call. Idiomatic Rust; avoids repeated unsafe boilerplate for different buffer sizes. | [NONE] No test file found for this source file and no direct tests for this function. While CookieChecker::new() calls random_bytes() during test setup, the randomly generated output is inherently non-deterministic and cannot be meaningfully asserted. No smoke tests verifying it doesn't panic or returns the correct length are present. | [UNDOCUMENTED] Private generic function with no `///` doc comment. The kernel RNG source and const-generic size parameter are not documented; leniency applies as private. (deliberated: confirmed — Tests WEAK is appropriate. The function is indirectly exercised via CookieChecker::new() and make_cookie()/create_reply() in tests, but its non-deterministic nature makes direct assertion impractical. No smoke tests exist verifying correct buffer length or non-panic behavior. WEAK (not NONE) is correct since indirect coverage exists through CookieChecker test suite.) |
| `CookieChecker` | L66–L71 | WEAK | 85% | [USED] Primary server-side cookie validation API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module server-side cookie validation state with public key, secret, timestamp, and under-load flag. | [OK] Struct field declarations are correct. Types are appropriate: u64 for nanosecond timestamps, [u8;32] for keys and public values. | [LEAN] Four fields encoding exactly the server-side cookie state described by the WireGuard spec. No superfluous generics, traits, or builder patterns. Methods are small and single-purpose. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, mac2_wrong_source_fails) exercise CookieChecker. Coverage is partial: secret rotation via maybe_rotate_secret() after COOKIE_SECRET_LIFETIME_NS elapses is not tested, create_reply() wire-format correctness is not independently asserted, and under_load flag transitions are not tested. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Server-side: generates cookies and validates MAC2.') but fields lack `///` comments, there are no `# Examples`, and the secret-rotation lifecycle and `under_load` flag semantics are not explained. (deliberated: confirmed — Tests WEAK confirmed: CookieChecker is exercised by multiple tests (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie) but important paths remain untested — secret rotation after COOKIE_SECRET_LIFETIME_NS, under_load flag transitions, and create_reply wire-format correctness. Documentation PARTIAL confirmed: has a one-line /// comment but fields lack documentation, no examples, and the secret rotation lifecycle and thread-safety requirements are undescribed for a security-critical pub(crate) struct.) |
| `CookieState` | L74–L77 | WEAK | 85% | [USED] Primary client-side cookie storage API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module client-side cookie storage with optional decrypted cookie and reception timestamp. | [OK] Option<[u8; COOKIE_LEN]> correctly models presence/absence of a stored cookie. u64 for the receipt timestamp is appropriate. | [LEAN] Two-field client-side struct: the cookie value and its timestamp. Minimal representation; freshness check is a simple subtraction. No unnecessary indirection. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, no_cookie_gives_zero_mac2) exercise CookieState. Coverage is partial: staleness expiry (is_fresh returning false after COOKIE_SECRET_LIFETIME_NS) is not tested; process_reply with an invalid/tampered encrypted payload and the false-return path is also not covered. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Client-side: stores a received cookie for MAC2.') but fields are undocumented, freshness/staleness semantics are not described, and no `# Examples` section is present. (deliberated: confirmed — Tests WEAK confirmed: CookieState is covered by several tests (cookie_roundtrip, no_cookie_gives_zero_mac2) but staleness expiry (is_fresh returning false after timeout) and process_reply with tampered payloads are untested. Documentation PARTIAL confirmed: single-line /// comment present but fields undocumented, freshness semantics unexplained, and no examples for a pub(crate) type.) |
| `constant_time_eq` | L202–L205 | WEAK | 80% | [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, called in verify_mac1 (104) and verify_mac2 (144). | [DUPLICATE] RAG score 0.962 with noise.rs constant_time_eq — identical implementation. Both check length equality then use wg_crypto_memneq for constant-time comparison, with same parameter passing and return logic. | [OK] Correctly delegates to the kernel's crypto_memneq, which is guaranteed constant-time and cannot be optimized away by the compiler. The early-exit length check is safe because all callers pass fixed-length 16-byte slices, so the branch carries no secret-dependent timing information. | [LEAN] Security-critical helper delegating to the kernel's crypto_memneq, which is guaranteed not to be optimized away. Extracting it into a named function documents intent and prevents accidental substitution with a non-constant-time comparison elsewhere. | [WEAK] No direct tests found. Indirectly exercised through verify_mac1() and verify_mac2() in the CookieChecker test suite (established by prior deliberation). Critical edge cases are untested: mismatched-length slices (returns false early without calling wg_crypto_memneq), equal-length but differing slices, and zero-length slices. The constant-time guarantee itself is structurally untestable at the unit level, but behavioral correctness for these edge cases is never directly asserted. | [PARTIAL] Private function has a `///` doc comment noting its constant-time guarantee via `wg_crypto_memneq`. However, parameter semantics (slice length requirement), return-value meaning, and panic/safety conditions are not described. Leniency applies as private. (deliberated: confirmed — Duplication DUPLICATE: reported as 100% identical to noise.rs constant_time_eq — both check length then delegate to wg_crypto_memneq. This is a security-critical utility that absolutely should live in a shared module to prevent divergent copies. Tests WEAK: only exercised indirectly through verify_mac1/verify_mac2; mismatched-length and zero-length edge cases never tested directly. Documentation PARTIAL: has a /// comment noting the constant-time guarantee (good) but parameter semantics and return value meaning are undescribed.) |

### `rustguard-tun/src/linux_mq.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `set_name` | L53–L57 | NONE | 88% | [USED] Called at L100, L122, L233, L242, L257 to copy interface name into ifreq structures | [DUPLICATE] Identical to linux.rs version - copies string bytes to fixed buffer with length constraint | [OK] Copies up to IFNAMSIZ-1 bytes, preserving the zeroes already present beyond the written region. All call sites zero-initialize the buffer before passing it in, guaranteeing a null terminator. Correct. | [LEAN] Focused helper that safely copies a Rust str into a fixed-size C name buffer; used at 5+ call sites, justified. | [NONE] No test file exists for rustguard-tun/src/linux_mq.rs. set_name has zero test coverage despite being a utility with a non-trivial name-truncation edge case (names >= IFNAMSIZ). | [UNDOCUMENTED] Private function, no `///` doc comment. Name and signature (`buf: &mut [u8; IFNAMSIZ], name: &str`) are self-descriptive. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: function is 100% identical to linux.rs counterpart in the same crate; both share the same byte-copy logic with IFNAMSIZ constraint. Extracting to a shared linux_common.rs internal module is straightforward. Tests reclassified WEAK→NONE: the detail explicitly states 'No test file exists' and 'zero test coverage' — WEAK implies partial test coverage exists, but none does.) |
| `make_sockaddr_in` | L59–L68 | NONE | 88% | [USED] Called at L232, L239, L244 to construct sockaddr_in for address, destination, and netmask | [DUPLICATE] Identical to linux.rs version - creates sockaddr_in struct with identical field initialization | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. octets() returns bytes in network (big-endian) order; from_ne_bytes reinterprets them as a native-endian u32 without byte-swapping the underlying bytes. On a little-endian host, from_ne_bytes([192,168,1,1]) = 0x0101a8c0, which when written to memory produces [0xc0,0xa8,0x01,0x01] — the correct network-byte-order in-memory representation. Semantically equivalent to a safe transmute of the octets array. | [LEAN] Simple constructor for sockaddr_in from Ipv4Addr; eliminates repetitive struct initialization across 3 ioctl calls in configure_interface. | [NONE] No test file exists. make_sockaddr_in has zero test coverage; the NE-byte-order conversion for s_addr is untested. | [UNDOCUMENTED] Private function, no `///` doc comment. Name and single `Ipv4Addr` parameter make purpose clear. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical signature and implementation to linux.rs version within the same crate — same sockaddr_in construction with from_ne_bytes. Tests reclassified WEAK→NONE: detail confirms 'No test file exists' and 'zero test coverage'; no tests exist at all.) |
| `close_and_error` | L70–L74 | NONE | 88% | [USED] Called at L106, L128, L237, L242, L247, L261, L266 to close file descriptors and return OS errors | [DUPLICATE] Identical to linux.rs version - captures OS error, closes fd, returns error in same sequence | [OK] Captures last_os_error before closing, preventing close(2) from overwriting errno. Correct ordering. | [LEAN] Two-line helper capturing last_os_error before closing an fd; prevents the common bug of closing fd before reading errno. Used at multiple error-return sites. | [NONE] No test file exists. close_and_error has zero test coverage. | [UNDOCUMENTED] Private function, no `///` doc comment. Name clearly indicates it closes an fd and captures the last OS error. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical to linux.rs version — same errno-capture-before-close pattern. Trivially extractable to a shared module. Tests reclassified WEAK→NONE: detail confirms 'No test file exists' and 'zero test coverage'.) |
| `MultiQueueTun` | L77–L81 | NONE | 90% | [DEAD] Exported public struct with 0 workspace importers per analysis, suggesting incomplete cross-crate usage capture; however, crate name (rustguard-tun) and complete pub API imply external consumption not reflected in analysis | [UNIQUE] Struct specific to multi-queue TUN feature, no RAG duplicates | [NEEDS_FIX] In MultiQueueTun::create, the additional-queues loop calls open_tun_queue(None)? which can propagate an error via ?. At that point fds (Vec<i32>) is dropped by Rust's stack unwinding, but Vec<i32>::drop only frees heap memory — it does not call libc::close on the contained file descriptors. Any fds pushed into the vector before the failing iteration are silently leaked. The ioctl-failure path in the same loop correctly closes all prior fds before returning, but the open_tun_queue failure path (handled by ?) does not. | [ACCEPTABLE] The num_queues field is mildly redundant with fds.len() — queue_fd and num_queues() could use fds.len() directly. However the struct's overall design is appropriate for multi-queue TUN management and the redundancy is harmless. | [NONE] No test file exists for this module. MultiQueueTun::create, read_queue, write_queue, queue_fd, num_queues, name, and Drop are all completely untested. This is the primary public API of the module and requires Linux kernel interaction, but neither unit nor integration tests are present. | [PARTIAL] Public struct has a `///` doc comment ('A multi-queue TUN device with N file descriptors.'). Public methods (create, num_queues, name, queue_fd, read_queue, write_queue) each carry a brief single-line `///` comment. However, no method documents parameters or return values explicitly, and no `# Examples` section exists anywhere on this public API, which is expected for public functions. Documentation is present but incomplete. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX confirmed: fd leak in additional-queues loop at L116 when open_tun_queue(None)? fails via ? operator — Vec<i32>::drop only frees heap memory, silently leaking all previously opened file descriptors. The ioctl failure path below correctly closes all prior fds, but the open failure path does not. Utility reclassified DEAD→USED: this is the primary and sole public API of a library crate (rustguard-tun); the original detail itself acknowledges 'crate name and complete pub API imply external consumption not reflected in analysis'. A library crate's main exported struct is by definition USED. Tests NONE confirmed: no test file exists for this module. Documentation reclassified UNDOCUMENTED→PARTIAL: the source clearly shows '/// A multi-queue TUN device with N file descriptors.' on the struct and all 6 public methods carry single-line `///` comments (create, num_queues, name, queue_fd, read_queue, write_queue). Documentation exists but lacks parameter/return descriptions and examples — this is PARTIAL, not UNDOCUMENTED.) |
| `open_tun_queue` | L195–L204 | NONE | 88% | [USED] Called at L89 and L118 within create() to open file descriptors to /dev/net/tun device | [UNIQUE] No similar functions found by RAG semantic search across codebase | [OK] Opens /dev/net/tun with O_RDWR|O_CLOEXEC and returns the fd or propagates the OS error. The _name parameter is intentionally unused (underscore prefix); naming is deferred to the caller via TUNSETIFF. Correct. | [ACCEPTABLE] The _name: Option<&str> parameter is entirely unused (always called with None) and is a dead premature-generalization slot. Removing it would simplify the signature. However, the core logic is minimal and the dead parameter causes only minor call-site noise. | [NONE] No test file exists. open_tun_queue has zero test coverage; the /dev/net/tun open failure path is untested. | [UNDOCUMENTED] Private unsafe function, no `///` doc comment. Purpose (open /dev/net/tun with O_RDWR|O_CLOEXEC) is evident from body. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Tests reclassified WEAK→NONE: the detail explicitly states 'No test file exists' and 'zero test coverage'. WEAK implies some tests exist but are insufficient; however no tests exist whatsoever for this module.) |
| `configure_interface` | L206–L260 | NONE | 85% | [USED] Called at L136 within create() to set IP address, netmask, MTU, and bring interface up | [DUPLICATE] Very similar to linux.rs version (RAG score 0.954) - identical configuration sequence for address, destination, netmask, MTU, and flags via ioctl calls | [OK] Each error branch calls close_and_error(sock) which closes sock exactly once before returning; the success path closes sock via libc::close at the end. The ioctl sequence (SIOCSIFADDR → SIOCSIFDSTADDR → SIOCSIFNETMASK → SIOCSIFMTU → SIOCGIFFLAGS → SIOCSIFFLAGS|IFF_UP) is correct for address configuration and interface activation. IfreqFlags (40 bytes) is used for the SIOCG/SIOCSIFFLAGS round-trip, so no size-mismatch issue applies there. | [LEAN] Sequentially configures address, destination, netmask, MTU, and brings the interface up — exactly the required steps. Each ioctl is necessary; no abstraction layers are added beyond what the kernel protocol demands. | [NONE] No test file exists. configure_interface has zero test coverage; none of the ioctl call paths (address, destination, netmask, MTU, flags up) are exercised by any test. | [UNDOCUMENTED] Private unsafe function, no `///` doc comment. The function is non-trivial (sets address, destination, netmask, MTU, brings interface up via multiple ioctls). A `///` summary would be warranted, but it is private so the lack is tolerated per private-item leniency rule. (deliberated: confirmed — Duplication DUPLICATE confirmed: 0.954 semantic similarity to linux.rs version with identical ioctl configuration sequence (SIOCSIFADDR→SIOCSIFDSTADDR→SIOCSIFNETMASK→SIOCSIFMTU→SIOCGIFFLAGS→SIOCSIFFLAGS|IFF_UP). Both reside in the same crate and share the same struct types, making extraction to a common internal module feasible. Tests NONE confirmed. Documentation UNDOCUMENTED confirmed: private unsafe function performing 6-step ioctl configuration with no doc comment — while tolerated by private-item leniency, the complexity warrants at minimum a summary comment.) |

## Test Improvements

- [ ] `rustguard-enroll/src/control.rs` — 10 untested
  Create `rustguard-enroll/src/control.test.rs` covering: socket_path, new_window, is_open, open_window, close_window, remaining, start_listener, handle_client, send_command, cleanup

- [ ] `rustguard-cli/src/main.rs` — 4 untested, 6 weak
  Improve `rustguard-cli/src/main.test.rs` covering: main, usage, cmd_up, cmd_serve, cmd_join, cmd_open, cmd_close, cmd_status, cmd_genkey, cmd_pubkey
  - usage: [USED] Called twice in main() at lines 21 and 27. Prints help text for command usage. | [UNIQUE] Prints usage information for CLI. No similar functions found. | [OK] Pure output function; no logic to evaluate. | [LEAN] Simple static help text printed to stderr. One function, one responsibility, no abstraction needed. | [NONE] No test file found. The usage/help output function has no tests verifying correct command listing or formatting. | [UNDOCUMENTED] No /// doc comments. Private helper with a clear name; the body itself serves as implicit documentation of supported commands, so leniency applies. (deliberated: confirmed — Tests WEAK is reasonable — no test file exists but this is a pure eprintln output function with minimal logic. Testing it provides low value. The detail text says NONE but WEAK is a fair merged assessment given the function's trivial nature.)
  - cmd_open: [USED] Called in main() at line 13 via match arm for 'open' command. Handles enrollment window open. | [UNIQUE] Sends OPEN command with duration to control endpoint. No similar functions found. | [OK] Silently defaults to 60 on unparseable input; this matches the documented behavior (default 60s). The OPEN command is formatted and sent correctly. | [LEAN] Parses an optional integer argument with a sensible default and sends a single control command. Minimal and clear. | [NONE] No test file found. Default seconds fallback (60), custom seconds parsing, and control command error path are untested. | [UNDOCUMENTED] No /// doc comments. Private helper; the default-60-second fallback and the IPC mechanism used (control socket) are not described. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists but the function is a thin wrapper around control::send_command with a simple default-seconds fallback. Testing value is moderate.)
  - cmd_close: [USED] Called in main() at line 14 via match arm for 'close' command. Handles enrollment window close. | [UNIQUE] Sends CLOSE command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Single call to send_command with error handling. Could not be simpler for its purpose. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. 9-line function that directly delegates to control::send_command. No test file exists but function is trivially simple.)
  - cmd_status: [USED] Called in main() at line 15 via match arm for 'status' command. Handles status query. | [UNIQUE] Sends STATUS command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Identical structure to cmd_close; single control command dispatch. Appropriately minimal. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. Structurally identical to cmd_close — pure delegation to control::send_command. No test file exists.)
  - cmd_genkey: [USED] Called in main() at line 16 via match arm for 'genkey' command. Generates cryptographic key. | [UNIQUE] Generates random private key and encodes as base64. No similar functions found. | [OK] Generates a random StaticSecret, encodes bytes as base64, and prints. No correctness issues. | [LEAN] Generates a random static secret and base64-encodes it in four lines. Exactly as complex as the task demands. | [NONE] No test file found. Key generation and base64 encoding output are untested. | [UNDOCUMENTED] No /// doc comments. Private; the output format (base64-encoded 32-byte Curve25519 scalar) is implied but not stated. (deliberated: confirmed — Tests WEAK is reasonable. 5-line function generating a key and encoding it. No test file exists. The crypto correctness is in the library, not here.)
  - cmd_pubkey: [USED] Called in main() at line 17 via match arm for 'pubkey' command. Derives public key from private key. | [UNIQUE] Reads base64-encoded private key from stdin and derives public key. No similar functions found. | [OK] Reads a line, trims whitespace, decodes base64, coerces Vec<u8> to [u8; 32] via TryInto (panics with a clear message on wrong length), derives the public key, and prints base64. All error paths use expect() which is appropriate for a CLI tool. No logic bugs. | [LEAN] Reads a base64 private key from stdin, derives the public key, and prints it. Sequential and direct with no unnecessary abstraction. | [NONE] No test file found. stdin reading, base64 decode, 32-byte length validation, and public key derivation are all untested. | [UNDOCUMENTED] No /// doc comments. Private; the expected stdin format (base64 line), key length constraint (32 bytes), and output format are not documented, which could mislead callers piping data. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists. The function does have more logic (stdin read, base64 decode, length check, key derivation) than the simpler commands, so WEAK correctly captures that there's meaningful untested logic but it's a CLI tool where integration tests are the norm.)

- [ ] `rustguard-daemon/src/config.rs` — 7 weak
  Improve `rustguard-daemon/src/config.test.rs` covering: Config, CidrAddr, decode_key, parse_interface, parse_peer, parse_cidr, prefix_to_netmask
  - Config: [USED] Main exported struct holding parsed WireGuard configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Public struct with unique field composition (interface, peers); no duplicate definitions found | [NEEDS_FIX] Config::parse (L120-L122) sets current_section to 'interface' but never flushes current_peer_kvs when a [Interface] header arrives while parsing a [Peer] section. If any [Peer] block precedes [Interface], its accumulated KVs are not cleared; the next [Peer] header's flush guard (current_section == Some('peer')) is then false, so those stale KVs merge with the next peer's KVs. The end-of-file flush emits a single peer that silently drops the first peer's data (last-writer-wins for duplicated keys like PublicKey). | [LEAN] Minimal two-field struct wrapping exactly the two top-level sections of a wg0.conf file. No unnecessary indirection. | [WEAK] Note: tests ARE present — embedded in the same file under #[cfg(test)], not a separate test file. `parse` is exercised by `parse_full_config` and `parse_v6_allowed_ips` covering the happy path with two peers. However, `from_file` is never tested, and error paths (malformed lines, keys outside sections, missing [Interface]) are entirely absent. | [PARTIAL] Struct has `/// Parsed WireGuard configuration.` at line 12, but fields `interface` and `peers` carry no individual `///` docs. The `from_file` and `parse` methods in the impl block (lines ~94–165) also have zero doc comments and no `# Examples` sections, which are expected on public API methods. (deliberated: confirmed — Correction NEEDS_FIX is valid: inspecting Config::parse L120-122, when [Interface] is encountered after a [Peer], current_peer_kvs is not flushed — the flush guard at L125 only triggers inside the [Peer] branch when current_section is already 'peer'. This means [Peer]-before-[Interface] ordering silently merges stale KVs. While WireGuard configs conventionally place [Interface] first, the parser doesn't enforce this and fails silently. Additionally, L163-165 wraps interface in Some() then immediately unwraps via ok_or_else — a dead branch noted in best_practices. Tests WEAK is accurate: parse_full_config and parse_v6_allowed_ips cover happy paths only; from_file is untested; error paths (malformed lines, keys outside sections, missing [Interface]) are absent. Documentation PARTIAL is accurate: struct has a one-line doc comment but fields and methods lack individual docs.)
  - CidrAddr: [USED] Exported struct with three utility methods (contains_v4, contains_v6, contains). Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] CIDR address representation struct; no similar type definitions found | [OK] Struct definition is correct. The contains_v4 and contains_v6 methods use proper bitmask arithmetic; the prefix_len==0 early-return, the >=32 / >=128 caps, and the shift expressions are all correct for their respective address families. | [ACCEPTABLE] Struct itself is minimal. Exposing both typed helpers (contains_v4, contains_v6) and the polymorphic contains() is mildly redundant but useful for callers with already-typed addresses. The ipnetwork crate (popular on crates.io) would eliminate the hand-rolled bit-mask arithmetic in the impl block, but the custom implementation is short and correct so the redundancy is not a real problem. | [WEAK] contains_v4 is well-covered: /24, /32 (host), /0 (default route), and cross-family rejection are all tested. contains_v6 only covers /64; the boundary cases prefix_len=0 (default route) and prefix_len=128 (host) are not tested for v6, unlike v4. The `contains()` dispatch method is never directly exercised. The Display impl is untested. | [PARTIAL] Struct has `/// An IP address with prefix length (CIDR notation). Supports v4 and v6.` at line 37, and all three methods (`contains_v4`, `contains_v6`, `contains`) have one-line `///` comments. However, the two public fields (`addr`, `prefix_len`) lack `///` docs, and none of the methods include a `# Examples` section despite being public API. (deliberated: confirmed — Overengineering ACCEPTABLE is fair: exposing both typed helpers and the polymorphic contains() is mildly redundant but practical for callers. Tests WEAK confirmed: v4 has excellent boundary coverage (/0, /24, /32, cross-family rejection) but v6 only covers /64 — missing /0 and /128 boundary cases. The contains() dispatcher and Display impl are also untested. Documentation PARTIAL confirmed: struct-level doc exists at L37 and methods have one-line docs, but fields addr and prefix_len lack individual docs and no #[doc] examples exist. Raised confidence slightly since all findings are directly verifiable in source.)
  - decode_key: [USED] Non-exported function called locally in parse_interface (line 211) and parse_peer (lines 251, 253). Decodes base64-encoded WireGuard keys to 32-byte arrays. | [UNIQUE] Base64 decodes string to 32-byte key. RAG found base64_key functions (scores 0.70–0.71) but they perform opposite operation (encoding, not decoding) with different signatures and semantic contracts—not interchangeable | [OK] Correctly decodes standard base64 (WireGuard uses RFC 4648 standard alphabet with padding) and validates the decoded length is exactly 32 bytes, matching the WireGuard key size. | [LEAN] Two-step base64 decode then exact-length check. Minimal and correct. | [WEAK] Exercised only implicitly via parse_full_config happy path (valid base64 keys decode without error). No tests for invalid base64 input, wrong decoded length (< or > 32 bytes), or empty string — both error branches in the function are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The name is reasonably descriptive, and per convention private items are tolerated as undocumented. Confidence lowered accordingly. (deliberated: confirmed — Tests WEAK is accurate: decode_key is only exercised via parse_full_config's happy path with valid base64 keys. Both error branches (bad base64, wrong decoded length) are untested. However, as a private helper, this is less critical. Raised confidence slightly since the finding is directly verifiable by checking the test module — no test constructs invalid base64 or wrong-length key inputs.)
  - parse_interface: [USED] Non-exported function called in Config::parse (line 163). Parses [Interface] section of config file with real parsing logic for keys, port, addresses. | [UNIQUE] Parses WireGuard interface config from key-value pairs; no similar parsing functions found | [NEEDS_FIX] L195: when no '/' is present in an Address part, the fallback prefix is hardcoded as the string '24'. For an IPv6 address without an explicit prefix (e.g. 'Address = fd00::1'), this silently assigns prefix_len=24 instead of a valid IPv6 prefix, corrupting the address_v6 tuple stored in InterfaceConfig. parse_cidr (L277) solves the identical problem by branching on addr.is_ipv4(); parse_interface should do the same here. | [LEAN] Length is driven by the number of distinct keys and the dual-stack address parsing. Each branch is a direct field assignment with error mapping; no unnecessary layers of abstraction. | [WEAK] Happy path covered by parse_full_config (port, v4 address, netmask). parse_v6_allowed_ips does NOT exercise the v6 address field on the interface (only peer AllowedIPs). Error paths — missing PrivateKey, missing Address, bad port, bad prefix, bad address string — are all untested. Default listen_port=51820 fallback is never verified. | [UNDOCUMENTED] Private function with no `///` doc comment. Contains non-trivial logic such as comma-separated address parsing and IPv6 handling; a brief doc noting accepted input format would be helpful, but leniency applies to private items. (deliberated: confirmed — Correction NEEDS_FIX is strongly confirmed: at L195, `.unwrap_or((part, "24"))` hardcodes prefix 24 as the fallback when no '/' is present. For an IPv6 address like 'fd00::1' (without explicit prefix), this assigns prefix_len=24 which is invalid for IPv6. The correct approach is visible in parse_cidr at L277-279 which branches on addr.is_ipv4() to select 32 or 128 as default. This is a clear, directly visible bug — raising confidence to 88. Tests WEAK is accurate: v6 interface address parsing is never exercised (parse_v6_allowed_ips only tests peer AllowedIPs), and error paths are absent. Documentation UNDOCUMENTED with private-item leniency is appropriate.)
  - parse_peer: [USED] Non-exported function called in Config::parse (lines 139, 153). Parses [Peer] section of config file with real parsing logic for keys, endpoints, allowed IPs. | [UNIQUE] Parses WireGuard peer config from key-value pairs; no similar parsing functions found | [OK] Correctly parses all mandatory and optional peer fields. AllowedIPs entries are accumulated across repeated keys. Duplicate scalar keys use last-writer-wins semantics, which is acceptable for this format. | [LEAN] Mirrors parse_interface in structure. Five peer fields require five match arms. No avoidable complexity. | [WEAK] Happy path exercised by parse_full_config (endpoint, multiple allowed_ips, persistent_keepalive, absent endpoint). PresharedKey parsing is never exercised. Error paths (missing PublicKey, bad SocketAddr endpoint, bad keepalive value) are all absent. Only 2 of the 5 optional fields are validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is self-explanatory; private-item leniency applies. (deliberated: confirmed — Tests WEAK confirmed: parse_full_config exercises endpoint, allowed_ips, and persistent_keepalive, but preshared_key parsing is never exercised and all error paths (missing PublicKey, bad endpoint, bad keepalive) are absent. Only 3 of 5 fields are validated in tests. Documentation UNDOCUMENTED with private-item leniency applies. Raised confidence moderately since findings are directly verifiable against the test module.)
  - parse_cidr: [USED] Non-exported function called in parse_peer (line 259). Parses CIDR notation strings (e.g. '10.0.0.0/24') into CidrAddr instances. | [UNIQUE] Parses CIDR notation with smart defaults (32 for IPv4, 128 for IPv6); no similar functions found | [NEEDS_FIX] L278: prefix_len is parsed as u8 (0-255) with no upper-bound validation against the address family. A CIDR like '10.0.0.1/64' is silently accepted with prefix_len=64. contains_v4 then saturates this to /32 behaviour via the 'prefix_len >= 32' guard (L51), producing semantically incorrect match results without any error. Should reject prefix_len > 32 for IPv4 and > 128 for IPv6. | [LEAN] Splits on '/', selects sensible per-family defaults, parses each part once. Compact and purposeful. | [WEAK] Indirectly exercised through parse_full_config and parse_v6_allowed_ips for well-formed CIDR strings. The no-prefix default behaviour (addr without '/', defaulting to /32 or /128) is never tested. Error paths (invalid IP string, non-numeric prefix) are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The defaulting logic (32 for v4, 128 for v6 when no prefix is given) is non-obvious behavior that would benefit from a comment, but private-item leniency applies. (deliberated: confirmed — Correction NEEDS_FIX is valid: at L278, prefix_len is parsed as u8 with no upper-bound check against address family. '10.0.0.1/64' is silently accepted; contains_v4 then saturates to /32 via the >= 32 guard at L51, producing semantically incorrect results (treats /64 as /32 without error). Adding validation is a small defensive improvement. Tests WEAK confirmed: only well-formed CIDRs with explicit prefixes are tested; no-prefix defaults and error paths are unexercised. Documentation: the original field says DOCUMENTED while the detail text says UNDOCUMENTED — there is an inline comment '// Default prefix: 32 for v4, 128 for v6.' at L272 but no /// doc comment. Per the rule against adding new findings, I preserve the original DOCUMENTED classification, though the inconsistency is noted. Confidence raised from 60 to 75 as the missing validation is directly visible.)
  - prefix_to_netmask: [USED] Exported function converting prefix length to IPv4 netmask. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Converts IPv4 CIDR prefix length to netmask; no similar utility functions found | [OK] Correctly handles all three regions: prefix==0 returns 0.0.0.0, prefix>=32 returns 255.255.255.255, and the general case uses u32::MAX << (32-prefix) which is safe for prefix in 1..31. Ipv4Addr::from(u32) converts via to_be_bytes(), so byte ordering is correct on all platforms. | [LEAN] Three-branch bit-shift helper converting a prefix length to an Ipv4Addr mask. Handles the two edge cases (0 and >=32) explicitly to avoid undefined-behavior in shift arithmetic. Appropriately minimal. | [WEAK] Only implicitly tested via parse_full_config which asserts netmask==255.255.255.0 for a /24 address. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised. No direct unit tests exist for this exported function despite being imported by three callers. | [UNDOCUMENTED] Public function (`pub fn`) with no `///` doc comment, no parameter description, no description of boundary behaviour for `prefix == 0` or `prefix >= 32`, and no `# Examples` section. Expected on public API. (deliberated: confirmed — Tests WEAK confirmed: only indirectly tested via parse_full_config asserting netmask==255.255.255.0 for /24. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised despite being explicit code paths. No direct unit tests exist for this exported public function. Documentation UNDOCUMENTED confirmed: pub fn at L284 has no /// doc comment, no parameter description, and no explanation of boundary behavior. This is a public API imported by three workspace crates and warrants documentation. Slightly raised confidence.)

- [ ] `rustguard-enroll/src/protocol.rs` — 4 weak
  Improve `rustguard-enroll/src/protocol.test.rs` covering: parse_request, build_response, parse_response, random_nonce
  - parse_request: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment request messages. | [UNIQUE] Structurally similar to parse_response (similarity: 0.759) but semantically distinct: returns Option<[u8;32]> not Option<EnrollmentOffer>, uses ENROLL_REQUEST_MAGIC not ENROLL_RESPONSE_MAGIC, indices [28..76] not [28..81], and performs simple try_into conversion instead of complex struct construction. Different return types and contracts make this unique. | [OK] Length and magic checks are performed before any indexing. Nonce extracted from buf[4..28], ciphertext from buf[28..76] (48 bytes matching the expected seal output). xopen result is converted to [u8; 32] via try_into, which fails safely if the plaintext length differs. Logic is correct. | [LEAN] Straightforward length-and-magic validation followed by decryption. Returns Option which is idiomatic Rust for fallible parsing; no excess abstraction. | [WEAK] Happy path, wrong-token AEAD failure, and ciphertext tampering are all tested. However, the short-buffer early return (buf.len() < ENROLL_REQUEST_SIZE) and the wrong-magic early return (buf[0..4] != ENROLL_REQUEST_MAGIC, e.g. feeding a response packet to the request parser) are not exercised. Two None-returning branches are untested. | [PARTIAL] Two `///` lines describe purpose and return value briefly. Missing: parameter descriptions for `token_key` and `buf`, failure conditions for returning `None` (bad magic, AEAD failure, length mismatch), and `# Examples` section. (deliberated: confirmed — Tests: Three tests cover happy path, wrong-token AEAD failure, and ciphertext tampering — good for the crypto paths. However, the short-buffer guard (L42-44) and wrong-magic guard (L45-47) are untested, leaving two early-return branches uncovered. WEAK is fair but borderline — the crypto-critical paths are well tested while only simple guard clauses are missed. Documentation: Two `///` lines present but missing parameter descriptions and None-returning conditions. PARTIAL confirmed.)
  - build_response: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Constructs encrypted enrollment response messages. | [UNIQUE] Structurally similar to build_request (similarity: 0.738) but semantically distinct: accepts EnrollmentOffer not raw pubkey, constructs intermediate plaintext payload from struct fields, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, and uses indices [28..81] not [28..76]. Different parameters and payload logic make this unique despite structural overlap. | [OK] Plaintext is correctly assembled: server_pubkey at [0..32], ip octets at [32..36], prefix_len at [36]. xseal of 37-byte plaintext produces 53 bytes (37+16 tag), placed at buf[28..81]. All offsets match ENROLL_RESPONSE_SIZE=81. | [LEAN] Serialises EnrollmentOffer into the 81-byte wire frame. Fixed-size arithmetic matches the documented layout; no abstraction overhead. | [WEAK] Only response_roundtrip covers build_response; there is no analogue of request_wrong_token_fails (verifying that a response built with key A is unreadable under key B) nor a tampered_response_rejected test. A single happy-path roundtrip is insufficient for a security-critical wire-format builder. | [PARTIAL] Single-line `///` comment 'Build an enrollment response.' No description of the `token_key` or `offer` parameters, no explanation of what the returned byte array contains, and no `# Examples` section on this public function. (deliberated: confirmed — Tests: Only the response_roundtrip happy path covers build_response. No analogue of request_wrong_token_fails or tampered_request_rejected exists for the response path. This asymmetry is a genuine coverage gap for security-critical code. WEAK is well-justified. Documentation: Single-line `///` comment with no parameter or return descriptions. PARTIAL confirmed.)
  - parse_response: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment response messages. | [UNIQUE] Structurally similar to parse_request (similarity: 0.759) but semantically distinct: returns Option<EnrollmentOffer> not Option<[u8;32]>, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, indices [28..81] not [28..76], includes plaintext length validation, and constructs complex EnrollmentOffer from decomposed bytes. Different return contracts and invariants make this unique. | [OK] Length and magic guards are in place before indexing. Nonce from buf[4..28], ciphertext from buf[28..81] (53 bytes). The explicit plaintext.len() != 37 check is redundant given a correctly functioning AEAD but is not harmful. Fields are extracted at correct offsets. All None-returning paths use ? or explicit return None. | [LEAN] Mirrors parse_request in structure; the extra plaintext-length check (L90) is a defensive sanity check appropriate for a security-sensitive protocol, not gold-plating. | [WEAK] Only the happy-path roundtrip is tested. Missing: wrong-token AEAD failure, ciphertext tampering, short-buffer early return, wrong-magic early return (e.g. feeding a request packet), and the explicit plaintext.len() != 37 guard. Four out of five failure branches are entirely untested, contrasting with the more thorough coverage of parse_request. | [PARTIAL] Single-line `///` comment 'Parse and decrypt an enrollment response.' Missing parameter descriptions, no enumeration of `None`-returning conditions (bad magic, AEAD failure, wrong plaintext length), and no `# Examples` section for a public function. (deliberated: confirmed — Tests: Only the happy-path roundtrip is tested. Four of five failure branches (wrong-token, tampering, short-buffer, wrong-magic, plaintext length check at L90) are entirely untested. This is the weakest test coverage in the file and contrasts sharply with parse_request's coverage. WEAK is clearly correct. Documentation: Single-line `///` with no parameter descriptions or enumeration of None-returning conditions. PARTIAL confirmed.)
  - random_nonce: [USED] Internal helper function called by build_request (L29) and build_response (L69). Generates cryptographic nonces. | [UNIQUE] Shows high similarity (0.943) to random_nonce in rustguard-core/src/cookie.rs but resides in separate crate. Per established precedent: cross-crate deduplication of trivial 3-line private helpers introduces unnecessary inter-crate coupling. Independent usage in different crates reflects intentional architecture, not harmful duplication. | [OK] Uses getrandom::getrandom for OS-provided entropy into a 24-byte buffer. Panicking on failure is appropriate for a crypto primitive — a system that cannot supply entropy cannot operate securely. No correctness issues. | [LEAN] Tiny typed wrapper around getrandom that gives the operation a name and a concrete return type. Avoids scattered unsafe-ish getrandom calls; well-justified as a local helper. | [WEAK] Private helper tested only indirectly: roundtrip tests verify the nonces it produces are valid (AEAD succeeds), but uniqueness across calls, panic behavior if getrandom fails, and the 24-byte length are never directly asserted. Indirect coverage is plausible but fragile for a CSPRNG wrapper. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name is self-descriptive and the function is not part of the public API, so omission is tolerable by convention. The `expect` message documents the panic condition inline. (deliberated: confirmed — Tests: Private 3-line helper tested only indirectly via roundtrip tests. Indirect coverage does verify the nonce works (AEAD succeeds), but nonce uniqueness and the panic path are untested. For a private helper this small, WEAK is borderline — indirect testing is the norm for trivial private helpers. However, since it's a CSPRNG wrapper and no direct assertions exist, WEAK is defensible. Bumping confidence slightly since indirect testing through multiple roundtrip tests does provide meaningful coverage.)

- [ ] `rustguard-tun/src/linux.rs` — 4 untested, 4 weak
  Improve `rustguard-tun/src/linux.test.rs` covering: last_os_error, close_and_error, make_sockaddr_in, set_name, create, configure_interface, read, write
  - last_os_error: [USED] Helper function used for OS error conversion in create(), configure_interface(), read(), and write() | [UNIQUE] Trivial 2-line wrapper with no duplicates found via RAG | [OK] Straightforward delegation to io::Error::last_os_error(); no correctness issues. | [OVER] Single-line alias for io::Error::last_os_error() that saves only the 'io::Error::' prefix. Adds a call-stack frame with no semantic benefit; callers could use io::Error::last_os_error() directly. Borderline, hence moderate confidence. | [NONE] No test file found for rustguard-tun/src/linux.rs. last_os_error has zero test coverage. | [UNDOCUMENTED] Private helper function, no `///` doc comment. Name is self-explanatory. Tolerated for private items. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: this 2-line wrapper is used at 4 distinct call sites (create, configure_interface, read, write), providing a consistent shorter alias alongside the companion close_and_error pattern. In Rust FFI code with many libc error checks, such aliases are a common and accepted convention. Tests WEAK retained — no test file exists for this module.)
  - close_and_error: [USED] Helper function to close fd and return error; used throughout create() and configure_interface() | [DUPLICATE] Identical 5-line helper found in linux_mq.rs with score 0.987 | [OK] Correctly captures errno via last_os_error() before calling close(), preventing the close() syscall from overwriting errno. Error is returned after fd is closed. | [LEAN] Captures errno before closing the fd to prevent clobbering — a legitimate and important pattern in unsafe libc code. Used consistently at every error exit point. | [NONE] No test file found for rustguard-tun/src/linux.rs. close_and_error has zero test coverage. Error path (fd cleanup on failure) is entirely untested. | [UNDOCUMENTED] Private helper function that closes an fd and captures the last OS error. No `///` doc comment. Tolerated for private items with clear names. (deliberated: confirmed — Duplication is genuine — identical 5-line function exists in linux_mq.rs. Both capture errno before close(fd) to prevent clobbering. Could be extracted to a shared internal utility module (e.g., linux_common.rs). Tests WEAK retained — no test file for this module.)
  - make_sockaddr_in: [USED] Helper function called 3 times in configure_interface() to construct sockaddr_in structs | [DUPLICATE] Identical 10-line function in linux_mq.rs with score 0.973 | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. On a little-endian host, from_ne_bytes([a,b,c,d]) produces the integer 0xdcba…, which when stored in memory LE lays down bytes [a,b,c,d] — preserving network (big-endian) byte order in memory exactly as the kernel expects. | [LEAN] Encapsulates boilerplate sockaddr_in initialisation used three times in configure_interface; avoids repetition and ensures sin_family/sin_zero are always correctly set. | [NONE] No test file found for rustguard-tun/src/linux.rs. make_sockaddr_in has zero test coverage. The byte-order conversion (u32::from_ne_bytes) and struct initialization are not validated by any test. | [UNDOCUMENTED] Private helper constructing a `sockaddr_in` from an `Ipv4Addr`. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 10-line function in linux_mq.rs constructing libc::sockaddr_in from Ipv4Addr with identical field initialization. Same semantic contract in both files, prime candidate for shared module extraction. Tests WEAK retained.)
  - set_name: [USED] Helper function used in create() and configure_interface() to copy interface name into buffers | [DUPLICATE] Identical 5-line function in linux_mq.rs with score 0.968 | [OK] Copies up to IFNAMSIZ-1 bytes, leaving the final byte as the zero from the caller's zero-initialized buffer. Null termination is always present. | [LEAN] Tiny but used at five distinct call sites; centralises the null-termination and length-clamping logic that would otherwise be repeated inline each time. | [NONE] No test file found for rustguard-tun/src/linux.rs. set_name has zero test coverage. Truncation at IFNAMSIZ-1 for long names and NUL-termination behavior are not tested. | [UNDOCUMENTED] Private helper writing a name into a fixed-size `[u8; IFNAMSIZ]` buffer. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 5-line function in linux_mq.rs copying null-terminated name bytes into fixed-size buffer. Same contract, same truncation logic. Tests WEAK retained.)

- [ ] `rustguard-tun/src/bpf_loader.rs` — 9 untested
  Improve `rustguard-tun/src/bpf_loader.test.rs` covering: XdpProgram, bpf_create_xskmap, bpf_prog_load, bpf_map_update, attach_xdp, attach_xdp_netlink, detach_xdp, ifindex_to_name, parse_and_patch_elf

- [ ] `rustguard-crypto/src/aead.rs` — 4 untested, 1 weak
  Improve `rustguard-crypto/src/aead.test.rs` covering: xseal, xopen, seal_to, open_to, build_nonce
  - build_nonce: [USED] Non-exported internal function called 4 times within file: in seal, open, seal_to, open_to | [UNIQUE] Private helper that builds WireGuard nonce from counter (4 zero bytes + 8-byte LE counter). No similar functions found in RAG search results. | [OK] WireGuard specifies nonce = 4 zero bytes || 8-byte little-endian counter. nonce[4..] is exactly 8 bytes and to_le_bytes() produces 8 bytes; construction is correct. | [LEAN] Minimal private helper encoding the WireGuard nonce format (4 zero bytes || 8-byte LE counter). Four lines, single responsibility, well-commented at call sites. | [WEAK] Private helper exercised indirectly through seal/open tests; wrong_counter_fails implicitly validates that distinct counter values produce distinct nonces. However the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted, and seal_to/open_to (which also call build_nonce) have no tests at all. | [DOCUMENTED] Private function with a clear `///` comment describing the WireGuard nonce layout (4 zero bytes || 8-byte LE counter). For a private helper, this is sufficient; no examples are expected per private-item leniency. (deliberated: confirmed — Tests WEAK is reasonable. The function is exercised indirectly through seal/open tests, and wrong_counter_fails implicitly validates distinct counters produce distinct nonces. However, the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted. For a private helper, this indirect coverage provides some assurance but not full confidence in the byte layout. Confidence raised slightly as the indirect coverage is real, but the layout-specific gap remains valid.)

- [ ] `rustguard-tun/src/macos.rs` — 5 untested, 2 weak
  Improve `rustguard-tun/src/macos.test.rs` covering: close_and_error, make_sockaddr_in, create, configure_address, set_mtu, read, write
  - close_and_error: [USED] Called 4 times to safely preserve OS error before closing file descriptor (L111, L127, L142, L165) | [DUPLICATE] Identical implementation found in linux.rs and linux_mq.rs with scores 0.977 and 0.979. Captures errno, closes fd, returns error—same logic and semantic contract across all three platform modules. | [OK] Correctly captures errno before calling close() to prevent the close() syscall from clobbering errno. | [LEAN] Solves a real, documented problem: libc::close() can clobber errno, so the error must be captured first. The comment explains the invariant clearly. Not abstraction for its own sake. | [NONE] No test file exists for this module. This private fd-cleanup helper has zero test coverage. The errno-before-close ordering is untested. | [PARTIAL] Private function with three `///` lines (lines 76–78) explaining purpose ('Capture the OS error, close the fd'), motivation ('avoids close() clobbering errno'), and a Safety precondition. Adequate for a private unsafe helper. No formal `# Parameters` or `# Examples`, but those are not expected for private functions. (deliberated: confirmed — Duplication DUPLICATE is technically accurate — identical 4-line implementations exist in linux.rs and linux_mq.rs. However, these are platform-gated modules (only one compiles per target), so deduplication into a shared utility is a low-value refactor. Keeping DUPLICATE but lowering confidence to reflect that cross-platform-module duplication is a weaker signal. Documentation PARTIAL is accurate: the three /// lines adequately document purpose and safety for a private unsafe helper.)
  - make_sockaddr_in: [USED] Called 3 times in configure_address() to construct SockaddrIn for address configuration (L205-207) | [UNIQUE] High RAG similarity (0.968) but returns custom SockaddrIn struct; linux variants return libc::sockaddr_in. Different struct layouts and initialization logic—macos uses sin_len field, linux uses u32::from_ne_bytes conversion. Different semantic contracts due to struct type differences. | [OK] Correctly constructs a Darwin sockaddr_in with sin_len=16, AF_INET family, zero port, and octets from Ipv4Addr. | [LEAN] Used three times in configure_address() for addr, broadaddr, and mask. DRY helper with no generics or unnecessary indirection; appropriate factoring. | [NONE] No test file exists for this module. make_sockaddr_in is a pure function (Ipv4Addr -> SockaddrIn) that is trivially unit-testable without kernel access, yet has zero test coverage. Field values including sin_len, sin_family, and sin_addr byte ordering are never asserted. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Name and single `Ipv4Addr` parameter are self-describing. Tolerated under private-item leniency. (deliberated: confirmed — Tests WEAK kept. This is a pure function (Ipv4Addr → SockaddrIn) with actual struct construction logic including sin_len=16, sin_family assignment, and octet mapping. It is trivially unit-testable without kernel access, yet has zero coverage. WEAK is appropriate rather than NONE since the function's simplicity reduces the risk.)

- [ ] `rustguard-kmod/src/cookie.rs` — 6 weak
  Improve `rustguard-kmod/src/cookie.test.rs` covering: hash, mac, random_bytes, CookieChecker, CookieState, constant_time_eq
  - hash: [USED] Internal helper function called in verify_mac1 (line 101), create_reply (line 126), process_reply (line 154) for BLAKE2s hashing. | [UNIQUE] RAG score 0.937 with noise.rs hash, but implementations differ: this version uses array size 4 and passes uncapped chunks.len() to C function; noise.rs uses array size 8 and passes capped num. Different capacity invariants and capping behavior suggest intentionally different constraints despite semantic similarity. | [NEEDS_FIX] Line 49 passes `chunks.len() as u32` as `num_chunks` to the C function, but the loop at line 44 uses `.take(4)` and populates at most 4 entries in `ptrs` and `lens`. If `chunks.len() > 4`, the C function is told there are more chunks than have been written, causing it to read beyond the bounds of the 4-element Rust arrays — a stack over-read and undefined behavior. The argument must be clamped: `chunks.len().min(4) as u32`. | [LEAN] Thin FFI adapter that avoids repeating unsafe boilerplate at every call site. The hard cap of 4 chunks mirrors kernel-module constraints (no heap). Silent truncation beyond 4 is a mild concern, but the design is minimal and fit-for-purpose. | [WEAK] No direct tests found for this private function. However, prior deliberation established that tests such as cookie_roundtrip() and wrong_mac1_fails_decrypt() exist and exercise CookieChecker::verify_mac1(), which calls hash() internally. Indirect coverage only — hash is never tested with edge-case inputs (empty slice, >4 chunks clipped by take(4), zero-length chunks), and no assertions target hash output directly. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_hash` with a slice-of-slices interface; the chunked hashing behavior and 4-chunk maximum are not explained. (deliberated: confirmed — Correction NEEDS_FIX is confirmed with high confidence: line 44 uses .take(4) populating at most 4 entries in ptrs/lens arrays, but line 49 passes chunks.len() as u32 — the raw unclamped caller count — to the C function. If chunks.len() > 4, the C function reads past the 4-element stack arrays causing out-of-bounds access. All current callers pass exactly 2 chunks so the bug is latent, but the API provides zero enforcement. This is a genuine memory safety defect. Tests WEAK is correct: hash is only exercised indirectly through verify_mac1/create_reply/process_reply; no direct tests for edge cases (>4 chunks, empty input, zero-length chunks).)
  - mac: [USED] Internal helper for BLAKE2s MAC computation, called in verify_mac1 (103), make_cookie (117), verify_mac2 (143), compute_mac2 (189). | [DUPLICATE] RAG score 0.974 with noise.rs mac — identical signature and implementation. Both compute BLAKE2s-256 MAC by calling wg_blake2s_256_mac with same parameter passing and return value extraction. | [OK] Straightforward FFI wrapper for BLAKE2s-MAC. Key, data, lengths, and output pointer are all forwarded correctly with no type mismatches. | [LEAN] One-liner FFI wrapper that eliminates repeated unsafe blocks. No unnecessary generics or abstraction layers. | [WEAK] No direct tests found. Indirectly exercised through CookieChecker::verify_mac1() and make_cookie() in the test suite established by prior deliberation (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie). Edge cases such as empty key, empty data, or mismatched key lengths are never directly validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_256_mac` but the keyed-MAC semantics and return size (32 bytes) are not described. (deliberated: confirmed — Duplication: reported as 100% identical to noise.rs mac function — both are thin wrappers calling wg_blake2s_256_mac with the same parameter pattern. This is a true functional duplicate that belongs in a shared crypto utility module. Tests WEAK: only indirect coverage through CookieChecker methods; no direct tests for empty key, empty data, or edge-case inputs. Both findings maintained.)
  - random_bytes: [USED] Generic random bytes generator called for secret initialization (85), secret rotation (112), and nonce generation (131). | [UNIQUE] RAG score 0.841 with rustguard-core/src/cookie.rs, but different semantic contract: this version is generic <const N: usize>() returning [u8; N]; core version is fixed fn random_bytes() -> [u8; 32]. Also calls different RNG function (wg_get_random_bytes vs fill_random). | [OK] Const-generic buffer is correctly sized and its length is passed as u32. All call sites use small N (24 and 32 bytes), well within u32 range. | [LEAN] Const-generic N produces a zero-cost, type-safe wrapper over the FFI random call. Idiomatic Rust; avoids repeated unsafe boilerplate for different buffer sizes. | [NONE] No test file found for this source file and no direct tests for this function. While CookieChecker::new() calls random_bytes() during test setup, the randomly generated output is inherently non-deterministic and cannot be meaningfully asserted. No smoke tests verifying it doesn't panic or returns the correct length are present. | [UNDOCUMENTED] Private generic function with no `///` doc comment. The kernel RNG source and const-generic size parameter are not documented; leniency applies as private. (deliberated: confirmed — Tests WEAK is appropriate. The function is indirectly exercised via CookieChecker::new() and make_cookie()/create_reply() in tests, but its non-deterministic nature makes direct assertion impractical. No smoke tests exist verifying correct buffer length or non-panic behavior. WEAK (not NONE) is correct since indirect coverage exists through CookieChecker test suite.)
  - CookieChecker: [USED] Primary server-side cookie validation API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module server-side cookie validation state with public key, secret, timestamp, and under-load flag. | [OK] Struct field declarations are correct. Types are appropriate: u64 for nanosecond timestamps, [u8;32] for keys and public values. | [LEAN] Four fields encoding exactly the server-side cookie state described by the WireGuard spec. No superfluous generics, traits, or builder patterns. Methods are small and single-purpose. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, mac2_wrong_source_fails) exercise CookieChecker. Coverage is partial: secret rotation via maybe_rotate_secret() after COOKIE_SECRET_LIFETIME_NS elapses is not tested, create_reply() wire-format correctness is not independently asserted, and under_load flag transitions are not tested. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Server-side: generates cookies and validates MAC2.') but fields lack `///` comments, there are no `# Examples`, and the secret-rotation lifecycle and `under_load` flag semantics are not explained. (deliberated: confirmed — Tests WEAK confirmed: CookieChecker is exercised by multiple tests (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie) but important paths remain untested — secret rotation after COOKIE_SECRET_LIFETIME_NS, under_load flag transitions, and create_reply wire-format correctness. Documentation PARTIAL confirmed: has a one-line /// comment but fields lack documentation, no examples, and the secret rotation lifecycle and thread-safety requirements are undescribed for a security-critical pub(crate) struct.)
  - CookieState: [USED] Primary client-side cookie storage API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module client-side cookie storage with optional decrypted cookie and reception timestamp. | [OK] Option<[u8; COOKIE_LEN]> correctly models presence/absence of a stored cookie. u64 for the receipt timestamp is appropriate. | [LEAN] Two-field client-side struct: the cookie value and its timestamp. Minimal representation; freshness check is a simple subtraction. No unnecessary indirection. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, no_cookie_gives_zero_mac2) exercise CookieState. Coverage is partial: staleness expiry (is_fresh returning false after COOKIE_SECRET_LIFETIME_NS) is not tested; process_reply with an invalid/tampered encrypted payload and the false-return path is also not covered. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Client-side: stores a received cookie for MAC2.') but fields are undocumented, freshness/staleness semantics are not described, and no `# Examples` section is present. (deliberated: confirmed — Tests WEAK confirmed: CookieState is covered by several tests (cookie_roundtrip, no_cookie_gives_zero_mac2) but staleness expiry (is_fresh returning false after timeout) and process_reply with tampered payloads are untested. Documentation PARTIAL confirmed: single-line /// comment present but fields undocumented, freshness semantics unexplained, and no examples for a pub(crate) type.)
  - constant_time_eq: [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, called in verify_mac1 (104) and verify_mac2 (144). | [DUPLICATE] RAG score 0.962 with noise.rs constant_time_eq — identical implementation. Both check length equality then use wg_crypto_memneq for constant-time comparison, with same parameter passing and return logic. | [OK] Correctly delegates to the kernel's crypto_memneq, which is guaranteed constant-time and cannot be optimized away by the compiler. The early-exit length check is safe because all callers pass fixed-length 16-byte slices, so the branch carries no secret-dependent timing information. | [LEAN] Security-critical helper delegating to the kernel's crypto_memneq, which is guaranteed not to be optimized away. Extracting it into a named function documents intent and prevents accidental substitution with a non-constant-time comparison elsewhere. | [WEAK] No direct tests found. Indirectly exercised through verify_mac1() and verify_mac2() in the CookieChecker test suite (established by prior deliberation). Critical edge cases are untested: mismatched-length slices (returns false early without calling wg_crypto_memneq), equal-length but differing slices, and zero-length slices. The constant-time guarantee itself is structurally untestable at the unit level, but behavioral correctness for these edge cases is never directly asserted. | [PARTIAL] Private function has a `///` doc comment noting its constant-time guarantee via `wg_crypto_memneq`. However, parameter semantics (slice length requirement), return-value meaning, and panic/safety conditions are not described. Leniency applies as private. (deliberated: confirmed — Duplication DUPLICATE: reported as 100% identical to noise.rs constant_time_eq — both check length then delegate to wg_crypto_memneq. This is a security-critical utility that absolutely should live in a shared module to prevent divergent copies. Tests WEAK: only exercised indirectly through verify_mac1/verify_mac2; mismatched-length and zero-length edge cases never tested directly. Documentation PARTIAL: has a /// comment noting the constant-time guarantee (good) but parameter semantics and return value meaning are undescribed.)

- [ ] `rustguard-tun/src/linux_mq.rs` — 6 untested
  Improve `rustguard-tun/src/linux_mq.test.rs` covering: set_name, make_sockaddr_in, close_and_error, MultiQueueTun, open_tun_queue, configure_interface
