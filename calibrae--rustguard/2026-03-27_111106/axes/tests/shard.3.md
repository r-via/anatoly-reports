[← Back to Tests](./index.md) · [← Back to report](../../public_report.md)

# Tests — Shard 3

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-enroll/src/fast_udp.rs` | NEEDS_REFACTOR | 6 | 85% | [details](#rustguard-enrollsrcfastudprs) |
| `rustguard-enroll/src/client.rs` | NEEDS_REFACTOR | 4 | 88% | [details](#rustguard-enrollsrcclientrs) |
| `rustguard-crypto/src/blake2s.rs` | NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-cryptosrcblake2srs) |
| `rustguard-crypto/src/x25519.rs` | NEEDS_REFACTOR | 4 | 88% | [details](#rustguard-cryptosrcx25519rs) |
| `rustguard-enroll/src/packet.rs` | NEEDS_REFACTOR | 3 | 88% | [details](#rustguard-enrollsrcpacketrs) |
| `rustguard-enroll/src/state.rs` | NEEDS_REFACTOR | 3 | 88% | [details](#rustguard-enrollsrcstaters) |
| `rustguard-tun/examples/tun_echo.rs` | NEEDS_REFACTOR | 4 | 85% | [details](#rustguard-tunexamplestunechors) |
| `rustguard-kmod/src/timers.rs` | NEEDS_REFACTOR | 2 | 95% | [details](#rustguard-kmodsrctimersrs) |
| `rustguard-kmod/src/allowedips.rs` | NEEDS_REFACTOR | 2 | 85% | [details](#rustguard-kmodsrcallowedipsrs) |
| `rustguard-tun/src/uring.rs` | NEEDS_REFACTOR | 2 | 90% | [details](#rustguard-tunsrcuringrs) |

## Symbol Details

### `rustguard-enroll/src/fast_udp.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `RecvBatch` | L19–L24 | NONE | 85% | [USED] Public struct; primary type returned by recv_batch in library API | [UNIQUE] Struct for batch packet reception. No similar struct definitions found. | [OK] Struct fields and new() initializer are correct. Option<SocketAddr> implements Copy (SocketAddr is Copy), so [None; BATCH_SIZE] is valid. All fields are zero/None initialized properly. Stack size (~66 KB) is within typical default stack limits. | [LEAN] Plain data struct holding the three parallel arrays that map directly onto mmsghdr semantics (buffers, lengths, peer addrs) plus a count. No generics, no traits, no indirection — exactly what recvmmsg needs. | [NONE] No test file found for this source file. RecvBatch includes a new() constructor with non-trivial initialization (zero-filling fixed-size arrays, Option::None initialization); none of this is exercised by any test. | [PARTIAL] Struct has `/// A batch of received packets.` on the type, but all four public fields (`bufs`, `lens`, `addrs`, `count`) lack `///` doc comments. Per Rust doc conventions, a fully-documented struct requires per-field docs. No `# Examples` section is present. (deliberated: confirmed — Tests NONE is accurate — no test file exists and the struct's constructor with fixed-size array initialization is untested. Documentation PARTIAL is fair: the type-level doc exists but all four public fields lack per-field doc comments. Both findings stand.) |
| `recv_batch` | L39–L83 | NONE | 80% | [USED] macOS fallback implementation; public library API for batch receives | [UNIQUE] macOS-specific recv_batch using recv_from. No similar functions found. | [OK] macOS fallback correctly receives one packet into bufs[0], populates lens[0], addrs[0], and count=1, returns 1. WouldBlock is handled by returning Ok(0) and setting count=0. Other errors are propagated. | [LEAN] macOS fallback that degrades gracefully to recv_from, filling slot 0 of the batch. Minimal code for the platform-compat path; mirrors the Linux contract exactly. | [NONE] No test file found. macOS fallback recv_batch handles the Ok path, WouldBlock path, and general error path; none are covered by any test. | [PARTIAL] Has `/// macOS fallback: receive one packet at a time.` but parameters `sock` and `batch` are undescribed, the WouldBlock → 0 return contract is not stated here (only in the Linux variant's doc), and there is no `# Examples` section on this public function. (deliberated: confirmed — macOS recv_batch (L112-126). Tests NONE is accurate — no test file covers any of the three code paths (Ok, WouldBlock, Err). Documentation PARTIAL is fair: the doc comment exists but omits parameter descriptions and the WouldBlock return contract. Both findings retained.) |
| `sockaddr_to_socketaddr` | L86–L102 | NONE | 60% | [USED] Called within recv_batch at line 79; internal helper for address conversion | [UNIQUE] Helper converting libc sockaddr to SocketAddr. No similar functions found. | [OK] IPv4: s_addr is network-byte-order u32; u32::from_be converts to host byte order; Ipv4Addr::from(u32) takes host byte order — sequence is correct. IPv6: s6_addr is [u8;16] in network order; Ipv6Addr::from([u8;16]) takes octets directly in network order — correct. Both port conversions use u16::from_be on network-byte-order values, which is correct. Unknown family returns None safely. | [LEAN] Private helper that performs the mandatory AF_INET/AF_INET6 union cast to produce a Rust SocketAddr. Both branches are necessary; no NIH concern since the std library does not expose this conversion from sockaddr_storage. | [NONE] No test file found. Private helper performing unsafe sockaddr_storage casts for both AF_INET and AF_INET6 families, plus an unknown-family None branch — three distinct code paths, zero coverage. | [UNDOCUMENTED] Private function (`fn`, no `pub`) with no `///` doc comment. The name is reasonably self-explanatory. Per evaluation rules, undocumented private items with clear names are tolerated; confidence is reduced accordingly. (deliberated: confirmed — Tests NONE is accurate — private helper performing unsafe pointer casts across three branches with zero coverage. Documentation UNDOCUMENTED is technically correct (no doc comment exists), though the evaluator already reduced confidence to 60 acknowledging the self-descriptive name. The function is private so UNDOCUMENTED is tolerated but noted. Both findings retained as-is.) |
| `send_packet` | L106–L108 | NONE | 85% | [USED] macOS fallback implementation; public library API for packet transmission | [UNIQUE] macOS send_packet trivial wrapper. Marked UNIQUE as trivial per RAG analysis. | [OK] Identical to the Linux variant; thin wrapper around UdpSocket::send_to with correct return type propagation. | [LEAN] macOS fallback that simply delegates to send_to. Provides a uniform module API at negligible cost; no abstraction overhead. | [NONE] No test file found. macOS send_packet is a thin wrapper over send_to; trivial but still untested. | [UNDOCUMENTED] macOS variant of `send_packet` has no `///` doc comment at all, unlike the Linux variant which at least has a terse one. Public function missing all documentation. (deliberated: confirmed — macOS send_packet at L129-131. This is correctly identified as the macOS variant and indeed has no doc comment, unlike its Linux sibling at L105. UNDOCUMENTED is accurate. Tests NONE is accurate but trivial risk given the one-line delegation. Both findings retained.) |
| `recv_batch` | L112–L126 | NONE | 80% | [USED] macOS fallback implementation; public library API for batch receives | [UNIQUE] macOS-specific recv_batch using recv_from. No similar functions found. | [OK] macOS fallback correctly receives one packet into bufs[0], populates lens[0], addrs[0], and count=1, returns 1. WouldBlock is handled by returning Ok(0) and setting count=0. Other errors are propagated. | [LEAN] macOS fallback that degrades gracefully to recv_from, filling slot 0 of the batch. Minimal code for the platform-compat path; mirrors the Linux contract exactly. | [NONE] No test file found. macOS fallback recv_batch handles the Ok path, WouldBlock path, and general error path; none are covered by any test. | [PARTIAL] Has `/// macOS fallback: receive one packet at a time.` but parameters `sock` and `batch` are undescribed, the WouldBlock → 0 return contract is not stated here (only in the Linux variant's doc), and there is no `# Examples` section on this public function. (deliberated: confirmed — macOS recv_batch (L112-126). Tests NONE is accurate — no test file covers any of the three code paths (Ok, WouldBlock, Err). Documentation PARTIAL is fair: the doc comment exists but omits parameter descriptions and the WouldBlock return contract. Both findings retained.) |
| `send_packet` | L129–L131 | NONE | 85% | [USED] macOS fallback implementation; public library API for packet transmission | [UNIQUE] macOS send_packet trivial wrapper. Marked UNIQUE as trivial per RAG analysis. | [OK] Identical to the Linux variant; thin wrapper around UdpSocket::send_to with correct return type propagation. | [LEAN] macOS fallback that simply delegates to send_to. Provides a uniform module API at negligible cost; no abstraction overhead. | [NONE] No test file found. macOS send_packet is a thin wrapper over send_to; trivial but still untested. | [UNDOCUMENTED] macOS variant of `send_packet` has no `///` doc comment at all, unlike the Linux variant which at least has a terse one. Public function missing all documentation. (deliberated: confirmed — macOS send_packet at L129-131. This is correctly identified as the macOS variant and indeed has no doc comment, unlike its Linux sibling at L105. UNDOCUMENTED is accurate. Tests NONE is accurate but trivial risk given the one-line delegation. Both findings retained.) |

### `rustguard-enroll/src/client.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `run` | L28–L192 | NONE | 85% | [USED] Public function in library crate with 0 workspace importers. Pattern from known false positives indicates such public APIs are expected to have 0 workspace imports when used by external consumers. Confidence lowered from 95 due to this contradictory evidence. | [UNIQUE] Client enrollment and tunnel flow (score 0.737 below DUPLICATE threshold of 0.82). Semantically different from server.rs::run: client performs single enrollment, receives IP assignment, and tunnels as peer; server manages pool, multiple peers, and multi-queue TUN. Different parameters, different control flow, different caller expectations. | [OK] Operator precedence for `& | 1` is correct in Rust (`&` binds tighter than `|`), so `(network_addr) | 1` correctly produces the .1 host for standard subnets. Mutex is held during `send_to` in the outbound thread, but UDP sends are non-blocking so no deadlock occurs. The inbound thread's catch-all `Err(_) => continue` correctly handles both `WouldBlock` and `TimedOut` (which `set_read_timeout` can return on some platforms). No runtime correctness bugs identified on supported platforms. | [ACCEPTABLE] The function is ~164 lines and handles three distinct phases (enrollment exchange, WireGuard handshake, bidirectional tunnel threads) inside a single function. The Arc/Mutex wrapping is necessary for the thread split. The mild concern is that `Arc<Mutex<Option<TransportSession>>>` wraps a session that is fully established synchronously before any thread is spawned — the session value could be moved directly into each thread by cloning or splitting, avoiding the runtime lock cost. Still, this is a straightforward sequential protocol driver and the complexity tracks the domain, not gratuitous abstraction. | [NONE] No test file exists for rustguard-enroll/src/client.rs. The run function orchestrates UDP enrollment, WireGuard handshake, TUN setup, and bidirectional packet forwarding — all completely untested. Critical error paths (enrollment timeout, bad token, handshake failure) and happy path are not covered. | [UNDOCUMENTED] Primary public entry-point of the crate carries no `///` doc comment. No description of what the function does, what errors it may return, what side-effects it has (TUN creation, route installation, blocking threads), or an `# Examples` section. The module-level `//!` block provides a rough outline but does not substitute for per-symbol documentation. (deliberated: confirmed — Overengineering ACCEPTABLE is fair — the Arc<Mutex<Option<>>> wrapping is slightly heavier than needed since the session is fully established before threads spawn, but the complexity tracks the domain. Tests NONE is correct: 164 lines orchestrating enrollment, handshake, and bidirectional tunneling with zero test coverage. Documentation UNDOCUMENTED is correct: primary public entry point of the crate with no `///` doc comment describing behavior, errors, or side effects (TUN creation, route installation, blocking threads). Raising confidence as all three findings are well-evidenced.) |
| `rand_index` | L194–L198 | NONE | 85% | [USED] Internal function called locally at line 110 within run() to generate WireGuard handshake sender_index. Essential for handshake initiation. | [DUPLICATE] Identical implementation — both generate random u32 via getrandom and le_bytes conversion. Score 0.965 with same-crate target indicates textbook trivial helper duplication. | [OK] Uses `getrandom::getrandom` for CSPRNG bytes; panics on failure, which is acceptable for RNG initialisation. Conversion via `u32::from_le_bytes` is correct. | [LEAN] Minimal helper: reads 4 bytes from the OS RNG and reinterprets them as a u32. Exactly the right amount of code for this purpose. | [NONE] No test file found for this module. rand_index has zero test coverage despite being a security-relevant RNG utility used to seed the WireGuard sender index. | [UNDOCUMENTED] Private function with a reasonably self-explanatory name. No `///` doc comment, but leniency applies for private items. The name alone does not surface that it panics on RNG failure (`expect("rng")`), which is mildly useful to document. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE is correct — 100% identical implementation to server.rs::rand_index within the same crate (rustguard-enroll). Both read 4 random bytes via getrandom and convert to u32 via from_le_bytes. This is textbook same-crate duplication that should be extracted to a shared utility. Tests reclassified WEAK → NONE: the detail text explicitly states 'No test file found for this module. rand_index has zero test coverage' — there are no tests whatsoever, which is NONE not WEAK. The merged field contradicts its own detail text.) |
| `base64_key` | L200–L203 | NONE | 85% | [USED] Internal function called locally at line 69 to display the server public key in base64 format for user output. Used in enrollment confirmation message. | [DUPLICATE] Identical implementation — both encode 32-byte key to base64 string using BASE64_STANDARD. Score 0.994 indicates nearly perfect structural match. Trivial 3-line helper appears identically across multiple files. | [OK] Straightforward BASE64_STANDARD encode of a 32-byte slice; no correctness issues. | [LEAN] Thin display helper over the base64 crate. Single responsibility, minimal code. | [NONE] No test file found for this module. base64_key is a trivial formatting helper but has no tests verifying its output for known inputs. | [UNDOCUMENTED] Private helper with a self-descriptive name. No `///` doc comment, but leniency applies for private items with clear naming conventions. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE is correct — nearly identical (99%) to rustguard-daemon/src/tunnel.rs::base64_key. Both encode a [u8; 32] key to base64 string using BASE64_STANDARD.encode(). Cross-crate duplication of a trivial helper that could live in rustguard-core or a shared util. Tests reclassified WEAK → NONE: same issue as rand_index — the detail text explicitly states 'No test file found for this module. base64_key...has no tests' which is NONE, not WEAK. The merged field is inconsistent with its own evidence.) |
| `add_route` | L205–L226 | NONE | 88% | [USED] Internal function called locally at line 83 to configure OS routing for the VPN network pool via platform-specific commands (route on macOS, ip on Linux). | [UNIQUE] Despite high structural similarity (score 0.860), functions have different semantic contracts: client version has 2 parameters (cidr, ifname), returns void, prints output directly. Daemon version has 3 parameters (route, interface, is_v6), returns io::Result<Output>, and handles both IPv4/IPv6. Different signatures and different caller expectations indicate these are intentionally distinct utilities, not interchangeable duplicates. | [NEEDS_FIX] The binding `let result = …` is guarded by `#[cfg(target_os = "macos")]` and `#[cfg(target_os = "linux")]`. On any other platform (e.g. FreeBSD, OpenBSD, Windows) neither branch compiles, leaving `result` undefined when the unconditional `match result { … }` is reached, which is a compile-time error. A catch-all `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` branch (or restructuring with a single `match` on the OS) is required to compile on other targets. | [LEAN] Idiomatic use of Rust cfg attributes to dispatch to the correct platform command (route on macOS, ip on Linux). Error handling is appropriate and the logic is direct — no unnecessary abstraction layers. | [NONE] No test file found for this module. add_route shells out to platform-specific route commands (macOS/Linux) with three distinct outcomes (success, failure, command error) — none of which are tested. | [UNDOCUMENTED] Private function; leniency applies. However, the function silently swallows errors on unsupported platforms (no compile error, no `cfg` guard for non-Linux/macOS), and has platform-specific behaviour worth noting. A `///` comment explaining the platform dispatch and silent-failure behaviour would be useful even for a private item. (deliberated: confirmed — Correction NEEDS_FIX is valid — in Rust, `#[cfg(target_os = "macos")] let result = ...` and `#[cfg(target_os = "linux")] let result = ...` are two conditionally-compiled let bindings. On any other target, neither binding exists, so the unconditional `match result { ... }` on L218 references an undefined variable, causing a compile error. A `#[cfg(not(any(...)))]` fallback branch is needed. Tests NONE is correct: platform-specific shell-out with three distinct outcomes, completely untested. Documentation UNDOCUMENTED is correct: private function with platform-specific dispatch and silent error swallowing behavior that would benefit from documentation even with private-item leniency. Raising confidence as the cfg compilation issue is verifiable directly from the source.) |

### `rustguard-crypto/src/blake2s.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `hash` | L9–L15 | WEAK | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for BLAKE2s-256 hashing. | [UNIQUE] Although RAG shows 0.847 similarity to hash in rustguard-kmod (cookie.rs, noise.rs), the implementations are fundamentally different: blake2s.rs uses the Blake2s256 library directly (pure Rust), while kmod versions use unsafe C FFI calls to kernel functions. Located in separate crates with different purposes (crypto library vs kernel module wrapper). High similarity score reflects matching semantic behavior (computing BLAKE2s-256), but different implementation strategies and cross-crate context warrant UNIQUE classification. | [OK] Blake2s256 produces 32-byte output. The multi-chunk streaming update is equivalent to hashing the concatenation, and finalize().into() correctly converts the generic array to [u8; 32]. | [LEAN] Minimal wrapper accepting &[&[u8]] to avoid forced pre-concatenation by callers. Straightforward streaming update pattern with no unnecessary abstraction. | [WEAK] Three inline tests cover determinism, input sensitivity, and multi-chunk streaming equivalence. Missing: empty-input edge case, single empty-slice chunk, and crucially no known test vectors against BLAKE2s-256 spec or WireGuard reference values. For a cryptographic primitive used in WireGuard handshakes, absence of spec-verified vectors leaves correctness unconfirmed. | [PARTIAL] Public function has a single-line `/// BLAKE2s-256 hash.` doc comment. Missing parameter description for `data`, no return value description, and no `# Examples` section — all expected for a public API function. (deliberated: confirmed — Tests WEAK: Confirmed. Three inline tests cover determinism, input sensitivity, and multi-chunk streaming, which is decent for a thin wrapper. However, for a cryptographic primitive in a WireGuard implementation, the absence of any known-answer test vectors (RFC or WireGuard reference) is a legitimate gap. WEAK stands. Documentation PARTIAL: Confirmed. The single-line `/// BLAKE2s-256 hash.` doc comment is functional but sparse for a public crypto API function — missing parameter semantics and return value description.) |
| `mac` | L24–L32 | WEAK | 85% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for keyed BLAKE2s MAC. | [UNIQUE] RAG shows 0.915 and 0.906 similarity to mac in rustguard-kmod (noise.rs, cookie.rs), but signatures differ: blake2s.rs accepts multiple chunks &[&[u8]], while kmod versions accept single chunk &[u8]. Implementation strategies are fundamentally different (Blake2sMac library vs C FFI). Cross-crate duplication in different modules with different purposes. Parameter variation indicates different semantic contracts despite similar high-level goal. | [OK] Blake2sMac::<U32> correctly instantiates the keyed BLAKE2s MAC with 32-byte output. new_from_slice rejects keys longer than 32 bytes, which is the BLAKE2s maximum key length. The panic message accurately describes the constraint. result.into_bytes().into() correctly extracts [u8; 32]. | [LEAN] Clean wrapper around Blake2sMac. The explicit CtOutput binding on L30 is mildly verbose but serves as a type annotation aid in a context where the type parameter is non-trivial. Overall minimal. | [WEAK] Only two tests touch `mac`: key-sensitivity (mac_differs_with_key) and differentiation from hmac_blake2s. Missing: determinism test for mac itself, multi-chunk equivalence, behavior with a zero-length key, and no known test vectors for BLAKE2s keyed MAC. WireGuard MAC1/MAC2 callers rely on correct truncation to 16 bytes, which is never validated here. | [PARTIAL] Public function has meaningful multi-line doc comments covering its purpose, WireGuard context, keying mode distinction from HMAC, and a truncation note. However, parameters `key` and `data` are not individually described, and there is no `# Examples` section required for a public API. (deliberated: confirmed — Tests WEAK: Confirmed. Only mac_differs_with_key and hmac_differs_from_keyed_blake2s exercise mac(). No determinism test, no multi-chunk equivalence test, and crucially no known test vectors for BLAKE2s keyed MAC. For crypto code used in WireGuard MAC1/MAC2, this is insufficient. Documentation PARTIAL: Confirmed. The multi-line doc comments are genuinely good — covering WireGuard context, keying mode distinction, and truncation note. However, formal parameter descriptions for `key` and `data` are absent, and there's no `# Examples` section. PARTIAL is fair given the quality of what's there versus what's missing.) |
| `hkdf` | L40–L60 | WEAK | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for HKDF key derivation. | [UNIQUE] RAG shows 0.905 similarity to hkdf in rustguard-kmod/noise.rs, but implementations are fundamentally different: blake2s.rs implements HKDF in pure Rust using internal hmac_blake2s calls, while kmod uses a single unsafe C FFI call to wg_hkdf. Located in separate crates (crypto library vs kernel module wrapper). Same semantic outcome but drastically different code paths and implementation strategies warrant UNIQUE classification. | [OK] Matches the Noise Protocol Framework HKDF construction (section 4.4) used by WireGuard: PRK = HMAC(key, input); T1 = HMAC(PRK, 0x01); T2 = HMAC(PRK, T1||0x02); T3 = HMAC(PRK, T2||0x03). The 33-byte input buffers for T2 and T3 correctly hold the prior 32-byte output plus a counter byte. No info field is included, consistent with WireGuard's spec. | [LEAN] Direct transcription of WireGuard's three-output HKDF (extract + expand T1/T2/T3). Fixed-size stack arrays for T2/T3 inputs are idiomatic in no-alloc crypto Rust. No generalization beyond the protocol requirement. | [WEAK] Two tests cover determinism and mutual distinctness of the three outputs. No test verifies against WireGuard Noise protocol KDF test vectors, making it impossible to confirm spec compliance. Missing: key-sensitivity test (different keys produce different outputs), empty-input case, and any assertion that the expansion counters (0x01/0x02/0x03) are correctly chained. | [PARTIAL] Public function has multi-line doc comments describing WireGuard HKDF, the underlying hash, extract-then-expand structure, and the return tuple convention. Missing formal `key` and `input` parameter descriptions and no `# Examples` section, which are expected for public functions in a crypto library. (deliberated: confirmed — Tests WEAK: Confirmed. Two tests (determinism + distinct outputs) provide basic sanity but no spec-compliance verification. The WireGuard Noise protocol KDF is security-critical; absence of known test vectors means a subtle bug in counter chaining (0x01/0x02/0x03) or expansion logic could go undetected. Documentation PARTIAL: Confirmed. Good contextual documentation covering WireGuard HKDF, extract-then-expand structure, and return tuple convention. Missing formal parameter descriptions for `key` and `input`.) |
| `hmac_blake2s` | L67–L91 | WEAK | 85% | [USED] Private function called in hkdf (lines 42, 45, 51, 57) and tested in test module. | [UNIQUE] Manual RFC 2104 HMAC-BLAKE2s implementation. RAG results show only the public mac() function and C FFI wrappers as similar, which are semantically different constructs. No true duplicate of the private hmac_blake2s function found. This is a hand-written HMAC implementation, not an FFI wrapper or library delegation. | [OK] Correct RFC 2104 HMAC construction. The 32-byte key is zero-padded to the 64-byte block size (valid since 32 < 64, no pre-hashing needed). ipad = 0x36 XOR padded_key and opad = 0x5C XOR padded_key are computed correctly. Inner hash feeds ipad then data; outer hash feeds opad then inner_hash. The BLAKE2s-256 unkeyed hash is properly used as the underlying hash function, distinct from the keyed MAC mode used in mac(). | [ACCEPTABLE] Hand-rolled HMAC (RFC 2104) construction when the RustCrypto `hmac` crate (>10M weekly downloads) supports `Hmac<Blake2s256>` generically via the same `digest` trait already in use. Not installed per visible imports, so ACCEPTABLE rather than OVER — but adding `hmac` as a dependency would reduce ~25 lines to ~3 and eliminate a bespoke security-critical primitive. | [WEAK] Private function tested via hmac_test_vector (determinism + key sensitivity) and hmac_differs_from_keyed_blake2s (structural differentiation). However, hmac_test_vector does NOT compare against a precomputed RFC 2104 or WireGuard-spec value — it only asserts self-consistency. No test exercises the ipad/opad XOR logic correctness against a known answer, so a systematic bug (e.g., wrong pad constants) could pass all tests. | [DOCUMENTED] Private function with well-written `///` doc comments: explains the full HMAC construction formula, cites RFC 2104, distinguishes it from the built-in keyed BLAKE2s mode, and explains its role in WireGuard's KDF. For a private helper, `# Examples` is not expected; the inline comments in the body further clarify each step. (deliberated: confirmed — Overengineering ACCEPTABLE: Confirmed. Hand-rolling RFC 2104 HMAC in ~25 lines when the RustCrypto `hmac` crate could reduce it to ~3 lines is a valid observation. However, since `hmac` is not currently a dependency, this is a trade-off (fewer dependencies vs. more bespoke crypto code) rather than gratuitous overengineering. ACCEPTABLE is the right classification. Tests WEAK: Confirmed. The misleadingly named `hmac_test_vector` test only checks determinism and key sensitivity — it compares no actual known-answer vector. The ipad/opad XOR logic (0x36/0x5c constants) is never verified against a reference implementation. For a hand-rolled security-critical HMAC construction, this is the most concerning WEAK finding in the file. Raised confidence to 85 as I can directly verify the test code confirms this gap.) |

### `rustguard-crypto/src/x25519.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `StaticSecret` | L7–L7 | WEAK | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Core X25519 static secret key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for long-lived keys. | [NEEDS_FIX] StaticSecret::diffie_hellman (L46–49) does not validate that the resulting shared secret is non-zero. X25519 over a low-order public key point produces an all-zero output; x25519_dalek does not reject this automatically. For WireGuard (the evident use case of this crate), RFC-based implementations require the DH result to be checked against the zero value and the operation to fail if so. Silently returning a zero SharedSecret breaks the security contract of the protocol. | [LEAN] Thin newtype over x25519_dalek::StaticSecret. The wrapper is justified: it adds ZeroizeOnDrop (essential for long-lived crypto secrets), exposes a controlled API surface, and semantically distinguishes static keys from ephemeral ones at the type level — a real protocol concern in WireGuard. No unnecessary generics or layers. | [WEAK] Inline tests cover `random()`, `public_key()`, and `diffie_hellman()` via happy-path DH exchange. However, `from_bytes()`, `to_bytes()`, and `random_from_rng()` are never exercised. No known-answer vector tests, no round-trip through `from_bytes`/`to_bytes`, and zeroize-on-drop behavior is untested. The test using `random()` is behind `#[cfg(feature = "std")]` implicitly, so non-std paths (via `random_from_rng`) are wholly untested. | [PARTIAL] Type-level doc comment is present and meaningful ('A static (long-lived) X25519 secret key. Zeroized on drop.'), but several public methods — `from_bytes`, `diffie_hellman`, `public_key`, and `to_bytes` — carry no `///` comments at all. Only `random_from_rng` and `random` are individually documented. No `# Examples` section exists anywhere on the type or its impl block. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK: The evaluator claims 'RFC-based implementations require the DH result to be checked against the zero value,' but this is factually incorrect. RFC 7748 Section 6.1 explicitly states implementations 'MAY check... whether K is the all-zero value,' not MUST. The WireGuard Noise protocol framework does not mandate this check either — the KDF chain and authentication in the handshake prevent exploitation of low-order point inputs. x25519_dalek intentionally follows RFC 7748 by not rejecting zero outputs. The wrapper code correctly delegates to x25519_dalek and does not establish a security contract beyond what the RFC specifies. A zero-check would be a reasonable defense-in-depth hardening but its absence is not a correctness defect. Tests WEAK is upheld: from_bytes, to_bytes, and random_from_rng are untested; no known-answer vectors exist. Documentation PARTIAL is upheld: type-level docs exist but diffie_hellman, from_bytes, to_bytes, and public_key methods lack /// comments, which is notable in a crypto library where methods carry implicit security contracts.) |
| `EphemeralSecret` | L11–L11 | WEAK | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Ephemeral X25519 key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for single-use ephemeral keys. | [NEEDS_FIX] EphemeralSecret::diffie_hellman (L75–78) has the identical low-order point defect as StaticSecret::diffie_hellman: no check for an all-zero result. Because ephemeral DH is used in the first handshake message in WireGuard (with the initiator's ephemeral key), a crafted low-order responder public key passed here would produce a zero shared secret that propagates silently into the ChaCha20-Poly1305 key derivation chain. | [LEAN] Wraps x25519_dalek::StaticSecret internally (with a clear comment explaining why: WireGuard requires the ephemeral key to participate in multiple DH operations, which x25519_dalek::EphemeralSecret's consume-on-use API forbids). The semantic type distinction from StaticSecret is protocol-meaningful and prevents misuse. No abstraction excess. | [WEAK] The `ephemeral_dh_works` test covers `random()`, `diffie_hellman()`, and `public_key()` on the happy path. `random_from_rng()` is untested. The design comment that EphemeralSecret wraps StaticSecret specifically to allow reuse across multiple DH ops (a WireGuard protocol requirement) is never exercised in tests — no test calls `diffie_hellman()` twice on the same ephemeral key to confirm it isn't consumed. Zeroize behavior is untested. | [PARTIAL] Type-level doc comment is present ('An ephemeral X25519 secret key. Single use, zeroized on drop.'), and `random_from_rng` and `random` have brief docs (including a useful inline note about why StaticSecret is used under the hood). However, `diffie_hellman` and `public_key` are entirely undocumented. No `# Examples` section is provided for any of the public API surface. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK for the same reasons as StaticSecret: RFC 7748 does not require rejection of all-zero DH outputs, and the Noise/WireGuard protocol handles low-order point inputs through its KDF chain and MAC-based authentication. The code correctly follows the x25519 specification. Tests WEAK is upheld: random_from_rng is untested, and critically, no test exercises the multi-DH-op property that justified using StaticSecret internally (the design comment at L63-65 explains this WireGuard requirement, but no test calls diffie_hellman twice on the same ephemeral key). Documentation PARTIAL is upheld: diffie_hellman and public_key lack /// docs.) |
| `PublicKey` | L16–L16 | WEAK | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. X25519 public key with constant-time equality. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::PublicKey with constant-time comparison support. | [OK] PartialEq is correctly implemented using subtle::ConstantTimeEq to prevent timing side-channels on key comparison. Eq is provided, from_bytes and as_bytes delegate faithfully to x25519_dalek::PublicKey. No correctness issues found. | [LEAN] The custom PartialEq using subtle::ConstantTimeEq is a security necessity — without it, the derived PartialEq would introduce a timing side-channel exploitable in key-comparison paths. The wrapper is minimal and every addition is justified by the crypto threat model. | [WEAK] `public_key_roundtrip` covers `from_bytes()`, `as_bytes()`, and the `PartialEq` equality path. However, inequality (two distinct keys) is never asserted, so the constant-time `ct_eq` path in `PartialEq::eq` is not validated behaviorally. `AsRef<[u8]>` impl is untested. The security-critical property — that comparison is actually constant-time — cannot be verified by the existing assertions alone. | [PARTIAL] Type-level doc comments are informative, mentioning wire size and constant-time comparison rationale. However, neither `from_bytes` nor `as_bytes` in the `impl PublicKey` block carry any `///` documentation. The `PartialEq` and `Eq` impls are undocumented, though trait impls are tolerated. No `# Examples` section is present. (deliberated: confirmed — Tests WEAK upheld: public_key_roundtrip covers from_bytes, as_bytes, and the equality path of PartialEq, which is reasonable coverage. However, inequality (two distinct keys returning false) is never asserted, and AsRef<[u8]> is untested. The constant-time property of ct_eq cannot be verified by functional tests alone, but at minimum a behavioral inequality test should exist. Documentation PARTIAL upheld: type-level docs are good (mentioning wire size and constant-time rationale), but from_bytes and as_bytes lack method-level docs. For a crypto library, documenting from_bytes to note that X25519 does not validate subgroup membership would be valuable.) |
| `SharedSecret` | L28–L28 | WEAK | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. DH shared secret result with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps 32-byte array for DH shared secrets with Zeroize semantics. | [OK] Correctly wraps [u8; 32] with Zeroize and ZeroizeOnDrop; the derive macro can call zeroize() on u8 arrays. as_bytes() returns a reference to the inner array without copying. No correctness issues found. | [LEAN] Minimal newtype over [u8; 32] with a single as_bytes() accessor. ZeroizeOnDrop is mandatory for ephemeral DH output. The type prevents raw byte arrays from leaking out of DH operations without the zeroization guarantee. Could not be simpler while still meeting the security requirements. | [WEAK] `as_bytes()` is exercised in both DH tests to compare the two sides of the exchange, which is the primary usage. However, no known-answer test (fixed input → expected output bytes) is present, so correctness against the X25519 specification is not verified. Zeroize-on-drop behavior is untested. The struct's only method is covered but only via equality checks with no authoritative reference value. | [PARTIAL] Type-level doc comment is present but very terse ('Result of a DH operation. Zeroized on drop.'). The sole public method `as_bytes` has no `///` documentation. No guidance on how callers should consume or handle the secret bytes (e.g., pass to a KDF), and no `# Examples` section. (deliberated: confirmed — Tests WEAK upheld: as_bytes is exercised indirectly via DH exchange equality assertions, which validates the primary use case. However, no known-answer test exists to verify correctness against the X25519 specification with fixed inputs, and zeroize-on-drop is untested. Documentation PARTIAL upheld: the type-level doc is terse and as_bytes has no /// comment. For a type holding sensitive key material, documenting that callers should pass the result through a KDF before use would be valuable guidance.) |

### `rustguard-enroll/src/packet.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `parse_eth_udp` | L16–L28 | WEAK | 88% | [DEAD] Exported function with 0 workspace importers per pre-computed analysis. Matches rule 2: exported symbol with 0 runtime and 0 type-only importers equals DEAD. | [UNIQUE] Dispatcher function that routes based on ethertype (0x0800 vs 0x86DD). Fundamentally different role from protocol-specific parsers (parse_ipv4_udp, parse_ipv6_udp). Not interchangeable with them despite RAG structural similarity scores. | [OK] Ethertype is read from the correct canonical offsets (bytes 12–13) using big-endian conversion. Dispatch values 0x0800 (IPv4) and 0x86DD (IPv6) are correct. The 14-byte minimum length guard ensures the ethertype bytes are accessible before they are read. | [LEAN] Straightforward dispatcher that reads ethertype and delegates to protocol-specific parsers. The match on ethertype is the minimal correct approach for Ethernet frame demultiplexing. | [WEAK] One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only. The too-short frame branch (frame.len() < 14), the IPv6 dispatch arm (0x86DD), and the unknown-ethertype None branch are all untested. Critical caller paths (AF_XDP fast-path) demand better negative-case coverage. | [PARTIAL] Has a two-line doc comment covering purpose and the `None` return conditions, but the `frame` parameter is not described, there is no `# Examples` section, and the supported ethertype set (0x0800, 0x86DD) is not documented. `# Examples` is expected on public API functions. (deliberated: reclassified: tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD kept: 0 workspace importers per pre-computed analysis. As the main public entry point for AF_XDP packet parsing, it is likely consumed externally, but analysis scope is definitive. Tests NONE→WEAK reclassified: the evaluator's detail text states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only' — that is WEAK by definition, not NONE. The function is called at line 97 via parse_eth_udp(&frame).unwrap(). IPv6, short-frame, and unknown-ethertype branches remain untested, so WEAK is accurate. Documentation UNDOCUMENTED→PARTIAL reclassified: lines 14-15 clearly show two `///` doc comments ('Parse a raw Ethernet frame…' and 'Returns None if not…'). The evaluator's own detail confirms this. Missing parameter docs and examples make PARTIAL correct.) |
| `parse_ipv4_udp` | L30–L52 | WEAK | 85% | [USED] Non-exported function called from parse_eth_udp (line 24) within match statement. Matches rule 5: non-exported symbol with local usage equals USED. | [UNIQUE] Parses IPv4 packets with variable header length (IHL-based). Checks protocol at byte 9, extracts IP from bytes 12-15, port at offset IHL, payload at IHL+8. Semantic contract differs fundamentally from parse_ipv6_udp due to variable header, fixed field positions, and IPv4-specific invariants. Not interchangeable despite 0.819 structural similarity score. | [NEEDS_FIX] The computed IHL is never validated against its minimum legal value of 20 bytes (IHL nibble >= 5). After the initial `len < 20` guard, `ihl` can legally be 0, 4, 8, 12, or 16 for a nibble value of 0–4. In those cases `ihl + 8` is still less than the slice length (>= 20), so the second guard also passes, and `ip_data[ihl]` / `ip_data[ihl+1]` index well inside the IP header instead of the UDP source-port field. The resulting `src_port` is garbage and the wrong region is returned as the payload. A check `if ihl < 20 { return None; }` immediately after computing `ihl` is required. | [LEAN] Direct, idiomatic byte-level parsing of IPv4 + UDP headers with correct IHL handling and bounds checks. No unnecessary abstractions; each step maps 1-to-1 to the protocol spec. | [WEAK] Exercised only via the single parse_ipv4_udp_frame test which takes the minimum-IHL, valid-protocol happy path. The three early-return error branches — ip_data.len() < 20, protocol != 17, and ip_data.len() < ihl+8 (for IHL > 5) — are never tested. IHL options parsing is also unverified. | [UNDOCUMENTED] Private function (`fn`, no `pub`) with no `///` doc comments. Lenient treatment applies for private items; clear name and inline comments partially compensate, but no doc comment exists. (deliberated: confirmed — Correction NEEDS_FIX confirmed with raised confidence: the IHL bug is real and directly visible in the code. Line 35 computes `ihl = (ip_data[0] & 0x0f) * 4`. For a malformed packet with IHL nibble 0–4, ihl is 0/4/8/12/16. The guard at line 42 (`ip_data.len() < ihl + 8`) passes since len ≥ 20, so lines 46-47 read `ip_data[ihl]` and `ip_data[ihl+1]` from inside the IP header, not the UDP header. This silently returns garbage src_port and wrong payload — a clear correctness defect. Original confidence of 60 was too low; raising to 85 since the bug is directly verifiable in the source. Tests WEAK kept: only the happy path (IHL=5, protocol=17) is exercised by parse_ipv4_udp_frame. The three early-return branches and the IHL validation gap are untested. Documentation UNDOCUMENTED kept: private function with no `///` doc comments anywhere. Lenient treatment for private items acknowledged.) |
| `parse_ipv6_udp` | L54–L77 | NONE | 85% | [USED] Non-exported function called from parse_eth_udp (line 25) within match statement. Matches rule 5: non-exported symbol with local usage equals USED. | [UNIQUE] Parses IPv6 packets with fixed 40-byte header. Checks next_header at byte 6, extracts IP from bytes 8-24, port at offset 40, payload at 48. Semantic contract differs fundamentally from parse_ipv4_udp due to fixed header structure, different field positions, and IPv6-specific invariants. Parallel implementation for different protocol, not a duplicate. | [OK] The IPv6 fixed header is 40 bytes; Next Header is at byte 6, source address at bytes 8–23, and the UDP header begins at byte 40. All offsets and slice ranges are correct. The two length guards (>= 40 to read Next Header, >= 48 to cover the 8-byte UDP header) are sufficient. copy_from_slice on ip6_data[8..24] (exactly 16 bytes into a [u8; 16] array) is sound. | [LEAN] Minimal IPv6 fixed-header + UDP parser. Intentionally skips extension headers with a comment acknowledging the limitation. Complexity is appropriate for low-level AF_XDP frame parsing in a WireGuard context. | [NONE] The sole inline test uses an IPv4 frame (ethertype 0x0800), so parse_ipv6_udp is never called in any test. There is no IPv6 happy-path test, no short-buffer test, and no extension-header (next_header != 17) test. Zero coverage. | [UNDOCUMENTED] Private function (`fn`, no `pub`) with no `///` doc comments. An inline comment notes extension-header limitation (`// Not UDP (or has extension headers — skip for now).`) but this is not a doc comment. Lenient treatment applies for private items. (deliberated: confirmed — Tests NONE confirmed with raised confidence: the only test constructs an IPv4 frame (ethertype 0x0800), so parse_ipv6_udp at line 54 is never invoked in any test. Zero test coverage is directly verifiable from the test module. Documentation UNDOCUMENTED confirmed: private function with no `///` doc comments. The inline comment on line 60 is not a doc comment. Lenient treatment for private items. Original confidence of 60 was too low given the evidence is clear in the provided source.) |

### `rustguard-enroll/src/state.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `default_state_path` | L19–L21 | NONE | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Provides default path for state file. | [UNIQUE] Trivial 2-line function returning a PathBuf constant. No similar functions found. Unique by design. | [OK] Returns a hardcoded PathBuf. No logic or type issues. | [LEAN] Single-purpose function returning a hardcoded default path. No abstraction overhead; appropriate for its role as a named constant alternative. | [NONE] No test exercises default_state_path(). Neither inline test calls it — both tests supply explicit paths. The function is trivially simple but has zero test coverage. | [PARTIAL] Has a one-line `///` comment ('Default state file path.') but does not surface the concrete path value `/var/lib/rustguard/peers.state` in the doc, omits any note about configurability or platform assumptions, and provides no `# Examples` section as expected for a public function. (deliberated: confirmed — Tests NONE is technically accurate — no test invokes this function. However, it is a trivial 2-line constant-returning function, so the practical risk is negligible. Documentation PARTIAL is fair: the concrete path `/var/lib/rustguard/peers.state` is not surfaced in the doc string, requiring readers to inspect source. Both findings are low severity and I keep them as-is.) |
| `save` | L25–L45 | WEAK | 88% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Persists peer state with atomic file write. | [UNIQUE] Persists peers to state file with atomic write semantics. No similar functions found in RAG results. | [OK] Atomic write via temp-file-then-rename is correct; temp path is derived via with_extension so it is always in the same directory as the target, making rename across filesystems a non-issue. create_dir_all on an empty parent (relative path with no directory components) returns Ok(()) in Rust std, so no spurious error. BASE64_STANDARD.encode on a fixed-size array is correct. Format string produces 'key ip\n' matching the load parser. | [LEAN] Straightforward serialization to a line-oriented text format with an atomic write via rename. Parent dir creation and temp-file pattern are both justified for a daemon state file. No unnecessary abstraction layers. | [WEAK] save_and_load_roundtrip covers the happy path with two peers. Not tested: saving an empty peer slice, the create_dir_all branch (test pre-creates the dir), atomic write semantics (temp+rename), or any I/O error paths (e.g., permission denied on write). | [PARTIAL] Two `///` lines describe purpose and file format, which is useful. However, neither `path` nor `peers` parameters are documented, there is no `# Errors` section explaining the `io::Result` failure modes, and the atomic-write-via-rename behaviour (a non-obvious correctness property) appears only in the module `//!` preamble rather than on the function itself. No `# Examples` section. (deliberated: confirmed — Tests WEAK is correct: `save_and_load_roundtrip` covers the happy path with two peers but no edge cases (empty slice, IO errors, atomic write behavior). Documentation PARTIAL is fair: two doc lines describe purpose and format but omit parameter docs, `# Errors` section, and the atomic-write-via-rename semantics. Correction OK is well-justified — the temp-file-then-rename pattern is correctly implemented with same-directory guarantee via `with_extension`.) |
| `load` | L48–L84 | WEAK | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Loads and parses peer state from file. | [UNIQUE] Loads and parses peers from state file with validation. No similar functions found in RAG results. | [OK] NotFound is correctly handled as an empty list. trim() removes CR/LF remnants before split_once. split_once(' ') correctly isolates the base64 key from the IP because standard base64 contains no spaces. Vec<u8>::try_into::<[u8;32]>() is stable since Rust 1.59 and the error path is mapped correctly. ip_str.parse::<Ipv4Addr>() is correct. All error paths return InvalidData with descriptive messages. | [LEAN] Manual line-by-line parsing is proportionate to the simple 'base64_key ip' format. Not-found gracefully returns empty vec. Error mapping is explicit but necessary given io::Result return type. No reinvented wheels — the format is too trivial to justify serde. | [WEAK] Two tests cover happy path (roundtrip) and the NotFound branch. However, all error-path branches inside the parsing loop are untested: malformed line with no space (InvalidData), invalid base64 key string, key bytes not exactly 32 bytes, and unparseable IP string. These are the most likely real-world failure modes and none of them are exercised. | [PARTIAL] Single `///` line ('Load peers from the state file.') is present but extremely terse. The important non-obvious behaviour — returning `Ok(Vec::new())` rather than an error when the file is absent — is not documented on the function. No parameter docs, no `# Errors` section, and no `# Examples` section for a non-trivial public parsing function. (deliberated: confirmed — Tests WEAK is accurate: roundtrip and NotFound are covered, but all four parsing error branches (no space, bad base64, wrong key length, bad IP) are untested — these are the most likely real-world failure modes. Documentation PARTIAL is correct: the critical non-obvious behavior of returning `Ok(Vec::new())` on missing file is undocumented on the function itself. Correction OK is well-justified — all error mappings and parsing logic are sound.) |

### `rustguard-tun/examples/tun_echo.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `main` | L12–L79 | NONE | 78% | [USED] Binary entry point that orchestrates TUN interface creation, packet reading, and echo reply logic. Called implicitly by Rust runtime. | [UNIQUE] No semantic duplicates detected; unique entry point implementing TUN device echo-server pattern for ICMP handling | [NEEDS_FIX] Two correctness issues: (1) The println! at L40-44 accesses buf[12..=19] unconditionally for every Ok(n), but the guard 'version == 4 && n >= 20' only gates the proto check. Because buf is a fixed 1500-byte array this won't panic, but when n < 20 stale or zero bytes are printed as src/dst IPs — minor but incorrect log output. (2) More importantly, the ICMP processing block at L48-72 hard-codes the IP header length as exactly 20 bytes: it uses buf[9] for protocol (correct for IHL=5) and then assumes ICMP starts at byte 20 (buf[20] == 8, reply[20] = 0, &reply[20..n]). The actual header length is ((buf[0] & 0x0f) * 4) bytes. If a packet arrives with IP options (IHL > 5), buf[20] is part of the options region, not the ICMP type, so the guard condition 'buf[20] == 8' is evaluated against the wrong byte and — if it accidentally matches — the code overwrites and checksums data at the wrong offsets, producing a malformed reply. | [LEAN] Straightforward smoke-test binary: creates a TUN device, reads packets in a loop, performs inline ICMP echo reply construction with manual checksum fixup. No unnecessary abstractions, generics, or patterns. The inline packet manipulation is appropriate for a self-contained example with no reuse intent. Length is driven by inherent protocol logic, not structural complexity. | [NONE] No test file exists for this example binary. The function is a manual smoke-test entry point with an infinite read loop; it cannot be unit-tested as written, and no integration test harness exercises it. The project's rustguard-tun crate has no tests/ directory at all. | [UNDOCUMENTED] No `///` doc comment directly precedes `fn main()`. The file-level `///` block (L1–7) documents the example as a whole but is syntactically attached to the first `use` statement, not to `main`. For an example binary entry point this is tolerable, but by strict doc-comment convention the function itself is undocumented. No parameter or return-value docs exist (N/A here), and no `# Examples` section is expected on `main`. (deliberated: reclassified: documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX kept: the hardcoded IHL=5 (20-byte header) assumption is a real bug — packets with IP options would misidentify ICMP offsets and produce malformed replies. The stale-buffer println is minor but valid. However, severity is tempered because this is a smoke-test example and ICMP echo requests almost never carry IP options in practice. Tests NONE kept: example binary with an infinite read loop is not reasonably unit-testable; this is expected for examples/ code. Documentation reclassified UNDOCUMENTED → PARTIAL: the file-level doc block (L1-7) comprehensively describes the example's purpose, how to run it, and how to verify results. In idiomatic Rust, examples/ binaries are documented at file level, not on fn main(). The function is effectively documented by the file header.) |
| `icmp_checksum` | L81–L83 | WEAK | 85% | [USED] Wrapper function called at line 53 to compute ICMP checksum for echo reply packets. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Thin one-liner wrapper around `checksum_fold` that adds no logic, no documentation distinction, and no type safety. Both `icmp_checksum` and `ip_checksum` are identical delegating wrappers; collapsing them into direct calls to `checksum_fold` (or a single named alias) would eliminate the indirection without any loss of clarity. The split is premature abstraction for a single-file example. | [NONE] No test file exists for this source file. icmp_checksum is a trivial one-line delegate to checksum_fold with no independent tests. Despite being a pure, deterministic function that would be straightforward to unit-test with known ICMP vectors, it has zero test coverage. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name alone hints at purpose (ICMP checksum computation), and the one-liner body delegating to `checksum_fold` is self-evident. Per leniency rules for private items with clear names, confidence is lowered to 65. A brief doc noting the 1s-complement algorithm and the expected slice layout (20-byte ICMP segment) would be useful but is not strictly required for private code. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: the named wrapper `icmp_checksum` provides semantic clarity at the call site (`icmp_checksum(&reply[20..n])` is more self-documenting than `checksum_fold(&reply[20..n])`). For an example binary intended to teach TUN/ICMP concepts, named domain-specific wrappers are a pedagogically sound choice, not premature abstraction. The one-liner cost is trivial. Tests WEAK kept: no test coverage exists, which is expected for a private helper in an example binary. Documentation DOCUMENTED kept: name is self-descriptive, body is a single delegation — no further docs needed for private example code.) |
| `ip_checksum` | L85–L87 | WEAK | 85% | [USED] Wrapper function called at line 60 to recompute IP header checksum after swapping source and destination addresses. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Identical in structure to `icmp_checksum` — a trivial one-liner that only delegates to `checksum_fold`. Having two separate wrapper functions for what is the same RFC 1071 one's-complement checksum algorithm, differentiated only by name, is unnecessary duplication. A single `checksum_fold` call at each call site would be equally readable and remove pointless indirection. | [NONE] No test file exists for this source file. ip_checksum is a trivial one-line delegate to checksum_fold with no independent tests. As a pure function operating on a fixed 20-byte IP header, it would be easy to test with known RFC-compliant headers, but no tests are present. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Mirrors `icmp_checksum` in structure; the name makes the intent clear (IP header checksum), and the body is a single delegation to `checksum_fold`. Leniency applies for private items with descriptive names. A note clarifying the expected input (20-byte IPv4 header) would improve correctness but is not required for private helpers. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: same rationale as icmp_checksum — `ip_checksum(&reply[..20])` at the call site clearly communicates intent to recompute the IP header checksum. In an educational example, domain-specific naming aids comprehension and is worth the trivial wrapper cost. The evaluator conflated structural similarity with semantic redundancy; these wrappers serve different conceptual roles (ICMP vs IP checksum) even though both delegate to the same algorithm. Tests WEAK kept. Documentation DOCUMENTED kept.) |
| `checksum_fold` | L89–L103 | NONE | 75% | [USED] Core checksum algorithm called by both icmp_checksum (line 82) and ip_checksum (line 86). Implements RFC 1071 checksum folding. | [UNIQUE] RAG scores reflect code relationships (wrappers calling this function), not duplication. icmp_checksum and ip_checksum are trivial pass-through wrappers; checksum_fold is the actual algorithm implementation (15 LOC). Different semantic contracts and caller expectations—not interchangeable. | [OK] Implements RFC 1071 Internet checksum correctly: sums 16-bit big-endian words, pads an odd trailing byte as the high byte of a 16-bit word, folds all 32-bit carry bits until none remain, then returns the one's complement. The fold loop terminates because each iteration strictly reduces the magnitude (max two iterations needed for any u32 value reachable by summing u16s). The final !(sum as u16) cast is safe — after folding, sum fits in 16 bits. No correctness issues found. | [LEAN] Correct, minimal implementation of the RFC 1071 one's-complement Internet checksum fold. The iterative byte-pair accumulation, odd-byte handling, and carry-fold loop are all required by the algorithm. No unnecessary generics or abstractions; the function does exactly one thing. Appropriate for a no-external-deps example binary where pulling in a checksum crate would be overkill. | [NONE] No test file exists for this source file. checksum_fold is the core Internet checksum (RFC 1071) implementation with three distinct code paths: even-length input, odd-length trailing byte, and carry-folding loop. All three paths are untested. This is a critical correctness function (wrong checksums silently drop packets) that deserves thorough unit tests with known-good vectors, but has none. | [UNDOCUMENTED] Private function implementing RFC 1071 ones-complement checksum with carry folding. No `///` doc comment present. The algorithm is non-trivial — it handles odd-length slices, accumulates 16-bit words, folds the 32-bit carry, and bitwise-inverts the result — yet none of this is described. A brief doc explaining the algorithm, the odd-byte handling, and the semantics of the return value would meaningfully aid maintainers. Confidence is 72 rather than 95 because the function is private and its name ('fold') partially hints at the carry-folding step. (deliberated: confirmed — Tests NONE kept: no tests exist for this core checksum implementation. While understandable for an example binary, the function implements a non-trivial algorithm (RFC 1071) with multiple code paths (even-length, odd-byte, carry-fold) that would benefit from known-good test vectors. Documentation UNDOCUMENTED kept: unlike the trivial wrappers, checksum_fold implements a multi-step algorithm with non-obvious behavior (odd-byte padding as high byte, iterative carry folding, ones-complement inversion). The name 'checksum_fold' only hints at the carry-folding step, not the full algorithm. A brief RFC 1071 reference comment would meaningfully aid readers of this example.) |

### `rustguard-kmod/src/timers.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `now_ns` | L31–L33 | NONE | 65% | [USED] Called 10 times across timer methods (lines 65, 73, 78, 90, 101, 109, 119, 136, 144, 151) to obtain current kernel monotonic time. | [UNIQUE] Trivial wrapper (1 line of code). Calls kernel monotonic clock via unsafe FFI. | [OK] Thin unsafe wrapper delegating to the declared extern C symbol. Return type u64 is compatible with the kernel's ktime_get_ns() return type. No logic to go wrong here. | [LEAN] One-line wrapper that confines the `unsafe` FFI call to a single site, giving callers a safe interface. This is canonical Rust idiom for FFI isolation — not over-abstraction. | [NONE] No test file found for rustguard-kmod/src/timers.rs. now_ns wraps an unsafe FFI call to wg_ktime_get_ns() and has zero test coverage. The kernel FFI boundary makes direct unit testing difficult, but no integration or mock-based tests exist either. | [UNDOCUMENTED] Private function with no /// doc comment. Lenient confidence applied per private-item rule. The name and single-expression body (unsafe FFI call) are self-explanatory, but a note about the unsafe block and the kernel monotonic clock source would be appropriate. (deliberated: reclassified: tests: WEAK → NONE — Tests: The merged symbol says WEAK but the detail text explicitly states '[NONE] No test file found' and 'zero test coverage'. The evidence supports NONE — no tests exist anywhere for this function. Reclassified WEAK→NONE to match the actual detail evidence. However, this is a 1-line unsafe FFI wrapper to a kernel monotonic clock; the practical ability to unit-test it is very limited, so low confidence is appropriate. Documentation: UNDOCUMENTED is technically correct (no /// comment), but the function is private, named clearly (now_ns), and has a single-expression body. The lack of a // SAFETY: comment is the more actionable gap. Keeping UNDOCUMENTED with low confidence per private-item leniency.) |
| `SessionTimers` | L36–L49 | NONE | 88% | [USED] pub(crate) struct serving as crate-level session timer API with 6+ public methods; design and scope indicate internal use despite 0 importers in analysis (consistent with known false positive pattern for pub(crate) API types). | [UNIQUE] Core session timer state struct with 6 fields. No similar symbols found in codebase. | [NEEDS_FIX] Two logic bugs in the impl block. (1) is_dead() at L107-L112 measures elapsed time from session_established, but the doc comment says '240 seconds of inactivity' — inactivity must compare against last_received. A session that received a packet 5 seconds ago but was established 250 seconds ago is incorrectly declared dead, causing premature session-key zeroing. (2) needs_keepalive() at L115-L131: when last_sent == 0 (no packet ever sent), the else branch (L124) sets since_last_send = now.saturating_sub(last_received), making since_last_send == since_last_recv. The guard `since_last_send >= keepalive_interval_ns && since_last_recv < keepalive_interval_ns` then requires x >= K && x < K simultaneously, which is always false. A peer that has received data but never sent any will never emit a keepalive regardless of how long the interval has been exceeded. | [LEAN] Six fields, each directly corresponding to a distinct WireGuard timer checkpoint (session established, handshake sent, last rx, last tx, rekey flag, keepalive interval). No unnecessary generics, no inheritance, no factory. Every field is consumed by at least one method in the impl block. The struct is exactly as large as the protocol demands. | [NONE] No test file found for rustguard-kmod/src/timers.rs. SessionTimers contains critical WireGuard session lifecycle logic across multiple methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) that drive rekey decisions, session expiry, and keepalive scheduling. None of these methods or their time-based edge cases (boundary conditions for REKEY_AFTER_TIME_NS, REJECT_AFTER_TIME_NS, counter thresholds, keepalive logic with varying last_sent/last_received combinations) are exercised by any test. | [PARTIAL] pub(crate) struct with '/// Per-peer timer state.' at the type level and /// comments on all six fields explaining sentinel-value conventions (0 = no session, 0 = never, 0 = disabled). Passes the struct rule (type + all fields documented) but the type-level comment is a near-tautological restatement of the name rather than describing lifecycle, ownership, or how the struct is driven. No # Examples section, though pub(crate) reduces that expectation. Field-level docs are good quality. (deliberated: confirmed — Correction: Both logic bugs are independently verifiable from the source code and are genuine. (1) is_dead() at L109 measures elapsed from session_established, but both the method's doc comment ('should be zeroed') and the constant's doc comment ('240 seconds of inactivity') indicate inactivity semantics requiring last_received. A peer receiving packets 1s ago but established 241s ago is incorrectly killed. (2) needs_keepalive() when last_sent==0: since_last_send is set to now-last_received, making it equal to since_last_recv, so the guard (x >= K && x < K) is always false — verified by algebraic substitution. I increase confidence to 88 because both bugs are mechanically provable from the code. Tests: NONE is correct — no test file exists for timers.rs and no tests cover any of the 6+ session lifecycle methods. These methods contain time-based boundary logic that critically needs test coverage. Documentation: PARTIAL is correct — the struct has a type-level doc comment and all fields are documented with good sentinel-value conventions, but the type-level comment is thin and new() lacks any doc comment.) |

### `rustguard-kmod/src/allowedips.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `TrieNode` | L16–L25 | NONE | 85% | [USED] Internal radix trie node struct extensively used: instantiated via TrieNode::new() in insert, stored in AllowedIps fields, and used in all recursive traversal functions (insert_recursive, lookup_recursive, remove_peer_recursive) | [UNIQUE] No RAG data available. Struct definition for radix trie node, no similar structures found. | [NEEDS_FIX] The `bit: u32` field is documented as 'Bit position to test (0 = MSB of first byte)' — the canonical role in a compressed radix trie where nodes skip directly to the discriminating bit. However, TrieNode::new() always initialises it to 0 and no method in the impl ever writes or reads it; routing uses an implicit `depth` counter instead. The result is an uncompressed binary trie (one node per prefix bit), contradicting the module-level claim of 'compressed radix trie'. For IPv6 /128 prefixes this creates a 128-node chain, increasing memory pressure in a kernel context and risking stack-depth issues on deep recursive traversal of densely populated tables. | [LEAN] Standard binary radix-trie node mirroring the kernel WireGuard allowedips.c layout. Fields are minimal: bit position, prefix length, peer index, and two optional boxed children. The `bit` field is declared but never written by the insertion logic (depth is used instead), which is dead storage but a correctness/incompleteness issue rather than over-abstraction. No unnecessary generics or patterns. | [NONE] No test file exists for rustguard-kmod/src/allowedips.rs. TrieNode is a private internal struct; none of its construction, field values, or child-linking logic is exercised by any test in the project. | [DOCUMENTED] Private struct with a struct-level `/// A node in the radix trie.` comment and individual `///` doc comments on all four fields (`bit`, `cidr`, `peer_idx`, `children`), each describing semantics (e.g. 'Bit position to test (0 = MSB of first byte)', 'Peer index (valid when cidr > 0)'). Private items are held to a lower standard; this exceeds it. (deliberated: reclassified: tests: WEAK → NONE — Correction NEEDS_FIX: confirmed by direct source inspection — `bit` field is initialized to 0 in TrieNode::new() (line 37) and never read or written anywhere else; insertion uses a `depth` parameter instead. This makes the trie uncompressed despite the module doc claiming 'compressed radix trie'. The bit field is dead storage. Increasing confidence from 60 to 85 since the evidence is directly visible in the source. Tests WEAK → NONE: the detail text explicitly states '[NONE] No test file exists for rustguard-kmod/src/allowedips.rs. TrieNode is a private internal struct; none of its construction, field values, or child-linking logic is exercised by any test.' WEAK was a merge error.) |
| `AllowedIps` | L28–L31 | NONE | 85% | [DEAD] Exported routing table struct with 0 workspace importers; defined with public API but not instantiated or used within the module. Possible external library consumer, but no evidence in workspace analysis | [UNIQUE] No RAG data available. Struct definition for IP routing table, no duplicates in provided codebase. | [NEEDS_FIX] Two correctness issues in the impl block. (1) In `insert()`, the cidr==0 branch executes dead assignments `node.cidr = 0; node.peer_idx = peer_idx;` that are immediately overwritten — the resulting cidr value is 255, but the double-write is evidence of an incomplete or contradictory change that could mislead future edits into restoring the cidr=0 path and silently breaking default-route lookup (which gates on cidr==255). (2) In `lookup_recursive()`, the parent-side pre-check (line 177) explicitly handles `child.cidr == 255` and updates `best`, but the recursive-entry check (line 170) excludes cidr==255 via `node.cidr != 255`. Should any code path ever produce a non-root node with cidr==255, the pre-check would fire for it while the entry check silently ignores it, causing that node's peer to be recorded once but its children never to contribute to `best` — a latent incorrect-LPM result. | [LEAN] Thin wrapper holding two trie roots (IPv4/IPv6), directly matching WireGuard's dual-family routing table. Public API surface is flat and purposeful: insert_v4/v6, lookup_v4/v6, lookup_packet, remove_by_peer. No factory indirection, no trait objects, no configuration structs. The sentinel cidr==255 hack for default routes is a design smell but not an abstraction excess. | [NONE] No test file exists for this module. The critical public API — insert_v4, insert_v6, lookup_v4, lookup_v6, lookup_packet, remove_by_peer — has zero test coverage. Longest-prefix-match correctness, default-route (cidr=0) sentinel logic, IPv4/IPv6 dispatch in lookup_packet, and remove_by_peer semantics are all untested. | [PARTIAL] Has struct-level `/// AllowedIPs routing table.` but neither field (`root4`, `root6`) carries a `///` doc comment. As the primary public export of this module the struct warrants field-level documentation explaining the IPv4/IPv6 root split and lifecycle expectations. No `# Examples` section is present on the type. Terse but present at the type level, so PARTIAL rather than UNDOCUMENTED. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX: confirmed — lines 108-112 show dead assignments (`node.cidr = 0; node.peer_idx = peer_idx;`) immediately overwritten by sentinel assignments. The lookup_recursive boolean `child.cidr == 255 || (child.cidr > 0 && child.cidr != 255)` simplifies to `child.cidr > 0` — needlessly complex and asymmetric with the entry check. Both are real code quality issues. Utility DEAD → USED: AllowedIps is the sole pub(crate) struct of this module, the module doc explicitly says 'Used on the TX path', and it mirrors WireGuard's core allowedips.c data structure. A 200-line module with 6 public methods was clearly written for use by the rest of the crate. The '0 workspace importers' likely reflects incomplete workspace analysis (only this file was provided). Documentation UNDOCUMENTED → PARTIAL: source code line 28 clearly shows `/// AllowedIPs routing table.` — the struct HAS a doc comment. Fields lack docs, matching PARTIAL. The detail text itself concludes 'PARTIAL rather than UNDOCUMENTED.' Tests NONE: confirmed, no test coverage exists for any public method.) |

### `rustguard-tun/src/uring.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `BufferPool` | L26–L31 | NONE | 88% | [DEAD] Exported struct with 0 workspace importers. Published as pub field in UringTun but UringTun itself has 0 importers. Follows library module pattern seen in known false positives (ServeConfig, CookieChecker) where public APIs show 0 workspace consumers. | [UNIQUE] Pre-allocated buffer pool struct for io_uring fixed buffer I/O; no duplicates found | [NEEDS_FIX] slot_ptr (L54-56) obtains *const u8 via self.data.as_ptr() (from a &self borrow) and casts it to *mut u8. The kernel then writes through this pointer while Rust's aliasing model considers the backing Vec<u8> immutably borrowed. Without UnsafeCell wrapping the buffer storage, this violates Rust's strict aliasing rules and is formally UB; an aggressive optimizer may cache loads from self.data and miss kernel-written updates. Additionally, free() (L70-71) performs no bounds check on idx before indexing self.in_flight, causing a panic on any caller-supplied out-of-range index. | [LEAN] Fixed-buffer pool is a first-class io_uring concept (register_buffers requires stable iovecs). The struct encapsulates contiguous allocation, in-flight tracking, slot accessors, and iovec generation — all directly needed for correct io_uring fixed-buffer I/O. Linear scan in alloc() is O(256) worst-case but trivial for this pool size. No unnecessary generics or abstraction layers. | [NONE] No test file exists for rustguard-tun/src/uring.rs. BufferPool has non-trivial runtime behavior including linear-scan alloc(), free(), and bounded slot indexing. The alloc-all-then-None path, double-free, and out-of-bounds slot access are entirely untested. | [DOCUMENTED] Type-level `///` describes purpose. Both private fields carry `///` comments. No public fields to document, so the struct-and-all-fields rule is satisfied. No `# Examples` expected on a struct definition lacking public methods in its declaration block. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → DOCUMENTED — Correction NEEDS_FIX kept: slot_ptr casting *const u8 to *mut u8 from &self is a genuine aliasing violation under stacked borrows; the free() missing bounds check is a minor but valid concern. Utility reclassified DEAD→USED: this is a library crate pattern — BufferPool is pub and exposed as a pub field of UringTun, which is the module's public API entry point. Zero workspace importers is expected for library modules consumed externally. Tests NONE kept: no test file exists and BufferPool has non-trivial alloc/free logic. Documentation reclassified UNDOCUMENTED→DOCUMENTED: the source clearly shows `/// A pre-allocated buffer pool for io_uring fixed buffer I/O.` at L26, and both private fields have `///` comments (L28, L30). The detail text itself confirms DOCUMENTED; the symbol-level UNDOCUMENTED was a merge error.) |
| `UringTun` | L96–L102 | NONE | 88% | [DEAD] Exported struct with 0 workspace importers. Provides public constructor UringTun::new() and public methods (submit_write, submit_and_wait, poll). Typical library module entry point with no workspace consumers detected. | [UNIQUE] io_uring TUN I/O engine struct; no semantic duplicates found | [NEEDS_FIX] Two issues: (1) pending_reads -= 1 at L194 and L226 (in submit_and_wait and poll respectively) has no underflow guard. pending_reads is usize, so an unexpected or duplicate read completion causes a debug-mode panic or a silent release-mode wrap to usize::MAX, after which the refill logic ((NUM_BUFS/2).saturating_sub(pending_reads)) emits zero and the read pipeline permanently stalls. (2) fill_reads is called at L207/L238 while the just-completed buffer slots are still marked in_flight=true (the caller has not yet had a chance to call bufs.free()). alloc() therefore cannot reclaim those slots for the refill; the refill can only draw from slots that were free prior to this call. Under sustained load where all 256 slots are cycling through the caller, fill_reads returns 0 and read starvation occurs until the caller frees slots and calls submit_and_wait/poll again. | [LEAN] The engine composes IoUring + BufferPool + tun_fd + pending_reads into a single coherent unit. Four public methods (new, submit_write, submit_and_wait, poll) map cleanly onto distinct call-site needs. The CQ-processing block is duplicated between submit_and_wait and poll — a mild DRY concern, but not overengineering; inlining avoids extra state for what amounts to a hot loop. No unnecessary generics or factory layers. | [NONE] No test file exists for this module. UringTun has complex runtime behavior: io_uring ring creation, fixed-buffer registration, SQ push failure handling, pending_reads accounting, pipeline refill logic in both submit_and_wait() and poll(). None of this is exercised by any test. | [PARTIAL] Type-level `/// io_uring TUN I/O engine.` is present. Private field `pending_reads` has `///`. However, the public field `pub bufs: BufferPool` has no `///` doc comment at all. Per the struct rule, `///` on each public field is required for DOCUMENTED status; the missing `bufs` doc makes this PARTIAL. (deliberated: reclassified: utility: DEAD → USED — Correction NEEDS_FIX kept: pending_reads -= 1 without underflow guard at L194/L226 is a real risk — usize underflow in release mode wraps to MAX and permanently stalls the read pipeline. The fill_reads timing concern (refill before caller frees slots) is a valid design issue under sustained load. Both are legitimate. Utility reclassified DEAD→USED: UringTun is the primary public entry point of this io_uring TUN engine library module. Zero workspace importers is the standard false-positive pattern for library crates. Tests NONE kept: no test file exists and UringTun has complex stateful logic (ring setup, pending_reads accounting, pipeline refill). Documentation PARTIAL kept: the struct has a type-level doc comment, but `pub bufs: BufferPool` at L98 lacks a `///` doc comment while being the only public field.) |

## Test Improvements

- [ ] `rustguard-enroll/src/fast_udp.rs` — 6 untested
  Improve `rustguard-enroll/src/fast_udp.test.rs` covering: RecvBatch, recv_batch, sockaddr_to_socketaddr, send_packet, recv_batch, send_packet

- [ ] `rustguard-enroll/src/client.rs` — 4 untested
  Improve `rustguard-enroll/src/client.test.rs` covering: run, rand_index, base64_key, add_route

- [ ] `rustguard-crypto/src/blake2s.rs` — 4 weak
  Improve `rustguard-crypto/src/blake2s.test.rs` covering: hash, mac, hkdf, hmac_blake2s
  - hash: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for BLAKE2s-256 hashing. | [UNIQUE] Although RAG shows 0.847 similarity to hash in rustguard-kmod (cookie.rs, noise.rs), the implementations are fundamentally different: blake2s.rs uses the Blake2s256 library directly (pure Rust), while kmod versions use unsafe C FFI calls to kernel functions. Located in separate crates with different purposes (crypto library vs kernel module wrapper). High similarity score reflects matching semantic behavior (computing BLAKE2s-256), but different implementation strategies and cross-crate context warrant UNIQUE classification. | [OK] Blake2s256 produces 32-byte output. The multi-chunk streaming update is equivalent to hashing the concatenation, and finalize().into() correctly converts the generic array to [u8; 32]. | [LEAN] Minimal wrapper accepting &[&[u8]] to avoid forced pre-concatenation by callers. Straightforward streaming update pattern with no unnecessary abstraction. | [WEAK] Three inline tests cover determinism, input sensitivity, and multi-chunk streaming equivalence. Missing: empty-input edge case, single empty-slice chunk, and crucially no known test vectors against BLAKE2s-256 spec or WireGuard reference values. For a cryptographic primitive used in WireGuard handshakes, absence of spec-verified vectors leaves correctness unconfirmed. | [PARTIAL] Public function has a single-line `/// BLAKE2s-256 hash.` doc comment. Missing parameter description for `data`, no return value description, and no `# Examples` section — all expected for a public API function. (deliberated: confirmed — Tests WEAK: Confirmed. Three inline tests cover determinism, input sensitivity, and multi-chunk streaming, which is decent for a thin wrapper. However, for a cryptographic primitive in a WireGuard implementation, the absence of any known-answer test vectors (RFC or WireGuard reference) is a legitimate gap. WEAK stands. Documentation PARTIAL: Confirmed. The single-line `/// BLAKE2s-256 hash.` doc comment is functional but sparse for a public crypto API function — missing parameter semantics and return value description.)
  - mac: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for keyed BLAKE2s MAC. | [UNIQUE] RAG shows 0.915 and 0.906 similarity to mac in rustguard-kmod (noise.rs, cookie.rs), but signatures differ: blake2s.rs accepts multiple chunks &[&[u8]], while kmod versions accept single chunk &[u8]. Implementation strategies are fundamentally different (Blake2sMac library vs C FFI). Cross-crate duplication in different modules with different purposes. Parameter variation indicates different semantic contracts despite similar high-level goal. | [OK] Blake2sMac::<U32> correctly instantiates the keyed BLAKE2s MAC with 32-byte output. new_from_slice rejects keys longer than 32 bytes, which is the BLAKE2s maximum key length. The panic message accurately describes the constraint. result.into_bytes().into() correctly extracts [u8; 32]. | [LEAN] Clean wrapper around Blake2sMac. The explicit CtOutput binding on L30 is mildly verbose but serves as a type annotation aid in a context where the type parameter is non-trivial. Overall minimal. | [WEAK] Only two tests touch `mac`: key-sensitivity (mac_differs_with_key) and differentiation from hmac_blake2s. Missing: determinism test for mac itself, multi-chunk equivalence, behavior with a zero-length key, and no known test vectors for BLAKE2s keyed MAC. WireGuard MAC1/MAC2 callers rely on correct truncation to 16 bytes, which is never validated here. | [PARTIAL] Public function has meaningful multi-line doc comments covering its purpose, WireGuard context, keying mode distinction from HMAC, and a truncation note. However, parameters `key` and `data` are not individually described, and there is no `# Examples` section required for a public API. (deliberated: confirmed — Tests WEAK: Confirmed. Only mac_differs_with_key and hmac_differs_from_keyed_blake2s exercise mac(). No determinism test, no multi-chunk equivalence test, and crucially no known test vectors for BLAKE2s keyed MAC. For crypto code used in WireGuard MAC1/MAC2, this is insufficient. Documentation PARTIAL: Confirmed. The multi-line doc comments are genuinely good — covering WireGuard context, keying mode distinction, and truncation note. However, formal parameter descriptions for `key` and `data` are absent, and there's no `# Examples` section. PARTIAL is fair given the quality of what's there versus what's missing.)
  - hkdf: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for HKDF key derivation. | [UNIQUE] RAG shows 0.905 similarity to hkdf in rustguard-kmod/noise.rs, but implementations are fundamentally different: blake2s.rs implements HKDF in pure Rust using internal hmac_blake2s calls, while kmod uses a single unsafe C FFI call to wg_hkdf. Located in separate crates (crypto library vs kernel module wrapper). Same semantic outcome but drastically different code paths and implementation strategies warrant UNIQUE classification. | [OK] Matches the Noise Protocol Framework HKDF construction (section 4.4) used by WireGuard: PRK = HMAC(key, input); T1 = HMAC(PRK, 0x01); T2 = HMAC(PRK, T1||0x02); T3 = HMAC(PRK, T2||0x03). The 33-byte input buffers for T2 and T3 correctly hold the prior 32-byte output plus a counter byte. No info field is included, consistent with WireGuard's spec. | [LEAN] Direct transcription of WireGuard's three-output HKDF (extract + expand T1/T2/T3). Fixed-size stack arrays for T2/T3 inputs are idiomatic in no-alloc crypto Rust. No generalization beyond the protocol requirement. | [WEAK] Two tests cover determinism and mutual distinctness of the three outputs. No test verifies against WireGuard Noise protocol KDF test vectors, making it impossible to confirm spec compliance. Missing: key-sensitivity test (different keys produce different outputs), empty-input case, and any assertion that the expansion counters (0x01/0x02/0x03) are correctly chained. | [PARTIAL] Public function has multi-line doc comments describing WireGuard HKDF, the underlying hash, extract-then-expand structure, and the return tuple convention. Missing formal `key` and `input` parameter descriptions and no `# Examples` section, which are expected for public functions in a crypto library. (deliberated: confirmed — Tests WEAK: Confirmed. Two tests (determinism + distinct outputs) provide basic sanity but no spec-compliance verification. The WireGuard Noise protocol KDF is security-critical; absence of known test vectors means a subtle bug in counter chaining (0x01/0x02/0x03) or expansion logic could go undetected. Documentation PARTIAL: Confirmed. Good contextual documentation covering WireGuard HKDF, extract-then-expand structure, and return tuple convention. Missing formal parameter descriptions for `key` and `input`.)
  - hmac_blake2s: [USED] Private function called in hkdf (lines 42, 45, 51, 57) and tested in test module. | [UNIQUE] Manual RFC 2104 HMAC-BLAKE2s implementation. RAG results show only the public mac() function and C FFI wrappers as similar, which are semantically different constructs. No true duplicate of the private hmac_blake2s function found. This is a hand-written HMAC implementation, not an FFI wrapper or library delegation. | [OK] Correct RFC 2104 HMAC construction. The 32-byte key is zero-padded to the 64-byte block size (valid since 32 < 64, no pre-hashing needed). ipad = 0x36 XOR padded_key and opad = 0x5C XOR padded_key are computed correctly. Inner hash feeds ipad then data; outer hash feeds opad then inner_hash. The BLAKE2s-256 unkeyed hash is properly used as the underlying hash function, distinct from the keyed MAC mode used in mac(). | [ACCEPTABLE] Hand-rolled HMAC (RFC 2104) construction when the RustCrypto `hmac` crate (>10M weekly downloads) supports `Hmac<Blake2s256>` generically via the same `digest` trait already in use. Not installed per visible imports, so ACCEPTABLE rather than OVER — but adding `hmac` as a dependency would reduce ~25 lines to ~3 and eliminate a bespoke security-critical primitive. | [WEAK] Private function tested via hmac_test_vector (determinism + key sensitivity) and hmac_differs_from_keyed_blake2s (structural differentiation). However, hmac_test_vector does NOT compare against a precomputed RFC 2104 or WireGuard-spec value — it only asserts self-consistency. No test exercises the ipad/opad XOR logic correctness against a known answer, so a systematic bug (e.g., wrong pad constants) could pass all tests. | [DOCUMENTED] Private function with well-written `///` doc comments: explains the full HMAC construction formula, cites RFC 2104, distinguishes it from the built-in keyed BLAKE2s mode, and explains its role in WireGuard's KDF. For a private helper, `# Examples` is not expected; the inline comments in the body further clarify each step. (deliberated: confirmed — Overengineering ACCEPTABLE: Confirmed. Hand-rolling RFC 2104 HMAC in ~25 lines when the RustCrypto `hmac` crate could reduce it to ~3 lines is a valid observation. However, since `hmac` is not currently a dependency, this is a trade-off (fewer dependencies vs. more bespoke crypto code) rather than gratuitous overengineering. ACCEPTABLE is the right classification. Tests WEAK: Confirmed. The misleadingly named `hmac_test_vector` test only checks determinism and key sensitivity — it compares no actual known-answer vector. The ipad/opad XOR logic (0x36/0x5c constants) is never verified against a reference implementation. For a hand-rolled security-critical HMAC construction, this is the most concerning WEAK finding in the file. Raised confidence to 85 as I can directly verify the test code confirms this gap.)

- [ ] `rustguard-crypto/src/x25519.rs` — 4 weak
  Improve `rustguard-crypto/src/x25519.test.rs` covering: StaticSecret, EphemeralSecret, PublicKey, SharedSecret
  - StaticSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Core X25519 static secret key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for long-lived keys. | [NEEDS_FIX] StaticSecret::diffie_hellman (L46–49) does not validate that the resulting shared secret is non-zero. X25519 over a low-order public key point produces an all-zero output; x25519_dalek does not reject this automatically. For WireGuard (the evident use case of this crate), RFC-based implementations require the DH result to be checked against the zero value and the operation to fail if so. Silently returning a zero SharedSecret breaks the security contract of the protocol. | [LEAN] Thin newtype over x25519_dalek::StaticSecret. The wrapper is justified: it adds ZeroizeOnDrop (essential for long-lived crypto secrets), exposes a controlled API surface, and semantically distinguishes static keys from ephemeral ones at the type level — a real protocol concern in WireGuard. No unnecessary generics or layers. | [WEAK] Inline tests cover `random()`, `public_key()`, and `diffie_hellman()` via happy-path DH exchange. However, `from_bytes()`, `to_bytes()`, and `random_from_rng()` are never exercised. No known-answer vector tests, no round-trip through `from_bytes`/`to_bytes`, and zeroize-on-drop behavior is untested. The test using `random()` is behind `#[cfg(feature = "std")]` implicitly, so non-std paths (via `random_from_rng`) are wholly untested. | [PARTIAL] Type-level doc comment is present and meaningful ('A static (long-lived) X25519 secret key. Zeroized on drop.'), but several public methods — `from_bytes`, `diffie_hellman`, `public_key`, and `to_bytes` — carry no `///` comments at all. Only `random_from_rng` and `random` are individually documented. No `# Examples` section exists anywhere on the type or its impl block. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK: The evaluator claims 'RFC-based implementations require the DH result to be checked against the zero value,' but this is factually incorrect. RFC 7748 Section 6.1 explicitly states implementations 'MAY check... whether K is the all-zero value,' not MUST. The WireGuard Noise protocol framework does not mandate this check either — the KDF chain and authentication in the handshake prevent exploitation of low-order point inputs. x25519_dalek intentionally follows RFC 7748 by not rejecting zero outputs. The wrapper code correctly delegates to x25519_dalek and does not establish a security contract beyond what the RFC specifies. A zero-check would be a reasonable defense-in-depth hardening but its absence is not a correctness defect. Tests WEAK is upheld: from_bytes, to_bytes, and random_from_rng are untested; no known-answer vectors exist. Documentation PARTIAL is upheld: type-level docs exist but diffie_hellman, from_bytes, to_bytes, and public_key methods lack /// comments, which is notable in a crypto library where methods carry implicit security contracts.)
  - EphemeralSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Ephemeral X25519 key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for single-use ephemeral keys. | [NEEDS_FIX] EphemeralSecret::diffie_hellman (L75–78) has the identical low-order point defect as StaticSecret::diffie_hellman: no check for an all-zero result. Because ephemeral DH is used in the first handshake message in WireGuard (with the initiator's ephemeral key), a crafted low-order responder public key passed here would produce a zero shared secret that propagates silently into the ChaCha20-Poly1305 key derivation chain. | [LEAN] Wraps x25519_dalek::StaticSecret internally (with a clear comment explaining why: WireGuard requires the ephemeral key to participate in multiple DH operations, which x25519_dalek::EphemeralSecret's consume-on-use API forbids). The semantic type distinction from StaticSecret is protocol-meaningful and prevents misuse. No abstraction excess. | [WEAK] The `ephemeral_dh_works` test covers `random()`, `diffie_hellman()`, and `public_key()` on the happy path. `random_from_rng()` is untested. The design comment that EphemeralSecret wraps StaticSecret specifically to allow reuse across multiple DH ops (a WireGuard protocol requirement) is never exercised in tests — no test calls `diffie_hellman()` twice on the same ephemeral key to confirm it isn't consumed. Zeroize behavior is untested. | [PARTIAL] Type-level doc comment is present ('An ephemeral X25519 secret key. Single use, zeroized on drop.'), and `random_from_rng` and `random` have brief docs (including a useful inline note about why StaticSecret is used under the hood). However, `diffie_hellman` and `public_key` are entirely undocumented. No `# Examples` section is provided for any of the public API surface. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK for the same reasons as StaticSecret: RFC 7748 does not require rejection of all-zero DH outputs, and the Noise/WireGuard protocol handles low-order point inputs through its KDF chain and MAC-based authentication. The code correctly follows the x25519 specification. Tests WEAK is upheld: random_from_rng is untested, and critically, no test exercises the multi-DH-op property that justified using StaticSecret internally (the design comment at L63-65 explains this WireGuard requirement, but no test calls diffie_hellman twice on the same ephemeral key). Documentation PARTIAL is upheld: diffie_hellman and public_key lack /// docs.)
  - PublicKey: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. X25519 public key with constant-time equality. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::PublicKey with constant-time comparison support. | [OK] PartialEq is correctly implemented using subtle::ConstantTimeEq to prevent timing side-channels on key comparison. Eq is provided, from_bytes and as_bytes delegate faithfully to x25519_dalek::PublicKey. No correctness issues found. | [LEAN] The custom PartialEq using subtle::ConstantTimeEq is a security necessity — without it, the derived PartialEq would introduce a timing side-channel exploitable in key-comparison paths. The wrapper is minimal and every addition is justified by the crypto threat model. | [WEAK] `public_key_roundtrip` covers `from_bytes()`, `as_bytes()`, and the `PartialEq` equality path. However, inequality (two distinct keys) is never asserted, so the constant-time `ct_eq` path in `PartialEq::eq` is not validated behaviorally. `AsRef<[u8]>` impl is untested. The security-critical property — that comparison is actually constant-time — cannot be verified by the existing assertions alone. | [PARTIAL] Type-level doc comments are informative, mentioning wire size and constant-time comparison rationale. However, neither `from_bytes` nor `as_bytes` in the `impl PublicKey` block carry any `///` documentation. The `PartialEq` and `Eq` impls are undocumented, though trait impls are tolerated. No `# Examples` section is present. (deliberated: confirmed — Tests WEAK upheld: public_key_roundtrip covers from_bytes, as_bytes, and the equality path of PartialEq, which is reasonable coverage. However, inequality (two distinct keys returning false) is never asserted, and AsRef<[u8]> is untested. The constant-time property of ct_eq cannot be verified by functional tests alone, but at minimum a behavioral inequality test should exist. Documentation PARTIAL upheld: type-level docs are good (mentioning wire size and constant-time rationale), but from_bytes and as_bytes lack method-level docs. For a crypto library, documenting from_bytes to note that X25519 does not validate subgroup membership would be valuable.)
  - SharedSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. DH shared secret result with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps 32-byte array for DH shared secrets with Zeroize semantics. | [OK] Correctly wraps [u8; 32] with Zeroize and ZeroizeOnDrop; the derive macro can call zeroize() on u8 arrays. as_bytes() returns a reference to the inner array without copying. No correctness issues found. | [LEAN] Minimal newtype over [u8; 32] with a single as_bytes() accessor. ZeroizeOnDrop is mandatory for ephemeral DH output. The type prevents raw byte arrays from leaking out of DH operations without the zeroization guarantee. Could not be simpler while still meeting the security requirements. | [WEAK] `as_bytes()` is exercised in both DH tests to compare the two sides of the exchange, which is the primary usage. However, no known-answer test (fixed input → expected output bytes) is present, so correctness against the X25519 specification is not verified. Zeroize-on-drop behavior is untested. The struct's only method is covered but only via equality checks with no authoritative reference value. | [PARTIAL] Type-level doc comment is present but very terse ('Result of a DH operation. Zeroized on drop.'). The sole public method `as_bytes` has no `///` documentation. No guidance on how callers should consume or handle the secret bytes (e.g., pass to a KDF), and no `# Examples` section. (deliberated: confirmed — Tests WEAK upheld: as_bytes is exercised indirectly via DH exchange equality assertions, which validates the primary use case. However, no known-answer test exists to verify correctness against the X25519 specification with fixed inputs, and zeroize-on-drop is untested. Documentation PARTIAL upheld: the type-level doc is terse and as_bytes has no /// comment. For a type holding sensitive key material, documenting that callers should pass the result through a KDF before use would be valuable guidance.)

- [ ] `rustguard-enroll/src/packet.rs` — 1 untested, 2 weak
  Improve `rustguard-enroll/src/packet.test.rs` covering: parse_eth_udp, parse_ipv4_udp, parse_ipv6_udp
  - parse_eth_udp: [DEAD] Exported function with 0 workspace importers per pre-computed analysis. Matches rule 2: exported symbol with 0 runtime and 0 type-only importers equals DEAD. | [UNIQUE] Dispatcher function that routes based on ethertype (0x0800 vs 0x86DD). Fundamentally different role from protocol-specific parsers (parse_ipv4_udp, parse_ipv6_udp). Not interchangeable with them despite RAG structural similarity scores. | [OK] Ethertype is read from the correct canonical offsets (bytes 12–13) using big-endian conversion. Dispatch values 0x0800 (IPv4) and 0x86DD (IPv6) are correct. The 14-byte minimum length guard ensures the ethertype bytes are accessible before they are read. | [LEAN] Straightforward dispatcher that reads ethertype and delegates to protocol-specific parsers. The match on ethertype is the minimal correct approach for Ethernet frame demultiplexing. | [WEAK] One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only. The too-short frame branch (frame.len() < 14), the IPv6 dispatch arm (0x86DD), and the unknown-ethertype None branch are all untested. Critical caller paths (AF_XDP fast-path) demand better negative-case coverage. | [PARTIAL] Has a two-line doc comment covering purpose and the `None` return conditions, but the `frame` parameter is not described, there is no `# Examples` section, and the supported ethertype set (0x0800, 0x86DD) is not documented. `# Examples` is expected on public API functions. (deliberated: reclassified: tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD kept: 0 workspace importers per pre-computed analysis. As the main public entry point for AF_XDP packet parsing, it is likely consumed externally, but analysis scope is definitive. Tests NONE→WEAK reclassified: the evaluator's detail text states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only' — that is WEAK by definition, not NONE. The function is called at line 97 via parse_eth_udp(&frame).unwrap(). IPv6, short-frame, and unknown-ethertype branches remain untested, so WEAK is accurate. Documentation UNDOCUMENTED→PARTIAL reclassified: lines 14-15 clearly show two `///` doc comments ('Parse a raw Ethernet frame…' and 'Returns None if not…'). The evaluator's own detail confirms this. Missing parameter docs and examples make PARTIAL correct.)
  - parse_ipv4_udp: [USED] Non-exported function called from parse_eth_udp (line 24) within match statement. Matches rule 5: non-exported symbol with local usage equals USED. | [UNIQUE] Parses IPv4 packets with variable header length (IHL-based). Checks protocol at byte 9, extracts IP from bytes 12-15, port at offset IHL, payload at IHL+8. Semantic contract differs fundamentally from parse_ipv6_udp due to variable header, fixed field positions, and IPv4-specific invariants. Not interchangeable despite 0.819 structural similarity score. | [NEEDS_FIX] The computed IHL is never validated against its minimum legal value of 20 bytes (IHL nibble >= 5). After the initial `len < 20` guard, `ihl` can legally be 0, 4, 8, 12, or 16 for a nibble value of 0–4. In those cases `ihl + 8` is still less than the slice length (>= 20), so the second guard also passes, and `ip_data[ihl]` / `ip_data[ihl+1]` index well inside the IP header instead of the UDP source-port field. The resulting `src_port` is garbage and the wrong region is returned as the payload. A check `if ihl < 20 { return None; }` immediately after computing `ihl` is required. | [LEAN] Direct, idiomatic byte-level parsing of IPv4 + UDP headers with correct IHL handling and bounds checks. No unnecessary abstractions; each step maps 1-to-1 to the protocol spec. | [WEAK] Exercised only via the single parse_ipv4_udp_frame test which takes the minimum-IHL, valid-protocol happy path. The three early-return error branches — ip_data.len() < 20, protocol != 17, and ip_data.len() < ihl+8 (for IHL > 5) — are never tested. IHL options parsing is also unverified. | [UNDOCUMENTED] Private function (`fn`, no `pub`) with no `///` doc comments. Lenient treatment applies for private items; clear name and inline comments partially compensate, but no doc comment exists. (deliberated: confirmed — Correction NEEDS_FIX confirmed with raised confidence: the IHL bug is real and directly visible in the code. Line 35 computes `ihl = (ip_data[0] & 0x0f) * 4`. For a malformed packet with IHL nibble 0–4, ihl is 0/4/8/12/16. The guard at line 42 (`ip_data.len() < ihl + 8`) passes since len ≥ 20, so lines 46-47 read `ip_data[ihl]` and `ip_data[ihl+1]` from inside the IP header, not the UDP header. This silently returns garbage src_port and wrong payload — a clear correctness defect. Original confidence of 60 was too low; raising to 85 since the bug is directly verifiable in the source. Tests WEAK kept: only the happy path (IHL=5, protocol=17) is exercised by parse_ipv4_udp_frame. The three early-return branches and the IHL validation gap are untested. Documentation UNDOCUMENTED kept: private function with no `///` doc comments anywhere. Lenient treatment for private items acknowledged.)

- [ ] `rustguard-enroll/src/state.rs` — 1 untested, 2 weak
  Improve `rustguard-enroll/src/state.test.rs` covering: default_state_path, save, load
  - save: [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Persists peer state with atomic file write. | [UNIQUE] Persists peers to state file with atomic write semantics. No similar functions found in RAG results. | [OK] Atomic write via temp-file-then-rename is correct; temp path is derived via with_extension so it is always in the same directory as the target, making rename across filesystems a non-issue. create_dir_all on an empty parent (relative path with no directory components) returns Ok(()) in Rust std, so no spurious error. BASE64_STANDARD.encode on a fixed-size array is correct. Format string produces 'key ip\n' matching the load parser. | [LEAN] Straightforward serialization to a line-oriented text format with an atomic write via rename. Parent dir creation and temp-file pattern are both justified for a daemon state file. No unnecessary abstraction layers. | [WEAK] save_and_load_roundtrip covers the happy path with two peers. Not tested: saving an empty peer slice, the create_dir_all branch (test pre-creates the dir), atomic write semantics (temp+rename), or any I/O error paths (e.g., permission denied on write). | [PARTIAL] Two `///` lines describe purpose and file format, which is useful. However, neither `path` nor `peers` parameters are documented, there is no `# Errors` section explaining the `io::Result` failure modes, and the atomic-write-via-rename behaviour (a non-obvious correctness property) appears only in the module `//!` preamble rather than on the function itself. No `# Examples` section. (deliberated: confirmed — Tests WEAK is correct: `save_and_load_roundtrip` covers the happy path with two peers but no edge cases (empty slice, IO errors, atomic write behavior). Documentation PARTIAL is fair: two doc lines describe purpose and format but omit parameter docs, `# Errors` section, and the atomic-write-via-rename semantics. Correction OK is well-justified — the temp-file-then-rename pattern is correctly implemented with same-directory guarantee via `with_extension`.)
  - load: [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Loads and parses peer state from file. | [UNIQUE] Loads and parses peers from state file with validation. No similar functions found in RAG results. | [OK] NotFound is correctly handled as an empty list. trim() removes CR/LF remnants before split_once. split_once(' ') correctly isolates the base64 key from the IP because standard base64 contains no spaces. Vec<u8>::try_into::<[u8;32]>() is stable since Rust 1.59 and the error path is mapped correctly. ip_str.parse::<Ipv4Addr>() is correct. All error paths return InvalidData with descriptive messages. | [LEAN] Manual line-by-line parsing is proportionate to the simple 'base64_key ip' format. Not-found gracefully returns empty vec. Error mapping is explicit but necessary given io::Result return type. No reinvented wheels — the format is too trivial to justify serde. | [WEAK] Two tests cover happy path (roundtrip) and the NotFound branch. However, all error-path branches inside the parsing loop are untested: malformed line with no space (InvalidData), invalid base64 key string, key bytes not exactly 32 bytes, and unparseable IP string. These are the most likely real-world failure modes and none of them are exercised. | [PARTIAL] Single `///` line ('Load peers from the state file.') is present but extremely terse. The important non-obvious behaviour — returning `Ok(Vec::new())` rather than an error when the file is absent — is not documented on the function. No parameter docs, no `# Errors` section, and no `# Examples` section for a non-trivial public parsing function. (deliberated: confirmed — Tests WEAK is accurate: roundtrip and NotFound are covered, but all four parsing error branches (no space, bad base64, wrong key length, bad IP) are untested — these are the most likely real-world failure modes. Documentation PARTIAL is correct: the critical non-obvious behavior of returning `Ok(Vec::new())` on missing file is undocumented on the function itself. Correction OK is well-justified — all error mappings and parsing logic are sound.)

- [ ] `rustguard-tun/examples/tun_echo.rs` — 2 untested, 2 weak
  Improve `rustguard-tun/examples/tun_echo.test.rs` covering: main, icmp_checksum, ip_checksum, checksum_fold
  - icmp_checksum: [USED] Wrapper function called at line 53 to compute ICMP checksum for echo reply packets. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Thin one-liner wrapper around `checksum_fold` that adds no logic, no documentation distinction, and no type safety. Both `icmp_checksum` and `ip_checksum` are identical delegating wrappers; collapsing them into direct calls to `checksum_fold` (or a single named alias) would eliminate the indirection without any loss of clarity. The split is premature abstraction for a single-file example. | [NONE] No test file exists for this source file. icmp_checksum is a trivial one-line delegate to checksum_fold with no independent tests. Despite being a pure, deterministic function that would be straightforward to unit-test with known ICMP vectors, it has zero test coverage. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name alone hints at purpose (ICMP checksum computation), and the one-liner body delegating to `checksum_fold` is self-evident. Per leniency rules for private items with clear names, confidence is lowered to 65. A brief doc noting the 1s-complement algorithm and the expected slice layout (20-byte ICMP segment) would be useful but is not strictly required for private code. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: the named wrapper `icmp_checksum` provides semantic clarity at the call site (`icmp_checksum(&reply[20..n])` is more self-documenting than `checksum_fold(&reply[20..n])`). For an example binary intended to teach TUN/ICMP concepts, named domain-specific wrappers are a pedagogically sound choice, not premature abstraction. The one-liner cost is trivial. Tests WEAK kept: no test coverage exists, which is expected for a private helper in an example binary. Documentation DOCUMENTED kept: name is self-descriptive, body is a single delegation — no further docs needed for private example code.)
  - ip_checksum: [USED] Wrapper function called at line 60 to recompute IP header checksum after swapping source and destination addresses. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Identical in structure to `icmp_checksum` — a trivial one-liner that only delegates to `checksum_fold`. Having two separate wrapper functions for what is the same RFC 1071 one's-complement checksum algorithm, differentiated only by name, is unnecessary duplication. A single `checksum_fold` call at each call site would be equally readable and remove pointless indirection. | [NONE] No test file exists for this source file. ip_checksum is a trivial one-line delegate to checksum_fold with no independent tests. As a pure function operating on a fixed 20-byte IP header, it would be easy to test with known RFC-compliant headers, but no tests are present. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Mirrors `icmp_checksum` in structure; the name makes the intent clear (IP header checksum), and the body is a single delegation to `checksum_fold`. Leniency applies for private items with descriptive names. A note clarifying the expected input (20-byte IPv4 header) would improve correctness but is not required for private helpers. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: same rationale as icmp_checksum — `ip_checksum(&reply[..20])` at the call site clearly communicates intent to recompute the IP header checksum. In an educational example, domain-specific naming aids comprehension and is worth the trivial wrapper cost. The evaluator conflated structural similarity with semantic redundancy; these wrappers serve different conceptual roles (ICMP vs IP checksum) even though both delegate to the same algorithm. Tests WEAK kept. Documentation DOCUMENTED kept.)

- [ ] `rustguard-kmod/src/timers.rs` — 2 untested
  Improve `rustguard-kmod/src/timers.test.rs` covering: now_ns, SessionTimers

- [ ] `rustguard-kmod/src/allowedips.rs` — 2 untested
  Improve `rustguard-kmod/src/allowedips.test.rs` covering: TrieNode, AllowedIps

- [ ] `rustguard-tun/src/uring.rs` — 2 untested
  Improve `rustguard-tun/src/uring.test.rs` covering: BufferPool, UringTun
