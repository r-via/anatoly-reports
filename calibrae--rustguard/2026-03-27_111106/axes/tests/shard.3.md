[← Back to Tests](./index.md) · [← Back to report](../../public_report.md)

# Tests — Shard 3

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-enroll/src/fast_udp.rs` | NEEDS_REFACTOR | 6 | 85% | [details](../reviews/rustguard-enroll-src-fast_udp.rev.md) |
| `rustguard-enroll/src/client.rs` | NEEDS_REFACTOR | 4 | 88% | [details](../reviews/rustguard-enroll-src-client.rev.md) |
| `rustguard-crypto/src/blake2s.rs` | NEEDS_REFACTOR | 4 | 90% | [details](../reviews/rustguard-crypto-src-blake2s.rev.md) |
| `rustguard-crypto/src/x25519.rs` | NEEDS_REFACTOR | 4 | 88% | [details](../reviews/rustguard-crypto-src-x25519.rev.md) |
| `rustguard-enroll/src/packet.rs` | NEEDS_REFACTOR | 3 | 88% | [details](../reviews/rustguard-enroll-src-packet.rev.md) |
| `rustguard-enroll/src/state.rs` | NEEDS_REFACTOR | 3 | 88% | [details](../reviews/rustguard-enroll-src-state.rev.md) |
| `rustguard-tun/examples/tun_echo.rs` | NEEDS_REFACTOR | 4 | 85% | [details](../reviews/rustguard-tun-examples-tun_echo.rev.md) |
| `rustguard-kmod/src/timers.rs` | NEEDS_REFACTOR | 2 | 95% | [details](../reviews/rustguard-kmod-src-timers.rev.md) |
| `rustguard-kmod/src/allowedips.rs` | NEEDS_REFACTOR | 2 | 85% | [details](../reviews/rustguard-kmod-src-allowedips.rev.md) |
| `rustguard-tun/src/uring.rs` | NEEDS_REFACTOR | 2 | 90% | [details](../reviews/rustguard-tun-src-uring.rev.md) |

## Symbol Details

### `rustguard-enroll/src/fast_udp.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `RecvBatch` | L19–L24 | NONE | 85% | [USED] Public struct; primary type returned by recv_batch in library API | [U... |
| `recv_batch` | L39–L83 | NONE | 80% | [USED] macOS fallback implementation; public library API for batch receives |... |
| `sockaddr_to_socketaddr` | L86–L102 | NONE | 60% | [USED] Called within recv_batch at line 79; internal helper for address conve... |
| `send_packet` | L106–L108 | NONE | 85% | [USED] macOS fallback implementation; public library API for packet transmiss... |
| `recv_batch` | L112–L126 | NONE | 80% | [USED] macOS fallback implementation; public library API for batch receives |... |
| `send_packet` | L129–L131 | NONE | 85% | [USED] macOS fallback implementation; public library API for packet transmiss... |

### `rustguard-enroll/src/client.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `run` | L28–L192 | NONE | 85% | [USED] Public function in library crate with 0 workspace importers. Pattern f... |
| `rand_index` | L194–L198 | NONE | 85% | [USED] Internal function called locally at line 110 within run() to generate ... |
| `base64_key` | L200–L203 | NONE | 85% | [USED] Internal function called locally at line 69 to display the server publ... |
| `add_route` | L205–L226 | NONE | 88% | [USED] Internal function called locally at line 83 to configure OS routing fo... |

### `rustguard-crypto/src/blake2s.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `hash` | L9–L15 | WEAK | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for ... |
| `mac` | L24–L32 | WEAK | 85% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for ... |
| `hkdf` | L40–L60 | WEAK | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for ... |
| `hmac_blake2s` | L67–L91 | WEAK | 85% | [USED] Private function called in hkdf (lines 42, 45, 51, 57) and tested in t... |

### `rustguard-crypto/src/x25519.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `StaticSecret` | L7–L7 | WEAK | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Core ... |
| `EphemeralSecret` | L11–L11 | WEAK | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Ephem... |
| `PublicKey` | L16–L16 | WEAK | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. X2551... |
| `SharedSecret` | L28–L28 | WEAK | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. DH sh... |

### `rustguard-enroll/src/packet.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `parse_eth_udp` | L16–L28 | WEAK | 88% | [DEAD] Exported function with 0 workspace importers per pre-computed analysis... |
| `parse_ipv4_udp` | L30–L52 | WEAK | 85% | [USED] Non-exported function called from parse_eth_udp (line 24) within match... |
| `parse_ipv6_udp` | L54–L77 | NONE | 85% | [USED] Non-exported function called from parse_eth_udp (line 25) within match... |

### `rustguard-enroll/src/state.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `default_state_path` | L19–L21 | NONE | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server... |
| `save` | L25–L45 | WEAK | 88% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server... |
| `load` | L48–L84 | WEAK | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server... |

### `rustguard-tun/examples/tun_echo.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `main` | L12–L79 | NONE | 78% | [USED] Binary entry point that orchestrates TUN interface creation, packet re... |
| `icmp_checksum` | L81–L83 | WEAK | 85% | [USED] Wrapper function called at line 53 to compute ICMP checksum for echo r... |
| `ip_checksum` | L85–L87 | WEAK | 85% | [USED] Wrapper function called at line 60 to recompute IP header checksum aft... |
| `checksum_fold` | L89–L103 | NONE | 75% | [USED] Core checksum algorithm called by both icmp_checksum (line 82) and ip_... |

### `rustguard-kmod/src/timers.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `now_ns` | L31–L33 | NONE | 65% | [USED] Called 10 times across timer methods (lines 65, 73, 78, 90, 101, 109, ... |
| `SessionTimers` | L36–L49 | NONE | 88% | [USED] pub(crate) struct serving as crate-level session timer API with 6+ pub... |

### `rustguard-kmod/src/allowedips.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `TrieNode` | L16–L25 | NONE | 85% | [USED] Internal radix trie node struct extensively used: instantiated via Tri... |
| `AllowedIps` | L28–L31 | NONE | 85% | [DEAD] Exported routing table struct with 0 workspace importers; defined with... |

### `rustguard-tun/src/uring.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `BufferPool` | L26–L31 | NONE | 88% | [DEAD] Exported struct with 0 workspace importers. Published as pub field in ... |
| `UringTun` | L96–L102 | NONE | 88% | [DEAD] Exported struct with 0 workspace importers. Provides public constructo... |

## Test Improvements

- [ ] `rustguard-enroll/src/fast_udp.rs` — 6 untested
  Improve `rustguard-enroll/src/fast_udp.test.rs` covering: RecvBatch, recv_batch, sockaddr_to_socketaddr, send_packet, recv_batch, send_packet

- [ ] `rustguard-enroll/src/client.rs` — 4 untested
  Improve `rustguard-enroll/src/client.test.rs` covering: run, rand_index, base64_key, add_route

- [ ] `rustguard-crypto/src/blake2s.rs` — 4 weak
  Improve `rustguard-crypto/src/blake2s.test.rs` covering: hash, mac, hkdf, hmac_blake2s
  - hash: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for BLAKE2s-256 hashing. | [UNIQUE] Although RAG shows 0.847 similarity to hash in rustguard-kmod (cookie.rs, noise.rs), the implementations are fundamentally different: blake2s.rs uses the Blake2s256 library directly (pure Rust), while kmod versions use unsafe C FFI calls to kernel functions. Located in separate crates with different purposes (crypto library vs kernel module wrapper). High similarity score reflects matching semantic behavior (computing BLAKE2s-256), but different implementation strategies and cross-crate context warrant UNIQUE classification. | [OK] Blake2s256 produces 32-byte output. The multi-chunk streaming update is equivalent to hashing the concatenation, and finalize().into() correctly converts the generic array to [u8; 32]. | [LEAN] Minimal wrapper accepting &[&[u8]] to avoid forced pre-concatenation by callers. Straightforward streaming update pattern with no unnecessary abstraction. | [WEAK] Three inline tests cover determinism, input sensitivity, and multi-chunk streaming equivalence. Missing: empty-input edge case, single empty-slice chunk, and crucially no known test vectors against BLAKE2s-256 spec or WireGuard reference values. For a cryptographic primitive used in WireGuard handshakes, absence of spec-verified vectors leaves correctness unconfirmed. | [PARTIAL] Public function has a single-line `/// BLAKE2s-256 hash.` doc comment. Missing parameter description for `data`, no return value description, and no `# Examples` section — all expected for a public API function. (deliberated: confirmed — Tests WEAK: Confirmed. Three inline tests cover determinism, input sensitivity, and multi-chunk streaming, which is decent for a thin wrapper. However, for a cryptographic primitive in a WireGuard implementation, the absence of any known-answer test vectors (RFC or WireGuard reference) is a legitimate gap. WEAK stands. Documentation PARTIAL: Confirmed. The single-line `/// BLAKE2s-256 hash.` doc comment is functional but sparse for a public crypto API function — missing parameter semantics and return value description.)
  - mac: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for keyed BLAKE2s MAC. | [UNIQUE] RAG shows 0.915 and 0.906 similarity to mac in rustguard-kmod (noise.rs, cookie.rs), but signatures differ: blake2s.rs accepts multiple chunks &[&[u8]], while kmod versions accept single chunk &[u8]. Implementation strategies are fundamentally different (Blake2sMac library vs C FFI). Cross-crate duplication in different modules with different purposes. Parameter variation indicates different semantic contracts despite similar high-level goal. | [OK] Blake2sMac::<U32> correctly instantiates the keyed BLAKE2s MAC with 32-byte output. new_from_slice rejects keys longer than 32 bytes, which is the BLAKE2s maximum key length. The panic message accurately describes the constraint. result.into_bytes().into() correctly extracts [u8; 32]. | [LEAN] Clean wrapper around Blake2sMac. The explicit CtOutput binding on L30 is mildly verbose but serves as a type annotation aid in a context where the type parameter is non-trivial. Overall minimal. | [WEAK] Only two tests touch `mac`: key-sensitivity (mac_differs_with_key) and differentiation from hmac_blake2s. Missing: determinism test for mac itself, multi-chunk equivalence, behavior with a zero-length key, and no known test vectors for BLAKE2s keyed MAC. WireGuard MAC1/MAC2 callers rely on correct truncation to 16 bytes, which is never validated here. | [PARTIAL] Public function has meaningful multi-line doc comments covering its purpose, WireGuard context, keying mode distinction from HMAC, and a truncation note. However, parameters `key` and `data` are not individually described, and there is no `# Examples` section required for a public API. (deliberated: confirmed — Tests WEAK: Confirmed. Only mac_differs_with_key and hmac_differs_from_keyed_blake2s exercise mac(). No determinism test, no multi-chunk equivalence test, and crucially no known test vectors for BLAKE2s keyed MAC. For crypto code used in WireGuard MAC1/MAC2, this is insufficient. Documentation PARTIAL: Confirmed. The multi-line doc comments are genuinely good — covering WireGuard context, keying mode distinction, and truncation note. However, formal parameter descriptions for `key` and `data` are absent, and there's no `# Examples` section. PARTIAL is fair given the quality of what's there versus what's missing.)
  - hkdf: [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for HKDF key derivation. | [UNIQUE] RAG shows 0.905 similarity to hkdf in rustguard-kmod/noise.rs, but implementations are fundamentally different: blake2s.rs implements HKDF in pure Rust using internal hmac_blake2s calls, while kmod uses a single unsafe C FFI call to wg_hkdf. Located in separate crates (crypto library vs kernel module wrapper). Same semantic outcome but drastically different code paths and implementation strategies warrant UNIQUE classification. | [OK] Matches the Noise Protocol Framework HKDF construction (section 4.4) used by WireGuard: PRK = HMAC(key, input); T1 = HMAC(PRK, 0x01); T2 = HMAC(PRK, T1||0x02); T3 = HMAC(PRK, T2||0x03). The 33-byte input buffers for T2 and T3 correctly hold the prior 32-byte output plus a counter byte. No info field is included, consistent with WireGuard's spec. | [LEAN] Direct transcription of WireGuard's three-output HKDF (extract + expand T1/T2/T3). Fixed-size stack arrays for T2/T3 inputs are idiomatic in no-alloc crypto Rust. No generalization beyond the protocol requirement. | [WEAK] Two tests cover determinism and mutual distinctness of the three outputs. No test verifies against WireGuard Noise protocol KDF test vectors, making it impossible to confirm spec compliance. Missing: key-sensitivity test (different keys produce different outputs), empty-input case, and any assertion that the expansion counters (0x01/0x02/0x03) are correctly chained. | [PARTIAL] Public function has multi-line doc comments describing WireGuard HKDF, the underlying hash, extract-then-expand structure, and the return tuple convention. Missing formal `key` and `input` parameter descriptions and no `# Examples` section, which are expected for public functions in a crypto library. (deliberated: confirmed — Tests WEAK: Confirmed. Two tests (determinism + distinct outputs) provide basic sanity but no spec-compliance verification. The WireGuard Noise protocol KDF is security-critical; absence of known test vectors means a subtle bug in counter chaining (0x01/0x02/0x03) or expansion logic could go undetected. Documentation PARTIAL: Confirmed. Good contextual documentation covering WireGuard HKDF, extract-then-expand structure, and return tuple convention. Missing formal parameter descriptions for `key` and `input`.)
  - hmac_blake2s: [USED] Private function called in hkdf (lines 42, 45, 51, 57) and tested in test module. | [UNIQUE] Manual RFC 2104 HMAC-BLAKE2s implementation. RAG results show only the public mac() function and C FFI wrappers as similar, which are semantically different constructs. No true duplicate of the private hmac_blake2s function found. This is a hand-written HMAC implementation, not an FFI wrapper or library delegation. | [OK] Correct RFC 2104 HMAC construction. The 32-byte key is zero-padded to the 64-byte block size (valid since 32 < 64, no pre-hashing needed). ipad = 0x36 XOR padded_key and opad = 0x5C XOR padded_key are computed correctly. Inner hash feeds ipad then data; outer hash feeds opad then inner_hash. The BLAKE2s-256 unkeyed hash is properly used as the underlying hash function, distinct from the keyed MAC mode used in mac(). | [ACCEPTABLE] Hand-rolled HMAC (RFC 2104) construction when the RustCrypto `hmac` crate (>10M weekly downloads) supports `Hmac<Blake2s256>` generically via the same `digest` trait already in use. Not installed per visible imports, so ACCEPTABLE rather than OVER — but adding `hmac` as a dependency would reduce ~25 lines to ~3 and eliminate a bespoke security-critical primitive. | [WEAK] Private function tested via hmac_test_vector (determinism + key sensitivity) and hmac_differs_from_keyed_blake2s (structural differentiation). However, hmac_test_vector does NOT compare against a precomputed RFC 2104 or WireGuard-spec value — it only asserts self-consistency. No test exercises the ipad/opad XOR logic correctness against a known answer, so a systematic bug (e.g., wrong pad constants) could pass all tests. | [DOCUMENTED] Private function with well-written `///` doc comments: explains the full HMAC construction formula, cites RFC 2104, distinguishes it from the built-in keyed BLAKE2s mode, and explains its role in WireGuard's KDF. For a private helper, `# Examples` is not expected; the inline comments in the body further clarify each step. (deliberated: confirmed — Overengineering ACCEPTABLE: Confirmed. Hand-rolling RFC 2104 HMAC in ~25 lines when the RustCrypto `hmac` crate could reduce it to ~3 lines is a valid observation. However, since `hmac` is not currently a dependency, this is a trade-off (fewer dependencies vs. more bespoke crypto code) rather than gratuitous overengineering. ACCEPTABLE is the right classification. Tests WEAK: Confirmed. The misleadingly named `hmac_test_vector` test only checks determinism and key sensitivity — it compares no actual known-answer vector. The ipad/opad XOR logic (0x36/0x5c constants) is never verified against a reference implementation. For a hand-rolled security-critical HMAC construction, this is the most concerning WEAK finding in the file. Raised confidence to 85 as I can directly verify the test code confirms this gap.)

- [ ] `rustguard-crypto/src/x25519.rs` — 4 weak
  Improve `rustguard-crypto/src/x25519.test.rs` covering: StaticSecret, EphemeralSecret, PublicKey, SharedSecret
  - StaticSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Core X25519 static secret key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for long-lived keys. | [NEEDS_FIX] StaticSecret::diffie_hellman (L46–49) does not validate that the resulting shared secret is non-zero. X25519 over a low-order public key point produces an all-zero output; x25519_dalek does not reject this automatically. For WireGuard (the evident use case of this crate), RFC-based implementations require the DH result to be checked against the zero value and the operation to fail if so. Silently returning a zero SharedSecret breaks the security contract of the protocol. | [LEAN] Thin newtype over x25519_dalek::StaticSecret. The wrapper is justified: it adds ZeroizeOnDrop (essential for long-lived crypto secrets), exposes a controlled API surface, and semantically distinguishes static keys from ephemeral ones at the type level — a real protocol concern in WireGuard. No unnecessary generics or layers. | [WEAK] Inline tests cover `random()`, `public_key()`, and `diffie_hellman()` via happy-path DH exchange. However, `from_bytes()`, `to_bytes()`, and `random_from_rng()` are never exercised. No known-answer vector tests, no round-trip through `from_bytes`/`to_bytes`, and zeroize-on-drop behavior is untested. The test using `random()` is behind `#[cfg(feature = "std")]` implicitly, so non-std paths (via `random_from_rng`) are wholly untested. | [PARTIAL] Type-level doc comment is present and meaningful ('A static (long-lived) X25519 secret key. Zeroized on drop.'), but several public methods — `from_bytes`, `diffie_hellman`, `public_key`, and `to_bytes` — carry no `///` comments at all. Only `random_from_rng` and `random` are individually documented. No `# Examples` section exists anywhere on the type or its impl block. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK: The evaluator claims 'RFC-based implementations require the DH result to be checked against the zero value,' but this is factually incorrect. RFC 7748 Section 6.1 explicitly states implementations 'MAY check... whether K is the all-zero value,' not MUST. The WireGuard Noise protocol framework does not mandate this check either — the KDF chain and authentication in the handshake prevent exploitation of low-order point inputs. x25519_dalek intentionally follows RFC 7748 by not rejecting zero outputs. The wrapper code correctly delegates to x25519_dalek and does not establish a security contract beyond what the RFC specifies. A zero-check would be a reasonable defense-in-depth hardening but its absence is not a correctness defect. Tests WEAK is upheld: from_bytes, to_bytes, and random_from_rng are untested; no known-answer vectors exist. Documentation PARTIAL is upheld: type-level docs exist but diffie_hellman, from_bytes, to_bytes, and public_key methods lack /// comments, which is notable in a crypto library where methods carry implicit security contracts.)
  - EphemeralSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Ephemeral X25519 key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for single-use ephemeral keys. | [NEEDS_FIX] EphemeralSecret::diffie_hellman (L75–78) has the identical low-order point defect as StaticSecret::diffie_hellman: no check for an all-zero result. Because ephemeral DH is used in the first handshake message in WireGuard (with the initiator's ephemeral key), a crafted low-order responder public key passed here would produce a zero shared secret that propagates silently into the ChaCha20-Poly1305 key derivation chain. | [LEAN] Wraps x25519_dalek::StaticSecret internally (with a clear comment explaining why: WireGuard requires the ephemeral key to participate in multiple DH operations, which x25519_dalek::EphemeralSecret's consume-on-use API forbids). The semantic type distinction from StaticSecret is protocol-meaningful and prevents misuse. No abstraction excess. | [WEAK] The `ephemeral_dh_works` test covers `random()`, `diffie_hellman()`, and `public_key()` on the happy path. `random_from_rng()` is untested. The design comment that EphemeralSecret wraps StaticSecret specifically to allow reuse across multiple DH ops (a WireGuard protocol requirement) is never exercised in tests — no test calls `diffie_hellman()` twice on the same ephemeral key to confirm it isn't consumed. Zeroize behavior is untested. | [PARTIAL] Type-level doc comment is present ('An ephemeral X25519 secret key. Single use, zeroized on drop.'), and `random_from_rng` and `random` have brief docs (including a useful inline note about why StaticSecret is used under the hood). However, `diffie_hellman` and `public_key` are entirely undocumented. No `# Examples` section is provided for any of the public API surface. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK for the same reasons as StaticSecret: RFC 7748 does not require rejection of all-zero DH outputs, and the Noise/WireGuard protocol handles low-order point inputs through its KDF chain and MAC-based authentication. The code correctly follows the x25519 specification. Tests WEAK is upheld: random_from_rng is untested, and critically, no test exercises the multi-DH-op property that justified using StaticSecret internally (the design comment at L63-65 explains this WireGuard requirement, but no test calls diffie_hellman twice on the same ephemeral key). Documentation PARTIAL is upheld: diffie_hellman and public_key lack /// docs.)
  - PublicKey: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. X25519 public key with constant-time equality. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::PublicKey with constant-time comparison support. | [OK] PartialEq is correctly implemented using subtle::ConstantTimeEq to prevent timing side-channels on key comparison. Eq is provided, from_bytes and as_bytes delegate faithfully to x25519_dalek::PublicKey. No correctness issues found. | [LEAN] The custom PartialEq using subtle::ConstantTimeEq is a security necessity — without it, the derived PartialEq would introduce a timing side-channel exploitable in key-comparison paths. The wrapper is minimal and every addition is justified by the crypto threat model. | [WEAK] `public_key_roundtrip` covers `from_bytes()`, `as_bytes()`, and the `PartialEq` equality path. However, inequality (two distinct keys) is never asserted, so the constant-time `ct_eq` path in `PartialEq::eq` is not validated behaviorally. `AsRef<[u8]>` impl is untested. The security-critical property — that comparison is actually constant-time — cannot be verified by the existing assertions alone. | [PARTIAL] Type-level doc comments are informative, mentioning wire size and constant-time comparison rationale. However, neither `from_bytes` nor `as_bytes` in the `impl PublicKey` block carry any `///` documentation. The `PartialEq` and `Eq` impls are undocumented, though trait impls are tolerated. No `# Examples` section is present. (deliberated: confirmed — Tests WEAK upheld: public_key_roundtrip covers from_bytes, as_bytes, and the equality path of PartialEq, which is reasonable coverage. However, inequality (two distinct keys returning false) is never asserted, and AsRef<[u8]> is untested. The constant-time property of ct_eq cannot be verified by functional tests alone, but at minimum a behavioral inequality test should exist. Documentation PARTIAL upheld: type-level docs are good (mentioning wire size and constant-time rationale), but from_bytes and as_bytes lack method-level docs. For a crypto library, documenting from_bytes to note that X25519 does not validate subgroup membership would be valuable.)
  - SharedSecret: [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. DH shared secret result with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps 32-byte array for DH shared secrets with Zeroize semantics. | [OK] Correctly wraps [u8; 32] with Zeroize and ZeroizeOnDrop; the derive macro can call zeroize() on u8 arrays. as_bytes() returns a reference to the inner array without copying. No correctness issues found. | [LEAN] Minimal newtype over [u8; 32] with a single as_bytes() accessor. ZeroizeOnDrop is mandatory for ephemeral DH output. The type prevents raw byte arrays from leaking out of DH operations without the zeroization guarantee. Could not be simpler while still meeting the security requirements. | [WEAK] `as_bytes()` is exercised in both DH tests to compare the two sides of the exchange, which is the primary usage. However, no known-answer test (fixed input → expected output bytes) is present, so correctness against the X25519 specification is not verified. Zeroize-on-drop behavior is untested. The struct's only method is covered but only via equality checks with no authoritative reference value. | [PARTIAL] Type-level doc comment is present but very terse ('Result of a DH operation. Zeroized on drop.'). The sole public method `as_bytes` has no `///` documentation. No guidance on how callers should consume or handle the secret bytes (e.g., pass to a KDF), and no `# Examples` section. (deliberated: confirmed — Tests WEAK upheld: as_bytes is exercised indirectly via DH exchange equality assertions, which validates the primary use case. However, no known-answer test exists to verify correctness against the X25519 specification with fixed inputs, and zeroize-on-drop is untested. Documentation PARTIAL upheld: the type-level doc is terse and as_bytes has no /// comment. For a type holding sensitive key material, documenting that callers should pass the result through a KDF before use would be valuable guidance.)

- [ ] `rustguard-enroll/src/packet.rs` — 1 untested, 2 weak
  Improve `rustguard-enroll/src/packet.test.rs` covering: parse_eth_udp, parse_ipv4_udp, parse_ipv6_udp
  - parse_eth_udp: [DEAD] Exported function with 0 workspace importers per pre-computed analysis. Matches rule 2: exported symbol with 0 runtime and 0 type-only importers equals DEAD. | [UNIQUE] Dispatcher function that routes based on ethertype (0x0800 vs 0x86DD). Fundamentally different role from protocol-specific parsers (parse_ipv4_udp, parse_ipv6_udp). Not interchangeable with them despite RAG structural similarity scores. | [OK] Ethertype is read from the correct canonical offsets (bytes 12–13) using big-endian conversion. Dispatch values 0x0800 (IPv4) and 0x86DD (IPv6) are correct. The 14-byte minimum length guard ensures the ethertype bytes are accessible before they are read. | [LEAN] Straightforward dispatcher that reads ethertype and delegates to protocol-specific parsers. The match on ethertype is the minimal correct approach for Ethernet frame demultiplexing. | [WEAK] One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only. The too-short frame branch (frame.len() < 14), the IPv6 dispatch arm (0x86DD), and the unknown-ethertype None branch are all untested. Critical caller paths (AF_XDP fast-path) demand better negative-case coverage. | [PARTIAL] Has a two-line doc comment covering purpose and the `None` return conditions, but the `frame` parameter is not described, there is no `# Examples` section, and the supported ethertype set (0x0800, 0x86DD) is not documented. `# Examples` is expected on public API functions. (deliberated: reclassified: tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD kept: 0 workspace importers per pre-computed analysis. As the main public entry point for AF_XDP packet parsing, it is likely consumed externally, but analysis scope is definitive. Tests NONE→WEAK reclassified: the evaluator's detail text states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only' — that is WEAK by definition, not NONE. The function is called at line 97 via parse_eth_udp(&frame).unwrap(). IPv6, short-frame, and unknown-ethertype branches remain untested, so WEAK is accurate. Documentation UNDOCUMENTED→PARTIAL reclassified: lines 14-15 clearly show two `///` doc comments ('Parse a raw Ethernet frame…' and 'Returns None if not…'). The evaluator's own detail confirms this. Missing parameter docs and examples make PARTIAL correct.)
  - parse_ipv4_udp: [USED] Non-exported function called from parse_eth_udp (line 24) within match statement. Matches rule 5: non-exported symbol with local usage equals USED. | [UNIQUE] Parses IPv4 packets with variable header length (IHL-based). Checks protocol at byte 9, extracts IP from bytes 12-15, port at offset IHL, payload at IHL+8. Semantic contract differs fundamentally from parse_ipv6_udp due to variable header, fixed field positions, and IPv4-specific invariants. Not interchangeable despite 0.819 structural similarity score. | [NEEDS_FIX] The computed IHL is never validated against its minimum legal value of 20 bytes (IHL nibble >= 5). After the initial `len < 20` guard, `ihl` can legally be 0, 4, 8, 12, or 16 for a nibble value of 0–4. In those cases `ihl + 8` is still less than the slice length (>= 20), so the second guard also passes, and `ip_data[ihl]` / `ip_data[ihl+1]` index well inside the IP header instead of the UDP source-port field. The resulting `src_port` is garbage and the wrong region is returned as the payload. A check `if ihl < 20 { return None; }` immediately after computing `ihl` is required. | [LEAN] Direct, idiomatic byte-level parsing of IPv4 + UDP headers with correct IHL handling and bounds checks. No unnecessary abstractions; each step maps 1-to-1 to the protocol spec. | [WEAK] Exercised only via the single parse_ipv4_udp_frame test which takes the minimum-IHL, valid-protocol happy path. The three early-return error branches — ip_data.len() < 20, protocol != 17, and ip_data.len() < ihl+8 (for IHL > 5) — are never tested. IHL options parsing is also unverified. | [UNDOCUMENTED] Private function (`fn`, no `pub`) with no `///` doc comments. Lenient treatment applies for private items; clear name and inline comments partially compensate, but no doc comment exists. (deliberated: confirmed — Correction NEEDS_FIX confirmed with raised confidence: the IHL bug is real and directly visible in the code. Line 35 computes `ihl = (ip_data[0] & 0x0f) * 4`. For a malformed packet with IHL nibble 0–4, ihl is 0/4/8/12/16. The guard at line 42 (`ip_data.len() < ihl + 8`) passes since len ≥ 20, so lines 46-47 read `ip_data[ihl]` and `ip_data[ihl+1]` from inside the IP header, not the UDP header. This silently returns garbage src_port and wrong payload — a clear correctness defect. Original confidence of 60 was too low; raising to 85 since the bug is directly verifiable in the source. Tests WEAK kept: only the happy path (IHL=5, protocol=17) is exercised by parse_ipv4_udp_frame. The three early-return branches and the IHL validation gap are untested. Documentation UNDOCUMENTED kept: private function with no `///` doc comments anywhere. Lenient treatment for private items acknowledged.)

- [ ] `rustguard-enroll/src/state.rs` — 1 untested, 2 weak
  Improve `rustguard-enroll/src/state.test.rs` covering: default_state_path, save, load
  - save: [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Persists peer state with atomic file write. | [UNIQUE] Persists peers to state file with atomic write semantics. No similar functions found in RAG results. | [OK] Atomic write via temp-file-then-rename is correct; temp path is derived via with_extension so it is always in the same directory as the target, making rename across filesystems a non-issue. create_dir_all on an empty parent (relative path with no directory components) returns Ok(()) in Rust std, so no spurious error. BASE64_STANDARD.encode on a fixed-size array is correct. Format string produces 'key ip\n' matching the load parser. | [LEAN] Straightforward serialization to a line-oriented text format with an atomic write via rename. Parent dir creation and temp-file pattern are both justified for a daemon state file. No unnecessary abstraction layers. | [WEAK] save_and_load_roundtrip covers the happy path with two peers. Not tested: saving an empty peer slice, the create_dir_all branch (test pre-creates the dir), atomic write semantics (temp+rename), or any I/O error paths (e.g., permission denied on write). | [PARTIAL] Two `///` lines describe purpose and file format, which is useful. However, neither `path` nor `peers` parameters are documented, there is no `# Errors` section explaining the `io::Result` failure modes, and the atomic-write-via-rename behaviour (a non-obvious correctness property) appears only in the module `//!` preamble rather than on the function itself. No `# Examples` section. (deliberated: confirmed — Tests WEAK is correct: `save_and_load_roundtrip` covers the happy path with two peers but no edge cases (empty slice, IO errors, atomic write behavior). Documentation PARTIAL is fair: two doc lines describe purpose and format but omit parameter docs, `# Errors` section, and the atomic-write-via-rename semantics. Correction OK is well-justified — the temp-file-then-rename pattern is correctly implemented with same-directory guarantee via `with_extension`.)
  - load: [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Loads and parses peer state from file. | [UNIQUE] Loads and parses peers from state file with validation. No similar functions found in RAG results. | [OK] NotFound is correctly handled as an empty list. trim() removes CR/LF remnants before split_once. split_once(' ') correctly isolates the base64 key from the IP because standard base64 contains no spaces. Vec<u8>::try_into::<[u8;32]>() is stable since Rust 1.59 and the error path is mapped correctly. ip_str.parse::<Ipv4Addr>() is correct. All error paths return InvalidData with descriptive messages. | [LEAN] Manual line-by-line parsing is proportionate to the simple 'base64_key ip' format. Not-found gracefully returns empty vec. Error mapping is explicit but necessary given io::Result return type. No reinvented wheels — the format is too trivial to justify serde. | [WEAK] Two tests cover happy path (roundtrip) and the NotFound branch. However, all error-path branches inside the parsing loop are untested: malformed line with no space (InvalidData), invalid base64 key string, key bytes not exactly 32 bytes, and unparseable IP string. These are the most likely real-world failure modes and none of them are exercised. | [PARTIAL] Single `///` line ('Load peers from the state file.') is present but extremely terse. The important non-obvious behaviour — returning `Ok(Vec::new())` rather than an error when the file is absent — is not documented on the function. No parameter docs, no `# Errors` section, and no `# Examples` section for a non-trivial public parsing function. (deliberated: confirmed — Tests WEAK is accurate: roundtrip and NotFound are covered, but all four parsing error branches (no space, bad base64, wrong key length, bad IP) are untested — these are the most likely real-world failure modes. Documentation PARTIAL is correct: the critical non-obvious behavior of returning `Ok(Vec::new())` on missing file is undocumented on the function itself. Correction OK is well-justified — all error mappings and parsing logic are sound.)

- [ ] `rustguard-tun/examples/tun_echo.rs` — 2 untested, 2 weak
  Improve `rustguard-tun/examples/tun_echo.test.rs` covering: main, icmp_checksum, ip_checksum, checksum_fold
  - icmp_checksum: [USED] Wrapper function called at line 53 to compute ICMP checksum for echo reply packets. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Thin one-liner wrapper around `checksum_fold` that adds no logic, no documentation distinction, and no type safety. Both `icmp_checksum` and `ip_checksum` are identical delegating wrappers; collapsing them into direct calls to `checksum_fold` (or a single named alias) would eliminate the indirection without any loss of clarity. The split is premature abstraction for a single-file example. | [NONE] No test file exists for this source file. icmp_checksum is a trivial one-line delegate to checksum_fold with no independent tests. Despite being a pure, deterministic function that would be straightforward to unit-test with known ICMP vectors, it has zero test coverage. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name alone hints at purpose (ICMP checksum computation), and the one-liner body delegating to `checksum_fold` is self-evident. Per leniency rules for private items with clear names, confidence is lowered to 65. A brief doc noting the 1s-complement algorithm and the expected slice layout (20-byte ICMP segment) would be useful but is not strictly required for private code. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: the named wrapper `icmp_checksum` provides semantic clarity at the call site (`icmp_checksum(&reply[20..n])` is more self-documenting than `checksum_fold(&reply[20..n])`). For an example binary intended to teach TUN/ICMP concepts, named domain-specific wrappers are a pedagogically sound choice, not premature abstraction. The one-liner cost is trivial. Tests WEAK kept: no test coverage exists, which is expected for a private helper in an example binary. Documentation DOCUMENTED kept: name is self-descriptive, body is a single delegation — no further docs needed for private example code.)
  - ip_checksum: [USED] Wrapper function called at line 60 to recompute IP header checksum after swapping source and destination addresses. | [UNIQUE] Trivial wrapper function (1 LOC); audit guidelines mark trivial and wrapper functions as UNIQUE to avoid false positive duplication flags | [OK] Thin delegating wrapper; delegates entirely to checksum_fold which is correct. No independent logic to evaluate. | [OVER] Identical in structure to `icmp_checksum` — a trivial one-liner that only delegates to `checksum_fold`. Having two separate wrapper functions for what is the same RFC 1071 one's-complement checksum algorithm, differentiated only by name, is unnecessary duplication. A single `checksum_fold` call at each call site would be equally readable and remove pointless indirection. | [NONE] No test file exists for this source file. ip_checksum is a trivial one-line delegate to checksum_fold with no independent tests. As a pure function operating on a fixed 20-byte IP header, it would be easy to test with known RFC-compliant headers, but no tests are present. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Mirrors `icmp_checksum` in structure; the name makes the intent clear (IP header checksum), and the body is a single delegation to `checksum_fold`. Leniency applies for private items with descriptive names. A note clarifying the expected input (20-byte IPv4 header) would improve correctness but is not required for private helpers. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: same rationale as icmp_checksum — `ip_checksum(&reply[..20])` at the call site clearly communicates intent to recompute the IP header checksum. In an educational example, domain-specific naming aids comprehension and is worth the trivial wrapper cost. The evaluator conflated structural similarity with semantic redundancy; these wrappers serve different conceptual roles (ICMP vs IP checksum) even though both delegate to the same algorithm. Tests WEAK kept. Documentation DOCUMENTED kept.)

- [ ] `rustguard-kmod/src/timers.rs` — 2 untested
  Improve `rustguard-kmod/src/timers.test.rs` covering: now_ns, SessionTimers

- [ ] `rustguard-kmod/src/allowedips.rs` — 2 untested
  Improve `rustguard-kmod/src/allowedips.test.rs` covering: TrieNode, AllowedIps

- [ ] `rustguard-tun/src/uring.rs` — 2 untested
  Improve `rustguard-tun/src/uring.test.rs` covering: BufferPool, UringTun
