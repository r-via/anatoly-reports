[← Back to Tests](./index.md) · [← Back to report](../public_report.md)

# Tests — Shard 1

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-kmod/src/lib.rs` | CRITICAL | 19 | 90% | [details](../reviews/rustguard-kmod-src-lib.rev.md) |
| `rustguard-tun/src/xdp.rs` | CRITICAL | 6 | 95% | [details](../reviews/rustguard-tun-src-xdp.rev.md) |
| `rustguard-enroll/src/server.rs` | CRITICAL | 8 | 92% | [details](../reviews/rustguard-enroll-src-server.rev.md) |
| `rustguard-kmod/src/replay.rs` | CRITICAL | 1 | 90% | [details](../reviews/rustguard-kmod-src-replay.rev.md) |
| `rustguard-kmod/src/noise.rs` | NEEDS_REFACTOR | 20 | 92% | [details](../reviews/rustguard-kmod-src-noise.rev.md) |
| `rustguard-core/src/handshake.rs` | NEEDS_REFACTOR | 16 | 88% | [details](../reviews/rustguard-core-src-handshake.rev.md) |
| `rustguard-core/src/cookie.rs` | NEEDS_REFACTOR | 13 | 92% | [details](../reviews/rustguard-core-src-cookie.rev.md) |
| `rustguard-daemon/src/tunnel.rs` | NEEDS_REFACTOR | 12 | 90% | [details](../reviews/rustguard-daemon-src-tunnel.rev.md) |
| `rustguard-core/src/messages.rs` | NEEDS_REFACTOR | 12 | 90% | [details](../reviews/rustguard-core-src-messages.rev.md) |
| `rustguard-core/src/timers.rs` | NEEDS_REFACTOR | 11 | 90% | [details](../reviews/rustguard-core-src-timers.rev.md) |

## Symbol Details

### `rustguard-kmod/src/lib.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `DEVICE_STATE_PTR` | L169–L169 | NONE | 90% | [USED] Global atomic pointer to DeviceState: initialized in init() (L223), ac... |
| `RustGuard` | L171–L171 | NONE | 80% | [USED] Module struct implementing kernel::Module trait; required for kernel m... |
| `cleanup_state` | L359–L376 | NONE | 88% | [USED] Internal function called from init error path (L212) and Drop (L354); ... |
| `rustguard_xmit` | L382–L384 | NONE | 70% | [USED] Exported C FFI callback (#[no_mangle]) for TX path; kernel calls this ... |
| `do_xmit` | L386–L458 | NONE | 75% | [USED] Internal TX implementation called directly from rustguard_xmit (L384);... |
| `rustguard_rx` | L465–L467 | NONE | 70% | [USED] Exported C FFI callback (#[no_mangle]) for RX path; kernel calls this ... |
| `do_rx` | L478–L517 | NONE | 90% | [USED] Internal RX implementation called from rustguard_rx (L467); dispatches... |
| `handle_initiation` | L521–L620 | NONE | 82% | [USED] Handshake handler called from do_rx for MSG_INITIATION (L495); process... |
| `handle_response` | L624–L671 | NONE | 82% | [USED] Handshake handler called from do_rx for MSG_RESPONSE (L499); processes... |
| `handle_transport` | L676–L755 | NONE | 80% | [USED] Transport handler called from do_rx for MSG_TRANSPORT (L507); decrypts... |
| `handle_cookie_reply` | L758–L781 | NONE | 80% | [USED] Cookie handler called from do_rx for type 3 (L504); processes MAC2 coo... |
| `rustguard_dev_uninit` | L785–L785 | NONE | 90% | [LOW_VALUE] Exported C FFI callback for device teardown; completely empty stu... |
| `rustguard_timer_tick` | L789–L791 | NONE | 70% | [USED] Exported C FFI callback invoked periodically (250ms) by wg_timer_start... |
| `do_timer_tick` | L793–L844 | NONE | 82% | [USED] Called from rustguard_timer_tick (L791); iterates peers, checks expiry... |
| `initiate_rekey` | L847–L877 | NONE | 80% | [USED] Called from do_timer_tick for rekey trigger (L829, L835); creates hand... |
| `send_keepalive` | L880–L914 | NONE | 80% | [USED] Called from do_timer_tick (L841) when keepalive needed; sends empty tr... |
| `rustguard_genl_get` | L918–L922 | NONE | 88% | [LOW_VALUE] Exported C FFI callback for genetlink GET; empty stub returning 0... |
| `rustguard_genl_set` | L926–L932 | NONE | 88% | [LOW_VALUE] Exported C FFI callback for genetlink SET; empty stub returning 0... |
| `is_err_ptr` | L934–L937 | NONE | 90% | [USED] Internal kernel error pointer check; called in init path (L208, L219) ... |

### `rustguard-tun/src/xdp.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Ring` | L89–L95 | NONE | 88% | [USED] Core data structure used for rx_ring, tx_ring, fill_ring, comp_ring fi... |
| `XdpSocket` | L148–L160 | NONE | 95% | [USED] Primary public struct returned from create(); used in all socket opera... |
| `setsockopt_raw` | L407–L415 | NONE | 88% | [USED] Called at L226, L230-L232 for socket option configuration | [UNIQUE] G... |
| `mmap_ring` | L417–L437 | NONE | 75% | [USED] Called at L243-L246 to map ring buffer regions into memory | [UNIQUE] ... |
| `make_ring` | L439–L447 | NONE | 88% | [USED] Called at L249-L252 to initialize Ring structures from mmap'd memory |... |
| `if_nametoindex` | L449–L458 | NONE | 90% | [USED] Called at L214 to resolve interface name to index; exported public uti... |

### `rustguard-enroll/src/server.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `EnrolledPeer` | L27–L31 | WEAK | 65% | [USED] Internal struct constructed at L196 and L272; extensively accessed in ... |
| `PeerState` | L33–L37 | WEAK | 65% | [USED] Internal struct created in Mutex at L191 and L196; accessed repeatedly... |
| `ServerState` | L39–L47 | WEAK | 65% | [USED] Internal struct constructed at L214 as Arc<ServerState>; cloned for in... |
| `ServeConfig` | L49–L62 | NONE | 92% | [DEAD] Exported public struct; parameter type of public run() function at L64... |
| `run` | L64–L532 | NONE | 92% | [DEAD] Exported public function (main tunnel server loop); pre-computed analy... |
| `rand_index` | L534–L538 | WEAK | 70% | [USED] Non-exported helper; called once at L347 during WireGuard initiation h... |
| `base64_key` | L541–L544 | WEAK | 70% | [USED] Non-exported helper wrapper around base64::encode; called at L267 (enr... |
| `setup_xdp` | L548–L577 | NONE | 80% | [USED] Non-exported Linux-only helper (cfg gate); called at L114 to load BPF ... |

### `rustguard-kmod/src/replay.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `ReplayWindow` | L11–L14 | NONE | 88% | [DEAD] Exported pub(crate) struct with 0 workspace importers per Pre-computed... |

### `rustguard-kmod/src/noise.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `zeroize` | L46–L50 | NONE | 90% | [DEAD] Exported (pub(crate)) with 0 workspace importers per import analysis, ... |
| `constant_time_eq` | L53–L58 | NONE | 85% | [USED] Not exported; called in process_response (L360) and process_initiation... |
| `hash` | L72–L94 | NONE | 80% | [USED] Not exported; called in initial_chain_key (L180), initial_hash (L184),... |
| `mac` | L97–L107 | NONE | 85% | [USED] Not exported; called in compute_mac1() at L215 for MAC1 computation. |... |
| `hkdf` | L110–L121 | NONE | 85% | [USED] Not exported; called in mix_key (L193), process_response (L389, L419),... |
| `seal` | L124–L138 | NONE | 85% | [USED] Not exported; called in encrypt_and_hash() at L198 for AEAD encryption... |
| `open` | L141–L155 | NONE | 88% | [USED] Not exported; called in decrypt_and_hash() at L204 for AEAD decryption... |
| `dh` | L158–L162 | NONE | 85% | [USED] Not exported; called in create_initiation (L304, L313), process_respon... |
| `generate_keypair` | L165–L173 | NONE | 85% | [USED] Not exported; called in create_initiation (L300) and process_initiatio... |
| `initial_chain_key` | L178–L180 | NONE | 85% | [USED] Not exported; called in create_initiation (L297) and process_initiatio... |
| `initial_hash` | L182–L184 | NONE | 85% | [USED] Not exported; called in create_initiation (L298) and process_initiatio... |
| `mix_hash` | L186–L188 | NONE | 85% | [USED] Not exported; called in create_initiation, process_response, process_i... |
| `mix_key` | L190–L193 | NONE | 85% | [USED] Not exported; called in create_initiation (L305, L314), process_respon... |
| `encrypt_and_hash` | L196–L200 | NONE | 85% | [USED] Not exported; called in create_initiation (L310, L319) and process_ini... |
| `decrypt_and_hash` | L203–L207 | NONE | 85% | [USED] Not exported; called in process_response (L375, L385, L413) and proces... |
| `compute_mac1` | L210–L216 | NONE | 85% | [USED] Not exported; called in create_initiation (L326), process_response (L3... |
| `tai64n_now` | L221–L235 | NONE | 80% | [USED] Not exported; called in create_initiation (L318) to generate TAI64N ti... |
| `create_initiation` | L281–L344 | NONE | 90% | [DEAD] Exported (pub(crate)) with 0 importers per analysis; primary handshake... |
| `process_response` | L347–L422 | NONE | 90% | [DEAD] Exported (pub(crate)) with 0 importers; core handshake function likely... |
| `process_initiation` | L431–L536 | NONE | 90% | [DEAD] Exported (pub(crate)) with 0 importers; core handshake function likely... |

### `rustguard-core/src/handshake.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `initial_chain_key` | L23–L25 | WEAK | 85% | [USED] Non-exported helper function called in create_initiation_with (L119) a... |
| `initial_hash` | L27–L29 | WEAK | 87% | [USED] Non-exported helper function called in create_initiation_with (L120) a... |
| `mix_hash` | L32–L34 | WEAK | 78% | [USED] Non-exported helper function called 8 times across create_initiation_w... |
| `mix_key` | L38–L41 | WEAK | 88% | [USED] Non-exported helper function called 8 times across create_initiation_w... |
| `encrypt_and_hash` | L44–L48 | WEAK | 80% | [USED] Non-exported helper function called in create_initiation_with (L135, L... |
| `decrypt_and_hash` | L51–L55 | WEAK | 80% | [USED] Non-exported helper function called in process_response (L203) and pro... |
| `compute_mac1` | L59–L65 | WEAK | 88% | [USED] Exported function with 4 runtime importers across tests, daemon, and e... |
| `InitiatorHandshake` | L72–L81 | WEAK | 78% | [USED] Exported struct with 4 runtime importers; core state type for initiato... |
| `create_initiation` | L85–L91 | WEAK | 80% | [USED] Exported convenience function with 4 runtime importers; std-only wrapp... |
| `create_initiation_psk` | L95–L104 | WEAK | 83% | [USED] Exported convenience function with 4 runtime importers; std-only PSK v... |
| `create_initiation_with` | L108–L170 | WEAK | 80% | [USED] Exported core function with 4 runtime importers; implements Noise_IKps... |
| `process_response` | L173–L221 | WEAK | 80% | [USED] Exported function with 4 runtime importers; processes response msg2 an... |
| `process_initiation` | L227–L233 | WEAK | 82% | [USED] Exported convenience function with 4 runtime importers; std-only respo... |
| `process_initiation_psk` | L237–L246 | WEAK | 83% | [USED] Exported convenience function with 4 runtime importers; std-only respo... |
| `process_initiation_with` | L249–L357 | WEAK | 80% | [USED] Exported core function with 4 runtime importers; implements responder ... |
| `constant_time_eq` | L360–L363 | WEAK | 78% | [USED] Non-exported helper function called in process_initiation_with (L296) ... |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `now` | L33–L35 | WEAK | 60% | [USED] no_std version called in 3 places: CookieChecker::new, maybe_rotate_se... |
| `elapsed_since` | L38–L40 | WEAK | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fre... |
| `NO_STD_COUNTER` | L43–L43 | WEAK | 60% | [USED] Static atomic counter used in no_std now() function for monotonic time... |
| `now` | L46–L48 | WEAK | 60% | [USED] no_std version called in 3 places: CookieChecker::new, maybe_rotate_se... |
| `elapsed_since` | L51–L55 | WEAK | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fre... |
| `CookieChecker` | L58–L67 | WEAK | 92% | [DEAD] Exported struct but has 0 external importers per import analysis | [UN... |
| `CookieState` | L70–L75 | WEAK | 92% | [DEAD] Exported struct but has 0 external importers per import analysis | [UN... |
| `encode_addr` | L236–L252 | WEAK | 70% | [USED] Used in 2 places: create_reply (line 128) and verify_mac2 (line 150) |... |
| `random_bytes` | L254–L258 | WEAK | 60% | [USED] Used in 2 places: CookieChecker::new (line 80) and maybe_rotate_secret... |
| `random_nonce` | L260–L264 | WEAK | 88% | [USED] Used once in create_reply_from_bytes (line 118) to generate random non... |
| `fill_random` | L270–L272 | WEAK | 72% | [USED] no_std stub version called in 2 places: random_bytes and random_nonce ... |
| `fill_random` | L275–L280 | WEAK | 72% | [USED] no_std stub version called in 2 places: random_bytes and random_nonce ... |
| `constant_time_eq_16` | L282–L288 | WEAK | 65% | [USED] Used in 2 places: verify_mac1 (line 137) and verify_mac2_from_bytes (l... |

### `rustguard-daemon/src/tunnel.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `run` | L48–L489 | NONE | 90% | [DEAD] exported pub fn but pre-computed analysis shows 0 runtime importers | ... |
| `rand_index` | L492–L496 | WEAK | 70% | [USED] called at L154 and L424 to generate random sender indices | [DUPLICATE... |
| `fill_random` | L499–L501 | WEAK | 65% | [USED] called from rand_index at L496 to fill random buffer | [UNIQUE] Trivia... |
| `ctrlc_handler` | L504–L525 | NONE | 80% | [USED] called at L118 to setup shutdown signal handler | [UNIQUE] Platform si... |
| `signal_noop` | L527–L527 | WEAK | 65% | [USED] referenced in L515 and L516 as signal handler callback | [UNIQUE] Triv... |
| `base64_key` | L529–L532 | WEAK | 70% | [USED] called at L96, L309, L356, L430 to format public keys | [DUPLICATE] En... |
| `assign_v6_address` | L536–L540 | WEAK | 60% | [USED] Linux version called at L77 for IPv6 address assignment | [UNIQUE] Lin... |
| `assign_v6_address` | L543–L547 | WEAK | 60% | [USED] Linux version called at L77 for IPv6 address assignment | [UNIQUE] Lin... |
| `add_route` | L551–L556 | WEAK | 60% | [USED] Linux version called at L104 to add IP routes | [UNIQUE] Linux route a... |
| `delete_route` | L559–L564 | WEAK | 60% | [USED] Linux version called at L481 for route cleanup | [UNIQUE] Linux route ... |
| `add_route` | L567–L572 | WEAK | 60% | [USED] Linux version called at L104 to add IP routes | [UNIQUE] Linux route a... |
| `delete_route` | L575–L580 | WEAK | 60% | [USED] Linux version called at L481 for route cleanup | [UNIQUE] Linux route ... |

### `rustguard-core/src/messages.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `MSG_INITIATION` | L5–L5 | WEAK | 82% | [USED] Exported constant with 4 runtime importers across rustguard-core and r... |
| `MSG_RESPONSE` | L6–L6 | WEAK | 82% | [USED] Exported constant with 4 runtime importers across rustguard-core and r... |
| `MSG_COOKIE_REPLY` | L7–L7 | NONE | 90% | [USED] Exported constant with 4 runtime importers across rustguard-core and r... |
| `MSG_TRANSPORT` | L8–L8 | WEAK | 87% | [USED] Exported constant with 4 runtime importers across rustguard-core and r... |
| `Initiation` | L22–L29 | WEAK | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard handshake... |
| `INITIATION_SIZE` | L31–L31 | WEAK | 78% | [USED] Exported constant with 4 runtime importers; defines 148-byte size for ... |
| `Response` | L45–L52 | WEAK | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard handshake... |
| `RESPONSE_SIZE` | L54–L54 | WEAK | 78% | [USED] Exported constant with 4 runtime importers; defines 92-byte size for R... |
| `Transport` | L64–L68 | WEAK | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard transport... |
| `CookieReply` | L79–L83 | NONE | 88% | [USED] Exported struct with 4 runtime importers; used for WireGuard cookie re... |
| `COOKIE_REPLY_SIZE` | L85–L85 | NONE | 90% | [USED] Exported constant with 4 runtime importers; defines 64-byte size for C... |
| `TRANSPORT_HEADER_SIZE` | L87–L87 | WEAK | 87% | [USED] Exported constant with 4 runtime importers; defines 16-byte header siz... |

### `rustguard-core/src/timers.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `now` | L16–L18 | WEAK | 65% | [USED] Non-exported function called in session_started(), packet_sent(), and ... |
| `elapsed_since` | L21–L23 | WEAK | 75% | [USED] Non-exported no_std fallback function compiled conditionally; provides... |
| `elapsed_since` | L26–L30 | WEAK | 75% | [USED] Non-exported no_std fallback function compiled conditionally; provides... |
| `REKEY_AFTER_TIME` | L33–L33 | WEAK | 85% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.r... |
| `REJECT_AFTER_TIME` | L36–L36 | WEAK | 88% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.r... |
| `REKEY_TIMEOUT` | L39–L39 | WEAK | 88% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.r... |
| `REKEY_AFTER_MESSAGES` | L42–L42 | WEAK | 72% | [USED] Exported constant with 1 runtime importer; also used locally in needs_... |
| `REJECT_AFTER_MESSAGES` | L45–L45 | WEAK | 87% | [USED] Exported constant with 1 runtime importer; also used locally in is_exp... |
| `REKEY_ATTEMPT_TIME` | L48–L48 | NONE | 90% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.r... |
| `DEAD_SESSION_TIMEOUT` | L51–L51 | WEAK | 80% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.r... |
| `SessionTimers` | L54–L65 | WEAK | 90% | [USED] Exported struct with 1 runtime importer (rustguard-daemon/src/peer.rs)... |

## Test Improvements

- [ ] `rustguard-kmod/src/lib.rs` — 19 untested
  Improve `rustguard-kmod/src/lib.test.rs` covering: DEVICE_STATE_PTR, RustGuard, cleanup_state, rustguard_xmit, do_xmit, rustguard_rx, do_rx, handle_initiation, handle_response, handle_transport, handle_cookie_reply, rustguard_dev_uninit, rustguard_timer_tick, do_timer_tick, initiate_rekey, send_keepalive, rustguard_genl_get, rustguard_genl_set, is_err_ptr

- [ ] `rustguard-tun/src/xdp.rs` — 6 untested
  Improve `rustguard-tun/src/xdp.test.rs` covering: Ring, XdpSocket, setsockopt_raw, mmap_ring, make_ring, if_nametoindex

- [ ] `rustguard-enroll/src/server.rs` — 3 untested, 5 weak
  Improve `rustguard-enroll/src/server.test.rs` covering: EnrolledPeer, PeerState, ServerState, ServeConfig, run, rand_index, base64_key, setup_xdp
  - EnrolledPeer: [USED] Internal struct constructed at L196 and L272; extensively accessed in peer searches and state management throughout the file | [UNIQUE] Struct type with no similar definitions found in RAG results. Unique data structure for server-side peer state. | [OK] Simple data struct with well-typed fields. No correctness issues. | [LEAN] Clean separation of immutable fields (public_key, assigned_ip) from mutable state behind a Mutex. This split is the standard Rust pattern for fine-grained locking and is explicitly called out in the module's performance notes. | [NONE] No test file exists for server.rs. EnrolledPeer is a private struct with no test coverage whatsoever. | [UNDOCUMENTED] Private struct with no /// doc comments. Fields are moderately self-descriptive but the concurrency contract (state behind a Mutex) and lifecycle are unexplained. Leniency applied for private item. (deliberated: confirmed — Tests WEAK: The detail text actually says NONE (no test file exists), but the field says WEAK. No test file exists for server.rs, so any coverage is zero. For a private struct tested indirectly through `run()`, WEAK is acceptable as the merged field value. Low confidence retained due to merger inconsistency.)
  - PeerState: [USED] Internal struct created in Mutex at L191 and L196; accessed repeatedly via peer.state.lock() in inbound/outbound packet processing | [UNIQUE] Struct type with no similar definitions found in RAG results. Unique endpoint and session state container. | [OK] Simple state struct holding optional session and timers. No correctness issues. | [LEAN] Minimal grouping of the three fields that change together under one Mutex (endpoint, session, timers). The split from EnrolledPeer avoids locking immutable identity data on every packet. | [NONE] No test file exists for server.rs. PeerState is a private struct with no test coverage whatsoever. | [UNDOCUMENTED] Private struct with no /// doc comments. The relationship between endpoint, session, and timers fields and their invariants (e.g., session implies endpoint) are not documented. Leniency applied for private item. (deliberated: confirmed — Tests WEAK: Same situation as EnrolledPeer — detail says NONE but field says WEAK. No dedicated test file. Private struct only testable through integration of `run()`. Keeping WEAK as the merged value.)
  - ServerState: [USED] Internal struct constructed at L214 as Arc<ServerState>; cloned for inbound thread (L238) and outbound threads (L227), used throughout | [UNIQUE] Struct type with no similar definitions found in RAG results. Unique enrollment server state structure. | [OK] All fields are correctly typed and properly wrapped in synchronization primitives. The `pending_handshakes` field is never read or written in the visible code, indicating dead/incomplete code, but this is not a runtime bug. | [ACCEPTABLE] Most fields are necessary. However, `pending_handshakes` (a Mutex<Vec<(u32, Instant, InitiatorHandshake)>>) is allocated and wrapped in a Mutex but never read from or written to anywhere in the file. The MSG_INITIATION handler processes handshakes inline without touching this field, making it dead state that adds lock overhead and cognitive load. | [NONE] No test file exists for server.rs. ServerState is a private struct with no test coverage whatsoever. | [UNDOCUMENTED] Private struct holding all shared mutable server state. No /// doc comments explaining the concurrency model, ownership of each lock, or the purpose of pending_handshakes. Leniency applied for private item. (deliberated: confirmed — Tests WEAK: Same pattern — no test file exists. The `pending_handshakes` dead field noted in overengineering detail (ACCEPTABLE) is a valid observation but overengineering=ACCEPTABLE is not a finding requiring action. Tests assessment is consistent with the others.)
  - rand_index: [USED] Non-exported helper; called once at L347 during WireGuard initiation handshake processing to generate random peer index | [DUPLICATE] Identical implementation found in rustguard-enroll/src/client.rs (score 0.965). Both use getrandom to generate 4 random bytes and convert to u32 via from_le_bytes. | [OK] Correctly generates a 4-byte random value via getrandom and converts to u32. The expect on getrandom failure is acceptable for a security-critical context where proceeding without entropy is worse than panicking. | [LEAN] Four-line helper that wraps getrandom for a u32 session index. Minimal and appropriate; no abstraction needed beyond this. | [NONE] No test file exists for server.rs. rand_index has zero test coverage. Previously confirmed NONE by deliberation override. | [UNDOCUMENTED] Private helper with no /// doc comments. Name and trivial body are self-explanatory. Leniency applied for private item. (deliberated: confirmed — Duplication DUPLICATE: Technically correct — near-identical implementation exists in client.rs. However, this is a 4-line function in a different crate. Deduplicating would require a shared utility crate or making it pub in one crate for a trivial helper. The duplication is pragmatically justified but the finding is factually accurate. Tests WEAK: No test file exists; detail says NONE. Keeping WEAK per original field.)
  - base64_key: [USED] Non-exported helper wrapper around base64::encode; called at L267 (enrollment logging) and L349 (handshake logging) for key display | [DUPLICATE] Identical implementation found in rustguard-enroll/src/client.rs (score 0.992) and rustguard-daemon/src/tunnel.rs (score 0.993). Both use base64::BASE64_STANDARD.encode() to encode 32-byte keys. | [OK] Straightforward base64 encoding of a fixed 32-byte key. No correctness issues. | [LEAN] Three-line display helper for logging public keys. Appropriately small; inlining the base64 import inside the function keeps scope contained. | [NONE] No test file exists for server.rs. base64_key has zero test coverage. Previously confirmed NONE by deliberation override. | [UNDOCUMENTED] Private helper with no /// doc comments. Name and parameter type [u8; 32] clearly communicate intent. Leniency applied for private item. (deliberated: confirmed — Duplication DUPLICATE: Same situation as rand_index — 3-line helper duplicated across crates. Factually correct duplication finding, though the overhead of a shared crate for this trivial function may not be justified. Tests WEAK: No test file; same merger inconsistency as other symbols.)

- [ ] `rustguard-kmod/src/replay.rs` — 1 untested
  Improve `rustguard-kmod/src/replay.test.rs` covering: ReplayWindow

- [ ] `rustguard-kmod/src/noise.rs` — 20 untested
  Improve `rustguard-kmod/src/noise.test.rs` covering: zeroize, constant_time_eq, hash, mac, hkdf, seal, open, dh, generate_keypair, initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, tai64n_now, create_initiation, process_response, process_initiation

- [ ] `rustguard-core/src/handshake.rs` — 16 weak
  Improve `rustguard-core/src/handshake.test.rs` covering: initial_chain_key, initial_hash, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, InitiatorHandshake, create_initiation, create_initiation_psk, create_initiation_with, process_response, process_initiation, process_initiation_psk, process_initiation_with, constant_time_eq
  - initial_chain_key: [USED] Non-exported helper function called in create_initiation_with (L119) and process_initiation_with (L284) | [UNIQUE] Trivial single-call wrapper; too small for meaningful duplication analysis | [OK] Correctly hashes CONSTRUCTION constant to produce the initial chain key per WireGuard Noise_IKpsk2 spec. | [LEAN] Single-expression function computing a one-time constant per the Noise protocol spec. Naming matches the spec operation exactly, aiding auditability. | [WEAK] Private helper exercised only indirectly through the full handshake integration tests. No direct unit test verifies the output value against a known-good constant derived from the WireGuard spec, making it impossible to catch a subtle hash input ordering bug in isolation. | [PARTIAL] Has two /// lines describing purpose ('Precomputed initial handshake state from the protocol constants. These are the same for every handshake — compute once.'). Private function with no parameters, but return value semantics are not described. Adequate intent, incomplete by strict standards. (deliberated: reclassified: documentation: PARTIAL → DOCUMENTED — Tests WEAK is fair — no pinned test vector for this crypto constant, only indirect coverage. Documentation reclassified PARTIAL → DOCUMENTED: two /// lines clearly describe purpose and caching semantics for a private parameterless function; the return value IS the 'initial chain key' as the name states — demanding separate return-value docs on a self-descriptive private helper is overly strict.)
  - initial_hash: [USED] Non-exported helper function called in create_initiation_with (L120) and process_initiation_with (L285) | [UNIQUE] Trivial single-call wrapper; too small for meaningful duplication analysis | [OK] Correctly computes Hash(ck || IDENTIFIER) for the initial hash state, matching the WireGuard protocol specification. | [LEAN] Single-expression Noise protocol operation. Correctly separated from initial_chain_key because h depends on ck. No unnecessary abstraction. | [WEAK] Same situation as initial_chain_key — exercised only through end-to-end handshake tests. No test pins the computed value against a reference vector, so a regression in the CONSTRUCTION/IDENTIFIER concatenation order would only surface as a cryptic handshake failure. | [UNDOCUMENTED] No /// doc comment whatsoever. Private function immediately following initial_chain_key with no documentation; the ck parameter purpose and return semantics are unexplained. (deliberated: reclassified: documentation: DOCUMENTED → UNDOCUMENTED — Tests WEAK is correct — no test vector coverage. Documentation: merged field says DOCUMENTED but the detail section says '[UNDOCUMENTED] No /// doc comment whatsoever', and source confirms lines 27-29 have no doc comment. Reclassified to UNDOCUMENTED to match actual source. Confidence raised because the source code is unambiguous.)
  - mix_hash: [USED] Non-exported helper function called 8 times across create_initiation_with and process_initiation_with | [UNIQUE] Trivial single-call wrapper; too small for meaningful duplication analysis | [OK] Standard Noise MixHash: Hash(h || data). Correctly concatenates h and data via the slice-of-slices interface to crypto::hash. | [LEAN] Trivial one-liner wrapper, but named after the canonical Noise protocol operation it implements. Improves readability in the large handshake functions where it is called many times. | [WEAK] Private helper tested only indirectly. Called at multiple points in both initiator and responder paths; the integration tests implicitly verify it must produce matching values on both sides, but no test targets its behavior in isolation or with known test vectors. | [PARTIAL] Single /// line ('Mix a public key into the hash. Used to bind the responder's identity.'). Private function; the `data` parameter is called 'public key' in the comment but is typed as a generic byte slice, creating a minor mismatch. No return value description. (deliberated: confirmed — Tests WEAK: only indirect coverage, no isolated test — fair for a crypto helper. Documentation PARTIAL: doc says 'Mix a public key' but the function is used for ciphertext, timestamps, and other data too (called 8 times with heterogeneous inputs). The misleading comment is a legitimate doc quality issue.)
  - mix_key: [USED] Non-exported helper function called 8 times across create_initiation_with, process_response, and process_initiation_with | [DUPLICATE] Identical logic to rustguard-kmod version (score 0.974). Both extract and return (new_ck, key) from hkdf. Parameter type difference (SharedSecret vs [u8;32]) is abstraction-level only; semantic contract is identical. | [OK] Calls HKDF with 3 outputs and returns the first two (new_ck = T1, temp_key = T2), discarding T3. This is correct for Noise MixKey / WireGuard KDF2: the first output is the new chaining key, the second is the temp encryption key. | [LEAN] Minimal adapter around crypto::hkdf that projects the (ck, key) pair from the triple. Directly corresponds to the MixKey operation in the Noise spec. | [WEAK] Private helper exercised indirectly. Returns (new_ck, key) from an HKDF; only the first two outputs are used (third ignored). No test verifies the key derivation against reference vectors or confirms the ignored third output doesn't silently affect state. | [PARTIAL] Two /// lines covering purpose and return tuple ('Returns (new_ck, key) where key is for AEAD encryption'). Private function with meaningful coverage; the dh_result parameter is not described and no # Examples are present, though examples are not expected for private helpers. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: the kmod counterpart lives in a kernel module crate with fundamentally different compilation targets, type systems (SharedSecret vs raw [u8;32]), allocators, and build constraints. Deduplicating a 3-line function across a userspace library and a Linux kernel module would require a shared no_std+no_alloc crate abstracting over type differences — this adds coupling and complexity far exceeding the cost of maintaining a trivial function in both places. This is intentional architectural separation, not accidental duplication. Tests WEAK and docs PARTIAL are correct as stated.)
  - encrypt_and_hash: [USED] Non-exported helper function called in create_initiation_with (L135, L143) and process_initiation_with (L325) | [UNIQUE] High structural similarity (0.904) to kmod version, but different semantic contracts: current allocates Vec<u8>, kmod uses provided buffer and returns Option<(h, usize)>. Return type differences and allocation strategies constitute distinct contracts. | [OK] Nonce 0 is hardcoded. This is correct: in WireGuard's Noise_IKpsk2 handshake each operation uses a freshly derived key (from HKDF), so nonce 0 is legitimate per spec. AEAD seal then MixHash(ciphertext) is the correct Noise EncryptAndHash pattern. | [LEAN] Implements the EncryptAndHash Noise primitive faithfully in two lines. The function boundary keeps the large handshake functions readable and matches the spec naming. | [WEAK] Tested indirectly via the full handshake. The tampered_initiation_rejected and tampered_response_rejected tests implicitly validate AEAD authentication, but there are no direct tests of the function's output structure, nonce handling (hardcoded 0), or known-plaintext vectors. | [PARTIAL] Single /// line ('AEAD encrypt, then mix ciphertext into hash'). Private function; operation order is clear from the comment but parameters key, h, and plaintext are not described individually, and the return tuple (new_h, ciphertext) is not documented. (deliberated: confirmed — Tests WEAK: no direct test, no known-plaintext vectors, only integration coverage — correct assessment for a security-critical AEAD wrapper. Documentation PARTIAL: single /// line covers the operation but omits return tuple semantics and parameter descriptions. Both findings are accurate.)
  - decrypt_and_hash: [USED] Non-exported helper function called in process_response (L203) and process_initiation_with (L298, L311) | [UNIQUE] High structural similarity (0.919) to kmod version, but different semantic contracts: current returns Option<(h, Vec<u8>)>, kmod uses buffer and returns Option<(h, usize)>. Allocation and return type differences constitute distinct contracts. | [OK] Symmetric counterpart to encrypt_and_hash. AEAD open with nonce 0 is correct (see encrypt_and_hash reasoning). Hash is mixed with the original ciphertext (not the plaintext), matching the Noise spec. Returns None on AEAD failure — correct error propagation. | [LEAN] Symmetric counterpart to encrypt_and_hash. Returns Option to propagate AEAD auth failure. Minimal and correct. | [WEAK] Tested indirectly. Authentication failure path is covered by tamper tests, but only at message granularity. No test verifies the function returns None on authentication failure in isolation, or that the hash state is correctly updated only when decryption succeeds. | [PARTIAL] Single /// line ('AEAD decrypt, then mix ciphertext into hash'). Private function; the Option return and its failure condition (authentication failure) are not documented, which is a meaningful omission for a security-critical function. (deliberated: confirmed — Tests WEAK: authentication failure path is covered by tamper tests at message granularity but never isolated. Documentation PARTIAL: the Option return (None on auth failure) is a meaningful omission for a security function. Both findings stand.)
  - compute_mac1: [USED] Exported function with 4 runtime importers across tests, daemon, and enrollment modules | [DUPLICATE] Identical logic to rustguard-kmod version (score 0.954). Both hash, mac, and extract 16-byte result. Parameter type difference (PublicKey vs [u8;32]) is abstraction-level only; semantic contract is identical. | [OK] Correctly implements WireGuard MAC1 = MAC(Hash(LABEL_MAC1 || receiver_pub), msg). Truncation of the 32-byte BLAKE2s MAC to 16 bytes is spec-compliant. | [LEAN] Implements the WireGuard MAC1 computation exactly as the RFC specifies. The 16-byte slice copy is necessary because the underlying MAC primitive likely returns more bytes. | [WEAK] Public function imported by daemon, enroll client/server, and integration tests, but never tested directly. wrong_responder_key_rejects exercises the MAC1 rejection path in process_initiation_with. However, there is no test that tampers with the mac1 field directly, no test for the output against a known reference value, and no test for the truncation from 32 to 16 bytes. | [PARTIAL] Public function with two /// lines covering purpose and the BLAKE2s construction formula. No parameter descriptions for responder_public or msg_bytes, return value not described, and no # Examples section — all required for a public API function. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: same rationale as mix_key — the kmod version uses raw [u8;32] and lives in a kernel module with entirely different build/runtime constraints. A 6-line protocol-mandated function does not warrant cross-crate abstraction between kernel and userspace. Tests WEAK: no direct test of output against reference vector or truncation behavior. Documentation PARTIAL: public function missing parameter and return descriptions. Both are legitimate.)
  - InitiatorHandshake: [USED] Exported struct with 4 runtime importers; core state type for initiator handshake protocol | [UNIQUE] No similar struct definitions found in RAG results. Unique data structure for this module. | [OK] Sensitive fields (ck, h, ephemeral, psk) are correctly zeroized on drop. their_public and sender_index are non-secret and correctly skipped or not needing zeroization. | [LEAN] Minimal state bundle for the half-open handshake window. Zeroize/ZeroizeOnDrop derives are security-critical for a crypto library. The #[zeroize(skip)] on non-secret fields is correct and deliberate. | [WEAK] Struct is created and consumed in integration tests but its ZeroizeOnDrop guarantee — a security-critical property — is never verified. No test checks that sensitive fields (ck, h, psk) are zeroed after drop, which is the primary reason for this type's existence. | [PARTIAL] Public struct with two /// lines describing its lifecycle role and the ZeroizeOnDrop security rationale. All fields are private and undocumented. Missing a # Examples section showing the initiator handshake lifecycle, which is expected for a public exported type. (deliberated: confirmed — Tests WEAK: the ZeroizeOnDrop property is security-critical but testing it would essentially be testing the zeroize crate's derive macro — borderline but fair to flag. Documentation PARTIAL: public struct with only lifecycle description; private fields being undocumented is normal Rust but the struct-level docs could mention the security contract more explicitly.)
  - create_initiation: [USED] Exported convenience function with 4 runtime importers; std-only wrapper around create_initiation_with | [UNIQUE] Convenience wrapper that delegates to create_initiation_psk with zero PSK. Different purpose from its target (provides std default values). Not a duplicate of the function it wraps. | [OK] Thin std wrapper that delegates with a zero PSK, matching WireGuard's no-PSK mode (zero key is the protocol-specified default). | [LEAN] One-line std convenience shim that supplies a zero PSK for the common WireGuard-without-PSK case. Calling convention is clean and reduces friction for the majority of callers. | [WEAK] Happy path tested via full_handshake_and_transport and the rejection tests. This is a thin wrapper delegating to create_initiation_psk with a zero PSK. The key gap is that no test verifies the returned Initiation message structure independently (field lengths, mac1 presence) and the PSK=0 assumption is untested with non-zero values. | [PARTIAL] Public function with one /// line noting it is an std convenience wrapper. No parameter descriptions for our_static, their_public, or sender_index; return tuple (Initiation, InitiatorHandshake) is undescribed; no # Examples section for this primary public entry point. (deliberated: confirmed — Tests WEAK: thin wrapper exercised in multiple tests but never with non-default PSK; the wrapper's contract (zero PSK default) is implicitly tested. Documentation PARTIAL: public entry point with no parameter or return descriptions. Both findings stand.)
  - create_initiation_psk: [USED] Exported convenience function with 4 runtime importers; std-only PSK variant wrapper | [UNIQUE] Std convenience wrapper that generates random ephemeral and timestamp, then delegates to create_initiation_with. Wrapper pattern, not duplication of core logic. | [OK] Correctly generates a random EphemeralSecret and a current Tai64n timestamp before delegating to the core function. | [LEAN] Separates RNG and clock access from the deterministic core function, a well-established Rust pattern for testability and no_std compatibility. Three lines of real logic. | [WEAK] Exercised only via create_initiation with a hard-coded all-zero PSK. No test ever passes a non-zero PSK, meaning the PSK mixing path (Noise_IKpsk2 differentiation from IK) is effectively untested. This is a security-critical code path that is invisible in the current test suite. | [PARTIAL] Public function with one brief /// line ('Create a handshake initiation with PSK (std convenience)'). The psk parameter semantics (e.g., all-zeros meaning no PSK) are not documented; no parameter or return value descriptions; no # Examples section. (deliberated: confirmed — Tests WEAK: the PSK code path is never exercised with a non-zero PSK, which is a genuine security-critical gap. Documentation PARTIAL: psk semantics (zero = no PSK) undocumented. Both correct.)
  - create_initiation_with: [USED] Exported core function with 4 runtime importers; implements Noise_IKpsk2 msg1 generation with explicit inputs | [UNIQUE] High similarity (0.925) to rustguard-kmod create_initiation in protocol logic. However, semantic contracts differ: uses StaticSecret/PublicKey/EphemeralSecret/Tai64n abstractions and returns (Initiation, InitiatorHandshake), vs kmod's raw arrays and different struct. Different environments (library vs kernel), type systems, and return structures constitute distinct contracts despite identical Noise_IKpsk2 protocol implementation. | [OK] Follows Noise_IKpsk2 initiator message-1 pattern exactly: MixHash(Spub_r), MixHash(e_pub), KDF2(ck, DH(e,S_r)) -> (ck,k1), EncryptAndHash(k1, Spub_i), KDF2(ck, DH(s,S_r)) -> (ck,k2), EncryptAndHash(k2, timestamp). MAC1 is computed over the first 116 bytes (correct WireGuard wire offset excluding mac1+mac2). expect() panics on wrong AEAD output size are invariant-checks on internal functions, not runtime errors. | [LEAN] The deterministic no_std core. Each operation maps one-to-one to a named Noise_IKpsk2 protocol step. Length is protocol-driven, not artificial. Named variable reassignments (ck, h, key) mirror the spec's sequential state threading. | [WEAK] Core handshake creation function tested indirectly through the wrapper chain. The deterministic ephemeral variant (explicit EphemeralSecret injection) is never called directly in tests, making it impossible to verify against WireGuard protocol test vectors. PSK is always zero in all test paths. MAC1 slice boundary (&wire[..116]) is not verified. | [PARTIAL] Public core function with two /// lines noting it accepts explicit ephemeral and timestamp and is no_std compatible. No parameter descriptions for any of the six parameters, no return value documentation, and no # Examples section for this most general public variant. (deliberated: confirmed — Tests WEAK: core deterministic function never called directly in tests — a missed opportunity for test-vector validation. Documentation PARTIAL: most general public variant with six parameters, none documented. Both findings are accurate and important.)
  - process_response: [USED] Exported function with 4 runtime importers; processes response msg2 and derives transport keys | [UNIQUE] Moderate similarity (0.839) to rustguard-kmod process_response in protocol logic. However, semantic contracts differ: uses high-level crypto types and returns TransportSession, vs kmod's raw arrays and different return structure. Different environments and type abstractions constitute distinct contracts. | [NEEDS_FIX] The response message's mac1 field is never verified. process_initiation_with performs MAC1 verification first (before any DH) as a cheap DDoS guard; process_response skips this check entirely. An attacker can flood the initiator with crafted response packets that each trigger two DH scalar multiplications before being rejected by AEAD. The asymmetry is also a protocol non-compliance with the WireGuard spec which mandates MAC1 checking on all received messages. | [LEAN] Straightforward protocol implementation. receiver_index check, two DH operations, PSK mix, decrypt-and-hash, and key derivation — all mandated by the spec. No superfluous structure. | [WEAK] Three meaningful tests cover this function: happy path (full_handshake_and_transport), ciphertext tampering (tampered_response_rejected), and wrong receiver_index (mismatched_receiver_index_rejected). However, PSK is always zero, the PSK mixing branch (hkdf with psk) is never tested with a real PSK, and the resulting transport key directionality (initiator sends on first, receives on second) is tested in only one direction per session. | [PARTIAL] Public function with a single /// line ('Process a handshake response and derive transport keys'). Failure conditions returning None (receiver_index mismatch, AEAD decryption failure) are not documented; no parameter or return value descriptions; no # Examples. (deliberated: confirmed — Correction NEEDS_FIX confirmed: the WireGuard spec mandates MAC1 verification on all received messages. process_initiation_with correctly checks MAC1 before DH (L258-261), but process_response skips it entirely. While receiver_index provides a cheap first guard, an attacker who observes or guesses the sender_index can force two DH scalar multiplications per forged packet. The asymmetry is a protocol non-compliance. Tests WEAK: PSK always zero, no anti-replay testing. Documentation PARTIAL: failure conditions undocumented. All three findings stand.)
  - process_initiation: [USED] Exported convenience function with 4 runtime importers; std-only responder-side wrapper | [UNIQUE] Std convenience wrapper that delegates to process_initiation_psk with zero PSK. Wrapper pattern providing default values, not duplication. | [OK] Correct std convenience wrapper with zero PSK and no last-timestamp replay protection (caller responsibility). | [LEAN] One-line std convenience shim for the no-PSK, no-replay-check case. Mirrors the structure of create_initiation for API symmetry. | [WEAK] Thin std wrapper tested via wrong_responder_key_rejects and tampered_initiation_rejected, but always delegates to process_initiation_psk with zero PSK and no last_timestamp. The timestamp replay-prevention branch (last_timestamp is Some) is never exercised from this wrapper, which is the primary replay-protection mechanism. | [PARTIAL] Public function with one /// line describing it as the std convenience responder path. The four-element return tuple (PublicKey, Tai64n, Response, TransportSession) is entirely undescribed; no failure condition documentation; no # Examples. (deliberated: confirmed — Tests WEAK: always delegates with zero PSK and None timestamp — replay prevention is never exercised through this wrapper. Documentation PARTIAL: four-element return tuple entirely undescribed. Both correct.)
  - process_initiation_psk: [USED] Exported convenience function with 4 runtime importers; std-only responder PSK variant wrapper | [UNIQUE] Std convenience wrapper that generates random ephemeral and delegates to process_initiation_with. Wrapper pattern providing random value generation, not duplication. | [OK] Correctly generates a fresh random responder EphemeralSecret and delegates to the core function. | [LEAN] Injects RNG-generated ephemeral so the deterministic core remains no_std-compatible and testable. Clean separation of concerns with no padding. | [WEAK] Called only with zero PSK and last_timestamp=None in all test paths. The two security-distinguishing parameters — the PSK itself and the anti-replay timestamp check — are never exercised with non-trivial values. A bug in PSK mixing or timestamp comparison would not be caught by the existing test suite. | [PARTIAL] Public function with one /// line. The security-critical last_timestamp replay-prevention parameter is not documented; psk semantics not described; no # Examples section despite being a primary responder API. (deliberated: confirmed — Tests WEAK: the two security-differentiating parameters (PSK and last_timestamp) are never exercised with non-trivial values — a significant gap. Documentation PARTIAL: last_timestamp replay-prevention semantics undocumented. Both correct.)
  - process_initiation_with: [USED] Exported core function with 4 runtime importers; implements responder msg1 processing with no_std support | [UNIQUE] High similarity (0.877) to rustguard-kmod process_initiation in Noise_IKpsk2 responder logic. However, semantic contracts differ: uses StaticSecret/PublicKey/EphemeralSecret/Tai64n and returns (PublicKey, Tai64n, Response, TransportSession), vs kmod's raw arrays and different return structure. Different environments (library vs kernel) and type systems constitute distinct contracts. | [OK] MAC1 is verified before any DH (correct). Noise_IKpsk2 responder flow is correctly implemented: DH(S_resp,e_init), EncryptAndHash decrypt (static), DH(S_resp,s_init), EncryptAndHash decrypt (timestamp), replay check. Response: MixHash(e_resp), KDF2(ck,DH(e_resp,e_init)), KDF2(ck,DH(e_resp,s_init)), KDF3(ck,psk)->(ck,t,k3), MixHash(t), EncryptAndHash(k3,empty). Transport keys: KDF2(ck,'') with (recv_key,send_key)=(T1,T2) correctly mirrors the initiator's (send_key,recv_key)=(T1,T2) so keys align across the session. | [LEAN] Longest function in the file but every line is a mandatory protocol step (MAC1 verify, DH, decrypt, timestamp replay check, response construction). MAC1 checked first as a cheap pre-DH guard is a deliberate and correct optimization. | [WEAK] Core responder function with several meaningful rejection tests (MAC1, AEAD tampering, wrong key). Critical gap: the last_timestamp replay-protection branch (the is_after check) is never exercised — last_timestamp is always None in tests. Non-zero PSK is never tested. These are both security-critical paths. The MAC2/cookie TODO also indicates an untested future surface. | [PARTIAL] Public no_std-compatible function with a single /// line. The most complex function in the file: MAC1 verification, multiple DH operations, replay detection, and PSK phase are all undescribed in the doc comment. Multiple None return paths and the four-element return tuple are not documented. No # Examples. (deliberated: confirmed — Tests WEAK: last_timestamp replay check never exercised (always None), non-zero PSK never tested — both are security-critical paths with zero coverage. Documentation PARTIAL: most complex function in file (108 lines) with a single /// line, multiple failure modes undocumented. Both findings are significant and correct.)
  - constant_time_eq: [USED] Non-exported helper function called in process_initiation_with (L296) for constant-time MAC comparison | [UNIQUE] Different implementations and parameter contracts from similar functions: uses subtle crate's ConstantTimeEq trait (safe, high-level), vs kmod's unsafe C memneq call. Fixed [u8;16] parameter vs variable-length &[u8]. Different approaches to constant-time comparison constitute distinct contracts. | [OK] Uses subtle::ConstantTimeEq correctly. ct_eq on [u8;16] arrays produces a Choice that is safely coerced to bool via Into<bool>. No timing side-channel. | [LEAN] Thin, correctly-typed wrapper over the subtle crate's ConstantTimeEq. Encapsulates the .into() conversion and provides a clean call site. Using subtle rather than a hand-rolled comparison is the right choice. | [WEAK] Exercised indirectly by wrong_responder_key_rejects (MAC1 mismatch path). No direct unit test verifies constant-time semantics, confirms that a single differing byte returns false, or that equal arrays return true. The subtle::ConstantTimeEq usage is correct by inspection but untested in isolation. | [PARTIAL] Private function with one /// line naming the subtle crate. The security rationale (prevents timing side-channel attacks on MAC comparison) is implied but not stated. Acceptable for a private two-line helper but the security significance warrants more explicit documentation. (deliberated: confirmed — Tests WEAK: exercised indirectly via MAC1 mismatch path but no direct unit test. Documentation PARTIAL: security rationale (timing side-channel prevention) is implied but not explicitly stated. For a security-critical helper, this is a fair assessment on both axes.)

- [ ] `rustguard-core/src/cookie.rs` — 13 weak
  Improve `rustguard-core/src/cookie.test.rs` covering: now, elapsed_since, NO_STD_COUNTER, now, elapsed_since, CookieChecker, CookieState, encode_addr, random_bytes, random_nonce, fill_random, fill_random, constant_time_eq_16
  - now: [USED] no_std version called in 3 places: CookieChecker::new, maybe_rotate_secret, process_reply | [UNIQUE] no_std variant returning atomic counter increment. Trivial 2-line function. | [OK] No_std stub returns an incrementing counter. The value is stored in secret_generated and received fields but is always ignored by elapsed_since (which returns ZERO), so the counter value has no effect on program behavior. | [ACCEPTABLE] Increments NO_STD_COUNTER but elapsed_since always returns Duration::ZERO regardless, so the increment is meaningless. Could return a constant 0u64, but keeping the pattern symmetric with the std version is a valid stylistic choice. | [NONE] no_std version of now(), gated behind #[cfg(not(feature = "std"))]. All inline tests compile with std, so this path is entirely untested. | [UNDOCUMENTED] no_std-gated private function with no `///` doc comment. The important caveat — that it uses a simple incrementing counter, not real time — is undocumented at the function level. (deliberated: confirmed — Tests WEAK for the no_std version at L46-48: gated behind cfg(not(feature="std")), never compiled or tested. WEAK is fair given it's a trivial atomic increment.)
  - elapsed_since: [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.)
  - NO_STD_COUNTER: [USED] Static atomic counter used in no_std now() function for monotonic timestamp | [UNIQUE] Static atomic counter for no_std clock fallback. No similar symbols found. | [OK] AtomicU64 initialized to 0 serves as a monotonic stub for no_std now(). Its returned value is stored but fully ignored by no_std elapsed_since, so overflow is not a correctness concern. | [ACCEPTABLE] Atomic counter used as a fake Timestamp in no_std. However, elapsed_since ignores it and returns ZERO, making every fetch_add a side-effect that serves no real purpose. The no_std path effectively disables time-based expiry, which the comment acknowledges. Slightly over-designed: the counter could simply be omitted and now() could return 0u64 directly, but the overhead is negligible. | [NONE] Compiled only under #[cfg(not(feature = "std"))]. All inline tests use the std feature path, so this atomic counter is never exercised by any test in the suite. | [UNDOCUMENTED] Private no_std static with no `///` doc comment. Its role as a monotonic counter substitute is only implied by context; there is no documentation explaining its limitations or purpose. (deliberated: confirmed — Tests WEAK is accurate: no_std static compiled only under cfg(not(feature="std")), never exercised by any test since all tests compile with std.)
  - now: [USED] no_std version called in 3 places: CookieChecker::new, maybe_rotate_secret, process_reply | [UNIQUE] no_std variant returning atomic counter increment. Trivial 2-line function. | [OK] No_std stub returns an incrementing counter. The value is stored in secret_generated and received fields but is always ignored by elapsed_since (which returns ZERO), so the counter value has no effect on program behavior. | [ACCEPTABLE] Increments NO_STD_COUNTER but elapsed_since always returns Duration::ZERO regardless, so the increment is meaningless. Could return a constant 0u64, but keeping the pattern symmetric with the std version is a valid stylistic choice. | [NONE] no_std version of now(), gated behind #[cfg(not(feature = "std"))]. All inline tests compile with std, so this path is entirely untested. | [UNDOCUMENTED] no_std-gated private function with no `///` doc comment. The important caveat — that it uses a simple incrementing counter, not real time — is undocumented at the function level. (deliberated: confirmed — Tests WEAK for the no_std version at L46-48: gated behind cfg(not(feature="std")), never compiled or tested. WEAK is fair given it's a trivial atomic increment.)
  - elapsed_since: [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.)
  - CookieChecker: [DEAD] Exported struct but has 0 external importers per import analysis | [UNIQUE] Server-side cookie checker struct. No similar struct found in codebase. | [OK] All struct field types are correct and logically appropriate for their roles. | [LEAN] Four fields that map 1:1 to the server-side WireGuard cookie state: public key, rotating secret, timestamp, and load flag. No unnecessary generics or traits. | [WEAK] Inline tests cover create_reply, verify_mac2, and the wrong-source failure case. However, verify_mac1 is never tested (no test sends a message and checks MAC1 acceptance/rejection), new_with is untested, and secret rotation via maybe_rotate_secret has no coverage. The create_reply_from_bytes and verify_mac2_from_bytes no_std paths are also untested. | [PARTIAL] Public struct has a clear one-line `///` description and each field (including the sole pub field `under_load`) has a `///` comment. Missing: no `# Examples` demonstrating construction and usage, and the struct doc does not mention the expected usage pattern or lifecycle. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieChecker is the primary public API struct for server-side cookie checking in a library crate. '0 external importers' only reflects workspace analysis, not downstream consumers. Tests reclassified NONE→WEAK: the test module uses CookieChecker in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), mac2_wrong_source_fails(). The detail itself says 'WEAK' with notes on missing verify_mac1 and secret rotation coverage, contradicting the NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 57 has `/// Server-side cookie state: generates cookies and validates MAC2.` and each field has `///` comments. Missing examples and lifecycle docs make it PARTIAL, not UNDOCUMENTED.)
  - CookieState: [DEAD] Exported struct but has 0 external importers per import analysis | [UNIQUE] Client-side cookie storage struct. No similar struct found in codebase. | [OK] Option-wrapped fields are correctly typed for optional cookie and received-timestamp state. | [LEAN] Minimal two-field client-side state: the cookie and when it was received. Nothing extraneous. | [WEAK] new, process_reply (happy path and wrong-mac1 failure), compute_mac2 (with and without cookie) are all tested. However, the staleness expiry path (cookie older than COOKIE_SECRET_LIFETIME causing compute_mac2 to return zeros) is not tested — is_fresh returning false is never exercised. | [PARTIAL] Public struct has a one-line `///` description and private fields are individually documented. Missing: no `# Examples` section and no mention of how to obtain and use a CookieState (e.g., pair with CookieChecker). Public API lacks usage guidance. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieState is the primary public API struct for client-side cookie storage in a library crate. Tests reclassified NONE→WEAK: used in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), no_cookie_gives_zero_mac2(). Detail says 'WEAK' with notes on missing staleness expiry test, contradicting NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 69 has `/// Client-side cookie state: stores a received cookie for MAC2.` with field docs. Terse but present.)
  - encode_addr: [USED] Used in 2 places: create_reply (line 128) and verify_mac2 (line 150) | [UNIQUE] Encodes SocketAddr to byte representation for MAC. No similar functions found. | [OK] IPv4 encoding (4-byte octets + 2-byte BE port = 6 bytes) and IPv6 encoding (16-byte octets + 2-byte BE port = 18 bytes) are both correct and match the documentation in make_cookie_from_bytes. | [ACCEPTABLE] Allocates a Vec<u8> for a payload that is always exactly 6 or 18 bytes. A fixed-size stack buffer (e.g. an enum or a [u8; 18] with a length) would avoid the heap allocation, but the Vec keeps the caller interface simple (just &[u8]). Minor inefficiency, not a design smell. | [WEAK] Only IPv4 addresses (10.0.0.1:12345) are used in the inline tests. The IPv6 branch (SocketAddr::V6, 18-byte encoding) is never exercised. Missing coverage for the IPv6 code path and port byte-order correctness for IPv6. | [PARTIAL] Private std-gated function has `/// Encode a socket address to bytes for cookie MAC.` — a minimal description. No parameter documentation, no mention of the encoding format (though format is described on `make_cookie_from_bytes`). Acceptable for a private helper but incomplete. (deliberated: confirmed — Tests WEAK is accurate: only IPv4 addresses tested in the suite; IPv6 branch at L247 is never exercised. Documentation PARTIAL is accurate: has a minimal `///` comment but no parameter docs or encoding format description.)
  - random_bytes: [USED] Used in 2 places: CookieChecker::new (line 80) and maybe_rotate_secret (line 97) | [UNIQUE] Generates random 32-byte buffer via fill_random. Similar kernel version uses unsafe calls; different implementation platforms. | [OK] Allocates a 32-byte zeroed buffer and delegates to fill_random. The function logic itself is correct; any no_std deficiency lives in fill_random. | [LEAN] Thin typed wrapper over fill_random for a 32-byte buffer. Keeps key-generation sites readable. | [WEAK] Private helper called by CookieChecker::new and maybe_rotate_secret. Exercised implicitly by the roundtrip tests but no test checks output length, non-zero output, or that successive calls differ (uniqueness). Only incidentally validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is self-explanatory but there is no documentation of the security requirement (CSPRNG) or the output length. (deliberated: confirmed — Tests WEAK is fair: implicitly exercised through CookieChecker construction and secret rotation in tests, but no direct assertions on output length, non-zero, or uniqueness.)
  - random_nonce: [USED] Used once in create_reply_from_bytes (line 118) to generate random nonce for encryption | [DUPLICATE] Generates random 24-byte nonce. High semantic similarity (score 0.943) with identical structure in protocol.rs. | [OK] Allocates 24 bytes (correct for XChaCha20-Poly1305) and delegates to fill_random. The function logic itself is correct. | [LEAN] Mirrors random_bytes for a 24-byte nonce. Minor structural duplication with random_bytes — a const-generic helper could unify them — but at this scale the duplication is negligible and both functions are self-documenting. | [WEAK] Private helper called inside create_reply_from_bytes. Implicitly exercised by the roundtrip test but nonce length, uniqueness, or non-zero properties are never directly asserted. | [UNDOCUMENTED] Private function with no `///` doc comment. No documentation of nonce size (24 bytes for XSalsa20) or the requirement for uniqueness/randomness. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE→UNIQUE: the duplicate target is in `rustguard-enroll/src/protocol.rs`, a different crate. Cross-crate deduplication of a trivial 3-line private helper would introduce unnecessary inter-crate coupling. Two functions with the same structure in separate crates reflect independent usage, not harmful duplication. Tests WEAK kept: implicitly exercised but not directly verified.)
  - fill_random: [USED] no_std stub version called in 2 places: random_bytes and random_nonce | [UNIQUE] no_std stub returning immediately (no-op for kernel fallback). Different from std getrandom version despite RAG score. | [NEEDS_FIX] The no_std stub body is `let _ = buf` — it does not fill the buffer, leaving it as all zeros. create_reply_from_bytes() is not gated on #[cfg(feature = "std")], so it is callable in no_std mode. It calls random_nonce() -> fill_random() and gets a constant 24-byte zero nonce. The encryption key crypto::hash(&[LABEL_COOKIE, our_public]) is also fixed per peer. A fixed key combined with a constant zero nonce means the identical XChaCha20-Poly1305 keystream encrypts every Cookie Reply sent to the same peer, allowing an attacker who observes two ciphertexts to XOR them and recover plaintext cookie bytes. | [LEAN] Clearly commented no_std stub. The comment explains the kernel override strategy, making the design intent unambiguous. | [NONE] no_std stub that does nothing (leaves buffer unchanged). Gated behind #[cfg(not(feature = "std"))]. All inline tests use the std path, so this stub is never compiled or executed in the test suite. | [UNDOCUMENTED] no_std-gated counterpart of fill_random. No `///` doc comment; the important detail that this is a stub that produces zeroed output is conveyed only through inline `//` comments invisible to rustdoc. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L275-280: the stub leaves the buffer zeroed. Since create_reply_from_bytes() is not cfg-gated, calling it in no_std produces a constant zero nonce with a fixed encryption key, enabling keystream reuse attacks. While comments document the stub as compilation-only with kernel override, the lack of compile-time enforcement (cfg gate) makes this a legitimate safety concern. Tests WEAK kept: never compiled under test configuration.)
  - fill_random: [USED] no_std stub version called in 2 places: random_bytes and random_nonce | [UNIQUE] no_std stub returning immediately (no-op for kernel fallback). Different from std getrandom version despite RAG score. | [NEEDS_FIX] The no_std stub body is `let _ = buf` — it does not fill the buffer, leaving it as all zeros. create_reply_from_bytes() is not gated on #[cfg(feature = "std")], so it is callable in no_std mode. It calls random_nonce() -> fill_random() and gets a constant 24-byte zero nonce. The encryption key crypto::hash(&[LABEL_COOKIE, our_public]) is also fixed per peer. A fixed key combined with a constant zero nonce means the identical XChaCha20-Poly1305 keystream encrypts every Cookie Reply sent to the same peer, allowing an attacker who observes two ciphertexts to XOR them and recover plaintext cookie bytes. | [LEAN] Clearly commented no_std stub. The comment explains the kernel override strategy, making the design intent unambiguous. | [NONE] no_std stub that does nothing (leaves buffer unchanged). Gated behind #[cfg(not(feature = "std"))]. All inline tests use the std path, so this stub is never compiled or executed in the test suite. | [UNDOCUMENTED] no_std-gated counterpart of fill_random. No `///` doc comment; the important detail that this is a stub that produces zeroed output is conveyed only through inline `//` comments invisible to rustdoc. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L275-280: the stub leaves the buffer zeroed. Since create_reply_from_bytes() is not cfg-gated, calling it in no_std produces a constant zero nonce with a fixed encryption key, enabling keystream reuse attacks. While comments document the stub as compilation-only with kernel override, the lack of compile-time enforcement (cfg gate) makes this a legitimate safety concern. Tests WEAK kept: never compiled under test configuration.)
  - constant_time_eq_16: [USED] Used in 2 places: verify_mac1 (line 137) and verify_mac2_from_bytes (line 155) | [UNIQUE] Compares 16-byte arrays with slice flexibility and length check. Different signature and semantic contract from handshake.rs variants. | [OK] The early return for b.len() < 16 is safe because all callers pass slices of exactly 16 bytes (&expected[..16]). The subtle::ConstantTimeEq comparison is timing-safe and correctly restricts comparison to the first 16 bytes. | [LEAN] Correct use of the subtle crate for constant-time MAC comparison. Asymmetric signature (&[u8;16] vs &[u8]) is pragmatic given that callers slice from crypto::mac output. The early-return length check is a safe guard, not bloat. | [WEAK] Exercised indirectly by verify_mac2 (matching and mismatched cases). However, the explicit short-slice guard (b.len() < 16 returning false) is never tested directly, and verify_mac1 — the other caller — is not tested at all in the inline suite. | [UNDOCUMENTED] Private function with no `///` doc comment. Its security-critical nature (constant-time comparison to prevent timing side-channels) and the length-check short-circuit are not documented. (deliberated: confirmed — Tests WEAK is accurate: indirectly tested through verify_mac2 (matching and mismatched cases), but the short-slice guard (b.len() < 16) is never directly tested, and verify_mac1 (the other caller) has no test coverage in the inline suite.)

- [ ] `rustguard-daemon/src/tunnel.rs` — 2 untested, 10 weak
  Improve `rustguard-daemon/src/tunnel.test.rs` covering: run, rand_index, fill_random, ctrlc_handler, signal_noop, base64_key, assign_v6_address, assign_v6_address, add_route, delete_route, add_route, delete_route
  - rand_index: [USED] called at L154 and L424 to generate random sender indices | [DUPLICATE] Generates random u32 index via getrandom. RAG score 0.960 shows nearly identical implementation in server.rs—both fill buffer and convert to u32 identically. | [OK] Correctly reads 4 bytes of OS entropy via fill_random and converts to u32 for the sender index. | [LEAN] Tiny focused helper that reads 4 bytes of OS entropy and converts to u32. The two-level split (rand_index → fill_random) is minor but readable. | [NONE] No test file found. rand_index wraps fill_random with a u32 conversion; no tests exist for distribution properties, output length, or uniqueness. | [DOCUMENTED] Private function with `/// Generate a random u32 sender index using OS entropy.` clearly stating purpose and entropy source. No examples expected on a private helper with no parameters. (deliberated: confirmed — Duplication DUPLICATE is technically correct — 96% identical logic exists in rustguard-enroll/src/server.rs. However, these are in separate binary crates; extracting 5 lines to a shared crate has its own cost. Finding stands but practical impact is low. Tests WEAK is appropriate for a thin wrapper with no test file. Raised confidence slightly since cross-crate duplication is verifiable.)
  - fill_random: [USED] called from rand_index at L496 to fill random buffer | [UNIQUE] Trivial 2-line wrapper for getrandom with expect. No similar functions found. | [OK] getrandom::getrandom is cryptographically secure and cross-platform; panicking on failure is appropriate in a security-sensitive path. | [LEAN] One-line wrapper around getrandom with a descriptive panic message. Too small to flag; improves call-site clarity. | [NONE] No test file found. Thin wrapper over getrandom::getrandom with a panic on failure; no tests cover the happy path or the (unreachable in practice) panic branch. | [DOCUMENTED] Private function with `/// Read random bytes from the OS via the getrandom crate (cross-platform).` — names the crate, mentions cross-platform behavior. Sufficient for a private one-liner helper. (deliberated: confirmed — Tests WEAK is appropriate: one-line wrapper over getrandom with no test coverage. The function is trivial enough that WEAK (not NONE) is the correct classification.)
  - signal_noop: [USED] referenced in L515 and L516 as signal handler callback | [UNIQUE] Trivial 1-line extern C signal handler stub. No duplicates identified. | [OK] Valid extern "C" fn(c_int) signature for a signal handler; intentional no-op is correct given that delivery is intended to be handled by sigwait in the handler thread. | [LEAN] Required no-op extern C function for the libc::signal registration in ctrlc_handler. Minimal and unavoidable given the chosen signal approach. | [NONE] No test file found. Empty extern C signal handler; no tests exist, though behavioral coverage is trivial given the empty body. | [UNDOCUMENTED] Private `extern "C"` signal handler with no `///` doc comment. Name implies a no-op, but why it exists alongside ctrlc_handler is undocumented. Private-item leniency applied. (deliberated: confirmed — Tests WEAK is correct: empty extern C function that serves as a no-op signal handler. Practically untestable in isolation; WEAK is the right classification for a trivial companion to ctrlc_handler.)
  - base64_key: [USED] called at L96, L309, L356, L430 to format public keys | [DUPLICATE] Encodes 32-byte key to base64 string. RAG score 0.994 with identical logic in client.rs—both use BASE64_STANDARD.encode on [u8; 32]. | [OK] Correctly encodes a 32-byte key slice with BASE64_STANDARD; no correctness issues. | [LEAN] Simple display helper that encodes a 32-byte key to base64 for logging. Three lines, single responsibility. | [NONE] No test file found. base64_key encodes a 32-byte key; no tests verify correctness of encoding, padding, or known-vector output. | [UNDOCUMENTED] Private helper with no `///` doc comment. Name is self-descriptive; the function is trivial. Private-item leniency applied. (deliberated: confirmed — Duplication DUPLICATE is correct — 99% identical to base64_key in rustguard-enroll/src/client.rs. Both encode [u8;32] via BASE64_STANDARD.encode. Cross-crate dedup to a shared utility is warranted given the near-identical signature and logic. Tests WEAK correct for a trivial encoder with no test file.)
  - assign_v6_address: [USED] Linux version called at L77 for IPv6 address assignment | [UNIQUE] Linux-specific IPv6 address assignment via ip command. Platform-conditional. No duplicates found. | [OK] Linux: correct 'ip -6 addr add <addr> dev <iface>' invocation for IPv6 address assignment. | [LEAN] Minimal Linux-specific ip addr wrapper. Symmetric to the macOS variant; no unnecessary abstraction. | [NONE] No test file found (Linux cfg variant). Shells out to ip -6 addr add; no tests cover successful assignment, failure propagation, or stderr capture. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment at all — the doc comment exists only on the macOS variant. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated variant (L543-547). Shells out to ip command; no test coverage exists.)
  - assign_v6_address: [USED] Linux version called at L77 for IPv6 address assignment | [UNIQUE] Linux-specific IPv6 address assignment via ip command. Platform-conditional. No duplicates found. | [OK] Linux: correct 'ip -6 addr add <addr> dev <iface>' invocation for IPv6 address assignment. | [LEAN] Minimal Linux-specific ip addr wrapper. Symmetric to the macOS variant; no unnecessary abstraction. | [NONE] No test file found (Linux cfg variant). Shells out to ip -6 addr add; no tests cover successful assignment, failure propagation, or stderr capture. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment at all — the doc comment exists only on the macOS variant. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated variant (L543-547). Shells out to ip command; no test coverage exists.)
  - add_route: [USED] Linux version called at L104 to add IP routes | [UNIQUE] Linux route addition via ip command. Similar function in client.rs (score 0.864) has different signature (lacks is_v6 parameter). Different semantic contract. | [OK] Linux: correct 'ip [-4|-6] route add <route> dev <iface>' invocation. | [LEAN] Linux ip route add wrapper with v4/v6 flag selection. Minimal and correct. | [NONE] No test file found (Linux cfg variant). Shells out to ip route add; no tests cover IPv4/IPv6 subcmd selection, argument construction, or error handling. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated add_route (L567-572). OS command wrapper with no test coverage.)
  - delete_route: [USED] Linux version called at L481 for route cleanup | [UNIQUE] Linux route deletion via ip command. Platform-conditional. Different function from add_route despite structural similarity (del vs add in command). | [OK] Linux: correctly mirrors add_route using 'ip route del', symmetric with the add path. | [LEAN] Linux ip route del wrapper, symmetric counterpart to add_route. No overengineering. | [NONE] No test file found (Linux cfg variant). Shells out to ip route del; no tests cover argument construction, error handling, or cleanup on shutdown. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated delete_route (L575-580). OS command wrapper with no test coverage.)
  - add_route: [USED] Linux version called at L104 to add IP routes | [UNIQUE] Linux route addition via ip command. Similar function in client.rs (score 0.864) has different signature (lacks is_v6 parameter). Different semantic contract. | [OK] Linux: correct 'ip [-4|-6] route add <route> dev <iface>' invocation. | [LEAN] Linux ip route add wrapper with v4/v6 flag selection. Minimal and correct. | [NONE] No test file found (Linux cfg variant). Shells out to ip route add; no tests cover IPv4/IPv6 subcmd selection, argument construction, or error handling. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated add_route (L567-572). OS command wrapper with no test coverage.)
  - delete_route: [USED] Linux version called at L481 for route cleanup | [UNIQUE] Linux route deletion via ip command. Platform-conditional. Different function from add_route despite structural similarity (del vs add in command). | [OK] Linux: correctly mirrors add_route using 'ip route del', symmetric with the add path. | [LEAN] Linux ip route del wrapper, symmetric counterpart to add_route. No overengineering. | [NONE] No test file found (Linux cfg variant). Shells out to ip route del; no tests cover argument construction, error handling, or cleanup on shutdown. | [UNDOCUMENTED] Linux cfg-gated variant has no `///` doc comment. Private function; leniency applied. (deliberated: confirmed — Tests WEAK is correct for the Linux cfg-gated delete_route (L575-580). OS command wrapper with no test coverage.)

- [ ] `rustguard-core/src/messages.rs` — 3 untested, 9 weak
  Improve `rustguard-core/src/messages.test.rs` covering: MSG_INITIATION, MSG_RESPONSE, MSG_COOKIE_REPLY, MSG_TRANSPORT, Initiation, INITIATION_SIZE, Response, RESPONSE_SIZE, Transport, CookieReply, COOKIE_REPLY_SIZE, TRANSPORT_HEADER_SIZE
  - MSG_INITIATION: [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 1 is correct for WireGuard handshake initiation type. to_le_bytes() yields [0x01,0x00,0x00,0x00] matching the wire format documented. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [WEAK] The initiation_roundtrip test checks that bytes[0..4] equal MSG_INITIATION.to_le_bytes(), but never asserts the constant's actual wire value (1). The test only verifies self-consistency, not the protocol-mandated value. | [UNDOCUMENTED] The group `///` comment at line 3 is separated from this constant by a blank line, so it does not attach to MSG_INITIATION in Rust's doc-comment model. No individual `///` is present on this constant. (deliberated: confirmed — Tests WEAK is correct: initiation_roundtrip asserts bytes[0..4] == MSG_INITIATION.to_le_bytes(), which exercises the constant but only verifies self-consistency, never the protocol-mandated value 1 directly. Documentation UNDOCUMENTED is technically correct — the blank line at line 4 breaks Rust doc-comment attachment from the line 3 group comment, so rustdoc won't associate it. However, the constant's name and value are self-documenting in protocol context, making the practical impact low.)
  - MSG_RESPONSE: [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 2 is correct for WireGuard handshake response type. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [WEAK] The response_roundtrip test checks that bytes[0..4] equal MSG_RESPONSE.to_le_bytes() for self-consistency, but the actual value (2) is never directly asserted against the WireGuard spec. | [UNDOCUMENTED] No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: confirmed — Tests WEAK: same self-consistency pattern as MSG_INITIATION — response_roundtrip asserts type bytes match MSG_RESPONSE.to_le_bytes() but never asserts the value 2. Documentation UNDOCUMENTED: same blank-line attachment issue as MSG_INITIATION. Both findings are accurate.)
  - MSG_TRANSPORT: [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 4 is correct for WireGuard transport data type. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [NONE] The transport_roundtrip test never checks bytes[0..4] against MSG_TRANSPORT, so the constant's encoding is never verified. Its actual wire value (4) is also never directly asserted. | [UNDOCUMENTED] No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: MSG_TRANSPORT IS exercised in transport_roundtrip via Transport::to_bytes() which writes MSG_TRANSPORT.to_le_bytes() into the buffer. The constant participates in the test code path — it's just never directly asserted against in assertions. This is the same pattern as MSG_INITIATION/MSG_RESPONSE (exercised but not independently validated), which were rated WEAK. NONE was inconsistent. Documentation UNDOCUMENTED stands — no attached doc comment.)
  - Initiation: [USED] Exported struct with 4 runtime importers; used for WireGuard handshake initiation messages | [UNIQUE] No RAG data available; unique WireGuard handshake initiation struct | [OK] Field sizes are consistent with the wire format doc: ephemeral(32)+encrypted_static(48)+encrypted_timestamp(28)+mac1(16)+mac2(16). to_bytes/from_bytes byte ranges are exactly correct and cover all 148 bytes. The unwrap() calls in from_bytes are safe because the input is a fixed-size &[u8; 148] so all sub-slices have statically known sizes. | [LEAN] Plain struct with fixed-size arrays exactly mirroring the WireGuard handshake initiation wire layout (148 bytes). No generics, no inheritance, no unnecessary abstraction. Field sizes correspond precisely to spec-mandated crypto sizes. | [WEAK] initiation_roundtrip covers to_bytes/from_bytes and verifies the type tag, sender_index, and ephemeral fields. However, encrypted_static, encrypted_timestamp, mac1, and mac2 are never verified after the roundtrip, leaving half the struct fields untested. | [PARTIAL] Type-level `///` comment (lines 11–21) provides a thorough wire-format diagram covering all fields. However, individual public fields carry no `///` doc comments, and there is no `# Examples` section as expected for a public API struct. (deliberated: confirmed — Tests WEAK: initiation_roundtrip verifies sender_index and ephemeral after roundtrip but does not assert encrypted_static, encrypted_timestamp, mac1, or mac2 — leaving 4 of 6 fields unchecked. While the serialization pattern is identical for all fields, byte range errors in unchecked fields would go undetected. Documentation PARTIAL: the struct has an excellent wire-format diagram at the type level, but individual public fields lack doc comments. Both findings are accurate and proportionate.)
  - INITIATION_SIZE: [USED] Exported constant with 4 runtime importers; defines 148-byte size for Initiation messages | [UNIQUE] No RAG data available; unique size constant for Initiation message | [OK] type(4)+sender(4)+ephemeral(32)+encrypted_static(48)+encrypted_timestamp(28)+mac1(16)+mac2(16)=148. Correct. | [LEAN] Named size constant used in the fixed-size return type of to_bytes(). Eliminates magic numbers and is directly consumed by the impl block. | [WEAK] INITIATION_SIZE is exercised implicitly as an array size in to_bytes/from_bytes, which are called in initiation_roundtrip, but the constant value of 148 is never directly asserted. | [UNDOCUMENTED] No `///` doc comment present. The value 148 is inferable from the struct's wire-format diagram, but the constant itself has no attached documentation. (deliberated: confirmed — Tests WEAK: INITIATION_SIZE is implicitly exercised as the array size in to_bytes/from_bytes called during initiation_roundtrip, but the value 148 is never directly asserted. If the value were wrong, compilation would fail with mismatched array sizes rather than a test failure, so this is an inherent compile-time check. Still, WEAK is fair since no explicit test validates the constant. Documentation UNDOCUMENTED: no doc comment attached to this constant.)
  - Response: [USED] Exported struct with 4 runtime importers; used for WireGuard handshake response messages | [UNIQUE] No RAG data available; unique WireGuard handshake response struct | [OK] Field sizes consistent with wire doc. to_bytes byte ranges verified: type(0-3), sender(4-7), receiver(8-11), ephemeral(12-43, 32 bytes), encrypted_empty(44-59, 16 bytes), mac1(60-75), mac2(76-91). Total 92 bytes matches RESPONSE_SIZE. | [LEAN] Plain struct mapping exactly to the 92-byte WireGuard handshake response wire format. Minimal fields, fixed-size arrays, no unnecessary indirection. | [WEAK] response_roundtrip covers to_bytes/from_bytes and verifies the type tag, sender_index, and receiver_index. However, ephemeral, encrypted_empty, mac1, and mac2 are never verified after the roundtrip. | [PARTIAL] Type-level `///` comment (lines 33–44) provides a detailed wire-format diagram. Individual public fields lack `///` doc comments and there is no `# Examples` section. (deliberated: confirmed — Tests WEAK: response_roundtrip verifies sender_index and receiver_index but not ephemeral, encrypted_empty, mac1, or mac2 — leaving 4 of 6 fields unchecked after deserialization. Documentation PARTIAL: excellent struct-level wire-format diagram but no field-level doc comments. Both findings match the evidence in the test module.)
  - RESPONSE_SIZE: [USED] Exported constant with 4 runtime importers; defines 92-byte size for Response messages | [UNIQUE] No RAG data available; unique size constant for Response message | [OK] type(4)+sender(4)+receiver(4)+ephemeral(32)+encrypted_empty(16)+mac1(16)+mac2(16)=92. Correct. | [LEAN] Named size constant used as the return array size in to_bytes(). Directly justified by usage in impl block and spec documentation. | [WEAK] RESPONSE_SIZE is implicitly exercised through the Response impl used in response_roundtrip, but the constant's value of 92 is never directly asserted. | [UNDOCUMENTED] No `///` doc comment present. The constant has no attached documentation. (deliberated: confirmed — Tests WEAK: implicitly exercised as array size in Response::to_bytes/from_bytes but never directly asserted. Same pattern as INITIATION_SIZE — compile-time array sizing provides some implicit validation. Documentation UNDOCUMENTED: no attached doc comment.)
  - Transport: [USED] Exported struct with 4 runtime importers; used for WireGuard transport data packets | [UNIQUE] No RAG data available; unique WireGuard transport data struct | [OK] to_bytes correctly emits type(4)+receiver(4)+counter(8)+payload. from_bytes guards on TRANSPORT_HEADER_SIZE(16) before accessing fixed offsets. Byte ranges buf[4..8], buf[8..16] are safe because the length check ensures buf.len()>=16. Payload extracted from buf[16..] correctly. Counter nonce is correctly read as LE u64. | [LEAN] Plain struct with Vec<u8> payload, which is appropriate since transport packet payloads are variable-length encrypted blobs. No abstraction overhead. | [WEAK] transport_roundtrip covers to_bytes/from_bytes for the happy path and checks receiver_index, counter, and payload length. However: the type byte is never verified, payload content is not checked (only length), and the from_bytes None path (buffer shorter than TRANSPORT_HEADER_SIZE) is never tested. | [PARTIAL] Type-level `///` comment (lines 56–63) provides a wire-format diagram. Public fields (`receiver_index`, `counter`, `payload`) have no individual `///` comments, and no `# Examples` section is provided. (deliberated: confirmed — Tests WEAK: transport_roundtrip checks receiver_index, counter, and payload.len() but never verifies the type tag bytes, never checks actual payload content (only length), and never tests the from_bytes None path (buffer shorter than TRANSPORT_HEADER_SIZE). Documentation PARTIAL: has struct-level wire-format diagram, but public fields lack individual doc comments.)
  - TRANSPORT_HEADER_SIZE: [USED] Exported constant with 4 runtime importers; defines 16-byte header size for Transport messages | [UNIQUE] No RAG data available; unique size constant for Transport header | [OK] type(4)+receiver_index(4)+counter(8)=16. Used correctly as minimum buffer length guard in from_bytes and as capacity offset in to_bytes. | [LEAN] Named constant for the 16-byte fixed transport header, used in Transport::to_bytes() capacity hint and Transport::from_bytes() minimum-length guard. Justified and minimal. | [NONE] TRANSPORT_HEADER_SIZE governs the minimum buffer length check in Transport::from_bytes, but the short-buffer None branch is never tested, so this constant's semantic role is entirely unverified. | [UNDOCUMENTED] No `///` doc comment present. The meaning (16-byte transport header) is inferable from context but no doc comment is attached to this constant. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: TRANSPORT_HEADER_SIZE is exercised in transport_roundtrip via Transport::to_bytes() (capacity hint) and Transport::from_bytes() (length guard happy path). The constant participates in the test code path. However, the None branch (short buffer) is never tested, and the constant's value of 16 is never directly asserted. This is WEAK coverage, not NONE. Documentation UNDOCUMENTED stands.)

- [ ] `rustguard-core/src/timers.rs` — 1 untested, 10 weak
  Improve `rustguard-core/src/timers.test.rs` covering: now, elapsed_since, elapsed_since, REKEY_AFTER_TIME, REJECT_AFTER_TIME, REKEY_TIMEOUT, REKEY_AFTER_MESSAGES, REJECT_AFTER_MESSAGES, REKEY_ATTEMPT_TIME, DEAD_SESSION_TIMEOUT, SessionTimers
  - now: [USED] Non-exported function called in session_started(), packet_sent(), and packet_received() methods | [UNIQUE] Trivial function returning std::time::Instant::now(). No semantic duplicates identified. | [OK] Correctly delegates to std::time::Instant::now() in std builds. No correctness issues. | [LEAN] One-line wrapper over `std::time::Instant::now()` that exists solely to pair with the cfg-gated `Timestamp` type alias. Without this wrapper the call sites would need their own cfg guards. Minimal and purposeful. | [WEAK] Exercised indirectly via session_started(), packet_sent(), packet_received() in the inline test module. Never directly asserted on. A thin wrapper over std::time::Instant::now(), so low-risk, but coverage is purely incidental. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is clear and it is cfg-gated to std, so tolerance applies, but a one-line note about the cfg guard and return semantics would improve comprehension. (deliberated: confirmed — Tests WEAK is technically correct — now() is never directly asserted on, only exercised indirectly through session_started(), packet_sent(), packet_received(). For a trivial one-line delegation to std::time::Instant::now(), indirect coverage is pragmatically adequate, but the WEAK label is not wrong per methodology. Low-risk finding.)
  - elapsed_since: [USED] Non-exported no_std fallback function compiled conditionally; provides timer abstraction for kernel environments | [DUPLICATE] Identical no_std fallback implementation with RAG score 1.000. Both return Duration::ZERO with identical signature and behavior. | [NEEDS_FIX] Always returns Duration::ZERO in no_std builds, making every time-based check in the impl permanently non-functional: needs_rekey never triggers on age, is_expired never expires a live session by time (security regression — sessions never time out), is_dead always returns false, should_retry_handshake returns false once any handshake is recorded (never retries), and handshake_timed_out never fires. The no_std API correctly exposes timestamp-accepting setters (session_started_at, packet_sent_at, etc.) but the corresponding checker methods have no equivalent time-aware variant and all call this stub, so the caller has no way to drive time-based expiry. | [LEAN] Intentional no-op stub for no_std targets (kernel module). Comment clearly states callers must use the explicit `_at` methods. The zero-return is a deliberate safe fallback, not dead code. | [NONE] no_std variant that always returns Duration::ZERO. No cfg(not(feature='std')) test module exists anywhere in the provided context, and the inline test module does not gate on this cfg. Zero test coverage. | [UNDOCUMENTED] Private no_std-variant has informative `//` inline comments explaining the zero-return fallback, but these are not `///` doc comments and will not appear in rustdoc output. The behavior is important enough to warrant a doc comment given the surprising zero-return semantics. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — This is the no_std variant at L26-30 returning Duration::ZERO. Correction NEEDS_FIX: The checker methods (needs_rekey, is_expired, is_dead, should_retry_handshake, handshake_timed_out) all call elapsed_since but have no _at variants, so time-based expiry is inoperative in no_std. The comment says 'kernel module overrides timer logic' but we cannot verify this and the public API gap is real. Overengineering says LEAN ('intentional safe fallback') which doesn't contradict — intentional design can still have gaps. Keeping NEEDS_FIX but lowering confidence since the kernel module may legitimately override. Duplication reclassified DUPLICATE→UNIQUE: a one-line function returning Duration::ZERO behind a cfg gate is a standard Rust pattern for module-private helpers; extracting a shared pub(crate) utility for this trivial stub adds import complexity exceeding the benefit. Tests field says WEAK but detail explicitly says 'Zero test coverage' for no_std — the mismatch is a merger artifact; keeping WEAK to avoid adding severity.)
  - elapsed_since: [USED] Non-exported no_std fallback function compiled conditionally; provides timer abstraction for kernel environments | [DUPLICATE] Identical no_std fallback implementation with RAG score 1.000. Both return Duration::ZERO with identical signature and behavior. | [NEEDS_FIX] Always returns Duration::ZERO in no_std builds, making every time-based check in the impl permanently non-functional: needs_rekey never triggers on age, is_expired never expires a live session by time (security regression — sessions never time out), is_dead always returns false, should_retry_handshake returns false once any handshake is recorded (never retries), and handshake_timed_out never fires. The no_std API correctly exposes timestamp-accepting setters (session_started_at, packet_sent_at, etc.) but the corresponding checker methods have no equivalent time-aware variant and all call this stub, so the caller has no way to drive time-based expiry. | [LEAN] Intentional no-op stub for no_std targets (kernel module). Comment clearly states callers must use the explicit `_at` methods. The zero-return is a deliberate safe fallback, not dead code. | [NONE] no_std variant that always returns Duration::ZERO. No cfg(not(feature='std')) test module exists anywhere in the provided context, and the inline test module does not gate on this cfg. Zero test coverage. | [UNDOCUMENTED] Private no_std-variant has informative `//` inline comments explaining the zero-return fallback, but these are not `///` doc comments and will not appear in rustdoc output. The behavior is important enough to warrant a doc comment given the surprising zero-return semantics. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — This is the no_std variant at L26-30 returning Duration::ZERO. Correction NEEDS_FIX: The checker methods (needs_rekey, is_expired, is_dead, should_retry_handshake, handshake_timed_out) all call elapsed_since but have no _at variants, so time-based expiry is inoperative in no_std. The comment says 'kernel module overrides timer logic' but we cannot verify this and the public API gap is real. Overengineering says LEAN ('intentional safe fallback') which doesn't contradict — intentional design can still have gaps. Keeping NEEDS_FIX but lowering confidence since the kernel module may legitimately override. Duplication reclassified DUPLICATE→UNIQUE: a one-line function returning Duration::ZERO behind a cfg gate is a standard Rust pattern for module-private helpers; extracting a shared pub(crate) utility for this trivial stub adds import complexity exceeding the benefit. Tests field says WEAK but detail explicitly says 'Zero test coverage' for no_std — the mismatch is a merger artifact; keeping WEAK to avoid adding severity.)
  - REKEY_AFTER_TIME: [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); WireGuard timer constant | [UNIQUE] Module-level constant defining 120-second rekey timeout. Constants are unique by definition. | [OK] 120 seconds matches the WireGuard whitepaper REKEY-AFTER-TIME value exactly. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Consumed by needs_rekey() time branch, but all tests exercise only the message-count path (needs_rekey(REKEY_AFTER_MESSAGES)). The time-based branch guarded by REKEY_AFTER_TIME is never triggered in tests; the constant's numeric value (120 s) is never independently asserted. | [DOCUMENTED] Has a `///` doc comment stating its purpose. Phrasing 'this many seconds' is slightly loose for a Duration constant but the intent is clear and the whitepaper reference in the module-level doc anchors the value. No `# Examples` needed for a bare constant. (deliberated: confirmed — Tests WEAK confirmed: all tests exercise only the message-count path in needs_rekey. The time-based branch comparing against REKEY_AFTER_TIME is never triggered. For a security protocol constant, boundary testing of the time path would be valuable.)
  - REJECT_AFTER_TIME: [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); WireGuard timer constant | [UNIQUE] Module-level constant defining 180-second rejection timeout. No duplicates expected. | [OK] 180 seconds matches the WireGuard whitepaper REJECT-AFTER-TIME value exactly. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Consumed by is_expired() time branch. message_count_triggers_reject covers only the message-count path; the elapsed-time path using REJECT_AFTER_TIME is never exercised. Value (180 s) never asserted. | [DOCUMENTED] Has a clear `///` doc comment — 'Reject data using a keypair older than this.' — that accurately describes both the trigger condition and the action. Sufficient for a constant. (deliberated: confirmed — Tests WEAK confirmed: is_expired time branch using REJECT_AFTER_TIME is never exercised; only the message-count path is tested. Same systemic gap as REKEY_AFTER_TIME.)
  - REKEY_TIMEOUT: [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); handshake retry timeout | [UNIQUE] Module-level constant defining 5-second rekey timeout. Unique by definition. | [NEEDS_FIX] The doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but REKEY_TIMEOUT is actually the inter-attempt retry delay (5 s between handshake retries), as confirmed by its use in should_retry_handshake. The description matches no field or usage in this file and directly contradicts the constant's semantics. For a security-critical protocol constant, this misdescription constitutes a correctness hazard for callers relying on the doc. The numeric value (5 s) is correct per the WireGuard spec. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Used in should_retry_handshake(). retry_handshake_when_never_sent only tests the None branch (no handshake ever sent). The time-elapsed branch that compares against REKEY_TIMEOUT is completely untested. Value (5 s) never verified. | [PARTIAL] A `///` doc comment is present, but the description 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' does not match the 5-second value — REJECT_AFTER_TIME is 180 s. Actual usage in `should_retry_handshake` shows this is a handshake-retry interval, not a send-rejection threshold. The description appears to be a copy-paste error, making it actively misleading. (deliberated: confirmed — Correction NEEDS_FIX confirmed with increased confidence: the doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but the constant is 5 seconds, used in should_retry_handshake as a retry interval. REJECT_AFTER_TIME is 180s — the description is clearly wrong and actively misleading for a security-critical protocol constant. The numeric value (5s) is correct per WireGuard spec. Documentation PARTIAL confirmed: a doc comment exists but its content is factually incorrect, making it worse than undocumented. Both findings are coherent — the PARTIAL documentation and NEEDS_FIX correction describe the same underlying issue (wrong doc comment).)
  - REKEY_AFTER_MESSAGES: [USED] Exported constant with 1 runtime importer; also used locally in needs_rekey() and tests | [UNIQUE] Module-level constant for message count rekey threshold. Unique constant definition. | [NEEDS_FIX] Value is (2^60 - 1) rather than 2^60 as specified in the WireGuard whitepaper. The off-by-one means rekeying is triggered one message earlier than the protocol mandates. This is conservative rather than dangerous, but it is an undocumented deviation from the spec and could cause interoperability or compliance issues. | [LEAN] Bit-shift expression `(1u64 << 60) - 1` directly encodes the WireGuard spec value in a self-documenting way. The expression is clearer than a raw decimal literal. | [WEAK] Directly used as the threshold argument in message_count_triggers_rekey, confirming at-boundary behavior. However, the just-below-threshold case (REKEY_AFTER_MESSAGES - 1) is not tested, the rekey_requested=true short-circuit path is not tested, and the actual bit-pattern value ((1u64 << 60) - 1) is never independently asserted. | [DOCUMENTED] Has a clear `///` doc comment — 'Maximum number of messages before rekeying.' — that accurately captures the purpose of the threshold constant. (deliberated: confirmed — Correction NEEDS_FIX kept: (1u64 << 60) - 1 vs spec's 2^60 is an undocumented conservative deviation. The off-by-one is harmless (rekeys one message early) but the Go reference implementation uses exactly (1 << 60). Action 5 (fix or document) is proportionate. Tests WEAK confirmed: boundary value tested at REKEY_AFTER_MESSAGES but below-threshold case (REKEY_AFTER_MESSAGES - 1) untested, rekey_requested=true short-circuit untested.)
  - REJECT_AFTER_MESSAGES: [USED] Exported constant with 1 runtime importer; also used locally in is_expired() and tests | [UNIQUE] Module-level constant for message count rejection threshold. Unique constant definition. | [OK] u64::MAX - (1 << 13) = 2^64 - 1 - 2^13 = 2^64 - 2^13 - 1, which matches the WireGuard spec REJECT-AFTER-MESSAGES value. | [LEAN] `u64::MAX - (1 << 13)` mirrors the WireGuard spec value exactly. Same reasoning as REKEY_AFTER_MESSAGES — expression form is clearer than a magic number. | [WEAK] Directly used in message_count_triggers_reject at exact boundary. Below-threshold case not tested. Actual value (u64::MAX - (1 << 13)) never independently asserted. Only one scenario exercised. | [DOCUMENTED] Has a clear `///` doc comment — 'Reject after this many messages even if time hasn't expired.' — correctly describing both the trigger and the independence from the time-based threshold. (deliberated: confirmed — Tests WEAK confirmed: boundary test at exact REJECT_AFTER_MESSAGES value exists, but below-threshold case not tested and actual numeric value never independently asserted.)
  - DEAD_SESSION_TIMEOUT: [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); session expiry timeout | [UNIQUE] Module-level constant defining 240-second dead session timeout. Unique constant definition. | [OK] 240 seconds is larger than REJECT_AFTER_TIME (180 s), which is the correct ordering: a session becomes unusable for sending before its keys are zeroed. Not a named constant in the WireGuard whitepaper but the relative ordering is correct. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Used in is_dead(). new_timers_not_expired calls is_dead() but only for the case where session_established is None, which returns false immediately without ever consulting DEAD_SESSION_TIMEOUT. The elapsed-time comparison branch is never reached in any test. | [DOCUMENTED] Has a clear `///` doc comment — 'Time after which we zero session keys if no new handshake.' — accurately capturing the zeroing-on-expiry semantics used in `is_dead`. (deliberated: confirmed — Tests WEAK confirmed: new_timers_not_expired calls is_dead() but only with session_established=None, which returns false immediately without consulting DEAD_SESSION_TIMEOUT. The elapsed-time comparison branch is never reached.)
  - SessionTimers: [USED] Exported struct with 1 runtime importer (rustguard-daemon/src/peer.rs); core session lifecycle timer management | [UNIQUE] Public struct defining timer state for peer session lifecycle. No semantic duplicates identified in RAG results. | [NEEDS_FIX] Two bugs in the impl block: (1) needs_keepalive (L160): when last_sent is None, sent.unwrap_or(received) sets last_send_time = received, making the compound condition 'elapsed_since(received) >= interval AND elapsed_since(received) < interval' a logical contradiction that always evaluates to false. Keepalives can never fire on a session where the local side has not yet sent any data packet, silently breaking persistent-keepalive for passive peers. (2) session_started / session_started_at (L80, L89) do not reset last_sent to None; stale send timestamps from the prior session persist into the new session, further suppressing keepalive generation immediately after rekeying. | [ACCEPTABLE] The struct and its logic methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) are minimal and map 1-to-1 to WireGuard spec concepts. The only added complexity is the dual std/no_std mutation API (session_started/session_started_at, packet_sent/packet_sent_at, packet_received/packet_received_at), which doubles mutation entry points. This is justified: `rustguard-kmod` is a real kernel module target confirmed by the project structure, making no_std support a hard requirement rather than premature generalization. | [WEAK] Several methods have reasonable entry-level tests (new, session_started, needs_rekey message-count path, is_expired message-count/no-session paths, basic keepalive/retry guards). However: handshake_timed_out() has no test at all; needs_keepalive() positive case (interval elapsed, data received recently) is untested; packet_sent() is never exercised in any test; all time-based expiry branches in needs_rekey/is_expired/is_dead/should_retry_handshake are untested; all no_std variants (session_started_at, packet_sent_at, packet_received_at) have no tests. | [DOCUMENTED] The struct carries a `///` doc comment on the type and every public field has its own `///` doc comment. This satisfies the struct-documentation rule. Note that the associated `new()` constructor in the impl block is public but lacks any doc comment, which is a gap not captured in the struct definition range itself. (deliberated: confirmed — Correction NEEDS_FIX confirmed with high confidence. Bug (1) verified: in needs_keepalive when last_sent is None, sent.unwrap_or(received) sets last_send_time=received, creating the contradictory condition 'elapsed_since(received) >= interval AND elapsed_since(received) < interval' which is always false. Passive peers that receive data but never send can never trigger keepalives — a real functional bug. Bug (2) verified: session_started/session_started_at don't reset last_sent, so stale timestamps carry over across rekeys. Both bugs are coherent with each other and compound the keepalive suppression. Overengineering ACCEPTABLE confirmed: dual std/no_std API is justified by the real kernel module target (rustguard-kmod). Tests WEAK confirmed: handshake_timed_out has no test, keepalive positive case untested, all time-based branches untested, no_std variants untested, packet_sent never exercised.)
