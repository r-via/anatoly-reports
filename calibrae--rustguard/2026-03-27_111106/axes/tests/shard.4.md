[← Back to Tests](./index.md) · [← Back to report](../public_report.md)

# Tests — Shard 4

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-tun/src/lib.rs` | NEEDS_REFACTOR | 1 | 88% | [details](../reviews/rustguard-tun-src-lib.rev.md) |
| `rustguard-crypto/src/tai64n.rs` | NEEDS_REFACTOR | 2 | 87% | [details](../reviews/rustguard-crypto-src-tai64n.rev.md) |
| `rustguard-enroll/src/pool.rs` | NEEDS_REFACTOR | 1 | 90% | [details](../reviews/rustguard-enroll-src-pool.rev.md) |
| `rustguard-daemon/src/peer.rs` | NEEDS_REFACTOR | 1 | 86% | [details](../reviews/rustguard-daemon-src-peer.rev.md) |
| `rustguard-core/src/session.rs` | NEEDS_REFACTOR | 1 | 85% | [details](../reviews/rustguard-core-src-session.rev.md) |

## Symbol Details

### `rustguard-tun/src/lib.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Tun` | L25–L30 | NONE | 78% | [USED] Public struct in library crate with documented public API methods (cre... |

### `rustguard-crypto/src/tai64n.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Tai64n` | L9–L9 | WEAK | 87% | [USED] Exported struct with 1 runtime importer (rustguard-crypto/src/lib.rs) ... |
| `TAI64_EPOCH_OFFSET` | L12–L12 | WEAK | 82% | [USED] Non-exported constant directly used in from_unix() method at line 34 f... |

### `rustguard-enroll/src/pool.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `IpPool` | L9–L20 | WEAK | 90% | [USED] Exported struct with 1 runtime importer (rustguard-enroll/src/server.r... |

### `rustguard-daemon/src/peer.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Peer` | L16–L26 | NONE | 86% | [USED] Exported struct with 1 runtime importer (rustguard-daemon/src/tunnel.r... |

### `rustguard-core/src/session.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `TransportSession` | L13–L30 | WEAK | 85% | [USED] Exported struct with 3 runtime importers (handshake.rs, peer.rs, clien... |

## Test Improvements

- [ ] `rustguard-tun/src/lib.rs` — 1 untested
  Improve `rustguard-tun/src/lib.test.rs` covering: Tun

- [ ] `rustguard-crypto/src/tai64n.rs` — 2 weak
  Improve `rustguard-crypto/src/tai64n.test.rs` covering: Tai64n, TAI64_EPOCH_OFFSET
  - Tai64n: [USED] Exported struct with 1 runtime importer (rustguard-crypto/src/lib.rs) per pre-computed analysis. | [UNIQUE] No RAG similarity found; single struct definition implementing TAI64N timestamp for WireGuard | [NEEDS_FIX] Two correctness issues in the impl block. (1) `from_unix` at L32 computes `secs + TAI64_EPOCH_OFFSET` using plain `+`, which wraps silently in release builds or panics in debug builds for any `secs` value above ~13.8e18 — not a concern for realistic Unix timestamps, but the public API accepts arbitrary u64. `saturating_add` or an explicit overflow check is the correct fix. (2) `from_bytes` at L43-L44 accepts any 12-byte payload without validating that the nanoseconds field (bytes 8–11) is ≤ 999_999_999. The doc-comment on `from_unix` explicitly names the 999_999_999 clamp as the invariant that WireGuard's byte-wise ordering relies on; a deserialized timestamp with nanos > 999_999_999 would byte-compare as strictly greater than a legitimately-created timestamp with the same second and nanos = 999_999_999, silently breaking replay-protection ordering. | [LEAN] Minimal newtype wrapper over [u8; 12] with exactly the API surface WireGuard replay-prevention requires: construction from system time or raw unix values, byte serialization, and a strict ordering predicate. No unnecessary generics, no trait hierarchies, no configuration. The `#[cfg(feature = "std")]` gating on `now()` is idiomatic and justified for no_std (kernel) targets. Byte-wise Ord derived from the big-endian encoding is a clean protocol-level trick, not complexity. | [WEAK] Inline tests cover monotonic ordering via now()+is_after(), and roundtrip via as_bytes()/from_bytes() for both now() and from_unix(). However, the documented nanos-clamping invariant (nanos > 999_999_999 → 999_999_999) — explicitly called out as critical for WireGuard byte-wise ordering — is never tested. The false case of is_after() is absent. No test verifies the actual byte-level output against expected WireGuard-compatible TAI64N values, so protocol correctness is unconfirmed. | [PARTIAL] The struct carries four lines of `///` commentary (lines 4–8) that explain its purpose (replay prevention), binary layout (8 B seconds + 4 B nanoseconds), and WireGuard semantics. However, no `# Examples` section is present on this public type, which is expected by Rust doc conventions for public API structs. Individual methods have their own docs, but the type-level entry point lacks a usage example showing how to construct and compare timestamps. (deliberated: confirmed — Correction NEEDS_FIX confirmed for both issues. (1) Overflow in `secs + TAI64_EPOCH_OFFSET` at L34 is low-severity but valid — in a crypto crate with a public API, defensive arithmetic is warranted; saturating_add is a trivial fix. (2) `from_bytes` lacking nanos validation is the more important issue: the crate explicitly documents the nanos ≤ 999_999_999 invariant as critical for WireGuard byte-wise ordering, yet `from_bytes` (which deserializes untrusted wire data) does not enforce it, creating a real attack surface for replay-protection bypass. Tests WEAK confirmed: the three tests cover happy-path monotonicity, roundtrip, and from_unix roundtrip, but miss nanos-clamping verification (e.g. from_unix with nanos > 999_999_999), the false case of is_after, and known-good byte comparison against WireGuard reference vectors — all meaningful gaps for a crypto primitive. Documentation PARTIAL confirmed: struct-level docs are good but `as_bytes` and `from_bytes` lack any `///` comments, and no `# Examples` section exists on the public type.)
  - TAI64_EPOCH_OFFSET: [USED] Non-exported constant directly used in from_unix() method at line 34 for TAI conversion calculation. | [UNIQUE] No RAG similarity found; single const defining TAI64 epoch offset (TAI ahead of UTC + 2^62) | [OK] The value 0x4000_0000_0000_000a equals 2^62 + 10, which exactly matches the WireGuard reference implementations (wireguard-go, wireguard-tools). The +10 represents the TAI-UTC offset fixed at the POSIX epoch (1970-01-01); WireGuard intentionally does not track accumulated leap seconds beyond that, using the timestamp purely for monotonic replay protection. The comment accurately describes this. | [LEAN] Single named constant encoding the TAI64 epoch offset (2^62 + 10) as mandated by the TAI64 specification and WireGuard's wire format. Naming it instead of inlining the magic value is the right choice for readability and auditability in a crypto crate. No overengineering possible at this level. | [WEAK] The constant is exercised implicitly via from_unix() in from_unix_roundtrip, but no test ever asserts the actual numeric byte output produced by from_unix against a known-good WireGuard TAI64N byte sequence. The specific value 0x4000_0000_0000_000a is critical for protocol interoperability and its correctness is never directly validated. | [DOCUMENTED] Private constant with a `///` comment on line 11 that decodes the otherwise opaque hex literal `0x4000_0000_0000_000a` into its two components: the 2^62 TAI64 epoch bias and the 10-second TAI-UTC offset at the 1970 epoch. For a private constant this is fully adequate; `# Examples` are not expected, and the derivation is explicitly stated. (deliberated: confirmed — Tests WEAK is fair but borderline. The constant is private and exercised through `from_unix` in from_unix_roundtrip, but no test asserts the actual byte output of `from_unix(0, 0)` or `from_unix(known_epoch, known_nanos)` against a WireGuard reference vector. For a crypto/protocol constant where interoperability depends on the exact value, a direct assertion would be valuable. However, since the constant is private and its correctness is verifiable by inspection against the well-known TAI64 spec value, the risk is lower than for a complex computation. Keeping WEAK at slightly reduced confidence. All other axes are clean and confirmed.)

- [ ] `rustguard-enroll/src/pool.rs` — 1 weak
  Improve `rustguard-enroll/src/pool.test.rs` covering: IpPool
  - IpPool: [USED] Exported struct with 1 runtime importer (rustguard-enroll/src/server.rs). Core public API for CIDR range IP pool management. | [UNIQUE] No RAG semantic similarity data available. IP pool management struct with no identified duplicates in the codebase. | [OK] Struct definition and all impl methods are logically correct. The `capacity` field stores `!mask` (e.g., 255 for /24), which equals the broadcast offset and is used as the exclusive upper bound in `allocate`'s loop (`2..self.capacity`). This correctly excludes both the network address (offset 0), the server address (offset 1 pre-inserted into `assigned`), and the broadcast address (offset == capacity). Verified for /24 (2..255 → .2–.254), /30 (2..3 → .2 only), and /29 (2..7 → .2–.6), all matching the test assertions. The `release` method correctly guards the server address. The `contains` method correctly handles the /0 edge case and uses safe shift arithmetic for prefix_len 1–30 (shift amounts 2–31 are all valid for u32). The `new` guard (`prefix_len > 30`) prevents shifts of 0 or 1 that would make capacity too small to yield any client address. No overflow is possible in `network + offset` given that the maximum network base for any valid CIDR fits within u32 and the maximum offset is always less than the broadcast. The field-level doc comment 'Number of usable host addresses' is misleading (the value is actually `2^(32−prefix_len)−1`, one less than the total range, not the usable count), but this does not cause any incorrect behavior. | [LEAN] Struct carries exactly the fields needed: `network` and `prefix_len` define the CIDR, `capacity` is a cheap precomputed constant (avoids recomputing `!mask` on every `allocate` call), `assigned` is a `HashSet` giving O(1) membership checks, and `server_addr` is cached to guard the reservation in `release`. No unnecessary generics, no trait abstraction, no factory indirection. The design is minimal and well-matched to its single purpose. | [WEAK] Inline #[cfg(test)] module covers the main happy paths well: basic_allocation, release_and_reuse, pool_exhaustion, contains_check, and server_addr_not_released. However, several gaps remain: (1) IpPool::new() returning None for prefix_len > 30 is never tested; (2) allocate_specific() has zero test coverage despite being a distinct public method used for restoring persisted peers; (3) contains() with a /0 prefix (the special-case 'matches everything' branch) is untested; (4) assigned_count() is never exercised in any assertion. The tested paths are realistic but incomplete relative to the public surface area. | [PARTIAL] The struct itself has no `///` doc comment directly on the `pub struct IpPool` declaration. All five fields carry individual `///` comments with examples (e.g. '10.150.0.0'), which is good. The module-level `//!` block describes the pool's purpose and the .1 reservation, but module-level inner docs do not substitute for struct-level `///` docs. Additionally, there is no `# Examples` section on the type, which is expected for a public API struct in a library crate. (deliberated: confirmed — Tests: WEAK is accurate. Verified against the inline test module: `new()` with prefix_len > 30 (None path) is never tested, `allocate_specific()` has zero coverage, `contains()` with /0 is untested, and `assigned_count()` is never asserted. The five existing tests cover the core happy paths well but leave meaningful gaps in the public API surface. Documentation: PARTIAL is accurate. The `pub struct IpPool` declaration at line 9 has no `///` doc comment; only the module-level `//!` block (lines 1-4) describes the pool. All fields and public methods do have `///` comments, which is good, but rustdoc consumers browsing the struct type directly will see no summary. Both findings are low-severity hygiene issues that do not affect correctness.)

- [ ] `rustguard-daemon/src/peer.rs` — 1 untested
  Create `rustguard-daemon/src/peer.test.rs` covering: Peer

- [ ] `rustguard-core/src/session.rs` — 1 weak
  Improve `rustguard-core/src/session.test.rs` covering: TransportSession
  - TransportSession: [USED] Exported struct with 3 runtime importers (handshake.rs, peer.rs, client.rs). Core cryptographic session abstraction managing encryption/decryption keys, nonce counters, and anti-replay windows for secure transport. | [UNIQUE] No RAG similarity data available. Struct represents a unique transport session with cryptographic state, nonce counter, and anti-replay window management for secure packet encryption/decryption. | [OK] The struct definition is correct. Cryptographic key fields (key_send, key_recv) are properly subject to Zeroize/ZeroizeOnDrop while non-sensitive metadata fields (our_index, their_index, send_counter, recv_window) are correctly annotated with #[zeroize(skip)]. Field types and visibility are appropriate. Note: the requested symbol name 'export class TransportSession' is TypeScript syntax and does not exist in this Rust file; evaluating the actual Rust symbol 'TransportSession' at the specified line range. | [LEAN] The struct carries exactly the state required for a WireGuard-style bidirectional transport session: dual keys (send/recv) for asymmetric keying, a monotonic nonce counter, and an anti-replay window. `Zeroize`/`ZeroizeOnDrop` are mandatory for key material in cryptographic code, and `#[zeroize(skip)]` is correctly applied to non-sensitive fields. No unnecessary generics, traits, or abstraction layers are present. Field count and visibility are appropriate to the protocol domain. | [WEAK] The inline #[cfg(test)] module provides solid coverage for the allocating variants: encrypt/decrypt round-trip, counter increment, wrong-counter rejection, anti-replay window, out-of-order delivery, and bidirectional flow are all exercised. However, the two zero-alloc methods encrypt_to and decrypt_in_place — likely the hot-path used by rustguard-daemon/src/peer.rs and rustguard-enroll/src/client.rs — have zero test coverage. No test verifies buffer-too-small behavior for encrypt_to (the 'out must be at least plaintext+16' contract), nor does any test exercise decrypt_in_place's replay-window or AEAD failure paths. The test file prompt says 'No test file found' but inline tests exist and are counted; coverage is WEAK due to the untested zero-alloc surface. | [DOCUMENTED] The struct carries a thorough /// block (lines 6–11) that explains its purpose, the initiator/responder key-direction convention, and the two-key model. Both public fields (our_index, their_index) have /// comments describing their roles, and all private fields are also individually annotated. No # Examples section is present, but the Rust doc-comment rules only require examples on public functions/methods, not on struct declarations, so the omission does not demote the rating. (deliberated: confirmed — Tests WEAK is justified: the inline #[cfg(test)] module provides 6 solid tests covering encrypt/decrypt round-trip, counter increment, wrong-counter rejection, replay rejection, out-of-order acceptance, and bidirectional flow — but the zero-alloc hot-path methods encrypt_to and decrypt_in_place have zero coverage. No test exercises buffer-too-small behavior for encrypt_to or decrypt_in_place's replay/AEAD failure paths. All other axes are clean and well-supported: correction OK (proper Zeroize/ZeroizeOnDrop with correct skip annotations, checked_add for nonce exhaustion), utility USED (3 confirmed importers), duplication UNIQUE (no similar session abstractions), overengineering LEAN (minimal fields precisely matching WireGuard transport state), documentation DOCUMENTED (thorough doc comments on struct and all methods except new/send_counter which are self-evident). Confidence raised to 85 after holistic cross-axis verification confirms coherence.)
