[← Back to Documentation](./index.md) · [← Back to report](../../public_report.md)

# Documentation — Shard 1

## Findings

| File | Verdict | Documentation | Conf. | Details |
|------|---------|---------------|-------|---------|
| `rustguard-kmod/src/lib.rs` | CRITICAL | 13 | 90% | [details](#rustguard-kmodsrclibrs) |
| `rustguard-tun/src/xdp.rs` | CRITICAL | 5 | 95% | [details](#rustguard-tunsrcxdprs) |
| `rustguard-enroll/src/server.rs` | CRITICAL | 3 | 92% | [details](#rustguard-enrollsrcserverrs) |
| `rustguard-kmod/src/replay.rs` | CRITICAL | 1 | 90% | [details](#rustguard-kmodsrcreplayrs) |
| `rustguard-kmod/src/noise.rs` | NEEDS_REFACTOR | 19 | 92% | [details](#rustguard-kmodsrcnoisers) |
| `rustguard-core/src/handshake.rs` | NEEDS_REFACTOR | 14 | 88% | [details](#rustguard-coresrchandshakers) |
| `rustguard-core/src/cookie.rs` | NEEDS_REFACTOR | 5 | 92% | [details](#rustguard-coresrccookiers) |
| `rustguard-daemon/src/tunnel.rs` | NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-daemonsrctunnelrs) |
| `rustguard-core/src/messages.rs` | NEEDS_REFACTOR | 12 | 90% | [details](#rustguard-coresrcmessagesrs) |
| `rustguard-core/src/timers.rs` | NEEDS_REFACTOR | 1 | 90% | [details](#rustguard-coresrctimersrs) |

## Symbol Details

### `rustguard-kmod/src/lib.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `AEAD_TAG_SIZE` | L111–L111 | PARTIAL | 82% | [USED] Exported constant used internally for tag size calculations in encryption/decryption and buffer management (L395, L433, L680, L730, L895, L907) | [UNIQUE] Exported constant, no duplicate found. Standard AEAD tag size (16 bytes). | [OK] 16 bytes is the correct authentication tag length for ChaCha20-Poly1305. | [LEAN] Simple named constant for AEAD authentication tag size. Appropriate and minimal. | [GOOD] Plain numeric constant (pub const) with no runtime behavior; rule 6 applies — GOOD by default. | [PARTIAL] Public constant has `/// AEAD authentication tag size.` but omits which AEAD scheme (ChaCha20-Poly1305), the unit (bytes), and has no `# Examples` section expected for public API items. (deliberated: confirmed — PARTIAL documentation confirmed: public constant documents 'AEAD authentication tag size' but omits the AEAD scheme (ChaCha20-Poly1305) and unit (bytes). Acceptable for a well-named constant but technically incomplete for a public API item.) |
| `rustguard_xmit` | L382–L384 | PARTIAL | 70% | [USED] Exported C FFI callback (#[no_mangle]) for TX path; kernel calls this for outbound packets; no Rust importers but essential for kernel module operation | [UNIQUE] Trivial FFI wrapper (2 lines). Delegates to do_xmit. No duplicate found. | [OK] Thin #[no_mangle] extern wrapper; all logic is in do_xmit. | [LEAN] Thin #[no_mangle] FFI shim that delegates to do_xmit. The split between safe extern wrapper and unsafe inner function is the standard Rust kernel pattern. | [NONE] Exported C callback for TX path; thin wrapper over do_xmit. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has `/// TX callback: encrypt plaintext and send as WireGuard transport packet.` but lacks parameter descriptions for `skb` and `priv_`, return value semantics, error conditions, and `# Examples`. (deliberated: confirmed — NONE tests confirmed: FFI callback with no test coverage. PARTIAL docs confirmed: has doc comment but lacks parameter descriptions for skb/priv_ and return semantics.) |
| `rustguard_rx` | L465–L467 | PARTIAL | 70% | [USED] Exported C FFI callback (#[no_mangle]) for RX path; kernel calls this for incoming packets; no Rust importers but essential for kernel module operation | [UNIQUE] Trivial FFI wrapper (2 lines). Delegates to do_rx. No duplicate found. | [OK] Thin #[no_mangle] extern wrapper; all logic is in do_rx. | [LEAN] Thin #[no_mangle] FFI shim delegating to do_rx. Minimal and idiomatic. | [NONE] Exported C callback for RX path; thin wrapper over do_rx. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has two `///` lines mentioning message types handled and byte-order of src_ip/src_port, but lacks per-parameter descriptions for `skb` and `priv_`, return value semantics, and `# Examples`. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: FFI callback has doc comment mentioning message types and byte order but lacks per-parameter descriptions.) |
| `handle_initiation` | L521–L620 | PARTIAL | 82% | [USED] Handshake handler called from do_rx for MSG_INITIATION (L495); processes responder-side Noise_IK, manages peer session setup | [UNIQUE] RX state wrapper for handshake initiation (responder role). High structural similarity to process_initiation (RAG: 0.717) but different semantic contract: wraps cryptographic processing with DoS protection, peer state management, and endpoint updates. Not interchangeable — handles one role (responder), calls noise::process_initiation for crypto. Different signatures, different responsibilities. | [OK] TAI64N timestamp comparison uses lexicographic ordering on [u8;12], which is semantically correct for big-endian timestamps. Since INITIATION_SIZE==148 (standard WireGuard layout), the outer pkt_len check ensures pkt_len>=148 before the cookie under_load block, making the inner pkt_len>=148 guard always true. Session rotation and index_map registration are consistent with handle_response. | [LEAN] Handles a full Noise_IK initiation message including DoS cookie check, timestamp replay protection, peer lookup, session rotation, and index_map registration. Each step is mandated by the WireGuard specification. Length is due to protocol complexity, not abstraction overhead. | [NONE] Responder-side handshake: DoS cookie check, process_initiation, timestamp replay protection, session install. Security-critical with many branches; no test file exists for rustguard-kmod. | [PARTIAL] Private unsafe function with two `///` lines stating role (responder) and safety precondition (socket lock). Missing: parameter descriptions, return semantics, description of side effects (session install, index_map update, timestamp update, cookie reply path). (deliberated: confirmed — NONE tests confirmed: security-critical responder handshake handler with no test coverage. PARTIAL docs confirmed: two /// lines state role and safety precondition but omit parameter descriptions and side effects (session install, index_map update, timestamp update, cookie reply path).) |
| `handle_response` | L624–L671 | PARTIAL | 82% | [USED] Handshake handler called from do_rx for MSG_RESPONSE (L499); processes initiator-side Noise response, completes session establishment | [UNIQUE] RX state wrapper for handshake response (initiator role). Similar to process_response (RAG: 0.716) but different contract: wraps cryptographic processing with peer lookup, session rotation, and timer management. Not interchangeable — manages state after receiving response, calls noise::process_response for crypto. Different signatures and purposes. | [OK] receiver_index at bytes 8..12 is correct for the WireGuard Noise_IK response message layout. Matching by pending_handshake.sender_index == resp_receiver is the right lookup strategy. Session rotation and index_map registration mirror handle_initiation correctly. | [LEAN] Handles handshake response as initiator: finds pending handshake by sender_index, processes response, installs session. Straightforward protocol implementation with no unnecessary indirection. | [NONE] Initiator-side response processing: receiver_index matching, process_response, session install. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Private unsafe function with two `///` lines stating role (initiator) and safety precondition. Missing: parameter descriptions, side effects (session rotation, index_map update, endpoint update), and failure conditions. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: similar to handle_initiation — has brief two-line doc comment but lacks parameter descriptions and side-effect documentation (session rotation, index_map, endpoint update).) |
| `handle_transport` | L676–L755 | PARTIAL | 80% | [USED] Transport handler called from do_rx for MSG_TRANSPORT (L507); decrypts packets with replay protection, injects plaintext into kernel stack | [UNIQUE] RX transport packet handler: decrypts with replay protection, injects into network stack. No similar transport handler found. | [NEEDS_FIX] The stack decrypt buffer is hard-coded to 2048 bytes and packets with encrypted payload >2048 bytes are silently dropped. Maximum usable plaintext is 2032 bytes (2048 - AEAD_TAG_SIZE). Standard Ethernet MTU (1500) fits, but the net_device MTU can be configured larger (jumbo frames up to 9000+ bytes). Packets exceeding 2032 bytes of plaintext are silently discarded, causing unexplained connectivity failures without any error counter or log entry. | [LEAN] Decrypts transport packets with replay protection and dual-session fallback (current + previous session for in-flight rekeying). The stack buffer approach (2048 bytes) is a deliberate no_std constraint. All complexity is protocol-driven. | [NONE] Transport decryption with replay window check, fallback to prev_session, plaintext injection into network stack. Security-critical; no test file exists for rustguard-kmod. | [PARTIAL] Private unsafe function has three `///` lines covering purpose, safety, and replay_window exclusivity. Better than sibling handlers but still missing parameter descriptions, decrypt-fallback-to-prev_session behavior, and keepalive/endpoint-roaming handling. (deliberated: confirmed — NEEDS_FIX confirmed: L708 hardcodes 2048-byte stack buffer and L710 silently drops packets with encrypted_len > 2048. Max usable plaintext is 2032 bytes, insufficient for jumbo frames (9000+). Silent discard with no error counter or log is the key issue. NONE tests and PARTIAL docs confirmed.) |
| `handle_cookie_reply` | L758–L781 | PARTIAL | 80% | [USED] Cookie handler called from do_rx for type 3 (L504); processes MAC2 cookie from peer, stores for retry on initiator side | [UNIQUE] Cookie reply (type 3) message handler. Stores cookie state for MAC2 validation. No similar handler found. | [NEEDS_FIX] our_mac1 is hardcoded to [0u8;16] (acknowledged TODO). Cookie reply authentication uses HMAC-keyed over the initiator's MAC1; passing all-zeros means any crafted reply is accepted and stored. The subsequent retry with MAC2 will compute its MAC2 over the stored-but-wrong cookie, failing server-side MAC2 verification. DoS protection is non-functional as a result. | [LEAN] Minimal cookie reply handler. The TODO comment about MAC1 storage acknowledges a known limitation rather than papering over it with abstraction. | [NONE] Cookie reply handler for MAC2 DoS protection; stores received cookie for retry. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Private unsafe function has one `///` line stating purpose. Missing: parameter descriptions, the known limitation (TODO: store last sent MAC1 per peer documented only inline), and return semantics. (deliberated: confirmed — NEEDS_FIX confirmed: L777 uses [0u8;16] for our_mac1 with an acknowledged TODO. Without correct MAC1, cookie reply HMAC verification is bypassed and any crafted reply is accepted. Subsequent MAC2 retry will compute over the wrong cookie, failing server-side. DoS protection is non-functional. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_dev_uninit` | L785–L785 | PARTIAL | 90% | [LOW_VALUE] Exported C FFI callback for device teardown; completely empty stub (no-op); part of kernel interface but provides no functional value | [UNIQUE] Trivial FFI stub (empty, 1 line). No duplicate found. | [OK] Empty stub; no logic to evaluate. | [LEAN] Stub FFI callback required by the C net_device interface. No implementation needed at this stage; presence is mandatory for ABI compatibility. | [NONE] Exported no-op stub. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has `/// Device teardown callback.` — minimal description for a public symbol. No parameter docs, no description of when it is called, and the stub nature is undocumented. (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: this is a #[no_mangle] extern-C callback required by the net_device operations table ABI. Removing it would break kernel module registration. An empty stub is the correct implementation — net_device ops require all callbacks to be present. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_timer_tick` | L789–L791 | PARTIAL | 70% | [USED] Exported C FFI callback invoked periodically (250ms) by wg_timer_start; calls do_timer_tick to handle rekeying, keepalives, timers | [UNIQUE] Trivial FFI wrapper (2 lines). Delegates to do_timer_tick. No duplicate found. | [OK] Thin #[no_mangle] extern wrapper delegating to do_timer_tick. | [LEAN] Thin #[no_mangle] FFI shim delegating to do_timer_tick. Standard pattern. | [NONE] Exported C timer callback; thin wrapper over do_timer_tick. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has `/// Periodic timer callback (every 250ms) — check rekeying and keepalives.` Mentions periodicity and actions but lacks parameter description for `priv_` and does not list side effects or error behavior. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: FFI timer callback has doc comment mentioning 250ms periodicity and rekeying/keepalive actions but lacks parameter description for priv_.) |
| `initiate_rekey` | L847–L877 | PARTIAL | 80% | [USED] Called from do_timer_tick for rekey trigger (L829, L835); creates handshake initiation, sends it via UDP socket | [UNIQUE] Initiates handshake rekey for a specific peer. Called from do_timer_tick and elsewhere. No similar rekey function found. | [OK] Standalone logic is correct: generates a random sender_index, calls create_initiation with the correct keys, sends the message, and stores pending_handshake. UB arises only at call sites in do_timer_tick where aliased &mut Peer references are created. | [LEAN] Focused helper that sends a handshake initiation and updates pending state. Single responsibility, appropriately extracted from do_timer_tick. | [NONE] Rekey handshake initiation: generates sender_index, calls create_initiation, sends init message, stores pending state. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Private unsafe function has `/// Initiate a rekey handshake for a peer.` — one terse line. Missing: parameter descriptions, preconditions (peer must have non-zero public key), and side effects (pending_handshake set, timer updated). (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: has single-line /// doc comment but lacks parameter descriptions, preconditions (peer must have non-zero public key), and side effects (pending_handshake set, timer updated).) |
| `send_keepalive` | L880–L914 | PARTIAL | 80% | [USED] Called from do_timer_tick (L841) when keepalive needed; sends empty transport packet to refresh NAT, update timers | [UNIQUE] Sends empty transport packet to keep session alive. Builds header, encrypts tag, sends. No similar keepalive function found. | [OK] Correctly constructs a WireGuard keepalive: atomically fetches counter, writes 16-byte transport header, encrypts zero-length plaintext producing a 16-byte tag at buf[16..32], and sends the 32-byte packet. Standalone logic is correct; call-site aliasing from do_timer_tick is flagged separately. | [LEAN] Constructs and sends an empty WireGuard transport packet for keepalive. Minimal and protocol-correct. | [NONE] Sends empty AEAD-encrypted transport packet to maintain session liveness. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Private unsafe function has `/// Send an empty keepalive transport packet.` — one terse line. Missing: parameter descriptions, precondition that an active session must exist, and the zero-length plaintext AEAD encoding detail. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: has single-line /// doc comment but lacks parameter descriptions and precondition that an active session must exist.) |
| `rustguard_genl_get` | L918–L922 | PARTIAL | 88% | [LOW_VALUE] Exported C FFI callback for genetlink GET; empty stub returning 0; marked 'stub' in comment; genetlink registration may fail, callback may never be called | [UNIQUE] Genetlink GET callback stub. Returns 0. No similar stub found. | [OK] Acknowledged stub returning 0; no logic to evaluate. | [LEAN] Stub genetlink GET callback returning 0. Placeholder for future wg(8) tool compatibility; presence is required by the genetlink ABI registered in init(). | [NONE] Exported genetlink GET stub returning 0. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has `/// Genetlink GET callback (stub — returns device info).` Notes stub status but lacks descriptions for all three parameters and the return value (always 0). (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: this stub is registered as the genetlink GET callback in wg_genl_init(). Removing it would leave a null function pointer in the genetlink family registration, causing a kernel crash when wg(8) tool issues a GET. Stub returning 0 is the correct placeholder. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_genl_set` | L926–L932 | PARTIAL | 88% | [LOW_VALUE] Exported C FFI callback for genetlink SET; empty stub returning 0; marked 'stub' in comment; genetlink registration may fail, callback may never be called | [UNIQUE] Genetlink SET callback stub. Returns 0. No similar stub found. | [OK] Acknowledged stub returning 0; no logic to evaluate. | [LEAN] Stub genetlink SET callback returning 0. Same rationale as rustguard_genl_get; required by the registered genetlink family interface. | [NONE] Exported genetlink SET stub returning 0. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Public `#[no_mangle]` extern-C function has `/// Genetlink SET callback (stub — configures peers).` Notes stub status but has seven parameters with no individual descriptions, and the return value (always 0) is undocumented. (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: same as rustguard_genl_get — registered as genetlink SET callback and must exist for ABI safety. Removing it risks null pointer dereference in kernel context. NONE tests and PARTIAL docs confirmed.) |

### `rustguard-tun/src/xdp.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `XdpDesc` | L62–L66 | UNDOCUMENTED | 90% | [USED] Used locally at L279, L290 in unsafe casts for descriptor access; exported public API type | [UNIQUE] Ring buffer descriptor struct (addr, len, options). Unique structure with no duplicates. | [OK] Struct layout exactly matches the kernel xdp_desc uapi: addr u64, len u32, options u32. | [LEAN] repr(C) ring descriptor struct matching kernel xdp_desc uapi; exported because callers interpret ring entries. Mandatory ABI shape. | [GOOD] Public plain #[repr(C)] struct with derived Clone, Copy, Default; no methods. Rule 6 applies. | [UNDOCUMENTED] Public `pub struct` with three `pub` fields (`addr`, `len`, `options`). No `///` doc comment on the type or on any field. Public visibility requires documentation; neither purpose nor field semantics are described. (deliberated: confirmed — Documentation UNDOCUMENTED confirmed: public exported struct with three public fields and no `///` doc comment on type or fields. Public visibility demands documentation per Rust conventions. All other axes clean.) |
| `Ring` | L89–L95 | PARTIAL | 88% | [USED] Core data structure used for rx_ring, tx_ring, fill_ring, comp_ring fields | [UNIQUE] Lock-free single-producer/single-consumer ring buffer. Unique abstraction with no duplicates. | [OK] Ring struct and methods correctly use Acquire loads for producer/consumer reads and Release stores for producer/consumer updates, matching the required memory ordering for the lock-free shared ring protocol between user space and kernel. | [LEAN] Four identical ring structures (RX, TX, fill, completion) share this abstraction. Encapsulating atomic producer/consumer access and the needs_wakeup check avoids repeating the same unsafe pointer arithmetic four times. Appropriate DRY for a low-level ring-buffer protocol. | [NONE] Private struct with unsafe raw-pointer methods (producer/consumer atomic loads/stores, needs_wakeup bit-check). No test file exists for this module; zero test coverage. | [PARTIAL] Private struct. Has one type-level `///` doc comment: 'A single-producer/single-consumer ring over mmap'd memory.' No field-level docs; the inline comment on `descs` is a regular `//` comment, not a `///` doc comment. Basic description present but incomplete. (deliberated: reclassified: tests: WEAK → NONE — Tests reclassified WEAK → NONE: the detail text explicitly states 'No test file exists for this module; zero test coverage.' Zero coverage is NONE, not WEAK — the WEAK classification was inconsistent with its own justification. Documentation PARTIAL confirmed: has type-level doc comment but no field-level docs; acceptable for private struct but still incomplete. Confidence raised since these are straightforward observations from the source.) |
| `XdpConfig` | L126–L132 | PARTIAL | 88% | [USED] Used as parameter type at L167 and fields accessed in create_inner; exported public API | [UNIQUE] Configuration structure for XDP socket creation. Unique config type with no duplicates. | [OK] Struct definition is correct. Default values (frame_size=4096, num_frames=4096, ring_size=2048) are powers of two as required by AF_XDP ring index masking. | [LEAN] Config struct with five independent socket parameters and a sensible Default. Passing these as separate arguments would be worse; a builder pattern would be overkill. Idiomatic Rust for multi-parameter socket construction. | [GOOD] Plain configuration struct; Default impl is trivially initialized scalar/String values with no logic to exercise. Rule 6 applies. | [PARTIAL] Public struct. Has a single type-level `///` doc comment: 'Configuration for creating an XDP socket.' No `///` doc comments on any of the five `pub` fields (`ifname`, `queue_id`, `frame_size`, `num_frames`, `ring_size`). No `# Examples` section. Type-level intent is stated but field semantics, units, and constraints are undocumented. (deliberated: confirmed — Documentation PARTIAL confirmed: public struct has a type-level `///` comment but none of the five public fields have doc comments. Field semantics (units, constraints, defaults reasoning) are undocumented. All other axes clean.) |
| `XdpSocket` | L148–L160 | PARTIAL | 95% | [USED] Primary public struct returned from create(); used in all socket operations | [UNIQUE] AF_XDP socket wrapper managing UMEM and ring buffers. Unique socket implementation with no duplicates. | [ERROR] Three correctness bugs found in impl methods. (1) tx_send (around L348-L352) calls copy_nonoverlapping with data.len() bytes but never checks data.len() <= self.frame_size; any caller passing a packet larger than frame_size causes an out-of-bounds write into adjacent UMEM frames, silently corrupting in-flight packets or kernel-visible memory. (2) Drop (around L396-L403) unmaps only self.umem; the four ring mmap regions allocated by mmap_ring are never unmapped because make_ring discards the base pointers and their sizes are not stored anywhere in Ring or XdpSocket, leaking at minimum four mmap regions on every drop. (3) create_inner (around L217-L220) discards the return value of all four ring-size setsockopt calls; a kernel-side failure leaves the socket with default (likely zero) ring sizes, after which the mmap offsets returned by getsockopt and the ring I/O operations produce undefined behavior without any indication to the caller. | [LEAN] Owns exactly what AF_XDP requires: fd, UMEM region, four rings, and a free-frame pool. No extraneous layers; the public API (rx_poll, rx_release, tx_send, tx_complete, fd) maps one-to-one with the AF_XDP usage model. | [NONE] Central AF_XDP socket struct with complex unsafe lifecycle methods (create, rx_poll, rx_release, tx_send, tx_complete, drop). No test file exists for this module; entirely untested. | [PARTIAL] Public struct. Has a two-line type-level `///` doc comment describing purpose and zero-copy role. Only one field (`free_frames`) has a `///` comment; all other fields (`fd`, `umem`, `umem_size`, `frame_size`, ring fields, `ring_size`) lack doc comments. No `# Examples` section on the type. (deliberated: reclassified: overengineering: ACCEPTABLE → LEAN — Correction ERROR confirmed at high confidence — all three bugs verified in source: (1) tx_send at ~L333 calls copy_nonoverlapping with data.len() but never bounds-checks against frame_size, enabling out-of-bounds write into adjacent UMEM frames; (2) Drop at L397-400 only munmaps self.umem while four ring mmap regions from mmap_ring are never stored/unmapped, leaking kernel-mapped memory every drop; (3) setsockopt_raw calls at L230-233 discard return values silently, allowing the socket to proceed with misconfigured rings. Overengineering reclassified ACCEPTABLE → LEAN: the detail itself states 'No extraneous layers; the public API maps one-to-one with the AF_XDP usage model,' which is the definition of LEAN — ACCEPTABLE was unjustified. Tests NONE confirmed: no test file exists. Documentation PARTIAL confirmed: type-level docs present but most fields undocumented.) |
| `if_nametoindex` | L449–L458 | UNDOCUMENTED | 90% | [USED] Called at L214 to resolve interface name to index; exported public utility function | [UNIQUE] Converts interface name string to ifindex u32 (similarity 0.757 with ifindex_to_name). Inverse operation: if_nametoindex converts name→index while ifindex_to_name does index→name. Different semantic contracts and opposite directions. | [OK] Correctly converts the Rust str to a NUL-terminated CString, calls libc::if_nametoindex, and maps the zero return value to an OS error via last_os_error. | [LEAN] Thin wrapper converting a &str to CString and delegating to libc::if_nametoindex with proper errno mapping. No abstraction beyond what is needed for safe, ergonomic Rust usage. | [NONE] Public function wrapping libc::if_nametoindex with CString conversion and error mapping. No test file exists; neither the happy path nor invalid-name error branch is tested. | [UNDOCUMENTED] Public `pub fn`. No `///` doc comment of any kind. The function converts an interface name string to a kernel interface index and propagates OS errors; its parameter, return type semantics, and error conditions are entirely undocumented despite public visibility. (deliberated: confirmed — Tests NONE confirmed: public function with no test coverage, neither happy path nor error branch tested. Documentation UNDOCUMENTED confirmed: public function with no `///` doc comment — parameter semantics, return type, and error conditions completely undocumented despite public visibility. Both findings well-supported.) |

### `rustguard-enroll/src/server.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ServeConfig` | L49–L62 | UNDOCUMENTED | 92% | [DEAD] Exported public struct; parameter type of public run() function at L64; pre-computed import analysis shows 0 external importers | [UNIQUE] Public configuration struct with no similar definitions found. Specific to enrollment server configuration. | [OK] Plain configuration struct with all public fields. No correctness issues. | [ACCEPTABLE] Core fields (listen_port, pool_*, token, open_immediately, state_path) are clearly necessary. The three Linux-specific performance knobs (xdp_ifname, tun_queues, use_uring) each gate a distinct I/O backend; given the explicit performance focus of this codebase, including them in a single config struct is justified rather than premature abstraction. | [NONE] No test file exists for server.rs. ServeConfig is a public struct but has no test coverage. Types/structs with no runtime behavior would normally be GOOD, but ServeConfig contains logic-relevant fields (xdp_ifname, tun_queues, use_uring) that affect runtime branching, so NONE is appropriate. | [PARTIAL] Public struct with no /// on the type itself. Three fields (xdp_ifname, tun_queues, use_uring) have /// comments; core fields listen_port, pool_network, pool_prefix, token, open_immediately, and state_path have none. No # Examples section anywhere on the type. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD→USED: ServeConfig is the sole parameter type of `pub fn run(config: ServeConfig)` at L64. Any caller of `run()` must construct a ServeConfig. The '0 external importers' result is a static analysis limitation — this is a library crate whose public entry point is consumed by a binary crate (not captured by import analysis). Removing ServeConfig would break the public API. Tests NONE: confirmed, no test file. Documentation UNDOCUMENTED: confirmed, public type with no `///` on the struct itself (detail text says PARTIAL due to some field comments, but the type-level doc is absent; keeping UNDOCUMENTED as original field).) |
| `run` | L64–L532 | UNDOCUMENTED | 92% | [DEAD] Exported public function (main tunnel server loop); pre-computed analysis shows 0 external importers (likely binary-internal call pattern not captured) | [UNIQUE] Similar functions in rustguard-daemon/src/tunnel.rs (score 0.760) and rustguard-enroll/src/client.rs (score 0.737) both below 0.82 threshold. Despite structural similarity in TUN/UDP/threading patterns, these have different semantic contracts: enrollment server with dynamic peer pool vs daemon tunnel with static config vs client enrollment. Not interchangeable. | [ERROR] Two correctness defects: (1) In the MSG_TRANSPORT handler, `decrypt_buf` is fixed at 2048 bytes, but `ct = &buf[TRANSPORT_HEADER_SIZE..]` is unbounded — if a peer (or attacker with a known session receiver_index) sends a UDP datagram whose payload exceeds 2048 bytes, `decrypt_buf[..ct.len()].copy_from_slice(ct)` will panic with an out-of-bounds index, crashing the inbound thread. XDP frames can be up to 4096 bytes, making this reachable. A bounds check (`if ct.len() > decrypt_buf.len() { continue; }`) is required before the copy. (2) In the MSG_INITIATION handler, `handshake::process_initiation` returns a 4-tuple `(pk, ts, resp, session)` where `ts` is the TAI64N handshake timestamp; the binding discards it via `_ts` and the timestamp is never stored per-peer. WireGuard requires the responder to record the latest received timestamp per initiator and reject any initiation whose timestamp is not strictly greater, providing replay protection. Discarding `_ts` means any captured initiation packet can be replayed indefinitely to reset sessions. | [ACCEPTABLE] At ~470 lines this function carries three distinct I/O backends (standard UDP, io_uring, AF_XDP), multi-queue TUN, peer restoration, enrollment logic, and full WireGuard handshake + transport handling — all in a single monolith. The per-path logic itself is not redundant, but the function mixes initialization, thread spawning, and runtime concerns without decomposition. In the XDP inbound path the lock on xsk_mtx is also dropped and immediately re-acquired for rx_release, and packets are collected into a heap-allocated Vec<> each iteration against the stated zero-alloc goal. The complexity is largely justified by explicit performance requirements, but decomposing setup (TUN/XDP/uring init) from the thread closures would reduce cognitive overhead without sacrificing performance. | [NONE] No test file exists for server.rs. The run function is the core server entrypoint encompassing enrollment, handshake, inbound/outbound packet paths, XDP, io_uring, multi-queue TUN, and peer persistence. None of these paths have any test coverage. | [UNDOCUMENTED] Public function and sole public entry point of the enrollment server with no /// doc comments. No description of overall behavior, ServeConfig parameter semantics, io::Result error conditions (bind failures, TUN creation, io_uring init), blocking behavior, or # Examples. Critical documentation gap for a ~470-line public API function. (deliberated: reclassified: utility: DEAD → USED — Correction ERROR confirmed (both defects verified in source): (1) L444-446: `decrypt_buf` is 2048 bytes but `ct = &buf[TRANSPORT_HEADER_SIZE..]` is unbounded from UDP recv; `decrypt_buf[..ct.len()].copy_from_slice(ct)` will panic if ct.len() > 2048. XDP frames up to 4096 bytes make this reachable. (2) L413: `_ts` (TAI64N timestamp) is discarded, removing WireGuard's mandated initiation replay protection. Both are genuine security/correctness bugs visible in the code. Utility reclassified DEAD→USED: `run` is the sole public entry point of the enrollment server module — it is the raison d'être of this crate. The binary crate calls it from main(). '0 external importers' is a static analysis false positive for binary-consumed library crates. Tests NONE and Documentation UNDOCUMENTED: both confirmed.) |
| `setup_xdp` | L548–L577 | PARTIAL | 80% | [USED] Non-exported Linux-only helper (cfg gate); called at L114 to load BPF program and initialize AF_XDP socket when xdp_ifname is configured | [UNIQUE] No similar functions found in RAG results. Linux-specific AF_XDP initialization function with no duplicates in codebase. | [OK] Loads and attaches BPF program, creates XDP socket, and registers it in the XSKMAP. Error propagation via `?` is correct. Queue 0 is hard-coded which is intentional for single-queue XDP. | [LEAN] Cleanly extracts the three-step AF_XDP setup (BPF load/attach, XSK creation, XSKMAP registration) from the already-large run function. Linux-only via #[cfg], returns a typed tuple, and keeps run readable. The complexity is intrinsic to AF_XDP, not introduced by the abstraction. | [NONE] No test file exists for server.rs. setup_xdp is a Linux-only function for AF_XDP socket setup with no test coverage at all. | [PARTIAL] Private Linux-only function with one /// line: 'Set up AF_XDP: load BPF program, create XSK, register in XSKMAP.' Purpose is covered but the ifname parameter, returned tuple semantics (program must outlive socket to keep BPF attached), and io::Result error conditions are not documented. Acceptable for a private item but worth improving. (deliberated: confirmed — Tests NONE: Confirmed, no test file exists and this Linux-only function has zero coverage. Documentation PARTIAL: Confirmed — has one `///` summary line but no parameter/return/error documentation. Both findings are accurate and consistent.) |

### `rustguard-kmod/src/replay.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ReplayWindow` | L11–L14 | PARTIAL | 88% | [DEAD] Exported pub(crate) struct with 0 workspace importers per Pre-computed Import Analysis. | [UNIQUE] No semantic duplicates found in RAG results. Anti-replay window struct implementing RFC 6479 sliding window algorithm. | [ERROR] Critical bug in shift_window: the bit-level carry loop iterates `for i in (0..BITMAP_LEN).rev()` — i.e. from index BITMAP_LEN-1 down to 0 — but carry must propagate in the opposite direction. When bits are shifted LEFT within each 64-bit word (moving toward higher bit positions = older counters), any bit at word[i] bit 63 that overflows must land at word[i+1] bit 0 (higher index = older). Achieving this requires iterating 0..BITMAP_LEN (low to high), collecting the overflow from word[i] as carry and applying it to word[i+1] in the next iteration. With the reversed loop, carry from word[i] is instead applied to word[i-1] (newer), and the carry produced by the lowest-indexed word is discarded entirely. Concrete counterexample: bitmap[0] = 0x8000_0000_0000_0000 (bit 63 set, counter top-63 seen), bitmap[1] = 0, bit_shift = 1. Expected result: bitmap[0] = 0, bitmap[1] = 1 (bit 63 moves to word[1] bit 0). Actual result with buggy loop: bitmap[0] = 0, bitmap[1] = 0 — the seen-bit is silently dropped. Any counter whose 'seen' bit sits at bit 63 of a word will have that bit lost on the next window advance that has a non-zero bit_shift, allowing a replayed packet to be accepted. | [LEAN] Struct encapsulates exactly two fields needed for the sliding-window algorithm (top counter + bitmap). No generics, no trait hierarchies, no builder pattern — just a flat struct implementing a well-known, fixed-size cryptographic primitive from RFC 6479. The implementation complexity in the methods is inherent to the algorithm, not imposed by the design. | [NONE] No test file found for rustguard-kmod/src/replay.rs. ReplayWindow implements security-critical anti-replay logic (check, update, shift_window) equivalent to RFC 6479 / kernel WireGuard. There are zero tests for the happy path, edge cases (counter == top, counter just inside/outside the 2048-bit window, counter == 0 on init, large forward jumps that zero the bitmap), or replay detection correctness. The sister implementation in rustguard-core/src/replay.rs may have tests, but this kmod copy is entirely untested. | [UNDOCUMENTED] pub(crate) struct has no /// doc comment at the type level. No description of the sliding-window semantics, invariants, or usage lifecycle is attached to the struct declaration itself. While two methods (check, update) carry brief /// comments, the struct type — which governs how top and bitmap jointly encode state — is entirely undocumented. Confidence reduced to 75 because pub(crate) visibility is crate-internal only, warranting some leniency. (deliberated: reclassified: documentation: UNDOCUMENTED → PARTIAL — CORRECTION ERROR confirmed at 93% confidence: Traced the carry loop manually. With `(0..BITMAP_LEN).rev()`, at iteration i, carry accumulated from word[i+1] is applied to word[i], propagating carry high→low. But a left shift within words produces overflow at bit 63 that must land in word[i+1] bit 0, requiring low→high propagation. The code comment even states 'Carry propagates from word[0] to word[BITMAP_LEN-1]' (correct direction) but then contradictorily iterates high-to-low. Concrete trace: bitmap[0]=0x8000..., bit_shift=1 → reverse loop drops the overflow bit entirely, forward loop correctly places it in bitmap[1] bit 0. Security-critical bug confirmed. UTILITY DEAD kept: 0 workspace importers per pre-computed analysis is factually correct, though the file header ('Ported from rustguard-core/src/replay.rs') suggests active development — the module may simply not be wired up yet. Confidence stays at 75 to reflect this ambiguity. TESTS NONE confirmed: no test file exists for this security-critical anti-replay module. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the module-level `//!` doc block explicitly describes 'Anti-replay sliding window (2048-bit bitmap)' and references RFC 6479, which directly covers this struct (the sole exported type). Both `check` and `update` methods carry `///` doc comments. Only the struct declaration itself lacks a `///` comment. For `pub(crate)` visibility, this is PARTIAL rather than fully UNDOCUMENTED.) |

### `rustguard-kmod/src/noise.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `zeroize` | L46–L50 | PARTIAL | 90% | [DEAD] Exported (pub(crate)) with 0 workspace importers per import analysis, but used locally in handshake functions; likely part of public API for library crate but not captured by workspace-only analysis. | [UNIQUE] Wrapper around C FFI memzero_explicit; no similar functions found in codebase | [OK] Correctly guards against empty slices and passes buf.len() as usize, matching the FFI signature. wg_memzero (memzero_explicit) cannot be optimized away. | [LEAN] Minimal thin wrapper over wg_memzero with an empty-slice guard. Exactly as complex as required for safe FFI. | [NONE] No test file exists for rustguard-kmod/src/noise.rs. zeroize is security-critical (prevents secret key material from lingering in memory) and has zero test coverage. | [PARTIAL] Has one `///` line describing purpose and the memzero_explicit guarantee. Missing parameter description for `buf` and no `# Examples` section; expected on a `pub(crate)` function. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: zeroize is called extensively throughout this file (L306, L308, L311, L313, L319, L321, L343, L363, L365, L367, L395–L400, L489, L491, L494, L525, L527). Workspace import analysis missed intra-file usage. Documentation UNDOCUMENTED→PARTIAL: line 47 has `/// Zeroize a mutable byte slice using memzero_explicit (cannot be optimized away).` which is a valid doc comment; only missing `buf` parameter description. Tests NONE confirmed: no test file exists.) |
| `constant_time_eq` | L53–L58 | PARTIAL | 85% | [USED] Not exported; called in process_response (L360) and process_initiation (L409) for MAC1 verification. | [DUPLICATE] Identical signature and implementation to constant_time_eq in cookie.rs (score 0.962); both wrap wg_crypto_memneq with same logic | [OK] Length-mismatch short-circuit is safe here (not a timing leak since lengths are public in the wire format). wg_crypto_memneq == 0 correctly means equal. | [LEAN] Minimal wrapper: length short-circuit then delegates to crypto_memneq. No unnecessary indirection. | [NONE] No test file exists for this module. constant_time_eq is a security-critical primitive (guards against timing side-channels) with zero test coverage. Length-mismatch branch and equality branch are both untested. | [PARTIAL] Has one `///` line. Private function, so tolerance is higher, but parameters `a`/`b` and the length-mismatch early-return behavior are undescribed. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: 99% identical to cookie.rs version, genuine candidate for extraction into shared module. Tests WEAK→NONE: detail explicitly states 'No test file exists for this module' with zero coverage; WEAK implies some tests exist, but none do. Documentation PARTIAL confirmed: has one-line doc but omits parameter semantics.) |
| `hash` | L72–L94 | PARTIAL | 80% | [USED] Not exported; called in initial_chain_key (L180), initial_hash (L184), mix_hash (L188), and compute_mac1 (L215). | [UNIQUE] Similar to cookie.rs hash (0.937 score) but different semantic contract: noise.rs supports 8 chunks, cookie.rs supports 4; chunk capacity is a semantic difference | [NEEDS_FIX] When chunks.len() > 8, excess chunks are silently dropped and never hashed; the function returns a wrong hash with no error signal. All current internal callers pass at most 2 chunks so no existing call is affected, but the silent truncation is a latent correctness hazard for any future caller. | [LEAN] Two separate loops to fill pointer and length arrays are driven entirely by the C shim's ABI (parallel ptr/len arrays). The variadic-chunk design matches Noise protocol usage patterns (1-2 chunks in practice). Verbosity is FFI-imposed, not self-inflicted. | [NONE] No test file exists for this module. hash delegates to the C shim and the 8-chunk cap logic is untested. Known-answer tests for BLAKE2s vectors would be required for meaningful coverage. | [PARTIAL] Has one `///` line: 'BLAKE2s-256 hash of concatenated chunks (max 8).' The 8-chunk limit is mentioned but the reason (C shim constraint) and the truncation behavior are not explained. Private function, so lower bar applies. (deliberated: confirmed — Correction NEEDS_FIX confirmed: silent truncation at 8 chunks is a latent correctness hazard. All current callers pass ≤2 chunks so no active bug, but a debug_assert or explicit error would prevent future misuse. Tests NONE confirmed. Documentation PARTIAL confirmed: mentions 8-chunk limit but not truncation behavior.) |
| `mac` | L97–L107 | PARTIAL | 85% | [USED] Not exported; called in compute_mac1() at L215 for MAC1 computation. | [DUPLICATE] Identical implementation to mac in cookie.rs (score 0.974); same FFI call to wg_blake2s_256_mac with identical parameter order and return handling | [OK] Passes correct key, key_len, data, data_len to wg_blake2s_256_mac. Output buffer is 32 bytes. No issues. | [LEAN] Straight FFI wrapper; returns a fixed 32-byte buffer. No unnecessary layers. | [NONE] No test file exists for this module. Keyed BLAKE2s MAC has zero test coverage; no known-answer or edge-case tests present. | [PARTIAL] Has one `///` line: 'Keyed BLAKE2s MAC (for MAC1/MAC2).' Mentions use-case context. Missing parameter and return-value descriptions. Private function. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical FFI wrapper to cookie.rs mac. Tests WEAK→NONE: no test file exists, zero coverage confirmed by detail. Documentation PARTIAL confirmed.) |
| `hkdf` | L110–L121 | PARTIAL | 85% | [USED] Not exported; called in mix_key (L193), process_response (L389, L419), and process_initiation (L515, L521). | [UNIQUE] Similar to blake2s.rs hkdf (0.905 score) but fundamentally different implementations: noise.rs uses C FFI wrapper wg_hkdf, blake2s.rs implements pure Rust HMAC-BLAKE2s expansion loop | [OK] Delegates correctly to wg_hkdf with three separate 32-byte output buffers. input.len() as u32 cast is safe for all inputs used (32-byte PSK or empty slice). | [LEAN] Returning all three HKDF outputs as a tuple mirrors the C shim's three out-pointer API. Callers discard unused outputs with _ patterns — correct and minimal. | [NONE] No test file exists for this module. hkdf is a foundational key-derivation primitive used throughout the handshake; zero test coverage. | [PARTIAL] Has one `///` line: 'HKDF extract + expand.' Very terse; the three output tuple members and their semantics (T1, T2, T3) are not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: detail confirms 'No test file exists for this module' and 'zero test coverage.' WEAK is inaccurate when no tests exist at all. Documentation PARTIAL confirmed: very terse one-liner, tuple semantics undescribed.) |
| `seal` | L124–L138 | PARTIAL | 85% | [USED] Not exported; called in encrypt_and_hash() at L198 for AEAD encryption. | [UNIQUE] Different semantic contract than aead.rs seal (0.880 score): noise.rs writes to pre-allocated buffer dst and returns Option<usize>, aead.rs returns Vec<u8> with allocation | [NEEDS_FIX] dst.as_mut_ptr() is passed to the C function with no prior check that dst.len() >= plaintext.len() + AEAD_TAG_SIZE. If dst is undersized, the C encryption function writes beyond the slice boundary, causing memory corruption. All current callers happen to size dst correctly, but the function itself provides no safety guarantee. | [LEAN] Thin, idiomatic Option-returning wrapper over the AEAD encrypt FFI. Appropriate level of abstraction. | [NONE] No test file exists for this module. ChaCha20-Poly1305 AEAD encryption with no coverage — happy path, error return (ret != 0), and output length are all untested. | [PARTIAL] Has one `///` line describing algorithm and return semantics ('Returns ciphertext + 16-byte tag, or None on failure.'). Parameters `counter`, `ad`, and `dst` are not individually described. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: no bounds check that dst.len() >= plaintext.len() + AEAD_TAG_SIZE before passing dst.as_mut_ptr() to C FFI. If dst is undersized, C function writes out-of-bounds — a memory safety violation in a kernel module. All current callers size correctly but the function itself provides no guarantee. Tests NONE confirmed. Documentation PARTIAL confirmed.) |
| `open` | L141–L155 | PARTIAL | 88% | [USED] Not exported; called in decrypt_and_hash() at L204 for AEAD decryption. | [UNIQUE] Different semantic contract than aead.rs open (0.894 score): noise.rs writes to pre-allocated buffer and returns Option<usize>, aead.rs returns Option<Vec<u8>> with allocation | [NEEDS_FIX] Line 151: `Some(ciphertext.len() - AEAD_TAG_SIZE)` performs usize subtraction with no prior guard that ciphertext.len() >= AEAD_TAG_SIZE. If a short ciphertext is passed, this overflows: panics in debug builds (kernel oops in this context) or silently wraps to a huge usize in release builds, causing callers to treat the result as a valid plaintext length. | [LEAN] Symmetric counterpart to seal; same minimal FFI-wrapping pattern. LEAN. | [NONE] No test file exists for this module. AEAD decryption with authentication failure path is entirely untested; authentication-failure (None return) is a security-critical branch. | [PARTIAL] Has one `///` line: 'AEAD decrypt. Returns plaintext length or None on auth failure.' Return semantics are documented; parameters are not. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: L151 `ciphertext.len() - AEAD_TAG_SIZE` performs unchecked usize subtraction. If ciphertext.len() < AEAD_TAG_SIZE, this panics in debug (kernel oops) or wraps in release. A guard `if ciphertext.len() < AEAD_TAG_SIZE { return None; }` is needed. Tests NONE confirmed. Documentation PARTIAL confirmed. Raising confidence as the bug is clearly visible in source.) |
| `dh` | L158–L162 | PARTIAL | 85% | [USED] Not exported; called in create_initiation (L304, L313), process_response (L369, L375), and process_initiation (L381, L384) for Curve25519 DH. | [UNIQUE] Curve25519 DH wrapper around FFI; no similar functions found in codebase | [OK] Correctly treats a non-zero wg_curve25519 return as a low-order point / zero-result failure and returns None. | [LEAN] Four-liner FFI wrapper returning Option to signal low-order point failure. Exactly right. | [NONE] No test file exists for this module. Curve25519 DH and its low-order-point rejection (ret != 0 → None) are untested. | [PARTIAL] Has one `///` line: 'Curve25519 DH.' Extremely terse. Does not describe the None-on-zero-result behavior, which is security-critical. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero test coverage confirmed, no test file exists. Documentation PARTIAL confirmed: extremely terse, does not describe None-on-zero-result security behavior.) |
| `generate_keypair` | L165–L173 | PARTIAL | 85% | [USED] Not exported; called in create_initiation (L300) and process_initiation (L507) to generate ephemeral keypairs. | [UNIQUE] Ephemeral keypair generation using Curve25519 FFI; no similar functions found in codebase | [OK] Calls wg_curve25519_generate_secret before wg_curve25519_generate_public, passing the secret to derive the public key. Order is correct. | [LEAN] Thin wrapper calling two sequential C functions and returning a tuple. No abstraction beyond what is needed. | [NONE] No test file exists for this module. Ephemeral keypair generation has no coverage. | [PARTIAL] Has one `///` line: 'Generate ephemeral keypair.' Does not document the return tuple order (secret, public) or the randomness source. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: no test file, zero coverage. Documentation PARTIAL confirmed: does not document return tuple order (secret, public) or randomness source.) |
| `encrypt_and_hash` | L196–L200 | PARTIAL | 85% | [USED] Not exported; called in create_initiation (L310, L319) and process_initiation (L526). | [UNIQUE] Similar to handshake.rs encrypt_and_hash (0.904 score) but different semantic contract: noise.rs writes to pre-allocated buffer ct_buf and returns usize, handshake.rs returns Vec<u8> with allocation | [OK] Encrypts with nonce=0 (correct for one-shot handshake AEAD), then mixes the ciphertext bytes (not plaintext) into h, matching the Noise ENCRYPT-AND-HASH operation. | [LEAN] Composites seal and mix_hash into a single Noise protocol primitive. Used repeatedly across both handshake paths; factoring it out is justified. | [NONE] No test file exists for this module. Noise encrypt-and-hash compound operation has no coverage; the None propagation path and hash mixing are untested. | [PARTIAL] Has one `///` line: 'Encrypt-and-hash: AEAD encrypt, mix ciphertext into hash.' Operation clearly named. Return tuple (new_h, ct_len) and failure semantics not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero coverage, no test file. Documentation PARTIAL confirmed: return tuple and failure semantics undescribed.) |
| `decrypt_and_hash` | L203–L207 | PARTIAL | 85% | [USED] Not exported; called in process_response (L375, L385, L413) and process_initiation (L375, L385). | [UNIQUE] Similar to handshake.rs decrypt_and_hash (0.919 score) but different semantic contract: noise.rs writes to pre-allocated buffer pt_buf and returns usize, handshake.rs returns Vec<u8> with allocation | [OK] Mixes the original ciphertext into h after decryption, which is symmetric with encrypt_and_hash and correct per Noise. Updated h is returned to caller. | [LEAN] Symmetric counterpart to encrypt_and_hash. Equally justified and minimal. | [NONE] No test file exists for this module. Noise decrypt-and-hash compound operation has no coverage; auth-failure propagation path is untested. | [PARTIAL] Has one `///` line: 'Decrypt-and-hash: AEAD decrypt, mix ciphertext into hash.' Symmetric with encrypt_and_hash. Return tuple and auth-failure propagation not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero coverage. Documentation PARTIAL confirmed: symmetric with encrypt_and_hash, same gaps.) |
| `compute_mac1` | L210–L216 | PARTIAL | 85% | [USED] Not exported; called in create_initiation (L326), process_response (L357), and process_initiation (L406, L533). | [UNIQUE] Similar to handshake.rs compute_mac1 (0.954 score) but lives in kernel module crate with different type system: noise.rs takes raw [u8;32], handshake.rs takes PublicKey wrapper; architectural separation across kernel vs userspace justified | [OK] MAC key is HASH(LABEL_MAC1 || responder_public); the full 32-byte BLAKE2s MAC is truncated to 16 bytes for the wire field. This matches the WireGuard MAC1 specification. | [LEAN] Three lines implementing the WireGuard MAC1 derivation exactly as specified. Extracting it avoids duplication between initiator and responder paths. | [NONE] No test file exists for this module. MAC1 computation (used to authenticate initiation and response messages before DH) is entirely untested. | [PARTIAL] Has one `///` line: 'Compute MAC1 over a message.' Does not explain the key derivation step (BLAKE2s of LABEL_MAC1 ‖ responder_public) or why only the first 16 bytes are returned. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: MAC1 computation is entirely untested. Documentation PARTIAL confirmed: key derivation step and 16-byte truncation undocumented.) |
| `tai64n_now` | L221–L235 | PARTIAL | 80% | [USED] Not exported; called in create_initiation (L318) to generate TAI64N timestamp. | [UNIQUE] TAI64N timestamp generation from kernel wall clock via FFI; no similar functions found in codebase | [OK] TAI64N layout is correct: seconds big-endian at [0..8], nanoseconds big-endian at [8..12]. `nsecs as u32` is safe in kernel context where nanoseconds are always in [0, 999_999_999] which fits in u32. | [LEAN] Inline extern declaration for wg_ktime_get_real is unusual but acceptable in a kernel module context where the FFI surface is deliberately minimised. Implementation is a direct TAI64N byte-packing with no excess logic. | [NONE] No test file exists for this module. TAI64N timestamp encoding (including the epoch-offset calculation and big-endian byte layout) has zero test coverage. | [PARTIAL] Has one `///` line: 'Generate a TAI64N timestamp from kernel wall clock.' Return format (12-byte big-endian secs+nanos) and the TAI64N encoding are not described. Private function. (deliberated: confirmed — Tests NONE confirmed: TAI64N encoding with epoch offset and big-endian byte packing has zero coverage. Documentation PARTIAL confirmed: return format undescribed.) |
| `INITIATION_SIZE` | L246–L246 | UNDOCUMENTED | 90% | [DEAD] Exported (pub(crate)) with 0 importers; used locally in create_initiation return type (L283) but not imported externally; likely part of public API. | [UNIQUE] WireGuard handshake initiation message size constant; protocol-mandated value | [OK] 148 = 4+4+32+48+28+16+16 (type, sender_index, eph_pub, enc_static, enc_ts, mac1, mac2). Verified against the byte offsets used in create_initiation. | [LEAN] Fixed message size constant derived directly from the WireGuard wire format. | [GOOD] Compile-time constant with no runtime behavior. Rule 6 applies — no tests needed. | [UNDOCUMENTED] `pub(crate)` constant with no `///` doc comment. The size derivation (4+4+32+48+28+16+16 bytes) and its wire-format meaning are not explained. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD — Utility DEAD→USED: used as array size parameter at L288 (return type), L343 (msg array), and L437 (function parameter). Tests NONE→GOOD: compile-time constant, rule 6. Documentation UNDOCUMENTED confirmed: line 246 genuinely has no `///` doc comment; the wire layout derivation should be documented.) |
| `RESPONSE_SIZE` | L247–L247 | UNDOCUMENTED | 90% | [DEAD] Exported (pub(crate)) with 0 importers; used locally in process_initiation return type (L431) but not imported externally; likely part of public API. | [UNIQUE] WireGuard handshake response message size constant; protocol-mandated value | [OK] 92 = 4+4+4+32+16+16+16 (type, sender_index, receiver_index, eph_pub, enc_empty, mac1, mac2). Verified against the byte offsets used in process_initiation and process_response. | [LEAN] Fixed message size constant derived directly from the WireGuard wire format. | [GOOD] Compile-time constant with no runtime behavior. Rule 6 applies — no tests needed. | [UNDOCUMENTED] `pub(crate)` constant with no `///` doc comment. The response wire layout and size derivation (4+4+4+32+16+16+16 bytes) are not documented. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD — Utility DEAD→USED: used as array size at L431 (function parameter) and in process_initiation return type. Tests NONE→GOOD: compile-time constant, rule 6. Documentation UNDOCUMENTED confirmed: line 247 has no doc comment.) |
| `InitiatorState` | L268–L275 | PARTIAL | 90% | [DEAD] Exported (pub(crate)) with 0 importers; used locally in create_initiation and process_response but not imported externally; likely part of public handshake API. | [UNIQUE] Kernel module struct for initiator handshake state; no similar types found in codebase | [OK] Captures all state needed to resume the handshake in process_response: ck, h, eph_secret, sender_index, their_public, psk. | [LEAN] Carries only the six fields strictly required to bridge create_initiation and process_response. No unnecessary indirection or generics. | [GOOD] Plain data struct holding intermediate handshake state; no runtime behavior. Rule 6 applies — no tests needed. | [PARTIAL] Has `///` on the struct: 'State held between sending initiation and receiving response.' No field-level docs. The `pub(crate)` field `sender_index` is undocumented; private fields tolerate this but the opaque `ck`/`h`/`eph_secret` fields would benefit from brief comments. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: created by create_initiation (L337) and consumed by process_response (L349). Tests NONE→GOOD: plain data struct, rule 6. Documentation UNDOCUMENTED→PARTIAL: line 269 has `/// State held between sending initiation and receiving response.` which is a valid doc comment, but field-level docs are absent; PARTIAL is appropriate.) |
| `create_initiation` | L281–L344 | PARTIAL | 90% | [DEAD] Exported (pub(crate)) with 0 importers per analysis; primary handshake function likely part of public API despite lack of measured imports in workspace. | [UNIQUE] Similar to handshake.rs create_initiation_with (0.925 score) but lives in kernel module with fundamentally different type systems (raw [u8;32] vs StaticSecret/PublicKey wrappers) and compilation targets; intentional architectural separation between kernel and userspace implementations | [OK] Noise IKpsk2 initiator state machine is correctly sequenced: ck/h init, responder pub mixed into h, ephemeral generated, DH(eph, rs) advances ck, static pub encrypted-and-hashed, DH(is, rs) advances ck, timestamp encrypted-and-hashed. Wire layout matches INITIATION_SIZE. Intermediate keys zeroed after use. Local ck/h copies remain on stack after being moved into state, but this is a security hygiene issue rather than a logic bug. | [LEAN] Length is protocol-mandated: two DH operations, two encryptions, MAC1, and wire-message assembly — each step maps 1:1 to a Noise_IKpsk2 spec step. Explicit zeroize calls after each ephemeral key use are a security requirement, not verbosity. No artificial layers. | [NONE] No test file exists for this module. create_initiation is the critical Noise_IKpsk2 initiator path — builds the 148-byte wire message, performs two DH operations, and derives session state. Zero test coverage: happy path, DH zero-result rejection (None), MAC1 correctness, wire layout, and PSK mixing are all untested. | [PARTIAL] Has a multi-line `///` block describing purpose, return value, and the zero-result DH failure case. Missing individual parameter descriptions and no `# Examples` section, both expected on `pub(crate)` functions. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: primary initiator handshake API function. pub(crate) entry point for the module; workspace import analysis is unreliable for kernel module crates. Tests NONE confirmed: critical Noise_IKpsk2 path with zero coverage. Documentation UNDOCUMENTED→PARTIAL: lines 281–284 have multi-line `///` describing purpose, return value, and DH failure; missing parameter descriptions.) |
| `process_response` | L347–L422 | PARTIAL | 90% | [DEAD] Exported (pub(crate)) with 0 importers; core handshake function likely part of public API for initiator-side response processing. | [UNIQUE] Similar to handshake.rs process_response (0.839 score) but lives in kernel module crate with different type systems and buffer handling; architectural separation between kernel and userspace implementations justified | [OK] MAC1 is verified before any DH operation. receiver_index sanity check is present. Response processing correctly resumes Noise state: resp_eph mixed into h, DH(eph_i, eph_r) and DH(static_i, eph_r) advance ck only, PSK HKDF runs correctly, empty payload decrypted, transport keys derived with initiator convention (send=first, recv=second). The PSK-phase key is not declared mut so cannot be zeroed — a security hygiene gap but not a logic correctness defect. | [LEAN] Complexity is inherent to the responder-side Noise_IKpsk2 processing: MAC1 verification, index check, two DH ops, PSK phase, empty-payload decryption, and key derivation. Early-exit zeroize blocks on each error path are a security necessity. No superfluous abstraction. | [NONE] No test file exists for this module. process_response completes the initiator side of the handshake and derives transport keys. Critical security paths — MAC1 verification failure, receiver-index mismatch, DH failure, PSK phase, and final key derivation — are entirely untested. | [PARTIAL] Has one `///` line: 'Process a handshake response (type 2) and derive transport keys.' Does not document parameters, MAC1 verification behavior, index mismatch failure, or the returned TransportKeys layout. No `# Examples`. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: core handshake completion API function. Workspace analysis missed cross-module usage in kernel crate. Tests NONE confirmed: MAC1 verification, index check, DH, PSK phase all untested. Documentation UNDOCUMENTED→PARTIAL: line 347 has `///` describing purpose; parameters, failure modes, and key derivation not documented.) |
| `process_initiation` | L431–L536 | PARTIAL | 90% | [DEAD] Exported (pub(crate)) with 0 importers; core handshake function likely part of public API for responder-side initiation processing. | [UNIQUE] Similar to handshake.rs process_initiation_with (0.877 score) but lives in kernel module crate with different type systems and buffer handling; architectural separation between kernel and userspace implementations justified | [OK] MAC1 verified before DH. Responder Noise state machine is correctly sequenced: static decrypt, timestamp decrypt, response ephemeral generated, DH(eph_r, eph_i) and DH(eph_r, static_i) advance ck, PSK HKDF correct, empty payload encrypted. Transport keys derived with responder convention (recv=first, send=second), which is symmetric with the initiator's assignment in process_response. Response wire layout matches RESPONSE_SIZE. Intermediate secrets zeroed. | [LEAN] Largest function but scope is justified: it combines full initiator-message verification, static-key decryption, four DH steps, PSK phase, response construction, and MAC1 tagging — all protocol-mandated. Returning a 4-tuple is unconventional but avoids an extra struct for a one-shot operation. No premature generalization. | [NONE] No test file exists for this module. process_initiation is the responder side of the full Noise_IKpsk2 handshake and generates transport keys. All branches — MAC1 rejection, four DH steps, timestamp decryption, PSK phase, response MAC1, and key derivation — have zero test coverage. This is among the most security-sensitive code in the project. | [PARTIAL] Has a multi-line `///` block naming all four return tuple members and explicitly calling out the replay-protection requirement (H3). Missing individual parameter descriptions and no `# Examples` section. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: responder-side handshake API function. pub(crate) entry point. Tests NONE confirmed: most security-sensitive function in the project with zero coverage. Documentation UNDOCUMENTED→PARTIAL: lines 431–435 have multi-line `///` naming return tuple members and replay-protection caveat; missing parameter descriptions.) |

### `rustguard-core/src/handshake.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `mix_hash` | L32–L34 | PARTIAL | 78% | [USED] Non-exported helper function called 8 times across create_initiation_with and process_initiation_with | [UNIQUE] Trivial single-call wrapper; too small for meaningful duplication analysis | [OK] Standard Noise MixHash: Hash(h || data). Correctly concatenates h and data via the slice-of-slices interface to crypto::hash. | [LEAN] Trivial one-liner wrapper, but named after the canonical Noise protocol operation it implements. Improves readability in the large handshake functions where it is called many times. | [WEAK] Private helper tested only indirectly. Called at multiple points in both initiator and responder paths; the integration tests implicitly verify it must produce matching values on both sides, but no test targets its behavior in isolation or with known test vectors. | [PARTIAL] Single /// line ('Mix a public key into the hash. Used to bind the responder's identity.'). Private function; the `data` parameter is called 'public key' in the comment but is typed as a generic byte slice, creating a minor mismatch. No return value description. (deliberated: confirmed — Tests WEAK: only indirect coverage, no isolated test — fair for a crypto helper. Documentation PARTIAL: doc says 'Mix a public key' but the function is used for ciphertext, timestamps, and other data too (called 8 times with heterogeneous inputs). The misleading comment is a legitimate doc quality issue.) |
| `mix_key` | L38–L41 | PARTIAL | 88% | [USED] Non-exported helper function called 8 times across create_initiation_with, process_response, and process_initiation_with | [DUPLICATE] Identical logic to rustguard-kmod version (score 0.974). Both extract and return (new_ck, key) from hkdf. Parameter type difference (SharedSecret vs [u8;32]) is abstraction-level only; semantic contract is identical. | [OK] Calls HKDF with 3 outputs and returns the first two (new_ck = T1, temp_key = T2), discarding T3. This is correct for Noise MixKey / WireGuard KDF2: the first output is the new chaining key, the second is the temp encryption key. | [LEAN] Minimal adapter around crypto::hkdf that projects the (ck, key) pair from the triple. Directly corresponds to the MixKey operation in the Noise spec. | [WEAK] Private helper exercised indirectly. Returns (new_ck, key) from an HKDF; only the first two outputs are used (third ignored). No test verifies the key derivation against reference vectors or confirms the ignored third output doesn't silently affect state. | [PARTIAL] Two /// lines covering purpose and return tuple ('Returns (new_ck, key) where key is for AEAD encryption'). Private function with meaningful coverage; the dh_result parameter is not described and no # Examples are present, though examples are not expected for private helpers. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: the kmod counterpart lives in a kernel module crate with fundamentally different compilation targets, type systems (SharedSecret vs raw [u8;32]), allocators, and build constraints. Deduplicating a 3-line function across a userspace library and a Linux kernel module would require a shared no_std+no_alloc crate abstracting over type differences — this adds coupling and complexity far exceeding the cost of maintaining a trivial function in both places. This is intentional architectural separation, not accidental duplication. Tests WEAK and docs PARTIAL are correct as stated.) |
| `encrypt_and_hash` | L44–L48 | PARTIAL | 80% | [USED] Non-exported helper function called in create_initiation_with (L135, L143) and process_initiation_with (L325) | [UNIQUE] High structural similarity (0.904) to kmod version, but different semantic contracts: current allocates Vec<u8>, kmod uses provided buffer and returns Option<(h, usize)>. Return type differences and allocation strategies constitute distinct contracts. | [OK] Nonce 0 is hardcoded. This is correct: in WireGuard's Noise_IKpsk2 handshake each operation uses a freshly derived key (from HKDF), so nonce 0 is legitimate per spec. AEAD seal then MixHash(ciphertext) is the correct Noise EncryptAndHash pattern. | [LEAN] Implements the EncryptAndHash Noise primitive faithfully in two lines. The function boundary keeps the large handshake functions readable and matches the spec naming. | [WEAK] Tested indirectly via the full handshake. The tampered_initiation_rejected and tampered_response_rejected tests implicitly validate AEAD authentication, but there are no direct tests of the function's output structure, nonce handling (hardcoded 0), or known-plaintext vectors. | [PARTIAL] Single /// line ('AEAD encrypt, then mix ciphertext into hash'). Private function; operation order is clear from the comment but parameters key, h, and plaintext are not described individually, and the return tuple (new_h, ciphertext) is not documented. (deliberated: confirmed — Tests WEAK: no direct test, no known-plaintext vectors, only integration coverage — correct assessment for a security-critical AEAD wrapper. Documentation PARTIAL: single /// line covers the operation but omits return tuple semantics and parameter descriptions. Both findings are accurate.) |
| `decrypt_and_hash` | L51–L55 | PARTIAL | 80% | [USED] Non-exported helper function called in process_response (L203) and process_initiation_with (L298, L311) | [UNIQUE] High structural similarity (0.919) to kmod version, but different semantic contracts: current returns Option<(h, Vec<u8>)>, kmod uses buffer and returns Option<(h, usize)>. Allocation and return type differences constitute distinct contracts. | [OK] Symmetric counterpart to encrypt_and_hash. AEAD open with nonce 0 is correct (see encrypt_and_hash reasoning). Hash is mixed with the original ciphertext (not the plaintext), matching the Noise spec. Returns None on AEAD failure — correct error propagation. | [LEAN] Symmetric counterpart to encrypt_and_hash. Returns Option to propagate AEAD auth failure. Minimal and correct. | [WEAK] Tested indirectly. Authentication failure path is covered by tamper tests, but only at message granularity. No test verifies the function returns None on authentication failure in isolation, or that the hash state is correctly updated only when decryption succeeds. | [PARTIAL] Single /// line ('AEAD decrypt, then mix ciphertext into hash'). Private function; the Option return and its failure condition (authentication failure) are not documented, which is a meaningful omission for a security-critical function. (deliberated: confirmed — Tests WEAK: authentication failure path is covered by tamper tests at message granularity but never isolated. Documentation PARTIAL: the Option return (None on auth failure) is a meaningful omission for a security function. Both findings stand.) |
| `compute_mac1` | L59–L65 | PARTIAL | 88% | [USED] Exported function with 4 runtime importers across tests, daemon, and enrollment modules | [DUPLICATE] Identical logic to rustguard-kmod version (score 0.954). Both hash, mac, and extract 16-byte result. Parameter type difference (PublicKey vs [u8;32]) is abstraction-level only; semantic contract is identical. | [OK] Correctly implements WireGuard MAC1 = MAC(Hash(LABEL_MAC1 || receiver_pub), msg). Truncation of the 32-byte BLAKE2s MAC to 16 bytes is spec-compliant. | [LEAN] Implements the WireGuard MAC1 computation exactly as the RFC specifies. The 16-byte slice copy is necessary because the underlying MAC primitive likely returns more bytes. | [WEAK] Public function imported by daemon, enroll client/server, and integration tests, but never tested directly. wrong_responder_key_rejects exercises the MAC1 rejection path in process_initiation_with. However, there is no test that tampers with the mac1 field directly, no test for the output against a known reference value, and no test for the truncation from 32 to 16 bytes. | [PARTIAL] Public function with two /// lines covering purpose and the BLAKE2s construction formula. No parameter descriptions for responder_public or msg_bytes, return value not described, and no # Examples section — all required for a public API function. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: same rationale as mix_key — the kmod version uses raw [u8;32] and lives in a kernel module with entirely different build/runtime constraints. A 6-line protocol-mandated function does not warrant cross-crate abstraction between kernel and userspace. Tests WEAK: no direct test of output against reference vector or truncation behavior. Documentation PARTIAL: public function missing parameter and return descriptions. Both are legitimate.) |
| `InitiatorHandshake` | L72–L81 | PARTIAL | 78% | [USED] Exported struct with 4 runtime importers; core state type for initiator handshake protocol | [UNIQUE] No similar struct definitions found in RAG results. Unique data structure for this module. | [OK] Sensitive fields (ck, h, ephemeral, psk) are correctly zeroized on drop. their_public and sender_index are non-secret and correctly skipped or not needing zeroization. | [LEAN] Minimal state bundle for the half-open handshake window. Zeroize/ZeroizeOnDrop derives are security-critical for a crypto library. The #[zeroize(skip)] on non-secret fields is correct and deliberate. | [WEAK] Struct is created and consumed in integration tests but its ZeroizeOnDrop guarantee — a security-critical property — is never verified. No test checks that sensitive fields (ck, h, psk) are zeroed after drop, which is the primary reason for this type's existence. | [PARTIAL] Public struct with two /// lines describing its lifecycle role and the ZeroizeOnDrop security rationale. All fields are private and undocumented. Missing a # Examples section showing the initiator handshake lifecycle, which is expected for a public exported type. (deliberated: confirmed — Tests WEAK: the ZeroizeOnDrop property is security-critical but testing it would essentially be testing the zeroize crate's derive macro — borderline but fair to flag. Documentation PARTIAL: public struct with only lifecycle description; private fields being undocumented is normal Rust but the struct-level docs could mention the security contract more explicitly.) |
| `create_initiation` | L85–L91 | PARTIAL | 80% | [USED] Exported convenience function with 4 runtime importers; std-only wrapper around create_initiation_with | [UNIQUE] Convenience wrapper that delegates to create_initiation_psk with zero PSK. Different purpose from its target (provides std default values). Not a duplicate of the function it wraps. | [OK] Thin std wrapper that delegates with a zero PSK, matching WireGuard's no-PSK mode (zero key is the protocol-specified default). | [LEAN] One-line std convenience shim that supplies a zero PSK for the common WireGuard-without-PSK case. Calling convention is clean and reduces friction for the majority of callers. | [WEAK] Happy path tested via full_handshake_and_transport and the rejection tests. This is a thin wrapper delegating to create_initiation_psk with a zero PSK. The key gap is that no test verifies the returned Initiation message structure independently (field lengths, mac1 presence) and the PSK=0 assumption is untested with non-zero values. | [PARTIAL] Public function with one /// line noting it is an std convenience wrapper. No parameter descriptions for our_static, their_public, or sender_index; return tuple (Initiation, InitiatorHandshake) is undescribed; no # Examples section for this primary public entry point. (deliberated: confirmed — Tests WEAK: thin wrapper exercised in multiple tests but never with non-default PSK; the wrapper's contract (zero PSK default) is implicitly tested. Documentation PARTIAL: public entry point with no parameter or return descriptions. Both findings stand.) |
| `create_initiation_psk` | L95–L104 | PARTIAL | 83% | [USED] Exported convenience function with 4 runtime importers; std-only PSK variant wrapper | [UNIQUE] Std convenience wrapper that generates random ephemeral and timestamp, then delegates to create_initiation_with. Wrapper pattern, not duplication of core logic. | [OK] Correctly generates a random EphemeralSecret and a current Tai64n timestamp before delegating to the core function. | [LEAN] Separates RNG and clock access from the deterministic core function, a well-established Rust pattern for testability and no_std compatibility. Three lines of real logic. | [WEAK] Exercised only via create_initiation with a hard-coded all-zero PSK. No test ever passes a non-zero PSK, meaning the PSK mixing path (Noise_IKpsk2 differentiation from IK) is effectively untested. This is a security-critical code path that is invisible in the current test suite. | [PARTIAL] Public function with one brief /// line ('Create a handshake initiation with PSK (std convenience)'). The psk parameter semantics (e.g., all-zeros meaning no PSK) are not documented; no parameter or return value descriptions; no # Examples section. (deliberated: confirmed — Tests WEAK: the PSK code path is never exercised with a non-zero PSK, which is a genuine security-critical gap. Documentation PARTIAL: psk semantics (zero = no PSK) undocumented. Both correct.) |
| `create_initiation_with` | L108–L170 | PARTIAL | 80% | [USED] Exported core function with 4 runtime importers; implements Noise_IKpsk2 msg1 generation with explicit inputs | [UNIQUE] High similarity (0.925) to rustguard-kmod create_initiation in protocol logic. However, semantic contracts differ: uses StaticSecret/PublicKey/EphemeralSecret/Tai64n abstractions and returns (Initiation, InitiatorHandshake), vs kmod's raw arrays and different struct. Different environments (library vs kernel), type systems, and return structures constitute distinct contracts despite identical Noise_IKpsk2 protocol implementation. | [OK] Follows Noise_IKpsk2 initiator message-1 pattern exactly: MixHash(Spub_r), MixHash(e_pub), KDF2(ck, DH(e,S_r)) -> (ck,k1), EncryptAndHash(k1, Spub_i), KDF2(ck, DH(s,S_r)) -> (ck,k2), EncryptAndHash(k2, timestamp). MAC1 is computed over the first 116 bytes (correct WireGuard wire offset excluding mac1+mac2). expect() panics on wrong AEAD output size are invariant-checks on internal functions, not runtime errors. | [LEAN] The deterministic no_std core. Each operation maps one-to-one to a named Noise_IKpsk2 protocol step. Length is protocol-driven, not artificial. Named variable reassignments (ck, h, key) mirror the spec's sequential state threading. | [WEAK] Core handshake creation function tested indirectly through the wrapper chain. The deterministic ephemeral variant (explicit EphemeralSecret injection) is never called directly in tests, making it impossible to verify against WireGuard protocol test vectors. PSK is always zero in all test paths. MAC1 slice boundary (&wire[..116]) is not verified. | [PARTIAL] Public core function with two /// lines noting it accepts explicit ephemeral and timestamp and is no_std compatible. No parameter descriptions for any of the six parameters, no return value documentation, and no # Examples section for this most general public variant. (deliberated: confirmed — Tests WEAK: core deterministic function never called directly in tests — a missed opportunity for test-vector validation. Documentation PARTIAL: most general public variant with six parameters, none documented. Both findings are accurate and important.) |
| `process_response` | L173–L221 | PARTIAL | 80% | [USED] Exported function with 4 runtime importers; processes response msg2 and derives transport keys | [UNIQUE] Moderate similarity (0.839) to rustguard-kmod process_response in protocol logic. However, semantic contracts differ: uses high-level crypto types and returns TransportSession, vs kmod's raw arrays and different return structure. Different environments and type abstractions constitute distinct contracts. | [NEEDS_FIX] The response message's mac1 field is never verified. process_initiation_with performs MAC1 verification first (before any DH) as a cheap DDoS guard; process_response skips this check entirely. An attacker can flood the initiator with crafted response packets that each trigger two DH scalar multiplications before being rejected by AEAD. The asymmetry is also a protocol non-compliance with the WireGuard spec which mandates MAC1 checking on all received messages. | [LEAN] Straightforward protocol implementation. receiver_index check, two DH operations, PSK mix, decrypt-and-hash, and key derivation — all mandated by the spec. No superfluous structure. | [WEAK] Three meaningful tests cover this function: happy path (full_handshake_and_transport), ciphertext tampering (tampered_response_rejected), and wrong receiver_index (mismatched_receiver_index_rejected). However, PSK is always zero, the PSK mixing branch (hkdf with psk) is never tested with a real PSK, and the resulting transport key directionality (initiator sends on first, receives on second) is tested in only one direction per session. | [PARTIAL] Public function with a single /// line ('Process a handshake response and derive transport keys'). Failure conditions returning None (receiver_index mismatch, AEAD decryption failure) are not documented; no parameter or return value descriptions; no # Examples. (deliberated: confirmed — Correction NEEDS_FIX confirmed: the WireGuard spec mandates MAC1 verification on all received messages. process_initiation_with correctly checks MAC1 before DH (L258-261), but process_response skips it entirely. While receiver_index provides a cheap first guard, an attacker who observes or guesses the sender_index can force two DH scalar multiplications per forged packet. The asymmetry is a protocol non-compliance. Tests WEAK: PSK always zero, no anti-replay testing. Documentation PARTIAL: failure conditions undocumented. All three findings stand.) |
| `process_initiation` | L227–L233 | PARTIAL | 82% | [USED] Exported convenience function with 4 runtime importers; std-only responder-side wrapper | [UNIQUE] Std convenience wrapper that delegates to process_initiation_psk with zero PSK. Wrapper pattern providing default values, not duplication. | [OK] Correct std convenience wrapper with zero PSK and no last-timestamp replay protection (caller responsibility). | [LEAN] One-line std convenience shim for the no-PSK, no-replay-check case. Mirrors the structure of create_initiation for API symmetry. | [WEAK] Thin std wrapper tested via wrong_responder_key_rejects and tampered_initiation_rejected, but always delegates to process_initiation_psk with zero PSK and no last_timestamp. The timestamp replay-prevention branch (last_timestamp is Some) is never exercised from this wrapper, which is the primary replay-protection mechanism. | [PARTIAL] Public function with one /// line describing it as the std convenience responder path. The four-element return tuple (PublicKey, Tai64n, Response, TransportSession) is entirely undescribed; no failure condition documentation; no # Examples. (deliberated: confirmed — Tests WEAK: always delegates with zero PSK and None timestamp — replay prevention is never exercised through this wrapper. Documentation PARTIAL: four-element return tuple entirely undescribed. Both correct.) |
| `process_initiation_psk` | L237–L246 | PARTIAL | 83% | [USED] Exported convenience function with 4 runtime importers; std-only responder PSK variant wrapper | [UNIQUE] Std convenience wrapper that generates random ephemeral and delegates to process_initiation_with. Wrapper pattern providing random value generation, not duplication. | [OK] Correctly generates a fresh random responder EphemeralSecret and delegates to the core function. | [LEAN] Injects RNG-generated ephemeral so the deterministic core remains no_std-compatible and testable. Clean separation of concerns with no padding. | [WEAK] Called only with zero PSK and last_timestamp=None in all test paths. The two security-distinguishing parameters — the PSK itself and the anti-replay timestamp check — are never exercised with non-trivial values. A bug in PSK mixing or timestamp comparison would not be caught by the existing test suite. | [PARTIAL] Public function with one /// line. The security-critical last_timestamp replay-prevention parameter is not documented; psk semantics not described; no # Examples section despite being a primary responder API. (deliberated: confirmed — Tests WEAK: the two security-differentiating parameters (PSK and last_timestamp) are never exercised with non-trivial values — a significant gap. Documentation PARTIAL: last_timestamp replay-prevention semantics undocumented. Both correct.) |
| `process_initiation_with` | L249–L357 | PARTIAL | 80% | [USED] Exported core function with 4 runtime importers; implements responder msg1 processing with no_std support | [UNIQUE] High similarity (0.877) to rustguard-kmod process_initiation in Noise_IKpsk2 responder logic. However, semantic contracts differ: uses StaticSecret/PublicKey/EphemeralSecret/Tai64n and returns (PublicKey, Tai64n, Response, TransportSession), vs kmod's raw arrays and different return structure. Different environments (library vs kernel) and type systems constitute distinct contracts. | [OK] MAC1 is verified before any DH (correct). Noise_IKpsk2 responder flow is correctly implemented: DH(S_resp,e_init), EncryptAndHash decrypt (static), DH(S_resp,s_init), EncryptAndHash decrypt (timestamp), replay check. Response: MixHash(e_resp), KDF2(ck,DH(e_resp,e_init)), KDF2(ck,DH(e_resp,s_init)), KDF3(ck,psk)->(ck,t,k3), MixHash(t), EncryptAndHash(k3,empty). Transport keys: KDF2(ck,'') with (recv_key,send_key)=(T1,T2) correctly mirrors the initiator's (send_key,recv_key)=(T1,T2) so keys align across the session. | [LEAN] Longest function in the file but every line is a mandatory protocol step (MAC1 verify, DH, decrypt, timestamp replay check, response construction). MAC1 checked first as a cheap pre-DH guard is a deliberate and correct optimization. | [WEAK] Core responder function with several meaningful rejection tests (MAC1, AEAD tampering, wrong key). Critical gap: the last_timestamp replay-protection branch (the is_after check) is never exercised — last_timestamp is always None in tests. Non-zero PSK is never tested. These are both security-critical paths. The MAC2/cookie TODO also indicates an untested future surface. | [PARTIAL] Public no_std-compatible function with a single /// line. The most complex function in the file: MAC1 verification, multiple DH operations, replay detection, and PSK phase are all undescribed in the doc comment. Multiple None return paths and the four-element return tuple are not documented. No # Examples. (deliberated: confirmed — Tests WEAK: last_timestamp replay check never exercised (always None), non-zero PSK never tested — both are security-critical paths with zero coverage. Documentation PARTIAL: most complex function in file (108 lines) with a single /// line, multiple failure modes undocumented. Both findings are significant and correct.) |
| `constant_time_eq` | L360–L363 | PARTIAL | 78% | [USED] Non-exported helper function called in process_initiation_with (L296) for constant-time MAC comparison | [UNIQUE] Different implementations and parameter contracts from similar functions: uses subtle crate's ConstantTimeEq trait (safe, high-level), vs kmod's unsafe C memneq call. Fixed [u8;16] parameter vs variable-length &[u8]. Different approaches to constant-time comparison constitute distinct contracts. | [OK] Uses subtle::ConstantTimeEq correctly. ct_eq on [u8;16] arrays produces a Choice that is safely coerced to bool via Into<bool>. No timing side-channel. | [LEAN] Thin, correctly-typed wrapper over the subtle crate's ConstantTimeEq. Encapsulates the .into() conversion and provides a clean call site. Using subtle rather than a hand-rolled comparison is the right choice. | [WEAK] Exercised indirectly by wrong_responder_key_rejects (MAC1 mismatch path). No direct unit test verifies constant-time semantics, confirms that a single differing byte returns false, or that equal arrays return true. The subtle::ConstantTimeEq usage is correct by inspection but untested in isolation. | [PARTIAL] Private function with one /// line naming the subtle crate. The security rationale (prevents timing side-channel attacks on MAC comparison) is implied but not stated. Acceptable for a private two-line helper but the security significance warrants more explicit documentation. (deliberated: confirmed — Tests WEAK: exercised indirectly via MAC1 mismatch path but no direct unit test. Documentation PARTIAL: security rationale (timing side-channel prevention) is implied but not explicitly stated. For a security-critical helper, this is a fair assessment on both axes.) |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `COOKIE_SECRET_LIFETIME` | L21–L21 | PARTIAL | 75% | [USED] Used locally in two places: line 96 and line 216 for timeout comparisons | [UNIQUE] Constant defining cookie rotation interval. No similar symbols found in codebase. | [OK] 120-second rotation duration correctly matches the WireGuard spec comment and protocol requirement. | [LEAN] Named constant directly encoding the 120-second rotation period from the WireGuard spec. No abstraction overhead. | [GOOD] Pure constant declaration with no runtime behavior of its own. Used as a threshold in is_fresh and maybe_rotate_secret, but the constant value itself requires no test. Treated as GOOD per constant/type default rule. | [PARTIAL] Has a single-line `///` comment ('How often the cookie secret rotates.') which is adequate for a private constant, but omits the unit (seconds/minutes) explicitly in the doc text, relying on the reader to inspect the value. (deliberated: confirmed — Documentation PARTIAL is accurate: `/// How often the cookie secret rotates.` exists but omits the unit (seconds). Minor doc improvement needed.) |
| `COOKIE_LEN` | L24–L24 | PARTIAL | 92% | [DEAD] Exported constant but has 0 external importers per import analysis | [UNIQUE] Public constant for cookie size. No similar symbols found in codebase. | [OK] 16-byte cookie size is correct per the WireGuard cookie specification. | [LEAN] Simple named constant replacing a magic number. Used in multiple places, justified. | [GOOD] Pure usize constant with no runtime behavior. Used throughout for buffer sizing. No test needed for the declaration itself. | [PARTIAL] Public constant has `/// Cookie size.` — present but trivially terse. Does not state the unit (bytes), no context on how it relates to the MAC output, and no `# Examples` section expected for a pub constant used in public API. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: this is a `pub const` in a library crate (`rustguard-core`); 0 workspace importers is expected — downstream consumers use it. The '0 external importers' metric only checks within the workspace, not downstream dependents. Tests reclassified NONE→GOOD: pure constant with no runtime behavior; the detail itself states 'GOOD per constant/type default rule', contradicting the NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 23 clearly has `/// Cookie size.` — it exists but is terse (no unit/context).) |
| `CookieChecker` | L58–L67 | PARTIAL | 92% | [DEAD] Exported struct but has 0 external importers per import analysis | [UNIQUE] Server-side cookie checker struct. No similar struct found in codebase. | [OK] All struct field types are correct and logically appropriate for their roles. | [LEAN] Four fields that map 1:1 to the server-side WireGuard cookie state: public key, rotating secret, timestamp, and load flag. No unnecessary generics or traits. | [WEAK] Inline tests cover create_reply, verify_mac2, and the wrong-source failure case. However, verify_mac1 is never tested (no test sends a message and checks MAC1 acceptance/rejection), new_with is untested, and secret rotation via maybe_rotate_secret has no coverage. The create_reply_from_bytes and verify_mac2_from_bytes no_std paths are also untested. | [PARTIAL] Public struct has a clear one-line `///` description and each field (including the sole pub field `under_load`) has a `///` comment. Missing: no `# Examples` demonstrating construction and usage, and the struct doc does not mention the expected usage pattern or lifecycle. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieChecker is the primary public API struct for server-side cookie checking in a library crate. '0 external importers' only reflects workspace analysis, not downstream consumers. Tests reclassified NONE→WEAK: the test module uses CookieChecker in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), mac2_wrong_source_fails(). The detail itself says 'WEAK' with notes on missing verify_mac1 and secret rotation coverage, contradicting the NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 57 has `/// Server-side cookie state: generates cookies and validates MAC2.` and each field has `///` comments. Missing examples and lifecycle docs make it PARTIAL, not UNDOCUMENTED.) |
| `CookieState` | L70–L75 | PARTIAL | 92% | [DEAD] Exported struct but has 0 external importers per import analysis | [UNIQUE] Client-side cookie storage struct. No similar struct found in codebase. | [OK] Option-wrapped fields are correctly typed for optional cookie and received-timestamp state. | [LEAN] Minimal two-field client-side state: the cookie and when it was received. Nothing extraneous. | [WEAK] new, process_reply (happy path and wrong-mac1 failure), compute_mac2 (with and without cookie) are all tested. However, the staleness expiry path (cookie older than COOKIE_SECRET_LIFETIME causing compute_mac2 to return zeros) is not tested — is_fresh returning false is never exercised. | [PARTIAL] Public struct has a one-line `///` description and private fields are individually documented. Missing: no `# Examples` section and no mention of how to obtain and use a CookieState (e.g., pair with CookieChecker). Public API lacks usage guidance. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieState is the primary public API struct for client-side cookie storage in a library crate. Tests reclassified NONE→WEAK: used in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), no_cookie_gives_zero_mac2(). Detail says 'WEAK' with notes on missing staleness expiry test, contradicting NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 69 has `/// Client-side cookie state: stores a received cookie for MAC2.` with field docs. Terse but present.) |
| `encode_addr` | L236–L252 | PARTIAL | 70% | [USED] Used in 2 places: create_reply (line 128) and verify_mac2 (line 150) | [UNIQUE] Encodes SocketAddr to byte representation for MAC. No similar functions found. | [OK] IPv4 encoding (4-byte octets + 2-byte BE port = 6 bytes) and IPv6 encoding (16-byte octets + 2-byte BE port = 18 bytes) are both correct and match the documentation in make_cookie_from_bytes. | [ACCEPTABLE] Allocates a Vec<u8> for a payload that is always exactly 6 or 18 bytes. A fixed-size stack buffer (e.g. an enum or a [u8; 18] with a length) would avoid the heap allocation, but the Vec keeps the caller interface simple (just &[u8]). Minor inefficiency, not a design smell. | [WEAK] Only IPv4 addresses (10.0.0.1:12345) are used in the inline tests. The IPv6 branch (SocketAddr::V6, 18-byte encoding) is never exercised. Missing coverage for the IPv6 code path and port byte-order correctness for IPv6. | [PARTIAL] Private std-gated function has `/// Encode a socket address to bytes for cookie MAC.` — a minimal description. No parameter documentation, no mention of the encoding format (though format is described on `make_cookie_from_bytes`). Acceptable for a private helper but incomplete. (deliberated: confirmed — Tests WEAK is accurate: only IPv4 addresses tested in the suite; IPv6 branch at L247 is never exercised. Documentation PARTIAL is accurate: has a minimal `///` comment but no parameter docs or encoding format description.) |

### `rustguard-daemon/src/tunnel.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `TunnelState` | L30–L35 | PARTIAL | 80% | [USED] struct created at L116 and locked/accessed throughout threads | [UNIQUE] Struct definition for tunnel state. No similar structures found in RAG results. | [OK] Struct definition is correct; fields align with all usage sites across the tunnel threads. | [LEAN] Minimal shared-state struct with exactly the three fields needed for cross-thread coordination. No unnecessary indirection. | [GOOD] Private data struct with no runtime behavior of its own; per the types/structs default rule it is GOOD. No test file exists for this module, but the struct requires no behavioral tests. | [PARTIAL] Struct has a `///` doc comment explaining its role. `pending_handshakes` field is documented. `our_static` and `peers` fields have no `///` comments. Private struct, so leniency applies, but incomplete field coverage warrants PARTIAL. (deliberated: confirmed — Documentation PARTIAL is correct: struct-level doc exists but two of three fields lack `///` comments. Private struct leniency applies; no reclassification needed.) |
| `RouteEntry` | L41–L45 | PARTIAL | 80% | [USED] struct created at L109, stored in routes_added, iterated at L480 | [UNIQUE] Struct definition for route tracking. No similar structures found. | [OK] Correctly captures all three fields required to reconstruct the delete_route call on shutdown. | [LEAN] Minimal three-field struct purpose-built for deferred route cleanup. No generics or abstraction. | [GOOD] Private plain data struct with no methods or runtime behavior; treated as GOOD by default per the types/structs rule. | [PARTIAL] Struct has a `///` doc comment describing its purpose. None of the three fields (`route`, `interface`, `is_v6`) carry `///` comments. Private struct; leniency applies, but partial field documentation keeps this PARTIAL. (deliberated: confirmed — Documentation PARTIAL is correct: struct-level doc exists but none of the three fields carry `///` comments. Private struct with leniency, but partial field coverage is accurately flagged.) |
| `run` | L48–L489 | PARTIAL | 90% | [DEAD] exported pub fn but pre-computed analysis shows 0 runtime importers | [UNIQUE] Tunnel daemon main loop. Similar run() in server.rs (score 0.760) has different config type (ServeConfig vs Config) and purpose (enrollment server vs tunnel daemon)—different semantic contracts. | [NEEDS_FIX] Two bugs: (1) MSG_RESPONSE peer lookup falls back to 'p.endpoint.is_some() && !p.has_active_session()', which matches the first peer without an active session rather than the peer that originated this handshake. In any multi-peer setup the new session is assigned to the wrong peer. The pending_handshakes tuple must also store the peer index or public key to allow an exact correlation. (2) UDP recv_from timeout only continues on WouldBlock; on Windows a read timeout fires as TimedOut, causing spurious 'UDP recv error' log output on every 500 ms poll cycle instead of silently continuing. | [ACCEPTABLE] Large monolithic function (~300 lines) that inline-defines three large thread closures. The orchestration complexity is inherent to a WireGuard daemon, but extracting the thread bodies into named functions would improve readability without adding abstraction. Justified as a single entry point for the daemon lifecycle. | [NONE] No test file found for rustguard-daemon/src/tunnel.rs. The run function is the core tunnel orchestrator covering TUN/UDP threading, handshake initiation, route management, and shutdown — all completely untested. | [PARTIAL] Public API function with only `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` — no `# Examples` section, no `# Errors` section, and the `config` parameter is undescribed. For the sole public entry point of the daemon, this is materially incomplete. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD is a false positive: `run` is the sole public entry point of the rustguard-daemon tunnel module, called from main.rs. Binary-internal module calls are not cross-crate imports, which explains the 0-importer result. Reclassified to USED (confidence 92). Correction NEEDS_FIX retained: (1) MSG_RESPONSE peer lookup at ~L288 uses a broad `p.endpoint.is_some() && !p.has_active_session()` fallback that matches the wrong peer in multi-peer configs — real bug. (2) Windows TimedOut vs WouldBlock is a genuine cross-platform issue. Tests NONE is correct — no test file exists for this 440-line orchestrator. Documentation reclassified UNDOCUMENTED → PARTIAL: L48 clearly shows `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` which is a real doc comment, just incomplete (no #Errors, no parameter docs).) |
| `ctrlc_handler` | L504–L525 | PARTIAL | 80% | [USED] called at L118 to setup shutdown signal handler | [UNIQUE] Platform signal handler setup for SIGINT/SIGTERM via libc. No similar functions found. | [NEEDS_FIX] Signal race condition: sigprocmask(SIG_BLOCK) is called inside the newly spawned thread, not in the main thread before any threads are spawned. Between the signal() calls that install signal_noop and the moment the spawned thread's sigprocmask takes effect, SIGINT or SIGTERM can arrive and be delivered to any thread with an unblocked mask (main, outbound, inbound, or timer), where signal_noop consumes it silently. sigwait in the handler thread therefore never fires and the daemon cannot be cleanly shut down. The correct POSIX pattern is to block the signals in the main thread before spawning any threads so all children inherit the blocked mask, then call sigwait exclusively in the dedicated handler thread. | [OVER] NIH reimplementation of what the `ctrlc` crate (~5M weekly downloads) provides in two lines. Uses raw unsafe libc (signal, sigemptyset, sigaddset, sigprocmask, sigwait), wraps the callback in Mutex<Option<F>> to work around FnOnce restrictions, and spawns a dedicated thread — all complexity that ctrlc encapsulates. The companion signal_noop extern is also needed only because of this approach. | [NONE] No test file found. ctrlc_handler installs SIGINT/SIGTERM handlers via unsafe libc calls and spawns a blocking thread; signal delivery, callback invocation, and once-only semantics are all untested. | [PARTIAL] Private function with `/// Install a Ctrl-C / SIGTERM handler.` The closure parameter `f` is undescribed, and the unusual sigwait-based implementation strategy is unexplained. PARTIAL for a non-trivial private function. (deliberated: confirmed — Correction NEEDS_FIX retained: the signal race condition is valid — sigprocmask(SIG_BLOCK) runs only inside the spawned thread at L519, so between the signal() calls at L508-509 and the thread's sigprocmask, SIGINT/SIGTERM can be consumed by signal_noop in any thread (main, outbound, inbound, timer), preventing sigwait from ever firing. The correct POSIX pattern blocks signals in the main thread before spawning. Overengineering OVER retained: ~20 lines of unsafe libc with no SAFETY comments reimplements what the well-maintained ctrlc crate provides in 2-3 lines; for a security-sensitive daemon, minimizing unsafe surface area is arguably more important than minimizing dependencies. Tests NONE correct for complex signal handling with no coverage. Documentation PARTIAL correct — has one-line doc but parameter `f` and the sigwait strategy are unexplained.) |

### `rustguard-core/src/messages.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `MSG_INITIATION` | L5–L5 | UNDOCUMENTED | 82% | [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 1 is correct for WireGuard handshake initiation type. to_le_bytes() yields [0x01,0x00,0x00,0x00] matching the wire format documented. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [WEAK] The initiation_roundtrip test checks that bytes[0..4] equal MSG_INITIATION.to_le_bytes(), but never asserts the constant's actual wire value (1). The test only verifies self-consistency, not the protocol-mandated value. | [UNDOCUMENTED] The group `///` comment at line 3 is separated from this constant by a blank line, so it does not attach to MSG_INITIATION in Rust's doc-comment model. No individual `///` is present on this constant. (deliberated: confirmed — Tests WEAK is correct: initiation_roundtrip asserts bytes[0..4] == MSG_INITIATION.to_le_bytes(), which exercises the constant but only verifies self-consistency, never the protocol-mandated value 1 directly. Documentation UNDOCUMENTED is technically correct — the blank line at line 4 breaks Rust doc-comment attachment from the line 3 group comment, so rustdoc won't associate it. However, the constant's name and value are self-documenting in protocol context, making the practical impact low.) |
| `MSG_RESPONSE` | L6–L6 | UNDOCUMENTED | 82% | [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 2 is correct for WireGuard handshake response type. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [WEAK] The response_roundtrip test checks that bytes[0..4] equal MSG_RESPONSE.to_le_bytes() for self-consistency, but the actual value (2) is never directly asserted against the WireGuard spec. | [UNDOCUMENTED] No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: confirmed — Tests WEAK: same self-consistency pattern as MSG_INITIATION — response_roundtrip asserts type bytes match MSG_RESPONSE.to_le_bytes() but never asserts the value 2. Documentation UNDOCUMENTED: same blank-line attachment issue as MSG_INITIATION. Both findings are accurate.) |
| `MSG_COOKIE_REPLY` | L7–L7 | UNDOCUMENTED | 90% | [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 3 is correct for WireGuard cookie reply type. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [NONE] No test for CookieReply exists at all; MSG_COOKIE_REPLY is therefore never exercised in any test case. | [UNDOCUMENTED] No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: confirmed — Tests NONE is correct: no CookieReply test exists anywhere in the test module, so this constant is never exercised in tests at all. Documentation UNDOCUMENTED: same pattern as other MSG_* constants, no attached doc comment.) |
| `MSG_TRANSPORT` | L8–L8 | UNDOCUMENTED | 87% | [USED] Exported constant with 4 runtime importers across rustguard-core and rustguard-daemon modules | [UNIQUE] No RAG data available; unique message type constant defined locally | [OK] Value 4 is correct for WireGuard transport data type. | [LEAN] Single protocol discriminant constant directly mapping to the WireGuard spec. No abstraction needed or applied. | [NONE] The transport_roundtrip test never checks bytes[0..4] against MSG_TRANSPORT, so the constant's encoding is never verified. Its actual wire value (4) is also never directly asserted. | [UNDOCUMENTED] No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: MSG_TRANSPORT IS exercised in transport_roundtrip via Transport::to_bytes() which writes MSG_TRANSPORT.to_le_bytes() into the buffer. The constant participates in the test code path — it's just never directly asserted against in assertions. This is the same pattern as MSG_INITIATION/MSG_RESPONSE (exercised but not independently validated), which were rated WEAK. NONE was inconsistent. Documentation UNDOCUMENTED stands — no attached doc comment.) |
| `Initiation` | L22–L29 | PARTIAL | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard handshake initiation messages | [UNIQUE] No RAG data available; unique WireGuard handshake initiation struct | [OK] Field sizes are consistent with the wire format doc: ephemeral(32)+encrypted_static(48)+encrypted_timestamp(28)+mac1(16)+mac2(16). to_bytes/from_bytes byte ranges are exactly correct and cover all 148 bytes. The unwrap() calls in from_bytes are safe because the input is a fixed-size &[u8; 148] so all sub-slices have statically known sizes. | [LEAN] Plain struct with fixed-size arrays exactly mirroring the WireGuard handshake initiation wire layout (148 bytes). No generics, no inheritance, no unnecessary abstraction. Field sizes correspond precisely to spec-mandated crypto sizes. | [WEAK] initiation_roundtrip covers to_bytes/from_bytes and verifies the type tag, sender_index, and ephemeral fields. However, encrypted_static, encrypted_timestamp, mac1, and mac2 are never verified after the roundtrip, leaving half the struct fields untested. | [PARTIAL] Type-level `///` comment (lines 11–21) provides a thorough wire-format diagram covering all fields. However, individual public fields carry no `///` doc comments, and there is no `# Examples` section as expected for a public API struct. (deliberated: confirmed — Tests WEAK: initiation_roundtrip verifies sender_index and ephemeral after roundtrip but does not assert encrypted_static, encrypted_timestamp, mac1, or mac2 — leaving 4 of 6 fields unchecked. While the serialization pattern is identical for all fields, byte range errors in unchecked fields would go undetected. Documentation PARTIAL: the struct has an excellent wire-format diagram at the type level, but individual public fields lack doc comments. Both findings are accurate and proportionate.) |
| `INITIATION_SIZE` | L31–L31 | UNDOCUMENTED | 78% | [USED] Exported constant with 4 runtime importers; defines 148-byte size for Initiation messages | [UNIQUE] No RAG data available; unique size constant for Initiation message | [OK] type(4)+sender(4)+ephemeral(32)+encrypted_static(48)+encrypted_timestamp(28)+mac1(16)+mac2(16)=148. Correct. | [LEAN] Named size constant used in the fixed-size return type of to_bytes(). Eliminates magic numbers and is directly consumed by the impl block. | [WEAK] INITIATION_SIZE is exercised implicitly as an array size in to_bytes/from_bytes, which are called in initiation_roundtrip, but the constant value of 148 is never directly asserted. | [UNDOCUMENTED] No `///` doc comment present. The value 148 is inferable from the struct's wire-format diagram, but the constant itself has no attached documentation. (deliberated: confirmed — Tests WEAK: INITIATION_SIZE is implicitly exercised as the array size in to_bytes/from_bytes called during initiation_roundtrip, but the value 148 is never directly asserted. If the value were wrong, compilation would fail with mismatched array sizes rather than a test failure, so this is an inherent compile-time check. Still, WEAK is fair since no explicit test validates the constant. Documentation UNDOCUMENTED: no doc comment attached to this constant.) |
| `Response` | L45–L52 | PARTIAL | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard handshake response messages | [UNIQUE] No RAG data available; unique WireGuard handshake response struct | [OK] Field sizes consistent with wire doc. to_bytes byte ranges verified: type(0-3), sender(4-7), receiver(8-11), ephemeral(12-43, 32 bytes), encrypted_empty(44-59, 16 bytes), mac1(60-75), mac2(76-91). Total 92 bytes matches RESPONSE_SIZE. | [LEAN] Plain struct mapping exactly to the 92-byte WireGuard handshake response wire format. Minimal fields, fixed-size arrays, no unnecessary indirection. | [WEAK] response_roundtrip covers to_bytes/from_bytes and verifies the type tag, sender_index, and receiver_index. However, ephemeral, encrypted_empty, mac1, and mac2 are never verified after the roundtrip. | [PARTIAL] Type-level `///` comment (lines 33–44) provides a detailed wire-format diagram. Individual public fields lack `///` doc comments and there is no `# Examples` section. (deliberated: confirmed — Tests WEAK: response_roundtrip verifies sender_index and receiver_index but not ephemeral, encrypted_empty, mac1, or mac2 — leaving 4 of 6 fields unchecked after deserialization. Documentation PARTIAL: excellent struct-level wire-format diagram but no field-level doc comments. Both findings match the evidence in the test module.) |
| `RESPONSE_SIZE` | L54–L54 | UNDOCUMENTED | 78% | [USED] Exported constant with 4 runtime importers; defines 92-byte size for Response messages | [UNIQUE] No RAG data available; unique size constant for Response message | [OK] type(4)+sender(4)+receiver(4)+ephemeral(32)+encrypted_empty(16)+mac1(16)+mac2(16)=92. Correct. | [LEAN] Named size constant used as the return array size in to_bytes(). Directly justified by usage in impl block and spec documentation. | [WEAK] RESPONSE_SIZE is implicitly exercised through the Response impl used in response_roundtrip, but the constant's value of 92 is never directly asserted. | [UNDOCUMENTED] No `///` doc comment present. The constant has no attached documentation. (deliberated: confirmed — Tests WEAK: implicitly exercised as array size in Response::to_bytes/from_bytes but never directly asserted. Same pattern as INITIATION_SIZE — compile-time array sizing provides some implicit validation. Documentation UNDOCUMENTED: no attached doc comment.) |
| `Transport` | L64–L68 | PARTIAL | 85% | [USED] Exported struct with 4 runtime importers; used for WireGuard transport data packets | [UNIQUE] No RAG data available; unique WireGuard transport data struct | [OK] to_bytes correctly emits type(4)+receiver(4)+counter(8)+payload. from_bytes guards on TRANSPORT_HEADER_SIZE(16) before accessing fixed offsets. Byte ranges buf[4..8], buf[8..16] are safe because the length check ensures buf.len()>=16. Payload extracted from buf[16..] correctly. Counter nonce is correctly read as LE u64. | [LEAN] Plain struct with Vec<u8> payload, which is appropriate since transport packet payloads are variable-length encrypted blobs. No abstraction overhead. | [WEAK] transport_roundtrip covers to_bytes/from_bytes for the happy path and checks receiver_index, counter, and payload length. However: the type byte is never verified, payload content is not checked (only length), and the from_bytes None path (buffer shorter than TRANSPORT_HEADER_SIZE) is never tested. | [PARTIAL] Type-level `///` comment (lines 56–63) provides a wire-format diagram. Public fields (`receiver_index`, `counter`, `payload`) have no individual `///` comments, and no `# Examples` section is provided. (deliberated: confirmed — Tests WEAK: transport_roundtrip checks receiver_index, counter, and payload.len() but never verifies the type tag bytes, never checks actual payload content (only length), and never tests the from_bytes None path (buffer shorter than TRANSPORT_HEADER_SIZE). Documentation PARTIAL: has struct-level wire-format diagram, but public fields lack individual doc comments.) |
| `CookieReply` | L79–L83 | PARTIAL | 88% | [USED] Exported struct with 4 runtime importers; used for WireGuard cookie reply messages | [UNIQUE] No RAG data available; unique WireGuard cookie reply struct | [OK] nonce is [u8;24] matching XChaCha20's 192-bit nonce. encrypted_cookie is [u8;32] matching 16-byte cookie+16-byte Poly1305 tag. to_bytes ranges: type(0-3), receiver(4-7), nonce(8-31, 24 bytes), encrypted_cookie(32-63, 32 bytes). Total 64 bytes matches COOKIE_REPLY_SIZE. | [LEAN] Plain struct with three fields exactly matching the 64-byte WireGuard cookie reply wire format. Minimal and spec-faithful. | [NONE] No test exists for CookieReply. Neither to_bytes nor from_bytes is exercised in any test case, despite CookieReply being imported by four different files including integration tests. | [PARTIAL] Type-level `///` comment (lines 70–78) provides a thorough wire-format diagram. Public fields lack individual `///` doc comments and there is no `# Examples` section. (deliberated: confirmed — Tests NONE is correct: no test for CookieReply exists in the test module despite it being imported by four files. Neither to_bytes nor from_bytes is exercised. This is the most significant test gap in the file. Documentation PARTIAL: excellent wire-format diagram at struct level but no field-level docs.) |
| `COOKIE_REPLY_SIZE` | L85–L85 | UNDOCUMENTED | 90% | [USED] Exported constant with 4 runtime importers; defines 64-byte size for CookieReply messages | [UNIQUE] No RAG data available; unique size constant for CookieReply message | [OK] type(4)+receiver(4)+nonce(24)+encrypted_cookie(32)=64. Correct. | [LEAN] Named size constant consumed directly by CookieReply::to_bytes() return type. Eliminates magic numbers in a security-critical context. | [NONE] No test for CookieReply exists, so COOKIE_REPLY_SIZE is never exercised or asserted in any test case. | [UNDOCUMENTED] No `///` doc comment present. The constant has no attached documentation. (deliberated: confirmed — Tests NONE: since no CookieReply test exists, this constant is never exercised in any test. Documentation UNDOCUMENTED: no attached doc comment.) |
| `TRANSPORT_HEADER_SIZE` | L87–L87 | UNDOCUMENTED | 87% | [USED] Exported constant with 4 runtime importers; defines 16-byte header size for Transport messages | [UNIQUE] No RAG data available; unique size constant for Transport header | [OK] type(4)+receiver_index(4)+counter(8)=16. Used correctly as minimum buffer length guard in from_bytes and as capacity offset in to_bytes. | [LEAN] Named constant for the 16-byte fixed transport header, used in Transport::to_bytes() capacity hint and Transport::from_bytes() minimum-length guard. Justified and minimal. | [NONE] TRANSPORT_HEADER_SIZE governs the minimum buffer length check in Transport::from_bytes, but the short-buffer None branch is never tested, so this constant's semantic role is entirely unverified. | [UNDOCUMENTED] No `///` doc comment present. The meaning (16-byte transport header) is inferable from context but no doc comment is attached to this constant. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: TRANSPORT_HEADER_SIZE is exercised in transport_roundtrip via Transport::to_bytes() (capacity hint) and Transport::from_bytes() (length guard happy path). The constant participates in the test code path. However, the None branch (short buffer) is never tested, and the constant's value of 16 is never directly asserted. This is WEAK coverage, not NONE. Documentation UNDOCUMENTED stands.) |

### `rustguard-core/src/timers.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `REKEY_TIMEOUT` | L39–L39 | PARTIAL | 88% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); handshake retry timeout | [UNIQUE] Module-level constant defining 5-second rekey timeout. Unique by definition. | [NEEDS_FIX] The doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but REKEY_TIMEOUT is actually the inter-attempt retry delay (5 s between handshake retries), as confirmed by its use in should_retry_handshake. The description matches no field or usage in this file and directly contradicts the constant's semantics. For a security-critical protocol constant, this misdescription constitutes a correctness hazard for callers relying on the doc. The numeric value (5 s) is correct per the WireGuard spec. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Used in should_retry_handshake(). retry_handshake_when_never_sent only tests the None branch (no handshake ever sent). The time-elapsed branch that compares against REKEY_TIMEOUT is completely untested. Value (5 s) never verified. | [PARTIAL] A `///` doc comment is present, but the description 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' does not match the 5-second value — REJECT_AFTER_TIME is 180 s. Actual usage in `should_retry_handshake` shows this is a handshake-retry interval, not a send-rejection threshold. The description appears to be a copy-paste error, making it actively misleading. (deliberated: confirmed — Correction NEEDS_FIX confirmed with increased confidence: the doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but the constant is 5 seconds, used in should_retry_handshake as a retry interval. REJECT_AFTER_TIME is 180s — the description is clearly wrong and actively misleading for a security-critical protocol constant. The numeric value (5s) is correct per WireGuard spec. Documentation PARTIAL confirmed: a doc comment exists but its content is factually incorrect, making it worse than undocumented. Both findings are coherent — the PARTIAL documentation and NEEDS_FIX correction describe the same underlying issue (wrong doc comment).) |

## Hygiene

- [ ] <!-- ACT-3c9a32-1 --> **[documentation · medium · trivial]** `rustguard-core/src/messages.rs`: Add `///` doc comment documentation for exported symbol:  `MSG_INITIATION`, `MSG_RESPONSE`, `MSG_COOKIE_REPLY`, `MSG_TRANSPORT`, `INITIATION_SIZE`, `RESPONSE_SIZE`, `COOKIE_REPLY_SIZE`, `TRANSPORT_HEADER_SIZE` (`MSG_INITIATION, MSG_RESPONSE, MSG_COOKIE_REPLY, MSG_TRANSPORT, INITIATION_SIZE, RESPONSE_SIZE, COOKIE_REPLY_SIZE, TRANSPORT_HEADER_SIZE`) [L5-L5, L6-L6, L7-L7, L8-L8, L31-L31, L54-L54, L85-L85, L87-L87]
- [ ] <!-- ACT-bd20cf-6 --> **[documentation · medium · trivial]** `rustguard-daemon/src/tunnel.rs`: Add `///` doc comment documentation for exported symbol: `run` (`run`) [L48-L489]
- [ ] <!-- ACT-a732f2-4 --> **[documentation · medium · trivial]** `rustguard-enroll/src/server.rs`: Add `///` doc comment documentation for exported symbol: `ServeConfig` (`ServeConfig`) [L49-L62]
- [ ] <!-- ACT-a732f2-5 --> **[documentation · medium · trivial]** `rustguard-enroll/src/server.rs`: Add `///` doc comment documentation for exported symbol: `run` (`run`) [L64-L532]
- [ ] <!-- ACT-c30836-3 --> **[documentation · medium · trivial]** `rustguard-kmod/src/replay.rs`: Add `///` doc comment documentation for exported symbol: `ReplayWindow` (`ReplayWindow`) [L11-L14]
- [ ] <!-- ACT-8fc69f-5 --> **[documentation · medium · trivial]** `rustguard-tun/src/xdp.rs`: Add `///` doc comment documentation for exported symbol: `XdpDesc` (`XdpDesc`) [L62-L66]
- [ ] <!-- ACT-8fc69f-6 --> **[documentation · medium · trivial]** `rustguard-tun/src/xdp.rs`: Add `///` doc comment documentation for exported symbol: `if_nametoindex` (`if_nametoindex`) [L449-L458]
- [ ] <!-- ACT-a4f709-4 --> **[documentation · low · trivial]** `rustguard-core/src/cookie.rs`: Complete `///` doc comment documentation for: `encode_addr` (`encode_addr`) [L236-L252]
- [ ] <!-- ACT-5920d4-2 --> **[documentation · low · trivial]** `rustguard-core/src/handshake.rs`: Complete `///` doc comment documentation for:  `initial_chain_key`, `mix_hash`, `mix_key`, `encrypt_and_hash`, `decrypt_and_hash`, `compute_mac1`, `InitiatorHandshake`, `create_initiation`, `create_initiation_psk`, `create_initiation_with`, `process_response`, `process_initiation`, `process_initiation_psk`, `process_initiation_with`, `constant_time_eq` (`initial_chain_key, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, InitiatorHandshake, create_initiation, create_initiation_psk, create_initiation_with, process_response, process_initiation, process_initiation_psk, process_initiation_with, constant_time_eq`) [L23-L25, L32-L34, L38-L41, L44-L48, L51-L55, L59-L65, L72-L81, L85-L91, L95-L104, L108-L170, L173-L221, L227-L233, L237-L246, L249-L357, L360-L363]
- [ ] <!-- ACT-3c9a32-2 --> **[documentation · low · trivial]** `rustguard-core/src/messages.rs`: Complete `///` doc comment documentation for:  `Initiation`, `Response`, `Transport`, `CookieReply` (`Initiation, Response, Transport, CookieReply`) [L22-L29, L45-L52, L64-L68, L79-L83]
- [ ] <!-- ACT-fb2f2d-8 --> **[documentation · low · trivial]** `rustguard-core/src/timers.rs`: Complete `///` doc comment documentation for: `REKEY_TIMEOUT` (`REKEY_TIMEOUT`) [L39-L39]
- [ ] <!-- ACT-bd20cf-4 --> **[documentation · low · trivial]** `rustguard-daemon/src/tunnel.rs`: Complete `///` doc comment documentation for:  `TunnelState`, `RouteEntry`, `ctrlc_handler` (`TunnelState, RouteEntry, ctrlc_handler`) [L30-L35, L41-L45, L504-L525]
- [ ] <!-- ACT-a732f2-9 --> **[documentation · low · trivial]** `rustguard-enroll/src/server.rs`: Complete `///` doc comment documentation for: `setup_xdp` (`setup_xdp`) [L548-L577]
- [ ] <!-- ACT-bb41a6-8 --> **[documentation · low · trivial]** `rustguard-kmod/src/lib.rs`: Complete `///` doc comment documentation for:  `AEAD_TAG_SIZE`, `rustguard_xmit`, `rustguard_rx`, `handle_initiation`, `handle_response`, `handle_transport`, `handle_cookie_reply`, `rustguard_dev_uninit`, `rustguard_timer_tick`, `initiate_rekey`, `send_keepalive`, `rustguard_genl_get`, `rustguard_genl_set` (`AEAD_TAG_SIZE, rustguard_xmit, rustguard_rx, handle_initiation, handle_response, handle_transport, handle_cookie_reply, rustguard_dev_uninit, rustguard_timer_tick, initiate_rekey, send_keepalive, rustguard_genl_get, rustguard_genl_set`) [L111-L111, L382-L384, L465-L467, L521-L620, L624-L671, L676-L755, L758-L781, L785-L785, L789-L791, L847-L877, L880-L914, L918-L922, L926-L932]
- [ ] <!-- ACT-58e5ff-7 --> **[documentation · low · trivial]** `rustguard-kmod/src/noise.rs`: Complete `///` doc comment documentation for:  `constant_time_eq`, `hash`, `mac`, `hkdf`, `seal`, `open`, `dh`, `generate_keypair`, `encrypt_and_hash`, `decrypt_and_hash`, `compute_mac1`, `tai64n_now` (`constant_time_eq, hash, mac, hkdf, seal, open, dh, generate_keypair, encrypt_and_hash, decrypt_and_hash, compute_mac1, tai64n_now`) [L53-L58, L72-L94, L97-L107, L110-L121, L124-L138, L141-L155, L158-L162, L165-L173, L196-L200, L203-L207, L210-L216, L221-L235]
- [ ] <!-- ACT-8fc69f-7 --> **[documentation · low · trivial]** `rustguard-tun/src/xdp.rs`: Complete `///` doc comment documentation for:  `Ring`, `XdpConfig`, `XdpSocket` (`Ring, XdpConfig, XdpSocket`) [L89-L95, L126-L132, L148-L160]

## Documentation Coverage

### `rustguard-kmod/src/noise.rs` — 62% covered

- [ ] **create_initiation** — PARTIAL → `rustguard-kmod/src/noise.rs`: Return value and DH-zero failure documented; parameters and examples absent.
- [ ] **process_response** — PARTIAL → `rustguard-kmod/src/noise.rs`: Only a single-sentence description; MAC1 verification, index check, and key derivation outcomes are not described.
- [ ] **process_initiation** — PARTIAL → `rustguard-kmod/src/noise.rs`: Return tuple named and replay-protection caveat noted; parameters undocumented and no examples.
- [ ] **Wire format constants (MSG_*, INITIATION_SIZE, RESPONSE_SIZE)** — PARTIAL → `rustguard-kmod/src/noise.rs`: MSG_INITIATION/MSG_RESPONSE/MSG_TRANSPORT are documented with type and size; INITIATION_SIZE and RESPONSE_SIZE have no doc comments.
- [ ] **Crypto helpers (hash, mac, hkdf, seal, open, dh)** — PARTIAL → `rustguard-kmod/src/noise.rs`: All have brief one-line `///` comments; parameter semantics and failure modes are not spelled out.
- [ ] **Noise state helpers (mix_hash, mix_key, initial_hash)** — MISSING → `rustguard-kmod/src/noise.rs`: mix_hash, mix_key, and initial_hash have no `///` doc comments at all.
