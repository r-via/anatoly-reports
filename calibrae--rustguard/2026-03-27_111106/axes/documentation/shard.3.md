[← Back to Documentation](./index.md) · [← Back to report](../../public_report.md)

# Documentation — Shard 3

## Findings

| File | Verdict | Documentation | Conf. | Details |
|------|---------|---------------|-------|---------|
| `rustguard-enroll/src/client.rs` | NEEDS_REFACTOR | 2 | 88% | [details](#rustguard-enrollsrcclientrs) |
| `rustguard-crypto/src/blake2s.rs` | NEEDS_REFACTOR | 3 | 90% | [details](#rustguard-cryptosrcblake2srs) |
| `rustguard-crypto/src/x25519.rs` | NEEDS_REFACTOR | 4 | 88% | [details](#rustguard-cryptosrcx25519rs) |
| `rustguard-enroll/src/packet.rs` | NEEDS_REFACTOR | 2 | 88% | [details](#rustguard-enrollsrcpacketrs) |
| `rustguard-enroll/src/state.rs` | NEEDS_REFACTOR | 4 | 88% | [details](#rustguard-enrollsrcstaters) |
| `rustguard-tun/examples/tun_echo.rs` | NEEDS_REFACTOR | 1 | 85% | [details](#rustguard-tunexamplestunechors) |
| `rustguard-kmod/src/timers.rs` | NEEDS_REFACTOR | 1 | 95% | [details](#rustguard-kmodsrctimersrs) |
| `rustguard-kmod/src/allowedips.rs` | NEEDS_REFACTOR | 2 | 85% | [details](#rustguard-kmodsrcallowedipsrs) |
| `rustguard-tun/src/uring.rs` | NEEDS_REFACTOR | 1 | 90% | [details](#rustguard-tunsrcuringrs) |
| `rustguard-tun/src/lib.rs` | NEEDS_REFACTOR | 2 | 88% | [details](#rustguard-tunsrclibrs) |

## Symbol Details

### `rustguard-enroll/src/client.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `JoinConfig` | L23–L26 | UNDOCUMENTED | 85% | [USED] Public struct in library crate with 0 workspace importers. Pattern from known false positives (COOKIE_LEN, CookieChecker, CookieState) shows public library APIs with 0 workspace imports are often USED by external downstream consumers, not dead. Confidence lowered from 95 due to this contradictory pattern. | [UNIQUE] Simple struct with server_endpoint and token fields. No similar structs found in RAG results. | [OK] Plain data struct with no logic; fields are correctly typed. | [LEAN] Minimal two-field config struct. No unnecessary abstraction — exactly the data needed to initiate an enrollment. | [GOOD] Plain data struct with no runtime behavior (two public fields, no methods). Per rule 6, types with no runtime behavior are GOOD by default. | [UNDOCUMENTED] Public struct with no `///` doc comment on the type itself, and neither `server_endpoint` nor `token` fields carry any doc comments explaining their purpose or expected format (e.g., whether `token` is a pre-shared passphrase or a bearer token). (deliberated: confirmed — Documentation UNDOCUMENTED is correct — public struct with two fields and no `///` doc comments. Fields like `token` are ambiguous (bearer token? passphrase?) and warrant documentation. Raising confidence: this is a clear-cut public API without docs.) |
| `run` | L28–L192 | UNDOCUMENTED | 85% | [USED] Public function in library crate with 0 workspace importers. Pattern from known false positives indicates such public APIs are expected to have 0 workspace imports when used by external consumers. Confidence lowered from 95 due to this contradictory evidence. | [UNIQUE] Client enrollment and tunnel flow (score 0.737 below DUPLICATE threshold of 0.82). Semantically different from server.rs::run: client performs single enrollment, receives IP assignment, and tunnels as peer; server manages pool, multiple peers, and multi-queue TUN. Different parameters, different control flow, different caller expectations. | [OK] Operator precedence for `& | 1` is correct in Rust (`&` binds tighter than `|`), so `(network_addr) | 1` correctly produces the .1 host for standard subnets. Mutex is held during `send_to` in the outbound thread, but UDP sends are non-blocking so no deadlock occurs. The inbound thread's catch-all `Err(_) => continue` correctly handles both `WouldBlock` and `TimedOut` (which `set_read_timeout` can return on some platforms). No runtime correctness bugs identified on supported platforms. | [ACCEPTABLE] The function is ~164 lines and handles three distinct phases (enrollment exchange, WireGuard handshake, bidirectional tunnel threads) inside a single function. The Arc/Mutex wrapping is necessary for the thread split. The mild concern is that `Arc<Mutex<Option<TransportSession>>>` wraps a session that is fully established synchronously before any thread is spawned — the session value could be moved directly into each thread by cloning or splitting, avoiding the runtime lock cost. Still, this is a straightforward sequential protocol driver and the complexity tracks the domain, not gratuitous abstraction. | [NONE] No test file exists for rustguard-enroll/src/client.rs. The run function orchestrates UDP enrollment, WireGuard handshake, TUN setup, and bidirectional packet forwarding — all completely untested. Critical error paths (enrollment timeout, bad token, handshake failure) and happy path are not covered. | [UNDOCUMENTED] Primary public entry-point of the crate carries no `///` doc comment. No description of what the function does, what errors it may return, what side-effects it has (TUN creation, route installation, blocking threads), or an `# Examples` section. The module-level `//!` block provides a rough outline but does not substitute for per-symbol documentation. (deliberated: confirmed — Overengineering ACCEPTABLE is fair — the Arc<Mutex<Option<>>> wrapping is slightly heavier than needed since the session is fully established before threads spawn, but the complexity tracks the domain. Tests NONE is correct: 164 lines orchestrating enrollment, handshake, and bidirectional tunneling with zero test coverage. Documentation UNDOCUMENTED is correct: primary public entry point of the crate with no `///` doc comment describing behavior, errors, or side effects (TUN creation, route installation, blocking threads). Raising confidence as all three findings are well-evidenced.) |

### `rustguard-crypto/src/blake2s.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `hash` | L9–L15 | PARTIAL | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for BLAKE2s-256 hashing. | [UNIQUE] Although RAG shows 0.847 similarity to hash in rustguard-kmod (cookie.rs, noise.rs), the implementations are fundamentally different: blake2s.rs uses the Blake2s256 library directly (pure Rust), while kmod versions use unsafe C FFI calls to kernel functions. Located in separate crates with different purposes (crypto library vs kernel module wrapper). High similarity score reflects matching semantic behavior (computing BLAKE2s-256), but different implementation strategies and cross-crate context warrant UNIQUE classification. | [OK] Blake2s256 produces 32-byte output. The multi-chunk streaming update is equivalent to hashing the concatenation, and finalize().into() correctly converts the generic array to [u8; 32]. | [LEAN] Minimal wrapper accepting &[&[u8]] to avoid forced pre-concatenation by callers. Straightforward streaming update pattern with no unnecessary abstraction. | [WEAK] Three inline tests cover determinism, input sensitivity, and multi-chunk streaming equivalence. Missing: empty-input edge case, single empty-slice chunk, and crucially no known test vectors against BLAKE2s-256 spec or WireGuard reference values. For a cryptographic primitive used in WireGuard handshakes, absence of spec-verified vectors leaves correctness unconfirmed. | [PARTIAL] Public function has a single-line `/// BLAKE2s-256 hash.` doc comment. Missing parameter description for `data`, no return value description, and no `# Examples` section — all expected for a public API function. (deliberated: confirmed — Tests WEAK: Confirmed. Three inline tests cover determinism, input sensitivity, and multi-chunk streaming, which is decent for a thin wrapper. However, for a cryptographic primitive in a WireGuard implementation, the absence of any known-answer test vectors (RFC or WireGuard reference) is a legitimate gap. WEAK stands. Documentation PARTIAL: Confirmed. The single-line `/// BLAKE2s-256 hash.` doc comment is functional but sparse for a public crypto API function — missing parameter semantics and return value description.) |
| `mac` | L24–L32 | PARTIAL | 85% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for keyed BLAKE2s MAC. | [UNIQUE] RAG shows 0.915 and 0.906 similarity to mac in rustguard-kmod (noise.rs, cookie.rs), but signatures differ: blake2s.rs accepts multiple chunks &[&[u8]], while kmod versions accept single chunk &[u8]. Implementation strategies are fundamentally different (Blake2sMac library vs C FFI). Cross-crate duplication in different modules with different purposes. Parameter variation indicates different semantic contracts despite similar high-level goal. | [OK] Blake2sMac::<U32> correctly instantiates the keyed BLAKE2s MAC with 32-byte output. new_from_slice rejects keys longer than 32 bytes, which is the BLAKE2s maximum key length. The panic message accurately describes the constraint. result.into_bytes().into() correctly extracts [u8; 32]. | [LEAN] Clean wrapper around Blake2sMac. The explicit CtOutput binding on L30 is mildly verbose but serves as a type annotation aid in a context where the type parameter is non-trivial. Overall minimal. | [WEAK] Only two tests touch `mac`: key-sensitivity (mac_differs_with_key) and differentiation from hmac_blake2s. Missing: determinism test for mac itself, multi-chunk equivalence, behavior with a zero-length key, and no known test vectors for BLAKE2s keyed MAC. WireGuard MAC1/MAC2 callers rely on correct truncation to 16 bytes, which is never validated here. | [PARTIAL] Public function has meaningful multi-line doc comments covering its purpose, WireGuard context, keying mode distinction from HMAC, and a truncation note. However, parameters `key` and `data` are not individually described, and there is no `# Examples` section required for a public API. (deliberated: confirmed — Tests WEAK: Confirmed. Only mac_differs_with_key and hmac_differs_from_keyed_blake2s exercise mac(). No determinism test, no multi-chunk equivalence test, and crucially no known test vectors for BLAKE2s keyed MAC. For crypto code used in WireGuard MAC1/MAC2, this is insufficient. Documentation PARTIAL: Confirmed. The multi-line doc comments are genuinely good — covering WireGuard context, keying mode distinction, and truncation note. However, formal parameter descriptions for `key` and `data` are absent, and there's no `# Examples` section. PARTIAL is fair given the quality of what's there versus what's missing.) |
| `hkdf` | L40–L60 | PARTIAL | 80% | [USED] Exported function runtime-imported by rustguard-crypto/src/lib.rs for HKDF key derivation. | [UNIQUE] RAG shows 0.905 similarity to hkdf in rustguard-kmod/noise.rs, but implementations are fundamentally different: blake2s.rs implements HKDF in pure Rust using internal hmac_blake2s calls, while kmod uses a single unsafe C FFI call to wg_hkdf. Located in separate crates (crypto library vs kernel module wrapper). Same semantic outcome but drastically different code paths and implementation strategies warrant UNIQUE classification. | [OK] Matches the Noise Protocol Framework HKDF construction (section 4.4) used by WireGuard: PRK = HMAC(key, input); T1 = HMAC(PRK, 0x01); T2 = HMAC(PRK, T1||0x02); T3 = HMAC(PRK, T2||0x03). The 33-byte input buffers for T2 and T3 correctly hold the prior 32-byte output plus a counter byte. No info field is included, consistent with WireGuard's spec. | [LEAN] Direct transcription of WireGuard's three-output HKDF (extract + expand T1/T2/T3). Fixed-size stack arrays for T2/T3 inputs are idiomatic in no-alloc crypto Rust. No generalization beyond the protocol requirement. | [WEAK] Two tests cover determinism and mutual distinctness of the three outputs. No test verifies against WireGuard Noise protocol KDF test vectors, making it impossible to confirm spec compliance. Missing: key-sensitivity test (different keys produce different outputs), empty-input case, and any assertion that the expansion counters (0x01/0x02/0x03) are correctly chained. | [PARTIAL] Public function has multi-line doc comments describing WireGuard HKDF, the underlying hash, extract-then-expand structure, and the return tuple convention. Missing formal `key` and `input` parameter descriptions and no `# Examples` section, which are expected for public functions in a crypto library. (deliberated: confirmed — Tests WEAK: Confirmed. Two tests (determinism + distinct outputs) provide basic sanity but no spec-compliance verification. The WireGuard Noise protocol KDF is security-critical; absence of known test vectors means a subtle bug in counter chaining (0x01/0x02/0x03) or expansion logic could go undetected. Documentation PARTIAL: Confirmed. Good contextual documentation covering WireGuard HKDF, extract-then-expand structure, and return tuple convention. Missing formal parameter descriptions for `key` and `input`.) |

### `rustguard-crypto/src/x25519.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `StaticSecret` | L7–L7 | PARTIAL | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Core X25519 static secret key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for long-lived keys. | [NEEDS_FIX] StaticSecret::diffie_hellman (L46–49) does not validate that the resulting shared secret is non-zero. X25519 over a low-order public key point produces an all-zero output; x25519_dalek does not reject this automatically. For WireGuard (the evident use case of this crate), RFC-based implementations require the DH result to be checked against the zero value and the operation to fail if so. Silently returning a zero SharedSecret breaks the security contract of the protocol. | [LEAN] Thin newtype over x25519_dalek::StaticSecret. The wrapper is justified: it adds ZeroizeOnDrop (essential for long-lived crypto secrets), exposes a controlled API surface, and semantically distinguishes static keys from ephemeral ones at the type level — a real protocol concern in WireGuard. No unnecessary generics or layers. | [WEAK] Inline tests cover `random()`, `public_key()`, and `diffie_hellman()` via happy-path DH exchange. However, `from_bytes()`, `to_bytes()`, and `random_from_rng()` are never exercised. No known-answer vector tests, no round-trip through `from_bytes`/`to_bytes`, and zeroize-on-drop behavior is untested. The test using `random()` is behind `#[cfg(feature = "std")]` implicitly, so non-std paths (via `random_from_rng`) are wholly untested. | [PARTIAL] Type-level doc comment is present and meaningful ('A static (long-lived) X25519 secret key. Zeroized on drop.'), but several public methods — `from_bytes`, `diffie_hellman`, `public_key`, and `to_bytes` — carry no `///` comments at all. Only `random_from_rng` and `random` are individually documented. No `# Examples` section exists anywhere on the type or its impl block. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK: The evaluator claims 'RFC-based implementations require the DH result to be checked against the zero value,' but this is factually incorrect. RFC 7748 Section 6.1 explicitly states implementations 'MAY check... whether K is the all-zero value,' not MUST. The WireGuard Noise protocol framework does not mandate this check either — the KDF chain and authentication in the handshake prevent exploitation of low-order point inputs. x25519_dalek intentionally follows RFC 7748 by not rejecting zero outputs. The wrapper code correctly delegates to x25519_dalek and does not establish a security contract beyond what the RFC specifies. A zero-check would be a reasonable defense-in-depth hardening but its absence is not a correctness defect. Tests WEAK is upheld: from_bytes, to_bytes, and random_from_rng are untested; no known-answer vectors exist. Documentation PARTIAL is upheld: type-level docs exist but diffie_hellman, from_bytes, to_bytes, and public_key methods lack /// comments, which is notable in a crypto library where methods carry implicit security contracts.) |
| `EphemeralSecret` | L11–L11 | PARTIAL | 88% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. Ephemeral X25519 key wrapper with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::StaticSecret with Zeroize semantics for single-use ephemeral keys. | [NEEDS_FIX] EphemeralSecret::diffie_hellman (L75–78) has the identical low-order point defect as StaticSecret::diffie_hellman: no check for an all-zero result. Because ephemeral DH is used in the first handshake message in WireGuard (with the initiator's ephemeral key), a crafted low-order responder public key passed here would produce a zero shared secret that propagates silently into the ChaCha20-Poly1305 key derivation chain. | [LEAN] Wraps x25519_dalek::StaticSecret internally (with a clear comment explaining why: WireGuard requires the ephemeral key to participate in multiple DH operations, which x25519_dalek::EphemeralSecret's consume-on-use API forbids). The semantic type distinction from StaticSecret is protocol-meaningful and prevents misuse. No abstraction excess. | [WEAK] The `ephemeral_dh_works` test covers `random()`, `diffie_hellman()`, and `public_key()` on the happy path. `random_from_rng()` is untested. The design comment that EphemeralSecret wraps StaticSecret specifically to allow reuse across multiple DH ops (a WireGuard protocol requirement) is never exercised in tests — no test calls `diffie_hellman()` twice on the same ephemeral key to confirm it isn't consumed. Zeroize behavior is untested. | [PARTIAL] Type-level doc comment is present ('An ephemeral X25519 secret key. Single use, zeroized on drop.'), and `random_from_rng` and `random` have brief docs (including a useful inline note about why StaticSecret is used under the hood). However, `diffie_hellman` and `public_key` are entirely undocumented. No `# Examples` section is provided for any of the public API surface. (deliberated: reclassified: correction: NEEDS_FIX → OK — Correction reclassified to OK for the same reasons as StaticSecret: RFC 7748 does not require rejection of all-zero DH outputs, and the Noise/WireGuard protocol handles low-order point inputs through its KDF chain and MAC-based authentication. The code correctly follows the x25519 specification. Tests WEAK is upheld: random_from_rng is untested, and critically, no test exercises the multi-DH-op property that justified using StaticSecret internally (the design comment at L63-65 explains this WireGuard requirement, but no test calls diffie_hellman twice on the same ephemeral key). Documentation PARTIAL is upheld: diffie_hellman and public_key lack /// docs.) |
| `PublicKey` | L16–L16 | PARTIAL | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. X25519 public key with constant-time equality. | [UNIQUE] No RAG similarity data available. Struct wraps x25519_dalek::PublicKey with constant-time comparison support. | [OK] PartialEq is correctly implemented using subtle::ConstantTimeEq to prevent timing side-channels on key comparison. Eq is provided, from_bytes and as_bytes delegate faithfully to x25519_dalek::PublicKey. No correctness issues found. | [LEAN] The custom PartialEq using subtle::ConstantTimeEq is a security necessity — without it, the derived PartialEq would introduce a timing side-channel exploitable in key-comparison paths. The wrapper is minimal and every addition is justified by the crypto threat model. | [WEAK] `public_key_roundtrip` covers `from_bytes()`, `as_bytes()`, and the `PartialEq` equality path. However, inequality (two distinct keys) is never asserted, so the constant-time `ct_eq` path in `PartialEq::eq` is not validated behaviorally. `AsRef<[u8]>` impl is untested. The security-critical property — that comparison is actually constant-time — cannot be verified by the existing assertions alone. | [PARTIAL] Type-level doc comments are informative, mentioning wire size and constant-time comparison rationale. However, neither `from_bytes` nor `as_bytes` in the `impl PublicKey` block carry any `///` documentation. The `PartialEq` and `Eq` impls are undocumented, though trait impls are tolerated. No `# Examples` section is present. (deliberated: confirmed — Tests WEAK upheld: public_key_roundtrip covers from_bytes, as_bytes, and the equality path of PartialEq, which is reasonable coverage. However, inequality (two distinct keys returning false) is never asserted, and AsRef<[u8]> is untested. The constant-time property of ct_eq cannot be verified by functional tests alone, but at minimum a behavioral inequality test should exist. Documentation PARTIAL upheld: type-level docs are good (mentioning wire size and constant-time rationale), but from_bytes and as_bytes lack method-level docs. For a crypto library, documenting from_bytes to note that X25519 does not validate subgroup membership would be valuable.) |
| `SharedSecret` | L28–L28 | PARTIAL | 87% | [USED] Exported struct runtime-imported by rustguard-crypto/src/lib.rs. DH shared secret result with zeroization. | [UNIQUE] No RAG similarity data available. Struct wraps 32-byte array for DH shared secrets with Zeroize semantics. | [OK] Correctly wraps [u8; 32] with Zeroize and ZeroizeOnDrop; the derive macro can call zeroize() on u8 arrays. as_bytes() returns a reference to the inner array without copying. No correctness issues found. | [LEAN] Minimal newtype over [u8; 32] with a single as_bytes() accessor. ZeroizeOnDrop is mandatory for ephemeral DH output. The type prevents raw byte arrays from leaking out of DH operations without the zeroization guarantee. Could not be simpler while still meeting the security requirements. | [WEAK] `as_bytes()` is exercised in both DH tests to compare the two sides of the exchange, which is the primary usage. However, no known-answer test (fixed input → expected output bytes) is present, so correctness against the X25519 specification is not verified. Zeroize-on-drop behavior is untested. The struct's only method is covered but only via equality checks with no authoritative reference value. | [PARTIAL] Type-level doc comment is present but very terse ('Result of a DH operation. Zeroized on drop.'). The sole public method `as_bytes` has no `///` documentation. No guidance on how callers should consume or handle the secret bytes (e.g., pass to a KDF), and no `# Examples` section. (deliberated: confirmed — Tests WEAK upheld: as_bytes is exercised indirectly via DH exchange equality assertions, which validates the primary use case. However, no known-answer test exists to verify correctness against the X25519 specification with fixed inputs, and zeroize-on-drop is untested. Documentation PARTIAL upheld: the type-level doc is terse and as_bytes has no /// comment. For a type holding sensitive key material, documenting that callers should pass the result through a KDF before use would be valuable guidance.) |

### `rustguard-enroll/src/packet.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ParsedUdp` | L9–L12 | PARTIAL | 87% | [DEAD] Exported struct with 0 workspace importers per pre-computed analysis. Matches rule 2: exported symbol with 0 runtime and 0 type-only importers equals DEAD. | [UNIQUE] Struct definition with no similar struct found in RAG results. Unique data container for parsed UDP frames. | [OK] Simple lifetime-annotated struct. Fields are correctly typed: SocketAddr for the parsed address and a borrowed slice for the payload. No correctness issues. | [LEAN] Minimal struct with exactly two fields needed for the use case: source address and payload slice. No unnecessary generics, traits, or abstraction layers. | [GOOD] Plain data struct with no runtime behavior; per rule 6 defaults to GOOD. Additionally, its two fields (src_addr and payload) are directly asserted in the inline test parse_ipv4_udp_frame, confirming the struct is exercised. | [PARTIAL] Struct has a type-level `///` comment ('Parsed UDP payload from a raw Ethernet frame.') but neither public field (`src_addr`, `payload`) carries its own `///` doc comment, and no `# Examples` section is present. Per evaluation rules, DOCUMENTED requires `///` on each public field. (deliberated: reclassified: tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD kept: pre-computed analysis reports 0 workspace importers, though as a library return type it may have external consumers not captured by workspace analysis. Tests NONE→GOOD reclassified: the detail text itself acknowledges ParsedUdp is a plain data struct with no runtime behavior (rule 6 → GOOD), and both fields (src_addr, payload) are directly asserted in the parse_ipv4_udp_frame test at lines 97-101. NONE contradicts the evaluator's own detailed analysis. Documentation UNDOCUMENTED→PARTIAL reclassified: the source clearly shows `/// Parsed UDP payload from a raw Ethernet frame.` at line 8 — the struct has a type-level doc comment. The evaluator's detail itself says 'has a type-level `///` comment'. Missing field-level docs and examples make this PARTIAL, not UNDOCUMENTED.) |
| `parse_eth_udp` | L16–L28 | PARTIAL | 88% | [DEAD] Exported function with 0 workspace importers per pre-computed analysis. Matches rule 2: exported symbol with 0 runtime and 0 type-only importers equals DEAD. | [UNIQUE] Dispatcher function that routes based on ethertype (0x0800 vs 0x86DD). Fundamentally different role from protocol-specific parsers (parse_ipv4_udp, parse_ipv6_udp). Not interchangeable with them despite RAG structural similarity scores. | [OK] Ethertype is read from the correct canonical offsets (bytes 12–13) using big-endian conversion. Dispatch values 0x0800 (IPv4) and 0x86DD (IPv6) are correct. The 14-byte minimum length guard ensures the ethertype bytes are accessible before they are read. | [LEAN] Straightforward dispatcher that reads ethertype and delegates to protocol-specific parsers. The match on ethertype is the minimal correct approach for Ethernet frame demultiplexing. | [WEAK] One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only. The too-short frame branch (frame.len() < 14), the IPv6 dispatch arm (0x86DD), and the unknown-ethertype None branch are all untested. Critical caller paths (AF_XDP fast-path) demand better negative-case coverage. | [PARTIAL] Has a two-line doc comment covering purpose and the `None` return conditions, but the `frame` parameter is not described, there is no `# Examples` section, and the supported ethertype set (0x0800, 0x86DD) is not documented. `# Examples` is expected on public API functions. (deliberated: reclassified: tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD kept: 0 workspace importers per pre-computed analysis. As the main public entry point for AF_XDP packet parsing, it is likely consumed externally, but analysis scope is definitive. Tests NONE→WEAK reclassified: the evaluator's detail text states 'One inline test (parse_ipv4_udp_frame) covers the IPv4 happy path only' — that is WEAK by definition, not NONE. The function is called at line 97 via parse_eth_udp(&frame).unwrap(). IPv6, short-frame, and unknown-ethertype branches remain untested, so WEAK is accurate. Documentation UNDOCUMENTED→PARTIAL reclassified: lines 14-15 clearly show two `///` doc comments ('Parse a raw Ethernet frame…' and 'Returns None if not…'). The evaluator's own detail confirms this. Missing parameter docs and examples make PARTIAL correct.) |

### `rustguard-enroll/src/state.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `PersistedPeer` | L13–L16 | PARTIAL | 85% | [USED] Exported struct with 1 runtime importer (rustguard-enroll/src/server.rs). Core data structure for persisted peer state. | [UNIQUE] Simple struct holding peer public key and assigned IP. No similar structures found in RAG semantic search. | [OK] Simple struct with two well-typed fields. Derives Clone correctly. No correctness issues. | [LEAN] Plain data struct with exactly two fields needed for persistence. No unnecessary generics, traits, or abstractions. Minimal and appropriate. | [GOOD] Pure data struct with no runtime behavior. Used as test data in save_and_load_roundtrip. Per rule 6, types with no runtime logic default to GOOD. | [PARTIAL] Struct-level `///` doc present ('A persisted peer — just enough to re-enroll on restart.'), but both public fields `public_key` and `assigned_ip` carry no `///` doc comments explaining their semantics (e.g. key encoding, IP assignment scheme). No `# Examples` section. (deliberated: confirmed — Documentation PARTIAL is fair: struct-level doc exists but public fields `public_key` and `assigned_ip` lack field-level `///` comments. While the types are somewhat self-descriptive, for a public API surface in rustdoc these should be documented (e.g., key encoding, IP assignment semantics). All other axes are clean and I agree with them.) |
| `default_state_path` | L19–L21 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Provides default path for state file. | [UNIQUE] Trivial 2-line function returning a PathBuf constant. No similar functions found. Unique by design. | [OK] Returns a hardcoded PathBuf. No logic or type issues. | [LEAN] Single-purpose function returning a hardcoded default path. No abstraction overhead; appropriate for its role as a named constant alternative. | [NONE] No test exercises default_state_path(). Neither inline test calls it — both tests supply explicit paths. The function is trivially simple but has zero test coverage. | [PARTIAL] Has a one-line `///` comment ('Default state file path.') but does not surface the concrete path value `/var/lib/rustguard/peers.state` in the doc, omits any note about configurability or platform assumptions, and provides no `# Examples` section as expected for a public function. (deliberated: confirmed — Tests NONE is technically accurate — no test invokes this function. However, it is a trivial 2-line constant-returning function, so the practical risk is negligible. Documentation PARTIAL is fair: the concrete path `/var/lib/rustguard/peers.state` is not surfaced in the doc string, requiring readers to inspect source. Both findings are low severity and I keep them as-is.) |
| `save` | L25–L45 | PARTIAL | 88% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Persists peer state with atomic file write. | [UNIQUE] Persists peers to state file with atomic write semantics. No similar functions found in RAG results. | [OK] Atomic write via temp-file-then-rename is correct; temp path is derived via with_extension so it is always in the same directory as the target, making rename across filesystems a non-issue. create_dir_all on an empty parent (relative path with no directory components) returns Ok(()) in Rust std, so no spurious error. BASE64_STANDARD.encode on a fixed-size array is correct. Format string produces 'key ip\n' matching the load parser. | [LEAN] Straightforward serialization to a line-oriented text format with an atomic write via rename. Parent dir creation and temp-file pattern are both justified for a daemon state file. No unnecessary abstraction layers. | [WEAK] save_and_load_roundtrip covers the happy path with two peers. Not tested: saving an empty peer slice, the create_dir_all branch (test pre-creates the dir), atomic write semantics (temp+rename), or any I/O error paths (e.g., permission denied on write). | [PARTIAL] Two `///` lines describe purpose and file format, which is useful. However, neither `path` nor `peers` parameters are documented, there is no `# Errors` section explaining the `io::Result` failure modes, and the atomic-write-via-rename behaviour (a non-obvious correctness property) appears only in the module `//!` preamble rather than on the function itself. No `# Examples` section. (deliberated: confirmed — Tests WEAK is correct: `save_and_load_roundtrip` covers the happy path with two peers but no edge cases (empty slice, IO errors, atomic write behavior). Documentation PARTIAL is fair: two doc lines describe purpose and format but omit parameter docs, `# Errors` section, and the atomic-write-via-rename semantics. Correction OK is well-justified — the temp-file-then-rename pattern is correctly implemented with same-directory guarantee via `with_extension`.) |
| `load` | L48–L84 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer (rustguard-enroll/src/server.rs). Loads and parses peer state from file. | [UNIQUE] Loads and parses peers from state file with validation. No similar functions found in RAG results. | [OK] NotFound is correctly handled as an empty list. trim() removes CR/LF remnants before split_once. split_once(' ') correctly isolates the base64 key from the IP because standard base64 contains no spaces. Vec<u8>::try_into::<[u8;32]>() is stable since Rust 1.59 and the error path is mapped correctly. ip_str.parse::<Ipv4Addr>() is correct. All error paths return InvalidData with descriptive messages. | [LEAN] Manual line-by-line parsing is proportionate to the simple 'base64_key ip' format. Not-found gracefully returns empty vec. Error mapping is explicit but necessary given io::Result return type. No reinvented wheels — the format is too trivial to justify serde. | [WEAK] Two tests cover happy path (roundtrip) and the NotFound branch. However, all error-path branches inside the parsing loop are untested: malformed line with no space (InvalidData), invalid base64 key string, key bytes not exactly 32 bytes, and unparseable IP string. These are the most likely real-world failure modes and none of them are exercised. | [PARTIAL] Single `///` line ('Load peers from the state file.') is present but extremely terse. The important non-obvious behaviour — returning `Ok(Vec::new())` rather than an error when the file is absent — is not documented on the function. No parameter docs, no `# Errors` section, and no `# Examples` section for a non-trivial public parsing function. (deliberated: confirmed — Tests WEAK is accurate: roundtrip and NotFound are covered, but all four parsing error branches (no space, bad base64, wrong key length, bad IP) are untested — these are the most likely real-world failure modes. Documentation PARTIAL is correct: the critical non-obvious behavior of returning `Ok(Vec::new())` on missing file is undocumented on the function itself. Correction OK is well-justified — all error mappings and parsing logic are sound.) |

### `rustguard-tun/examples/tun_echo.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `main` | L12–L79 | PARTIAL | 78% | [USED] Binary entry point that orchestrates TUN interface creation, packet reading, and echo reply logic. Called implicitly by Rust runtime. | [UNIQUE] No semantic duplicates detected; unique entry point implementing TUN device echo-server pattern for ICMP handling | [NEEDS_FIX] Two correctness issues: (1) The println! at L40-44 accesses buf[12..=19] unconditionally for every Ok(n), but the guard 'version == 4 && n >= 20' only gates the proto check. Because buf is a fixed 1500-byte array this won't panic, but when n < 20 stale or zero bytes are printed as src/dst IPs — minor but incorrect log output. (2) More importantly, the ICMP processing block at L48-72 hard-codes the IP header length as exactly 20 bytes: it uses buf[9] for protocol (correct for IHL=5) and then assumes ICMP starts at byte 20 (buf[20] == 8, reply[20] = 0, &reply[20..n]). The actual header length is ((buf[0] & 0x0f) * 4) bytes. If a packet arrives with IP options (IHL > 5), buf[20] is part of the options region, not the ICMP type, so the guard condition 'buf[20] == 8' is evaluated against the wrong byte and — if it accidentally matches — the code overwrites and checksums data at the wrong offsets, producing a malformed reply. | [LEAN] Straightforward smoke-test binary: creates a TUN device, reads packets in a loop, performs inline ICMP echo reply construction with manual checksum fixup. No unnecessary abstractions, generics, or patterns. The inline packet manipulation is appropriate for a self-contained example with no reuse intent. Length is driven by inherent protocol logic, not structural complexity. | [NONE] No test file exists for this example binary. The function is a manual smoke-test entry point with an infinite read loop; it cannot be unit-tested as written, and no integration test harness exercises it. The project's rustguard-tun crate has no tests/ directory at all. | [UNDOCUMENTED] No `///` doc comment directly precedes `fn main()`. The file-level `///` block (L1–7) documents the example as a whole but is syntactically attached to the first `use` statement, not to `main`. For an example binary entry point this is tolerable, but by strict doc-comment convention the function itself is undocumented. No parameter or return-value docs exist (N/A here), and no `# Examples` section is expected on `main`. (deliberated: reclassified: documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX kept: the hardcoded IHL=5 (20-byte header) assumption is a real bug — packets with IP options would misidentify ICMP offsets and produce malformed replies. The stale-buffer println is minor but valid. However, severity is tempered because this is a smoke-test example and ICMP echo requests almost never carry IP options in practice. Tests NONE kept: example binary with an infinite read loop is not reasonably unit-testable; this is expected for examples/ code. Documentation reclassified UNDOCUMENTED → PARTIAL: the file-level doc block (L1-7) comprehensively describes the example's purpose, how to run it, and how to verify results. In idiomatic Rust, examples/ binaries are documented at file level, not on fn main(). The function is effectively documented by the file header.) |

### `rustguard-kmod/src/timers.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `SessionTimers` | L36–L49 | PARTIAL | 88% | [USED] pub(crate) struct serving as crate-level session timer API with 6+ public methods; design and scope indicate internal use despite 0 importers in analysis (consistent with known false positive pattern for pub(crate) API types). | [UNIQUE] Core session timer state struct with 6 fields. No similar symbols found in codebase. | [NEEDS_FIX] Two logic bugs in the impl block. (1) is_dead() at L107-L112 measures elapsed time from session_established, but the doc comment says '240 seconds of inactivity' — inactivity must compare against last_received. A session that received a packet 5 seconds ago but was established 250 seconds ago is incorrectly declared dead, causing premature session-key zeroing. (2) needs_keepalive() at L115-L131: when last_sent == 0 (no packet ever sent), the else branch (L124) sets since_last_send = now.saturating_sub(last_received), making since_last_send == since_last_recv. The guard `since_last_send >= keepalive_interval_ns && since_last_recv < keepalive_interval_ns` then requires x >= K && x < K simultaneously, which is always false. A peer that has received data but never sent any will never emit a keepalive regardless of how long the interval has been exceeded. | [LEAN] Six fields, each directly corresponding to a distinct WireGuard timer checkpoint (session established, handshake sent, last rx, last tx, rekey flag, keepalive interval). No unnecessary generics, no inheritance, no factory. Every field is consumed by at least one method in the impl block. The struct is exactly as large as the protocol demands. | [NONE] No test file found for rustguard-kmod/src/timers.rs. SessionTimers contains critical WireGuard session lifecycle logic across multiple methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) that drive rekey decisions, session expiry, and keepalive scheduling. None of these methods or their time-based edge cases (boundary conditions for REKEY_AFTER_TIME_NS, REJECT_AFTER_TIME_NS, counter thresholds, keepalive logic with varying last_sent/last_received combinations) are exercised by any test. | [PARTIAL] pub(crate) struct with '/// Per-peer timer state.' at the type level and /// comments on all six fields explaining sentinel-value conventions (0 = no session, 0 = never, 0 = disabled). Passes the struct rule (type + all fields documented) but the type-level comment is a near-tautological restatement of the name rather than describing lifecycle, ownership, or how the struct is driven. No # Examples section, though pub(crate) reduces that expectation. Field-level docs are good quality. (deliberated: confirmed — Correction: Both logic bugs are independently verifiable from the source code and are genuine. (1) is_dead() at L109 measures elapsed from session_established, but both the method's doc comment ('should be zeroed') and the constant's doc comment ('240 seconds of inactivity') indicate inactivity semantics requiring last_received. A peer receiving packets 1s ago but established 241s ago is incorrectly killed. (2) needs_keepalive() when last_sent==0: since_last_send is set to now-last_received, making it equal to since_last_recv, so the guard (x >= K && x < K) is always false — verified by algebraic substitution. I increase confidence to 88 because both bugs are mechanically provable from the code. Tests: NONE is correct — no test file exists for timers.rs and no tests cover any of the 6+ session lifecycle methods. These methods contain time-based boundary logic that critically needs test coverage. Documentation: PARTIAL is correct — the struct has a type-level doc comment and all fields are documented with good sentinel-value conventions, but the type-level comment is thin and new() lacks any doc comment.) |

### `rustguard-kmod/src/allowedips.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `MAX_PEERS` | L13–L13 | PARTIAL | 78% | [DEAD] Exported constant with 0 workspace importers and no usage within the module; never referenced in any function or struct | [UNIQUE] No RAG data available. Constant declaration with no duplicates in provided codebase. | [OK] Constant is correctly typed as usize with value 8. It is exported crate-internally and its declaration has no correctness defect. Callers are responsible for validating peer_idx against this bound before calling insert methods. | [LEAN] Simple compile-time constant capping peer count. Appropriate for a kernel module with fixed-size constraints. Not used as a bound inside this file's insert paths (a correctness gap), but the declaration itself is minimal and justified. | [GOOD] Compile-time constant with no runtime behavior. Per rule 6, constants/types with no runtime behavior are GOOD by default. No test is needed to validate a fixed numeric literal. | [PARTIAL] Has `/// Maximum number of peers we support.` — a one-liner that names the concept but omits any rationale for the value 8, any relationship to hardware limits or protocol constraints, and no `# Examples`. For a `pub(crate)` constant that gates peer table allocation this is terse but present; PARTIAL is appropriate. (deliberated: reclassified: tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD: keeping evaluator's workspace analysis showing 0 importers, though as a pub(crate) constant in a kernel module crate it may be used in files not analyzed — reducing confidence to 78. Documentation UNDOCUMENTED → PARTIAL: the source code clearly has `/// Maximum number of peers we support.` on line 12, directly contradicting UNDOCUMENTED. The detail text itself says PARTIAL is appropriate. Tests NONE → GOOD: the detail text explicitly states '[GOOD] Compile-time constant with no runtime behavior. Per rule 6, constants/types with no runtime behavior are GOOD by default.' This was a merge error where the field didn't match the evaluator's actual assessment.) |
| `AllowedIps` | L28–L31 | PARTIAL | 85% | [DEAD] Exported routing table struct with 0 workspace importers; defined with public API but not instantiated or used within the module. Possible external library consumer, but no evidence in workspace analysis | [UNIQUE] No RAG data available. Struct definition for IP routing table, no duplicates in provided codebase. | [NEEDS_FIX] Two correctness issues in the impl block. (1) In `insert()`, the cidr==0 branch executes dead assignments `node.cidr = 0; node.peer_idx = peer_idx;` that are immediately overwritten — the resulting cidr value is 255, but the double-write is evidence of an incomplete or contradictory change that could mislead future edits into restoring the cidr=0 path and silently breaking default-route lookup (which gates on cidr==255). (2) In `lookup_recursive()`, the parent-side pre-check (line 177) explicitly handles `child.cidr == 255` and updates `best`, but the recursive-entry check (line 170) excludes cidr==255 via `node.cidr != 255`. Should any code path ever produce a non-root node with cidr==255, the pre-check would fire for it while the entry check silently ignores it, causing that node's peer to be recorded once but its children never to contribute to `best` — a latent incorrect-LPM result. | [LEAN] Thin wrapper holding two trie roots (IPv4/IPv6), directly matching WireGuard's dual-family routing table. Public API surface is flat and purposeful: insert_v4/v6, lookup_v4/v6, lookup_packet, remove_by_peer. No factory indirection, no trait objects, no configuration structs. The sentinel cidr==255 hack for default routes is a design smell but not an abstraction excess. | [NONE] No test file exists for this module. The critical public API — insert_v4, insert_v6, lookup_v4, lookup_v6, lookup_packet, remove_by_peer — has zero test coverage. Longest-prefix-match correctness, default-route (cidr=0) sentinel logic, IPv4/IPv6 dispatch in lookup_packet, and remove_by_peer semantics are all untested. | [PARTIAL] Has struct-level `/// AllowedIPs routing table.` but neither field (`root4`, `root6`) carries a `///` doc comment. As the primary public export of this module the struct warrants field-level documentation explaining the IPv4/IPv6 root split and lifecycle expectations. No `# Examples` section is present on the type. Terse but present at the type level, so PARTIAL rather than UNDOCUMENTED. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX: confirmed — lines 108-112 show dead assignments (`node.cidr = 0; node.peer_idx = peer_idx;`) immediately overwritten by sentinel assignments. The lookup_recursive boolean `child.cidr == 255 || (child.cidr > 0 && child.cidr != 255)` simplifies to `child.cidr > 0` — needlessly complex and asymmetric with the entry check. Both are real code quality issues. Utility DEAD → USED: AllowedIps is the sole pub(crate) struct of this module, the module doc explicitly says 'Used on the TX path', and it mirrors WireGuard's core allowedips.c data structure. A 200-line module with 6 public methods was clearly written for use by the rest of the crate. The '0 workspace importers' likely reflects incomplete workspace analysis (only this file was provided). Documentation UNDOCUMENTED → PARTIAL: source code line 28 clearly shows `/// AllowedIPs routing table.` — the struct HAS a doc comment. Fields lack docs, matching PARTIAL. The detail text itself concludes 'PARTIAL rather than UNDOCUMENTED.' Tests NONE: confirmed, no test coverage exists for any public method.) |

### `rustguard-tun/src/uring.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `UringTun` | L96–L102 | PARTIAL | 88% | [DEAD] Exported struct with 0 workspace importers. Provides public constructor UringTun::new() and public methods (submit_write, submit_and_wait, poll). Typical library module entry point with no workspace consumers detected. | [UNIQUE] io_uring TUN I/O engine struct; no semantic duplicates found | [NEEDS_FIX] Two issues: (1) pending_reads -= 1 at L194 and L226 (in submit_and_wait and poll respectively) has no underflow guard. pending_reads is usize, so an unexpected or duplicate read completion causes a debug-mode panic or a silent release-mode wrap to usize::MAX, after which the refill logic ((NUM_BUFS/2).saturating_sub(pending_reads)) emits zero and the read pipeline permanently stalls. (2) fill_reads is called at L207/L238 while the just-completed buffer slots are still marked in_flight=true (the caller has not yet had a chance to call bufs.free()). alloc() therefore cannot reclaim those slots for the refill; the refill can only draw from slots that were free prior to this call. Under sustained load where all 256 slots are cycling through the caller, fill_reads returns 0 and read starvation occurs until the caller frees slots and calls submit_and_wait/poll again. | [LEAN] The engine composes IoUring + BufferPool + tun_fd + pending_reads into a single coherent unit. Four public methods (new, submit_write, submit_and_wait, poll) map cleanly onto distinct call-site needs. The CQ-processing block is duplicated between submit_and_wait and poll — a mild DRY concern, but not overengineering; inlining avoids extra state for what amounts to a hot loop. No unnecessary generics or factory layers. | [NONE] No test file exists for this module. UringTun has complex runtime behavior: io_uring ring creation, fixed-buffer registration, SQ push failure handling, pending_reads accounting, pipeline refill logic in both submit_and_wait() and poll(). None of this is exercised by any test. | [PARTIAL] Type-level `/// io_uring TUN I/O engine.` is present. Private field `pending_reads` has `///`. However, the public field `pub bufs: BufferPool` has no `///` doc comment at all. Per the struct rule, `///` on each public field is required for DOCUMENTED status; the missing `bufs` doc makes this PARTIAL. (deliberated: reclassified: utility: DEAD → USED — Correction NEEDS_FIX kept: pending_reads -= 1 without underflow guard at L194/L226 is a real risk — usize underflow in release mode wraps to MAX and permanently stalls the read pipeline. The fill_reads timing concern (refill before caller frees slots) is a valid design issue under sustained load. Both are legitimate. Utility reclassified DEAD→USED: UringTun is the primary public entry point of this io_uring TUN engine library module. Zero workspace importers is the standard false-positive pattern for library crates. Tests NONE kept: no test file exists and UringTun has complex stateful logic (ring setup, pending_reads accounting, pipeline refill). Documentation PARTIAL kept: the struct has a type-level doc comment, but `pub bufs: BufferPool` at L98 lacks a `///` doc comment while being the only public field.) |

### `rustguard-tun/src/lib.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `Tun` | L25–L30 | PARTIAL | 78% | [USED] Public struct in library crate with documented public API methods (create, raw_fd, name, read, write). Core abstraction for TUN device operations. Zero workspace importers is a known false positive for library crates consumed by external dependents, consistent with ServeConfig and CookieChecker reclassifications. | [UNIQUE] No RAG data available. Platform-agnostic TUN device wrapper with raw fd and interface name. | [NEEDS_FIX] Two correctness concerns in the impl blocks that belong to this type. (1) The Drop impl at line 102 calls libc::close(self.fd) unconditionally without checking fd >= 0. If a platform-specific create() ever stores an invalid sentinel fd (e.g. -1) in the struct — e.g. on a partially-constructed error path — close(-1) silently returns EBADF and the error is swallowed, masking a real bug. (2) raw_fd() returns a bare i32 rather than BorrowedFd<'_> (std::os::unix::io::BorrowedFd). The method is explicitly documented for io_uring and AF_XDP use. Callers can trivially wrap the returned integer in an OwnedFd or equivalent owning type; when that type is dropped it calls close() on the same fd, and then Tun::drop() calls close() a second time, producing a double-close. On Linux a double-close can silently close an unrelated fd that was opened between the two closes — a data-corruption-class bug, not merely a resource leak. | [LEAN] Two-field struct holding exactly what is needed: a raw fd for syscall/io_uring/XDP integration and an interface name for diagnostics. No unnecessary generics, no trait indirection, no builder machinery. The platform dispatch is handled at the method level via cfg attributes rather than via vtables or dynamic dispatch, which is the idiomatic and minimal Rust approach. | [NONE] No test file exists anywhere under rustguard-tun/. The Tun struct carries significant runtime behavior via its impl block (create, read, write, raw_fd, Drop), all of which involve OS-level syscalls. None of these code paths—happy path, error paths, platform guards, or Drop cleanup—are exercised by any test. | [PARTIAL] Has a type-level /// doc comment (L21–L24) describing purpose and platform-specific behaviour (macOS utun vs Linux /dev/net/tun). Private fields fd and name carry /// annotations. No # Examples section is present for this public struct, and no description of error conditions, thread-safety, or typical creation/read/write usage pattern is provided. (deliberated: confirmed — Correction NEEDS_FIX: The raw_fd() returning bare i32 concern is valid — the standard library introduced BorrowedFd/OwnedFd specifically to prevent double-close bugs, and this is a public API explicitly documented for io_uring/AF_XDP integration where callers are highly likely to wrap fds in owning types. This is a real API safety gap, not just a style preference. The Drop fd >= 0 guard (action 2) is lower severity — Tun can only be constructed via create() which returns Result, so an invalid fd reaching Drop requires a logic error in the platform modules — but it's still sound defensive programming. Keeping NEEDS_FIX at moderate confidence since these are design-level improvements rather than outright bugs in current code paths. Tests NONE: Accurate — no tests exist. This is a systems-level struct requiring OS privileges to test, which explains the absence but doesn't excuse it; at minimum, TunConfig construction and basic Tun field accessors could be tested with mock fds. Documentation PARTIAL: Accurate — type-level and field-level docs exist and are informative, but a public library API of this importance should have # Examples, # Errors, and # Safety sections (especially for raw_fd).) |
| `TunConfig` | L33–L44 | PARTIAL | 88% | [USED] Public configuration struct required as parameter for Tun::create() public method. Any external caller must construct TunConfig to use the library's core functionality. Zero workspace importers reflects analysis limitation for library crates; semantically required by public API contract. | [UNIQUE] No RAG data available. Configuration struct for TUN device creation with network parameters. | [OK] Pure data struct; no logic to contain bugs. The mtu: u16 field admits zero and sub-68 values (below the RFC 791 minimum), but that is a validation concern appropriately deferred to Tun::create(). No correctness defects are present in the struct definition itself. | [LEAN] Plain data struct with exactly five fields that map 1-to-1 to the parameters needed when calling ifconfig/ioctl to bring up a point-to-point TUN interface (name, MTU, local address, peer address, netmask). No builder pattern, no validation layer, no generics. Using std::net::Ipv4Addr for address fields is the idiomatic standard-library type — not overengineering. The Option<String> on name is the minimal way to express 'let the OS pick a name', a real and common need. | [GOOD] TunConfig is a plain data struct with only public fields and no impl block or runtime behavior. Per rule 6, types with no runtime behavior are GOOD by default. Its correctness is enforced entirely at the type level (Ipv4Addr, u16, Option<String>), leaving nothing meaningful to unit-test. | [PARTIAL] Type-level /// doc comment is present and all five public fields carry // comments with concise, informative descriptions including defaults (mtu: 1420 WireGuard standard) and inline value examples (e.g. '10.0.0.1'). Missing a # Examples section showing construction of a TunConfig and its use with Tun::create; no documentation on field interaction constraints (e.g. address/destination/netmask coherence requirements). (deliberated: confirmed — Documentation PARTIAL is accurate. All five public fields have inline comments with useful context (defaults, examples), but for a public library config struct, a # Examples section showing typical construction and use with Tun::create() would significantly improve discoverability. No field interaction constraints are documented (e.g., address/netmask coherence). All other axes are clean — pure data struct with no logic, appropriate derives noted as a best-practice suggestion rather than a correctness issue.) |

## Hygiene

- [ ] <!-- ACT-ca1d92-2 --> **[documentation · medium · trivial]** `rustguard-enroll/src/client.rs`: Add `///` doc comment documentation for exported symbol: `JoinConfig` (`JoinConfig`) [L23-L26]
- [ ] <!-- ACT-ca1d92-3 --> **[documentation · medium · trivial]** `rustguard-enroll/src/client.rs`: Add `///` doc comment documentation for exported symbol: `run` (`run`) [L28-L192]
- [ ] <!-- ACT-4cf3f3-4 --> **[documentation · medium · trivial]** `rustguard-enroll/src/packet.rs`: Add `///` doc comment documentation for exported symbol: `ParsedUdp` (`ParsedUdp`) [L9-L12]
- [ ] <!-- ACT-4cf3f3-5 --> **[documentation · medium · trivial]** `rustguard-enroll/src/packet.rs`: Add `///` doc comment documentation for exported symbol: `parse_eth_udp` (`parse_eth_udp`) [L16-L28]
- [ ] <!-- ACT-bc7d6e-1 --> **[documentation · low · trivial]** `rustguard-crypto/src/blake2s.rs`: Complete `///` doc comment documentation for:  `hash`, `mac`, `hkdf` (`hash, mac, hkdf`) [L9-L15, L24-L32, L40-L60]
- [ ] <!-- ACT-7726d0-3 --> **[documentation · low · trivial]** `rustguard-crypto/src/x25519.rs`: Complete `///` doc comment documentation for:  `StaticSecret`, `EphemeralSecret`, `PublicKey`, `SharedSecret` (`StaticSecret, EphemeralSecret, PublicKey, SharedSecret`) [L7-L7, L11-L11, L16-L16, L28-L28]
- [ ] <!-- ACT-ee407e-1 --> **[documentation · low · trivial]** `rustguard-enroll/src/state.rs`: Complete `///` doc comment documentation for:  `PersistedPeer`, `default_state_path`, `save`, `load` (`PersistedPeer, default_state_path, save, load`) [L13-L16, L19-L21, L25-L45, L48-L84]
- [ ] <!-- ACT-050448-4 --> **[documentation · low · trivial]** `rustguard-kmod/src/timers.rs`: Complete `///` doc comment documentation for: `SessionTimers` (`SessionTimers`) [L36-L49]
- [ ] <!-- ACT-4d0b70-3 --> **[documentation · low · trivial]** `rustguard-tun/src/lib.rs`: Complete `///` doc comment documentation for: `Tun` (`Tun`) [L25-L30]
- [ ] <!-- ACT-4d0b70-4 --> **[documentation · low · trivial]** `rustguard-tun/src/lib.rs`: Complete `///` doc comment documentation for: `TunConfig` (`TunConfig`) [L33-L44]
