[← Back to Documentation](./index.md) · [← Back to report](../../public_report.md)

# Documentation — Shard 2

## Findings

| File | Verdict | Documentation | Conf. | Details |
|------|---------|---------------|-------|---------|
| `rustguard-enroll/src/control.rs` | NEEDS_REFACTOR | 9 | 95% | [details](#rustguard-enrollsrccontrolrs) |
| `rustguard-daemon/src/config.rs` | NEEDS_REFACTOR | 5 | 95% | [details](#rustguard-daemonsrcconfigrs) |
| `rustguard-enroll/src/protocol.rs` | NEEDS_REFACTOR | 8 | 92% | [details](#rustguard-enrollsrcprotocolrs) |
| `rustguard-tun/src/linux.rs` | NEEDS_REFACTOR | 3 | 90% | [details](#rustguard-tunsrclinuxrs) |
| `rustguard-tun/src/bpf_loader.rs` | NEEDS_REFACTOR | 1 | 88% | [details](#rustguard-tunsrcbpfloaderrs) |
| `rustguard-crypto/src/aead.rs` | NEEDS_REFACTOR | 7 | 92% | [details](#rustguard-cryptosrcaeadrs) |
| `rustguard-tun/src/macos.rs` | NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-tunsrcmacosrs) |
| `rustguard-kmod/src/cookie.rs` | NEEDS_REFACTOR | 3 | 92% | [details](#rustguard-kmodsrccookiers) |
| `rustguard-tun/src/linux_mq.rs` | NEEDS_REFACTOR | 1 | 90% | [details](#rustguard-tunsrclinuxmqrs) |
| `rustguard-enroll/src/fast_udp.rs` | NEEDS_REFACTOR | 6 | 85% | [details](#rustguard-enrollsrcfastudprs) |

## Symbol Details

### `rustguard-enroll/src/control.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `socket_path` | L18–L20 | PARTIAL | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by start_listener (L89) and send_command (L154). | [UNIQUE] Trivial utility returning hardcoded socket path. No similar functions found. | [OK] Simple function returning a hardcoded PathBuf. No correctness issues. | [LEAN] Single source of truth for the socket path. Trivially simple, no abstraction overhead. | [NONE] No test file exists for control.rs. socket_path has no tests whatsoever. | [PARTIAL] Has `/// Default socket path.` but does not mention the actual hardcoded path `/tmp/rustguard.sock`, omits return-value description, and has no `# Examples` section for a public API function. (deliberated: confirmed — Tests NONE confirmed — no test file exists. Documentation PARTIAL is accurate: the `/// Default socket path.` comment exists but omits the actual hardcoded value `/tmp/rustguard.sock` and has no `# Examples`. For a trivial one-liner this is minor but technically correct.) |
| `new_window` | L26–L28 | UNDOCUMENTED | 95% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for creating enrollment window state. | [UNIQUE] Trivial constructor wrapping AtomicI64 initialization. No similar functions found. | [OK] Correctly initialises Arc<AtomicI64> to 0 (closed state). No issues. | [LEAN] Minimal constructor for the EnrollmentWindow type alias. The one-liner factory is justified as it pairs cleanly with the opaque type alias and avoids callers spelling out Arc<AtomicI64::new(0)> everywhere. | [NONE] No test file exists for control.rs. new_window has no tests whatsoever. | [UNDOCUMENTED] No `///` doc comment on the function itself. The `EnrollmentWindow` type alias above has docs, but the constructor function has none — no description of purpose, initial state (deadline=0), or `# Examples`. (deliberated: confirmed — Tests NONE confirmed — no tests exist. Documentation UNDOCUMENTED is correct: the function is `pub` and has zero `///` doc comments. The type alias above it is documented, but the constructor itself is not. This is a legitimate gap for a public API item.) |
| `is_open` | L31–L41 | PARTIAL | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for checking enrollment window status. | [UNIQUE] Checks if enrollment open by comparing deadline to current time. Distinct semantic contract from remaining(). | [OK] Logic is correct: 0 => closed, compare now < deadline. The `.unwrap()` on `duration_since(UNIX_EPOCH)` would panic if the system clock were set before 1970, but this is practically impossible on a running system in 2026. The i64 cast of as_secs() is safe for all timestamps until year 2262. | [LEAN] Single-responsibility predicate with minimal branching. Straightforward deadline comparison against UNIX timestamp. | [NONE] No test file exists for control.rs. is_open has no tests; edge cases like deadline==0, now==deadline, now<deadline all go untested. | [PARTIAL] Has `/// Check if enrollment is currently open.` but omits description of the `window` parameter, does not document the boolean return semantics (expired deadline treated as closed), and has no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed — no test coverage for edge cases (deadline==0, now==deadline, now<deadline). Documentation PARTIAL is fair: `/// Check if enrollment is currently open.` is present but omits parameter description and return value semantics (e.g., expired deadline → false).) |
| `open_window` | L44–L51 | PARTIAL | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L116. | [UNIQUE] Opens enrollment by calculating deadline from duration. No similar functions found. | [OK] Correctly computes deadline as now + duration and stores it atomically. The `duration_secs as i64` cast is safe for any plausible enrollment duration. Same theoretical `unwrap()` concern as is_open; inconsequential in practice. | [LEAN] Minimal setter that computes deadline and stores it atomically. No unnecessary indirection. | [NONE] No test file exists for control.rs. open_window has no tests; deadline arithmetic and atomic store are untested. | [PARTIAL] Has `/// Open enrollment for \`duration_secs\` seconds.` which references one parameter inline, but does not describe the `window` parameter, does not document overflow/wrap behavior for large durations, and has no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL accurate: the inline reference to `duration_secs` is helpful but `window` parameter and overflow behavior are undocumented. Both findings stand.) |
| `close_window` | L54–L56 | PARTIAL | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L122. | [UNIQUE] Trivial function storing zero to close window. No similar functions found. | [OK] Atomically stores 0 to signal closed state. Correct and complete. | [LEAN] One-liner that stores the sentinel value 0. As minimal as it gets. | [NONE] No test file exists for control.rs. close_window has no tests. | [PARTIAL] Has `/// Close enrollment immediately.` — minimal but present. Missing `window` parameter description and no `# Examples` section for a public API function. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL is technically correct — `/// Close enrollment immediately.` is present and clear for such a trivial function, but missing `window` param docs means it doesn't meet full Rust doc standards. Both findings stand.) |
| `remaining` | L59–L73 | PARTIAL | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also called internally by handle_client at L128. | [UNIQUE] Returns seconds remaining in window. Different semantic contract from is_open(). | [OK] Guards against the past-deadline case before casting to u64, preventing underflow. Logic is correct. Same theoretical unwrap concern on duration_since as the other time-reading functions. | [LEAN] Simple arithmetic to compute remaining seconds. The UNIX timestamp snippet is repeated from is_open/open_window but that is a minor DRY gap, not overengineering. Logic is flat and readable. | [NONE] No test file exists for control.rs. remaining has no tests; branches for deadline==0, now>=deadline, and now<deadline are all untested. | [PARTIAL] Has `/// Seconds remaining in the enrollment window. 0 if closed.` which covers the return value semantics. Missing `window` parameter description, no mention of behavior when deadline is in the past vs exactly now, and no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed — multiple branches (deadline==0, now>=deadline, now<deadline) all untested. Documentation PARTIAL is fair: return semantics are documented (`0 if closed`) but parameter description is missing. Both findings hold.) |
| `start_listener` | L77–L104 | PARTIAL | 78% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Substantial function that sets up socket listener and spawns background thread. | [UNIQUE] Initializes Unix socket listener with permissions and spawns handler thread. No similar functions found. | [NEEDS_FIX] The single background thread handles connections sequentially via `handle_client`, which loops over `reader.lines()` with no server-side read timeout set on the accepted stream. A client that connects but never sends data (or never closes the connection) will block the thread inside `handle_client` indefinitely, making the control socket unresponsive to all subsequent `OPEN`/`STATUS` commands for the lifetime of the server. `send_command` sets a client-side read timeout but there is no corresponding server-side write or read timeout, so a stalled or malicious client can permanently wedge the listener. | [LEAN] Appropriately complex for spawning a UNIX socket listener: stale-socket cleanup, bind, chmod, and a single background thread. Each step is necessary. No layers of abstraction around what is inherently a multi-step OS setup. | [NONE] No test file exists for control.rs. start_listener has no tests; socket binding, stale-socket removal, permission setting, and background thread spawning are all untested. | [PARTIAL] Has two `///` lines describing purpose and return value. Missing descriptions for both `window` and `peer_count` parameters, no documentation of error conditions (`io::Error` cases), no mention of the background thread lifecycle or stale-socket cleanup, and no `# Examples` section. (deliberated: confirmed — Correction NEEDS_FIX is valid: the single background thread processes connections sequentially via `handle_client`, which blocks on `reader.lines()` with no server-side read timeout. A stalled or malicious client on the Unix socket permanently wedges the listener. The fix (set_read_timeout on the accepted stream or spawn per-connection) is small-effort. However, this is a local Unix domain socket for a system daemon, not internet-facing, which somewhat limits the practical attack surface — keeping confidence moderate at 78 rather than high. Tests NONE and documentation PARTIAL both confirmed.) |
| `send_command` | L147–L166 | PARTIAL | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public API for sending control commands to the socket. | [UNIQUE] Connects to socket, sends command, reads response. Inverse role of handle_client(). No similar functions found. | [OK] Correctly connects, sets a 5-second read timeout, writes cmd + newline, flushes, and reads exactly one response line. The response will contain a trailing newline, which callers may need to trim, but that is a caller concern, not a correctness defect. No write timeout is set, but for a Unix socket with a local daemon and small payloads this is not a practical problem. | [LEAN] Minimal client-side utility: connect, write, read one line, return. Correctly wraps the connect error with a user-facing message. No unnecessary abstraction for what is a single-shot RPC. | [NONE] No test file exists for control.rs. send_command has no tests; connection failure, timeout, write, and response-read paths are all untested. | [PARTIAL] Has `/// Send a command to the running server via the control socket.` but omits: description of the `cmd` format (expected protocol strings), what the returned `String` contains (server's response line), documented error conditions (connection refused, timeout), and no `# Examples` section demonstrating usage. (deliberated: confirmed — Tests NONE confirmed — connection failure, timeout, write, and response-read paths are all untested. Documentation PARTIAL is accurate: the doc comment describes the purpose but omits the expected `cmd` format, return value contents, and error conditions. Both findings hold.) |
| `cleanup` | L169–L171 | PARTIAL | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Utility function for socket file cleanup. | [UNIQUE] Trivial function removing socket file. No similar functions found. | [OK] Silently removes the socket file. Ignoring the result is intentional and correct for cleanup paths. | [LEAN] Trivial one-liner wrapper around remove_file. Exists solely to give callers a semantic name at the call site, which is appropriate. | [NONE] No test file exists for control.rs. cleanup has no tests. | [PARTIAL] Has `/// Cleanup: remove the socket file.` which is minimal but conveys purpose. Missing description of the `path` parameter, no note that errors are silently ignored (`let _ = ...`), and no `# Examples` section. (deliberated: confirmed — Tests NONE confirmed. Documentation PARTIAL accurate: `/// Cleanup: remove the socket file.` is present but doesn't note that errors are silently ignored (`let _ = ...`), which is a deliberate design choice callers should be aware of. Both findings stand.) |

### `rustguard-daemon/src/config.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `Config` | L13–L16 | PARTIAL | 72% | [USED] Main exported struct holding parsed WireGuard configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Public struct with unique field composition (interface, peers); no duplicate definitions found | [NEEDS_FIX] Config::parse (L120-L122) sets current_section to 'interface' but never flushes current_peer_kvs when a [Interface] header arrives while parsing a [Peer] section. If any [Peer] block precedes [Interface], its accumulated KVs are not cleared; the next [Peer] header's flush guard (current_section == Some('peer')) is then false, so those stale KVs merge with the next peer's KVs. The end-of-file flush emits a single peer that silently drops the first peer's data (last-writer-wins for duplicated keys like PublicKey). | [LEAN] Minimal two-field struct wrapping exactly the two top-level sections of a wg0.conf file. No unnecessary indirection. | [WEAK] Note: tests ARE present — embedded in the same file under #[cfg(test)], not a separate test file. `parse` is exercised by `parse_full_config` and `parse_v6_allowed_ips` covering the happy path with two peers. However, `from_file` is never tested, and error paths (malformed lines, keys outside sections, missing [Interface]) are entirely absent. | [PARTIAL] Struct has `/// Parsed WireGuard configuration.` at line 12, but fields `interface` and `peers` carry no individual `///` docs. The `from_file` and `parse` methods in the impl block (lines ~94–165) also have zero doc comments and no `# Examples` sections, which are expected on public API methods. (deliberated: confirmed — Correction NEEDS_FIX is valid: inspecting Config::parse L120-122, when [Interface] is encountered after a [Peer], current_peer_kvs is not flushed — the flush guard at L125 only triggers inside the [Peer] branch when current_section is already 'peer'. This means [Peer]-before-[Interface] ordering silently merges stale KVs. While WireGuard configs conventionally place [Interface] first, the parser doesn't enforce this and fails silently. Additionally, L163-165 wraps interface in Some() then immediately unwraps via ok_or_else — a dead branch noted in best_practices. Tests WEAK is accurate: parse_full_config and parse_v6_allowed_ips cover happy paths only; from_file is untested; error paths (malformed lines, keys outside sections, missing [Interface]) are absent. Documentation PARTIAL is accurate: struct has a one-line doc comment but fields and methods lack individual docs.) |
| `InterfaceConfig` | L19–L25 | UNDOCUMENTED | 95% | [USED] Exported struct for WireGuard interface configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] WireGuard interface configuration struct; no similar type definitions found | [OK] Struct definition is correct. All field types are appropriate for their semantic roles. | [LEAN] Five fields map 1-to-1 to WireGuard [Interface] keys. Optional v6 address tuple is the simplest representation that avoids a second struct. | [GOOD] Pure data struct with no runtime behaviour; GOOD by the type/interface default rule. Fields are also validated indirectly via parse_full_config (listen_port, address, netmask asserted explicitly). | [UNDOCUMENTED] Public struct with no `///` doc comment on the type or on any of its five public fields (`private_key`, `listen_port`, `address`, `netmask`, `address_v6`). Field semantics (e.g., the optional IPv6 tuple layout) are non-obvious and warrant documentation. (deliberated: confirmed — Documentation UNDOCUMENTED is confirmed: the struct at L18 has no /// doc comment, and none of the five public fields have docs. The address_v6 field's Option<(Ipv6Addr, u8)> tuple semantics are particularly non-obvious. This is a public, exported type used by three workspace crates.) |
| `PeerConfig` | L28–L34 | UNDOCUMENTED | 95% | [USED] Exported struct for WireGuard peer configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] WireGuard peer configuration struct; no duplicate definitions found | [OK] Struct definition is correct. Optional fields are properly typed. | [LEAN] Flat struct mirroring [Peer] section fields exactly. Optionality on each field reflects the WireGuard spec accurately. | [GOOD] Pure data struct with no runtime behaviour; GOOD by default. parse_full_config validates endpoint, allowed_ips, persistent_keepalive, and optional absence of endpoint for peer2. | [UNDOCUMENTED] Public struct with no `///` doc comment on the type or on any of its five public fields. Fields like `preshared_key` and `persistent_keepalive` carry semantics that are not self-evident from names alone. (deliberated: confirmed — Documentation UNDOCUMENTED is confirmed: no /// doc comment on the struct (L27) or any of its five public fields. Fields like preshared_key (optional PSK for additional layer of security) and persistent_keepalive (NAT traversal interval) have non-obvious semantics. Public, exported type used by three workspace crates.) |
| `CidrAddr` | L38–L41 | PARTIAL | 85% | [USED] Exported struct with three utility methods (contains_v4, contains_v6, contains). Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] CIDR address representation struct; no similar type definitions found | [OK] Struct definition is correct. The contains_v4 and contains_v6 methods use proper bitmask arithmetic; the prefix_len==0 early-return, the >=32 / >=128 caps, and the shift expressions are all correct for their respective address families. | [ACCEPTABLE] Struct itself is minimal. Exposing both typed helpers (contains_v4, contains_v6) and the polymorphic contains() is mildly redundant but useful for callers with already-typed addresses. The ipnetwork crate (popular on crates.io) would eliminate the hand-rolled bit-mask arithmetic in the impl block, but the custom implementation is short and correct so the redundancy is not a real problem. | [WEAK] contains_v4 is well-covered: /24, /32 (host), /0 (default route), and cross-family rejection are all tested. contains_v6 only covers /64; the boundary cases prefix_len=0 (default route) and prefix_len=128 (host) are not tested for v6, unlike v4. The `contains()` dispatch method is never directly exercised. The Display impl is untested. | [PARTIAL] Struct has `/// An IP address with prefix length (CIDR notation). Supports v4 and v6.` at line 37, and all three methods (`contains_v4`, `contains_v6`, `contains`) have one-line `///` comments. However, the two public fields (`addr`, `prefix_len`) lack `///` docs, and none of the methods include a `# Examples` section despite being public API. (deliberated: confirmed — Overengineering ACCEPTABLE is fair: exposing both typed helpers and the polymorphic contains() is mildly redundant but practical for callers. Tests WEAK confirmed: v4 has excellent boundary coverage (/0, /24, /32, cross-family rejection) but v6 only covers /64 — missing /0 and /128 boundary cases. The contains() dispatcher and Display impl are also untested. Documentation PARTIAL confirmed: struct-level doc exists at L37 and methods have one-line docs, but fields addr and prefix_len lack individual docs and no #[doc] examples exist. Raised confidence slightly since all findings are directly verifiable in source.) |
| `prefix_to_netmask` | L284–L292 | UNDOCUMENTED | 88% | [USED] Exported function converting prefix length to IPv4 netmask. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Converts IPv4 CIDR prefix length to netmask; no similar utility functions found | [OK] Correctly handles all three regions: prefix==0 returns 0.0.0.0, prefix>=32 returns 255.255.255.255, and the general case uses u32::MAX << (32-prefix) which is safe for prefix in 1..31. Ipv4Addr::from(u32) converts via to_be_bytes(), so byte ordering is correct on all platforms. | [LEAN] Three-branch bit-shift helper converting a prefix length to an Ipv4Addr mask. Handles the two edge cases (0 and >=32) explicitly to avoid undefined-behavior in shift arithmetic. Appropriately minimal. | [WEAK] Only implicitly tested via parse_full_config which asserts netmask==255.255.255.0 for a /24 address. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised. No direct unit tests exist for this exported function despite being imported by three callers. | [UNDOCUMENTED] Public function (`pub fn`) with no `///` doc comment, no parameter description, no description of boundary behaviour for `prefix == 0` or `prefix >= 32`, and no `# Examples` section. Expected on public API. (deliberated: confirmed — Tests WEAK confirmed: only indirectly tested via parse_full_config asserting netmask==255.255.255.0 for /24. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised despite being explicit code paths. No direct unit tests exist for this exported public function. Documentation UNDOCUMENTED confirmed: pub fn at L284 has no /// doc comment, no parameter description, and no explanation of boundary behavior. This is a public API imported by three workspace crates and warrants documentation. Slightly raised confidence.) |

### `rustguard-enroll/src/protocol.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ENROLL_REQUEST_SIZE` | L17–L17 | UNDOCUMENTED | 92% | [USED] Exported constant imported by 2 runtime files (client.rs, server.rs). Defines buffer size for enrollment requests (76 bytes). | [UNIQUE] Numeric constant; no duplication detected | [OK] magic(4) + nonce(24) + encrypted(32 pubkey + 16 tag)(48) = 76. Arithmetic is correct. | [LEAN] Named size constant derived directly from the documented wire format. Prevents magic numbers at call sites. | [GOOD] Constant; GOOD by default. Implicitly validated by build_request (return type [u8; ENROLL_REQUEST_SIZE]) and parse_request length guard exercised in roundtrip tests. The short-buffer branch of parse_request is untested, but as a constant itself it has no further runtime behavior. | [UNDOCUMENTED] Public constant with no `///` doc comment. The module-level `//!` block does describe the 76-byte wire format, but the constant itself carries no inline doc. Public API items should have their own `///` comment linking the value to its purpose. (deliberated: confirmed — Public constant with no `///` doc comment. The module-level `//!` block describes the wire format but the constant itself is unannotated. For a public API item, UNDOCUMENTED is correct.) |
| `ENROLL_RESPONSE_SIZE` | L18–L18 | UNDOCUMENTED | 92% | [USED] Exported constant imported by 2 runtime files (client.rs, server.rs). Defines buffer size for enrollment responses (81 bytes). | [UNIQUE] Numeric constant; no duplication detected | [OK] magic(4) + nonce(24) + encrypted(32 + 4 + 1 + 16 tag)(53) = 81. Arithmetic is correct. | [LEAN] Same rationale as ENROLL_REQUEST_SIZE. Both sizes are referenced in the module-level wire-format comment, so the constants are clearly grounded. | [GOOD] Constant; GOOD by default. Implicitly validated by build_response (return type) and parse_response length guard exercised in response_roundtrip. The short-buffer branch is untested, but as a constant it has no independent runtime behavior. | [UNDOCUMENTED] Public constant with no `///` doc comment. Same gap as ENROLL_REQUEST_SIZE. The module-level docs cover the 81-byte breakdown, but the constant itself is completely unannotated. (deliberated: confirmed — Same situation as ENROLL_REQUEST_SIZE — public constant without its own `///` doc comment. UNDOCUMENTED is correct and consistent.) |
| `derive_token_key` | L22–L24 | PARTIAL | 88% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Core function that derives encryption key from enrollment token. | [UNIQUE] Trivial single-statement function; no similar implementations found | [OK] Passes a fixed domain string and the token bytes as separate slices to crypto::hash. The &[u8; 19] literal coerces to &[u8] in the array context, producing a [&[u8]; 2] that satisfies a &[&[u8]] parameter. Domain separation is sufficient since the 19-byte prefix is fixed. | [LEAN] One-liner KDF with a domain-separation prefix. Thin, purposeful wrapper that names its intent clearly. | [GOOD] Exercised in all four inline tests. request_wrong_token_fails demonstrates that distinct tokens produce distinct keys (different outputs decrypt to None), which is the primary security property. Happy path and key-differentiation behavior both covered. | [PARTIAL] Has two `///` lines describing purpose and the BLAKE2s rationale. However, the `token` parameter is not described (e.g., expected format, max length), the return value type `[u8; 32]` is not explained, and there is no `# Examples` section expected on a public function. (deliberated: confirmed — Has two `///` lines covering purpose and crypto rationale, but token parameter semantics and return type are undocumented. PARTIAL is accurate for a public function with incomplete parameter docs.) |
| `build_request` | L27–L36 | PARTIAL | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Constructs encrypted enrollment request messages. | [UNIQUE] Structurally similar to build_response (similarity: 0.738) but semantically distinct: takes raw pubkey instead of EnrollmentOffer, uses ENROLL_REQUEST_MAGIC not ENROLL_RESPONSE_MAGIC, indices [28..76] not [28..81], and performs no intermediate plaintext construction. Different parameters and invariants make this unique despite structural overlap. | [OK] Nonce is 24 bytes placed at buf[4..28], encrypted (32-byte plaintext + 16-byte AEAD tag = 48 bytes) placed at buf[28..76]. All slice bounds are correct and consistent with ENROLL_REQUEST_SIZE=76. copy_from_slice will panic only if xseal returns an unexpected length, which would be a bug in the crypto crate, not here. | [LEAN] Directly encodes the documented 76-byte wire format: magic + nonce + ciphertext. No unnecessary layers or generics. | [GOOD] Tested via request_roundtrip (correct wire bytes decrypt successfully), request_wrong_token_fails (output is unreadable under wrong key), and tampered_request_rejected (single byte flip detected). Happy path and integrity properties are well covered; inputs are fixed-size arrays so boundary testing adds little value. | [PARTIAL] Single-line `///` comment 'Build an enrollment request.' is present but minimal. No parameter descriptions for `token_key` or `our_pubkey`, no explanation of the returned byte array structure, and no `# Examples` section for this public API. (deliberated: confirmed — Single-line `///` doc present but no parameter descriptions for token_key/our_pubkey and no explanation of the returned byte array's wire layout. PARTIAL is correct. Slight confidence bump since the wire format IS documented at module level, making the gap less severe.) |
| `parse_request` | L40–L51 | PARTIAL | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment request messages. | [UNIQUE] Structurally similar to parse_response (similarity: 0.759) but semantically distinct: returns Option<[u8;32]> not Option<EnrollmentOffer>, uses ENROLL_REQUEST_MAGIC not ENROLL_RESPONSE_MAGIC, indices [28..76] not [28..81], and performs simple try_into conversion instead of complex struct construction. Different return types and contracts make this unique. | [OK] Length and magic checks are performed before any indexing. Nonce extracted from buf[4..28], ciphertext from buf[28..76] (48 bytes matching the expected seal output). xopen result is converted to [u8; 32] via try_into, which fails safely if the plaintext length differs. Logic is correct. | [LEAN] Straightforward length-and-magic validation followed by decryption. Returns Option which is idiomatic Rust for fallible parsing; no excess abstraction. | [WEAK] Happy path, wrong-token AEAD failure, and ciphertext tampering are all tested. However, the short-buffer early return (buf.len() < ENROLL_REQUEST_SIZE) and the wrong-magic early return (buf[0..4] != ENROLL_REQUEST_MAGIC, e.g. feeding a response packet to the request parser) are not exercised. Two None-returning branches are untested. | [PARTIAL] Two `///` lines describe purpose and return value briefly. Missing: parameter descriptions for `token_key` and `buf`, failure conditions for returning `None` (bad magic, AEAD failure, length mismatch), and `# Examples` section. (deliberated: confirmed — Tests: Three tests cover happy path, wrong-token AEAD failure, and ciphertext tampering — good for the crypto paths. However, the short-buffer guard (L42-44) and wrong-magic guard (L45-47) are untested, leaving two early-return branches uncovered. WEAK is fair but borderline — the crypto-critical paths are well tested while only simple guard clauses are missed. Documentation: Two `///` lines present but missing parameter descriptions and None-returning conditions. PARTIAL confirmed.) |
| `EnrollmentOffer` | L54–L58 | PARTIAL | 88% | [USED] Exported struct imported by 2 runtime files (client.rs, server.rs). Core data structure for enrollment response payload. | [UNIQUE] Struct definition; no similar type definitions found | [OK] Plain data struct with correct field types for server_pubkey ([u8;32]), assigned_ip (Ipv4Addr), and prefix_len (u8). No logic issues. | [LEAN] Minimal plain-data struct grouping exactly the three fields carried in the response payload. No unnecessary generics, traits, or builder patterns. | [GOOD] Plain data struct with no runtime behavior; GOOD by default. All three fields are constructed and asserted in response_roundtrip, confirming correct field layout and types. | [PARTIAL] The struct has a `///` comment describing its role as an enrollment response payload. However, none of the three public fields (`server_pubkey`, `assigned_ip`, `prefix_len`) carry individual `///` doc comments. Per Rust doc conventions, public struct fields should each be documented. (deliberated: confirmed — Struct has a `///` doc comment but all three public fields (server_pubkey, assigned_ip, prefix_len) lack individual `///` comments. For a public struct with public fields, PARTIAL is accurate. The field names are reasonably self-descriptive which limits the practical impact.) |
| `build_response` | L61–L75 | PARTIAL | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Constructs encrypted enrollment response messages. | [UNIQUE] Structurally similar to build_request (similarity: 0.738) but semantically distinct: accepts EnrollmentOffer not raw pubkey, constructs intermediate plaintext payload from struct fields, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, and uses indices [28..81] not [28..76]. Different parameters and payload logic make this unique despite structural overlap. | [OK] Plaintext is correctly assembled: server_pubkey at [0..32], ip octets at [32..36], prefix_len at [36]. xseal of 37-byte plaintext produces 53 bytes (37+16 tag), placed at buf[28..81]. All offsets match ENROLL_RESPONSE_SIZE=81. | [LEAN] Serialises EnrollmentOffer into the 81-byte wire frame. Fixed-size arithmetic matches the documented layout; no abstraction overhead. | [WEAK] Only response_roundtrip covers build_response; there is no analogue of request_wrong_token_fails (verifying that a response built with key A is unreadable under key B) nor a tampered_response_rejected test. A single happy-path roundtrip is insufficient for a security-critical wire-format builder. | [PARTIAL] Single-line `///` comment 'Build an enrollment response.' No description of the `token_key` or `offer` parameters, no explanation of what the returned byte array contains, and no `# Examples` section on this public function. (deliberated: confirmed — Tests: Only the response_roundtrip happy path covers build_response. No analogue of request_wrong_token_fails or tampered_request_rejected exists for the response path. This asymmetry is a genuine coverage gap for security-critical code. WEAK is well-justified. Documentation: Single-line `///` comment with no parameter or return descriptions. PARTIAL confirmed.) |
| `parse_response` | L78–L102 | PARTIAL | 86% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment response messages. | [UNIQUE] Structurally similar to parse_request (similarity: 0.759) but semantically distinct: returns Option<EnrollmentOffer> not Option<[u8;32]>, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, indices [28..81] not [28..76], includes plaintext length validation, and constructs complex EnrollmentOffer from decomposed bytes. Different return contracts and invariants make this unique. | [OK] Length and magic guards are in place before indexing. Nonce from buf[4..28], ciphertext from buf[28..81] (53 bytes). The explicit plaintext.len() != 37 check is redundant given a correctly functioning AEAD but is not harmful. Fields are extracted at correct offsets. All None-returning paths use ? or explicit return None. | [LEAN] Mirrors parse_request in structure; the extra plaintext-length check (L90) is a defensive sanity check appropriate for a security-sensitive protocol, not gold-plating. | [WEAK] Only the happy-path roundtrip is tested. Missing: wrong-token AEAD failure, ciphertext tampering, short-buffer early return, wrong-magic early return (e.g. feeding a request packet), and the explicit plaintext.len() != 37 guard. Four out of five failure branches are entirely untested, contrasting with the more thorough coverage of parse_request. | [PARTIAL] Single-line `///` comment 'Parse and decrypt an enrollment response.' Missing parameter descriptions, no enumeration of `None`-returning conditions (bad magic, AEAD failure, wrong plaintext length), and no `# Examples` section for a public function. (deliberated: confirmed — Tests: Only the happy-path roundtrip is tested. Four of five failure branches (wrong-token, tampering, short-buffer, wrong-magic, plaintext length check at L90) are entirely untested. This is the weakest test coverage in the file and contrasts sharply with parse_request's coverage. WEAK is clearly correct. Documentation: Single-line `///` with no parameter descriptions or enumeration of None-returning conditions. PARTIAL confirmed.) |

### `rustguard-tun/src/linux.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `create` | L78–L122 | UNDOCUMENTED | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for platform module but likely re-exported by parent, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.856 with macos.rs but different semantic contract — Linux opens /dev/net/tun with TUNSETIFF; macOS opens AF_SYSTEM socket with CTLIOCGINFO and connect | [OK] Logic is sound: opens /dev/net/tun, issues TUNSETIFF with the correctly-sized IfreqFlags (40 bytes), extracts the kernel-assigned name via UTF-8 validated scan for the first NUL, and delegates address configuration. All early-exit paths close the fd before returning. | [LEAN] Three-step sequential logic (open device, TUNSETIFF, delegate to configure_interface) with clean resource cleanup on every failure path; no unnecessary abstraction layers. | [NONE] No test file found for rustguard-tun/src/linux.rs. create has zero test coverage. The primary public entry point for TUN device creation is entirely untested, including fd open failure, TUNSETIFF failure, UTF-8 name extraction, and configure_interface error paths. | [UNDOCUMENTED] Public function (`pub fn create`) with no `///` doc comment whatsoever. As the primary public entry point for creating a Linux TUN device it requires at minimum a purpose description, parameter docs for `config`, return value docs, error conditions, and an `# Examples` section. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD → USED: this is the primary public entry point of a platform-specific library module (linux.rs). It's exported for consumption by the parent crate module (likely conditionally compiled via cfg(target_os)). Classic false positive for library crate public APIs with no in-crate callers. Tests NONE and documentation UNDOCUMENTED are both valid — no test file exists and the public function has zero /// doc comments despite being the main API entry point.) |
| `read` | L193–L199 | PARTIAL | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for TUN read but likely re-exported by parent module, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.777 below threshold (0.82); macos.rs read() has different logic (strips 4-byte AF header), this version reads directly | [OK] libc::read returns ssize_t; negative check is correct; n as usize is safe after the guard. | [LEAN] Minimal safe wrapper around libc::read translating return value to io::Result<usize>; appropriate boundary between safe and unsafe code. | [NONE] No test file found for rustguard-tun/src/linux.rs. read has zero test coverage. The error path (n < 0) and successful read path are both untested. | [PARTIAL] Public function has two `///` lines describing purpose and the IFF_NO_PI behaviour. Missing: `fd` and `buf` parameter descriptions, explicit return-value docs (number of bytes read), error conditions, and a `# Examples` section expected on public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: public library API, same rationale as create. Documentation reclassified UNDOCUMENTED → PARTIAL: source code at lines 193-194 has two /// doc comment lines describing purpose and IFF_NO_PI behavior. Missing parameter docs and return value docs, hence PARTIAL not DOCUMENTED. Tests NONE retained — no test file exists.) |
| `write` | L203–L212 | PARTIAL | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for TUN write but likely re-exported by parent module, matching known false positive pattern for library crates | [UNIQUE] RAG score 0.830 meets threshold but source code has different semantic contract — linux.rs writes packet directly; macos.rs prepends AF family header and adjusts return value | [OK] Empty-packet guard is correct. libc::write return value is checked for < 0 before casting to usize. No correctness issues. | [LEAN] Safe wrapper around libc::write with a cheap empty-packet guard; the guard prevents a kernel write(2) call that would return an error anyway, making the error message clearer. | [NONE] No test file found for rustguard-tun/src/linux.rs. write has zero test coverage. The empty-packet guard (InvalidInput error), the libc::write failure path, and the happy path are all untested. | [PARTIAL] Public function has two `///` lines covering purpose and the no-header contract. Missing: `fd` and `packet` parameter descriptions, return-value docs (bytes written), error conditions (documents the empty-packet check implicitly but not explicitly), and a `# Examples` section. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: public library API, same rationale as create and read. Documentation reclassified UNDOCUMENTED → PARTIAL: source code at lines 203-204 has two /// doc comment lines covering purpose and no-header contract. Missing parameter docs and error condition docs, hence PARTIAL. Tests NONE retained.) |

### `rustguard-tun/src/bpf_loader.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `XdpProgram` | L37–L41 | PARTIAL | 88% | [DEAD] Exported public struct with 0 runtime importers and 0 type-only importers; no other files import or use it. | [UNIQUE] Public struct holding prog_fd, xsks_map_fd, and ifindex. Single definition with unique semantic contract. | [NEEDS_FIX] load_and_attach leaks file descriptors on partial failure. xsks_map_fd is created at L51 but if parse_and_patch_elf (L54), bpf_prog_load (L58), or attach_xdp (L63) subsequently return an error, the function exits via ? before the XdpProgram value is constructed, so its Drop impl never runs and the open kernel fds are never closed. Similarly, if bpf_prog_load succeeds but attach_xdp fails, both xsks_map_fd and prog_fd are leaked. | [LEAN] Clean RAII struct with 3 fields, a constructor, one accessor method, and a Drop impl that properly detaches and closes fds. No unnecessary abstraction. | [NONE] No test file found. The struct and its methods (load_and_attach, register_xsk, Drop) have zero test coverage. Critical paths like XDP attachment, XSKMAP registration, and fd cleanup on drop are entirely untested. | [PARTIAL] Has a struct-level `/// A loaded XDP program with its XSKMAP.` doc comment. However, public fields `prog_fd` and `xsks_map_fd` have no `///` field-level documentation explaining their semantics (e.g., that they are Linux file descriptors, what map type xsks_map_fd represents). No `# Examples` section exists. Qualifies as PARTIAL. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX confirmed: the fd leak in load_and_attach is real — xsks_map_fd (and later prog_fd) can leak if subsequent steps fail via ?. Utility reclassified DEAD→USED: XdpProgram is a pub struct and the primary public API of this BPF loader module. Being exported from a library sub-module means it's intended for consumption by the parent crate or external users. The 0-importer scan likely didn't cover the full crate workspace or the struct is newly integrated. Documentation reclassified UNDOCUMENTED→PARTIAL: the struct has a `/// A loaded XDP program with its XSKMAP.` doc comment at line 37, so it's not fully undocumented; however field-level docs are missing, making PARTIAL accurate. Tests NONE confirmed.) |

### `rustguard-crypto/src/aead.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `AEAD_TAG_LEN` | L7–L7 | UNDOCUMENTED | 92% | [USED] Exported constant with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Simple constant definition. No similar constants found in RAG search results. | [OK] Poly1305 authentication tag is always 16 bytes; constant is correct. | [LEAN] Simple named constant for the 16-byte Poly1305 authentication tag length. Appropriate and necessary for clarity. | [GOOD] Pure constant with no runtime behavior; additionally referenced directly in the seal_then_open assertion: `assert_eq!(ciphertext.len(), plaintext.len() + AEAD_TAG_LEN)`. Rule 6 applies. | [UNDOCUMENTED] Public constant has no `///` doc comment. The name implies a 16-byte Poly1305 tag, but there is no inline documentation explaining its purpose or relationship to the AEAD scheme. (deliberated: confirmed — Documentation UNDOCUMENTED is correct — the public constant has no `///` doc comment at all. While the name `AEAD_TAG_LEN` is reasonably descriptive, for a public exported constant in a crypto library, a one-line doc comment stating it's the Poly1305 tag length is warranted. All other axes are clean and confirmed.) |
| `seal` | L16–L28 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Encrypts with ChaCha20-Poly1305, returns Vec<u8>. Similar functions exist but with different semantic contracts: xseal uses XChaCha (different cipher), seal_to writes in-place (different output mechanism), noise.rs seal uses unsafe FFI (different implementation strategy). Each has distinct parameters and invariants. | [OK] Key, nonce, and Payload wiring are all correct. ChaCha20-Poly1305 encrypt never fails for in-memory plaintext of any length, so the expect is safe. | [LEAN] Thin, focused wrapper around chacha20poly1305::encrypt with WireGuard-specific nonce construction. Does exactly one thing with no unnecessary abstraction. | [GOOD] Covered by seal_then_open (happy path, verifies ciphertext length and plaintext round-trip), wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, and aad_mismatch_fails. Both the output length guarantee and authentication properties are exercised. Minor gap: no test with non-empty AAD on the encrypting side combined with a round-trip check, but coverage is substantive. | [PARTIAL] Has a two-line doc comment explaining the counter-to-nonce mapping and AAD usage. Missing formal parameter descriptions (`key`, `counter`, `aad`, `plaintext`), return-value documentation, and a `# Examples` section expected on public API functions. (deliberated: confirmed — Documentation PARTIAL is fair. The two-line doc comment explains counter-to-nonce mapping and AAD role, which is valuable, but a public crypto API function should document its parameters and return type. Missing param descriptions for `key`, `counter`, `aad`, `plaintext` is a legitimate gap. All other axes are clean.) |
| `open` | L33–L45 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Decrypts with ChaCha20-Poly1305, returns Option<Vec<u8>>. Similar functions exist but with different semantic contracts: xopen uses XChaCha, open_to writes in-place, noise.rs open uses unsafe FFI. Different semantic invariants and caller expectations. | [OK] Decryption path mirrors seal exactly; authentication failures are correctly surfaced as None via .ok(). | [LEAN] Mirror of seal for decryption, converting auth failure to None. Clean, minimal, and appropriate. | [GOOD] Exercised by seal_then_open (happy path, returns correct plaintext), wrong_key_fails, wrong_counter_fails, tampered_ciphertext_fails, and aad_mismatch_fails — all four authentication-failure modes are validated. The Option<Vec<u8>> return contract is well tested. | [PARTIAL] Has a two-line doc comment covering purpose and the `None` failure case. Missing formal parameter descriptions, no `# Examples` section. The error conditions listed (wrong key, tampered data, wrong nonce) are helpful but not exhaustive. (deliberated: confirmed — Documentation PARTIAL is fair. Similar to seal — the doc comment explains purpose and None failure case well, but omits formal parameter descriptions. The mention of specific failure modes (wrong key, tampered data, wrong nonce) is helpful but not exhaustive. All other axes are clean.) |
| `xseal` | L48–L54 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Encrypts with XChaCha20-Poly1305 (24-byte nonce) for cookie encryption, intentionally different from seal which uses ChaCha20-Poly1305 with 12-byte counter-derived nonce. Different cipher algorithm and nonce handling. | [OK] nonce is &[u8; 24] and XNonce is 24 bytes; from_slice will not panic. XChaCha20-Poly1305 API usage is correct. | [LEAN] Separate function for XChaCha20 cookie encryption with a 24-byte nonce is justified — it's a distinct cipher variant used for a specific protocol purpose (cookie encryption), not a redundant abstraction. | [NONE] No test in the inline test module or any referenced test file exercises xseal. Neither happy-path encryption nor authentication-failure paths are covered. | [PARTIAL] Single-line doc comment mentions 24-byte nonce and cookie-encryption use case. No parameter descriptions, no return-value documentation, and no `# Examples` section. Very terse for a public API function. (deliberated: confirmed — Tests NONE is confirmed — the test module contains no test that calls xseal, neither directly nor via a round-trip with xopen. This is a real gap for a public crypto function. Documentation PARTIAL is correct — the single-line comment is terse but at least mentions the 24-byte nonce and cookie-encryption use case. Confidence raised slightly as the test finding is unambiguous from the provided test code.) |
| `xopen` | L57–L63 | PARTIAL | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Decrypts with XChaCha20-Poly1305 (24-byte nonce) for cookie decryption, intentionally different from open which uses ChaCha20-Poly1305 with counter-derived nonce. Different cipher algorithm and nonce handling. | [OK] Symmetric counterpart to xseal; same nonce sizing guarantee applies. Authentication failure returned as None. | [LEAN] Matching decrypt for xseal. Protocol-driven distinction from open is appropriate and keeps the two cipher families clearly separated. | [NONE] No test exercises xopen. No round-trip with xseal, no wrong-key/wrong-nonce/tampered checks are present. | [PARTIAL] Single-line doc comment mirrors `xseal` but for decryption. No parameter descriptions, no documentation of `None` failure semantics beyond what is implied by the name, no `# Examples` section. (deliberated: confirmed — Tests NONE is confirmed — no test exercises xopen anywhere in the provided test module. Documentation PARTIAL mirrors xseal assessment. Both findings are well-supported by the source code.) |
| `seal_to` | L68–L84 | PARTIAL | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Encrypts ChaCha20-Poly1305 in-place (zero allocations), returns usize. Distinct semantic contract from seal: writes to pre-allocated buffer instead of allocating Vec. Different return type, different caller expectations, different invariants. | [OK] Buffer length is asserted before use. Plaintext is copied into buf, encrypted in-place, and the 16-byte tag is appended at buf[len..len+16]. Returned length is correct. Empty AAD matches the companion open_to, so the pair is internally consistent. | [LEAN] Zero-allocation in-place variant is justified for hot-path packet processing in a VPN tunnel. The additional complexity over seal is warranted by the explicit performance requirement stated in the doc comment. | [NONE] No test exercises the zero-allocation in-place encryption path. The assert!(buf.len() >= ...) panic path, correct output length, and tag placement are all untested. | [PARTIAL] Three-line doc comment explains return value (total length), buffer-size precondition, and zero-allocation property. Missing formal parameter descriptions and a `# Examples` section. The panic condition from the `assert!` is not documented in the doc comment. (deliberated: confirmed — Tests NONE is confirmed — no test in the test module exercises seal_to. The assert! panic path, output length correctness, and tag placement are all untested. Documentation PARTIAL is fair — the three-line doc comment covers return value, buffer precondition, and zero-allocation property, but the panic condition from the assert! is undocumented and param descriptions are missing.) |
| `open_to` | L89–L102 | PARTIAL | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.rs | [UNIQUE] Decrypts ChaCha20-Poly1305 in-place (zero allocations), returns Option<usize>. Distinct semantic contract from open: writes to pre-allocated buffer instead of allocating Vec. Different return type, different caller expectations, different invariants. | [OK] Guards ct_len >= AEAD_TAG_LEN and ct_len <= buf.len() before any indexing. split_at_mut(pt_len) yields exactly AEAD_TAG_LEN bytes for the tag slice, so Tag::from_slice cannot panic. Plaintext length is returned correctly on success. | [LEAN] Zero-allocation in-place decryption mirrors seal_to. The split_at_mut pattern to isolate tag bytes is idiomatic Rust and necessary for the AeadInPlace API. Appropriate complexity. | [NONE] No test exercises open_to. Neither the happy path (successful in-place decryption) nor the early-return guards (ct_len < AEAD_TAG_LEN, ct_len > buf.len()) are covered. | [PARTIAL] Three-line doc comment explains buffer layout (ciphertext+tag in, plaintext out), return value, and zero-allocation property. Missing formal parameter descriptions, no `# Examples` section, and the `ct_len < AEAD_TAG_LEN` early-return condition is not explicitly documented. (deliberated: confirmed — Tests NONE is confirmed — no test exercises open_to. Neither the happy path nor the early-return guard paths are covered. Documentation PARTIAL is fair — the doc comment explains buffer layout and zero-allocation property but omits the early-return condition for ct_len < AEAD_TAG_LEN and param descriptions.) |

### `rustguard-tun/src/macos.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `close_and_error` | L79–L83 | PARTIAL | 75% | [USED] Called 4 times to safely preserve OS error before closing file descriptor (L111, L127, L142, L165) | [DUPLICATE] Identical implementation found in linux.rs and linux_mq.rs with scores 0.977 and 0.979. Captures errno, closes fd, returns error—same logic and semantic contract across all three platform modules. | [OK] Correctly captures errno before calling close() to prevent the close() syscall from clobbering errno. | [LEAN] Solves a real, documented problem: libc::close() can clobber errno, so the error must be captured first. The comment explains the invariant clearly. Not abstraction for its own sake. | [NONE] No test file exists for this module. This private fd-cleanup helper has zero test coverage. The errno-before-close ordering is untested. | [PARTIAL] Private function with three `///` lines (lines 76–78) explaining purpose ('Capture the OS error, close the fd'), motivation ('avoids close() clobbering errno'), and a Safety precondition. Adequate for a private unsafe helper. No formal `# Parameters` or `# Examples`, but those are not expected for private functions. (deliberated: confirmed — Duplication DUPLICATE is technically accurate — identical 4-line implementations exist in linux.rs and linux_mq.rs. However, these are platform-gated modules (only one compiles per target), so deduplication into a shared utility is a low-value refactor. Keeping DUPLICATE but lowering confidence to reflect that cross-platform-module duplication is a weaker signal. Documentation PARTIAL is accurate: the three /// lines adequately document purpose and safety for a private unsafe helper.) |
| `create` | L95–L192 | UNDOCUMENTED | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. May be part of library crate public API consumed externally or by binary crates not captured in workspace analysis. Known false positives for library-entry-point functions suggest caution. | [UNIQUE] High structural similarity to linux.rs create (0.856), but platform-specific implementations differ fundamentally: macOS uses kernel control socket + connect(), Linux uses /dev/net/tun + TUNSETIFF ioctl. Different control flow, syscalls, and helper functions (configure_address vs configure_interface). Different semantic contracts for device creation methods. | [OK] Logic is correct: socket → CTLIOCGINFO → connect with sc_unit=N+1 for utunN or 0 for auto → getsockopt for name → configure address and MTU. The name_len < 2 guard correctly prevents underflow in the name_len-1 slice. The name_buf slice upper bound is safe because getsockopt caps the returned length at the supplied socklen (16). fcntl return value is unchecked but this is a non-fatal omission. | [LEAN] The six-step sequence (socket → CTLIOCGINFO → connect → getsockopt → configure_address → set_mtu) directly mirrors the Darwin kernel utun creation protocol. Each step is mandatory and the code faithfully follows the kernel contract without unnecessary layering. | [NONE] No test file exists for this module. The exported create() function requires a live macOS kernel and utun capability; integration tests would be needed. All code paths — socket creation, CTLIOCGINFO ioctl, connect, getsockopt name retrieval, configure_address delegation, set_mtu delegation, and all error branches — have zero test coverage. | [UNDOCUMENTED] Public function (`pub fn create`) with no `///` doc comment whatsoever. This is the primary public entry point for creating a utun device — the most critical symbol in the module. Missing: purpose statement, parameter semantics (`TunConfig` fields consumed), return value, error conditions (socket failure, ioctl failure, bad name format), platform requirements, and `# Examples`. Significant documentation gap. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD → USED. This is the primary public entry point of the macos.rs platform module, which is conditionally compiled via #[cfg(target_os = "macos")] and re-exported through lib.rs. Workspace analysis cannot trace cfg-gated platform module consumers — this is a textbook false positive for platform-specific library crate entry points. Tests NONE kept: requires live macOS kernel. Documentation UNDOCUMENTED confirmed: pub fn create has no /// doc comment, which is a legitimate gap for the module's main public API.) |
| `read` | L245–L263 | PARTIAL | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-specific module may be re-exported or conditionally imported; workspace analysis may miss binary crate consumers. | [UNIQUE] RAG similarity to linux read is 0.777 (below 0.82 threshold). Different semantic contract: macOS version strips utun's 4-byte AF header with buffer expansion; Linux reads directly (IFF_NO_PI means no packet info header). Validation logic differs—macOS checks minimum 4-byte header presence. | [OK] Temporary buffer is sized buf.len()+4 so payload_len = n-4 ≤ buf.len(); the copy_from_slice cannot panic. The n < 4 guard prevents underflow. Correctly strips the 4-byte utun AF header. | [LEAN] Correct and minimal implementation for stripping the utun 4-byte AF header. The temporary Vec allocation per call is a performance trade-off, not overengineering. Logic is straightforward with appropriate error handling. | [NONE] No test file exists for this module. The exported read() function contains testable logic: the 4-byte AF header stripping, the n<4 too-short guard, and the payload copy. None of these branches are covered by any test. | [PARTIAL] Public function with a single-line `///` comment ('Read an IP packet from the utun fd, stripping the 4-byte AF header'). Explains the AF-header stripping behavior, which is non-obvious. Missing: `fd` and `buf` parameter descriptions, return value semantics (the usize is the IP payload byte count), error conditions (`InvalidData` on short packet), and `# Examples` section expected for public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: same rationale as create — pub fn in a cfg-gated platform module, consumed via lib.rs re-export. Documentation reclassified UNDOCUMENTED → PARTIAL: the source clearly shows `/// Read an IP packet from the utun fd, stripping the 4-byte AF header.` at line 245. This is a meaningful single-line doc comment explaining the non-obvious AF header stripping behavior. Missing parameter/return docs keep it at PARTIAL rather than DOCUMENTED. Tests NONE accurate.) |
| `write` | L266–L296 | PARTIAL | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-specific module may be re-exported or conditionally imported; workspace analysis may miss binary crate consumers. | [UNIQUE] RAG similarity to linux write is 0.830, but logic differs significantly: macOS version inspects IP version nibble, determines AF_INET/AF_INET6, and prepends 4-byte AF header. Linux writes directly without header. macOS adjusts return value by subtracting 4 bytes. Different semantic contracts for packet transmission. | [OK] AF header [0,0,0,af] is correct for macOS utun (big-endian AF value). IP version dispatch via packet[0]>>4 is standard. saturating_sub(4) handles partial-write edge cases without panic. Empty-packet guard is correct. | [LEAN] Prepends the AF header by inspecting the IP version nibble, then writes atomically. IP version detection and AF header construction are both inherent to the utun write protocol. No unnecessary abstractions. | [NONE] No test file exists for this module. The exported write() function contains several testable branches: empty-packet rejection, IPv4 header detection (0x40 nibble), IPv6 header detection (0x60 nibble), unknown-version error, and the 4-byte AF header prepend logic. All branches have zero test coverage. | [PARTIAL] Public function with a single-line `///` comment ('Write an IP packet to the utun fd, prepending the 4-byte AF header'). Explains the AF-header prepend, which is non-obvious. Missing: `fd` and `packet` parameter descriptions, return value semantics (saturating_sub(4) meaning), error conditions (`InvalidInput` on empty packet or unknown IP version), and `# Examples` section required for public API. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD → USED: same platform-module reasoning. Documentation reclassified UNDOCUMENTED → PARTIAL: source shows `/// Write an IP packet to the utun fd, prepending the 4-byte AF header.` at line 266. Meaningful doc comment explaining the AF header prepend. Missing parameter/return/error docs keep it PARTIAL. Tests NONE accurate.) |

### `rustguard-kmod/src/cookie.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `CookieChecker` | L66–L71 | PARTIAL | 85% | [USED] Primary server-side cookie validation API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module server-side cookie validation state with public key, secret, timestamp, and under-load flag. | [OK] Struct field declarations are correct. Types are appropriate: u64 for nanosecond timestamps, [u8;32] for keys and public values. | [LEAN] Four fields encoding exactly the server-side cookie state described by the WireGuard spec. No superfluous generics, traits, or builder patterns. Methods are small and single-purpose. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, mac2_wrong_source_fails) exercise CookieChecker. Coverage is partial: secret rotation via maybe_rotate_secret() after COOKIE_SECRET_LIFETIME_NS elapses is not tested, create_reply() wire-format correctness is not independently asserted, and under_load flag transitions are not tested. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Server-side: generates cookies and validates MAC2.') but fields lack `///` comments, there are no `# Examples`, and the secret-rotation lifecycle and `under_load` flag semantics are not explained. (deliberated: confirmed — Tests WEAK confirmed: CookieChecker is exercised by multiple tests (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie) but important paths remain untested — secret rotation after COOKIE_SECRET_LIFETIME_NS, under_load flag transitions, and create_reply wire-format correctness. Documentation PARTIAL confirmed: has a one-line /// comment but fields lack documentation, no examples, and the secret rotation lifecycle and thread-safety requirements are undescribed for a security-critical pub(crate) struct.) |
| `CookieState` | L74–L77 | PARTIAL | 85% | [USED] Primary client-side cookie storage API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module client-side cookie storage with optional decrypted cookie and reception timestamp. | [OK] Option<[u8; COOKIE_LEN]> correctly models presence/absence of a stored cookie. u64 for the receipt timestamp is appropriate. | [LEAN] Two-field client-side struct: the cookie value and its timestamp. Minimal representation; freshness check is a simple subtraction. No unnecessary indirection. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, no_cookie_gives_zero_mac2) exercise CookieState. Coverage is partial: staleness expiry (is_fresh returning false after COOKIE_SECRET_LIFETIME_NS) is not tested; process_reply with an invalid/tampered encrypted payload and the false-return path is also not covered. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Client-side: stores a received cookie for MAC2.') but fields are undocumented, freshness/staleness semantics are not described, and no `# Examples` section is present. (deliberated: confirmed — Tests WEAK confirmed: CookieState is covered by several tests (cookie_roundtrip, no_cookie_gives_zero_mac2) but staleness expiry (is_fresh returning false after timeout) and process_reply with tampered payloads are untested. Documentation PARTIAL confirmed: single-line /// comment present but fields undocumented, freshness semantics unexplained, and no examples for a pub(crate) type.) |
| `constant_time_eq` | L202–L205 | PARTIAL | 80% | [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, called in verify_mac1 (104) and verify_mac2 (144). | [DUPLICATE] RAG score 0.962 with noise.rs constant_time_eq — identical implementation. Both check length equality then use wg_crypto_memneq for constant-time comparison, with same parameter passing and return logic. | [OK] Correctly delegates to the kernel's crypto_memneq, which is guaranteed constant-time and cannot be optimized away by the compiler. The early-exit length check is safe because all callers pass fixed-length 16-byte slices, so the branch carries no secret-dependent timing information. | [LEAN] Security-critical helper delegating to the kernel's crypto_memneq, which is guaranteed not to be optimized away. Extracting it into a named function documents intent and prevents accidental substitution with a non-constant-time comparison elsewhere. | [WEAK] No direct tests found. Indirectly exercised through verify_mac1() and verify_mac2() in the CookieChecker test suite (established by prior deliberation). Critical edge cases are untested: mismatched-length slices (returns false early without calling wg_crypto_memneq), equal-length but differing slices, and zero-length slices. The constant-time guarantee itself is structurally untestable at the unit level, but behavioral correctness for these edge cases is never directly asserted. | [PARTIAL] Private function has a `///` doc comment noting its constant-time guarantee via `wg_crypto_memneq`. However, parameter semantics (slice length requirement), return-value meaning, and panic/safety conditions are not described. Leniency applies as private. (deliberated: confirmed — Duplication DUPLICATE: reported as 100% identical to noise.rs constant_time_eq — both check length then delegate to wg_crypto_memneq. This is a security-critical utility that absolutely should live in a shared module to prevent divergent copies. Tests WEAK: only exercised indirectly through verify_mac1/verify_mac2; mismatched-length and zero-length edge cases never tested directly. Documentation PARTIAL: has a /// comment noting the constant-time guarantee (good) but parameter semantics and return value meaning are undescribed.) |

### `rustguard-tun/src/linux_mq.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `MultiQueueTun` | L77–L81 | PARTIAL | 90% | [DEAD] Exported public struct with 0 workspace importers per analysis, suggesting incomplete cross-crate usage capture; however, crate name (rustguard-tun) and complete pub API imply external consumption not reflected in analysis | [UNIQUE] Struct specific to multi-queue TUN feature, no RAG duplicates | [NEEDS_FIX] In MultiQueueTun::create, the additional-queues loop calls open_tun_queue(None)? which can propagate an error via ?. At that point fds (Vec<i32>) is dropped by Rust's stack unwinding, but Vec<i32>::drop only frees heap memory — it does not call libc::close on the contained file descriptors. Any fds pushed into the vector before the failing iteration are silently leaked. The ioctl-failure path in the same loop correctly closes all prior fds before returning, but the open_tun_queue failure path (handled by ?) does not. | [ACCEPTABLE] The num_queues field is mildly redundant with fds.len() — queue_fd and num_queues() could use fds.len() directly. However the struct's overall design is appropriate for multi-queue TUN management and the redundancy is harmless. | [NONE] No test file exists for this module. MultiQueueTun::create, read_queue, write_queue, queue_fd, num_queues, name, and Drop are all completely untested. This is the primary public API of the module and requires Linux kernel interaction, but neither unit nor integration tests are present. | [PARTIAL] Public struct has a `///` doc comment ('A multi-queue TUN device with N file descriptors.'). Public methods (create, num_queues, name, queue_fd, read_queue, write_queue) each carry a brief single-line `///` comment. However, no method documents parameters or return values explicitly, and no `# Examples` section exists anywhere on this public API, which is expected for public functions. Documentation is present but incomplete. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX confirmed: fd leak in additional-queues loop at L116 when open_tun_queue(None)? fails via ? operator — Vec<i32>::drop only frees heap memory, silently leaking all previously opened file descriptors. The ioctl failure path below correctly closes all prior fds, but the open failure path does not. Utility reclassified DEAD→USED: this is the primary and sole public API of a library crate (rustguard-tun); the original detail itself acknowledges 'crate name and complete pub API imply external consumption not reflected in analysis'. A library crate's main exported struct is by definition USED. Tests NONE confirmed: no test file exists for this module. Documentation reclassified UNDOCUMENTED→PARTIAL: the source clearly shows '/// A multi-queue TUN device with N file descriptors.' on the struct and all 6 public methods carry single-line `///` comments (create, num_queues, name, queue_fd, read_queue, write_queue). Documentation exists but lacks parameter/return descriptions and examples — this is PARTIAL, not UNDOCUMENTED.) |

### `rustguard-enroll/src/fast_udp.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `PKT_BUF_SIZE` | L16–L16 | PARTIAL | 80% | [USED] Public const in library crate API; used locally and by downstream consumers | [UNIQUE] Constant defining per-packet buffer size. No similar constants found. | [OK] Simple compile-time constant; 2048 bytes per packet buffer is a standard MTU-safe size for UDP datagrams. | [LEAN] Simple named constant for per-packet buffer allocation. Minimal and appropriate. | [GOOD] Pure compile-time constant with no runtime behavior; GOOD by default per type/constant rule. No test file exists, but constants require no behavioral tests. | [PARTIAL] Has `/// Per-packet buffer size.` but does not specify units (bytes), does not explain why 2048 was chosen (relation to MTU / WireGuard overhead), and gives no context for callers choosing allocation sizes. (deliberated: confirmed — Documentation PARTIAL is fair: the doc comment says `Per-packet buffer size.` but omits units (bytes) and rationale for the 2048 value (typical MTU headroom). All other axes are clean.) |
| `RecvBatch` | L19–L24 | PARTIAL | 85% | [USED] Public struct; primary type returned by recv_batch in library API | [UNIQUE] Struct for batch packet reception. No similar struct definitions found. | [OK] Struct fields and new() initializer are correct. Option<SocketAddr> implements Copy (SocketAddr is Copy), so [None; BATCH_SIZE] is valid. All fields are zero/None initialized properly. Stack size (~66 KB) is within typical default stack limits. | [LEAN] Plain data struct holding the three parallel arrays that map directly onto mmsghdr semantics (buffers, lengths, peer addrs) plus a count. No generics, no traits, no indirection — exactly what recvmmsg needs. | [NONE] No test file found for this source file. RecvBatch includes a new() constructor with non-trivial initialization (zero-filling fixed-size arrays, Option::None initialization); none of this is exercised by any test. | [PARTIAL] Struct has `/// A batch of received packets.` on the type, but all four public fields (`bufs`, `lens`, `addrs`, `count`) lack `///` doc comments. Per Rust doc conventions, a fully-documented struct requires per-field docs. No `# Examples` section is present. (deliberated: confirmed — Tests NONE is accurate — no test file exists and the struct's constructor with fixed-size array initialization is untested. Documentation PARTIAL is fair: the type-level doc exists but all four public fields lack per-field doc comments. Both findings stand.) |
| `recv_batch` | L39–L83 | PARTIAL | 80% | [USED] macOS fallback implementation; public library API for batch receives | [UNIQUE] macOS-specific recv_batch using recv_from. No similar functions found. | [OK] macOS fallback correctly receives one packet into bufs[0], populates lens[0], addrs[0], and count=1, returns 1. WouldBlock is handled by returning Ok(0) and setting count=0. Other errors are propagated. | [LEAN] macOS fallback that degrades gracefully to recv_from, filling slot 0 of the batch. Minimal code for the platform-compat path; mirrors the Linux contract exactly. | [NONE] No test file found. macOS fallback recv_batch handles the Ok path, WouldBlock path, and general error path; none are covered by any test. | [PARTIAL] Has `/// macOS fallback: receive one packet at a time.` but parameters `sock` and `batch` are undescribed, the WouldBlock → 0 return contract is not stated here (only in the Linux variant's doc), and there is no `# Examples` section on this public function. (deliberated: confirmed — macOS recv_batch (L112-126). Tests NONE is accurate — no test file covers any of the three code paths (Ok, WouldBlock, Err). Documentation PARTIAL is fair: the doc comment exists but omits parameter descriptions and the WouldBlock return contract. Both findings retained.) |
| `send_packet` | L106–L108 | UNDOCUMENTED | 85% | [USED] macOS fallback implementation; public library API for packet transmission | [UNIQUE] macOS send_packet trivial wrapper. Marked UNIQUE as trivial per RAG analysis. | [OK] Identical to the Linux variant; thin wrapper around UdpSocket::send_to with correct return type propagation. | [LEAN] macOS fallback that simply delegates to send_to. Provides a uniform module API at negligible cost; no abstraction overhead. | [NONE] No test file found. macOS send_packet is a thin wrapper over send_to; trivial but still untested. | [UNDOCUMENTED] macOS variant of `send_packet` has no `///` doc comment at all, unlike the Linux variant which at least has a terse one. Public function missing all documentation. (deliberated: confirmed — macOS send_packet at L129-131. This is correctly identified as the macOS variant and indeed has no doc comment, unlike its Linux sibling at L105. UNDOCUMENTED is accurate. Tests NONE is accurate but trivial risk given the one-line delegation. Both findings retained.) |
| `recv_batch` | L112–L126 | PARTIAL | 80% | [USED] macOS fallback implementation; public library API for batch receives | [UNIQUE] macOS-specific recv_batch using recv_from. No similar functions found. | [OK] macOS fallback correctly receives one packet into bufs[0], populates lens[0], addrs[0], and count=1, returns 1. WouldBlock is handled by returning Ok(0) and setting count=0. Other errors are propagated. | [LEAN] macOS fallback that degrades gracefully to recv_from, filling slot 0 of the batch. Minimal code for the platform-compat path; mirrors the Linux contract exactly. | [NONE] No test file found. macOS fallback recv_batch handles the Ok path, WouldBlock path, and general error path; none are covered by any test. | [PARTIAL] Has `/// macOS fallback: receive one packet at a time.` but parameters `sock` and `batch` are undescribed, the WouldBlock → 0 return contract is not stated here (only in the Linux variant's doc), and there is no `# Examples` section on this public function. (deliberated: confirmed — macOS recv_batch (L112-126). Tests NONE is accurate — no test file covers any of the three code paths (Ok, WouldBlock, Err). Documentation PARTIAL is fair: the doc comment exists but omits parameter descriptions and the WouldBlock return contract. Both findings retained.) |
| `send_packet` | L129–L131 | UNDOCUMENTED | 85% | [USED] macOS fallback implementation; public library API for packet transmission | [UNIQUE] macOS send_packet trivial wrapper. Marked UNIQUE as trivial per RAG analysis. | [OK] Identical to the Linux variant; thin wrapper around UdpSocket::send_to with correct return type propagation. | [LEAN] macOS fallback that simply delegates to send_to. Provides a uniform module API at negligible cost; no abstraction overhead. | [NONE] No test file found. macOS send_packet is a thin wrapper over send_to; trivial but still untested. | [UNDOCUMENTED] macOS variant of `send_packet` has no `///` doc comment at all, unlike the Linux variant which at least has a terse one. Public function missing all documentation. (deliberated: confirmed — macOS send_packet at L129-131. This is correctly identified as the macOS variant and indeed has no doc comment, unlike its Linux sibling at L105. UNDOCUMENTED is accurate. Tests NONE is accurate but trivial risk given the one-line delegation. Both findings retained.) |

## Hygiene

- [ ] <!-- ACT-efa3ec-1 --> **[documentation · medium · trivial]** `rustguard-crypto/src/aead.rs`: Add `///` doc comment documentation for exported symbol: `AEAD_TAG_LEN` (`AEAD_TAG_LEN`) [L7-L7]
- [ ] <!-- ACT-dbf131-6 --> **[documentation · medium · trivial]** `rustguard-daemon/src/config.rs`: Add `///` doc comment documentation for exported symbol:  `InterfaceConfig`, `PeerConfig`, `prefix_to_netmask` (`InterfaceConfig, PeerConfig, prefix_to_netmask`) [L19-L25, L28-L34, L284-L292]
- [ ] <!-- ACT-18d76d-3 --> **[documentation · medium · trivial]** `rustguard-enroll/src/control.rs`: Add `///` doc comment documentation for exported symbol: `new_window` (`new_window`) [L26-L28]
- [ ] <!-- ACT-c2ad01-3 --> **[documentation · medium · trivial]** `rustguard-enroll/src/fast_udp.rs`: Add `///` doc comment documentation for exported symbol: `send_packet` (`send_packet`) [L129-L131]
- [ ] <!-- ACT-d84ed3-1 --> **[documentation · medium · trivial]** `rustguard-enroll/src/protocol.rs`: Add `///` doc comment documentation for exported symbol: `ENROLL_REQUEST_SIZE` (`ENROLL_REQUEST_SIZE`) [L17-L17]
- [ ] <!-- ACT-d84ed3-2 --> **[documentation · medium · trivial]** `rustguard-enroll/src/protocol.rs`: Add `///` doc comment documentation for exported symbol: `ENROLL_RESPONSE_SIZE` (`ENROLL_RESPONSE_SIZE`) [L18-L18]
- [ ] <!-- ACT-62f2c7-6 --> **[documentation · medium · trivial]** `rustguard-tun/src/bpf_loader.rs`: Add `///` doc comment documentation for exported symbol: `XdpProgram` (`XdpProgram`) [L37-L41]
- [ ] <!-- ACT-22946f-7 --> **[documentation · medium · trivial]** `rustguard-tun/src/linux_mq.rs`: Add `///` doc comment documentation for exported symbol: `MultiQueueTun` (`MultiQueueTun`) [L77-L81]
- [ ] <!-- ACT-c62ee4-7 --> **[documentation · medium · trivial]** `rustguard-tun/src/linux.rs`: Add `///` doc comment documentation for exported symbol:  `create`, `read`, `write` (`create, read, write`) [L78-L122, L193-L199, L203-L212]
- [ ] <!-- ACT-b59607-7 --> **[documentation · medium · trivial]** `rustguard-tun/src/macos.rs`: Add `///` doc comment documentation for exported symbol:  `create`, `read`, `write` (`create, read, write`) [L95-L192, L245-L263, L266-L296]
- [ ] <!-- ACT-efa3ec-2 --> **[documentation · low · trivial]** `rustguard-crypto/src/aead.rs`: Complete `///` doc comment documentation for:  `MAX_PACKET_SIZE`, `seal`, `open`, `xseal`, `xopen`, `seal_to`, `open_to` (`MAX_PACKET_SIZE, seal, open, xseal, xopen, seal_to, open_to`) [L10-L10, L16-L28, L33-L45, L48-L54, L57-L63, L68-L84, L89-L102]
- [ ] <!-- ACT-dbf131-4 --> **[documentation · low · trivial]** `rustguard-daemon/src/config.rs`: Complete `///` doc comment documentation for: `Config` (`Config`) [L13-L16]
- [ ] <!-- ACT-dbf131-5 --> **[documentation · low · trivial]** `rustguard-daemon/src/config.rs`: Complete `///` doc comment documentation for: `CidrAddr` (`CidrAddr`) [L38-L41]
- [ ] <!-- ACT-18d76d-2 --> **[documentation · low · trivial]** `rustguard-enroll/src/control.rs`: Complete `///` doc comment documentation for:  `socket_path`, `is_open`, `open_window`, `close_window`, `remaining`, `start_listener`, `send_command`, `cleanup` (`socket_path, is_open, open_window, close_window, remaining, start_listener, send_command, cleanup`) [L18-L20, L31-L41, L44-L51, L54-L56, L59-L73, L77-L104, L147-L166, L169-L171]
- [ ] <!-- ACT-c2ad01-1 --> **[documentation · low · trivial]** `rustguard-enroll/src/fast_udp.rs`: Complete `///` doc comment documentation for:  `PKT_BUF_SIZE`, `RecvBatch`, `recv_batch`, `recv_batch` (`PKT_BUF_SIZE, RecvBatch, recv_batch, recv_batch`) [L16-L16, L19-L24, L39-L83, L112-L126]
- [ ] <!-- ACT-d84ed3-3 --> **[documentation · low · trivial]** `rustguard-enroll/src/protocol.rs`: Complete `///` doc comment documentation for:  `derive_token_key`, `build_request`, `parse_request`, `EnrollmentOffer`, `build_response`, `parse_response` (`derive_token_key, build_request, parse_request, EnrollmentOffer, build_response, parse_response`) [L22-L24, L27-L36, L40-L51, L54-L58, L61-L75, L78-L102]
- [ ] <!-- ACT-9105c7-5 --> **[documentation · low · trivial]** `rustguard-kmod/src/cookie.rs`: Complete `///` doc comment documentation for:  `CookieChecker`, `CookieState`, `constant_time_eq` (`CookieChecker, CookieState, constant_time_eq`) [L66-L71, L74-L77, L202-L205]
