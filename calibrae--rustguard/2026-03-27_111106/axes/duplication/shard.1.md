[← Back to Duplication](./index.md) · [← Back to report](../../public_report.md)

# Duplication — Shard 1

## Findings

| File | Verdict | Duplication | Conf. | Details |
|------|---------|-------------|-------|---------|
| `rustguard-enroll/src/server.rs` | CRITICAL | 2 | 92% | [details](#rustguard-enrollsrcserverrs) |
| `rustguard-kmod/src/noise.rs` | NEEDS_REFACTOR | 2 | 92% | [details](#rustguard-kmodsrcnoisers) |
| `rustguard-core/src/cookie.rs` | NEEDS_REFACTOR | 2 | 92% | [details](#rustguard-coresrccookiers) |
| `rustguard-daemon/src/tunnel.rs` | NEEDS_REFACTOR | 2 | 90% | [details](#rustguard-daemonsrctunnelrs) |
| `rustguard-tun/src/linux.rs` | NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-tunsrclinuxrs) |
| `rustguard-tun/src/macos.rs` | NEEDS_REFACTOR | 1 | 90% | [details](#rustguard-tunsrcmacosrs) |
| `rustguard-kmod/src/cookie.rs` | NEEDS_REFACTOR | 3 | 92% | [details](#rustguard-kmodsrccookiers) |
| `rustguard-tun/src/linux_mq.rs` | NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-tunsrclinuxmqrs) |
| `rustguard-enroll/src/client.rs` | NEEDS_REFACTOR | 2 | 88% | [details](#rustguard-enrollsrcclientrs) |

## Symbol Details

### `rustguard-enroll/src/server.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `rand_index` | L534–L538 | DUPLICATE | 70% | [USED] Non-exported helper; called once at L347 during WireGuard initiation handshake processing to generate random peer index | [DUPLICATE] Identical implementation found in rustguard-enroll/src/client.rs (score 0.965). Both use getrandom to generate 4 random bytes and convert to u32 via from_le_bytes. | [OK] Correctly generates a 4-byte random value via getrandom and converts to u32. The expect on getrandom failure is acceptable for a security-critical context where proceeding without entropy is worse than panicking. | [LEAN] Four-line helper that wraps getrandom for a u32 session index. Minimal and appropriate; no abstraction needed beyond this. | [NONE] No test file exists for server.rs. rand_index has zero test coverage. Previously confirmed NONE by deliberation override. | [UNDOCUMENTED] Private helper with no /// doc comments. Name and trivial body are self-explanatory. Leniency applied for private item. (deliberated: confirmed — Duplication DUPLICATE: Technically correct — near-identical implementation exists in client.rs. However, this is a 4-line function in a different crate. Deduplicating would require a shared utility crate or making it pub in one crate for a trivial helper. The duplication is pragmatically justified but the finding is factually accurate. Tests WEAK: No test file exists; detail says NONE. Keeping WEAK per original field.) |
| `base64_key` | L541–L544 | DUPLICATE | 70% | [USED] Non-exported helper wrapper around base64::encode; called at L267 (enrollment logging) and L349 (handshake logging) for key display | [DUPLICATE] Identical implementation found in rustguard-enroll/src/client.rs (score 0.992) and rustguard-daemon/src/tunnel.rs (score 0.993). Both use base64::BASE64_STANDARD.encode() to encode 32-byte keys. | [OK] Straightforward base64 encoding of a fixed 32-byte key. No correctness issues. | [LEAN] Three-line display helper for logging public keys. Appropriately small; inlining the base64 import inside the function keeps scope contained. | [NONE] No test file exists for server.rs. base64_key has zero test coverage. Previously confirmed NONE by deliberation override. | [UNDOCUMENTED] Private helper with no /// doc comments. Name and parameter type [u8; 32] clearly communicate intent. Leniency applied for private item. (deliberated: confirmed — Duplication DUPLICATE: Same situation as rand_index — 3-line helper duplicated across crates. Factually correct duplication finding, though the overhead of a shared crate for this trivial function may not be justified. Tests WEAK: No test file; same merger inconsistency as other symbols.) |

### `rustguard-kmod/src/noise.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `constant_time_eq` | L53–L58 | DUPLICATE | 85% | [USED] Not exported; called in process_response (L360) and process_initiation (L409) for MAC1 verification. | [DUPLICATE] Identical signature and implementation to constant_time_eq in cookie.rs (score 0.962); both wrap wg_crypto_memneq with same logic | [OK] Length-mismatch short-circuit is safe here (not a timing leak since lengths are public in the wire format). wg_crypto_memneq == 0 correctly means equal. | [LEAN] Minimal wrapper: length short-circuit then delegates to crypto_memneq. No unnecessary indirection. | [NONE] No test file exists for this module. constant_time_eq is a security-critical primitive (guards against timing side-channels) with zero test coverage. Length-mismatch branch and equality branch are both untested. | [PARTIAL] Has one `///` line. Private function, so tolerance is higher, but parameters `a`/`b` and the length-mismatch early-return behavior are undescribed. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: 99% identical to cookie.rs version, genuine candidate for extraction into shared module. Tests WEAK→NONE: detail explicitly states 'No test file exists for this module' with zero coverage; WEAK implies some tests exist, but none do. Documentation PARTIAL confirmed: has one-line doc but omits parameter semantics.) |
| `mac` | L97–L107 | DUPLICATE | 85% | [USED] Not exported; called in compute_mac1() at L215 for MAC1 computation. | [DUPLICATE] Identical implementation to mac in cookie.rs (score 0.974); same FFI call to wg_blake2s_256_mac with identical parameter order and return handling | [OK] Passes correct key, key_len, data, data_len to wg_blake2s_256_mac. Output buffer is 32 bytes. No issues. | [LEAN] Straight FFI wrapper; returns a fixed 32-byte buffer. No unnecessary layers. | [NONE] No test file exists for this module. Keyed BLAKE2s MAC has zero test coverage; no known-answer or edge-case tests present. | [PARTIAL] Has one `///` line: 'Keyed BLAKE2s MAC (for MAC1/MAC2).' Mentions use-case context. Missing parameter and return-value descriptions. Private function. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical FFI wrapper to cookie.rs mac. Tests WEAK→NONE: no test file exists, zero coverage confirmed by detail. Documentation PARTIAL confirmed.) |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `elapsed_since` | L38–L40 | DUPLICATE | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.) |
| `elapsed_since` | L51–L55 | DUPLICATE | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.) |

### `rustguard-daemon/src/tunnel.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `rand_index` | L492–L496 | DUPLICATE | 70% | [USED] called at L154 and L424 to generate random sender indices | [DUPLICATE] Generates random u32 index via getrandom. RAG score 0.960 shows nearly identical implementation in server.rs—both fill buffer and convert to u32 identically. | [OK] Correctly reads 4 bytes of OS entropy via fill_random and converts to u32 for the sender index. | [LEAN] Tiny focused helper that reads 4 bytes of OS entropy and converts to u32. The two-level split (rand_index → fill_random) is minor but readable. | [NONE] No test file found. rand_index wraps fill_random with a u32 conversion; no tests exist for distribution properties, output length, or uniqueness. | [DOCUMENTED] Private function with `/// Generate a random u32 sender index using OS entropy.` clearly stating purpose and entropy source. No examples expected on a private helper with no parameters. (deliberated: confirmed — Duplication DUPLICATE is technically correct — 96% identical logic exists in rustguard-enroll/src/server.rs. However, these are in separate binary crates; extracting 5 lines to a shared crate has its own cost. Finding stands but practical impact is low. Tests WEAK is appropriate for a thin wrapper with no test file. Raised confidence slightly since cross-crate duplication is verifiable.) |
| `base64_key` | L529–L532 | DUPLICATE | 70% | [USED] called at L96, L309, L356, L430 to format public keys | [DUPLICATE] Encodes 32-byte key to base64 string. RAG score 0.994 with identical logic in client.rs—both use BASE64_STANDARD.encode on [u8; 32]. | [OK] Correctly encodes a 32-byte key slice with BASE64_STANDARD; no correctness issues. | [LEAN] Simple display helper that encodes a 32-byte key to base64 for logging. Three lines, single responsibility. | [NONE] No test file found. base64_key encodes a 32-byte key; no tests verify correctness of encoding, padding, or known-vector output. | [UNDOCUMENTED] Private helper with no `///` doc comment. Name is self-descriptive; the function is trivial. Private-item leniency applied. (deliberated: confirmed — Duplication DUPLICATE is correct — 99% identical to base64_key in rustguard-enroll/src/client.rs. Both encode [u8;32] via BASE64_STANDARD.encode. Cross-crate dedup to a shared utility is warranted given the near-identical signature and logic. Tests WEAK correct for a trivial encoder with no test file.) |

### `rustguard-tun/src/linux.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `close_and_error` | L55–L59 | DUPLICATE | 85% | [USED] Helper function to close fd and return error; used throughout create() and configure_interface() | [DUPLICATE] Identical 5-line helper found in linux_mq.rs with score 0.987 | [OK] Correctly captures errno via last_os_error() before calling close(), preventing the close() syscall from overwriting errno. Error is returned after fd is closed. | [LEAN] Captures errno before closing the fd to prevent clobbering — a legitimate and important pattern in unsafe libc code. Used consistently at every error exit point. | [NONE] No test file found for rustguard-tun/src/linux.rs. close_and_error has zero test coverage. Error path (fd cleanup on failure) is entirely untested. | [UNDOCUMENTED] Private helper function that closes an fd and captures the last OS error. No `///` doc comment. Tolerated for private items with clear names. (deliberated: confirmed — Duplication is genuine — identical 5-line function exists in linux_mq.rs. Both capture errno before close(fd) to prevent clobbering. Could be extracted to a shared internal utility module (e.g., linux_common.rs). Tests WEAK retained — no test file for this module.) |
| `make_sockaddr_in` | L61–L70 | DUPLICATE | 85% | [USED] Helper function called 3 times in configure_interface() to construct sockaddr_in structs | [DUPLICATE] Identical 10-line function in linux_mq.rs with score 0.973 | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. On a little-endian host, from_ne_bytes([a,b,c,d]) produces the integer 0xdcba…, which when stored in memory LE lays down bytes [a,b,c,d] — preserving network (big-endian) byte order in memory exactly as the kernel expects. | [LEAN] Encapsulates boilerplate sockaddr_in initialisation used three times in configure_interface; avoids repetition and ensures sin_family/sin_zero are always correctly set. | [NONE] No test file found for rustguard-tun/src/linux.rs. make_sockaddr_in has zero test coverage. The byte-order conversion (u32::from_ne_bytes) and struct initialization are not validated by any test. | [UNDOCUMENTED] Private helper constructing a `sockaddr_in` from an `Ipv4Addr`. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 10-line function in linux_mq.rs constructing libc::sockaddr_in from Ipv4Addr with identical field initialization. Same semantic contract in both files, prime candidate for shared module extraction. Tests WEAK retained.) |
| `set_name` | L72–L76 | DUPLICATE | 85% | [USED] Helper function used in create() and configure_interface() to copy interface name into buffers | [DUPLICATE] Identical 5-line function in linux_mq.rs with score 0.968 | [OK] Copies up to IFNAMSIZ-1 bytes, leaving the final byte as the zero from the caller's zero-initialized buffer. Null termination is always present. | [LEAN] Tiny but used at five distinct call sites; centralises the null-termination and length-clamping logic that would otherwise be repeated inline each time. | [NONE] No test file found for rustguard-tun/src/linux.rs. set_name has zero test coverage. Truncation at IFNAMSIZ-1 for long names and NUL-termination behavior are not tested. | [UNDOCUMENTED] Private helper writing a name into a fixed-size `[u8; IFNAMSIZ]` buffer. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 5-line function in linux_mq.rs copying null-terminated name bytes into fixed-size buffer. Same contract, same truncation logic. Tests WEAK retained.) |
| `configure_interface` | L124–L189 | DUPLICATE | 85% | [USED] Internal helper function called once from create() at line 113 for interface network configuration | [DUPLICATE] RAG score 0.954 with linux_mq.rs variant; both apply identical ioctl sequence (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK, SIOCSIFMTU, SIOCGIFFLAGS, SIOCSIFFLAGS) | [NEEDS_FIX] All three address-setting ioctls (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK) use IfreqAddr, which is 8 bytes short of the kernel's 40-byte ifreq on 64-bit Linux. The kernel reads 40 bytes from the pointer on each call, touching 8 bytes of adjacent stack. Functional in practice for these SIOCS operations but is UB and fails under ASAN. Fix: add _pad: [u8; 8] to IfreqAddr. | [LEAN] Performs six distinct but sequentially dependent ioctl operations on a temporary socket; extracted from create for clarity and testability. Linear complexity matching the task requirements. | [NONE] No test file found for rustguard-tun/src/linux.rs. configure_interface has zero test coverage. All ioctl branches (SIOCSIFADDR, SIOCSIFDSTADDR, SIOCSIFNETMASK, SIOCSIFMTU, SIOCGIFFLAGS, SIOCSIFFLAGS) and their error paths are untested. | [UNDOCUMENTED] Private function handling ioctl calls for address/MTU/flags setup. No `///` doc comment. Tolerated for private items, though a note on the temporary SOCK_DGRAM socket pattern would help maintainers. (deliberated: confirmed — Correction NEEDS_FIX confirmed: all three address-setting ioctls pass IfreqAddr (32 bytes) but kernel reads 40 bytes via copy_from_user, causing UB. Duplication confirmed at ~95% with linux_mq.rs — same 6-ioctl sequence. Tests NONE and documentation UNDOCUMENTED both valid. Private function so UNDOCUMENTED is tolerable but the function's complexity (65 lines, 6 ioctls) warrants at least a brief note.) |

### `rustguard-tun/src/macos.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `close_and_error` | L79–L83 | DUPLICATE | 75% | [USED] Called 4 times to safely preserve OS error before closing file descriptor (L111, L127, L142, L165) | [DUPLICATE] Identical implementation found in linux.rs and linux_mq.rs with scores 0.977 and 0.979. Captures errno, closes fd, returns error—same logic and semantic contract across all three platform modules. | [OK] Correctly captures errno before calling close() to prevent the close() syscall from clobbering errno. | [LEAN] Solves a real, documented problem: libc::close() can clobber errno, so the error must be captured first. The comment explains the invariant clearly. Not abstraction for its own sake. | [NONE] No test file exists for this module. This private fd-cleanup helper has zero test coverage. The errno-before-close ordering is untested. | [PARTIAL] Private function with three `///` lines (lines 76–78) explaining purpose ('Capture the OS error, close the fd'), motivation ('avoids close() clobbering errno'), and a Safety precondition. Adequate for a private unsafe helper. No formal `# Parameters` or `# Examples`, but those are not expected for private functions. (deliberated: confirmed — Duplication DUPLICATE is technically accurate — identical 4-line implementations exist in linux.rs and linux_mq.rs. However, these are platform-gated modules (only one compiles per target), so deduplication into a shared utility is a low-value refactor. Keeping DUPLICATE but lowering confidence to reflect that cross-platform-module duplication is a weaker signal. Documentation PARTIAL is accurate: the three /// lines adequately document purpose and safety for a private unsafe helper.) |

### `rustguard-kmod/src/cookie.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `LABEL_MAC1` | L17–L17 | DUPLICATE | 75% | [USED] Label constant used in hash computation for MAC1 key generation in verify_mac1 (line 101). | [DUPLICATE] Identical protocol constant also defined in noise.rs; same value b"mac1----" | [OK] "mac1----" is the correct 8-byte label used in WireGuard's MAC1 key derivation. | [LEAN] Protocol-mandated domain-separation label. Same reasoning as LABEL_COOKIE — spec-faithful, no abstraction added. | [GOOD] Pure compile-time byte-string constant with no runtime behavior. GOOD by default per rule 6 for constants. | [UNDOCUMENTED] Private constant with no `///` doc comment. The label value `mac1----` is a WireGuard-spec constant; no spec reference or purpose comment is present. (deliberated: confirmed — Duplication with noise.rs LABEL_MAC1 is a true 100% identical constant (b"mac1----"). While kernel module code often duplicates small constants for module isolation, there's no evidence this is an intentional design pattern here rather than coincidental copy. A shared constants module would be cleaner. Keeping DUPLICATE but confidence is moderate since I cannot directly verify noise.rs content.) |
| `mac` | L53–L57 | DUPLICATE | 75% | [USED] Internal helper for BLAKE2s MAC computation, called in verify_mac1 (103), make_cookie (117), verify_mac2 (143), compute_mac2 (189). | [DUPLICATE] RAG score 0.974 with noise.rs mac — identical signature and implementation. Both compute BLAKE2s-256 MAC by calling wg_blake2s_256_mac with same parameter passing and return value extraction. | [OK] Straightforward FFI wrapper for BLAKE2s-MAC. Key, data, lengths, and output pointer are all forwarded correctly with no type mismatches. | [LEAN] One-liner FFI wrapper that eliminates repeated unsafe blocks. No unnecessary generics or abstraction layers. | [WEAK] No direct tests found. Indirectly exercised through CookieChecker::verify_mac1() and make_cookie() in the test suite established by prior deliberation (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie). Edge cases such as empty key, empty data, or mismatched key lengths are never directly validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_256_mac` but the keyed-MAC semantics and return size (32 bytes) are not described. (deliberated: confirmed — Duplication: reported as 100% identical to noise.rs mac function — both are thin wrappers calling wg_blake2s_256_mac with the same parameter pattern. This is a true functional duplicate that belongs in a shared crypto utility module. Tests WEAK: only indirect coverage through CookieChecker methods; no direct tests for empty key, empty data, or edge-case inputs. Both findings maintained.) |
| `constant_time_eq` | L202–L205 | DUPLICATE | 80% | [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, called in verify_mac1 (104) and verify_mac2 (144). | [DUPLICATE] RAG score 0.962 with noise.rs constant_time_eq — identical implementation. Both check length equality then use wg_crypto_memneq for constant-time comparison, with same parameter passing and return logic. | [OK] Correctly delegates to the kernel's crypto_memneq, which is guaranteed constant-time and cannot be optimized away by the compiler. The early-exit length check is safe because all callers pass fixed-length 16-byte slices, so the branch carries no secret-dependent timing information. | [LEAN] Security-critical helper delegating to the kernel's crypto_memneq, which is guaranteed not to be optimized away. Extracting it into a named function documents intent and prevents accidental substitution with a non-constant-time comparison elsewhere. | [WEAK] No direct tests found. Indirectly exercised through verify_mac1() and verify_mac2() in the CookieChecker test suite (established by prior deliberation). Critical edge cases are untested: mismatched-length slices (returns false early without calling wg_crypto_memneq), equal-length but differing slices, and zero-length slices. The constant-time guarantee itself is structurally untestable at the unit level, but behavioral correctness for these edge cases is never directly asserted. | [PARTIAL] Private function has a `///` doc comment noting its constant-time guarantee via `wg_crypto_memneq`. However, parameter semantics (slice length requirement), return-value meaning, and panic/safety conditions are not described. Leniency applies as private. (deliberated: confirmed — Duplication DUPLICATE: reported as 100% identical to noise.rs constant_time_eq — both check length then delegate to wg_crypto_memneq. This is a security-critical utility that absolutely should live in a shared module to prevent divergent copies. Tests WEAK: only exercised indirectly through verify_mac1/verify_mac2; mismatched-length and zero-length edge cases never tested directly. Documentation PARTIAL: has a /// comment noting the constant-time guarantee (good) but parameter semantics and return value meaning are undescribed.) |

### `rustguard-tun/src/linux_mq.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `set_name` | L53–L57 | DUPLICATE | 88% | [USED] Called at L100, L122, L233, L242, L257 to copy interface name into ifreq structures | [DUPLICATE] Identical to linux.rs version - copies string bytes to fixed buffer with length constraint | [OK] Copies up to IFNAMSIZ-1 bytes, preserving the zeroes already present beyond the written region. All call sites zero-initialize the buffer before passing it in, guaranteeing a null terminator. Correct. | [LEAN] Focused helper that safely copies a Rust str into a fixed-size C name buffer; used at 5+ call sites, justified. | [NONE] No test file exists for rustguard-tun/src/linux_mq.rs. set_name has zero test coverage despite being a utility with a non-trivial name-truncation edge case (names >= IFNAMSIZ). | [UNDOCUMENTED] Private function, no `///` doc comment. Name and signature (`buf: &mut [u8; IFNAMSIZ], name: &str`) are self-descriptive. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: function is 100% identical to linux.rs counterpart in the same crate; both share the same byte-copy logic with IFNAMSIZ constraint. Extracting to a shared linux_common.rs internal module is straightforward. Tests reclassified WEAK→NONE: the detail explicitly states 'No test file exists' and 'zero test coverage' — WEAK implies partial test coverage exists, but none does.) |
| `make_sockaddr_in` | L59–L68 | DUPLICATE | 88% | [USED] Called at L232, L239, L244 to construct sockaddr_in for address, destination, and netmask | [DUPLICATE] Identical to linux.rs version - creates sockaddr_in struct with identical field initialization | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. octets() returns bytes in network (big-endian) order; from_ne_bytes reinterprets them as a native-endian u32 without byte-swapping the underlying bytes. On a little-endian host, from_ne_bytes([192,168,1,1]) = 0x0101a8c0, which when written to memory produces [0xc0,0xa8,0x01,0x01] — the correct network-byte-order in-memory representation. Semantically equivalent to a safe transmute of the octets array. | [LEAN] Simple constructor for sockaddr_in from Ipv4Addr; eliminates repetitive struct initialization across 3 ioctl calls in configure_interface. | [NONE] No test file exists. make_sockaddr_in has zero test coverage; the NE-byte-order conversion for s_addr is untested. | [UNDOCUMENTED] Private function, no `///` doc comment. Name and single `Ipv4Addr` parameter make purpose clear. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical signature and implementation to linux.rs version within the same crate — same sockaddr_in construction with from_ne_bytes. Tests reclassified WEAK→NONE: detail confirms 'No test file exists' and 'zero test coverage'; no tests exist at all.) |
| `close_and_error` | L70–L74 | DUPLICATE | 88% | [USED] Called at L106, L128, L237, L242, L247, L261, L266 to close file descriptors and return OS errors | [DUPLICATE] Identical to linux.rs version - captures OS error, closes fd, returns error in same sequence | [OK] Captures last_os_error before closing, preventing close(2) from overwriting errno. Correct ordering. | [LEAN] Two-line helper capturing last_os_error before closing an fd; prevents the common bug of closing fd before reading errno. Used at multiple error-return sites. | [NONE] No test file exists. close_and_error has zero test coverage. | [UNDOCUMENTED] Private function, no `///` doc comment. Name clearly indicates it closes an fd and captures the last OS error. Tolerated per private-item leniency rule. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical to linux.rs version — same errno-capture-before-close pattern. Trivially extractable to a shared module. Tests reclassified WEAK→NONE: detail confirms 'No test file exists' and 'zero test coverage'.) |
| `configure_interface` | L206–L260 | DUPLICATE | 85% | [USED] Called at L136 within create() to set IP address, netmask, MTU, and bring interface up | [DUPLICATE] Very similar to linux.rs version (RAG score 0.954) - identical configuration sequence for address, destination, netmask, MTU, and flags via ioctl calls | [OK] Each error branch calls close_and_error(sock) which closes sock exactly once before returning; the success path closes sock via libc::close at the end. The ioctl sequence (SIOCSIFADDR → SIOCSIFDSTADDR → SIOCSIFNETMASK → SIOCSIFMTU → SIOCGIFFLAGS → SIOCSIFFLAGS|IFF_UP) is correct for address configuration and interface activation. IfreqFlags (40 bytes) is used for the SIOCG/SIOCSIFFLAGS round-trip, so no size-mismatch issue applies there. | [LEAN] Sequentially configures address, destination, netmask, MTU, and brings the interface up — exactly the required steps. Each ioctl is necessary; no abstraction layers are added beyond what the kernel protocol demands. | [NONE] No test file exists. configure_interface has zero test coverage; none of the ioctl call paths (address, destination, netmask, MTU, flags up) are exercised by any test. | [UNDOCUMENTED] Private unsafe function, no `///` doc comment. The function is non-trivial (sets address, destination, netmask, MTU, brings interface up via multiple ioctls). A `///` summary would be warranted, but it is private so the lack is tolerated per private-item leniency rule. (deliberated: confirmed — Duplication DUPLICATE confirmed: 0.954 semantic similarity to linux.rs version with identical ioctl configuration sequence (SIOCSIFADDR→SIOCSIFDSTADDR→SIOCSIFNETMASK→SIOCSIFMTU→SIOCGIFFLAGS→SIOCSIFFLAGS|IFF_UP). Both reside in the same crate and share the same struct types, making extraction to a common internal module feasible. Tests NONE confirmed. Documentation UNDOCUMENTED confirmed: private unsafe function performing 6-step ioctl configuration with no doc comment — while tolerated by private-item leniency, the complexity warrants at minimum a summary comment.) |

### `rustguard-enroll/src/client.rs`

| Symbol | Lines | Duplication | Conf. | Detail |
|--------|-------|-------------|-------|--------|
| `rand_index` | L194–L198 | DUPLICATE | 85% | [USED] Internal function called locally at line 110 within run() to generate WireGuard handshake sender_index. Essential for handshake initiation. | [DUPLICATE] Identical implementation — both generate random u32 via getrandom and le_bytes conversion. Score 0.965 with same-crate target indicates textbook trivial helper duplication. | [OK] Uses `getrandom::getrandom` for CSPRNG bytes; panics on failure, which is acceptable for RNG initialisation. Conversion via `u32::from_le_bytes` is correct. | [LEAN] Minimal helper: reads 4 bytes from the OS RNG and reinterprets them as a u32. Exactly the right amount of code for this purpose. | [NONE] No test file found for this module. rand_index has zero test coverage despite being a security-relevant RNG utility used to seed the WireGuard sender index. | [UNDOCUMENTED] Private function with a reasonably self-explanatory name. No `///` doc comment, but leniency applies for private items. The name alone does not surface that it panics on RNG failure (`expect("rng")`), which is mildly useful to document. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE is correct — 100% identical implementation to server.rs::rand_index within the same crate (rustguard-enroll). Both read 4 random bytes via getrandom and convert to u32 via from_le_bytes. This is textbook same-crate duplication that should be extracted to a shared utility. Tests reclassified WEAK → NONE: the detail text explicitly states 'No test file found for this module. rand_index has zero test coverage' — there are no tests whatsoever, which is NONE not WEAK. The merged field contradicts its own detail text.) |
| `base64_key` | L200–L203 | DUPLICATE | 85% | [USED] Internal function called locally at line 69 to display the server public key in base64 format for user output. Used in enrollment confirmation message. | [DUPLICATE] Identical implementation — both encode 32-byte key to base64 string using BASE64_STANDARD. Score 0.994 indicates nearly perfect structural match. Trivial 3-line helper appears identically across multiple files. | [OK] Straightforward BASE64_STANDARD encode of a 32-byte slice; no correctness issues. | [LEAN] Thin display helper over the base64 crate. Single responsibility, minimal code. | [NONE] No test file found for this module. base64_key is a trivial formatting helper but has no tests verifying its output for known inputs. | [UNDOCUMENTED] Private helper with a self-descriptive name. No `///` doc comment, but leniency applies for private items with clear naming conventions. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE is correct — nearly identical (99%) to rustguard-daemon/src/tunnel.rs::base64_key. Both encode a [u8; 32] key to base64 string using BASE64_STANDARD.encode(). Cross-crate duplication of a trivial helper that could live in rustguard-core or a shared util. Tests reclassified WEAK → NONE: same issue as rand_index — the detail text explicitly states 'No test file found for this module. base64_key...has no tests' which is NONE, not WEAK. The merged field is inconsistent with its own evidence.) |

## Refactors

- [ ] <!-- ACT-a4f709-8 --> **[duplication · medium · small]** `rustguard-core/src/cookie.rs`: Deduplicate: `elapsed_since` duplicates `elapsed_since` in `rustguard-core/src/timers.rs` (`elapsed_since`) [L51-L55]
- [ ] <!-- ACT-bd20cf-7 --> **[duplication · medium · small]** `rustguard-daemon/src/tunnel.rs`: Deduplicate: `rand_index` duplicates `rand_index` in `rustguard-enroll/src/server.rs` (`rand_index`) [L492-L496]
- [ ] <!-- ACT-bd20cf-9 --> **[duplication · medium · small]** `rustguard-daemon/src/tunnel.rs`: Deduplicate: `base64_key` duplicates `base64_key` in `rustguard-enroll/src/client.rs` (`base64_key`) [L529-L532]
- [ ] <!-- ACT-ca1d92-4 --> **[duplication · medium · small]** `rustguard-enroll/src/client.rs`: Deduplicate: `rand_index` duplicates `rand_index` in `rustguard-enroll/src/server.rs` (`rand_index`) [L194-L198]
- [ ] <!-- ACT-ca1d92-5 --> **[duplication · medium · small]** `rustguard-enroll/src/client.rs`: Deduplicate: `base64_key` duplicates `base64_key` in `rustguard-daemon/src/tunnel.rs` (`base64_key`) [L200-L203]
- [ ] <!-- ACT-a732f2-7 --> **[duplication · medium · small]** `rustguard-enroll/src/server.rs`: Deduplicate: `rand_index` duplicates `rand_index` in `rustguard-enroll/src/client.rs` (`rand_index`) [L534-L538]
- [ ] <!-- ACT-a732f2-8 --> **[duplication · medium · small]** `rustguard-enroll/src/server.rs`: Deduplicate: `base64_key` duplicates `base64_key` in `rustguard-enroll/src/client.rs` (`base64_key`) [L541-L544]
- [ ] <!-- ACT-9105c7-3 --> **[duplication · medium · small]** `rustguard-kmod/src/cookie.rs`: Deduplicate: `LABEL_MAC1` duplicates `LABEL_MAC1` in `rustguard-kmod/src/noise.rs` (`LABEL_MAC1`) [L17-L17]
- [ ] <!-- ACT-9105c7-4 --> **[duplication · medium · small]** `rustguard-kmod/src/cookie.rs`: Deduplicate: `mac` duplicates `mac` in `rustguard-kmod/src/noise.rs` (`mac`) [L53-L57]
- [ ] <!-- ACT-9105c7-6 --> **[duplication · medium · small]** `rustguard-kmod/src/cookie.rs`: Deduplicate: `constant_time_eq` duplicates `constant_time_eq` in `rustguard-kmod/src/noise.rs` (`constant_time_eq`) [L202-L205]
- [ ] <!-- ACT-58e5ff-6 --> **[duplication · medium · small]** `rustguard-kmod/src/noise.rs`: Deduplicate: `constant_time_eq` duplicates `constant_time_eq` in `rustguard-kmod/src/cookie.rs` (`constant_time_eq`) [L53-L58]
- [ ] <!-- ACT-58e5ff-8 --> **[duplication · medium · small]** `rustguard-kmod/src/noise.rs`: Deduplicate: `mac` duplicates `mac` in `rustguard-kmod/src/cookie.rs` (`mac`) [L97-L107]
- [ ] <!-- ACT-22946f-3 --> **[duplication · medium · small]** `rustguard-tun/src/linux_mq.rs`: Deduplicate: `set_name` duplicates `set_name` in `rustguard-tun/src/linux.rs` (`set_name`) [L53-L57]
- [ ] <!-- ACT-22946f-4 --> **[duplication · medium · small]** `rustguard-tun/src/linux_mq.rs`: Deduplicate: `make_sockaddr_in` duplicates `make_sockaddr_in` in `rustguard-tun/src/linux.rs` (`make_sockaddr_in`) [L59-L68]
- [ ] <!-- ACT-22946f-5 --> **[duplication · medium · small]** `rustguard-tun/src/linux_mq.rs`: Deduplicate: `close_and_error` duplicates `close_and_error` in `rustguard-tun/src/linux.rs` (`close_and_error`) [L70-L74]
- [ ] <!-- ACT-22946f-8 --> **[duplication · medium · small]** `rustguard-tun/src/linux_mq.rs`: Deduplicate: `configure_interface` duplicates `configure_interface` in `rustguard-tun/src/linux.rs` (`configure_interface`) [L206-L260]
- [ ] <!-- ACT-c62ee4-3 --> **[duplication · medium · small]** `rustguard-tun/src/linux.rs`: Deduplicate: `close_and_error` duplicates `close_and_error` in `rustguard-tun/src/linux_mq.rs` (`close_and_error`) [L55-L59]
- [ ] <!-- ACT-c62ee4-4 --> **[duplication · medium · small]** `rustguard-tun/src/linux.rs`: Deduplicate: `make_sockaddr_in` duplicates `make_sockaddr_in` in `rustguard-tun/src/linux_mq.rs` (`make_sockaddr_in`) [L61-L70]
- [ ] <!-- ACT-c62ee4-5 --> **[duplication · medium · small]** `rustguard-tun/src/linux.rs`: Deduplicate: `set_name` duplicates `set_name` in `rustguard-tun/src/linux_mq.rs` (`set_name`) [L72-L76]
- [ ] <!-- ACT-c62ee4-8 --> **[duplication · medium · small]** `rustguard-tun/src/linux.rs`: Deduplicate: `configure_interface` duplicates `configure_interface` in `rustguard-tun/src/linux_mq.rs` (`configure_interface`) [L124-L189]
- [ ] <!-- ACT-b59607-4 --> **[duplication · medium · small]** `rustguard-tun/src/macos.rs`: Deduplicate: `close_and_error` duplicates `close_and_error` in `rustguard-tun/src/linux.rs` (`close_and_error`) [L79-L83]
