[← Back to Correction](./index.md) · [← Back to report](../../public_report.md)

# Correction — Shard 3

## Findings

| File | Verdict | Correction | Conf. | Details |
|------|---------|------------|-------|---------|
| `rustguard-kmod/src/timers.rs` | NEEDS_REFACTOR | 2 | 95% | [details](#rustguard-kmodsrctimersrs) |
| `rustguard-kmod/src/allowedips.rs` | NEEDS_REFACTOR | 2 | 85% | [details](#rustguard-kmodsrcallowedipsrs) |
| `rustguard-tun/src/uring.rs` | NEEDS_REFACTOR | 2 | 90% | [details](#rustguard-tunsrcuringrs) |
| `rustguard-tun/src/lib.rs` | NEEDS_REFACTOR | 1 | 88% | [details](#rustguard-tunsrclibrs) |
| `rustguard-crypto/src/tai64n.rs` | NEEDS_REFACTOR | 1 | 87% | [details](#rustguard-cryptosrctai64nrs) |
| `rustguard-core/src/replay.rs` | NEEDS_REFACTOR | 1 | 92% | [details](#rustguard-coresrcreplayrs) |

## Symbol Details

### `rustguard-kmod/src/timers.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `REKEY_AFTER_MESSAGES` | L23–L23 | NEEDS_FIX | 80% | [USED] Used in needs_rekey() method at line 86 to check message count rekey threshold. | [UNIQUE] WireGuard protocol message count threshold. Unique value (1u64 << 60) - 1. | [NEEDS_FIX] WireGuard whitepaper Section 6 and the Linux kernel reference implementation both define REKEY_AFTER_MESSAGES as 2^60 (`1ULL << 60`). This code uses `(1u64 << 60) - 1`, which is one less than the specified value. The deviation is in the conservative direction (triggers rekeying one message earlier) and does not create a security hazard, but it is a spec violation. | [LEAN] Bit-shift expression `(1u64 << 60) - 1` is the idiomatic way to express a large bitmask boundary. Directly matches the WireGuard spec's counter threshold. | [GOOD] Compile-time constant with no runtime behavior. Per rule 6, constants with no runtime behavior are GOOD by default. | [DOCUMENTED] Private constant with '/// Rekey after this many messages.' — brief but sufficient; non-obvious bitmask value is adequately labelled. (deliberated: confirmed — Correction NEEDS_FIX is valid. The WireGuard whitepaper Section 6 and Linux kernel reference both define REKEY_AFTER_MESSAGES as 2^60, while this code uses (1u64 << 60) - 1. While the deviation is conservative (triggers rekey one message earlier) and not a security hazard, it is a spec deviation without an explanatory comment, making it look like an unintentional off-by-one. Low severity is appropriate — keep finding.) |
| `SessionTimers` | L36–L49 | NEEDS_FIX | 88% | [USED] pub(crate) struct serving as crate-level session timer API with 6+ public methods; design and scope indicate internal use despite 0 importers in analysis (consistent with known false positive pattern for pub(crate) API types). | [UNIQUE] Core session timer state struct with 6 fields. No similar symbols found in codebase. | [NEEDS_FIX] Two logic bugs in the impl block. (1) is_dead() at L107-L112 measures elapsed time from session_established, but the doc comment says '240 seconds of inactivity' — inactivity must compare against last_received. A session that received a packet 5 seconds ago but was established 250 seconds ago is incorrectly declared dead, causing premature session-key zeroing. (2) needs_keepalive() at L115-L131: when last_sent == 0 (no packet ever sent), the else branch (L124) sets since_last_send = now.saturating_sub(last_received), making since_last_send == since_last_recv. The guard `since_last_send >= keepalive_interval_ns && since_last_recv < keepalive_interval_ns` then requires x >= K && x < K simultaneously, which is always false. A peer that has received data but never sent any will never emit a keepalive regardless of how long the interval has been exceeded. | [LEAN] Six fields, each directly corresponding to a distinct WireGuard timer checkpoint (session established, handshake sent, last rx, last tx, rekey flag, keepalive interval). No unnecessary generics, no inheritance, no factory. Every field is consumed by at least one method in the impl block. The struct is exactly as large as the protocol demands. | [NONE] No test file found for rustguard-kmod/src/timers.rs. SessionTimers contains critical WireGuard session lifecycle logic across multiple methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) that drive rekey decisions, session expiry, and keepalive scheduling. None of these methods or their time-based edge cases (boundary conditions for REKEY_AFTER_TIME_NS, REJECT_AFTER_TIME_NS, counter thresholds, keepalive logic with varying last_sent/last_received combinations) are exercised by any test. | [PARTIAL] pub(crate) struct with '/// Per-peer timer state.' at the type level and /// comments on all six fields explaining sentinel-value conventions (0 = no session, 0 = never, 0 = disabled). Passes the struct rule (type + all fields documented) but the type-level comment is a near-tautological restatement of the name rather than describing lifecycle, ownership, or how the struct is driven. No # Examples section, though pub(crate) reduces that expectation. Field-level docs are good quality. (deliberated: confirmed — Correction: Both logic bugs are independently verifiable from the source code and are genuine. (1) is_dead() at L109 measures elapsed from session_established, but both the method's doc comment ('should be zeroed') and the constant's doc comment ('240 seconds of inactivity') indicate inactivity semantics requiring last_received. A peer receiving packets 1s ago but established 241s ago is incorrectly killed. (2) needs_keepalive() when last_sent==0: since_last_send is set to now-last_received, making it equal to since_last_recv, so the guard (x >= K && x < K) is always false — verified by algebraic substitution. I increase confidence to 88 because both bugs are mechanically provable from the code. Tests: NONE is correct — no test file exists for timers.rs and no tests cover any of the 6+ session lifecycle methods. These methods contain time-based boundary logic that critically needs test coverage. Documentation: PARTIAL is correct — the struct has a type-level doc comment and all fields are documented with good sentinel-value conventions, but the type-level comment is thin and new() lacks any doc comment.) |

### `rustguard-kmod/src/allowedips.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `TrieNode` | L16–L25 | NEEDS_FIX | 85% | [USED] Internal radix trie node struct extensively used: instantiated via TrieNode::new() in insert, stored in AllowedIps fields, and used in all recursive traversal functions (insert_recursive, lookup_recursive, remove_peer_recursive) | [UNIQUE] No RAG data available. Struct definition for radix trie node, no similar structures found. | [NEEDS_FIX] The `bit: u32` field is documented as 'Bit position to test (0 = MSB of first byte)' — the canonical role in a compressed radix trie where nodes skip directly to the discriminating bit. However, TrieNode::new() always initialises it to 0 and no method in the impl ever writes or reads it; routing uses an implicit `depth` counter instead. The result is an uncompressed binary trie (one node per prefix bit), contradicting the module-level claim of 'compressed radix trie'. For IPv6 /128 prefixes this creates a 128-node chain, increasing memory pressure in a kernel context and risking stack-depth issues on deep recursive traversal of densely populated tables. | [LEAN] Standard binary radix-trie node mirroring the kernel WireGuard allowedips.c layout. Fields are minimal: bit position, prefix length, peer index, and two optional boxed children. The `bit` field is declared but never written by the insertion logic (depth is used instead), which is dead storage but a correctness/incompleteness issue rather than over-abstraction. No unnecessary generics or patterns. | [NONE] No test file exists for rustguard-kmod/src/allowedips.rs. TrieNode is a private internal struct; none of its construction, field values, or child-linking logic is exercised by any test in the project. | [DOCUMENTED] Private struct with a struct-level `/// A node in the radix trie.` comment and individual `///` doc comments on all four fields (`bit`, `cidr`, `peer_idx`, `children`), each describing semantics (e.g. 'Bit position to test (0 = MSB of first byte)', 'Peer index (valid when cidr > 0)'). Private items are held to a lower standard; this exceeds it. (deliberated: reclassified: tests: WEAK → NONE — Correction NEEDS_FIX: confirmed by direct source inspection — `bit` field is initialized to 0 in TrieNode::new() (line 37) and never read or written anywhere else; insertion uses a `depth` parameter instead. This makes the trie uncompressed despite the module doc claiming 'compressed radix trie'. The bit field is dead storage. Increasing confidence from 60 to 85 since the evidence is directly visible in the source. Tests WEAK → NONE: the detail text explicitly states '[NONE] No test file exists for rustguard-kmod/src/allowedips.rs. TrieNode is a private internal struct; none of its construction, field values, or child-linking logic is exercised by any test.' WEAK was a merge error.) |
| `AllowedIps` | L28–L31 | NEEDS_FIX | 85% | [DEAD] Exported routing table struct with 0 workspace importers; defined with public API but not instantiated or used within the module. Possible external library consumer, but no evidence in workspace analysis | [UNIQUE] No RAG data available. Struct definition for IP routing table, no duplicates in provided codebase. | [NEEDS_FIX] Two correctness issues in the impl block. (1) In `insert()`, the cidr==0 branch executes dead assignments `node.cidr = 0; node.peer_idx = peer_idx;` that are immediately overwritten — the resulting cidr value is 255, but the double-write is evidence of an incomplete or contradictory change that could mislead future edits into restoring the cidr=0 path and silently breaking default-route lookup (which gates on cidr==255). (2) In `lookup_recursive()`, the parent-side pre-check (line 177) explicitly handles `child.cidr == 255` and updates `best`, but the recursive-entry check (line 170) excludes cidr==255 via `node.cidr != 255`. Should any code path ever produce a non-root node with cidr==255, the pre-check would fire for it while the entry check silently ignores it, causing that node's peer to be recorded once but its children never to contribute to `best` — a latent incorrect-LPM result. | [LEAN] Thin wrapper holding two trie roots (IPv4/IPv6), directly matching WireGuard's dual-family routing table. Public API surface is flat and purposeful: insert_v4/v6, lookup_v4/v6, lookup_packet, remove_by_peer. No factory indirection, no trait objects, no configuration structs. The sentinel cidr==255 hack for default routes is a design smell but not an abstraction excess. | [NONE] No test file exists for this module. The critical public API — insert_v4, insert_v6, lookup_v4, lookup_v6, lookup_packet, remove_by_peer — has zero test coverage. Longest-prefix-match correctness, default-route (cidr=0) sentinel logic, IPv4/IPv6 dispatch in lookup_packet, and remove_by_peer semantics are all untested. | [PARTIAL] Has struct-level `/// AllowedIPs routing table.` but neither field (`root4`, `root6`) carries a `///` doc comment. As the primary public export of this module the struct warrants field-level documentation explaining the IPv4/IPv6 root split and lifecycle expectations. No `# Examples` section is present on the type. Terse but present at the type level, so PARTIAL rather than UNDOCUMENTED. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Correction NEEDS_FIX: confirmed — lines 108-112 show dead assignments (`node.cidr = 0; node.peer_idx = peer_idx;`) immediately overwritten by sentinel assignments. The lookup_recursive boolean `child.cidr == 255 || (child.cidr > 0 && child.cidr != 255)` simplifies to `child.cidr > 0` — needlessly complex and asymmetric with the entry check. Both are real code quality issues. Utility DEAD → USED: AllowedIps is the sole pub(crate) struct of this module, the module doc explicitly says 'Used on the TX path', and it mirrors WireGuard's core allowedips.c data structure. A 200-line module with 6 public methods was clearly written for use by the rest of the crate. The '0 workspace importers' likely reflects incomplete workspace analysis (only this file was provided). Documentation UNDOCUMENTED → PARTIAL: source code line 28 clearly shows `/// AllowedIPs routing table.` — the struct HAS a doc comment. Fields lack docs, matching PARTIAL. The detail text itself concludes 'PARTIAL rather than UNDOCUMENTED.' Tests NONE: confirmed, no test coverage exists for any public method.) |

### `rustguard-tun/src/uring.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `BufferPool` | L26–L31 | NEEDS_FIX | 88% | [DEAD] Exported struct with 0 workspace importers. Published as pub field in UringTun but UringTun itself has 0 importers. Follows library module pattern seen in known false positives (ServeConfig, CookieChecker) where public APIs show 0 workspace consumers. | [UNIQUE] Pre-allocated buffer pool struct for io_uring fixed buffer I/O; no duplicates found | [NEEDS_FIX] slot_ptr (L54-56) obtains *const u8 via self.data.as_ptr() (from a &self borrow) and casts it to *mut u8. The kernel then writes through this pointer while Rust's aliasing model considers the backing Vec<u8> immutably borrowed. Without UnsafeCell wrapping the buffer storage, this violates Rust's strict aliasing rules and is formally UB; an aggressive optimizer may cache loads from self.data and miss kernel-written updates. Additionally, free() (L70-71) performs no bounds check on idx before indexing self.in_flight, causing a panic on any caller-supplied out-of-range index. | [LEAN] Fixed-buffer pool is a first-class io_uring concept (register_buffers requires stable iovecs). The struct encapsulates contiguous allocation, in-flight tracking, slot accessors, and iovec generation — all directly needed for correct io_uring fixed-buffer I/O. Linear scan in alloc() is O(256) worst-case but trivial for this pool size. No unnecessary generics or abstraction layers. | [NONE] No test file exists for rustguard-tun/src/uring.rs. BufferPool has non-trivial runtime behavior including linear-scan alloc(), free(), and bounded slot indexing. The alloc-all-then-None path, double-free, and out-of-bounds slot access are entirely untested. | [DOCUMENTED] Type-level `///` describes purpose. Both private fields carry `///` comments. No public fields to document, so the struct-and-all-fields rule is satisfied. No `# Examples` expected on a struct definition lacking public methods in its declaration block. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → DOCUMENTED — Correction NEEDS_FIX kept: slot_ptr casting *const u8 to *mut u8 from &self is a genuine aliasing violation under stacked borrows; the free() missing bounds check is a minor but valid concern. Utility reclassified DEAD→USED: this is a library crate pattern — BufferPool is pub and exposed as a pub field of UringTun, which is the module's public API entry point. Zero workspace importers is expected for library modules consumed externally. Tests NONE kept: no test file exists and BufferPool has non-trivial alloc/free logic. Documentation reclassified UNDOCUMENTED→DOCUMENTED: the source clearly shows `/// A pre-allocated buffer pool for io_uring fixed buffer I/O.` at L26, and both private fields have `///` comments (L28, L30). The detail text itself confirms DOCUMENTED; the symbol-level UNDOCUMENTED was a merge error.) |
| `UringTun` | L96–L102 | NEEDS_FIX | 88% | [DEAD] Exported struct with 0 workspace importers. Provides public constructor UringTun::new() and public methods (submit_write, submit_and_wait, poll). Typical library module entry point with no workspace consumers detected. | [UNIQUE] io_uring TUN I/O engine struct; no semantic duplicates found | [NEEDS_FIX] Two issues: (1) pending_reads -= 1 at L194 and L226 (in submit_and_wait and poll respectively) has no underflow guard. pending_reads is usize, so an unexpected or duplicate read completion causes a debug-mode panic or a silent release-mode wrap to usize::MAX, after which the refill logic ((NUM_BUFS/2).saturating_sub(pending_reads)) emits zero and the read pipeline permanently stalls. (2) fill_reads is called at L207/L238 while the just-completed buffer slots are still marked in_flight=true (the caller has not yet had a chance to call bufs.free()). alloc() therefore cannot reclaim those slots for the refill; the refill can only draw from slots that were free prior to this call. Under sustained load where all 256 slots are cycling through the caller, fill_reads returns 0 and read starvation occurs until the caller frees slots and calls submit_and_wait/poll again. | [LEAN] The engine composes IoUring + BufferPool + tun_fd + pending_reads into a single coherent unit. Four public methods (new, submit_write, submit_and_wait, poll) map cleanly onto distinct call-site needs. The CQ-processing block is duplicated between submit_and_wait and poll — a mild DRY concern, but not overengineering; inlining avoids extra state for what amounts to a hot loop. No unnecessary generics or factory layers. | [NONE] No test file exists for this module. UringTun has complex runtime behavior: io_uring ring creation, fixed-buffer registration, SQ push failure handling, pending_reads accounting, pipeline refill logic in both submit_and_wait() and poll(). None of this is exercised by any test. | [PARTIAL] Type-level `/// io_uring TUN I/O engine.` is present. Private field `pending_reads` has `///`. However, the public field `pub bufs: BufferPool` has no `///` doc comment at all. Per the struct rule, `///` on each public field is required for DOCUMENTED status; the missing `bufs` doc makes this PARTIAL. (deliberated: reclassified: utility: DEAD → USED — Correction NEEDS_FIX kept: pending_reads -= 1 without underflow guard at L194/L226 is a real risk — usize underflow in release mode wraps to MAX and permanently stalls the read pipeline. The fill_reads timing concern (refill before caller frees slots) is a valid design issue under sustained load. Both are legitimate. Utility reclassified DEAD→USED: UringTun is the primary public entry point of this io_uring TUN engine library module. Zero workspace importers is the standard false-positive pattern for library crates. Tests NONE kept: no test file exists and UringTun has complex stateful logic (ring setup, pending_reads accounting, pipeline refill). Documentation PARTIAL kept: the struct has a type-level doc comment, but `pub bufs: BufferPool` at L98 lacks a `///` doc comment while being the only public field.) |

### `rustguard-tun/src/lib.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `Tun` | L25–L30 | NEEDS_FIX | 78% | [USED] Public struct in library crate with documented public API methods (create, raw_fd, name, read, write). Core abstraction for TUN device operations. Zero workspace importers is a known false positive for library crates consumed by external dependents, consistent with ServeConfig and CookieChecker reclassifications. | [UNIQUE] No RAG data available. Platform-agnostic TUN device wrapper with raw fd and interface name. | [NEEDS_FIX] Two correctness concerns in the impl blocks that belong to this type. (1) The Drop impl at line 102 calls libc::close(self.fd) unconditionally without checking fd >= 0. If a platform-specific create() ever stores an invalid sentinel fd (e.g. -1) in the struct — e.g. on a partially-constructed error path — close(-1) silently returns EBADF and the error is swallowed, masking a real bug. (2) raw_fd() returns a bare i32 rather than BorrowedFd<'_> (std::os::unix::io::BorrowedFd). The method is explicitly documented for io_uring and AF_XDP use. Callers can trivially wrap the returned integer in an OwnedFd or equivalent owning type; when that type is dropped it calls close() on the same fd, and then Tun::drop() calls close() a second time, producing a double-close. On Linux a double-close can silently close an unrelated fd that was opened between the two closes — a data-corruption-class bug, not merely a resource leak. | [LEAN] Two-field struct holding exactly what is needed: a raw fd for syscall/io_uring/XDP integration and an interface name for diagnostics. No unnecessary generics, no trait indirection, no builder machinery. The platform dispatch is handled at the method level via cfg attributes rather than via vtables or dynamic dispatch, which is the idiomatic and minimal Rust approach. | [NONE] No test file exists anywhere under rustguard-tun/. The Tun struct carries significant runtime behavior via its impl block (create, read, write, raw_fd, Drop), all of which involve OS-level syscalls. None of these code paths—happy path, error paths, platform guards, or Drop cleanup—are exercised by any test. | [PARTIAL] Has a type-level /// doc comment (L21–L24) describing purpose and platform-specific behaviour (macOS utun vs Linux /dev/net/tun). Private fields fd and name carry /// annotations. No # Examples section is present for this public struct, and no description of error conditions, thread-safety, or typical creation/read/write usage pattern is provided. (deliberated: confirmed — Correction NEEDS_FIX: The raw_fd() returning bare i32 concern is valid — the standard library introduced BorrowedFd/OwnedFd specifically to prevent double-close bugs, and this is a public API explicitly documented for io_uring/AF_XDP integration where callers are highly likely to wrap fds in owning types. This is a real API safety gap, not just a style preference. The Drop fd >= 0 guard (action 2) is lower severity — Tun can only be constructed via create() which returns Result, so an invalid fd reaching Drop requires a logic error in the platform modules — but it's still sound defensive programming. Keeping NEEDS_FIX at moderate confidence since these are design-level improvements rather than outright bugs in current code paths. Tests NONE: Accurate — no tests exist. This is a systems-level struct requiring OS privileges to test, which explains the absence but doesn't excuse it; at minimum, TunConfig construction and basic Tun field accessors could be tested with mock fds. Documentation PARTIAL: Accurate — type-level and field-level docs exist and are informative, but a public library API of this importance should have # Examples, # Errors, and # Safety sections (especially for raw_fd).) |

### `rustguard-crypto/src/tai64n.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `Tai64n` | L9–L9 | NEEDS_FIX | 87% | [USED] Exported struct with 1 runtime importer (rustguard-crypto/src/lib.rs) per pre-computed analysis. | [UNIQUE] No RAG similarity found; single struct definition implementing TAI64N timestamp for WireGuard | [NEEDS_FIX] Two correctness issues in the impl block. (1) `from_unix` at L32 computes `secs + TAI64_EPOCH_OFFSET` using plain `+`, which wraps silently in release builds or panics in debug builds for any `secs` value above ~13.8e18 — not a concern for realistic Unix timestamps, but the public API accepts arbitrary u64. `saturating_add` or an explicit overflow check is the correct fix. (2) `from_bytes` at L43-L44 accepts any 12-byte payload without validating that the nanoseconds field (bytes 8–11) is ≤ 999_999_999. The doc-comment on `from_unix` explicitly names the 999_999_999 clamp as the invariant that WireGuard's byte-wise ordering relies on; a deserialized timestamp with nanos > 999_999_999 would byte-compare as strictly greater than a legitimately-created timestamp with the same second and nanos = 999_999_999, silently breaking replay-protection ordering. | [LEAN] Minimal newtype wrapper over [u8; 12] with exactly the API surface WireGuard replay-prevention requires: construction from system time or raw unix values, byte serialization, and a strict ordering predicate. No unnecessary generics, no trait hierarchies, no configuration. The `#[cfg(feature = "std")]` gating on `now()` is idiomatic and justified for no_std (kernel) targets. Byte-wise Ord derived from the big-endian encoding is a clean protocol-level trick, not complexity. | [WEAK] Inline tests cover monotonic ordering via now()+is_after(), and roundtrip via as_bytes()/from_bytes() for both now() and from_unix(). However, the documented nanos-clamping invariant (nanos > 999_999_999 → 999_999_999) — explicitly called out as critical for WireGuard byte-wise ordering — is never tested. The false case of is_after() is absent. No test verifies the actual byte-level output against expected WireGuard-compatible TAI64N values, so protocol correctness is unconfirmed. | [PARTIAL] The struct carries four lines of `///` commentary (lines 4–8) that explain its purpose (replay prevention), binary layout (8 B seconds + 4 B nanoseconds), and WireGuard semantics. However, no `# Examples` section is present on this public type, which is expected by Rust doc conventions for public API structs. Individual methods have their own docs, but the type-level entry point lacks a usage example showing how to construct and compare timestamps. (deliberated: confirmed — Correction NEEDS_FIX confirmed for both issues. (1) Overflow in `secs + TAI64_EPOCH_OFFSET` at L34 is low-severity but valid — in a crypto crate with a public API, defensive arithmetic is warranted; saturating_add is a trivial fix. (2) `from_bytes` lacking nanos validation is the more important issue: the crate explicitly documents the nanos ≤ 999_999_999 invariant as critical for WireGuard byte-wise ordering, yet `from_bytes` (which deserializes untrusted wire data) does not enforce it, creating a real attack surface for replay-protection bypass. Tests WEAK confirmed: the three tests cover happy-path monotonicity, roundtrip, and from_unix roundtrip, but miss nanos-clamping verification (e.g. from_unix with nanos > 999_999_999), the false case of is_after, and known-good byte comparison against WireGuard reference vectors — all meaningful gaps for a crypto primitive. Documentation PARTIAL confirmed: struct-level docs are good but `as_bytes` and `from_bytes` lack any `///` comments, and no `# Examples` section exists on the public type.) |

### `rustguard-core/src/replay.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `ReplayWindow` | L13–L19 | NEEDS_FIX | 92% | [USED] Exported struct with 1 runtime importer: rustguard-core/src/session.rs | [UNIQUE] No semantic duplication found. Struct implementing WireGuard anti-replay sliding window algorithm per RFC 6479. | [NEEDS_FIX] The bit-level shift inside shift_window (lines 112-116) is inverted in both direction and iteration order, causing all existing counter records to be silently erased on every window advance. The bitmap layout places newer counters at lower bit-indices (bit 0 = age 0 = current top). When the window advances by shift, every recorded counter becomes older by shift positions, so its bit-index must increase — a left shift within each word. The code instead right-shifts (*word >> bit_shift) and iterates from the highest word index down to the lowest (.rev()), which moves bits toward lower (newer) positions; the carry produced at word 0 is discarded, destroying all existing age information. Concrete trace: top=1, bitmap[0]=0b10 (counter 0 at age 1), new counter 3 arrives (shift=2). Correct result: bitmap[0] should become 0b1000 (counter 0 now at age 3). Actual result: the loop sets bitmap[0]=0 and loses counter 0 entirely, allowing it to be replayed without detection. All supplied tests pass only because none replay a counter value that was accepted before a window advance; in any real deployment this defeats the anti-replay guarantee. The fix is to iterate forward (low to high word index), left-shift (*word << bit_shift), and extract carry with *word >> (64 - bit_shift). | [LEAN] Two-field struct (top counter + fixed bitmap array) is the canonical minimal representation for an RFC 6479 / WireGuard sliding-window replay detector. No trait objects, generics, builder pattern, or unnecessary indirection — just the data the algorithm requires. | [GOOD] Nine in-file unit tests cover the struct's full security contract: sequential acceptance (sequential_counters_accepted), duplicate/replay rejection (duplicate_rejected), out-of-order delivery within the window (out_of_order_within_window, old_within_window_accepted_once), too-old rejection (too_old_rejected), large counter jump resetting the window (large_jump_clears_window), exact boundary semantics at age WINDOW_SIZE-1 vs WINDOW_SIZE (window_boundary_exact), and two stress tests (10 000 sequential, full reverse within window). Happy path, edge cases, and the core anti-replay security invariant are all explicitly verified. The only minor gap is that `check()` and `update()` are never exercised independently of `check_and_update`, but the combined tests functionally cover both code paths. | [PARTIAL] Public struct with no `///` doc comment on the type declaration itself — the module-level `//!` block explains the algorithm but does not substitute for a struct-level doc comment. Both private fields carry `///` comments describing their roles, which is good but does not satisfy the requirement for a type-level description. No `# Examples` section is present on the struct or its `new()` constructor. (deliberated: confirmed — Correction NEEDS_FIX is CONFIRMED with higher confidence after independent verification. The bit-level shift in shift_window (L112-116) is inverted. The bitmap layout (line 16-17) encodes age as flat_index = word*64+bit, with age 0 at bitmap[0] bit 0. When the window advances by `shift`, existing bits must move to higher flat indices (left-shift within words, carry from word j to j+1, iterating low→high). The code does the opposite: right-shifts within words and iterates high→low (.rev()), causing carry to flow from high words to low words — moving bits toward lower ages rather than higher. Concrete trace: top=5, bitmap[0]=0b111 (counters 3,4,5), counter 7 arrives (shift=2). Expected: bitmap[0]=0b11100. Actual: bitmap[0]=(0b111>>2)|0=0b1, destroying counters 3 and 4. All 9 tests pass only because none attempt to verify a previously-accepted counter survives a window advance — sequential tests never replay, out-of-order tests don't trigger shifts, and boundary tests check fresh counters. This silently defeats the anti-replay security guarantee in any real deployment where packets arrive out of order after window advances. Raising confidence from 55 to 92 based on complete manual trace. Documentation PARTIAL is also correct — struct lacks its own `///` doc comment despite being the sole public type.) |

## Quick Wins

- [ ] <!-- ACT-f55ce3-1 --> **[correction · high · small]** `rustguard-core/src/replay.rs`: Fix shift_window bit-level shift: change iteration from .rev() to forward order, replace `*word >> bit_shift` with `*word << bit_shift`, and replace carry extraction `*word << (64 - bit_shift)` with `*word >> (64 - bit_shift)`. This ensures existing counter bits advance to higher-index (older) positions rather than being destroyed on every window slide. [L112]
- [ ] <!-- ACT-4d0b70-1 --> **[correction · high · small]** `rustguard-tun/src/lib.rs`: Change raw_fd() return type from i32 to BorrowedFd<'_> (std::os::unix::io::BorrowedFd) so callers cannot wrap the fd in an owning type that will close it independently, avoiding a double-close that would silently close an unrelated fd on Linux. [L60]
- [ ] <!-- ACT-310cbd-2 --> **[correction · medium · small]** `rustguard-crypto/src/tai64n.rs`: Add nanoseconds validation in `from_bytes`: if `u32::from_be_bytes(bytes[8..12].try_into().unwrap()) > 999_999_999`, either return a `Result::Err`, clamp the field, or otherwise reject the input — to uphold the byte-wise ordering invariant documented on `from_unix` and required for correct replay-protection comparisons. [L43]
- [ ] <!-- ACT-a01ecd-1 --> **[correction · medium · small]** `rustguard-kmod/src/allowedips.rs`: Implement path compression: when creating a TrieNode during insertion, set `bit` to the actual bit index being tested at that node, and update `lookup_recursive` to jump directly to `node.bit` rather than using sequential depth. Alternatively, remove the `bit` field and update the module doc to state this is an uncompressed binary trie. [L18]
- [ ] <!-- ACT-050448-1 --> **[correction · medium · small]** `rustguard-kmod/src/timers.rs`: In is_dead() (L109), replace `self.session_established` with `self.last_received` so the dead-session check measures true inactivity (time since last received packet) rather than session age. Current code kills sessions with recent traffic once they are older than DEAD_SESSION_TIMEOUT_NS, which contradicts the documented semantics and can prematurely zero live session keys. [L109]
- [ ] <!-- ACT-050448-2 --> **[correction · medium · small]** `rustguard-kmod/src/timers.rs`: In needs_keepalive() (L121-L125), change the last_sent == 0 fallback from `now.saturating_sub(self.last_received)` to `u64::MAX`. The current fallback makes since_last_send identical to since_last_recv, making the condition `>= interval && < interval` permanently false. Using u64::MAX correctly models 'we have never sent, so infinite time has elapsed since our last send' and allows keepalives to fire as expected. [L124]
- [ ] <!-- ACT-0723df-1 --> **[correction · medium · small]** `rustguard-tun/src/uring.rs`: Replace the bare Vec<u8> backing store in BufferPool with a UnsafeCell<Vec<u8>> (or use raw allocation via alloc::alloc) so that shared mutable access by the kernel through pointers derived from &self does not violate Rust's aliasing rules. Alternatively, change slot_ptr to require &mut self to align the borrow with the mutation contract. [L55]
- [ ] <!-- ACT-0723df-2 --> **[correction · medium · small]** `rustguard-tun/src/uring.rs`: Guard pending_reads decrements with a checked subtraction or a debug_assert before subtracting 1 (e.g., `debug_assert!(self.pending_reads > 0); self.pending_reads -= 1;`) in both submit_and_wait (L194) and poll (L226) to prevent underflow from unexpected/duplicate read completions silently corrupting the refill counter. [L194]
- [ ] <!-- ACT-310cbd-1 --> **[correction · low · small]** `rustguard-crypto/src/tai64n.rs`: Replace `secs + TAI64_EPOCH_OFFSET` with `secs.saturating_add(TAI64_EPOCH_OFFSET)` (or add an explicit overflow check and return an error) to prevent undefined wrapping in release builds when an out-of-range `secs` value is passed to the public `from_unix` API. [L32]
- [ ] <!-- ACT-a01ecd-2 --> **[correction · low · small]** `rustguard-kmod/src/allowedips.rs`: Remove the two dead assignments `node.cidr = 0; node.peer_idx = peer_idx;` at lines 116–117 inside the `cidr == 0` branch of `insert()`. Only the sentinel assignments (cidr=255) should remain to avoid confusion about the actual stored state. [L116]
- [ ] <!-- ACT-a01ecd-3 --> **[correction · low · small]** `rustguard-kmod/src/allowedips.rs`: Eliminate the asymmetry between the parent pre-check and the recursive-entry check in `lookup_recursive`. Remove the pre-check block entirely (lines 176–179) — it is fully subsumed by the recursive call's own entry check — or unify both checks to handle cidr==255 identically, preventing a latent incorrect-match if a non-root node ever acquires cidr==255. [L177]
- [ ] <!-- ACT-050448-3 --> **[correction · low · small]** `rustguard-kmod/src/timers.rs`: Change REKEY_AFTER_MESSAGES (L23) from `(1u64 << 60) - 1` to `1u64 << 60` to align with the WireGuard whitepaper Section 6 and the Linux kernel reference implementation. [L23]
- [ ] <!-- ACT-4d0b70-2 --> **[correction · low · small]** `rustguard-tun/src/lib.rs`: Guard the libc::close call in Drop with `if self.fd >= 0` to avoid silently swallowing an EBADF if an invalid fd sentinel ever reaches the struct (defensive correctness for error-path robustness). [L102]
- [ ] <!-- ACT-0723df-3 --> **[correction · low · small]** `rustguard-tun/src/uring.rs`: Add a bounds check in BufferPool::free() (`assert!(idx < NUM_BUFS, ...);`) to produce a clear panic message rather than an opaque index-out-of-bounds when callers pass a stale or corrupted buf_idx. [L71]
- [ ] <!-- ACT-0723df-4 --> **[correction · low · small]** `rustguard-tun/src/uring.rs`: Consider moving the fill_reads refill call (L207, L239) to after the completions are returned to the caller (i.e., have the caller trigger a separate refill pass), or pre-free error completions inside submit_and_wait/poll before calling fill_reads, so that just-completed slots are immediately available for re-submission and the read pipeline does not stall when all buffers have been handed to the caller. [L207]
