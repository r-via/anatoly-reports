[← Back to Correction](./index.md) · [← Back to report](../../public_report.md)

# Correction — Shard 1

## Findings

| File | Verdict | Correction | Conf. | Details |
|------|---------|------------|-------|---------|
| `rustguard-kmod/src/lib.rs` | CRITICAL | 6 | 90% | [details](#rustguard-kmodsrclibrs) |
| `rustguard-tun/src/xdp.rs` | CRITICAL | 1 | 95% | [details](#rustguard-tunsrcxdprs) |
| `rustguard-enroll/src/server.rs` | CRITICAL | 1 | 92% | [details](#rustguard-enrollsrcserverrs) |
| `rustguard-kmod/src/replay.rs` | CRITICAL | 1 | 90% | [details](#rustguard-kmodsrcreplayrs) |
| `rustguard-kmod/src/noise.rs` | NEEDS_REFACTOR | 3 | 92% | [details](#rustguard-kmodsrcnoisers) |
| `rustguard-core/src/handshake.rs` | NEEDS_REFACTOR | 1 | 88% | [details](#rustguard-coresrchandshakers) |
| `rustguard-core/src/cookie.rs` | NEEDS_REFACTOR | 4 | 92% | [details](#rustguard-coresrccookiers) |
| `rustguard-daemon/src/tunnel.rs` | NEEDS_REFACTOR | 2 | 90% | [details](#rustguard-daemonsrctunnelrs) |
| `rustguard-core/src/timers.rs` | NEEDS_REFACTOR | 5 | 90% | [details](#rustguard-coresrctimersrs) |
| `rustguard-enroll/src/control.rs` | NEEDS_REFACTOR | 1 | 95% | [details](#rustguard-enrollsrccontrolrs) |

## Symbol Details

### `rustguard-kmod/src/lib.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `RustGuard` | L171–L171 | NEEDS_FIX | 80% | [USED] Module struct implementing kernel::Module trait; required for kernel module initialization and cleanup (init, Drop implementations) | [UNIQUE] Module struct implementing kernel::Module trait. No similar struct found. | [NEEDS_FIX] Multiple early-return error paths in init() leak already-initialized subsystems. When wg_queue_init() fails, wg_crypto_exit() is not called. When KBox allocation, wg_create_device, or wg_socket_create fail, neither wg_crypto_exit() nor wg_queue_destroy() is called despite both having succeeded. Additionally the return value of wg_timer_start() is silently ignored; a timer-start failure disables all rekeying and keepalive processing, causing sessions to expire without recovery. | [LEAN] Zero-sized struct used as the kernel module type token, following the standard kernel::Module pattern. Initialization and teardown logic is appropriately placed in init() and Drop. | [NONE] Module entry point struct with complex init() (keypair gen, device/socket creation, peer config, handshake initiation) and Drop teardown. No test file exists for rustguard-kmod; zero test coverage. | [UNDOCUMENTED] Private zero-sized struct with no `///` doc comment. Its role as the kernel module handle type implementing `kernel::Module` is undocumented at the symbol level. (deliberated: reclassified: tests: WEAK → NONE — NEEDS_FIX confirmed: init() has multiple early-return paths that leak subsystems — when wg_queue_init fails wg_crypto_exit is not called; when KBox allocation, wg_create_device, or wg_socket_create fail neither wg_crypto_exit nor wg_queue_destroy is called. wg_timer_start return value silently ignored at L289. Tests reclassified WEAK→NONE: no test file exists for this kernel module; zero test coverage.) |
| `cleanup_state` | L359–L376 | NEEDS_FIX | 88% | [USED] Internal function called from init error path (L212) and Drop (L354); zeroizes key material and frees DeviceState heap allocation | [UNIQUE] Zeroizes key material and deallocates device state. No similar cleanup function found. | [NEEDS_FIX] Only peer.session key material is zeroized; peer.prev_session.key_send and peer.prev_session.key_recv are never zeroized. After a rekey the previous session's keying material persists in freed heap memory. pending_handshake may also contain ephemeral scalar/DH material that is not scrubbed. | [LEAN] Zeroizes key material and frees heap allocation. All steps are necessary for security hygiene in a WireGuard implementation. No excess abstraction. | [NONE] Security-critical function that zeroizes key material before freeing DeviceState. No test file exists for rustguard-kmod; zero test coverage. | [UNDOCUMENTED] Private unsafe function with no `///` doc comment. Only has an inline `// C3: Zeroize key material before freeing.` comment. Safety preconditions (valid pointer, must not be used after call) and ownership transfer are unspecified. (deliberated: confirmed — NEEDS_FIX confirmed: source code at L363-373 zeroizes peer.session.key_send/key_recv but never touches peer.prev_session keys or pending_handshake ephemeral DH state. Genuine key-residue risk on module unload. NONE tests and UNDOCUMENTED confirmed: security-critical function with no tests and only an inline // comment, no /// doc comment.) |
| `do_xmit` | L386–L458 | ERROR | 75% | [USED] Internal TX implementation called directly from rustguard_xmit (L384); handles AllowedIPs lookup, encryption, and packet transmission | [UNIQUE] TX path implementation: performs AllowedIPs lookup, builds header, encrypts, sends. No similar implementation found. | [ERROR] wg_kfree_skb(skb) is called immediately after wg_skb_prepend_header(skb, WG_HEADER_SIZE, AEAD_TAG_SIZE) returns tx_skb. In standard Linux kernel networking, skb_push / skb_cow_head style helpers that 'prepend a header' operate in-place and return the same sk_buff pointer (tx_skb == skb). If that is the case here, wg_kfree_skb(skb) frees the buffer that tx_skb still references; every subsequent access — wg_skb_data_ptr(tx_skb), wg_queue_encrypt(tx_skb,...), wg_encrypt_skb(tx_skb,...), wg_socket_send_skb(...,tx_skb,...), wg_kfree_skb(tx_skb) — is a use-after-free. Even if the helper allocates a new skb, it may have already freed the original internally (ownership transfer), making the explicit wg_kfree_skb(skb) a double-free. | [LEAN] Implements the full TX path: AllowedIPs lookup, session expiry check, header construction, async/sync crypto dispatch, and stats update. Each step is required by the WireGuard protocol. The async/sync branch is justified by the module parameter. No unnecessary abstraction. | [NONE] Core TX path: AllowedIPs lookup, session expiry check, header construction, sync/async encrypt dispatch. Complex logic with multiple branches; no test file exists for rustguard-kmod. | [UNDOCUMENTED] Private unsafe function with no `///` doc comment. Inline comments explain individual steps but there is no function-level description of preconditions, the async vs sync crypto path selection, or session expiry behavior. (deliberated: confirmed — ERROR confirmed: L432-433 calls wg_kfree_skb(skb) immediately after wg_skb_prepend_header returns tx_skb. Under standard kernel networking conventions (skb_push/skb_cow_head operate in-place), tx_skb==skb producing a use-after-free. Even if the C shim allocates a new buffer, it may free the original internally (ownership transfer) making the explicit free a double-free. Cannot downgrade without ≥95 confidence in the C implementation contract. NONE tests and UNDOCUMENTED confirmed.) |
| `handle_transport` | L676–L755 | NEEDS_FIX | 80% | [USED] Transport handler called from do_rx for MSG_TRANSPORT (L507); decrypts packets with replay protection, injects plaintext into kernel stack | [UNIQUE] RX transport packet handler: decrypts with replay protection, injects into network stack. No similar transport handler found. | [NEEDS_FIX] The stack decrypt buffer is hard-coded to 2048 bytes and packets with encrypted payload >2048 bytes are silently dropped. Maximum usable plaintext is 2032 bytes (2048 - AEAD_TAG_SIZE). Standard Ethernet MTU (1500) fits, but the net_device MTU can be configured larger (jumbo frames up to 9000+ bytes). Packets exceeding 2032 bytes of plaintext are silently discarded, causing unexplained connectivity failures without any error counter or log entry. | [LEAN] Decrypts transport packets with replay protection and dual-session fallback (current + previous session for in-flight rekeying). The stack buffer approach (2048 bytes) is a deliberate no_std constraint. All complexity is protocol-driven. | [NONE] Transport decryption with replay window check, fallback to prev_session, plaintext injection into network stack. Security-critical; no test file exists for rustguard-kmod. | [PARTIAL] Private unsafe function has three `///` lines covering purpose, safety, and replay_window exclusivity. Better than sibling handlers but still missing parameter descriptions, decrypt-fallback-to-prev_session behavior, and keepalive/endpoint-roaming handling. (deliberated: confirmed — NEEDS_FIX confirmed: L708 hardcodes 2048-byte stack buffer and L710 silently drops packets with encrypted_len > 2048. Max usable plaintext is 2032 bytes, insufficient for jumbo frames (9000+). Silent discard with no error counter or log is the key issue. NONE tests and PARTIAL docs confirmed.) |
| `handle_cookie_reply` | L758–L781 | NEEDS_FIX | 80% | [USED] Cookie handler called from do_rx for type 3 (L504); processes MAC2 cookie from peer, stores for retry on initiator side | [UNIQUE] Cookie reply (type 3) message handler. Stores cookie state for MAC2 validation. No similar handler found. | [NEEDS_FIX] our_mac1 is hardcoded to [0u8;16] (acknowledged TODO). Cookie reply authentication uses HMAC-keyed over the initiator's MAC1; passing all-zeros means any crafted reply is accepted and stored. The subsequent retry with MAC2 will compute its MAC2 over the stored-but-wrong cookie, failing server-side MAC2 verification. DoS protection is non-functional as a result. | [LEAN] Minimal cookie reply handler. The TODO comment about MAC1 storage acknowledges a known limitation rather than papering over it with abstraction. | [NONE] Cookie reply handler for MAC2 DoS protection; stores received cookie for retry. No test file exists for rustguard-kmod; zero test coverage. | [PARTIAL] Private unsafe function has one `///` line stating purpose. Missing: parameter descriptions, the known limitation (TODO: store last sent MAC1 per peer documented only inline), and return semantics. (deliberated: confirmed — NEEDS_FIX confirmed: L777 uses [0u8;16] for our_mac1 with an acknowledged TODO. Without correct MAC1, cookie reply HMAC verification is bypassed and any crafted reply is accepted. Subsequent MAC2 retry will compute over the wrong cookie, failing server-side. DoS protection is non-functional. NONE tests and PARTIAL docs confirmed.) |
| `do_timer_tick` | L793–L844 | NEEDS_FIX | 82% | [USED] Called from rustguard_timer_tick (L791); iterates peers, checks expiry, triggers rekeying/keepalives via initiate_rekey and send_keepalive | [UNIQUE] Periodic timer callback (250ms): checks rekeying, keepalives, session expiry for all peers. No similar timer logic found. | [NEEDS_FIX] peer is bound as &mut Peer from (*state).peers[peer_idx] and remains live when initiate_rekey(state, peer_idx) is called; inside initiate_rekey a second &mut Peer is created from (*state).peers[peer_idx]. Simultaneously holding two &mut references to the same memory location is undefined behavior under Rust's aliasing rules even inside unsafe blocks. The same alias occurs for the second initiate_rekey call (retry branch) and the send_keepalive(state, peer_idx) call, both of which also re-borrow the same peer slot mutably. | [LEAN] Iterates peers and drives the WireGuard session state machine: dead session teardown, rekey initiation, handshake retry/timeout, and keepalive. All branches correspond to WireGuard timer requirements. No unnecessary abstraction. | [NONE] Periodic timer logic: dead session zeroization, rekey triggering, handshake retry/timeout, keepalive scheduling. Complex state machine; no test file exists for rustguard-kmod. | [UNDOCUMENTED] Private unsafe function with no `///` doc comment. The rekeying decision logic, keepalive trigger, and dead-session zeroization behavior are documented only through inline `//` comments. (deliberated: confirmed — NEEDS_FIX confirmed: L799 binds peer as &mut from (*state).peers[peer_idx] which remains live when initiate_rekey(state, peer_idx) is called at L829/L835, creating a second &mut to the same memory — undefined behavior under Rust aliasing rules. Same issue with send_keepalive at L841. NONE tests and UNDOCUMENTED confirmed.) |

### `rustguard-tun/src/xdp.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `XdpSocket` | L148–L160 | ERROR | 95% | [USED] Primary public struct returned from create(); used in all socket operations | [UNIQUE] AF_XDP socket wrapper managing UMEM and ring buffers. Unique socket implementation with no duplicates. | [ERROR] Three correctness bugs found in impl methods. (1) tx_send (around L348-L352) calls copy_nonoverlapping with data.len() bytes but never checks data.len() <= self.frame_size; any caller passing a packet larger than frame_size causes an out-of-bounds write into adjacent UMEM frames, silently corrupting in-flight packets or kernel-visible memory. (2) Drop (around L396-L403) unmaps only self.umem; the four ring mmap regions allocated by mmap_ring are never unmapped because make_ring discards the base pointers and their sizes are not stored anywhere in Ring or XdpSocket, leaking at minimum four mmap regions on every drop. (3) create_inner (around L217-L220) discards the return value of all four ring-size setsockopt calls; a kernel-side failure leaves the socket with default (likely zero) ring sizes, after which the mmap offsets returned by getsockopt and the ring I/O operations produce undefined behavior without any indication to the caller. | [LEAN] Owns exactly what AF_XDP requires: fd, UMEM region, four rings, and a free-frame pool. No extraneous layers; the public API (rx_poll, rx_release, tx_send, tx_complete, fd) maps one-to-one with the AF_XDP usage model. | [NONE] Central AF_XDP socket struct with complex unsafe lifecycle methods (create, rx_poll, rx_release, tx_send, tx_complete, drop). No test file exists for this module; entirely untested. | [PARTIAL] Public struct. Has a two-line type-level `///` doc comment describing purpose and zero-copy role. Only one field (`free_frames`) has a `///` comment; all other fields (`fd`, `umem`, `umem_size`, `frame_size`, ring fields, `ring_size`) lack doc comments. No `# Examples` section on the type. (deliberated: reclassified: overengineering: ACCEPTABLE → LEAN — Correction ERROR confirmed at high confidence — all three bugs verified in source: (1) tx_send at ~L333 calls copy_nonoverlapping with data.len() but never bounds-checks against frame_size, enabling out-of-bounds write into adjacent UMEM frames; (2) Drop at L397-400 only munmaps self.umem while four ring mmap regions from mmap_ring are never stored/unmapped, leaking kernel-mapped memory every drop; (3) setsockopt_raw calls at L230-233 discard return values silently, allowing the socket to proceed with misconfigured rings. Overengineering reclassified ACCEPTABLE → LEAN: the detail itself states 'No extraneous layers; the public API maps one-to-one with the AF_XDP usage model,' which is the definition of LEAN — ACCEPTABLE was unjustified. Tests NONE confirmed: no test file exists. Documentation PARTIAL confirmed: type-level docs present but most fields undocumented.) |

### `rustguard-enroll/src/server.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `run` | L64–L532 | ERROR | 92% | [DEAD] Exported public function (main tunnel server loop); pre-computed analysis shows 0 external importers (likely binary-internal call pattern not captured) | [UNIQUE] Similar functions in rustguard-daemon/src/tunnel.rs (score 0.760) and rustguard-enroll/src/client.rs (score 0.737) both below 0.82 threshold. Despite structural similarity in TUN/UDP/threading patterns, these have different semantic contracts: enrollment server with dynamic peer pool vs daemon tunnel with static config vs client enrollment. Not interchangeable. | [ERROR] Two correctness defects: (1) In the MSG_TRANSPORT handler, `decrypt_buf` is fixed at 2048 bytes, but `ct = &buf[TRANSPORT_HEADER_SIZE..]` is unbounded — if a peer (or attacker with a known session receiver_index) sends a UDP datagram whose payload exceeds 2048 bytes, `decrypt_buf[..ct.len()].copy_from_slice(ct)` will panic with an out-of-bounds index, crashing the inbound thread. XDP frames can be up to 4096 bytes, making this reachable. A bounds check (`if ct.len() > decrypt_buf.len() { continue; }`) is required before the copy. (2) In the MSG_INITIATION handler, `handshake::process_initiation` returns a 4-tuple `(pk, ts, resp, session)` where `ts` is the TAI64N handshake timestamp; the binding discards it via `_ts` and the timestamp is never stored per-peer. WireGuard requires the responder to record the latest received timestamp per initiator and reject any initiation whose timestamp is not strictly greater, providing replay protection. Discarding `_ts` means any captured initiation packet can be replayed indefinitely to reset sessions. | [ACCEPTABLE] At ~470 lines this function carries three distinct I/O backends (standard UDP, io_uring, AF_XDP), multi-queue TUN, peer restoration, enrollment logic, and full WireGuard handshake + transport handling — all in a single monolith. The per-path logic itself is not redundant, but the function mixes initialization, thread spawning, and runtime concerns without decomposition. In the XDP inbound path the lock on xsk_mtx is also dropped and immediately re-acquired for rx_release, and packets are collected into a heap-allocated Vec<> each iteration against the stated zero-alloc goal. The complexity is largely justified by explicit performance requirements, but decomposing setup (TUN/XDP/uring init) from the thread closures would reduce cognitive overhead without sacrificing performance. | [NONE] No test file exists for server.rs. The run function is the core server entrypoint encompassing enrollment, handshake, inbound/outbound packet paths, XDP, io_uring, multi-queue TUN, and peer persistence. None of these paths have any test coverage. | [UNDOCUMENTED] Public function and sole public entry point of the enrollment server with no /// doc comments. No description of overall behavior, ServeConfig parameter semantics, io::Result error conditions (bind failures, TUN creation, io_uring init), blocking behavior, or # Examples. Critical documentation gap for a ~470-line public API function. (deliberated: reclassified: utility: DEAD → USED — Correction ERROR confirmed (both defects verified in source): (1) L444-446: `decrypt_buf` is 2048 bytes but `ct = &buf[TRANSPORT_HEADER_SIZE..]` is unbounded from UDP recv; `decrypt_buf[..ct.len()].copy_from_slice(ct)` will panic if ct.len() > 2048. XDP frames up to 4096 bytes make this reachable. (2) L413: `_ts` (TAI64N timestamp) is discarded, removing WireGuard's mandated initiation replay protection. Both are genuine security/correctness bugs visible in the code. Utility reclassified DEAD→USED: `run` is the sole public entry point of the enrollment server module — it is the raison d'être of this crate. The binary crate calls it from main(). '0 external importers' is a static analysis false positive for binary-consumed library crates. Tests NONE and Documentation UNDOCUMENTED: both confirmed.) |

### `rustguard-kmod/src/replay.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `ReplayWindow` | L11–L14 | ERROR | 88% | [DEAD] Exported pub(crate) struct with 0 workspace importers per Pre-computed Import Analysis. | [UNIQUE] No semantic duplicates found in RAG results. Anti-replay window struct implementing RFC 6479 sliding window algorithm. | [ERROR] Critical bug in shift_window: the bit-level carry loop iterates `for i in (0..BITMAP_LEN).rev()` — i.e. from index BITMAP_LEN-1 down to 0 — but carry must propagate in the opposite direction. When bits are shifted LEFT within each 64-bit word (moving toward higher bit positions = older counters), any bit at word[i] bit 63 that overflows must land at word[i+1] bit 0 (higher index = older). Achieving this requires iterating 0..BITMAP_LEN (low to high), collecting the overflow from word[i] as carry and applying it to word[i+1] in the next iteration. With the reversed loop, carry from word[i] is instead applied to word[i-1] (newer), and the carry produced by the lowest-indexed word is discarded entirely. Concrete counterexample: bitmap[0] = 0x8000_0000_0000_0000 (bit 63 set, counter top-63 seen), bitmap[1] = 0, bit_shift = 1. Expected result: bitmap[0] = 0, bitmap[1] = 1 (bit 63 moves to word[1] bit 0). Actual result with buggy loop: bitmap[0] = 0, bitmap[1] = 0 — the seen-bit is silently dropped. Any counter whose 'seen' bit sits at bit 63 of a word will have that bit lost on the next window advance that has a non-zero bit_shift, allowing a replayed packet to be accepted. | [LEAN] Struct encapsulates exactly two fields needed for the sliding-window algorithm (top counter + bitmap). No generics, no trait hierarchies, no builder pattern — just a flat struct implementing a well-known, fixed-size cryptographic primitive from RFC 6479. The implementation complexity in the methods is inherent to the algorithm, not imposed by the design. | [NONE] No test file found for rustguard-kmod/src/replay.rs. ReplayWindow implements security-critical anti-replay logic (check, update, shift_window) equivalent to RFC 6479 / kernel WireGuard. There are zero tests for the happy path, edge cases (counter == top, counter just inside/outside the 2048-bit window, counter == 0 on init, large forward jumps that zero the bitmap), or replay detection correctness. The sister implementation in rustguard-core/src/replay.rs may have tests, but this kmod copy is entirely untested. | [UNDOCUMENTED] pub(crate) struct has no /// doc comment at the type level. No description of the sliding-window semantics, invariants, or usage lifecycle is attached to the struct declaration itself. While two methods (check, update) carry brief /// comments, the struct type — which governs how top and bitmap jointly encode state — is entirely undocumented. Confidence reduced to 75 because pub(crate) visibility is crate-internal only, warranting some leniency. (deliberated: reclassified: documentation: UNDOCUMENTED → PARTIAL — CORRECTION ERROR confirmed at 93% confidence: Traced the carry loop manually. With `(0..BITMAP_LEN).rev()`, at iteration i, carry accumulated from word[i+1] is applied to word[i], propagating carry high→low. But a left shift within words produces overflow at bit 63 that must land in word[i+1] bit 0, requiring low→high propagation. The code comment even states 'Carry propagates from word[0] to word[BITMAP_LEN-1]' (correct direction) but then contradictorily iterates high-to-low. Concrete trace: bitmap[0]=0x8000..., bit_shift=1 → reverse loop drops the overflow bit entirely, forward loop correctly places it in bitmap[1] bit 0. Security-critical bug confirmed. UTILITY DEAD kept: 0 workspace importers per pre-computed analysis is factually correct, though the file header ('Ported from rustguard-core/src/replay.rs') suggests active development — the module may simply not be wired up yet. Confidence stays at 75 to reflect this ambiguity. TESTS NONE confirmed: no test file exists for this security-critical anti-replay module. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the module-level `//!` doc block explicitly describes 'Anti-replay sliding window (2048-bit bitmap)' and references RFC 6479, which directly covers this struct (the sole exported type). Both `check` and `update` methods carry `///` doc comments. Only the struct declaration itself lacks a `///` comment. For `pub(crate)` visibility, this is PARTIAL rather than fully UNDOCUMENTED.) |

### `rustguard-kmod/src/noise.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `hash` | L72–L94 | NEEDS_FIX | 80% | [USED] Not exported; called in initial_chain_key (L180), initial_hash (L184), mix_hash (L188), and compute_mac1 (L215). | [UNIQUE] Similar to cookie.rs hash (0.937 score) but different semantic contract: noise.rs supports 8 chunks, cookie.rs supports 4; chunk capacity is a semantic difference | [NEEDS_FIX] When chunks.len() > 8, excess chunks are silently dropped and never hashed; the function returns a wrong hash with no error signal. All current internal callers pass at most 2 chunks so no existing call is affected, but the silent truncation is a latent correctness hazard for any future caller. | [LEAN] Two separate loops to fill pointer and length arrays are driven entirely by the C shim's ABI (parallel ptr/len arrays). The variadic-chunk design matches Noise protocol usage patterns (1-2 chunks in practice). Verbosity is FFI-imposed, not self-inflicted. | [NONE] No test file exists for this module. hash delegates to the C shim and the 8-chunk cap logic is untested. Known-answer tests for BLAKE2s vectors would be required for meaningful coverage. | [PARTIAL] Has one `///` line: 'BLAKE2s-256 hash of concatenated chunks (max 8).' The 8-chunk limit is mentioned but the reason (C shim constraint) and the truncation behavior are not explained. Private function, so lower bar applies. (deliberated: confirmed — Correction NEEDS_FIX confirmed: silent truncation at 8 chunks is a latent correctness hazard. All current callers pass ≤2 chunks so no active bug, but a debug_assert or explicit error would prevent future misuse. Tests NONE confirmed. Documentation PARTIAL confirmed: mentions 8-chunk limit but not truncation behavior.) |
| `seal` | L124–L138 | NEEDS_FIX | 85% | [USED] Not exported; called in encrypt_and_hash() at L198 for AEAD encryption. | [UNIQUE] Different semantic contract than aead.rs seal (0.880 score): noise.rs writes to pre-allocated buffer dst and returns Option<usize>, aead.rs returns Vec<u8> with allocation | [NEEDS_FIX] dst.as_mut_ptr() is passed to the C function with no prior check that dst.len() >= plaintext.len() + AEAD_TAG_SIZE. If dst is undersized, the C encryption function writes beyond the slice boundary, causing memory corruption. All current callers happen to size dst correctly, but the function itself provides no safety guarantee. | [LEAN] Thin, idiomatic Option-returning wrapper over the AEAD encrypt FFI. Appropriate level of abstraction. | [NONE] No test file exists for this module. ChaCha20-Poly1305 AEAD encryption with no coverage — happy path, error return (ret != 0), and output length are all untested. | [PARTIAL] Has one `///` line describing algorithm and return semantics ('Returns ciphertext + 16-byte tag, or None on failure.'). Parameters `counter`, `ad`, and `dst` are not individually described. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: no bounds check that dst.len() >= plaintext.len() + AEAD_TAG_SIZE before passing dst.as_mut_ptr() to C FFI. If dst is undersized, C function writes out-of-bounds — a memory safety violation in a kernel module. All current callers size correctly but the function itself provides no guarantee. Tests NONE confirmed. Documentation PARTIAL confirmed.) |
| `open` | L141–L155 | NEEDS_FIX | 88% | [USED] Not exported; called in decrypt_and_hash() at L204 for AEAD decryption. | [UNIQUE] Different semantic contract than aead.rs open (0.894 score): noise.rs writes to pre-allocated buffer and returns Option<usize>, aead.rs returns Option<Vec<u8>> with allocation | [NEEDS_FIX] Line 151: `Some(ciphertext.len() - AEAD_TAG_SIZE)` performs usize subtraction with no prior guard that ciphertext.len() >= AEAD_TAG_SIZE. If a short ciphertext is passed, this overflows: panics in debug builds (kernel oops in this context) or silently wraps to a huge usize in release builds, causing callers to treat the result as a valid plaintext length. | [LEAN] Symmetric counterpart to seal; same minimal FFI-wrapping pattern. LEAN. | [NONE] No test file exists for this module. AEAD decryption with authentication failure path is entirely untested; authentication-failure (None return) is a security-critical branch. | [PARTIAL] Has one `///` line: 'AEAD decrypt. Returns plaintext length or None on auth failure.' Return semantics are documented; parameters are not. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: L151 `ciphertext.len() - AEAD_TAG_SIZE` performs unchecked usize subtraction. If ciphertext.len() < AEAD_TAG_SIZE, this panics in debug (kernel oops) or wraps in release. A guard `if ciphertext.len() < AEAD_TAG_SIZE { return None; }` is needed. Tests NONE confirmed. Documentation PARTIAL confirmed. Raising confidence as the bug is clearly visible in source.) |

### `rustguard-core/src/handshake.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `process_response` | L173–L221 | NEEDS_FIX | 80% | [USED] Exported function with 4 runtime importers; processes response msg2 and derives transport keys | [UNIQUE] Moderate similarity (0.839) to rustguard-kmod process_response in protocol logic. However, semantic contracts differ: uses high-level crypto types and returns TransportSession, vs kmod's raw arrays and different return structure. Different environments and type abstractions constitute distinct contracts. | [NEEDS_FIX] The response message's mac1 field is never verified. process_initiation_with performs MAC1 verification first (before any DH) as a cheap DDoS guard; process_response skips this check entirely. An attacker can flood the initiator with crafted response packets that each trigger two DH scalar multiplications before being rejected by AEAD. The asymmetry is also a protocol non-compliance with the WireGuard spec which mandates MAC1 checking on all received messages. | [LEAN] Straightforward protocol implementation. receiver_index check, two DH operations, PSK mix, decrypt-and-hash, and key derivation — all mandated by the spec. No superfluous structure. | [WEAK] Three meaningful tests cover this function: happy path (full_handshake_and_transport), ciphertext tampering (tampered_response_rejected), and wrong receiver_index (mismatched_receiver_index_rejected). However, PSK is always zero, the PSK mixing branch (hkdf with psk) is never tested with a real PSK, and the resulting transport key directionality (initiator sends on first, receives on second) is tested in only one direction per session. | [PARTIAL] Public function with a single /// line ('Process a handshake response and derive transport keys'). Failure conditions returning None (receiver_index mismatch, AEAD decryption failure) are not documented; no parameter or return value descriptions; no # Examples. (deliberated: confirmed — Correction NEEDS_FIX confirmed: the WireGuard spec mandates MAC1 verification on all received messages. process_initiation_with correctly checks MAC1 before DH (L258-261), but process_response skips it entirely. While receiver_index provides a cheap first guard, an attacker who observes or guesses the sender_index can force two DH scalar multiplications per forged packet. The asymmetry is a protocol non-compliance. Tests WEAK: PSK always zero, no anti-replay testing. Documentation PARTIAL: failure conditions undocumented. All three findings stand.) |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `elapsed_since` | L38–L40 | NEEDS_FIX | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.) |
| `elapsed_since` | L51–L55 | NEEDS_FIX | 70% | [USED] no_std stub version called in 2 places: maybe_rotate_secret and is_fresh | [DUPLICATE] no_std stub returning Duration::ZERO. Identical logic and purpose as timers.rs variant. | [NEEDS_FIX] Always returns Duration::ZERO in no_std mode. This makes is_fresh() permanently return true for any stored cookie (ZERO < 120s is always true), meaning cookies in no_std never expire and stale cookies are silently accepted forever. It also prevents maybe_rotate_secret() from ever firing (ZERO >= 120s is always false). CookieState is not gated on #[cfg(feature = "std")], so any no_std caller of compute_mac2() will silently treat expired cookies as fresh. | [LEAN] Well-documented stub for no_std; explicitly states the kernel module provides real timekeeping. The Duration::ZERO return is the correct safe default for a compilation stub. | [NONE] no_std stub that always returns Duration::ZERO, gated behind #[cfg(not(feature = "std"))]. No test exercises this path; all inline tests run with std enabled. | [UNDOCUMENTED] no_std-gated private function. No `///` doc comment; the critical behaviour (always returns Duration::ZERO) is explained only via inline `//` comments, which are invisible to `rustdoc`. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L51-55: always returning Duration::ZERO means is_fresh() always returns true and maybe_rotate_secret() never fires. While the comment documents this as intentional (kernel provides real timekeeping), the lack of a cfg gate on CookieState means no_std callers silently get broken expiry semantics. Duplication DUPLICATE kept: identical no_std stub exists in timers.rs within the same crate — extractable to a shared time module. Tests WEAK kept: never compiled under test cfg.) |
| `fill_random` | L270–L272 | NEEDS_FIX | 72% | [USED] no_std stub version called in 2 places: random_bytes and random_nonce | [UNIQUE] no_std stub returning immediately (no-op for kernel fallback). Different from std getrandom version despite RAG score. | [NEEDS_FIX] The no_std stub body is `let _ = buf` — it does not fill the buffer, leaving it as all zeros. create_reply_from_bytes() is not gated on #[cfg(feature = "std")], so it is callable in no_std mode. It calls random_nonce() -> fill_random() and gets a constant 24-byte zero nonce. The encryption key crypto::hash(&[LABEL_COOKIE, our_public]) is also fixed per peer. A fixed key combined with a constant zero nonce means the identical XChaCha20-Poly1305 keystream encrypts every Cookie Reply sent to the same peer, allowing an attacker who observes two ciphertexts to XOR them and recover plaintext cookie bytes. | [LEAN] Clearly commented no_std stub. The comment explains the kernel override strategy, making the design intent unambiguous. | [NONE] no_std stub that does nothing (leaves buffer unchanged). Gated behind #[cfg(not(feature = "std"))]. All inline tests use the std path, so this stub is never compiled or executed in the test suite. | [UNDOCUMENTED] no_std-gated counterpart of fill_random. No `///` doc comment; the important detail that this is a stub that produces zeroed output is conveyed only through inline `//` comments invisible to rustdoc. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L275-280: the stub leaves the buffer zeroed. Since create_reply_from_bytes() is not cfg-gated, calling it in no_std produces a constant zero nonce with a fixed encryption key, enabling keystream reuse attacks. While comments document the stub as compilation-only with kernel override, the lack of compile-time enforcement (cfg gate) makes this a legitimate safety concern. Tests WEAK kept: never compiled under test configuration.) |
| `fill_random` | L275–L280 | NEEDS_FIX | 72% | [USED] no_std stub version called in 2 places: random_bytes and random_nonce | [UNIQUE] no_std stub returning immediately (no-op for kernel fallback). Different from std getrandom version despite RAG score. | [NEEDS_FIX] The no_std stub body is `let _ = buf` — it does not fill the buffer, leaving it as all zeros. create_reply_from_bytes() is not gated on #[cfg(feature = "std")], so it is callable in no_std mode. It calls random_nonce() -> fill_random() and gets a constant 24-byte zero nonce. The encryption key crypto::hash(&[LABEL_COOKIE, our_public]) is also fixed per peer. A fixed key combined with a constant zero nonce means the identical XChaCha20-Poly1305 keystream encrypts every Cookie Reply sent to the same peer, allowing an attacker who observes two ciphertexts to XOR them and recover plaintext cookie bytes. | [LEAN] Clearly commented no_std stub. The comment explains the kernel override strategy, making the design intent unambiguous. | [NONE] no_std stub that does nothing (leaves buffer unchanged). Gated behind #[cfg(not(feature = "std"))]. All inline tests use the std path, so this stub is never compiled or executed in the test suite. | [UNDOCUMENTED] no_std-gated counterpart of fill_random. No `///` doc comment; the important detail that this is a stub that produces zeroed output is conveyed only through inline `//` comments invisible to rustdoc. (deliberated: confirmed — Correction NEEDS_FIX kept for the no_std version at L275-280: the stub leaves the buffer zeroed. Since create_reply_from_bytes() is not cfg-gated, calling it in no_std produces a constant zero nonce with a fixed encryption key, enabling keystream reuse attacks. While comments document the stub as compilation-only with kernel override, the lack of compile-time enforcement (cfg gate) makes this a legitimate safety concern. Tests WEAK kept: never compiled under test configuration.) |

### `rustguard-daemon/src/tunnel.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `run` | L48–L489 | NEEDS_FIX | 90% | [DEAD] exported pub fn but pre-computed analysis shows 0 runtime importers | [UNIQUE] Tunnel daemon main loop. Similar run() in server.rs (score 0.760) has different config type (ServeConfig vs Config) and purpose (enrollment server vs tunnel daemon)—different semantic contracts. | [NEEDS_FIX] Two bugs: (1) MSG_RESPONSE peer lookup falls back to 'p.endpoint.is_some() && !p.has_active_session()', which matches the first peer without an active session rather than the peer that originated this handshake. In any multi-peer setup the new session is assigned to the wrong peer. The pending_handshakes tuple must also store the peer index or public key to allow an exact correlation. (2) UDP recv_from timeout only continues on WouldBlock; on Windows a read timeout fires as TimedOut, causing spurious 'UDP recv error' log output on every 500 ms poll cycle instead of silently continuing. | [ACCEPTABLE] Large monolithic function (~300 lines) that inline-defines three large thread closures. The orchestration complexity is inherent to a WireGuard daemon, but extracting the thread bodies into named functions would improve readability without adding abstraction. Justified as a single entry point for the daemon lifecycle. | [NONE] No test file found for rustguard-daemon/src/tunnel.rs. The run function is the core tunnel orchestrator covering TUN/UDP threading, handshake initiation, route management, and shutdown — all completely untested. | [PARTIAL] Public API function with only `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` — no `# Examples` section, no `# Errors` section, and the `config` parameter is undescribed. For the sole public entry point of the daemon, this is materially incomplete. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD is a false positive: `run` is the sole public entry point of the rustguard-daemon tunnel module, called from main.rs. Binary-internal module calls are not cross-crate imports, which explains the 0-importer result. Reclassified to USED (confidence 92). Correction NEEDS_FIX retained: (1) MSG_RESPONSE peer lookup at ~L288 uses a broad `p.endpoint.is_some() && !p.has_active_session()` fallback that matches the wrong peer in multi-peer configs — real bug. (2) Windows TimedOut vs WouldBlock is a genuine cross-platform issue. Tests NONE is correct — no test file exists for this 440-line orchestrator. Documentation reclassified UNDOCUMENTED → PARTIAL: L48 clearly shows `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` which is a real doc comment, just incomplete (no #Errors, no parameter docs).) |
| `ctrlc_handler` | L504–L525 | NEEDS_FIX | 80% | [USED] called at L118 to setup shutdown signal handler | [UNIQUE] Platform signal handler setup for SIGINT/SIGTERM via libc. No similar functions found. | [NEEDS_FIX] Signal race condition: sigprocmask(SIG_BLOCK) is called inside the newly spawned thread, not in the main thread before any threads are spawned. Between the signal() calls that install signal_noop and the moment the spawned thread's sigprocmask takes effect, SIGINT or SIGTERM can arrive and be delivered to any thread with an unblocked mask (main, outbound, inbound, or timer), where signal_noop consumes it silently. sigwait in the handler thread therefore never fires and the daemon cannot be cleanly shut down. The correct POSIX pattern is to block the signals in the main thread before spawning any threads so all children inherit the blocked mask, then call sigwait exclusively in the dedicated handler thread. | [OVER] NIH reimplementation of what the `ctrlc` crate (~5M weekly downloads) provides in two lines. Uses raw unsafe libc (signal, sigemptyset, sigaddset, sigprocmask, sigwait), wraps the callback in Mutex<Option<F>> to work around FnOnce restrictions, and spawns a dedicated thread — all complexity that ctrlc encapsulates. The companion signal_noop extern is also needed only because of this approach. | [NONE] No test file found. ctrlc_handler installs SIGINT/SIGTERM handlers via unsafe libc calls and spawns a blocking thread; signal delivery, callback invocation, and once-only semantics are all untested. | [PARTIAL] Private function with `/// Install a Ctrl-C / SIGTERM handler.` The closure parameter `f` is undescribed, and the unusual sigwait-based implementation strategy is unexplained. PARTIAL for a non-trivial private function. (deliberated: confirmed — Correction NEEDS_FIX retained: the signal race condition is valid — sigprocmask(SIG_BLOCK) runs only inside the spawned thread at L519, so between the signal() calls at L508-509 and the thread's sigprocmask, SIGINT/SIGTERM can be consumed by signal_noop in any thread (main, outbound, inbound, timer), preventing sigwait from ever firing. The correct POSIX pattern blocks signals in the main thread before spawning. Overengineering OVER retained: ~20 lines of unsafe libc with no SAFETY comments reimplements what the well-maintained ctrlc crate provides in 2-3 lines; for a security-sensitive daemon, minimizing unsafe surface area is arguably more important than minimizing dependencies. Tests NONE correct for complex signal handling with no coverage. Documentation PARTIAL correct — has one-line doc but parameter `f` and the sigwait strategy are unexplained.) |

### `rustguard-core/src/timers.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `elapsed_since` | L21–L23 | NEEDS_FIX | 75% | [USED] Non-exported no_std fallback function compiled conditionally; provides timer abstraction for kernel environments | [DUPLICATE] Identical no_std fallback implementation with RAG score 1.000. Both return Duration::ZERO with identical signature and behavior. | [NEEDS_FIX] Always returns Duration::ZERO in no_std builds, making every time-based check in the impl permanently non-functional: needs_rekey never triggers on age, is_expired never expires a live session by time (security regression — sessions never time out), is_dead always returns false, should_retry_handshake returns false once any handshake is recorded (never retries), and handshake_timed_out never fires. The no_std API correctly exposes timestamp-accepting setters (session_started_at, packet_sent_at, etc.) but the corresponding checker methods have no equivalent time-aware variant and all call this stub, so the caller has no way to drive time-based expiry. | [LEAN] Intentional no-op stub for no_std targets (kernel module). Comment clearly states callers must use the explicit `_at` methods. The zero-return is a deliberate safe fallback, not dead code. | [NONE] no_std variant that always returns Duration::ZERO. No cfg(not(feature='std')) test module exists anywhere in the provided context, and the inline test module does not gate on this cfg. Zero test coverage. | [UNDOCUMENTED] Private no_std-variant has informative `//` inline comments explaining the zero-return fallback, but these are not `///` doc comments and will not appear in rustdoc output. The behavior is important enough to warrant a doc comment given the surprising zero-return semantics. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — This is the no_std variant at L26-30 returning Duration::ZERO. Correction NEEDS_FIX: The checker methods (needs_rekey, is_expired, is_dead, should_retry_handshake, handshake_timed_out) all call elapsed_since but have no _at variants, so time-based expiry is inoperative in no_std. The comment says 'kernel module overrides timer logic' but we cannot verify this and the public API gap is real. Overengineering says LEAN ('intentional safe fallback') which doesn't contradict — intentional design can still have gaps. Keeping NEEDS_FIX but lowering confidence since the kernel module may legitimately override. Duplication reclassified DUPLICATE→UNIQUE: a one-line function returning Duration::ZERO behind a cfg gate is a standard Rust pattern for module-private helpers; extracting a shared pub(crate) utility for this trivial stub adds import complexity exceeding the benefit. Tests field says WEAK but detail explicitly says 'Zero test coverage' for no_std — the mismatch is a merger artifact; keeping WEAK to avoid adding severity.) |
| `elapsed_since` | L26–L30 | NEEDS_FIX | 75% | [USED] Non-exported no_std fallback function compiled conditionally; provides timer abstraction for kernel environments | [DUPLICATE] Identical no_std fallback implementation with RAG score 1.000. Both return Duration::ZERO with identical signature and behavior. | [NEEDS_FIX] Always returns Duration::ZERO in no_std builds, making every time-based check in the impl permanently non-functional: needs_rekey never triggers on age, is_expired never expires a live session by time (security regression — sessions never time out), is_dead always returns false, should_retry_handshake returns false once any handshake is recorded (never retries), and handshake_timed_out never fires. The no_std API correctly exposes timestamp-accepting setters (session_started_at, packet_sent_at, etc.) but the corresponding checker methods have no equivalent time-aware variant and all call this stub, so the caller has no way to drive time-based expiry. | [LEAN] Intentional no-op stub for no_std targets (kernel module). Comment clearly states callers must use the explicit `_at` methods. The zero-return is a deliberate safe fallback, not dead code. | [NONE] no_std variant that always returns Duration::ZERO. No cfg(not(feature='std')) test module exists anywhere in the provided context, and the inline test module does not gate on this cfg. Zero test coverage. | [UNDOCUMENTED] Private no_std-variant has informative `//` inline comments explaining the zero-return fallback, but these are not `///` doc comments and will not appear in rustdoc output. The behavior is important enough to warrant a doc comment given the surprising zero-return semantics. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — This is the no_std variant at L26-30 returning Duration::ZERO. Correction NEEDS_FIX: The checker methods (needs_rekey, is_expired, is_dead, should_retry_handshake, handshake_timed_out) all call elapsed_since but have no _at variants, so time-based expiry is inoperative in no_std. The comment says 'kernel module overrides timer logic' but we cannot verify this and the public API gap is real. Overengineering says LEAN ('intentional safe fallback') which doesn't contradict — intentional design can still have gaps. Keeping NEEDS_FIX but lowering confidence since the kernel module may legitimately override. Duplication reclassified DUPLICATE→UNIQUE: a one-line function returning Duration::ZERO behind a cfg gate is a standard Rust pattern for module-private helpers; extracting a shared pub(crate) utility for this trivial stub adds import complexity exceeding the benefit. Tests field says WEAK but detail explicitly says 'Zero test coverage' for no_std — the mismatch is a merger artifact; keeping WEAK to avoid adding severity.) |
| `REKEY_TIMEOUT` | L39–L39 | NEEDS_FIX | 88% | [USED] Exported constant with 1 runtime importer (rustguard-daemon/src/peer.rs); handshake retry timeout | [UNIQUE] Module-level constant defining 5-second rekey timeout. Unique by definition. | [NEEDS_FIX] The doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but REKEY_TIMEOUT is actually the inter-attempt retry delay (5 s between handshake retries), as confirmed by its use in should_retry_handshake. The description matches no field or usage in this file and directly contradicts the constant's semantics. For a security-critical protocol constant, this misdescription constitutes a correctness hazard for callers relying on the doc. The numeric value (5 s) is correct per the WireGuard spec. | [LEAN] Direct WireGuard whitepaper constant. Nothing to simplify further. | [WEAK] Used in should_retry_handshake(). retry_handshake_when_never_sent only tests the None branch (no handshake ever sent). The time-elapsed branch that compares against REKEY_TIMEOUT is completely untested. Value (5 s) never verified. | [PARTIAL] A `///` doc comment is present, but the description 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' does not match the 5-second value — REJECT_AFTER_TIME is 180 s. Actual usage in `should_retry_handshake` shows this is a handshake-retry interval, not a send-rejection threshold. The description appears to be a copy-paste error, making it actively misleading. (deliberated: confirmed — Correction NEEDS_FIX confirmed with increased confidence: the doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but the constant is 5 seconds, used in should_retry_handshake as a retry interval. REJECT_AFTER_TIME is 180s — the description is clearly wrong and actively misleading for a security-critical protocol constant. The numeric value (5s) is correct per WireGuard spec. Documentation PARTIAL confirmed: a doc comment exists but its content is factually incorrect, making it worse than undocumented. Both findings are coherent — the PARTIAL documentation and NEEDS_FIX correction describe the same underlying issue (wrong doc comment).) |
| `REKEY_AFTER_MESSAGES` | L42–L42 | NEEDS_FIX | 72% | [USED] Exported constant with 1 runtime importer; also used locally in needs_rekey() and tests | [UNIQUE] Module-level constant for message count rekey threshold. Unique constant definition. | [NEEDS_FIX] Value is (2^60 - 1) rather than 2^60 as specified in the WireGuard whitepaper. The off-by-one means rekeying is triggered one message earlier than the protocol mandates. This is conservative rather than dangerous, but it is an undocumented deviation from the spec and could cause interoperability or compliance issues. | [LEAN] Bit-shift expression `(1u64 << 60) - 1` directly encodes the WireGuard spec value in a self-documenting way. The expression is clearer than a raw decimal literal. | [WEAK] Directly used as the threshold argument in message_count_triggers_rekey, confirming at-boundary behavior. However, the just-below-threshold case (REKEY_AFTER_MESSAGES - 1) is not tested, the rekey_requested=true short-circuit path is not tested, and the actual bit-pattern value ((1u64 << 60) - 1) is never independently asserted. | [DOCUMENTED] Has a clear `///` doc comment — 'Maximum number of messages before rekeying.' — that accurately captures the purpose of the threshold constant. (deliberated: confirmed — Correction NEEDS_FIX kept: (1u64 << 60) - 1 vs spec's 2^60 is an undocumented conservative deviation. The off-by-one is harmless (rekeys one message early) but the Go reference implementation uses exactly (1 << 60). Action 5 (fix or document) is proportionate. Tests WEAK confirmed: boundary value tested at REKEY_AFTER_MESSAGES but below-threshold case (REKEY_AFTER_MESSAGES - 1) untested, rekey_requested=true short-circuit untested.) |
| `SessionTimers` | L54–L65 | NEEDS_FIX | 90% | [USED] Exported struct with 1 runtime importer (rustguard-daemon/src/peer.rs); core session lifecycle timer management | [UNIQUE] Public struct defining timer state for peer session lifecycle. No semantic duplicates identified in RAG results. | [NEEDS_FIX] Two bugs in the impl block: (1) needs_keepalive (L160): when last_sent is None, sent.unwrap_or(received) sets last_send_time = received, making the compound condition 'elapsed_since(received) >= interval AND elapsed_since(received) < interval' a logical contradiction that always evaluates to false. Keepalives can never fire on a session where the local side has not yet sent any data packet, silently breaking persistent-keepalive for passive peers. (2) session_started / session_started_at (L80, L89) do not reset last_sent to None; stale send timestamps from the prior session persist into the new session, further suppressing keepalive generation immediately after rekeying. | [ACCEPTABLE] The struct and its logic methods (needs_rekey, is_expired, is_dead, needs_keepalive, should_retry_handshake, handshake_timed_out) are minimal and map 1-to-1 to WireGuard spec concepts. The only added complexity is the dual std/no_std mutation API (session_started/session_started_at, packet_sent/packet_sent_at, packet_received/packet_received_at), which doubles mutation entry points. This is justified: `rustguard-kmod` is a real kernel module target confirmed by the project structure, making no_std support a hard requirement rather than premature generalization. | [WEAK] Several methods have reasonable entry-level tests (new, session_started, needs_rekey message-count path, is_expired message-count/no-session paths, basic keepalive/retry guards). However: handshake_timed_out() has no test at all; needs_keepalive() positive case (interval elapsed, data received recently) is untested; packet_sent() is never exercised in any test; all time-based expiry branches in needs_rekey/is_expired/is_dead/should_retry_handshake are untested; all no_std variants (session_started_at, packet_sent_at, packet_received_at) have no tests. | [DOCUMENTED] The struct carries a `///` doc comment on the type and every public field has its own `///` doc comment. This satisfies the struct-documentation rule. Note that the associated `new()` constructor in the impl block is public but lacks any doc comment, which is a gap not captured in the struct definition range itself. (deliberated: confirmed — Correction NEEDS_FIX confirmed with high confidence. Bug (1) verified: in needs_keepalive when last_sent is None, sent.unwrap_or(received) sets last_send_time=received, creating the contradictory condition 'elapsed_since(received) >= interval AND elapsed_since(received) < interval' which is always false. Passive peers that receive data but never send can never trigger keepalives — a real functional bug. Bug (2) verified: session_started/session_started_at don't reset last_sent, so stale timestamps carry over across rekeys. Both bugs are coherent with each other and compound the keepalive suppression. Overengineering ACCEPTABLE confirmed: dual std/no_std API is justified by the real kernel module target (rustguard-kmod). Tests WEAK confirmed: handshake_timed_out has no test, keepalive positive case untested, all time-based branches untested, no_std variants untested, packet_sent never exercised.) |

### `rustguard-enroll/src/control.rs`

| Symbol | Lines | Correction | Conf. | Detail |
|--------|-------|------------|-------|--------|
| `start_listener` | L77–L104 | NEEDS_FIX | 78% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Substantial function that sets up socket listener and spawns background thread. | [UNIQUE] Initializes Unix socket listener with permissions and spawns handler thread. No similar functions found. | [NEEDS_FIX] The single background thread handles connections sequentially via `handle_client`, which loops over `reader.lines()` with no server-side read timeout set on the accepted stream. A client that connects but never sends data (or never closes the connection) will block the thread inside `handle_client` indefinitely, making the control socket unresponsive to all subsequent `OPEN`/`STATUS` commands for the lifetime of the server. `send_command` sets a client-side read timeout but there is no corresponding server-side write or read timeout, so a stalled or malicious client can permanently wedge the listener. | [LEAN] Appropriately complex for spawning a UNIX socket listener: stale-socket cleanup, bind, chmod, and a single background thread. Each step is necessary. No layers of abstraction around what is inherently a multi-step OS setup. | [NONE] No test file exists for control.rs. start_listener has no tests; socket binding, stale-socket removal, permission setting, and background thread spawning are all untested. | [PARTIAL] Has two `///` lines describing purpose and return value. Missing descriptions for both `window` and `peer_count` parameters, no documentation of error conditions (`io::Error` cases), no mention of the background thread lifecycle or stale-socket cleanup, and no `# Examples` section. (deliberated: confirmed — Correction NEEDS_FIX is valid: the single background thread processes connections sequentially via `handle_client`, which blocks on `reader.lines()` with no server-side read timeout. A stalled or malicious client on the Unix socket permanently wedges the listener. The fix (set_read_timeout on the accepted stream or spawn per-connection) is small-effort. However, this is a local Unix domain socket for a system daemon, not internet-facing, which somewhat limits the practical attack surface — keeping confidence moderate at 78 rather than high. Tests NONE and documentation PARTIAL both confirmed.) |

## Quick Wins

- [ ] <!-- ACT-a4f709-1 --> **[correction · high · small]** `rustguard-core/src/cookie.rs`: Gate create_reply_from_bytes() with #[cfg(feature = "std")] or add a caller-supplied nonce parameter for no_std callers, so the no_std stub fill_random cannot silently produce a constant zero nonce that reuses the same XChaCha20-Poly1305 keystream for every Cookie Reply. [L275]
- [ ] <!-- ACT-fb2f2d-1 --> **[correction · high · small]** `rustguard-core/src/timers.rs`: Add checker methods that accept a current-time parameter in no_std builds (e.g., needs_rekey_at(send_counter, now_ns), is_expired_at, is_dead_at, should_retry_handshake_at) so time-based session expiry is actually usable without std. The current design leaves all time checks silently dead in no_std, including the critical is_expired path. [L26]
- [ ] <!-- ACT-a732f2-1 --> **[correction · high · small]** `rustguard-enroll/src/server.rs`: MSG_TRANSPORT handler: add a bounds check `if ct.len() > decrypt_buf.len() { continue; }` before `decrypt_buf[..ct.len()].copy_from_slice(ct)`. Without this check any UDP datagram (or XDP frame up to 4096 bytes) with payload > 2048 bytes panics the inbound thread, taking down the server. [L447]
- [ ] <!-- ACT-bb41a6-1 --> **[correction · high · small]** `rustguard-kmod/src/lib.rs`: In do_xmit, determine the ownership contract of wg_skb_prepend_header. If it operates in-place (returns same pointer), remove wg_kfree_skb(skb) after the call — the single skb is later freed as tx_skb. If it always allocates a new skb, document that the old skb is never freed internally and the explicit free is intentional. The current pattern produces either a use-after-free or double-free depending on the C implementation. [L415]
- [ ] <!-- ACT-c30836-1 --> **[correction · high · small]** `rustguard-kmod/src/replay.rs`: shift_window bit-carry loop direction is reversed. `for i in (0..BITMAP_LEN).rev()` propagates carry from higher-index words toward lower-index words, which is backwards: left-shifting sends overflow from word[i] bit 63 into word[i+1] bit 0, so carry must travel low→high. Change the loop to `for i in 0..BITMAP_LEN` (forward). The final carry value after the loop is correctly discarded — those are bits that fall off the oldest end of the window. Without this fix, 'seen' bits at the high end of any 64-bit word are silently dropped on every window advance, breaking replay-attack detection for affected counter values. [L91]
- [ ] <!-- ACT-8fc69f-1 --> **[correction · high · small]** `rustguard-tun/src/xdp.rs`: tx_send copies data.len() bytes into a UMEM frame with no bounds check against self.frame_size. If data.len() > frame_size the write overflows the allocated frame and corrupts adjacent UMEM memory visible to the kernel. Add a guard: if data.len() > self.frame_size as usize { self.free_frames.push(frame_addr); return false; } before the copy. [L348]
- [ ] <!-- ACT-a4f709-2 --> **[correction · medium · small]** `rustguard-core/src/cookie.rs`: Document or enforce via a cfg gate that CookieState::is_fresh() and CookieState::compute_mac2() must not be used in no_std mode without real timekeeping, or provide a no_std-compatible elapsed_since that takes a real clock input, since the current stub causes all stored cookies to appear permanently fresh. [L51]
- [ ] <!-- ACT-5920d4-1 --> **[correction · medium · small]** `rustguard-core/src/handshake.rs`: Add MAC1 verification in process_response before performing any DH operations. Compute expected_mac1 = compute_mac1(&our_static.public_key(), &msg.to_bytes()[..60]) and return None if it doesn't match msg.mac1 via constant_time_eq. This mirrors the guard already present in process_initiation_with and prevents a DDoS vector where an attacker triggers two DH scalar multiplications per forged response packet. [L189]
- [ ] <!-- ACT-fb2f2d-2 --> **[correction · medium · small]** `rustguard-core/src/timers.rs`: Fix needs_keepalive: replace `sent.unwrap_or(received)` with an explicit sentinel — when last_sent is None, treat elapsed time since last send as Duration::MAX so that a peer which has received data but never sent anything does trigger keepalives as expected. [L160]
- [ ] <!-- ACT-bd20cf-1 --> **[correction · medium · small]** `rustguard-daemon/src/tunnel.rs`: MSG_RESPONSE peer lookup uses 'p.endpoint.is_some() && !p.has_active_session()' as its effective matching condition, returning the first peer that has no active session rather than the peer that actually sent the handshake initiation. In any multi-peer configuration this silently assigns the new session to the wrong peer. Fix by adding the peer's index or public key as a fourth element of the pending_handshakes tuple so the response can be correlated exactly, eliminating the broad endpoint-based fallback. [L288]
- [ ] <!-- ACT-bd20cf-2 --> **[correction · medium · small]** `rustguard-daemon/src/tunnel.rs`: ctrlc_handler calls sigprocmask(SIG_BLOCK) only inside the spawned handler thread. All other threads (main, outbound, inbound, timer) inherit an unblocked signal mask from the main thread, so SIGINT/SIGTERM delivered to the process can be consumed by signal_noop in any of those threads before sigwait ever observes it, silently preventing clean shutdown. Fix by calling pthread_sigmask/sigprocmask in the main thread before any threads are spawned so the block is inherited by all children. [L519]
- [ ] <!-- ACT-18d76d-1 --> **[correction · medium · small]** `rustguard-enroll/src/control.rs`: Set a read timeout on the accepted UnixStream before passing it to handle_client (e.g., `stream.set_read_timeout(Some(Duration::from_secs(30)))?`) or spawn a new thread per connection. Without this, a client that connects but never writes (or never closes) will permanently block the single listener thread, making the control socket unresponsive to all subsequent commands for the life of the server process. [L99]
- [ ] <!-- ACT-a732f2-2 --> **[correction · medium · small]** `rustguard-enroll/src/server.rs`: MSG_INITIATION handler: store the returned TAI64N timestamp `_ts` in `PeerState` (e.g., `ps.timers.latest_handshake_ts`) and reject initiations whose timestamp is not strictly greater than the stored value. Discarding `_ts` removes WireGuard's mandated replay protection for handshake initiation messages. [L413]
- [ ] <!-- ACT-bb41a6-2 --> **[correction · medium · small]** `rustguard-kmod/src/lib.rs`: In RustGuard::init, add wg_crypto_exit() to the wg_queue_init failure path, and add both wg_queue_destroy() and wg_crypto_exit() to the KBox allocation failure, wg_create_device failure, and wg_socket_create failure paths to prevent subsystem resource leaks on module load failure. [L196]
- [ ] <!-- ACT-bb41a6-3 --> **[correction · medium · small]** `rustguard-kmod/src/lib.rs`: In do_timer_tick, drop the outer 'peer: &mut Peer' borrow before calling initiate_rekey() and send_keepalive(), then re-acquire it afterward if needed. Both callees create a second &mut Peer alias to the same slot, which is undefined behavior under Rust's aliasing rules. [L819]
- [ ] <!-- ACT-bb41a6-4 --> **[correction · medium · small]** `rustguard-kmod/src/lib.rs`: In handle_cookie_reply, store the MAC1 computed for the last outgoing handshake initiation per peer and pass it instead of [0u8;16]. Without the correct MAC1 the cookie reply cannot be authenticated, breaking DoS protection and causing every MAC2 retry to fail. [L777]
- [ ] <!-- ACT-58e5ff-2 --> **[correction · medium · small]** `rustguard-kmod/src/noise.rs`: In open(): add a guard `if ciphertext.len() < AEAD_TAG_SIZE { return None; }` before computing `ciphertext.len() - AEAD_TAG_SIZE`. Without it, an undersized ciphertext causes a usize underflow: panic (kernel oops) in debug builds, or a wrap-around length returned to the caller in release builds. [L151]
- [ ] <!-- ACT-58e5ff-3 --> **[correction · medium · small]** `rustguard-kmod/src/noise.rs`: In seal(): add a bounds check `if dst.len() < plaintext.len() + AEAD_TAG_SIZE { return None; }` before entering the unsafe block. Without it, passing an undersized dst causes the C function to write beyond the slice boundary, corrupting adjacent kernel memory. [L125]
- [ ] <!-- ACT-8fc69f-2 --> **[correction · medium · small]** `rustguard-tun/src/xdp.rs`: Drop only calls munmap on self.umem. The four ring mmap regions (rx, tx, fill, completion) returned from mmap_ring are leaked on every drop because make_ring discards the base pointer and no size is retained. Store the base pointer and size for each of the four rings in XdpSocket and unmap them in Drop. [L400]
- [ ] <!-- ACT-8fc69f-3 --> **[correction · medium · small]** `rustguard-tun/src/xdp.rs`: The four setsockopt calls at lines 217-220 that configure ring sizes silently discard their i32 return values. A failure leaves the kernel-side ring sizes at their defaults while the code proceeds to mmap rings using offsets from getsockopt, resulting in mismatched ring geometry and silent memory corruption. Each call must be checked and the error propagated with fd/umem cleanup. [L217]
- [ ] <!-- ACT-8fc69f-4 --> **[correction · medium · small]** `rustguard-tun/src/xdp.rs`: In create_inner, if the second, third, or fourth mmap_ring call fails (lines 241-243), all previously successful ring mmaps are leaked because no cleanup is performed before the ? operator returns the error. Introduce a cleanup guard or explicit munmap calls for each successfully mapped ring on all error paths. [L241]
- [ ] <!-- ACT-fb2f2d-3 --> **[correction · low · small]** `rustguard-core/src/timers.rs`: Reset self.last_sent to None inside session_started and session_started_at so stale send timestamps from the previous session do not carry over and suppress keepalive generation in the new session. [L80]
- [ ] <!-- ACT-fb2f2d-4 --> **[correction · low · small]** `rustguard-core/src/timers.rs`: Correct the doc comment on REKEY_TIMEOUT (L38): the constant is the delay between handshake retry attempts, not a keypair age limit. The current comment referencing REJECT_AFTER_TIME is factually wrong and misleading. [L38]
- [ ] <!-- ACT-fb2f2d-5 --> **[correction · low · small]** `rustguard-core/src/timers.rs`: Change REKEY_AFTER_MESSAGES to (1u64 << 60) to match the WireGuard spec, or add an explicit comment documenting the intentional one-message-early conservative deviation. [L42]
- [ ] <!-- ACT-bd20cf-3 --> **[correction · low · small]** `rustguard-daemon/src/tunnel.rs`: The UDP recv_from error handler only continues (suppresses) io::ErrorKind::WouldBlock. On Windows, a read timeout set via set_read_timeout fires with io::ErrorKind::TimedOut, causing the else branch to log a spurious 'UDP recv error' on every 500 ms poll cycle. Fix: add TimedOut to the continue arm: 'Err(ref e) if e.kind() == io::ErrorKind::WouldBlock || e.kind() == io::ErrorKind::TimedOut => continue'. [L232]
- [ ] <!-- ACT-bb41a6-5 --> **[correction · low · small]** `rustguard-kmod/src/lib.rs`: In cleanup_state, add zeroization of peer.prev_session.key_send and peer.prev_session.key_recv for each peer, and zeroize ephemeral key material in peer.pending_handshake before the KBox is dropped. [L369]
- [ ] <!-- ACT-bb41a6-6 --> **[correction · low · small]** `rustguard-kmod/src/lib.rs`: In RustGuard::init, check the return value of wg_timer_start(); if it fails, log an error (or return Err) since all rekeying and keepalive processing will be silently disabled, causing sessions to expire irreversibly. [L289]
- [ ] <!-- ACT-bb41a6-7 --> **[correction · low · small]** `rustguard-kmod/src/lib.rs`: In handle_transport, replace the hard-coded 2048-byte stack buffer with a heap allocation sized to the net_device MTU plus AEAD_TAG_SIZE, or raise the static limit and enforce it against the configured MTU. Packets with plaintext larger than 2032 bytes are currently silently dropped. [L708]
- [ ] <!-- ACT-58e5ff-1 --> **[correction · low · small]** `rustguard-kmod/src/noise.rs`: In hash(): when chunks.len() > 8, the function silently drops excess chunks and returns a wrong hash. Add `debug_assert!(chunks.len() <= 8)` or return an explicit error/panic so callers discover misuse immediately rather than receiving a silently incorrect hash value. [L74]
