[← Back to Tests](./index.md) · [← Back to report](../public_report.md)

# Tests — Shard 2

## Findings

| File | Verdict | Tests | Conf. | Details |
|------|---------|-------|-------|---------|
| `rustguard-enroll/src/control.rs` | NEEDS_REFACTOR | 10 | 95% | [details](../reviews/rustguard-enroll-src-control.rev.md) |
| `rustguard-cli/src/main.rs` | NEEDS_REFACTOR | 10 | 90% | [details](../reviews/rustguard-cli-src-main.rev.md) |
| `rustguard-daemon/src/config.rs` | NEEDS_REFACTOR | 7 | 95% | [details](../reviews/rustguard-daemon-src-config.rev.md) |
| `rustguard-enroll/src/protocol.rs` | NEEDS_REFACTOR | 4 | 92% | [details](../reviews/rustguard-enroll-src-protocol.rev.md) |
| `rustguard-tun/src/linux.rs` | NEEDS_REFACTOR | 8 | 90% | [details](../reviews/rustguard-tun-src-linux.rev.md) |
| `rustguard-tun/src/bpf_loader.rs` | NEEDS_REFACTOR | 9 | 88% | [details](../reviews/rustguard-tun-src-bpf_loader.rev.md) |
| `rustguard-crypto/src/aead.rs` | NEEDS_REFACTOR | 5 | 92% | [details](../reviews/rustguard-crypto-src-aead.rev.md) |
| `rustguard-tun/src/macos.rs` | NEEDS_REFACTOR | 7 | 90% | [details](../reviews/rustguard-tun-src-macos.rev.md) |
| `rustguard-kmod/src/cookie.rs` | NEEDS_REFACTOR | 6 | 92% | [details](../reviews/rustguard-kmod-src-cookie.rev.md) |
| `rustguard-tun/src/linux_mq.rs` | NEEDS_REFACTOR | 6 | 90% | [details](../reviews/rustguard-tun-src-linux_mq.rev.md) |

## Symbol Details

### `rustguard-enroll/src/control.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `socket_path` | L18–L20 | NONE | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also cal... |
| `new_window` | L26–L28 | NONE | 95% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public A... |
| `is_open` | L31–L41 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public A... |
| `open_window` | L44–L51 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also cal... |
| `close_window` | L54–L56 | NONE | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also cal... |
| `remaining` | L59–L73 | NONE | 87% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Also cal... |
| `start_listener` | L77–L104 | NONE | 78% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Substant... |
| `handle_client` | L106–L144 | NONE | 60% | [USED] Private function called by start_listener at L102 in spawned thread. H... |
| `send_command` | L147–L166 | NONE | 88% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Public A... |
| `cleanup` | L169–L171 | NONE | 85% | [USED] Exported, runtime-imported by rustguard-enroll/src/server.rs. Utility ... |

### `rustguard-cli/src/main.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `main` | L6–L28 | NONE | 75% | [USED] Entry point function required by Rust. Called automatically by the run... |
| `usage` | L30–L42 | WEAK | 72% | [USED] Called twice in main() at lines 21 and 27. Prints help text for comman... |
| `cmd_up` | L44–L66 | NONE | 75% | [USED] Called in main() at line 10 via match arm for 'up' command. Handles tu... |
| `cmd_serve` | L68–L161 | NONE | 90% | [USED] Called in main() at line 11 via match arm for 'serve' command. Handles... |
| `cmd_join` | L163–L211 | NONE | 90% | [USED] Called in main() at line 12 via match arm for 'join' command. Handles ... |
| `cmd_open` | L213–L226 | WEAK | 72% | [USED] Called in main() at line 13 via match arm for 'open' command. Handles ... |
| `cmd_close` | L228–L236 | WEAK | 72% | [USED] Called in main() at line 14 via match arm for 'close' command. Handles... |
| `cmd_status` | L238–L246 | WEAK | 72% | [USED] Called in main() at line 15 via match arm for 'status' command. Handle... |
| `cmd_genkey` | L248–L252 | WEAK | 72% | [USED] Called in main() at line 16 via match arm for 'genkey' command. Genera... |
| `cmd_pubkey` | L254–L267 | WEAK | 72% | [USED] Called in main() at line 17 via match arm for 'pubkey' command. Derive... |

### `rustguard-daemon/src/config.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `Config` | L13–L16 | WEAK | 72% | [USED] Main exported struct holding parsed WireGuard configuration. Runtime-i... |
| `CidrAddr` | L38–L41 | WEAK | 85% | [USED] Exported struct with three utility methods (contains_v4, contains_v6, ... |
| `decode_key` | L167–L174 | WEAK | 70% | [USED] Non-exported function called locally in parse_interface (line 211) and... |
| `parse_interface` | L176–L226 | WEAK | 88% | [USED] Non-exported function called in Config::parse (line 163). Parses [Inte... |
| `parse_peer` | L228–L269 | WEAK | 75% | [USED] Non-exported function called in Config::parse (lines 139, 153). Parses... |
| `parse_cidr` | L271–L282 | WEAK | 75% | [USED] Non-exported function called in parse_peer (line 259). Parses CIDR not... |
| `prefix_to_netmask` | L284–L292 | WEAK | 88% | [USED] Exported function converting prefix length to IPv4 netmask. Runtime-im... |

### `rustguard-enroll/src/protocol.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `parse_request` | L40–L51 | WEAK | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). ... |
| `build_response` | L61–L75 | WEAK | 85% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). ... |
| `parse_response` | L78–L102 | WEAK | 86% | [USED] Exported function imported by 2 runtime files (client.rs, server.rs). ... |
| `random_nonce` | L104–L108 | WEAK | 70% | [USED] Internal helper function called by build_request (L29) and build_respo... |

### `rustguard-tun/src/linux.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `last_os_error` | L51–L53 | WEAK | 85% | [USED] Helper function used for OS error conversion in create(), configure_in... |
| `close_and_error` | L55–L59 | WEAK | 85% | [USED] Helper function to close fd and return error; used throughout create()... |
| `make_sockaddr_in` | L61–L70 | WEAK | 85% | [USED] Helper function called 3 times in configure_interface() to construct s... |
| `set_name` | L72–L76 | WEAK | 85% | [USED] Helper function used in create() and configure_interface() to copy int... |
| `create` | L78–L122 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for ... |
| `configure_interface` | L124–L189 | NONE | 85% | [USED] Internal helper function called once from create() at line 113 for int... |
| `read` | L193–L199 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for ... |
| `write` | L203–L212 | NONE | 90% | [DEAD] Exported pub fn with 0 runtime importers per analysis; public API for ... |

### `rustguard-tun/src/bpf_loader.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `XdpProgram` | L37–L41 | NONE | 88% | [DEAD] Exported public struct with 0 runtime importers and 0 type-only import... |
| `bpf_create_xskmap` | L93–L122 | NONE | 60% | [USED] Function called from XdpProgram::load_and_attach() on line 55. | [UNIQ... |
| `bpf_prog_load` | L124–L175 | NONE | 60% | [USED] Function called from XdpProgram::load_and_attach() on line 61. | [UNIQ... |
| `bpf_map_update` | L177–L206 | NONE | 60% | [USED] Function called from XdpProgram::register_xsk() on line 82. | [UNIQUE]... |
| `attach_xdp` | L210–L233 | NONE | 75% | [USED] Function called from XdpProgram::load_and_attach() on line 68. | [UNIQ... |
| `attach_xdp_netlink` | L235–L342 | NONE | 80% | [USED] Called from attach_xdp() on line 231 and from detach_xdp() on line 346... |
| `detach_xdp` | L344–L346 | NONE | 85% | [USED] Function called from Drop implementation on line 89. | [UNIQUE] Trivia... |
| `ifindex_to_name` | L348–L357 | NONE | 85% | [USED] Function called from attach_xdp() on line 224. | [UNIQUE] Converts int... |
| `parse_and_patch_elf` | L361–L470 | NONE | 75% | [USED] Function called from XdpProgram::load_and_attach() on line 57. | [UNIQ... |

### `rustguard-crypto/src/aead.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `xseal` | L48–L54 | NONE | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.... |
| `xopen` | L57–L63 | NONE | 85% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.... |
| `seal_to` | L68–L84 | NONE | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.... |
| `open_to` | L89–L102 | NONE | 88% | [USED] Exported function with 1 runtime importer in rustguard-crypto/src/lib.... |
| `build_nonce` | L105–L109 | WEAK | 78% | [USED] Non-exported internal function called 4 times within file: in seal, op... |

### `rustguard-tun/src/macos.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `close_and_error` | L79–L83 | WEAK | 75% | [USED] Called 4 times to safely preserve OS error before closing file descrip... |
| `make_sockaddr_in` | L85–L93 | WEAK | 65% | [USED] Called 3 times in configure_address() to construct SockaddrIn for addr... |
| `create` | L95–L192 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. May be pa... |
| `configure_address` | L194–L218 | NONE | 88% | [USED] Called from create() to configure interface IP address via ioctl (L180... |
| `set_mtu` | L220–L242 | NONE | 88% | [USED] Called from create() to set interface MTU via ioctl (L186) | [UNIQUE] ... |
| `read` | L245–L263 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-... |
| `write` | L266–L296 | NONE | 90% | [DEAD] Exported pub fn with 0 workspace imports and no local usage. Platform-... |

### `rustguard-kmod/src/cookie.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `hash` | L41–L51 | WEAK | 92% | [USED] Internal helper function called in verify_mac1 (line 101), create_repl... |
| `mac` | L53–L57 | WEAK | 75% | [USED] Internal helper for BLAKE2s MAC computation, called in verify_mac1 (10... |
| `random_bytes` | L59–L63 | WEAK | 85% | [USED] Generic random bytes generator called for secret initialization (85), ... |
| `CookieChecker` | L66–L71 | WEAK | 85% | [USED] Primary server-side cookie validation API in library crate; zero works... |
| `CookieState` | L74–L77 | WEAK | 85% | [USED] Primary client-side cookie storage API in library crate; zero workspac... |
| `constant_time_eq` | L202–L205 | WEAK | 80% | [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, ... |

### `rustguard-tun/src/linux_mq.rs`

| Symbol | Lines | Tests | Conf. | Detail |
|--------|-------|-------|-------|--------|
| `set_name` | L53–L57 | NONE | 88% | [USED] Called at L100, L122, L233, L242, L257 to copy interface name into ifr... |
| `make_sockaddr_in` | L59–L68 | NONE | 88% | [USED] Called at L232, L239, L244 to construct sockaddr_in for address, desti... |
| `close_and_error` | L70–L74 | NONE | 88% | [USED] Called at L106, L128, L237, L242, L247, L261, L266 to close file descr... |
| `MultiQueueTun` | L77–L81 | NONE | 90% | [DEAD] Exported public struct with 0 workspace importers per analysis, sugges... |
| `open_tun_queue` | L195–L204 | NONE | 88% | [USED] Called at L89 and L118 within create() to open file descriptors to /de... |
| `configure_interface` | L206–L260 | NONE | 85% | [USED] Called at L136 within create() to set IP address, netmask, MTU, and br... |

## Test Improvements

- [ ] `rustguard-enroll/src/control.rs` — 10 untested
  Create `rustguard-enroll/src/control.test.rs` covering: socket_path, new_window, is_open, open_window, close_window, remaining, start_listener, handle_client, send_command, cleanup

- [ ] `rustguard-cli/src/main.rs` — 4 untested, 6 weak
  Improve `rustguard-cli/src/main.test.rs` covering: main, usage, cmd_up, cmd_serve, cmd_join, cmd_open, cmd_close, cmd_status, cmd_genkey, cmd_pubkey
  - usage: [USED] Called twice in main() at lines 21 and 27. Prints help text for command usage. | [UNIQUE] Prints usage information for CLI. No similar functions found. | [OK] Pure output function; no logic to evaluate. | [LEAN] Simple static help text printed to stderr. One function, one responsibility, no abstraction needed. | [NONE] No test file found. The usage/help output function has no tests verifying correct command listing or formatting. | [UNDOCUMENTED] No /// doc comments. Private helper with a clear name; the body itself serves as implicit documentation of supported commands, so leniency applies. (deliberated: confirmed — Tests WEAK is reasonable — no test file exists but this is a pure eprintln output function with minimal logic. Testing it provides low value. The detail text says NONE but WEAK is a fair merged assessment given the function's trivial nature.)
  - cmd_open: [USED] Called in main() at line 13 via match arm for 'open' command. Handles enrollment window open. | [UNIQUE] Sends OPEN command with duration to control endpoint. No similar functions found. | [OK] Silently defaults to 60 on unparseable input; this matches the documented behavior (default 60s). The OPEN command is formatted and sent correctly. | [LEAN] Parses an optional integer argument with a sensible default and sends a single control command. Minimal and clear. | [NONE] No test file found. Default seconds fallback (60), custom seconds parsing, and control command error path are untested. | [UNDOCUMENTED] No /// doc comments. Private helper; the default-60-second fallback and the IPC mechanism used (control socket) are not described. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists but the function is a thin wrapper around control::send_command with a simple default-seconds fallback. Testing value is moderate.)
  - cmd_close: [USED] Called in main() at line 14 via match arm for 'close' command. Handles enrollment window close. | [UNIQUE] Sends CLOSE command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Single call to send_command with error handling. Could not be simpler for its purpose. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. 9-line function that directly delegates to control::send_command. No test file exists but function is trivially simple.)
  - cmd_status: [USED] Called in main() at line 15 via match arm for 'status' command. Handles status query. | [UNIQUE] Sends STATUS command to control endpoint. No similar functions found. | [OK] Simple delegation to control::send_command with correct error handling. | [LEAN] Identical structure to cmd_close; single control command dispatch. Appropriately minimal. | [NONE] No test file found. The control socket send and error path have no test coverage. | [UNDOCUMENTED] No /// doc comments. Private, short function with a clear name; leniency fully applies. (deliberated: confirmed — Tests WEAK is reasonable. Structurally identical to cmd_close — pure delegation to control::send_command. No test file exists.)
  - cmd_genkey: [USED] Called in main() at line 16 via match arm for 'genkey' command. Generates cryptographic key. | [UNIQUE] Generates random private key and encodes as base64. No similar functions found. | [OK] Generates a random StaticSecret, encodes bytes as base64, and prints. No correctness issues. | [LEAN] Generates a random static secret and base64-encodes it in four lines. Exactly as complex as the task demands. | [NONE] No test file found. Key generation and base64 encoding output are untested. | [UNDOCUMENTED] No /// doc comments. Private; the output format (base64-encoded 32-byte Curve25519 scalar) is implied but not stated. (deliberated: confirmed — Tests WEAK is reasonable. 5-line function generating a key and encoding it. No test file exists. The crypto correctness is in the library, not here.)
  - cmd_pubkey: [USED] Called in main() at line 17 via match arm for 'pubkey' command. Derives public key from private key. | [UNIQUE] Reads base64-encoded private key from stdin and derives public key. No similar functions found. | [OK] Reads a line, trims whitespace, decodes base64, coerces Vec<u8> to [u8; 32] via TryInto (panics with a clear message on wrong length), derives the public key, and prints base64. All error paths use expect() which is appropriate for a CLI tool. No logic bugs. | [LEAN] Reads a base64 private key from stdin, derives the public key, and prints it. Sequential and direct with no unnecessary abstraction. | [NONE] No test file found. stdin reading, base64 decode, 32-byte length validation, and public key derivation are all untested. | [UNDOCUMENTED] No /// doc comments. Private; the expected stdin format (base64 line), key length constraint (32 bytes), and output format are not documented, which could mislead callers piping data. (deliberated: confirmed — Tests WEAK is reasonable. No test file exists. The function does have more logic (stdin read, base64 decode, length check, key derivation) than the simpler commands, so WEAK correctly captures that there's meaningful untested logic but it's a CLI tool where integration tests are the norm.)

- [ ] `rustguard-daemon/src/config.rs` — 7 weak
  Improve `rustguard-daemon/src/config.test.rs` covering: Config, CidrAddr, decode_key, parse_interface, parse_peer, parse_cidr, prefix_to_netmask
  - Config: [USED] Main exported struct holding parsed WireGuard configuration. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Public struct with unique field composition (interface, peers); no duplicate definitions found | [NEEDS_FIX] Config::parse (L120-L122) sets current_section to 'interface' but never flushes current_peer_kvs when a [Interface] header arrives while parsing a [Peer] section. If any [Peer] block precedes [Interface], its accumulated KVs are not cleared; the next [Peer] header's flush guard (current_section == Some('peer')) is then false, so those stale KVs merge with the next peer's KVs. The end-of-file flush emits a single peer that silently drops the first peer's data (last-writer-wins for duplicated keys like PublicKey). | [LEAN] Minimal two-field struct wrapping exactly the two top-level sections of a wg0.conf file. No unnecessary indirection. | [WEAK] Note: tests ARE present — embedded in the same file under #[cfg(test)], not a separate test file. `parse` is exercised by `parse_full_config` and `parse_v6_allowed_ips` covering the happy path with two peers. However, `from_file` is never tested, and error paths (malformed lines, keys outside sections, missing [Interface]) are entirely absent. | [PARTIAL] Struct has `/// Parsed WireGuard configuration.` at line 12, but fields `interface` and `peers` carry no individual `///` docs. The `from_file` and `parse` methods in the impl block (lines ~94–165) also have zero doc comments and no `# Examples` sections, which are expected on public API methods. (deliberated: confirmed — Correction NEEDS_FIX is valid: inspecting Config::parse L120-122, when [Interface] is encountered after a [Peer], current_peer_kvs is not flushed — the flush guard at L125 only triggers inside the [Peer] branch when current_section is already 'peer'. This means [Peer]-before-[Interface] ordering silently merges stale KVs. While WireGuard configs conventionally place [Interface] first, the parser doesn't enforce this and fails silently. Additionally, L163-165 wraps interface in Some() then immediately unwraps via ok_or_else — a dead branch noted in best_practices. Tests WEAK is accurate: parse_full_config and parse_v6_allowed_ips cover happy paths only; from_file is untested; error paths (malformed lines, keys outside sections, missing [Interface]) are absent. Documentation PARTIAL is accurate: struct has a one-line doc comment but fields and methods lack individual docs.)
  - CidrAddr: [USED] Exported struct with three utility methods (contains_v4, contains_v6, contains). Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] CIDR address representation struct; no similar type definitions found | [OK] Struct definition is correct. The contains_v4 and contains_v6 methods use proper bitmask arithmetic; the prefix_len==0 early-return, the >=32 / >=128 caps, and the shift expressions are all correct for their respective address families. | [ACCEPTABLE] Struct itself is minimal. Exposing both typed helpers (contains_v4, contains_v6) and the polymorphic contains() is mildly redundant but useful for callers with already-typed addresses. The ipnetwork crate (popular on crates.io) would eliminate the hand-rolled bit-mask arithmetic in the impl block, but the custom implementation is short and correct so the redundancy is not a real problem. | [WEAK] contains_v4 is well-covered: /24, /32 (host), /0 (default route), and cross-family rejection are all tested. contains_v6 only covers /64; the boundary cases prefix_len=0 (default route) and prefix_len=128 (host) are not tested for v6, unlike v4. The `contains()` dispatch method is never directly exercised. The Display impl is untested. | [PARTIAL] Struct has `/// An IP address with prefix length (CIDR notation). Supports v4 and v6.` at line 37, and all three methods (`contains_v4`, `contains_v6`, `contains`) have one-line `///` comments. However, the two public fields (`addr`, `prefix_len`) lack `///` docs, and none of the methods include a `# Examples` section despite being public API. (deliberated: confirmed — Overengineering ACCEPTABLE is fair: exposing both typed helpers and the polymorphic contains() is mildly redundant but practical for callers. Tests WEAK confirmed: v4 has excellent boundary coverage (/0, /24, /32, cross-family rejection) but v6 only covers /64 — missing /0 and /128 boundary cases. The contains() dispatcher and Display impl are also untested. Documentation PARTIAL confirmed: struct-level doc exists at L37 and methods have one-line docs, but fields addr and prefix_len lack individual docs and no #[doc] examples exist. Raised confidence slightly since all findings are directly verifiable in source.)
  - decode_key: [USED] Non-exported function called locally in parse_interface (line 211) and parse_peer (lines 251, 253). Decodes base64-encoded WireGuard keys to 32-byte arrays. | [UNIQUE] Base64 decodes string to 32-byte key. RAG found base64_key functions (scores 0.70–0.71) but they perform opposite operation (encoding, not decoding) with different signatures and semantic contracts—not interchangeable | [OK] Correctly decodes standard base64 (WireGuard uses RFC 4648 standard alphabet with padding) and validates the decoded length is exactly 32 bytes, matching the WireGuard key size. | [LEAN] Two-step base64 decode then exact-length check. Minimal and correct. | [WEAK] Exercised only implicitly via parse_full_config happy path (valid base64 keys decode without error). No tests for invalid base64 input, wrong decoded length (< or > 32 bytes), or empty string — both error branches in the function are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The name is reasonably descriptive, and per convention private items are tolerated as undocumented. Confidence lowered accordingly. (deliberated: confirmed — Tests WEAK is accurate: decode_key is only exercised via parse_full_config's happy path with valid base64 keys. Both error branches (bad base64, wrong decoded length) are untested. However, as a private helper, this is less critical. Raised confidence slightly since the finding is directly verifiable by checking the test module — no test constructs invalid base64 or wrong-length key inputs.)
  - parse_interface: [USED] Non-exported function called in Config::parse (line 163). Parses [Interface] section of config file with real parsing logic for keys, port, addresses. | [UNIQUE] Parses WireGuard interface config from key-value pairs; no similar parsing functions found | [NEEDS_FIX] L195: when no '/' is present in an Address part, the fallback prefix is hardcoded as the string '24'. For an IPv6 address without an explicit prefix (e.g. 'Address = fd00::1'), this silently assigns prefix_len=24 instead of a valid IPv6 prefix, corrupting the address_v6 tuple stored in InterfaceConfig. parse_cidr (L277) solves the identical problem by branching on addr.is_ipv4(); parse_interface should do the same here. | [LEAN] Length is driven by the number of distinct keys and the dual-stack address parsing. Each branch is a direct field assignment with error mapping; no unnecessary layers of abstraction. | [WEAK] Happy path covered by parse_full_config (port, v4 address, netmask). parse_v6_allowed_ips does NOT exercise the v6 address field on the interface (only peer AllowedIPs). Error paths — missing PrivateKey, missing Address, bad port, bad prefix, bad address string — are all untested. Default listen_port=51820 fallback is never verified. | [UNDOCUMENTED] Private function with no `///` doc comment. Contains non-trivial logic such as comma-separated address parsing and IPv6 handling; a brief doc noting accepted input format would be helpful, but leniency applies to private items. (deliberated: confirmed — Correction NEEDS_FIX is strongly confirmed: at L195, `.unwrap_or((part, "24"))` hardcodes prefix 24 as the fallback when no '/' is present. For an IPv6 address like 'fd00::1' (without explicit prefix), this assigns prefix_len=24 which is invalid for IPv6. The correct approach is visible in parse_cidr at L277-279 which branches on addr.is_ipv4() to select 32 or 128 as default. This is a clear, directly visible bug — raising confidence to 88. Tests WEAK is accurate: v6 interface address parsing is never exercised (parse_v6_allowed_ips only tests peer AllowedIPs), and error paths are absent. Documentation UNDOCUMENTED with private-item leniency is appropriate.)
  - parse_peer: [USED] Non-exported function called in Config::parse (lines 139, 153). Parses [Peer] section of config file with real parsing logic for keys, endpoints, allowed IPs. | [UNIQUE] Parses WireGuard peer config from key-value pairs; no similar parsing functions found | [OK] Correctly parses all mandatory and optional peer fields. AllowedIPs entries are accumulated across repeated keys. Duplicate scalar keys use last-writer-wins semantics, which is acceptable for this format. | [LEAN] Mirrors parse_interface in structure. Five peer fields require five match arms. No avoidable complexity. | [WEAK] Happy path exercised by parse_full_config (endpoint, multiple allowed_ips, persistent_keepalive, absent endpoint). PresharedKey parsing is never exercised. Error paths (missing PublicKey, bad SocketAddr endpoint, bad keepalive value) are all absent. Only 2 of the 5 optional fields are validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Name is self-explanatory; private-item leniency applies. (deliberated: confirmed — Tests WEAK confirmed: parse_full_config exercises endpoint, allowed_ips, and persistent_keepalive, but preshared_key parsing is never exercised and all error paths (missing PublicKey, bad endpoint, bad keepalive) are absent. Only 3 of 5 fields are validated in tests. Documentation UNDOCUMENTED with private-item leniency applies. Raised confidence moderately since findings are directly verifiable against the test module.)
  - parse_cidr: [USED] Non-exported function called in parse_peer (line 259). Parses CIDR notation strings (e.g. '10.0.0.0/24') into CidrAddr instances. | [UNIQUE] Parses CIDR notation with smart defaults (32 for IPv4, 128 for IPv6); no similar functions found | [NEEDS_FIX] L278: prefix_len is parsed as u8 (0-255) with no upper-bound validation against the address family. A CIDR like '10.0.0.1/64' is silently accepted with prefix_len=64. contains_v4 then saturates this to /32 behaviour via the 'prefix_len >= 32' guard (L51), producing semantically incorrect match results without any error. Should reject prefix_len > 32 for IPv4 and > 128 for IPv6. | [LEAN] Splits on '/', selects sensible per-family defaults, parses each part once. Compact and purposeful. | [WEAK] Indirectly exercised through parse_full_config and parse_v6_allowed_ips for well-formed CIDR strings. The no-prefix default behaviour (addr without '/', defaulting to /32 or /128) is never tested. Error paths (invalid IP string, non-numeric prefix) are untested. | [UNDOCUMENTED] Private function with no `///` doc comment. The defaulting logic (32 for v4, 128 for v6 when no prefix is given) is non-obvious behavior that would benefit from a comment, but private-item leniency applies. (deliberated: confirmed — Correction NEEDS_FIX is valid: at L278, prefix_len is parsed as u8 with no upper-bound check against address family. '10.0.0.1/64' is silently accepted; contains_v4 then saturates to /32 via the >= 32 guard at L51, producing semantically incorrect results (treats /64 as /32 without error). Adding validation is a small defensive improvement. Tests WEAK confirmed: only well-formed CIDRs with explicit prefixes are tested; no-prefix defaults and error paths are unexercised. Documentation: the original field says DOCUMENTED while the detail text says UNDOCUMENTED — there is an inline comment '// Default prefix: 32 for v4, 128 for v6.' at L272 but no /// doc comment. Per the rule against adding new findings, I preserve the original DOCUMENTED classification, though the inconsistency is noted. Confidence raised from 60 to 75 as the missing validation is directly visible.)
  - prefix_to_netmask: [USED] Exported function converting prefix length to IPv4 netmask. Runtime-imported by 3 workspace files (rustguard-cli/src/main.rs, rustguard-daemon/src/peer.rs, rustguard-daemon/src/tunnel.rs). | [UNIQUE] Converts IPv4 CIDR prefix length to netmask; no similar utility functions found | [OK] Correctly handles all three regions: prefix==0 returns 0.0.0.0, prefix>=32 returns 255.255.255.255, and the general case uses u32::MAX << (32-prefix) which is safe for prefix in 1..31. Ipv4Addr::from(u32) converts via to_be_bytes(), so byte ordering is correct on all platforms. | [LEAN] Three-branch bit-shift helper converting a prefix length to an Ipv4Addr mask. Handles the two edge cases (0 and >=32) explicitly to avoid undefined-behavior in shift arithmetic. Appropriately minimal. | [WEAK] Only implicitly tested via parse_full_config which asserts netmask==255.255.255.0 for a /24 address. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised. No direct unit tests exist for this exported function despite being imported by three callers. | [UNDOCUMENTED] Public function (`pub fn`) with no `///` doc comment, no parameter description, no description of boundary behaviour for `prefix == 0` or `prefix >= 32`, and no `# Examples` section. Expected on public API. (deliberated: confirmed — Tests WEAK confirmed: only indirectly tested via parse_full_config asserting netmask==255.255.255.0 for /24. The two boundary branches — prefix=0 returning 0.0.0.0 and prefix>=32 returning 255.255.255.255 — are never exercised despite being explicit code paths. No direct unit tests exist for this exported public function. Documentation UNDOCUMENTED confirmed: pub fn at L284 has no /// doc comment, no parameter description, and no explanation of boundary behavior. This is a public API imported by three workspace crates and warrants documentation. Slightly raised confidence.)

- [ ] `rustguard-enroll/src/protocol.rs` — 4 weak
  Improve `rustguard-enroll/src/protocol.test.rs` covering: parse_request, build_response, parse_response, random_nonce
  - parse_request: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment request messages. | [UNIQUE] Structurally similar to parse_response (similarity: 0.759) but semantically distinct: returns Option<[u8;32]> not Option<EnrollmentOffer>, uses ENROLL_REQUEST_MAGIC not ENROLL_RESPONSE_MAGIC, indices [28..76] not [28..81], and performs simple try_into conversion instead of complex struct construction. Different return types and contracts make this unique. | [OK] Length and magic checks are performed before any indexing. Nonce extracted from buf[4..28], ciphertext from buf[28..76] (48 bytes matching the expected seal output). xopen result is converted to [u8; 32] via try_into, which fails safely if the plaintext length differs. Logic is correct. | [LEAN] Straightforward length-and-magic validation followed by decryption. Returns Option which is idiomatic Rust for fallible parsing; no excess abstraction. | [WEAK] Happy path, wrong-token AEAD failure, and ciphertext tampering are all tested. However, the short-buffer early return (buf.len() < ENROLL_REQUEST_SIZE) and the wrong-magic early return (buf[0..4] != ENROLL_REQUEST_MAGIC, e.g. feeding a response packet to the request parser) are not exercised. Two None-returning branches are untested. | [PARTIAL] Two `///` lines describe purpose and return value briefly. Missing: parameter descriptions for `token_key` and `buf`, failure conditions for returning `None` (bad magic, AEAD failure, length mismatch), and `# Examples` section. (deliberated: confirmed — Tests: Three tests cover happy path, wrong-token AEAD failure, and ciphertext tampering — good for the crypto paths. However, the short-buffer guard (L42-44) and wrong-magic guard (L45-47) are untested, leaving two early-return branches uncovered. WEAK is fair but borderline — the crypto-critical paths are well tested while only simple guard clauses are missed. Documentation: Two `///` lines present but missing parameter descriptions and None-returning conditions. PARTIAL confirmed.)
  - build_response: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Constructs encrypted enrollment response messages. | [UNIQUE] Structurally similar to build_request (similarity: 0.738) but semantically distinct: accepts EnrollmentOffer not raw pubkey, constructs intermediate plaintext payload from struct fields, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, and uses indices [28..81] not [28..76]. Different parameters and payload logic make this unique despite structural overlap. | [OK] Plaintext is correctly assembled: server_pubkey at [0..32], ip octets at [32..36], prefix_len at [36]. xseal of 37-byte plaintext produces 53 bytes (37+16 tag), placed at buf[28..81]. All offsets match ENROLL_RESPONSE_SIZE=81. | [LEAN] Serialises EnrollmentOffer into the 81-byte wire frame. Fixed-size arithmetic matches the documented layout; no abstraction overhead. | [WEAK] Only response_roundtrip covers build_response; there is no analogue of request_wrong_token_fails (verifying that a response built with key A is unreadable under key B) nor a tampered_response_rejected test. A single happy-path roundtrip is insufficient for a security-critical wire-format builder. | [PARTIAL] Single-line `///` comment 'Build an enrollment response.' No description of the `token_key` or `offer` parameters, no explanation of what the returned byte array contains, and no `# Examples` section on this public function. (deliberated: confirmed — Tests: Only the response_roundtrip happy path covers build_response. No analogue of request_wrong_token_fails or tampered_request_rejected exists for the response path. This asymmetry is a genuine coverage gap for security-critical code. WEAK is well-justified. Documentation: Single-line `///` comment with no parameter or return descriptions. PARTIAL confirmed.)
  - parse_response: [USED] Exported function imported by 2 runtime files (client.rs, server.rs). Decrypts and validates enrollment response messages. | [UNIQUE] Structurally similar to parse_request (similarity: 0.759) but semantically distinct: returns Option<EnrollmentOffer> not Option<[u8;32]>, uses ENROLL_RESPONSE_MAGIC not ENROLL_REQUEST_MAGIC, indices [28..81] not [28..76], includes plaintext length validation, and constructs complex EnrollmentOffer from decomposed bytes. Different return contracts and invariants make this unique. | [OK] Length and magic guards are in place before indexing. Nonce from buf[4..28], ciphertext from buf[28..81] (53 bytes). The explicit plaintext.len() != 37 check is redundant given a correctly functioning AEAD but is not harmful. Fields are extracted at correct offsets. All None-returning paths use ? or explicit return None. | [LEAN] Mirrors parse_request in structure; the extra plaintext-length check (L90) is a defensive sanity check appropriate for a security-sensitive protocol, not gold-plating. | [WEAK] Only the happy-path roundtrip is tested. Missing: wrong-token AEAD failure, ciphertext tampering, short-buffer early return, wrong-magic early return (e.g. feeding a request packet), and the explicit plaintext.len() != 37 guard. Four out of five failure branches are entirely untested, contrasting with the more thorough coverage of parse_request. | [PARTIAL] Single-line `///` comment 'Parse and decrypt an enrollment response.' Missing parameter descriptions, no enumeration of `None`-returning conditions (bad magic, AEAD failure, wrong plaintext length), and no `# Examples` section for a public function. (deliberated: confirmed — Tests: Only the happy-path roundtrip is tested. Four of five failure branches (wrong-token, tampering, short-buffer, wrong-magic, plaintext length check at L90) are entirely untested. This is the weakest test coverage in the file and contrasts sharply with parse_request's coverage. WEAK is clearly correct. Documentation: Single-line `///` with no parameter descriptions or enumeration of None-returning conditions. PARTIAL confirmed.)
  - random_nonce: [USED] Internal helper function called by build_request (L29) and build_response (L69). Generates cryptographic nonces. | [UNIQUE] Shows high similarity (0.943) to random_nonce in rustguard-core/src/cookie.rs but resides in separate crate. Per established precedent: cross-crate deduplication of trivial 3-line private helpers introduces unnecessary inter-crate coupling. Independent usage in different crates reflects intentional architecture, not harmful duplication. | [OK] Uses getrandom::getrandom for OS-provided entropy into a 24-byte buffer. Panicking on failure is appropriate for a crypto primitive — a system that cannot supply entropy cannot operate securely. No correctness issues. | [LEAN] Tiny typed wrapper around getrandom that gives the operation a name and a concrete return type. Avoids scattered unsafe-ish getrandom calls; well-justified as a local helper. | [WEAK] Private helper tested only indirectly: roundtrip tests verify the nonces it produces are valid (AEAD succeeds), but uniqueness across calls, panic behavior if getrandom fails, and the 24-byte length are never directly asserted. Indirect coverage is plausible but fragile for a CSPRNG wrapper. | [UNDOCUMENTED] Private helper function with no `///` doc comment. The name is self-descriptive and the function is not part of the public API, so omission is tolerable by convention. The `expect` message documents the panic condition inline. (deliberated: confirmed — Tests: Private 3-line helper tested only indirectly via roundtrip tests. Indirect coverage does verify the nonce works (AEAD succeeds), but nonce uniqueness and the panic path are untested. For a private helper this small, WEAK is borderline — indirect testing is the norm for trivial private helpers. However, since it's a CSPRNG wrapper and no direct assertions exist, WEAK is defensible. Bumping confidence slightly since indirect testing through multiple roundtrip tests does provide meaningful coverage.)

- [ ] `rustguard-tun/src/linux.rs` — 4 untested, 4 weak
  Improve `rustguard-tun/src/linux.test.rs` covering: last_os_error, close_and_error, make_sockaddr_in, set_name, create, configure_interface, read, write
  - last_os_error: [USED] Helper function used for OS error conversion in create(), configure_interface(), read(), and write() | [UNIQUE] Trivial 2-line wrapper with no duplicates found via RAG | [OK] Straightforward delegation to io::Error::last_os_error(); no correctness issues. | [OVER] Single-line alias for io::Error::last_os_error() that saves only the 'io::Error::' prefix. Adds a call-stack frame with no semantic benefit; callers could use io::Error::last_os_error() directly. Borderline, hence moderate confidence. | [NONE] No test file found for rustguard-tun/src/linux.rs. last_os_error has zero test coverage. | [UNDOCUMENTED] Private helper function, no `///` doc comment. Name is self-explanatory. Tolerated for private items. (deliberated: reclassified: overengineering: OVER → ACCEPTABLE — Overengineering reclassified OVER → ACCEPTABLE: this 2-line wrapper is used at 4 distinct call sites (create, configure_interface, read, write), providing a consistent shorter alias alongside the companion close_and_error pattern. In Rust FFI code with many libc error checks, such aliases are a common and accepted convention. Tests WEAK retained — no test file exists for this module.)
  - close_and_error: [USED] Helper function to close fd and return error; used throughout create() and configure_interface() | [DUPLICATE] Identical 5-line helper found in linux_mq.rs with score 0.987 | [OK] Correctly captures errno via last_os_error() before calling close(), preventing the close() syscall from overwriting errno. Error is returned after fd is closed. | [LEAN] Captures errno before closing the fd to prevent clobbering — a legitimate and important pattern in unsafe libc code. Used consistently at every error exit point. | [NONE] No test file found for rustguard-tun/src/linux.rs. close_and_error has zero test coverage. Error path (fd cleanup on failure) is entirely untested. | [UNDOCUMENTED] Private helper function that closes an fd and captures the last OS error. No `///` doc comment. Tolerated for private items with clear names. (deliberated: confirmed — Duplication is genuine — identical 5-line function exists in linux_mq.rs. Both capture errno before close(fd) to prevent clobbering. Could be extracted to a shared internal utility module (e.g., linux_common.rs). Tests WEAK retained — no test file for this module.)
  - make_sockaddr_in: [USED] Helper function called 3 times in configure_interface() to construct sockaddr_in structs | [DUPLICATE] Identical 10-line function in linux_mq.rs with score 0.973 | [OK] u32::from_ne_bytes(addr.octets()) is correct for s_addr. On a little-endian host, from_ne_bytes([a,b,c,d]) produces the integer 0xdcba…, which when stored in memory LE lays down bytes [a,b,c,d] — preserving network (big-endian) byte order in memory exactly as the kernel expects. | [LEAN] Encapsulates boilerplate sockaddr_in initialisation used three times in configure_interface; avoids repetition and ensures sin_family/sin_zero are always correctly set. | [NONE] No test file found for rustguard-tun/src/linux.rs. make_sockaddr_in has zero test coverage. The byte-order conversion (u32::from_ne_bytes) and struct initialization are not validated by any test. | [UNDOCUMENTED] Private helper constructing a `sockaddr_in` from an `Ipv4Addr`. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 10-line function in linux_mq.rs constructing libc::sockaddr_in from Ipv4Addr with identical field initialization. Same semantic contract in both files, prime candidate for shared module extraction. Tests WEAK retained.)
  - set_name: [USED] Helper function used in create() and configure_interface() to copy interface name into buffers | [DUPLICATE] Identical 5-line function in linux_mq.rs with score 0.968 | [OK] Copies up to IFNAMSIZ-1 bytes, leaving the final byte as the zero from the caller's zero-initialized buffer. Null termination is always present. | [LEAN] Tiny but used at five distinct call sites; centralises the null-termination and length-clamping logic that would otherwise be repeated inline each time. | [NONE] No test file found for rustguard-tun/src/linux.rs. set_name has zero test coverage. Truncation at IFNAMSIZ-1 for long names and NUL-termination behavior are not tested. | [UNDOCUMENTED] Private helper writing a name into a fixed-size `[u8; IFNAMSIZ]` buffer. No `///` doc comment. Tolerated for private items. (deliberated: confirmed — Duplication is genuine — identical 5-line function in linux_mq.rs copying null-terminated name bytes into fixed-size buffer. Same contract, same truncation logic. Tests WEAK retained.)

- [ ] `rustguard-tun/src/bpf_loader.rs` — 9 untested
  Improve `rustguard-tun/src/bpf_loader.test.rs` covering: XdpProgram, bpf_create_xskmap, bpf_prog_load, bpf_map_update, attach_xdp, attach_xdp_netlink, detach_xdp, ifindex_to_name, parse_and_patch_elf

- [ ] `rustguard-crypto/src/aead.rs` — 4 untested, 1 weak
  Improve `rustguard-crypto/src/aead.test.rs` covering: xseal, xopen, seal_to, open_to, build_nonce
  - build_nonce: [USED] Non-exported internal function called 4 times within file: in seal, open, seal_to, open_to | [UNIQUE] Private helper that builds WireGuard nonce from counter (4 zero bytes + 8-byte LE counter). No similar functions found in RAG search results. | [OK] WireGuard specifies nonce = 4 zero bytes || 8-byte little-endian counter. nonce[4..] is exactly 8 bytes and to_le_bytes() produces 8 bytes; construction is correct. | [LEAN] Minimal private helper encoding the WireGuard nonce format (4 zero bytes || 8-byte LE counter). Four lines, single responsibility, well-commented at call sites. | [WEAK] Private helper exercised indirectly through seal/open tests; wrong_counter_fails implicitly validates that distinct counter values produce distinct nonces. However the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted, and seal_to/open_to (which also call build_nonce) have no tests at all. | [DOCUMENTED] Private function with a clear `///` comment describing the WireGuard nonce layout (4 zero bytes || 8-byte LE counter). For a private helper, this is sufficient; no examples are expected per private-item leniency. (deliberated: confirmed — Tests WEAK is reasonable. The function is exercised indirectly through seal/open tests, and wrong_counter_fails implicitly validates distinct counters produce distinct nonces. However, the specific WireGuard nonce layout (4 zero bytes || 8-byte LE counter) is never directly asserted. For a private helper, this indirect coverage provides some assurance but not full confidence in the byte layout. Confidence raised slightly as the indirect coverage is real, but the layout-specific gap remains valid.)

- [ ] `rustguard-tun/src/macos.rs` — 5 untested, 2 weak
  Improve `rustguard-tun/src/macos.test.rs` covering: close_and_error, make_sockaddr_in, create, configure_address, set_mtu, read, write
  - close_and_error: [USED] Called 4 times to safely preserve OS error before closing file descriptor (L111, L127, L142, L165) | [DUPLICATE] Identical implementation found in linux.rs and linux_mq.rs with scores 0.977 and 0.979. Captures errno, closes fd, returns error—same logic and semantic contract across all three platform modules. | [OK] Correctly captures errno before calling close() to prevent the close() syscall from clobbering errno. | [LEAN] Solves a real, documented problem: libc::close() can clobber errno, so the error must be captured first. The comment explains the invariant clearly. Not abstraction for its own sake. | [NONE] No test file exists for this module. This private fd-cleanup helper has zero test coverage. The errno-before-close ordering is untested. | [PARTIAL] Private function with three `///` lines (lines 76–78) explaining purpose ('Capture the OS error, close the fd'), motivation ('avoids close() clobbering errno'), and a Safety precondition. Adequate for a private unsafe helper. No formal `# Parameters` or `# Examples`, but those are not expected for private functions. (deliberated: confirmed — Duplication DUPLICATE is technically accurate — identical 4-line implementations exist in linux.rs and linux_mq.rs. However, these are platform-gated modules (only one compiles per target), so deduplication into a shared utility is a low-value refactor. Keeping DUPLICATE but lowering confidence to reflect that cross-platform-module duplication is a weaker signal. Documentation PARTIAL is accurate: the three /// lines adequately document purpose and safety for a private unsafe helper.)
  - make_sockaddr_in: [USED] Called 3 times in configure_address() to construct SockaddrIn for address configuration (L205-207) | [UNIQUE] High RAG similarity (0.968) but returns custom SockaddrIn struct; linux variants return libc::sockaddr_in. Different struct layouts and initialization logic—macos uses sin_len field, linux uses u32::from_ne_bytes conversion. Different semantic contracts due to struct type differences. | [OK] Correctly constructs a Darwin sockaddr_in with sin_len=16, AF_INET family, zero port, and octets from Ipv4Addr. | [LEAN] Used three times in configure_address() for addr, broadaddr, and mask. DRY helper with no generics or unnecessary indirection; appropriate factoring. | [NONE] No test file exists for this module. make_sockaddr_in is a pure function (Ipv4Addr -> SockaddrIn) that is trivially unit-testable without kernel access, yet has zero test coverage. Field values including sin_len, sin_family, and sin_addr byte ordering are never asserted. | [UNDOCUMENTED] Private helper function with no `///` doc comment. Name and single `Ipv4Addr` parameter are self-describing. Tolerated under private-item leniency. (deliberated: confirmed — Tests WEAK kept. This is a pure function (Ipv4Addr → SockaddrIn) with actual struct construction logic including sin_len=16, sin_family assignment, and octet mapping. It is trivially unit-testable without kernel access, yet has zero coverage. WEAK is appropriate rather than NONE since the function's simplicity reduces the risk.)

- [ ] `rustguard-kmod/src/cookie.rs` — 6 weak
  Improve `rustguard-kmod/src/cookie.test.rs` covering: hash, mac, random_bytes, CookieChecker, CookieState, constant_time_eq
  - hash: [USED] Internal helper function called in verify_mac1 (line 101), create_reply (line 126), process_reply (line 154) for BLAKE2s hashing. | [UNIQUE] RAG score 0.937 with noise.rs hash, but implementations differ: this version uses array size 4 and passes uncapped chunks.len() to C function; noise.rs uses array size 8 and passes capped num. Different capacity invariants and capping behavior suggest intentionally different constraints despite semantic similarity. | [NEEDS_FIX] Line 49 passes `chunks.len() as u32` as `num_chunks` to the C function, but the loop at line 44 uses `.take(4)` and populates at most 4 entries in `ptrs` and `lens`. If `chunks.len() > 4`, the C function is told there are more chunks than have been written, causing it to read beyond the bounds of the 4-element Rust arrays — a stack over-read and undefined behavior. The argument must be clamped: `chunks.len().min(4) as u32`. | [LEAN] Thin FFI adapter that avoids repeating unsafe boilerplate at every call site. The hard cap of 4 chunks mirrors kernel-module constraints (no heap). Silent truncation beyond 4 is a mild concern, but the design is minimal and fit-for-purpose. | [WEAK] No direct tests found for this private function. However, prior deliberation established that tests such as cookie_roundtrip() and wrong_mac1_fails_decrypt() exist and exercise CookieChecker::verify_mac1(), which calls hash() internally. Indirect coverage only — hash is never tested with edge-case inputs (empty slice, >4 chunks clipped by take(4), zero-length chunks), and no assertions target hash output directly. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_hash` with a slice-of-slices interface; the chunked hashing behavior and 4-chunk maximum are not explained. (deliberated: confirmed — Correction NEEDS_FIX is confirmed with high confidence: line 44 uses .take(4) populating at most 4 entries in ptrs/lens arrays, but line 49 passes chunks.len() as u32 — the raw unclamped caller count — to the C function. If chunks.len() > 4, the C function reads past the 4-element stack arrays causing out-of-bounds access. All current callers pass exactly 2 chunks so the bug is latent, but the API provides zero enforcement. This is a genuine memory safety defect. Tests WEAK is correct: hash is only exercised indirectly through verify_mac1/create_reply/process_reply; no direct tests for edge cases (>4 chunks, empty input, zero-length chunks).)
  - mac: [USED] Internal helper for BLAKE2s MAC computation, called in verify_mac1 (103), make_cookie (117), verify_mac2 (143), compute_mac2 (189). | [DUPLICATE] RAG score 0.974 with noise.rs mac — identical signature and implementation. Both compute BLAKE2s-256 MAC by calling wg_blake2s_256_mac with same parameter passing and return value extraction. | [OK] Straightforward FFI wrapper for BLAKE2s-MAC. Key, data, lengths, and output pointer are all forwarded correctly with no type mismatches. | [LEAN] One-liner FFI wrapper that eliminates repeated unsafe blocks. No unnecessary generics or abstraction layers. | [WEAK] No direct tests found. Indirectly exercised through CookieChecker::verify_mac1() and make_cookie() in the test suite established by prior deliberation (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie). Edge cases such as empty key, empty data, or mismatched key lengths are never directly validated. | [UNDOCUMENTED] Private function with no `///` doc comment. Wraps `wg_blake2s_256_mac` but the keyed-MAC semantics and return size (32 bytes) are not described. (deliberated: confirmed — Duplication: reported as 100% identical to noise.rs mac function — both are thin wrappers calling wg_blake2s_256_mac with the same parameter pattern. This is a true functional duplicate that belongs in a shared crypto utility module. Tests WEAK: only indirect coverage through CookieChecker methods; no direct tests for empty key, empty data, or edge-case inputs. Both findings maintained.)
  - random_bytes: [USED] Generic random bytes generator called for secret initialization (85), secret rotation (112), and nonce generation (131). | [UNIQUE] RAG score 0.841 with rustguard-core/src/cookie.rs, but different semantic contract: this version is generic <const N: usize>() returning [u8; N]; core version is fixed fn random_bytes() -> [u8; 32]. Also calls different RNG function (wg_get_random_bytes vs fill_random). | [OK] Const-generic buffer is correctly sized and its length is passed as u32. All call sites use small N (24 and 32 bytes), well within u32 range. | [LEAN] Const-generic N produces a zero-cost, type-safe wrapper over the FFI random call. Idiomatic Rust; avoids repeated unsafe boilerplate for different buffer sizes. | [NONE] No test file found for this source file and no direct tests for this function. While CookieChecker::new() calls random_bytes() during test setup, the randomly generated output is inherently non-deterministic and cannot be meaningfully asserted. No smoke tests verifying it doesn't panic or returns the correct length are present. | [UNDOCUMENTED] Private generic function with no `///` doc comment. The kernel RNG source and const-generic size parameter are not documented; leniency applies as private. (deliberated: confirmed — Tests WEAK is appropriate. The function is indirectly exercised via CookieChecker::new() and make_cookie()/create_reply() in tests, but its non-deterministic nature makes direct assertion impractical. No smoke tests exist verifying correct buffer length or non-panic behavior. WEAK (not NONE) is correct since indirect coverage exists through CookieChecker test suite.)
  - CookieChecker: [USED] Primary server-side cookie validation API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module server-side cookie validation state with public key, secret, timestamp, and under-load flag. | [OK] Struct field declarations are correct. Types are appropriate: u64 for nanosecond timestamps, [u8;32] for keys and public values. | [LEAN] Four fields encoding exactly the server-side cookie state described by the WireGuard spec. No superfluous generics, traits, or builder patterns. Methods are small and single-purpose. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, mac2_wrong_source_fails) exercise CookieChecker. Coverage is partial: secret rotation via maybe_rotate_secret() after COOKIE_SECRET_LIFETIME_NS elapses is not tested, create_reply() wire-format correctness is not independently asserted, and under_load flag transitions are not tested. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Server-side: generates cookies and validates MAC2.') but fields lack `///` comments, there are no `# Examples`, and the secret-rotation lifecycle and `under_load` flag semantics are not explained. (deliberated: confirmed — Tests WEAK confirmed: CookieChecker is exercised by multiple tests (cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie) but important paths remain untested — secret rotation after COOKIE_SECRET_LIFETIME_NS, under_load flag transitions, and create_reply wire-format correctness. Documentation PARTIAL confirmed: has a one-line /// comment but fields lack documentation, no examples, and the secret rotation lifecycle and thread-safety requirements are undescribed for a security-critical pub(crate) struct.)
  - CookieState: [USED] Primary client-side cookie storage API in library crate; zero workspace importers expected for library code consumed externally. | [UNIQUE] No similar struct found in RAG results; kernel-module client-side cookie storage with optional decrypted cookie and reception timestamp. | [OK] Option<[u8; COOKIE_LEN]> correctly models presence/absence of a stored cookie. u64 for the receipt timestamp is appropriate. | [LEAN] Two-field client-side struct: the cookie value and its timestamp. Minimal representation; freshness check is a simple subtraction. No unnecessary indirection. | [WEAK] Prior deliberation established that tests (setup, cookie_roundtrip, wrong_mac1_fails_decrypt, mac2_verified_after_cookie, no_cookie_gives_zero_mac2) exercise CookieState. Coverage is partial: staleness expiry (is_fresh returning false after COOKIE_SECRET_LIFETIME_NS) is not tested; process_reply with an invalid/tampered encrypted payload and the false-return path is also not covered. Not re-flagged as NONE per deliberation. | [PARTIAL] `pub(crate)` struct has a single-line `///` comment ('Client-side: stores a received cookie for MAC2.') but fields are undocumented, freshness/staleness semantics are not described, and no `# Examples` section is present. (deliberated: confirmed — Tests WEAK confirmed: CookieState is covered by several tests (cookie_roundtrip, no_cookie_gives_zero_mac2) but staleness expiry (is_fresh returning false after timeout) and process_reply with tampered payloads are untested. Documentation PARTIAL confirmed: single-line /// comment present but fields undocumented, freshness semantics unexplained, and no examples for a pub(crate) type.)
  - constant_time_eq: [USED] Internal constant-time comparison wrapper using kernel crypto_memneq, called in verify_mac1 (104) and verify_mac2 (144). | [DUPLICATE] RAG score 0.962 with noise.rs constant_time_eq — identical implementation. Both check length equality then use wg_crypto_memneq for constant-time comparison, with same parameter passing and return logic. | [OK] Correctly delegates to the kernel's crypto_memneq, which is guaranteed constant-time and cannot be optimized away by the compiler. The early-exit length check is safe because all callers pass fixed-length 16-byte slices, so the branch carries no secret-dependent timing information. | [LEAN] Security-critical helper delegating to the kernel's crypto_memneq, which is guaranteed not to be optimized away. Extracting it into a named function documents intent and prevents accidental substitution with a non-constant-time comparison elsewhere. | [WEAK] No direct tests found. Indirectly exercised through verify_mac1() and verify_mac2() in the CookieChecker test suite (established by prior deliberation). Critical edge cases are untested: mismatched-length slices (returns false early without calling wg_crypto_memneq), equal-length but differing slices, and zero-length slices. The constant-time guarantee itself is structurally untestable at the unit level, but behavioral correctness for these edge cases is never directly asserted. | [PARTIAL] Private function has a `///` doc comment noting its constant-time guarantee via `wg_crypto_memneq`. However, parameter semantics (slice length requirement), return-value meaning, and panic/safety conditions are not described. Leniency applies as private. (deliberated: confirmed — Duplication DUPLICATE: reported as 100% identical to noise.rs constant_time_eq — both check length then delegate to wg_crypto_memneq. This is a security-critical utility that absolutely should live in a shared module to prevent divergent copies. Tests WEAK: only exercised indirectly through verify_mac1/verify_mac2; mismatched-length and zero-length edge cases never tested directly. Documentation PARTIAL: has a /// comment noting the constant-time guarantee (good) but parameter semantics and return value meaning are undescribed.)

- [ ] `rustguard-tun/src/linux_mq.rs` — 6 untested
  Improve `rustguard-tun/src/linux_mq.test.rs` covering: set_name, make_sockaddr_in, close_and_error, MultiQueueTun, open_tun_queue, configure_interface
