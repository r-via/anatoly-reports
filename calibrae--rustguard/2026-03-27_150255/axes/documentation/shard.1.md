[← Back to Documentation](./index.md) · [← Back to report](../../public_report.md)

# 📝 Documentation — Shard 1

- [📊 Findings](#-findings)
- [🔍 Symbol Details](#-symbol-details)
- [🧹 Hygiene](#-hygiene)

## 📊 Findings

| File | Verdict | Documentation | Conf. | Details |
|------|---------|---------------|-------|---------|
| `rustguard-kmod/src/lib.rs` | 🔴 CRITICAL | 13 | 90% | [details](#rustguard-kmodsrclibrs) |
| `rustguard-tun/src/xdp.rs` | 🔴 CRITICAL | 5 | 95% | [details](#rustguard-tunsrcxdprs) |
| `rustguard-enroll/src/server.rs` | 🔴 CRITICAL | 3 | 92% | [details](#rustguard-enrollsrcserverrs) |
| `rustguard-kmod/src/replay.rs` | 🔴 CRITICAL | 1 | 90% | [details](#rustguard-kmodsrcreplayrs) |
| `rustguard-kmod/src/noise.rs` | 🟡 NEEDS_REFACTOR | 19 | 92% | [details](#rustguard-kmodsrcnoisers) |
| `rustguard-core/src/handshake.rs` | 🟡 NEEDS_REFACTOR | 14 | 88% | [details](#rustguard-coresrchandshakers) |
| `rustguard-core/src/cookie.rs` | 🟡 NEEDS_REFACTOR | 5 | 92% | [details](#rustguard-coresrccookiers) |
| `rustguard-daemon/src/tunnel.rs` | 🟡 NEEDS_REFACTOR | 4 | 90% | [details](#rustguard-daemonsrctunnelrs) |
| `rustguard-core/src/messages.rs` | 🟡 NEEDS_REFACTOR | 12 | 90% | [details](#rustguard-coresrcmessagesrs) |
| `rustguard-core/src/timers.rs` | 🟡 NEEDS_REFACTOR | 1 | 90% | [details](#rustguard-coresrctimersrs) |

## 🔍 Symbol Details

### `rustguard-kmod/src/lib.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `AEAD_TAG_SIZE` | L111–L111 |  PARTIAL | 82% | Public constant has `/// AEAD authentication tag size.` but omits which AEAD scheme (ChaCha20-Poly1305), the unit (bytes), and has no `# Examples` section expected for public API items. (deliberated: confirmed — PARTIAL documentation confirmed: public constant documents 'AEAD authentication tag size' but omits the AEAD scheme (ChaCha20-Poly1305) and unit (bytes). Acceptable for a well-named constant but technically incomplete for a public API item.) |
| `rustguard_xmit` | L382–L384 |  PARTIAL | 70% | Public `#[no_mangle]` extern-C function has `/// TX callback: encrypt plaintext and send as WireGuard transport packet.` but lacks parameter descriptions for `skb` and `priv_`, return value semantics, error conditions, and `# Examples`. (deliberated: confirmed — NONE tests confirmed: FFI callback with no test coverage. PARTIAL docs confirmed: has doc comment but lacks parameter descriptions for skb/priv_ and return semantics.) |
| `rustguard_rx` | L465–L467 |  PARTIAL | 70% | Public `#[no_mangle]` extern-C function has two `///` lines mentioning message types handled and byte-order of src_ip/src_port, but lacks per-parameter descriptions for `skb` and `priv_`, return value semantics, and `# Examples`. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: FFI callback has doc comment mentioning message types and byte order but lacks per-parameter descriptions.) |
| `handle_initiation` | L521–L620 |  PARTIAL | 82% | Private unsafe function with two `///` lines stating role (responder) and safety precondition (socket lock). Missing: parameter descriptions, return semantics, description of side effects (session install, index_map update, timestamp update, cookie reply path). (deliberated: confirmed — NONE tests confirmed: security-critical responder handshake handler with no test coverage. PARTIAL docs confirmed: two /// lines state role and safety precondition but omit parameter descriptions and side effects (session install, index_map update, timestamp update, cookie reply path).) |
| `handle_response` | L624–L671 |  PARTIAL | 82% | Private unsafe function with two `///` lines stating role (initiator) and safety precondition. Missing: parameter descriptions, side effects (session rotation, index_map update, endpoint update), and failure conditions. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: similar to handle_initiation — has brief two-line doc comment but lacks parameter descriptions and side-effect documentation (session rotation, index_map, endpoint update).) |
| `handle_transport` | L676–L755 |  PARTIAL | 80% | Private unsafe function has three `///` lines covering purpose, safety, and replay_window exclusivity. Better than sibling handlers but still missing parameter descriptions, decrypt-fallback-to-prev_session behavior, and keepalive/endpoint-roaming handling. (deliberated: confirmed — NEEDS_FIX confirmed: L708 hardcodes 2048-byte stack buffer and L710 silently drops packets with encrypted_len > 2048. Max usable plaintext is 2032 bytes, insufficient for jumbo frames (9000+). Silent discard with no error counter or log is the key issue. NONE tests and PARTIAL docs confirmed.) |
| `handle_cookie_reply` | L758–L781 |  PARTIAL | 80% | Private unsafe function has one `///` line stating purpose. Missing: parameter descriptions, the known limitation (TODO: store last sent MAC1 per peer documented only inline), and return semantics. (deliberated: confirmed — NEEDS_FIX confirmed: L777 uses [0u8;16] for our_mac1 with an acknowledged TODO. Without correct MAC1, cookie reply HMAC verification is bypassed and any crafted reply is accepted. Subsequent MAC2 retry will compute over the wrong cookie, failing server-side. DoS protection is non-functional. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_dev_uninit` | L785–L785 |  PARTIAL | 90% | Public `#[no_mangle]` extern-C function has `/// Device teardown callback.` — minimal description for a public symbol. No parameter docs, no description of when it is called, and the stub nature is undocumented. (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: this is a #[no_mangle] extern-C callback required by the net_device operations table ABI. Removing it would break kernel module registration. An empty stub is the correct implementation — net_device ops require all callbacks to be present. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_timer_tick` | L789–L791 |  PARTIAL | 70% | Public `#[no_mangle]` extern-C function has `/// Periodic timer callback (every 250ms) — check rekeying and keepalives.` Mentions periodicity and actions but lacks parameter description for `priv_` and does not list side effects or error behavior. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: FFI timer callback has doc comment mentioning 250ms periodicity and rekeying/keepalive actions but lacks parameter description for priv_.) |
| `initiate_rekey` | L847–L877 |  PARTIAL | 80% | Private unsafe function has `/// Initiate a rekey handshake for a peer.` — one terse line. Missing: parameter descriptions, preconditions (peer must have non-zero public key), and side effects (pending_handshake set, timer updated). (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: has single-line /// doc comment but lacks parameter descriptions, preconditions (peer must have non-zero public key), and side effects (pending_handshake set, timer updated).) |
| `send_keepalive` | L880–L914 |  PARTIAL | 80% | Private unsafe function has `/// Send an empty keepalive transport packet.` — one terse line. Missing: parameter descriptions, precondition that an active session must exist, and the zero-length plaintext AEAD encoding detail. (deliberated: confirmed — NONE tests and PARTIAL docs confirmed: has single-line /// doc comment but lacks parameter descriptions and precondition that an active session must exist.) |
| `rustguard_genl_get` | L918–L922 |  PARTIAL | 88% | Public `#[no_mangle]` extern-C function has `/// Genetlink GET callback (stub — returns device info).` Notes stub status but lacks descriptions for all three parameters and the return value (always 0). (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: this stub is registered as the genetlink GET callback in wg_genl_init(). Removing it would leave a null function pointer in the genetlink family registration, causing a kernel crash when wg(8) tool issues a GET. Stub returning 0 is the correct placeholder. NONE tests and PARTIAL docs confirmed.) |
| `rustguard_genl_set` | L926–L932 |  PARTIAL | 88% | Public `#[no_mangle]` extern-C function has `/// Genetlink SET callback (stub — configures peers).` Notes stub status but has seven parameters with no individual descriptions, and the return value (always 0) is undocumented. (deliberated: reclassified: utility: LOW_VALUE → USED — Utility reclassified LOW_VALUE→USED: same as rustguard_genl_get — registered as genetlink SET callback and must exist for ABI safety. Removing it risks null pointer dereference in kernel context. NONE tests and PARTIAL docs confirmed.) |

### `rustguard-tun/src/xdp.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `XdpDesc` | L62–L66 |  UNDOCUMENTED | 90% | Public `pub struct` with three `pub` fields (`addr`, `len`, `options`). No `///` doc comment on the type or on any field. Public visibility requires documentation; neither purpose nor field semantics are described. (deliberated: confirmed — Documentation UNDOCUMENTED confirmed: public exported struct with three public fields and no `///` doc comment on type or fields. Public visibility demands documentation per Rust conventions. All other axes clean.) |
| `Ring` | L89–L95 |  PARTIAL | 88% | Private struct. Has one type-level `///` doc comment: 'A single-producer/single-consumer ring over mmap'd memory.' No field-level docs; the inline comment on `descs` is a regular `//` comment, not a `///` doc comment. Basic description present but incomplete. (deliberated: reclassified: tests: WEAK → NONE — Tests reclassified WEAK → NONE: the detail text explicitly states 'No test file exists for this module; zero test coverage.' Zero coverage is NONE, not WEAK — the WEAK classification was inconsistent with its own justification. Documentation PARTIAL confirmed: has type-level doc comment but no field-level docs; acceptable for private struct but still incomplete. Confidence raised since these are straightforward observations from the source.) |
| `XdpConfig` | L126–L132 |  PARTIAL | 88% | Public struct. Has a single type-level `///` doc comment: 'Configuration for creating an XDP socket.' No `///` doc comments on any of the five `pub` fields (`ifname`, `queue_id`, `frame_size`, `num_frames`, `ring_size`). No `# Examples` section. Type-level intent is stated but field semantics, units, and constraints are undocumented. (deliberated: confirmed — Documentation PARTIAL confirmed: public struct has a type-level `///` comment but none of the five public fields have doc comments. Field semantics (units, constraints, defaults reasoning) are undocumented. All other axes clean.) |
| `XdpSocket` | L148–L160 |  PARTIAL | 95% | Public struct. Has a two-line type-level `///` doc comment describing purpose and zero-copy role. Only one field (`free_frames`) has a `///` comment; all other fields (`fd`, `umem`, `umem_size`, `frame_size`, ring fields, `ring_size`) lack doc comments. No `# Examples` section on the type. (deliberated: reclassified: overengineering: ACCEPTABLE → LEAN — Correction ERROR confirmed at high confidence — all three bugs verified in source: (1) tx_send at ~L333 calls copy_nonoverlapping with data.len() but never bounds-checks against frame_size, enabling out-of-bounds write into adjacent UMEM frames; (2) Drop at L397-400 only munmaps self.umem while four ring mmap regions from mmap_ring are never stored/unmapped, leaking kernel-mapped memory every drop; (3) setsockopt_raw calls at L230-233 discard return values silently, allowing the socket to proceed with misconfigured rings. Overengineering reclassified ACCEPTABLE → LEAN: the detail itself states 'No extraneous layers; the public API maps one-to-one with the AF_XDP usage model,' which is the definition of LEAN — ACCEPTABLE was unjustified. Tests NONE confirmed: no test file exists. Documentation PARTIAL confirmed: type-level docs present but most fields undocumented.) |
| `if_nametoindex` | L449–L458 |  UNDOCUMENTED | 90% | Public `pub fn`. No `///` doc comment of any kind. The function converts an interface name string to a kernel interface index and propagates OS errors; its parameter, return type semantics, and error conditions are entirely undocumented despite public visibility. (deliberated: confirmed — Tests NONE confirmed: public function with no test coverage, neither happy path nor error branch tested. Documentation UNDOCUMENTED confirmed: public function with no `///` doc comment — parameter semantics, return type, and error conditions completely undocumented despite public visibility. Both findings well-supported.) |

### `rustguard-enroll/src/server.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ServeConfig` | L49–L62 |  UNDOCUMENTED | 92% | Public struct with no /// on the type itself. Three fields (xdp_ifname, tun_queues, use_uring) have /// comments; core fields listen_port, pool_network, pool_prefix, token, open_immediately, and state_path have none. No # Examples section anywhere on the type. (deliberated: reclassified: utility: DEAD → USED — Utility reclassified DEAD→USED: ServeConfig is the sole parameter type of `pub fn run(config: ServeConfig)` at L64. Any caller of `run()` must construct a ServeConfig. The '0 external importers' result is a static analysis limitation — this is a library crate whose public entry point is consumed by a binary crate (not captured by import analysis). Removing ServeConfig would break the public API. Tests NONE: confirmed, no test file. Documentation UNDOCUMENTED: confirmed, public type with no `///` on the struct itself (detail text says PARTIAL due to some field comments, but the type-level doc is absent; keeping UNDOCUMENTED as original field).) |
| `run` | L64–L532 |  UNDOCUMENTED | 92% | Public function and sole public entry point of the enrollment server with no /// doc comments. No description of overall behavior, ServeConfig parameter semantics, io::Result error conditions (bind failures, TUN creation, io_uring init), blocking behavior, or # Examples. Critical documentation gap for a ~470-line public API function. (deliberated: reclassified: utility: DEAD → USED — Correction ERROR confirmed (both defects verified in source): (1) L444-446: `decrypt_buf` is 2048 bytes but `ct = &buf[TRANSPORT_HEADER_SIZE..]` is unbounded from UDP recv; `decrypt_buf[..ct.len()].copy_from_slice(ct)` will panic if ct.len() > 2048. XDP frames up to 4096 bytes make this reachable. (2) L413: `_ts` (TAI64N timestamp) is discarded, removing WireGuard's mandated initiation replay protection. Both are genuine security/correctness bugs visible in the code. Utility reclassified DEAD→USED: `run` is the sole public entry point of the enrollment server module — it is the raison d'être of this crate. The binary crate calls it from main(). '0 external importers' is a static analysis false positive for binary-consumed library crates. Tests NONE and Documentation UNDOCUMENTED: both confirmed.) |
| `setup_xdp` | L548–L577 |  PARTIAL | 80% | Private Linux-only function with one /// line: 'Set up AF_XDP: load BPF program, create XSK, register in XSKMAP.' Purpose is covered but the ifname parameter, returned tuple semantics (program must outlive socket to keep BPF attached), and io::Result error conditions are not documented. Acceptable for a private item but worth improving. (deliberated: confirmed — Tests NONE: Confirmed, no test file exists and this Linux-only function has zero coverage. Documentation PARTIAL: Confirmed — has one `///` summary line but no parameter/return/error documentation. Both findings are accurate and consistent.) |

### `rustguard-kmod/src/replay.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `ReplayWindow` | L11–L14 |  PARTIAL | 88% | pub(crate) struct has no /// doc comment at the type level. No description of the sliding-window semantics, invariants, or usage lifecycle is attached to the struct declaration itself. While two methods (check, update) carry brief /// comments, the struct type — which governs how top and bitmap jointly encode state — is entirely undocumented. Confidence reduced to 75 because pub(crate) visibility is crate-internal only, warranting some leniency. (deliberated: reclassified: documentation: UNDOCUMENTED → PARTIAL — CORRECTION ERROR confirmed at 93% confidence: Traced the carry loop manually. With `(0..BITMAP_LEN).rev()`, at iteration i, carry accumulated from word[i+1] is applied to word[i], propagating carry high→low. But a left shift within words produces overflow at bit 63 that must land in word[i+1] bit 0, requiring low→high propagation. The code comment even states 'Carry propagates from word[0] to word[BITMAP_LEN-1]' (correct direction) but then contradictorily iterates high-to-low. Concrete trace: bitmap[0]=0x8000..., bit_shift=1 → reverse loop drops the overflow bit entirely, forward loop correctly places it in bitmap[1] bit 0. Security-critical bug confirmed. UTILITY DEAD kept: 0 workspace importers per pre-computed analysis is factually correct, though the file header ('Ported from rustguard-core/src/replay.rs') suggests active development — the module may simply not be wired up yet. Confidence stays at 75 to reflect this ambiguity. TESTS NONE confirmed: no test file exists for this security-critical anti-replay module. DOCUMENTATION reclassified UNDOCUMENTED→PARTIAL: the module-level `//!` doc block explicitly describes 'Anti-replay sliding window (2048-bit bitmap)' and references RFC 6479, which directly covers this struct (the sole exported type). Both `check` and `update` methods carry `///` doc comments. Only the struct declaration itself lacks a `///` comment. For `pub(crate)` visibility, this is PARTIAL rather than fully UNDOCUMENTED.) |

### `rustguard-kmod/src/noise.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `zeroize` | L46–L50 |  PARTIAL | 90% | Has one `///` line describing purpose and the memzero_explicit guarantee. Missing parameter description for `buf` and no `# Examples` section; expected on a `pub(crate)` function. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: zeroize is called extensively throughout this file (L306, L308, L311, L313, L319, L321, L343, L363, L365, L367, L395–L400, L489, L491, L494, L525, L527). Workspace import analysis missed intra-file usage. Documentation UNDOCUMENTED→PARTIAL: line 47 has `/// Zeroize a mutable byte slice using memzero_explicit (cannot be optimized away).` which is a valid doc comment; only missing `buf` parameter description. Tests NONE confirmed: no test file exists.) |
| `constant_time_eq` | L53–L58 |  PARTIAL | 85% | Has one `///` line. Private function, so tolerance is higher, but parameters `a`/`b` and the length-mismatch early-return behavior are undescribed. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: 99% identical to cookie.rs version, genuine candidate for extraction into shared module. Tests WEAK→NONE: detail explicitly states 'No test file exists for this module' with zero coverage; WEAK implies some tests exist, but none do. Documentation PARTIAL confirmed: has one-line doc but omits parameter semantics.) |
| `hash` | L72–L94 |  PARTIAL | 80% | Has one `///` line: 'BLAKE2s-256 hash of concatenated chunks (max 8).' The 8-chunk limit is mentioned but the reason (C shim constraint) and the truncation behavior are not explained. Private function, so lower bar applies. (deliberated: confirmed — Correction NEEDS_FIX confirmed: silent truncation at 8 chunks is a latent correctness hazard. All current callers pass ≤2 chunks so no active bug, but a debug_assert or explicit error would prevent future misuse. Tests NONE confirmed. Documentation PARTIAL confirmed: mentions 8-chunk limit but not truncation behavior.) |
| `mac` | L97–L107 |  PARTIAL | 85% | Has one `///` line: 'Keyed BLAKE2s MAC (for MAC1/MAC2).' Mentions use-case context. Missing parameter and return-value descriptions. Private function. (deliberated: reclassified: tests: WEAK → NONE — Duplication DUPLICATE confirmed: identical FFI wrapper to cookie.rs mac. Tests WEAK→NONE: no test file exists, zero coverage confirmed by detail. Documentation PARTIAL confirmed.) |
| `hkdf` | L110–L121 |  PARTIAL | 85% | Has one `///` line: 'HKDF extract + expand.' Very terse; the three output tuple members and their semantics (T1, T2, T3) are not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: detail confirms 'No test file exists for this module' and 'zero test coverage.' WEAK is inaccurate when no tests exist at all. Documentation PARTIAL confirmed: very terse one-liner, tuple semantics undescribed.) |
| `seal` | L124–L138 |  PARTIAL | 85% | Has one `///` line describing algorithm and return semantics ('Returns ciphertext + 16-byte tag, or None on failure.'). Parameters `counter`, `ad`, and `dst` are not individually described. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: no bounds check that dst.len() >= plaintext.len() + AEAD_TAG_SIZE before passing dst.as_mut_ptr() to C FFI. If dst is undersized, C function writes out-of-bounds — a memory safety violation in a kernel module. All current callers size correctly but the function itself provides no guarantee. Tests NONE confirmed. Documentation PARTIAL confirmed.) |
| `open` | L141–L155 |  PARTIAL | 88% | Has one `///` line: 'AEAD decrypt. Returns plaintext length or None on auth failure.' Return semantics are documented; parameters are not. Private function. (deliberated: confirmed — Correction NEEDS_FIX confirmed: L151 `ciphertext.len() - AEAD_TAG_SIZE` performs unchecked usize subtraction. If ciphertext.len() < AEAD_TAG_SIZE, this panics in debug (kernel oops) or wraps in release. A guard `if ciphertext.len() < AEAD_TAG_SIZE { return None; }` is needed. Tests NONE confirmed. Documentation PARTIAL confirmed. Raising confidence as the bug is clearly visible in source.) |
| `dh` | L158–L162 |  PARTIAL | 85% | Has one `///` line: 'Curve25519 DH.' Extremely terse. Does not describe the None-on-zero-result behavior, which is security-critical. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero test coverage confirmed, no test file exists. Documentation PARTIAL confirmed: extremely terse, does not describe None-on-zero-result security behavior.) |
| `generate_keypair` | L165–L173 |  PARTIAL | 85% | Has one `///` line: 'Generate ephemeral keypair.' Does not document the return tuple order (secret, public) or the randomness source. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: no test file, zero coverage. Documentation PARTIAL confirmed: does not document return tuple order (secret, public) or randomness source.) |
| `encrypt_and_hash` | L196–L200 |  PARTIAL | 85% | Has one `///` line: 'Encrypt-and-hash: AEAD encrypt, mix ciphertext into hash.' Operation clearly named. Return tuple (new_h, ct_len) and failure semantics not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero coverage, no test file. Documentation PARTIAL confirmed: return tuple and failure semantics undescribed.) |
| `decrypt_and_hash` | L203–L207 |  PARTIAL | 85% | Has one `///` line: 'Decrypt-and-hash: AEAD decrypt, mix ciphertext into hash.' Symmetric with encrypt_and_hash. Return tuple and auth-failure propagation not described. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: zero coverage. Documentation PARTIAL confirmed: symmetric with encrypt_and_hash, same gaps.) |
| `compute_mac1` | L210–L216 |  PARTIAL | 85% | Has one `///` line: 'Compute MAC1 over a message.' Does not explain the key derivation step (BLAKE2s of LABEL_MAC1 ‖ responder_public) or why only the first 16 bytes are returned. Private function. (deliberated: reclassified: tests: WEAK → NONE — Tests WEAK→NONE: MAC1 computation is entirely untested. Documentation PARTIAL confirmed: key derivation step and 16-byte truncation undocumented.) |
| `tai64n_now` | L221–L235 |  PARTIAL | 80% | Has one `///` line: 'Generate a TAI64N timestamp from kernel wall clock.' Return format (12-byte big-endian secs+nanos) and the TAI64N encoding are not described. Private function. (deliberated: confirmed — Tests NONE confirmed: TAI64N encoding with epoch offset and big-endian byte packing has zero coverage. Documentation PARTIAL confirmed: return format undescribed.) |
| `INITIATION_SIZE` | L246–L246 |  UNDOCUMENTED | 90% | `pub(crate)` constant with no `///` doc comment. The size derivation (4+4+32+48+28+16+16 bytes) and its wire-format meaning are not explained. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD — Utility DEAD→USED: used as array size parameter at L288 (return type), L343 (msg array), and L437 (function parameter). Tests NONE→GOOD: compile-time constant, rule 6. Documentation UNDOCUMENTED confirmed: line 246 genuinely has no `///` doc comment; the wire layout derivation should be documented.) |
| `RESPONSE_SIZE` | L247–L247 |  UNDOCUMENTED | 90% | `pub(crate)` constant with no `///` doc comment. The response wire layout and size derivation (4+4+4+32+16+16+16 bytes) are not documented. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD — Utility DEAD→USED: used as array size at L431 (function parameter) and in process_initiation return type. Tests NONE→GOOD: compile-time constant, rule 6. Documentation UNDOCUMENTED confirmed: line 247 has no doc comment.) |
| `InitiatorState` | L268–L275 |  PARTIAL | 90% | Has `///` on the struct: 'State held between sending initiation and receiving response.' No field-level docs. The `pub(crate)` field `sender_index` is undocumented; private fields tolerate this but the opaque `ck`/`h`/`eph_secret` fields would benefit from brief comments. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: created by create_initiation (L337) and consumed by process_response (L349). Tests NONE→GOOD: plain data struct, rule 6. Documentation UNDOCUMENTED→PARTIAL: line 269 has `/// State held between sending initiation and receiving response.` which is a valid doc comment, but field-level docs are absent; PARTIAL is appropriate.) |
| `create_initiation` | L281–L344 |  PARTIAL | 90% | Has a multi-line `///` block describing purpose, return value, and the zero-result DH failure case. Missing individual parameter descriptions and no `# Examples` section, both expected on `pub(crate)` functions. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: primary initiator handshake API function. pub(crate) entry point for the module; workspace import analysis is unreliable for kernel module crates. Tests NONE confirmed: critical Noise_IKpsk2 path with zero coverage. Documentation UNDOCUMENTED→PARTIAL: lines 281–284 have multi-line `///` describing purpose, return value, and DH failure; missing parameter descriptions.) |
| `process_response` | L347–L422 |  PARTIAL | 90% | Has one `///` line: 'Process a handshake response (type 2) and derive transport keys.' Does not document parameters, MAC1 verification behavior, index mismatch failure, or the returned TransportKeys layout. No `# Examples`. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: core handshake completion API function. Workspace analysis missed cross-module usage in kernel crate. Tests NONE confirmed: MAC1 verification, index check, DH, PSK phase all untested. Documentation UNDOCUMENTED→PARTIAL: line 347 has `///` describing purpose; parameters, failure modes, and key derivation not documented.) |
| `process_initiation` | L431–L536 |  PARTIAL | 90% | Has a multi-line `///` block naming all four return tuple members and explicitly calling out the replay-protection requirement (H3). Missing individual parameter descriptions and no `# Examples` section. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD→USED: responder-side handshake API function. pub(crate) entry point. Tests NONE confirmed: most security-sensitive function in the project with zero coverage. Documentation UNDOCUMENTED→PARTIAL: lines 431–435 have multi-line `///` naming return tuple members and replay-protection caveat; missing parameter descriptions.) |

### `rustguard-core/src/handshake.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `mix_hash` | L32–L34 |  PARTIAL | 78% | Single /// line ('Mix a public key into the hash. Used to bind the responder's identity.'). Private function; the `data` parameter is called 'public key' in the comment but is typed as a generic byte slice, creating a minor mismatch. No return value description. (deliberated: confirmed — Tests WEAK: only indirect coverage, no isolated test — fair for a crypto helper. Documentation PARTIAL: doc says 'Mix a public key' but the function is used for ciphertext, timestamps, and other data too (called 8 times with heterogeneous inputs). The misleading comment is a legitimate doc quality issue.) |
| `mix_key` | L38–L41 |  PARTIAL | 88% | Two /// lines covering purpose and return tuple ('Returns (new_ck, key) where key is for AEAD encryption'). Private function with meaningful coverage; the dh_result parameter is not described and no # Examples are present, though examples are not expected for private helpers. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: the kmod counterpart lives in a kernel module crate with fundamentally different compilation targets, type systems (SharedSecret vs raw [u8;32]), allocators, and build constraints. Deduplicating a 3-line function across a userspace library and a Linux kernel module would require a shared no_std+no_alloc crate abstracting over type differences — this adds coupling and complexity far exceeding the cost of maintaining a trivial function in both places. This is intentional architectural separation, not accidental duplication. Tests WEAK and docs PARTIAL are correct as stated.) |
| `encrypt_and_hash` | L44–L48 |  PARTIAL | 80% | Single /// line ('AEAD encrypt, then mix ciphertext into hash'). Private function; operation order is clear from the comment but parameters key, h, and plaintext are not described individually, and the return tuple (new_h, ciphertext) is not documented. (deliberated: confirmed — Tests WEAK: no direct test, no known-plaintext vectors, only integration coverage — correct assessment for a security-critical AEAD wrapper. Documentation PARTIAL: single /// line covers the operation but omits return tuple semantics and parameter descriptions. Both findings are accurate.) |
| `decrypt_and_hash` | L51–L55 |  PARTIAL | 80% | Single /// line ('AEAD decrypt, then mix ciphertext into hash'). Private function; the Option return and its failure condition (authentication failure) are not documented, which is a meaningful omission for a security-critical function. (deliberated: confirmed — Tests WEAK: authentication failure path is covered by tamper tests at message granularity but never isolated. Documentation PARTIAL: the Option return (None on auth failure) is a meaningful omission for a security function. Both findings stand.) |
| `compute_mac1` | L59–L65 |  PARTIAL | 88% | Public function with two /// lines covering purpose and the BLAKE2s construction formula. No parameter descriptions for responder_public or msg_bytes, return value not described, and no # Examples section — all required for a public API function. (deliberated: reclassified: duplication: DUPLICATE → UNIQUE — Duplication reclassified DUPLICATE → UNIQUE: same rationale as mix_key — the kmod version uses raw [u8;32] and lives in a kernel module with entirely different build/runtime constraints. A 6-line protocol-mandated function does not warrant cross-crate abstraction between kernel and userspace. Tests WEAK: no direct test of output against reference vector or truncation behavior. Documentation PARTIAL: public function missing parameter and return descriptions. Both are legitimate.) |
| `InitiatorHandshake` | L72–L81 |  PARTIAL | 78% | Public struct with two /// lines describing its lifecycle role and the ZeroizeOnDrop security rationale. All fields are private and undocumented. Missing a # Examples section showing the initiator handshake lifecycle, which is expected for a public exported type. (deliberated: confirmed — Tests WEAK: the ZeroizeOnDrop property is security-critical but testing it would essentially be testing the zeroize crate's derive macro — borderline but fair to flag. Documentation PARTIAL: public struct with only lifecycle description; private fields being undocumented is normal Rust but the struct-level docs could mention the security contract more explicitly.) |
| `create_initiation` | L85–L91 |  PARTIAL | 80% | Public function with one /// line noting it is an std convenience wrapper. No parameter descriptions for our_static, their_public, or sender_index; return tuple (Initiation, InitiatorHandshake) is undescribed; no # Examples section for this primary public entry point. (deliberated: confirmed — Tests WEAK: thin wrapper exercised in multiple tests but never with non-default PSK; the wrapper's contract (zero PSK default) is implicitly tested. Documentation PARTIAL: public entry point with no parameter or return descriptions. Both findings stand.) |
| `create_initiation_psk` | L95–L104 |  PARTIAL | 83% | Public function with one brief /// line ('Create a handshake initiation with PSK (std convenience)'). The psk parameter semantics (e.g., all-zeros meaning no PSK) are not documented; no parameter or return value descriptions; no # Examples section. (deliberated: confirmed — Tests WEAK: the PSK code path is never exercised with a non-zero PSK, which is a genuine security-critical gap. Documentation PARTIAL: psk semantics (zero = no PSK) undocumented. Both correct.) |
| `create_initiation_with` | L108–L170 |  PARTIAL | 80% | Public core function with two /// lines noting it accepts explicit ephemeral and timestamp and is no_std compatible. No parameter descriptions for any of the six parameters, no return value documentation, and no # Examples section for this most general public variant. (deliberated: confirmed — Tests WEAK: core deterministic function never called directly in tests — a missed opportunity for test-vector validation. Documentation PARTIAL: most general public variant with six parameters, none documented. Both findings are accurate and important.) |
| `process_response` | L173–L221 |  PARTIAL | 80% | Public function with a single /// line ('Process a handshake response and derive transport keys'). Failure conditions returning None (receiver_index mismatch, AEAD decryption failure) are not documented; no parameter or return value descriptions; no # Examples. (deliberated: confirmed — Correction NEEDS_FIX confirmed: the WireGuard spec mandates MAC1 verification on all received messages. process_initiation_with correctly checks MAC1 before DH (L258-261), but process_response skips it entirely. While receiver_index provides a cheap first guard, an attacker who observes or guesses the sender_index can force two DH scalar multiplications per forged packet. The asymmetry is a protocol non-compliance. Tests WEAK: PSK always zero, no anti-replay testing. Documentation PARTIAL: failure conditions undocumented. All three findings stand.) |
| `process_initiation` | L227–L233 |  PARTIAL | 82% | Public function with one /// line describing it as the std convenience responder path. The four-element return tuple (PublicKey, Tai64n, Response, TransportSession) is entirely undescribed; no failure condition documentation; no # Examples. (deliberated: confirmed — Tests WEAK: always delegates with zero PSK and None timestamp — replay prevention is never exercised through this wrapper. Documentation PARTIAL: four-element return tuple entirely undescribed. Both correct.) |
| `process_initiation_psk` | L237–L246 |  PARTIAL | 83% | Public function with one /// line. The security-critical last_timestamp replay-prevention parameter is not documented; psk semantics not described; no # Examples section despite being a primary responder API. (deliberated: confirmed — Tests WEAK: the two security-differentiating parameters (PSK and last_timestamp) are never exercised with non-trivial values — a significant gap. Documentation PARTIAL: last_timestamp replay-prevention semantics undocumented. Both correct.) |
| `process_initiation_with` | L249–L357 |  PARTIAL | 80% | Public no_std-compatible function with a single /// line. The most complex function in the file: MAC1 verification, multiple DH operations, replay detection, and PSK phase are all undescribed in the doc comment. Multiple None return paths and the four-element return tuple are not documented. No # Examples. (deliberated: confirmed — Tests WEAK: last_timestamp replay check never exercised (always None), non-zero PSK never tested — both are security-critical paths with zero coverage. Documentation PARTIAL: most complex function in file (108 lines) with a single /// line, multiple failure modes undocumented. Both findings are significant and correct.) |
| `constant_time_eq` | L360–L363 |  PARTIAL | 78% | Private function with one /// line naming the subtle crate. The security rationale (prevents timing side-channel attacks on MAC comparison) is implied but not stated. Acceptable for a private two-line helper but the security significance warrants more explicit documentation. (deliberated: confirmed — Tests WEAK: exercised indirectly via MAC1 mismatch path but no direct unit test. Documentation PARTIAL: security rationale (timing side-channel prevention) is implied but not explicitly stated. For a security-critical helper, this is a fair assessment on both axes.) |

### `rustguard-core/src/cookie.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `COOKIE_SECRET_LIFETIME` | L21–L21 |  PARTIAL | 75% | Has a single-line `///` comment ('How often the cookie secret rotates.') which is adequate for a private constant, but omits the unit (seconds/minutes) explicitly in the doc text, relying on the reader to inspect the value. (deliberated: confirmed — Documentation PARTIAL is accurate: `/// How often the cookie secret rotates.` exists but omits the unit (seconds). Minor doc improvement needed.) |
| `COOKIE_LEN` | L24–L24 |  PARTIAL | 92% | Public constant has `/// Cookie size.` — present but trivially terse. Does not state the unit (bytes), no context on how it relates to the MAC output, and no `# Examples` section expected for a pub constant used in public API. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → GOOD, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: this is a `pub const` in a library crate (`rustguard-core`); 0 workspace importers is expected — downstream consumers use it. The '0 external importers' metric only checks within the workspace, not downstream dependents. Tests reclassified NONE→GOOD: pure constant with no runtime behavior; the detail itself states 'GOOD per constant/type default rule', contradicting the NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 23 clearly has `/// Cookie size.` — it exists but is terse (no unit/context).) |
| `CookieChecker` | L58–L67 |  PARTIAL | 92% | Public struct has a clear one-line `///` description and each field (including the sole pub field `under_load`) has a `///` comment. Missing: no `# Examples` demonstrating construction and usage, and the struct doc does not mention the expected usage pattern or lifecycle. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieChecker is the primary public API struct for server-side cookie checking in a library crate. '0 external importers' only reflects workspace analysis, not downstream consumers. Tests reclassified NONE→WEAK: the test module uses CookieChecker in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), mac2_wrong_source_fails(). The detail itself says 'WEAK' with notes on missing verify_mac1 and secret rotation coverage, contradicting the NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 57 has `/// Server-side cookie state: generates cookies and validates MAC2.` and each field has `///` comments. Missing examples and lifecycle docs make it PARTIAL, not UNDOCUMENTED.) |
| `CookieState` | L70–L75 |  PARTIAL | 92% | Public struct has a one-line `///` description and private fields are individually documented. Missing: no `# Examples` section and no mention of how to obtain and use a CookieState (e.g., pair with CookieChecker). Public API lacks usage guidance. (deliberated: reclassified: utility: DEAD → USED, tests: NONE → WEAK, documentation: UNDOCUMENTED → PARTIAL — Utility reclassified DEAD→USED: CookieState is the primary public API struct for client-side cookie storage in a library crate. Tests reclassified NONE→WEAK: used in setup(), cookie_roundtrip(), wrong_mac1_fails_decrypt(), mac2_verified_after_cookie(), no_cookie_gives_zero_mac2(). Detail says 'WEAK' with notes on missing staleness expiry test, contradicting NONE field. Documentation reclassified UNDOCUMENTED→PARTIAL: source line 69 has `/// Client-side cookie state: stores a received cookie for MAC2.` with field docs. Terse but present.) |
| `encode_addr` | L236–L252 |  PARTIAL | 70% | Private std-gated function has `/// Encode a socket address to bytes for cookie MAC.` — a minimal description. No parameter documentation, no mention of the encoding format (though format is described on `make_cookie_from_bytes`). Acceptable for a private helper but incomplete. (deliberated: confirmed — Tests WEAK is accurate: only IPv4 addresses tested in the suite; IPv6 branch at L247 is never exercised. Documentation PARTIAL is accurate: has a minimal `///` comment but no parameter docs or encoding format description.) |

### `rustguard-daemon/src/tunnel.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `TunnelState` | L30–L35 |  PARTIAL | 80% | Struct has a `///` doc comment explaining its role. `pending_handshakes` field is documented. `our_static` and `peers` fields have no `///` comments. Private struct, so leniency applies, but incomplete field coverage warrants PARTIAL. (deliberated: confirmed — Documentation PARTIAL is correct: struct-level doc exists but two of three fields lack `///` comments. Private struct leniency applies; no reclassification needed.) |
| `RouteEntry` | L41–L45 |  PARTIAL | 80% | Struct has a `///` doc comment describing its purpose. None of the three fields (`route`, `interface`, `is_v6`) carry `///` comments. Private struct; leniency applies, but partial field documentation keeps this PARTIAL. (deliberated: confirmed — Documentation PARTIAL is correct: struct-level doc exists but none of the three fields carry `///` comments. Private struct with leniency, but partial field coverage is accurately flagged.) |
| `run` | L48–L489 |  PARTIAL | 90% | Public API function with only `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` — no `# Examples` section, no `# Errors` section, and the `config` parameter is undescribed. For the sole public entry point of the daemon, this is materially incomplete. (deliberated: reclassified: utility: DEAD → USED, documentation: UNDOCUMENTED → PARTIAL — Utility DEAD is a false positive: `run` is the sole public entry point of the rustguard-daemon tunnel module, called from main.rs. Binary-internal module calls are not cross-crate imports, which explains the 0-importer result. Reclassified to USED (confidence 92). Correction NEEDS_FIX retained: (1) MSG_RESPONSE peer lookup at ~L288 uses a broad `p.endpoint.is_some() && !p.has_active_session()` fallback that matches the wrong peer in multi-peer configs — real bug. (2) Windows TimedOut vs WouldBlock is a genuine cross-platform issue. Tests NONE is correct — no test file exists for this 440-line orchestrator. Documentation reclassified UNDOCUMENTED → PARTIAL: L48 clearly shows `/// Start the tunnel. Blocks until SIGINT/SIGTERM or fatal error.` which is a real doc comment, just incomplete (no #Errors, no parameter docs).) |
| `ctrlc_handler` | L504–L525 |  PARTIAL | 80% | Private function with `/// Install a Ctrl-C / SIGTERM handler.` The closure parameter `f` is undescribed, and the unusual sigwait-based implementation strategy is unexplained. PARTIAL for a non-trivial private function. (deliberated: confirmed — Correction NEEDS_FIX retained: the signal race condition is valid — sigprocmask(SIG_BLOCK) runs only inside the spawned thread at L519, so between the signal() calls at L508-509 and the thread's sigprocmask, SIGINT/SIGTERM can be consumed by signal_noop in any thread (main, outbound, inbound, timer), preventing sigwait from ever firing. The correct POSIX pattern blocks signals in the main thread before spawning. Overengineering OVER retained: ~20 lines of unsafe libc with no SAFETY comments reimplements what the well-maintained ctrlc crate provides in 2-3 lines; for a security-sensitive daemon, minimizing unsafe surface area is arguably more important than minimizing dependencies. Tests NONE correct for complex signal handling with no coverage. Documentation PARTIAL correct — has one-line doc but parameter `f` and the sigwait strategy are unexplained.) |

### `rustguard-core/src/messages.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `MSG_INITIATION` | L5–L5 |  UNDOCUMENTED | 82% | The group `///` comment at line 3 is separated from this constant by a blank line, so it does not attach to MSG_INITIATION in Rust's doc-comment model. No individual `///` is present on this constant. (deliberated: confirmed — Tests WEAK is correct: initiation_roundtrip asserts bytes[0..4] == MSG_INITIATION.to_le_bytes(), which exercises the constant but only verifies self-consistency, never the protocol-mandated value 1 directly. Documentation UNDOCUMENTED is technically correct — the blank line at line 4 breaks Rust doc-comment attachment from the line 3 group comment, so rustdoc won't associate it. However, the constant's name and value are self-documenting in protocol context, making the practical impact low.) |
| `MSG_RESPONSE` | L6–L6 |  UNDOCUMENTED | 82% | No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: confirmed — Tests WEAK: same self-consistency pattern as MSG_INITIATION — response_roundtrip asserts type bytes match MSG_RESPONSE.to_le_bytes() but never asserts the value 2. Documentation UNDOCUMENTED: same blank-line attachment issue as MSG_INITIATION. Both findings are accurate.) |
| `MSG_COOKIE_REPLY` | L7–L7 |  UNDOCUMENTED | 90% | No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: confirmed — Tests NONE is correct: no CookieReply test exists anywhere in the test module, so this constant is never exercised in tests at all. Documentation UNDOCUMENTED: same pattern as other MSG_* constants, no attached doc comment.) |
| `MSG_TRANSPORT` | L8–L8 |  UNDOCUMENTED | 87% | No `///` doc comment directly precedes this constant. The orphaned group comment at line 3 does not attach to this item. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: MSG_TRANSPORT IS exercised in transport_roundtrip via Transport::to_bytes() which writes MSG_TRANSPORT.to_le_bytes() into the buffer. The constant participates in the test code path — it's just never directly asserted against in assertions. This is the same pattern as MSG_INITIATION/MSG_RESPONSE (exercised but not independently validated), which were rated WEAK. NONE was inconsistent. Documentation UNDOCUMENTED stands — no attached doc comment.) |
| `Initiation` | L22–L29 |  PARTIAL | 85% | Type-level `///` comment (lines 11–21) provides a thorough wire-format diagram covering all fields. However, individual public fields carry no `///` doc comments, and there is no `# Examples` section as expected for a public API struct. (deliberated: confirmed — Tests WEAK: initiation_roundtrip verifies sender_index and ephemeral after roundtrip but does not assert encrypted_static, encrypted_timestamp, mac1, or mac2 — leaving 4 of 6 fields unchecked. While the serialization pattern is identical for all fields, byte range errors in unchecked fields would go undetected. Documentation PARTIAL: the struct has an excellent wire-format diagram at the type level, but individual public fields lack doc comments. Both findings are accurate and proportionate.) |
| `INITIATION_SIZE` | L31–L31 |  UNDOCUMENTED | 78% | No `///` doc comment present. The value 148 is inferable from the struct's wire-format diagram, but the constant itself has no attached documentation. (deliberated: confirmed — Tests WEAK: INITIATION_SIZE is implicitly exercised as the array size in to_bytes/from_bytes called during initiation_roundtrip, but the value 148 is never directly asserted. If the value were wrong, compilation would fail with mismatched array sizes rather than a test failure, so this is an inherent compile-time check. Still, WEAK is fair since no explicit test validates the constant. Documentation UNDOCUMENTED: no doc comment attached to this constant.) |
| `Response` | L45–L52 |  PARTIAL | 85% | Type-level `///` comment (lines 33–44) provides a detailed wire-format diagram. Individual public fields lack `///` doc comments and there is no `# Examples` section. (deliberated: confirmed — Tests WEAK: response_roundtrip verifies sender_index and receiver_index but not ephemeral, encrypted_empty, mac1, or mac2 — leaving 4 of 6 fields unchecked after deserialization. Documentation PARTIAL: excellent struct-level wire-format diagram but no field-level doc comments. Both findings match the evidence in the test module.) |
| `RESPONSE_SIZE` | L54–L54 |  UNDOCUMENTED | 78% | No `///` doc comment present. The constant has no attached documentation. (deliberated: confirmed — Tests WEAK: implicitly exercised as array size in Response::to_bytes/from_bytes but never directly asserted. Same pattern as INITIATION_SIZE — compile-time array sizing provides some implicit validation. Documentation UNDOCUMENTED: no attached doc comment.) |
| `Transport` | L64–L68 |  PARTIAL | 85% | Type-level `///` comment (lines 56–63) provides a wire-format diagram. Public fields (`receiver_index`, `counter`, `payload`) have no individual `///` comments, and no `# Examples` section is provided. (deliberated: confirmed — Tests WEAK: transport_roundtrip checks receiver_index, counter, and payload.len() but never verifies the type tag bytes, never checks actual payload content (only length), and never tests the from_bytes None path (buffer shorter than TRANSPORT_HEADER_SIZE). Documentation PARTIAL: has struct-level wire-format diagram, but public fields lack individual doc comments.) |
| `CookieReply` | L79–L83 |  PARTIAL | 88% | Type-level `///` comment (lines 70–78) provides a thorough wire-format diagram. Public fields lack individual `///` doc comments and there is no `# Examples` section. (deliberated: confirmed — Tests NONE is correct: no test for CookieReply exists in the test module despite it being imported by four files. Neither to_bytes nor from_bytes is exercised. This is the most significant test gap in the file. Documentation PARTIAL: excellent wire-format diagram at struct level but no field-level docs.) |
| `COOKIE_REPLY_SIZE` | L85–L85 |  UNDOCUMENTED | 90% | No `///` doc comment present. The constant has no attached documentation. (deliberated: confirmed — Tests NONE: since no CookieReply test exists, this constant is never exercised in any test. Documentation UNDOCUMENTED: no attached doc comment.) |
| `TRANSPORT_HEADER_SIZE` | L87–L87 |  UNDOCUMENTED | 87% | No `///` doc comment present. The meaning (16-byte transport header) is inferable from context but no doc comment is attached to this constant. (deliberated: reclassified: tests: NONE → WEAK — Tests reclassified NONE → WEAK: TRANSPORT_HEADER_SIZE is exercised in transport_roundtrip via Transport::to_bytes() (capacity hint) and Transport::from_bytes() (length guard happy path). The constant participates in the test code path. However, the None branch (short buffer) is never tested, and the constant's value of 16 is never directly asserted. This is WEAK coverage, not NONE. Documentation UNDOCUMENTED stands.) |

### `rustguard-core/src/timers.rs`

| Symbol | Lines | Documentation | Conf. | Detail |
|--------|-------|---------------|-------|--------|
| `REKEY_TIMEOUT` | L39–L39 |  PARTIAL | 88% | A `///` doc comment is present, but the description 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' does not match the 5-second value — REJECT_AFTER_TIME is 180 s. Actual usage in `should_retry_handshake` shows this is a handshake-retry interval, not a send-rejection threshold. The description appears to be a copy-paste error, making it actively misleading. (deliberated: confirmed — Correction NEEDS_FIX confirmed with increased confidence: the doc comment on L38 states 'Don't try to send with a keypair older than this (REJECT_AFTER_TIME + padding)' but the constant is 5 seconds, used in should_retry_handshake as a retry interval. REJECT_AFTER_TIME is 180s — the description is clearly wrong and actively misleading for a security-critical protocol constant. The numeric value (5s) is correct per WireGuard spec. Documentation PARTIAL confirmed: a doc comment exists but its content is factually incorrect, making it worse than undocumented. Both findings are coherent — the PARTIAL documentation and NEEDS_FIX correction describe the same underlying issue (wrong doc comment).) |

## 🧹 Hygiene

- [ ] <!-- ACT-3c9a32-1 --> **[documentation · medium · trivial]** `rustguard-core/src/messages.rs`: Add `///` doc comment documentation for exported symbol:  `MSG_INITIATION`, `MSG_RESPONSE`, `MSG_COOKIE_REPLY`, `MSG_TRANSPORT`, `INITIATION_SIZE`, `RESPONSE_SIZE`, `COOKIE_REPLY_SIZE`, `TRANSPORT_HEADER_SIZE` (`MSG_INITIATION, MSG_RESPONSE, MSG_COOKIE_REPLY, MSG_TRANSPORT, INITIATION_SIZE, RESPONSE_SIZE, COOKIE_REPLY_SIZE, TRANSPORT_HEADER_SIZE`) [L5-L5, L6-L6, L7-L7, L8-L8, L31-L31, L54-L54, L85-L85, L87-L87]
- [ ] <!-- ACT-bd20cf-6 --> **[documentation · medium · trivial]** `rustguard-daemon/src/tunnel.rs`: Add `///` doc comment documentation for exported symbol: `run` (`run`) [L48-L489]
- [ ] <!-- ACT-a732f2-4 --> **[documentation · medium · trivial]** `rustguard-enroll/src/server.rs`: Add `///` doc comment documentation for exported symbol: `ServeConfig` (`ServeConfig`) [L49-L62]
- [ ] <!-- ACT-a732f2-5 --> **[documentation · medium · trivial]** `rustguard-enroll/src/server.rs`: Add `///` doc comment documentation for exported symbol: `run` (`run`) [L64-L532]
- [ ] <!-- ACT-c30836-3 --> **[documentation · medium · trivial]** `rustguard-kmod/src/replay.rs`: Add `///` doc comment documentation for exported symbol: `ReplayWindow` (`ReplayWindow`) [L11-L14]
- [ ] <!-- ACT-8fc69f-5 --> **[documentation · medium · trivial]** `rustguard-tun/src/xdp.rs`: Add `///` doc comment documentation for exported symbol: `XdpDesc` (`XdpDesc`) [L62-L66]
- [ ] <!-- ACT-8fc69f-6 --> **[documentation · medium · trivial]** `rustguard-tun/src/xdp.rs`: Add `///` doc comment documentation for exported symbol: `if_nametoindex` (`if_nametoindex`) [L449-L458]
- [ ] <!-- ACT-a4f709-4 --> **[documentation · low · trivial]** `rustguard-core/src/cookie.rs`: Complete `///` doc comment documentation for: `encode_addr` (`encode_addr`) [L236-L252]
- [ ] <!-- ACT-5920d4-2 --> **[documentation · low · trivial]** `rustguard-core/src/handshake.rs`: Complete `///` doc comment documentation for:  `initial_chain_key`, `mix_hash`, `mix_key`, `encrypt_and_hash`, `decrypt_and_hash`, `compute_mac1`, `InitiatorHandshake`, `create_initiation`, `create_initiation_psk`, `create_initiation_with`, `process_response`, `process_initiation`, `process_initiation_psk`, `process_initiation_with`, `constant_time_eq` (`initial_chain_key, mix_hash, mix_key, encrypt_and_hash, decrypt_and_hash, compute_mac1, InitiatorHandshake, create_initiation, create_initiation_psk, create_initiation_with, process_response, process_initiation, process_initiation_psk, process_initiation_with, constant_time_eq`) [L23-L25, L32-L34, L38-L41, L44-L48, L51-L55, L59-L65, L72-L81, L85-L91, L95-L104, L108-L170, L173-L221, L227-L233, L237-L246, L249-L357, L360-L363]
- [ ] <!-- ACT-3c9a32-2 --> **[documentation · low · trivial]** `rustguard-core/src/messages.rs`: Complete `///` doc comment documentation for:  `Initiation`, `Response`, `Transport`, `CookieReply` (`Initiation, Response, Transport, CookieReply`) [L22-L29, L45-L52, L64-L68, L79-L83]
- [ ] <!-- ACT-fb2f2d-8 --> **[documentation · low · trivial]** `rustguard-core/src/timers.rs`: Complete `///` doc comment documentation for: `REKEY_TIMEOUT` (`REKEY_TIMEOUT`) [L39-L39]
- [ ] <!-- ACT-bd20cf-4 --> **[documentation · low · trivial]** `rustguard-daemon/src/tunnel.rs`: Complete `///` doc comment documentation for:  `TunnelState`, `RouteEntry`, `ctrlc_handler` (`TunnelState, RouteEntry, ctrlc_handler`) [L30-L35, L41-L45, L504-L525]
- [ ] <!-- ACT-a732f2-9 --> **[documentation · low · trivial]** `rustguard-enroll/src/server.rs`: Complete `///` doc comment documentation for: `setup_xdp` (`setup_xdp`) [L548-L577]
- [ ] <!-- ACT-bb41a6-8 --> **[documentation · low · trivial]** `rustguard-kmod/src/lib.rs`: Complete `///` doc comment documentation for:  `AEAD_TAG_SIZE`, `rustguard_xmit`, `rustguard_rx`, `handle_initiation`, `handle_response`, `handle_transport`, `handle_cookie_reply`, `rustguard_dev_uninit`, `rustguard_timer_tick`, `initiate_rekey`, `send_keepalive`, `rustguard_genl_get`, `rustguard_genl_set` (`AEAD_TAG_SIZE, rustguard_xmit, rustguard_rx, handle_initiation, handle_response, handle_transport, handle_cookie_reply, rustguard_dev_uninit, rustguard_timer_tick, initiate_rekey, send_keepalive, rustguard_genl_get, rustguard_genl_set`) [L111-L111, L382-L384, L465-L467, L521-L620, L624-L671, L676-L755, L758-L781, L785-L785, L789-L791, L847-L877, L880-L914, L918-L922, L926-L932]
- [ ] <!-- ACT-58e5ff-7 --> **[documentation · low · trivial]** `rustguard-kmod/src/noise.rs`: Complete `///` doc comment documentation for:  `constant_time_eq`, `hash`, `mac`, `hkdf`, `seal`, `open`, `dh`, `generate_keypair`, `encrypt_and_hash`, `decrypt_and_hash`, `compute_mac1`, `tai64n_now` (`constant_time_eq, hash, mac, hkdf, seal, open, dh, generate_keypair, encrypt_and_hash, decrypt_and_hash, compute_mac1, tai64n_now`) [L53-L58, L72-L94, L97-L107, L110-L121, L124-L138, L141-L155, L158-L162, L165-L173, L196-L200, L203-L207, L210-L216, L221-L235]
- [ ] <!-- ACT-8fc69f-7 --> **[documentation · low · trivial]** `rustguard-tun/src/xdp.rs`: Complete `///` doc comment documentation for:  `Ring`, `XdpConfig`, `XdpSocket` (`Ring, XdpConfig, XdpSocket`) [L89-L95, L126-L132, L148-L160]

## Documentation Coverage

### `rustguard-kmod/src/noise.rs` — 62% covered

- [ ] **create_initiation** — PARTIAL → `rustguard-kmod/src/noise.rs`: Return value and DH-zero failure documented; parameters and examples absent.
- [ ] **process_response** — PARTIAL → `rustguard-kmod/src/noise.rs`: Only a single-sentence description; MAC1 verification, index check, and key derivation outcomes are not described.
- [ ] **process_initiation** — PARTIAL → `rustguard-kmod/src/noise.rs`: Return tuple named and replay-protection caveat noted; parameters undocumented and no examples.
- [ ] **Wire format constants (MSG_*, INITIATION_SIZE, RESPONSE_SIZE)** — PARTIAL → `rustguard-kmod/src/noise.rs`: MSG_INITIATION/MSG_RESPONSE/MSG_TRANSPORT are documented with type and size; INITIATION_SIZE and RESPONSE_SIZE have no doc comments.
- [ ] **Crypto helpers (hash, mac, hkdf, seal, open, dh)** — PARTIAL → `rustguard-kmod/src/noise.rs`: All have brief one-line `///` comments; parameter semantics and failure modes are not spelled out.
- [ ] **Noise state helpers (mix_hash, mix_key, initial_hash)** — MISSING → `rustguard-kmod/src/noise.rs`: mix_hash, mix_key, and initial_hash have no `///` doc comments at all.
