<p align="center">
  <img src="https://raw.githubusercontent.com/r-via/anatoly/main/assets/imgs/logo.jpg" width="400" alt="Anatoly" />
</p>

# Anatoly Audit Report

## Executive Summary

- **Files reviewed:** 130
- **Global verdict:** NEEDS_REFACTOR
- **Clean files:** 39
- **Files with findings:** 91

| Category | High | Medium | Low | Total |
|----------|------|--------|-----|-------|
| Documentation gaps | 0 | 0 | 255 | 255 |

## Axes

| Axis | Files | Shards | Link |
|------|-------|--------|------|
| Documentation | 91 | 10 | [axes/documentation/index.md](./axes/documentation/index.md) |

## Axis Summary

**Documentation** — 1143 symbols evaluated

| Verdict | Count | % |
|---------|-------|---|
| DOCUMENTED | 856 | 75% |
| PARTIAL | 187 | 16% |
| UNDOCUMENTED | 100 | 9% |

## Deliberation

| Metric | Value |
|--------|-------|
| Files deliberated | 94 |
| Symbols reclassified | 87 |
| Actions removed | 59 |
| Verdict changes | 8 |

**Verdict changes:**

- `scripts/lib/logging.sh`: NEEDS_REFACTOR → CLEAN
- `scripts/setup-embeddings.sh`: NEEDS_REFACTOR → CLEAN
- `src/core/axes/utility.ts`: NEEDS_REFACTOR → CLEAN
- `src/core/doc-scoring.ts`: NEEDS_REFACTOR → CLEAN
- `src/core/file-evaluator.ts`: CLEAN → NEEDS_REFACTOR
- `src/core/prompt-resolver.ts`: CLEAN → NEEDS_REFACTOR
- `src/prompts/__gold-set__/generated-protobuf.ts`: NEEDS_REFACTOR → CLEAN
- `src/prompts/__gold-set__/terrible-1.ts`: NEEDS_REFACTOR → CLEAN

**Reclassified files:**

- `scripts/lib/hardware.sh`: 2 symbol(s) — All findings are documentation-only—no bugs, dead code, duplication, or overengineering issues. The code is well-structured and functionally correct. Reclassified detect_gpu and has_docker from UNDOCUMENTED to PARTIAL: detect_gpu has a section banner and descriptive name that partially conveys intent; has_docker follows the conventional shell 'has_' idiom which inherently communicates exit-code semantics. Removed action 4 (has_docker) since it now has PARTIAL documentation. Retained UNDOCUMENTED for ensure_jq, ensure_curl, and has_nvidia_container_toolkit where documentation gaps are most impactful—especially ensure_jq's sudo side effects. All actions incorrectly reference 'JSDoc' for a shell script; shell comment blocks are the appropriate format. Verdict remains NEEDS_REFACTOR due to legitimate documentation gaps on functions with side effects and complex decision logic, though severity is low overall.
- `scripts/lib/logging.sh`: 2 symbol(s) — All 5 actions prescribe 'JSDoc documentation' for a bash shell script — JSDoc is a JavaScript-specific documentation format and cannot be applied to .sh files. Shell scripts use comment blocks (# style), which this file already employs via section headers and a comprehensive usage block in the file header (lines 1-11). The actions are invalid due to wrong documentation format and are removed entirely. All findings are documentation-only (PARTIAL), with no corrections, dead code, duplication, or overengineering issues. The file is a well-structured 94-line bash utility with clear naming conventions, section separators, and a complete usage example block. For shell scripting standards, the documentation level is adequate. Verdict upgraded to CLEAN.
- `scripts/setup-embeddings.sh`: 10 symbol(s) — This is a well-structured shell script with clear section banners, self-descriptive function and variable names, and logical flow. All findings are documentation-only — no correctness, utility, duplication, or overengineering issues exist. The original evaluation applied TypeScript/JSDoc documentation standards to a bash script, leading to inflated findings. Shell conventions rely on self-descriptive naming, section banners, and contextual clarity rather than formal doc-blocks. I reclassified 7 UNDOCUMENTED findings to DOCUMENTED where the shell idioms (function-name-as-documentation, self-documenting boolean patterns, section banners, standard idioms like $$) provide adequate clarity. Three symbols remain PARTIAL where magic numbers or non-obvious patterns genuinely warrant brief inline comments. All JSDoc-referencing actions for reclassified symbols are removed. Verdict downgraded from NEEDS_REFACTOR to CLEAN since the remaining PARTIAL items are minor documentation preferences (low severity, trivial effort) that are normal for shell scripts and don't constitute a refactoring need.
- `src/cli/screen-renderer.ts`: 4 symbol(s) — Overall the file is clean. The four private constants (SPINNER, FLASH_DURATION_MS, TASK_WIDTH, SEPARATOR) were flagged UNDOCUMENTED at low confidence (60-65), but all are self-descriptive private symbols that do not require JSDoc — reclassified to DOCUMENTED. The sole substantive finding is PARTIAL documentation on the exported ScreenRenderer class, which is legitimate but low-severity. The single action (complete JSDoc for ScreenRenderer) is appropriate. Verdict remains CLEAN.
- `src/commands/clean-run.ts`: 2 symbol(s) — All findings in this file are documentation-only (UNDOCUMENTED). The code has no correctness, utility, duplication, or overengineering issues — it is well-structured and functionally sound. I reclassified two private symbols (SHA_RE, finalSync) from UNDOCUMENTED to DOCUMENTED because they are short, self-evident private helpers where the name and implementation leave no ambiguity. All exported symbols remain UNDOCUMENTED: the exported git helpers (getGitSha, hasGitChanges, hasUncommittedChanges, rollbackToSha) have non-obvious error-handling contracts (silent false returns), the circuit breaker constants lack rationale, and the main registerCleanRunCommand function's complex protocol deserves a high-level JSDoc summary. No actions are removed since all 8 actions target exported symbols that remain UNDOCUMENTED. Verdict stays NEEDS_REFACTOR — the documentation gaps are real but the code itself is correct and well-organized.
- `src/commands/clean-sync.ts`: 3 symbol(s) — All findings in this file are documentation-only — no correctness, utility, duplication, or overengineering issues were identified, confirming the code logic is sound. Three PARTIAL documentation findings (checkAction, allActionsChecked, checkShardInIndex) were reclassified to DOCUMENTED: these are short, focused utility functions with descriptive names, good prose JSDoc, and explicit TypeScript signatures. Requiring formal @param tags on trivial functions where the prose already covers all semantics is overly strict. The syncAxis PARTIAL and registerCleanSyncCommand UNDOCUMENTED findings are upheld — syncAxis has non-trivial return semantics that warrant explanation, and the main entry point entirely lacks JSDoc. Verdict remains NEEDS_REFACTOR due to the UNDOCUMENTED main export.
- `src/commands/clean.ts`: 1 symbol(s) — All findings are documentation-only — no correctness, utility, duplication, or overengineering issues detected across any axis. Reclassified resolveAxisShards from PARTIAL to DOCUMENTED: its existing JSDoc is adequate for a private function with typed, self-descriptive parameters, so action 2 is removed. The remaining findings are legitimate: parseUncheckedActions (exported, missing @param), generatePrd and generateClaudeMd (private but complex enough to warrant brief JSDoc), and registerCleanCommand (exported entry point with no JSDoc at all). The medium-severity action on registerCleanCommand alone justifies NEEDS_REFACTOR verdict.
- `src/commands/rag-status.ts`: 1 symbol(s) — Overall: the only axis with findings is documentation. The two private helpers (`getStoreForMode`, `printStats`) are borderline — `getStoreForMode` is small and self-documenting enough to reclassify as DOCUMENTED, while `printStats` has non-obvious inline parameter semantics that justify keeping UNDOCUMENTED at informational level. The key finding is the exported `registerRagStatusCommand` which genuinely lacks documentation for a complex 147-line public function. Action 1 (add JSDoc to `registerRagStatusCommand`) is valid and retains medium severity. No correction, utility, duplication, or overengineering issues were found. Verdict remains NEEDS_REFACTOR due to the actionable documentation gap on the exported symbol.
- `src/commands/report.ts`: 1 symbol(s) — Overall the file has one valid documentation finding: the exported `registerReportCommand` lacks JSDoc despite having non-trivial behavior hidden inside its Commander action callback. The action (id 1) requesting JSDoc for it is appropriate and retained. The `printReportSummary` finding is reclassified to DOCUMENTED because it is a private helper with a self-descriptive name and straightforward console-output logic that does not warrant mandatory JSDoc. Verdict remains NEEDS_REFACTOR due to the single actionable hygiene finding on the exported function.
- `src/commands/reset.ts`: 1 symbol(s) — File has only documentation-axis findings. countResetItems is reclassified to DOCUMENTED: it has a JSDoc, TypeScript annotations, and self-descriptive names — sufficient for a non-exported helper. Action 1 removed accordingly. registerResetCommand remains UNDOCUMENTED: as a 131-line exported function with complex behavior, it warrants at least a brief JSDoc. No correctness, utility, duplication, or overengineering issues were found. Verdict stays NEEDS_REFACTOR due to the single medium-severity undocumented exported symbol.
- `src/commands/run.ts`: 1 symbol(s) — All findings are documentation-only — no correctness, utility, duplication, or overengineering issues. The one reclassification is formatDuration (UNDOCUMENTED → DOCUMENTED): a 6-line private helper with a perfectly self-descriptive name that falls under the small-helper leniency rule, and the original evaluator only had 60% confidence. All other findings are confirmed: the exported registerRunCommand and several large private orchestration functions (runSetupPhase, runRagPhase, runReviewPhase, runReportPhase, runDocLlmPhase) totalling ~1000+ lines have no JSDoc at all. The PARTIAL findings on functions with existing JSDoc (runDocBootstrap, runDocUpdate, copyCachedReviews, cacheReview) are valid but low-severity — missing @param tags and error-handling documentation. Verdict remains NEEDS_REFACTOR due to the scale of undocumented complexity in critical orchestration functions, particularly the exported entry point.
- `src/core/axes/best-practices.ts`: 1 symbol(s) — All findings are documentation-only. The code has no correctness, utility, duplication, or overengineering issues — it is functionally clean. The documentation gaps are real: five exported symbols lack JSDoc, with detectFileContext, buildBestPracticesUserMessage, and BestPracticesEvaluator being the most impactful gaps due to their complexity. I reclassified buildBestPracticesSystemPrompt from UNDOCUMENTED to PARTIAL because it is a trivial one-liner wrapper whose name is fully self-descriptive. All other findings are confirmed as-is. NEEDS_REFACTOR is the correct verdict given the documentation gaps on public API surfaces, though all remediation is trivial effort.
- `src/core/axes/correction.ts`: 2 symbol(s) — This file has zero correctness, utility, duplication, or overengineering issues — all findings are documentation-only. The code itself is well-structured with a clean two-pass correction/verification architecture. I reclassified two symbols: buildVerificationSystemPrompt (private, 3 lines, reviewer's own detail acknowledged leniency) and summarizePattern (private, 7 lines, under 10-line leniency with existing JSDoc). All other findings are confirmed: the main exported class CorrectionEvaluator and several exported functions lack JSDoc, which is the primary hygiene gap. Verdict remains NEEDS_REFACTOR due to the number of undocumented exported symbols, particularly the central CorrectionEvaluator class.
- `src/core/axes/documentation.ts`: 4 symbol(s) — This file has findings exclusively on the documentation axis. Three private unexported Zod schemas (DocumentationSymbolSchema, DocsCoverageConceptSchema, DocsCoverageSchema) were reclassified from UNDOCUMENTED to DOCUMENTED — they are small, self-descriptive, under a clear section comment, and have no external callers. The trivial one-liner buildDocumentationSystemPrompt was also reclassified to DOCUMENTED since its name and single-line body are fully self-documenting. Three legitimate UNDOCUMENTED findings remain on exported symbols: DocumentationResponseSchema (exported schema without usage context), buildDocumentationUserMessage (complex 67-line function with non-trivial logic), and DocumentationEvaluator (class with multiple non-obvious behaviors). These warrant NEEDS_REFACTOR. Action 2 (JSDoc for buildDocumentationSystemPrompt) removed due to reclassification.
- `src/core/axes/duplication.ts`: 2 symbol(s) — All findings in this file are documentation-only — no correctness, utility, duplication, or overengineering issues. Reclassified buildDuplicationSystemPrompt from PARTIAL to DOCUMENTED (trivial 3-line delegation, name is fully self-descriptive) and removed its action. Kept buildDuplicationUserMessage as UNDOCUMENTED given its substantial complexity. Softened DuplicationEvaluator from UNDOCUMENTED to PARTIAL since the AxisEvaluator interface provides implicit contract documentation. Verdict remains NEEDS_REFACTOR due to the remaining documentation gaps on two exported symbols, though these are hygiene-level concerns.
- `src/core/axes/utility.ts`: 2 symbol(s) — Overall assessment: The file is a clean, conventional axis evaluator implementation following established patterns. The only substantive documentation gap is on buildUtilityUserMessage (48 lines with non-trivial conditional logic). UtilityResponseSchema and UtilityEvaluator were reclassified to DOCUMENTED because they follow rigid, well-established codebase conventions where the schema name + section comment (for the schema) and the interface contract + pattern (for the class) provide sufficient documentation. With only one low-severity trivial-effort documentation suggestion remaining, the verdict is downgraded from NEEDS_REFACTOR to CLEAN — a single optional JSDoc does not constitute a refactoring need.
- `src/core/axis-evaluator.ts`: 4 symbol(s) — This file is well-structured, correctly typed, and has no correctness, utility, duplication, or overengineering issues. All findings are documentation-only. I reclassified 4 symbols: RelevantDoc and ExecQueryParams from PARTIAL → DOCUMENTED (self-descriptive fields with key semantics already documented inline, consistent with other similarly-rated types in this review), and tryValidate and formatMessage from UNDOCUMENTED → DOCUMENTED (small private helpers with self-descriptive names and clear signatures, well within private helper leniency). The remaining PARTIAL findings on exported interfaces (AxisEvaluator, AxisContext, AxisResult, AxisSymbolResult, SingleTurnQueryParams, PreResolvedRagEntry, composeAxisSystemPrompt) are legitimate low-severity hygiene items. The two UNDOCUMENTED findings on _runSingleTurnQueryInner and execQuery are warranted given their substantial size and side effects. Verdict stays NEEDS_REFACTOR due to the volume of documentation gaps on key exported contracts, though all are low-severity.
- `src/core/axis-merger.ts`: 4 symbol(s) — Overall: the file has genuine documentation drift in applyCoherenceRules and synthesizeActionsFromSymbols where JSDoc enumerations are stale (missing rules/action types actually implemented). mergeAxisResults PARTIAL is fair for the sole exported function. I reclassified 4 small private helpers (buildAxisMap, findAxisValue, mergeActions, mergeFileLevels) from UNDOCUMENTED to DOCUMENTED — the evaluator hedged on all of them at 60-65 confidence and acknowledged leniency applies. mergeSymbol, computeVerdict, and buildAxisMeta remain UNDOCUMENTED as they contain genuinely non-obvious logic. The 3 PARTIAL symbols (mergeAxisResults, applyCoherenceRules, synthesizeActionsFromSymbols) all have confidence above the verdict threshold, making partialDocCount=3 which triggers NEEDS_REFACTOR. All 3 actions remain valid as they target the PARTIAL symbols. Verdict stays NEEDS_REFACTOR.
- `src/core/calibration.ts`: 2 symbol(s) — Overall the file is well-structured and well-documented. Two UNDOCUMENTED findings on private trivial helpers (calibrationPath, RunMetrics) were reclassified to DOCUMENTED under the private-helper leniency rule—both are self-descriptive and scoped to internal use. The sole remaining finding is PARTIAL documentation on estimateCalibratedMinutes, where a valid JSDoc block exists but is misplaced above constant declarations rather than attached to the function, and the complex parameter list lacks @param tags. This is a low-severity hygiene issue (Action 1 retained) that does not warrant escalating the verdict beyond CLEAN.
- `src/core/doc-gap-detection.ts`: 2 symbol(s) — Overall: File has no correctness, utility, duplication, or overengineering issues. All findings are documentation-related. The two UNDOCUMENTED findings on detectDocGapsV2 and formatGapReportV2 (the main public API) are the most significant and justify NEEDS_REFACTOR. Multiple PARTIAL findings on private strategy functions and exported types are valid but low severity. Reclassified GapClassification and formatGapSummary from PARTIAL to DOCUMENTED — both are deprecated symbols where @deprecated JSDoc with a migration pointer is sufficient. Removed corresponding actions 11 and 14. The remaining 12 actions are valid documentation hygiene suggestions, with actions 9 and 10 (medium severity) being the most impactful.
- `src/core/doc-generator.ts`: 1 symbol(s) — The file's primary documentation issue is the misplaced JSDoc: the comment intended for buildPagePrompt (the main exported entry point) was placed above the DocNeighbor interface. This creates two problems simultaneously — DocNeighbor has misleading documentation and buildPagePrompt has none. Action 2 (add JSDoc to buildPagePrompt) is the highest-value fix. I reclassified buildUserMessage from UNDOCUMENTED to PARTIAL because the evaluator overlooked the section banner on line 75 and the extensive inline comments throughout the function body that document each section's purpose and truncation behavior. All three actions remain valid. The verdict stays NEEDS_REFACTOR — the misplaced JSDoc on the main entry point is a real hygiene issue worth addressing, but there are no correctness, utility, or structural problems in this file.
- `src/core/doc-llm-executor.ts`: 1 symbol(s) — Overall: The file is well-structured with clear naming conventions and self-descriptive types throughout. The only actionable findings are two PARTIAL documentation gaps on executeOnePage (private, ~35-line helper with undocumented side effects) and reviewDocStructure (public, 210-line function whose JSDoc understates its scope). DocPrompt was reclassified from PARTIAL to DOCUMENTED because its @deprecated migration notice is the correct and complete documentation for a deprecated type alias — action 1 removed accordingly. No corrections, no dead code, no duplication issues. Verdict remains NEEDS_REFACTOR due to the two remaining low-severity documentation actions.
- `src/core/doc-scoring.ts`: 2 symbol(s) — Overall assessment: This is a well-structured scoring module with comprehensive module-level JSDoc, well-documented types, and clear section banners. Of the four PARTIAL documentation findings, two are reclassified to DOCUMENTED: scoreDocumentation is effectively fully documented by the module JSDoc + typed interfaces, and computeNavigation is a small self-documenting private helper. Two PARTIAL findings are retained: computeStructural (borderline at 17 lines) and computeWeights (genuinely complex at 37 lines with non-obvious scaling logic). With only two low-severity trivial-effort documentation suggestions remaining on private helpers, the file is effectively CLEAN. Actions 1 and 3 are removed corresponding to the reclassified symbols.
- `src/core/estimator.ts`: 1 symbol(s) — Overall the file is well-written with thorough documentation throughout. Only two symbols had findings (SONNET_AXES PARTIAL, estimateTasksTokens PARTIAL). SONNET_AXES is reclassified to DOCUMENTED because the preceding JSDoc explicitly describes both the haiku and sonnet constant pair, and the inline comment completes the picture — removing action 1. estimateTasksTokens retains PARTIAL as the brief JSDoc undersells a non-trivial 42-line function with fallback logic and a multi-constant token formula. This single low-severity documentation action does not affect the CLEAN verdict.
- `src/core/language-adapters.ts`: 5 symbol(s) — Overall assessment: This is a well-structured file with a clear adapter pattern. All findings are documentation-only — no correctness, utility, duplication, or overengineering issues. I reclassified 5 adapter classes/functions from PARTIAL to DOCUMENTED where the class names, interface implementation, and language-idiomatic behavior make JSDoc purely ceremonial (TypeScriptAdapter, BashAdapter, RustAdapter, GoAdapter, resolveAdapter). I kept PARTIAL for symbols with genuinely non-obvious behavior: PythonAdapter (__all__ two-pass), JavaAdapter (member extraction beyond interface contract), CSharpAdapter (namespace recursion), SqlAdapter/YamlAdapter/JsonAdapter (extractSymbols-returns-[] pattern), ADAPTER_REGISTRY (key format), and heuristicParse (incomplete JSDoc). The extractDeclaration UNDOCUMENTED finding is valid for a 54-line private function. Verdict remains NEEDS_REFACTOR due to multiple meaningful documentation gaps on public API symbols, though all are low-severity hygiene items.
- `src/core/prompt-resolver.ts`: 1 symbol(s) — Overall assessment: This file is well-structured with clear naming conventions and a solid block comment above the registry constant. The only findings are documentation-related: registerDefaults lacks a JSDoc comment (low-impact given its repetitive, self-descriptive body), and resolveSystemPrompt has partial JSDoc missing @param/@throws annotations. The `register` helper was reclassified to DOCUMENTED under the small-private-helper leniency rule — a 3-line setter with a clear name and typed parameters needs no JSDoc. No correctness, utility, duplication, or overengineering issues were found. Action 1 (complete JSDoc for resolveSystemPrompt) remains valid as a low-severity hygiene improvement. Verdict stays CLEAN.
- `src/core/reporter.ts`: 4 symbol(s) — This file has findings exclusively on the documentation axis — no correctness, utility, duplication, or overengineering issues. The NEEDS_REFACTOR verdict is maintained due to 3 UNDOCUMENTED private functions exceeding 90 lines each (renderAxisMethodology, renderAxisVerdictDistribution, renderAxisSummary) and multiple PARTIAL findings on complex exported functions. However, 4 symbols were reclassified PARTIAL → DOCUMENTED: computeGlobalVerdict (7 lines), makeActId (4 lines), toReportAxisId (3 lines), and toAxisId (3 lines). These are tiny functions where the existing prose JSDoc combined with fully self-explanatory TypeScript signatures provides complete documentation — adding @param/@returns would be pure boilerplate with no information gain. The corresponding 4 actions (4, 6, 9, 10) are removed. The remaining 12 actions for documentation hygiene on larger/more complex symbols are valid low-severity improvements.
- `src/core/review-writer.ts`: 1 symbol(s) — Only two symbols had findings, both on the documentation axis. writeTranscript's PARTIAL is justified — it's an exported function missing parameter documentation. VALUE_TO_AXIS's PARTIAL is reclassified to DOCUMENTED because it is a private constant with a self-descriptive name and an immediately readable mapping structure; requiring an inline comment for this adds no meaningful value. Action 2 removed accordingly. Overall the file is clean with at most a trivial hygiene suggestion remaining.
- `src/prompts/__gold-set__/false-duplicate.ts`: 1 symbol(s) — This file is a gold-set fixture (`__gold-set__/false-duplicate.ts`) explicitly designed to validate the duplication axis evaluator against known-correct cases. Both functions are intentionally structurally similar but semantically distinct. The only finding was PARTIAL documentation on calculateMean for not documenting the empty-array edge case, which is an excessive standard for a short test fixture function where the behavior is immediately visible in the source. Reclassifying to DOCUMENTED and removing the associated action. Verdict remains CLEAN.
- `src/prompts/__gold-set__/generated-protobuf.ts`: 3 symbol(s) — The file contains a clear `@generated by protoc-gen-ts v2.1.0 — DO NOT EDIT` marker on line 5, making it auto-generated code. The merged review incorrectly set is_generated: false but partially acknowledged the generated nature in individual findings. All three PARTIAL documentation findings suggested adding JSDoc to a file explicitly marked DO NOT EDIT — this is contradictory and counterproductive. Generated protobuf code follows its own documentation conventions (self-descriptive names, mirrored interface/factory patterns) and should not be hand-edited. With proper generated-code leniency applied, all symbols are clean, and all three actions (which suggest editing generated code) are removed. Verdict reclassified from NEEDS_REFACTOR to CLEAN.
- `src/prompts/__gold-set__/mixed-lang-sql.ts`: 3 symbol(s) — This file contains a well-structured SQLite data access layer with clear naming conventions and strong TypeScript typing. All findings are documentation-only — no correctness, utility, duplication, or overengineering issues. I reclassified getUserById to DOCUMENTED because its TypeScript signature (`User | undefined`) already fully captures the contract the evaluator flagged. getActiveUsers and insertUser were softened to PARTIAL since their names and types do most of the documentation work, with only minor details (ordering, return semantics) uncaptured. The remaining three functions — searchUsers, deactivateUser, and deleteInactiveUsers — have genuinely non-obvious behavior that warrants JSDoc. The verdict remains NEEDS_REFACTOR due to the legitimate documentation gaps in these exported public functions.
- `src/prompts/__gold-set__/terrible-1.ts`: 6 symbol(s) — This file is located in `src/prompts/__gold-set__/` and has an explicit header comment (line 6): 'Gold-set fixture: intentionally terrible code for best-practices score ≤ 3.0'. It also carries `@ts-nocheck`. Every symbol in the file is deliberately written as a negative example for testing/benchmarking purposes. The documentation evaluator correctly identified the absence of JSDoc but failed to account for the file's explicit purpose as a test fixture. Adding JSDoc to intentionally terrible code would undermine its role as a gold-set negative example. The file-level comment provides sufficient documentation context for all symbols. All 6 documentation actions are false positives and should be removed. No other axes flagged findings. Verdict reclassified from NEEDS_REFACTOR to CLEAN.
- `src/rag/vector-store.ts`: 1 symbol(s) — Overall: Two legitimate PARTIAL documentation findings remain (UpsertOptions missing docEmbeddings mention, VectorStore missing class-level JSDoc and lifecycle docs). The rowToCard PARTIAL finding was reclassified to DOCUMENTED — the original evaluator had low confidence (68), the cited 'bigint-to-number coercion' concern was factually inaccurate (it's a simple type guard with default), and private helper leniency applies to a descriptively-named mapping function. The codebase is well-documented overall with thorough JSDoc on public methods, security-sensitive functions, and non-obvious private helpers. Verdict remains NEEDS_REFACTOR for the two low-severity documentation gaps on exported symbols.
- `src/schemas/config.ts`: 1 symbol(s) — This file is a well-structured Zod configuration schema with 14 exported symbols. The only findings are documentation-related PARTIAL assessments — no correctness, utility, duplication, or overengineering issues. After deliberation: LlmConfigSchema legitimately needs JSDoc for its non-obvious concurrency split, agentic_tools toggle, and deliberation settings. RagConfigSchema was reclassified to DOCUMENTED because three of its four non-trivial fields already have inline JSDoc — removing action 2. BadgeConfigSchema and DocumentationConfigSchema retain their PARTIAL findings for ambiguous fields (verdict, module_mapping). All remaining actions are low-severity hygiene improvements. Verdict stays NEEDS_REFACTOR due to three legitimate PARTIAL documentation gaps, though the file is otherwise clean.
- `src/utils/banner.ts`: 1 symbol(s) — Overall: one of the two documentation findings was a false positive. MOTD_LINES is a generated, self-descriptive ASCII-art constant where the name and content make the purpose immediately clear; requiring JSDoc on generated constants is excessive, especially given the file already has the generated-block marker pointing callers to the generating script. The printBanner finding stands: the altMotd replacement mechanism (targeting double-quoted substrings on a specific line) is non-obvious and merits a brief JSDoc. With one remaining medium-severity documentation action, the verdict stays NEEDS_REFACTOR.
- `src/utils/format.ts`: 2 symbol(s) — All findings are documentation-only. Reclassified buildProgressBar and verdictColor from PARTIAL to DOCUMENTED — both are short, simple functions where TypeScript types and descriptive parameter names provide sufficient documentation alongside the existing brief JSDoc. Retained PARTIAL for formatTokenSummary (non-obvious cache hit rate formula) and UNDOCUMENTED for countReviewFindings (complex logic with no JSDoc at all). Removed actions 1 and 2 corresponding to the reclassified symbols. Verdict remains NEEDS_REFACTOR due to the genuinely undocumented countReviewFindings.
- `src/utils/lock.ts`: 1 symbol(s) — Overall the file is clean and well-structured. The only substantive documentation gap is the missing @returns on acquireLock (action 1 retained). The releaseLock PARTIAL finding is reclassified to DOCUMENTED because the function is trivially self-documenting from its name, parameter name, and type signature — action 2 removed. All other symbols (LockData, isLockActive) had no findings and were correctly assessed as clean. Verdict remains CLEAN: only one low-severity hygiene suggestion remains.
- `src/utils/log-context.ts`: 1 symbol(s) — Two documentation findings were deliberated. The `als` PARTIAL was reclassified to DOCUMENTED: the section banner and self-documenting initializer provide sufficient context for a private constant, and the original evaluator's low confidence (65) reflected genuine uncertainty. Action 1 (add JSDoc to als) is removed accordingly. The `contextLogger` PARTIAL is confirmed: the namespace→component mapping is genuinely non-obvious and undocumented. With only one remaining low-severity trivial documentation suggestion, the file verdict stays CLEAN.

---

## Methodology

Each file is evaluated through 7 independent axis evaluators running in parallel.
Every symbol (function, class, variable, type) is analysed individually and receives a rating per axis along with a confidence score (0–100).
Findings with confidence < 30 are discarded; those with confidence < 60 are excluded from verdict computation.

| Axis | Model | Ratings | Description |
|------|-------|---------|-------------|
| Utility | haiku | USED / DEAD / LOW_VALUE | Is this symbol actually used in the codebase? |
| Duplication | haiku | UNIQUE / DUPLICATE | Is this symbol a copy of logic that exists elsewhere? |
| Correction | sonnet | OK / NEEDS_FIX / ERROR | Does this symbol contain bugs or correctness issues? |
| Overengineering | haiku | LEAN / OVER / ACCEPTABLE | Is the implementation unnecessarily complex? |
| Tests | haiku | GOOD / WEAK / NONE | Does this symbol have adequate test coverage? |
| Best Practices | sonnet | Score 0–10 (17 rules) | Does the file follow TypeScript best practices? |
| Documentation | haiku | DOCUMENTED / PARTIAL / UNDOCUMENTED | Are exported symbols properly documented with JSDoc? |

See each axis folder for detailed rating criteria and methodology.

### Severity Classification

- **High**: ERROR corrections, or NEEDS_FIX / DEAD / DUPLICATE with confidence >= 80%.
- **Medium**: NEEDS_FIX / DEAD / DUPLICATE with confidence < 80%, or OVER (any confidence).
- **Low**: LOW_VALUE utility or remaining minor findings.

### Verdict Rules

- **CLEAN**: No actionable findings with confidence >= 60%.
- **NEEDS_REFACTOR**: At least one confirmed finding (DEAD, DUPLICATE, OVER, or NEEDS_FIX) with confidence >= 60%.
- **CRITICAL**: At least one ERROR correction found.

### Inter-axis Coherence

After individual evaluation, coherence rules reconcile contradictions:

- If utility = DEAD, tests is forced to NONE (no point testing dead code).
- If utility = DEAD, documentation is forced to UNDOCUMENTED (no point documenting dead code).
- If correction = ERROR, overengineering is forced to ACCEPTABLE (complexity is secondary to correctness).

*Generated: 2026-03-25T13:30:12.511Z*
